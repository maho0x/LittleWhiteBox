/* eslint-disable */
var iL = Object.create, uI = Object.defineProperty, aL = Object.getOwnPropertyDescriptor, lL = Object.getOwnPropertyNames, uL = Object.getPrototypeOf, cL = Object.prototype.hasOwnProperty, Xc = (e, t) => () => (t || e((t = { exports: {} }).exports, t), t.exports), dL = (e, t, n, r) => {
  if (t && typeof t == "object" || typeof t == "function")
    for (var s = lL(t), o = 0, i = s.length, a; o < i; o++)
      a = s[o], !cL.call(e, a) && a !== n && uI(e, a, {
        get: ((l) => t[l]).bind(null, a),
        enumerable: !(r = aL(t, a)) || r.enumerable
      });
  return e;
}, cI = (e, t, n) => (n = e != null ? iL(uL(e)) : {}, dL(t || !e || !e.__esModule ? uI(n, "default", {
  value: e,
  enumerable: !0
}) : n, e));
// @__NO_SIDE_EFFECTS__
function Cp(e) {
  const t = /* @__PURE__ */ Object.create(null);
  for (const n of e.split(",")) t[n] = 1;
  return (n) => n in t;
}
var Wt = {}, nl = [], Rs = () => {
}, dI = () => !1, Ap = (e) => e.charCodeAt(0) === 111 && e.charCodeAt(1) === 110 && (e.charCodeAt(2) > 122 || e.charCodeAt(2) < 97), Ip = (e) => e.startsWith("onUpdate:"), fn = Object.assign, q_ = (e, t) => {
  const n = e.indexOf(t);
  n > -1 && e.splice(n, 1);
}, fL = Object.prototype.hasOwnProperty, Ot = (e, t) => fL.call(e, t), gt = Array.isArray, rl = (e) => Qc(e) === "[object Map]", xp = (e) => Qc(e) === "[object Set]", jS = (e) => Qc(e) === "[object Date]", St = (e) => typeof e == "function", tn = (e) => typeof e == "string", Ns = (e) => typeof e == "symbol", Gt = (e) => e !== null && typeof e == "object", fI = (e) => (Gt(e) || St(e)) && St(e.then) && St(e.catch), hI = Object.prototype.toString, Qc = (e) => hI.call(e), hL = (e) => Qc(e).slice(8, -1), pI = (e) => Qc(e) === "[object Object]", H_ = (e) => tn(e) && e !== "NaN" && e[0] !== "-" && "" + parseInt(e, 10) === e, sc = /* @__PURE__ */ Cp(",key,ref,ref_for,ref_key,onVnodeBeforeMount,onVnodeMounted,onVnodeBeforeUpdate,onVnodeUpdated,onVnodeBeforeUnmount,onVnodeUnmounted"), kp = (e) => {
  const t = /* @__PURE__ */ Object.create(null);
  return ((n) => t[n] || (t[n] = e(n)));
}, pL = /-\w/g, os = kp((e) => e.replace(pL, (t) => t.slice(1).toUpperCase())), mL = /\B([A-Z])/g, si = kp((e) => e.replace(mL, "-$1").toLowerCase()), mI = kp((e) => e.charAt(0).toUpperCase() + e.slice(1)), yg = kp((e) => e ? `on${mI(e)}` : ""), xs = (e, t) => !Object.is(e, t), Gf = (e, ...t) => {
  for (let n = 0; n < e.length; n++) e[n](...t);
}, gI = (e, t, n, r = !1) => {
  Object.defineProperty(e, t, {
    configurable: !0,
    enumerable: !1,
    writable: r,
    value: n
  });
}, Mp = (e) => {
  const t = parseFloat(e);
  return isNaN(t) ? e : t;
}, e0, Pp = () => e0 || (e0 = typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : typeof window < "u" ? window : typeof globalThis < "u" ? globalThis : {});
function Ir(e) {
  if (gt(e)) {
    const t = {};
    for (let n = 0; n < e.length; n++) {
      const r = e[n], s = tn(r) ? _L(r) : Ir(r);
      if (s) for (const o in s) t[o] = s[o];
    }
    return t;
  } else if (tn(e) || Gt(e)) return e;
}
var gL = /;(?![^(]*\))/g, vL = /:([^]+)/, yL = /\/\*[^]*?\*\//g;
function _L(e) {
  const t = {};
  return e.replace(yL, "").split(gL).forEach((n) => {
    if (n) {
      const r = n.split(vL);
      r.length > 1 && (t[r[0].trim()] = r[1].trim());
    }
  }), t;
}
function Fe(e) {
  let t = "";
  if (tn(e)) t = e;
  else if (gt(e)) for (let n = 0; n < e.length; n++) {
    const r = Fe(e[n]);
    r && (t += r + " ");
  }
  else if (Gt(e))
    for (const n in e) e[n] && (t += n + " ");
  return t.trim();
}
var vI = "itemscope,allowfullscreen,formnovalidate,ismap,nomodule,novalidate,readonly", bL = /* @__PURE__ */ Cp(vI), jie = /* @__PURE__ */ Cp(vI + ",async,autofocus,autoplay,controls,default,defer,disabled,hidden,inert,loop,open,required,reversed,scoped,seamless,checked,muted,multiple,selected");
function yI(e) {
  return !!e || e === "";
}
function wL(e, t) {
  if (e.length !== t.length) return !1;
  let n = !0;
  for (let r = 0; n && r < e.length; r++) n = Zc(e[r], t[r]);
  return n;
}
function Zc(e, t) {
  if (e === t) return !0;
  let n = jS(e), r = jS(t);
  if (n || r) return n && r ? e.getTime() === t.getTime() : !1;
  if (n = Ns(e), r = Ns(t), n || r) return e === t;
  if (n = gt(e), r = gt(t), n || r) return n && r ? wL(e, t) : !1;
  if (n = Gt(e), r = Gt(t), n || r) {
    if (!n || !r || Object.keys(e).length !== Object.keys(t).length) return !1;
    for (const s in e) {
      const o = e.hasOwnProperty(s), i = t.hasOwnProperty(s);
      if (o && !i || !o && i || !Zc(e[s], t[s])) return !1;
    }
  }
  return String(e) === String(t);
}
function SL(e, t) {
  return e.findIndex((n) => Zc(n, t));
}
var _I = (e) => !!(e && e.__v_isRef === !0), Q = (e) => tn(e) ? e : e == null ? "" : gt(e) || Gt(e) && (e.toString === hI || !St(e.toString)) ? _I(e) ? Q(e.value) : JSON.stringify(e, bI, 2) : String(e), bI = (e, t) => _I(t) ? bI(e, t.value) : rl(t) ? { [`Map(${t.size})`]: [...t.entries()].reduce((n, [r, s], o) => (n[_g(r, o) + " =>"] = s, n), {}) } : xp(t) ? { [`Set(${t.size})`]: [...t.values()].map((n) => _g(n)) } : Ns(t) ? _g(t) : Gt(t) && !gt(t) && !pI(t) ? String(t) : t, _g = (e, t = "") => {
  var n;
  return Ns(e) ? `Symbol(${(n = e.description) != null ? n : t})` : e;
}, En, TL = class {
  constructor(e = !1) {
    this.detached = e, this._active = !0, this._on = 0, this.effects = [], this.cleanups = [], this._isPaused = !1, this._warnOnRun = !0, this.__v_skip = !0, !e && En && (En.active ? (this.parent = En, this.index = (En.scopes || (En.scopes = [])).push(this) - 1) : (this._active = !1, this._warnOnRun = !1));
  }
  get active() {
    return this._active;
  }
  pause() {
    if (this._active) {
      this._isPaused = !0;
      let e, t;
      if (this.scopes) for (e = 0, t = this.scopes.length; e < t; e++) this.scopes[e].pause();
      for (e = 0, t = this.effects.length; e < t; e++) this.effects[e].pause();
    }
  }
  resume() {
    if (this._active && this._isPaused) {
      this._isPaused = !1;
      let e, t;
      if (this.scopes) for (e = 0, t = this.scopes.length; e < t; e++) this.scopes[e].resume();
      for (e = 0, t = this.effects.length; e < t; e++) this.effects[e].resume();
    }
  }
  run(e) {
    if (this._active) {
      const t = En;
      try {
        return En = this, e();
      } finally {
        En = t;
      }
    }
  }
  on() {
    ++this._on === 1 && (this.prevScope = En, En = this);
  }
  off() {
    if (this._on > 0 && --this._on === 0) {
      if (En === this) En = this.prevScope;
      else {
        let e = En;
        for (; e; ) {
          if (e.prevScope === this) {
            e.prevScope = this.prevScope;
            break;
          }
          e = e.prevScope;
        }
      }
      this.prevScope = void 0;
    }
  }
  stop(e) {
    if (this._active) {
      this._active = !1;
      let t, n;
      for (t = 0, n = this.effects.length; t < n; t++) this.effects[t].stop();
      for (this.effects.length = 0, t = 0, n = this.cleanups.length; t < n; t++) this.cleanups[t]();
      if (this.cleanups.length = 0, this.scopes) {
        for (t = 0, n = this.scopes.length; t < n; t++) this.scopes[t].stop(!0);
        this.scopes.length = 0;
      }
      if (!this.detached && this.parent && !e) {
        const r = this.parent.scopes.pop();
        r && r !== this && (this.parent.scopes[this.index] = r, r.index = this.index);
      }
      this.parent = void 0;
    }
  }
};
function EL() {
  return En;
}
var Kt, bg = /* @__PURE__ */ new WeakSet(), wI = class {
  constructor(e) {
    this.fn = e, this.deps = void 0, this.depsTail = void 0, this.flags = 5, this.next = void 0, this.cleanup = void 0, this.scheduler = void 0, En && (En.active ? En.effects.push(this) : this.flags &= -2);
  }
  pause() {
    this.flags |= 64;
  }
  resume() {
    this.flags & 64 && (this.flags &= -65, bg.has(this) && (bg.delete(this), this.trigger()));
  }
  notify() {
    this.flags & 2 && !(this.flags & 32) || this.flags & 8 || TI(this);
  }
  run() {
    if (!(this.flags & 1)) return this.fn();
    this.flags |= 2, t0(this), EI(this);
    const e = Kt, t = is;
    Kt = this, is = !0;
    try {
      return this.fn();
    } finally {
      CI(this), Kt = e, is = t, this.flags &= -3;
    }
  }
  stop() {
    if (this.flags & 1) {
      for (let e = this.deps; e; e = e.nextDep) V_(e);
      this.deps = this.depsTail = void 0, t0(this), this.onStop && this.onStop(), this.flags &= -2;
    }
  }
  trigger() {
    this.flags & 64 ? bg.add(this) : this.scheduler ? this.scheduler() : this.runIfDirty();
  }
  runIfDirty() {
    Nv(this) && this.run();
  }
  get dirty() {
    return Nv(this);
  }
}, SI = 0, oc, ic;
function TI(e, t = !1) {
  if (e.flags |= 8, t) {
    e.next = ic, ic = e;
    return;
  }
  e.next = oc, oc = e;
}
function G_() {
  SI++;
}
function W_() {
  if (--SI > 0) return;
  if (ic) {
    let t = ic;
    for (ic = void 0; t; ) {
      const n = t.next;
      t.next = void 0, t.flags &= -9, t = n;
    }
  }
  let e;
  for (; oc; ) {
    let t = oc;
    for (oc = void 0; t; ) {
      const n = t.next;
      if (t.next = void 0, t.flags &= -9, t.flags & 1) try {
        t.trigger();
      } catch (r) {
        e || (e = r);
      }
      t = n;
    }
  }
  if (e) throw e;
}
function EI(e) {
  for (let t = e.deps; t; t = t.nextDep)
    t.version = -1, t.prevActiveLink = t.dep.activeLink, t.dep.activeLink = t;
}
function CI(e) {
  let t, n = e.depsTail, r = n;
  for (; r; ) {
    const s = r.prevDep;
    r.version === -1 ? (r === n && (n = s), V_(r), CL(r)) : t = r, r.dep.activeLink = r.prevActiveLink, r.prevActiveLink = void 0, r = s;
  }
  e.deps = t, e.depsTail = n;
}
function Nv(e) {
  for (let t = e.deps; t; t = t.nextDep) if (t.dep.version !== t.version || t.dep.computed && (AI(t.dep.computed) || t.dep.version !== t.version)) return !0;
  return !!e._dirty;
}
function AI(e) {
  if (e.flags & 4 && !(e.flags & 16) || (e.flags &= -17, e.globalVersion === Cc) || (e.globalVersion = Cc, !e.isSSR && e.flags & 128 && (!e.deps && !e._dirty || !Nv(e)))) return;
  e.flags |= 2;
  const t = e.dep, n = Kt, r = is;
  Kt = e, is = !0;
  try {
    EI(e);
    const s = e.fn(e._value);
    (t.version === 0 || xs(s, e._value)) && (e.flags |= 128, e._value = s, t.version++);
  } catch (s) {
    throw t.version++, s;
  } finally {
    Kt = n, is = r, CI(e), e.flags &= -3;
  }
}
function V_(e, t = !1) {
  const { dep: n, prevSub: r, nextSub: s } = e;
  if (r && (r.nextSub = s, e.prevSub = void 0), s && (s.prevSub = r, e.nextSub = void 0), n.subs === e && (n.subs = r, !r && n.computed)) {
    n.computed.flags &= -5;
    for (let o = n.computed.deps; o; o = o.nextDep) V_(o, !0);
  }
  !t && !--n.sc && n.map && n.map.delete(n.key);
}
function CL(e) {
  const { prevDep: t, nextDep: n } = e;
  t && (t.nextDep = n, e.prevDep = void 0), n && (n.prevDep = t, e.nextDep = void 0);
}
var is = !0, II = [];
function uo() {
  II.push(is), is = !1;
}
function co() {
  const e = II.pop();
  is = e === void 0 ? !0 : e;
}
function t0(e) {
  const { cleanup: t } = e;
  if (e.cleanup = void 0, t) {
    const n = Kt;
    Kt = void 0;
    try {
      t();
    } finally {
      Kt = n;
    }
  }
}
var Cc = 0, AL = class {
  constructor(e, t) {
    this.sub = e, this.dep = t, this.version = t.version, this.nextDep = this.prevDep = this.nextSub = this.prevSub = this.prevActiveLink = void 0;
  }
}, K_ = class {
  constructor(e) {
    this.computed = e, this.version = 0, this.activeLink = void 0, this.subs = void 0, this.map = void 0, this.key = void 0, this.sc = 0, this.__v_skip = !0;
  }
  track(e) {
    if (!Kt || !is || Kt === this.computed) return;
    let t = this.activeLink;
    if (t === void 0 || t.sub !== Kt)
      t = this.activeLink = new AL(Kt, this), Kt.deps ? (t.prevDep = Kt.depsTail, Kt.depsTail.nextDep = t, Kt.depsTail = t) : Kt.deps = Kt.depsTail = t, xI(t);
    else if (t.version === -1 && (t.version = this.version, t.nextDep)) {
      const n = t.nextDep;
      n.prevDep = t.prevDep, t.prevDep && (t.prevDep.nextDep = n), t.prevDep = Kt.depsTail, t.nextDep = void 0, Kt.depsTail.nextDep = t, Kt.depsTail = t, Kt.deps === t && (Kt.deps = n);
    }
    return t;
  }
  trigger(e) {
    this.version++, Cc++, this.notify(e);
  }
  notify(e) {
    G_();
    try {
      for (let t = this.subs; t; t = t.prevSub) t.sub.notify() && t.sub.dep.notify();
    } finally {
      W_();
    }
  }
};
function xI(e) {
  if (e.dep.sc++, e.sub.flags & 4) {
    const t = e.dep.computed;
    if (t && !e.dep.subs) {
      t.flags |= 20;
      for (let r = t.deps; r; r = r.nextDep) xI(r);
    }
    const n = e.dep.subs;
    n !== e && (e.prevSub = n, n && (n.nextSub = e)), e.dep.subs = e;
  }
}
var $v = /* @__PURE__ */ new WeakMap(), zi = /* @__PURE__ */ Symbol(""), Dv = /* @__PURE__ */ Symbol(""), Ac = /* @__PURE__ */ Symbol("");
function $n(e, t, n) {
  if (is && Kt) {
    let r = $v.get(e);
    r || $v.set(e, r = /* @__PURE__ */ new Map());
    let s = r.get(n);
    s || (r.set(n, s = new K_()), s.map = r, s.key = n), s.track();
  }
}
function to(e, t, n, r, s, o) {
  const i = $v.get(e);
  if (!i) {
    Cc++;
    return;
  }
  const a = (l) => {
    l && l.trigger();
  };
  if (G_(), t === "clear") i.forEach(a);
  else {
    const l = gt(e), u = l && H_(n);
    if (l && n === "length") {
      const d = Number(r);
      i.forEach((h, p) => {
        (p === "length" || p === Ac || !Ns(p) && p >= d) && a(h);
      });
    } else
      switch ((n !== void 0 || i.has(void 0)) && a(i.get(n)), u && a(i.get(Ac)), t) {
        case "add":
          l ? u && a(i.get("length")) : (a(i.get(zi)), rl(e) && a(i.get(Dv)));
          break;
        case "delete":
          l || (a(i.get(zi)), rl(e) && a(i.get(Dv)));
          break;
        case "set":
          rl(e) && a(i.get(zi));
          break;
      }
  }
  W_();
}
function Ca(e) {
  const t = /* @__PURE__ */ Lt(e);
  return t === e ? t : ($n(t, "iterate", Ac), /* @__PURE__ */ zr(e) ? t : t.map(cs));
}
function Rp(e) {
  return $n(e = /* @__PURE__ */ Lt(e), "iterate", Ac), e;
}
function Cs(e, t) {
  return /* @__PURE__ */ fo(e) ? gl(/* @__PURE__ */ Ji(e) ? cs(t) : t) : cs(t);
}
var IL = {
  __proto__: null,
  [Symbol.iterator]() {
    return wg(this, Symbol.iterator, (e) => Cs(this, e));
  },
  concat(...e) {
    return Ca(this).concat(...e.map((t) => gt(t) ? Ca(t) : t));
  },
  entries() {
    return wg(this, "entries", (e) => (e[1] = Cs(this, e[1]), e));
  },
  every(e, t) {
    return Hs(this, "every", e, t, void 0, arguments);
  },
  filter(e, t) {
    return Hs(this, "filter", e, t, (n) => n.map((r) => Cs(this, r)), arguments);
  },
  find(e, t) {
    return Hs(this, "find", e, t, (n) => Cs(this, n), arguments);
  },
  findIndex(e, t) {
    return Hs(this, "findIndex", e, t, void 0, arguments);
  },
  findLast(e, t) {
    return Hs(this, "findLast", e, t, (n) => Cs(this, n), arguments);
  },
  findLastIndex(e, t) {
    return Hs(this, "findLastIndex", e, t, void 0, arguments);
  },
  forEach(e, t) {
    return Hs(this, "forEach", e, t, void 0, arguments);
  },
  includes(...e) {
    return Sg(this, "includes", e);
  },
  indexOf(...e) {
    return Sg(this, "indexOf", e);
  },
  join(e) {
    return Ca(this).join(e);
  },
  lastIndexOf(...e) {
    return Sg(this, "lastIndexOf", e);
  },
  map(e, t) {
    return Hs(this, "map", e, t, void 0, arguments);
  },
  pop() {
    return uu(this, "pop");
  },
  push(...e) {
    return uu(this, "push", e);
  },
  reduce(e, ...t) {
    return n0(this, "reduce", e, t);
  },
  reduceRight(e, ...t) {
    return n0(this, "reduceRight", e, t);
  },
  shift() {
    return uu(this, "shift");
  },
  some(e, t) {
    return Hs(this, "some", e, t, void 0, arguments);
  },
  splice(...e) {
    return uu(this, "splice", e);
  },
  toReversed() {
    return Ca(this).toReversed();
  },
  toSorted(e) {
    return Ca(this).toSorted(e);
  },
  toSpliced(...e) {
    return Ca(this).toSpliced(...e);
  },
  unshift(...e) {
    return uu(this, "unshift", e);
  },
  values() {
    return wg(this, "values", (e) => Cs(this, e));
  }
};
function wg(e, t, n) {
  const r = Rp(e), s = r[t]();
  return r !== e && !/* @__PURE__ */ zr(e) && (s._next = s.next, s.next = () => {
    const o = s._next();
    return o.done || (o.value = n(o.value)), o;
  }), s;
}
var xL = Array.prototype;
function Hs(e, t, n, r, s, o) {
  const i = Rp(e), a = i !== e && !/* @__PURE__ */ zr(e), l = i[t];
  if (l !== xL[t]) {
    const h = l.apply(e, o);
    return a ? cs(h) : h;
  }
  let u = n;
  i !== e && (a ? u = function(h, p) {
    return n.call(this, Cs(e, h), p, e);
  } : n.length > 2 && (u = function(h, p) {
    return n.call(this, h, p, e);
  }));
  const d = l.call(i, u, r);
  return a && s ? s(d) : d;
}
function n0(e, t, n, r) {
  const s = Rp(e), o = s !== e && !/* @__PURE__ */ zr(e);
  let i = n, a = !1;
  s !== e && (o ? (a = r.length === 0, i = function(u, d, h) {
    return a && (a = !1, u = Cs(e, u)), n.call(this, u, Cs(e, d), h, e);
  }) : n.length > 3 && (i = function(u, d, h) {
    return n.call(this, u, d, h, e);
  }));
  const l = s[t](i, ...r);
  return a ? Cs(e, l) : l;
}
function Sg(e, t, n) {
  const r = /* @__PURE__ */ Lt(e);
  $n(r, "iterate", Ac);
  const s = r[t](...n);
  return (s === -1 || s === !1) && /* @__PURE__ */ X_(n[0]) ? (n[0] = /* @__PURE__ */ Lt(n[0]), r[t](...n)) : s;
}
function uu(e, t, n = []) {
  uo(), G_();
  const r = (/* @__PURE__ */ Lt(e))[t].apply(e, n);
  return W_(), co(), r;
}
var kL = /* @__PURE__ */ Cp("__proto__,__v_isRef,__isVue"), kI = new Set(/* @__PURE__ */ Object.getOwnPropertyNames(Symbol).filter((e) => e !== "arguments" && e !== "caller").map((e) => Symbol[e]).filter(Ns));
function ML(e) {
  Ns(e) || (e = String(e));
  const t = /* @__PURE__ */ Lt(this);
  return $n(t, "has", e), t.hasOwnProperty(e);
}
var MI = class {
  constructor(e = !1, t = !1) {
    this._isReadonly = e, this._isShallow = t;
  }
  get(e, t, n) {
    if (t === "__v_skip") return e.__v_skip;
    const r = this._isReadonly, s = this._isShallow;
    if (t === "__v_isReactive") return !r;
    if (t === "__v_isReadonly") return r;
    if (t === "__v_isShallow") return s;
    if (t === "__v_raw")
      return n === (r ? s ? UL : $I : s ? NI : RI).get(e) || Object.getPrototypeOf(e) === Object.getPrototypeOf(n) ? e : void 0;
    const o = gt(e);
    if (!r) {
      let a;
      if (o && (a = IL[t])) return a;
      if (t === "hasOwnProperty") return ML;
    }
    const i = Reflect.get(e, t, /* @__PURE__ */ Xt(e) ? e : n);
    if ((Ns(t) ? kI.has(t) : kL(t)) || (r || $n(e, "get", t), s)) return i;
    if (/* @__PURE__ */ Xt(i)) {
      const a = o && H_(t) ? i : i.value;
      return r && Gt(a) ? /* @__PURE__ */ Ov(a) : a;
    }
    return Gt(i) ? r ? /* @__PURE__ */ Ov(i) : /* @__PURE__ */ J_(i) : i;
  }
}, PI = class extends MI {
  constructor(e = !1) {
    super(!1, e);
  }
  set(e, t, n, r) {
    let s = e[t];
    const o = gt(e) && H_(t);
    if (!this._isShallow) {
      const l = /* @__PURE__ */ fo(s);
      if (!/* @__PURE__ */ zr(n) && !/* @__PURE__ */ fo(n) && (s = /* @__PURE__ */ Lt(s), n = /* @__PURE__ */ Lt(n)), !o && /* @__PURE__ */ Xt(s) && !/* @__PURE__ */ Xt(n)) return l || (s.value = n), !0;
    }
    const i = o ? Number(t) < e.length : Ot(e, t), a = Reflect.set(e, t, n, /* @__PURE__ */ Xt(e) ? e : r);
    return e === /* @__PURE__ */ Lt(r) && (i ? xs(n, s) && to(e, "set", t, n, s) : to(e, "add", t, n)), a;
  }
  deleteProperty(e, t) {
    const n = Ot(e, t), r = e[t], s = Reflect.deleteProperty(e, t);
    return s && n && to(e, "delete", t, void 0, r), s;
  }
  has(e, t) {
    const n = Reflect.has(e, t);
    return (!Ns(t) || !kI.has(t)) && $n(e, "has", t), n;
  }
  ownKeys(e) {
    return $n(e, "iterate", gt(e) ? "length" : zi), Reflect.ownKeys(e);
  }
}, PL = class extends MI {
  constructor(e = !1) {
    super(!0, e);
  }
  set(e, t) {
    return !0;
  }
  deleteProperty(e, t) {
    return !0;
  }
}, RL = /* @__PURE__ */ new PI(), NL = /* @__PURE__ */ new PL(), $L = /* @__PURE__ */ new PI(!0), Lv = (e) => e, Yd = (e) => Reflect.getPrototypeOf(e);
function DL(e, t, n) {
  return function(...r) {
    const s = this.__v_raw, o = /* @__PURE__ */ Lt(s), i = rl(o), a = e === "entries" || e === Symbol.iterator && i, l = e === "keys" && i, u = s[e](...r), d = n ? Lv : t ? gl : cs;
    return !t && $n(o, "iterate", l ? Dv : zi), fn(Object.create(u), { next() {
      const { value: h, done: p } = u.next();
      return p ? {
        value: h,
        done: p
      } : {
        value: a ? [d(h[0]), d(h[1])] : d(h),
        done: p
      };
    } });
  };
}
function Xd(e) {
  return function(...t) {
    return e === "delete" ? !1 : e === "clear" ? void 0 : this;
  };
}
function LL(e, t) {
  const n = {
    get(r) {
      const s = this.__v_raw, o = /* @__PURE__ */ Lt(s), i = /* @__PURE__ */ Lt(r);
      e || (xs(r, i) && $n(o, "get", r), $n(o, "get", i));
      const { has: a } = Yd(o), l = t ? Lv : e ? gl : cs;
      if (a.call(o, r)) return l(s.get(r));
      if (a.call(o, i)) return l(s.get(i));
      s !== o && s.get(r);
    },
    get size() {
      const r = this.__v_raw;
      return !e && $n(/* @__PURE__ */ Lt(r), "iterate", zi), r.size;
    },
    has(r) {
      const s = this.__v_raw, o = /* @__PURE__ */ Lt(s), i = /* @__PURE__ */ Lt(r);
      return e || (xs(r, i) && $n(o, "has", r), $n(o, "has", i)), r === i ? s.has(r) : s.has(r) || s.has(i);
    },
    forEach(r, s) {
      const o = this, i = o.__v_raw, a = /* @__PURE__ */ Lt(i), l = t ? Lv : e ? gl : cs;
      return !e && $n(a, "iterate", zi), i.forEach((u, d) => r.call(s, l(u), l(d), o));
    }
  };
  return fn(n, e ? {
    add: Xd("add"),
    set: Xd("set"),
    delete: Xd("delete"),
    clear: Xd("clear")
  } : {
    add(r) {
      const s = /* @__PURE__ */ Lt(this), o = Yd(s), i = /* @__PURE__ */ Lt(r), a = !t && !/* @__PURE__ */ zr(r) && !/* @__PURE__ */ fo(r) ? i : r;
      return o.has.call(s, a) || xs(r, a) && o.has.call(s, r) || xs(i, a) && o.has.call(s, i) || (s.add(a), to(s, "add", a, a)), this;
    },
    set(r, s) {
      !t && !/* @__PURE__ */ zr(s) && !/* @__PURE__ */ fo(s) && (s = /* @__PURE__ */ Lt(s));
      const o = /* @__PURE__ */ Lt(this), { has: i, get: a } = Yd(o);
      let l = i.call(o, r);
      l || (r = /* @__PURE__ */ Lt(r), l = i.call(o, r));
      const u = a.call(o, r);
      return o.set(r, s), l ? xs(s, u) && to(o, "set", r, s, u) : to(o, "add", r, s), this;
    },
    delete(r) {
      const s = /* @__PURE__ */ Lt(this), { has: o, get: i } = Yd(s);
      let a = o.call(s, r);
      a || (r = /* @__PURE__ */ Lt(r), a = o.call(s, r));
      const l = i ? i.call(s, r) : void 0, u = s.delete(r);
      return a && to(s, "delete", r, void 0, l), u;
    },
    clear() {
      const r = /* @__PURE__ */ Lt(this), s = r.size !== 0, o = void 0, i = r.clear();
      return s && to(r, "clear", void 0, void 0, o), i;
    }
  }), [
    "keys",
    "values",
    "entries",
    Symbol.iterator
  ].forEach((r) => {
    n[r] = DL(r, e, t);
  }), n;
}
function z_(e, t) {
  const n = LL(e, t);
  return (r, s, o) => s === "__v_isReactive" ? !e : s === "__v_isReadonly" ? e : s === "__v_raw" ? r : Reflect.get(Ot(n, s) && s in r ? n : r, s, o);
}
var OL = { get: /* @__PURE__ */ z_(!1, !1) }, FL = { get: /* @__PURE__ */ z_(!1, !0) }, BL = { get: /* @__PURE__ */ z_(!0, !1) }, RI = /* @__PURE__ */ new WeakMap(), NI = /* @__PURE__ */ new WeakMap(), $I = /* @__PURE__ */ new WeakMap(), UL = /* @__PURE__ */ new WeakMap();
function qL(e) {
  switch (e) {
    case "Object":
    case "Array":
      return 1;
    case "Map":
    case "Set":
    case "WeakMap":
    case "WeakSet":
      return 2;
    default:
      return 0;
  }
}
// @__NO_SIDE_EFFECTS__
function J_(e) {
  return /* @__PURE__ */ fo(e) ? e : Y_(e, !1, RL, OL, RI);
}
// @__NO_SIDE_EFFECTS__
function HL(e) {
  return Y_(e, !1, $L, FL, NI);
}
// @__NO_SIDE_EFFECTS__
function Ov(e) {
  return Y_(e, !0, NL, BL, $I);
}
function Y_(e, t, n, r, s) {
  if (!Gt(e) || e.__v_raw && !(t && e.__v_isReactive) || e.__v_skip || !Object.isExtensible(e)) return e;
  const o = s.get(e);
  if (o) return o;
  const i = qL(hL(e));
  if (i === 0) return e;
  const a = new Proxy(e, i === 2 ? r : n);
  return s.set(e, a), a;
}
// @__NO_SIDE_EFFECTS__
function Ji(e) {
  return /* @__PURE__ */ fo(e) ? /* @__PURE__ */ Ji(e.__v_raw) : !!(e && e.__v_isReactive);
}
// @__NO_SIDE_EFFECTS__
function fo(e) {
  return !!(e && e.__v_isReadonly);
}
// @__NO_SIDE_EFFECTS__
function zr(e) {
  return !!(e && e.__v_isShallow);
}
// @__NO_SIDE_EFFECTS__
function X_(e) {
  return e ? !!e.__v_raw : !1;
}
// @__NO_SIDE_EFFECTS__
function Lt(e) {
  const t = e && e.__v_raw;
  return t ? /* @__PURE__ */ Lt(t) : e;
}
function GL(e) {
  return !Ot(e, "__v_skip") && Object.isExtensible(e) && gI(e, "__v_skip", !0), e;
}
var cs = (e) => Gt(e) ? /* @__PURE__ */ J_(e) : e, gl = (e) => Gt(e) ? /* @__PURE__ */ Ov(e) : e;
// @__NO_SIDE_EFFECTS__
function Xt(e) {
  return e ? e.__v_isRef === !0 : !1;
}
// @__NO_SIDE_EFFECTS__
function se(e) {
  return WL(e, !1);
}
function WL(e, t) {
  return /* @__PURE__ */ Xt(e) ? e : new VL(e, t);
}
var VL = class {
  constructor(e, t) {
    this.dep = new K_(), this.__v_isRef = !0, this.__v_isShallow = !1, this._rawValue = t ? e : /* @__PURE__ */ Lt(e), this._value = t ? e : cs(e), this.__v_isShallow = t;
  }
  get value() {
    return this.dep.track(), this._value;
  }
  set value(e) {
    const t = this._rawValue, n = this.__v_isShallow || /* @__PURE__ */ zr(e) || /* @__PURE__ */ fo(e);
    e = n ? e : /* @__PURE__ */ Lt(e), xs(e, t) && (this._rawValue = e, this._value = n ? e : cs(e), this.dep.trigger());
  }
};
function S(e) {
  return /* @__PURE__ */ Xt(e) ? e.value : e;
}
var KL = {
  get: (e, t, n) => t === "__v_raw" ? e : S(Reflect.get(e, t, n)),
  set: (e, t, n, r) => {
    const s = e[t];
    return /* @__PURE__ */ Xt(s) && !/* @__PURE__ */ Xt(n) ? (s.value = n, !0) : Reflect.set(e, t, n, r);
  }
};
function DI(e) {
  return /* @__PURE__ */ Ji(e) ? e : new Proxy(e, KL);
}
var zL = class {
  constructor(e, t, n) {
    this.fn = e, this.setter = t, this._value = void 0, this.dep = new K_(this), this.__v_isRef = !0, this.deps = void 0, this.depsTail = void 0, this.flags = 16, this.globalVersion = Cc - 1, this.next = void 0, this.effect = this, this.__v_isReadonly = !t, this.isSSR = n;
  }
  notify() {
    if (this.flags |= 16, !(this.flags & 8) && Kt !== this)
      return TI(this, !0), !0;
  }
  get value() {
    const e = this.dep.track();
    return AI(this), e && (e.version = this.dep.version), this._value;
  }
  set value(e) {
    this.setter && this.setter(e);
  }
};
// @__NO_SIDE_EFFECTS__
function JL(e, t, n = !1) {
  let r, s;
  return St(e) ? r = e : (r = e.get, s = e.set), new zL(r, s, n);
}
var Qd = {}, Sh = /* @__PURE__ */ new WeakMap(), ki = void 0;
function YL(e, t = !1, n = ki) {
  if (n) {
    let r = Sh.get(n);
    r || Sh.set(n, r = []), r.push(e);
  }
}
function XL(e, t, n = Wt) {
  const { immediate: r, deep: s, once: o, scheduler: i, augmentJob: a, call: l } = n, u = (D) => s ? D : /* @__PURE__ */ zr(D) || s === !1 || s === 0 ? no(D, 1) : no(D);
  let d, h, p, y, w = !1, E = !1;
  if (/* @__PURE__ */ Xt(e) ? (h = () => e.value, w = /* @__PURE__ */ zr(e)) : /* @__PURE__ */ Ji(e) ? (h = () => u(e), w = !0) : gt(e) ? (E = !0, w = e.some((D) => /* @__PURE__ */ Ji(D) || /* @__PURE__ */ zr(D)), h = () => e.map((D) => {
    if (/* @__PURE__ */ Xt(D)) return D.value;
    if (/* @__PURE__ */ Ji(D)) return u(D);
    if (St(D)) return l ? l(D, 2) : D();
  })) : St(e) ? t ? h = l ? () => l(e, 2) : e : h = () => {
    if (p) {
      uo();
      try {
        p();
      } finally {
        co();
      }
    }
    const D = ki;
    ki = d;
    try {
      return l ? l(e, 3, [y]) : e(y);
    } finally {
      ki = D;
    }
  } : h = Rs, t && s) {
    const D = h, m = s === !0 ? 1 / 0 : s;
    h = () => no(D(), m);
  }
  const C = EL(), M = () => {
    d.stop(), C && C.active && q_(C.effects, d);
  };
  if (o && t) {
    const D = t;
    t = (...m) => {
      D(...m), M();
    };
  }
  let k = E ? new Array(e.length).fill(Qd) : Qd;
  const R = (D) => {
    if (!(!(d.flags & 1) || !d.dirty && !D))
      if (t) {
        const m = d.run();
        if (s || w || (E ? m.some((b, v) => xs(b, k[v])) : xs(m, k))) {
          p && p();
          const b = ki;
          ki = d;
          try {
            const v = [
              m,
              k === Qd ? void 0 : E && k[0] === Qd ? [] : k,
              y
            ];
            k = m, l ? l(t, 3, v) : t(...v);
          } finally {
            ki = b;
          }
        }
      } else d.run();
  };
  return a && a(R), d = new wI(h), d.scheduler = i ? () => i(R, !1) : R, y = (D) => YL(D, !1, d), p = d.onStop = () => {
    const D = Sh.get(d);
    if (D) {
      if (l) l(D, 4);
      else for (const m of D) m();
      Sh.delete(d);
    }
  }, t ? r ? R(!0) : k = d.run() : i ? i(R.bind(null, !0), !0) : d.run(), M.pause = d.pause.bind(d), M.resume = d.resume.bind(d), M.stop = M, M;
}
function no(e, t = 1 / 0, n) {
  if (t <= 0 || !Gt(e) || e.__v_skip || (n = n || /* @__PURE__ */ new Map(), (n.get(e) || 0) >= t)) return e;
  if (n.set(e, t), t--, /* @__PURE__ */ Xt(e)) no(e.value, t, n);
  else if (gt(e)) for (let r = 0; r < e.length; r++) no(e[r], t, n);
  else if (xp(e) || rl(e)) e.forEach((r) => {
    no(r, t, n);
  });
  else if (pI(e)) {
    for (const r in e) no(e[r], t, n);
    for (const r of Object.getOwnPropertySymbols(e)) Object.prototype.propertyIsEnumerable.call(e, r) && no(e[r], t, n);
  }
  return e;
}
function jc(e, t, n, r) {
  try {
    return r ? e(...r) : e();
  } catch (s) {
    Np(s, t, n);
  }
}
function ds(e, t, n, r) {
  if (St(e)) {
    const s = jc(e, t, n, r);
    return s && fI(s) && s.catch((o) => {
      Np(o, t, n);
    }), s;
  }
  if (gt(e)) {
    const s = [];
    for (let o = 0; o < e.length; o++) s.push(ds(e[o], t, n, r));
    return s;
  }
}
function Np(e, t, n, r = !0) {
  const s = t ? t.vnode : null, { errorHandler: o, throwUnhandledErrorInProduction: i } = t && t.appContext.config || Wt;
  if (t) {
    let a = t.parent;
    const l = t.proxy, u = `https://vuejs.org/error-reference/#runtime-${n}`;
    for (; a; ) {
      const d = a.ec;
      if (d) {
        for (let h = 0; h < d.length; h++) if (d[h](e, l, u) === !1) return;
      }
      a = a.parent;
    }
    if (o) {
      uo(), jc(o, null, 10, [
        e,
        l,
        u
      ]), co();
      return;
    }
  }
  QL(e, n, s, r, i);
}
function QL(e, t, n, r = !0, s = !1) {
  if (s) throw e;
  console.error(e);
}
var Vn = [], Ss = -1, sl = [], $o = null, Ua = 0, LI = /* @__PURE__ */ Promise.resolve(), Th = null;
function Jt(e) {
  const t = Th || LI;
  return e ? t.then(this ? e.bind(this) : e) : t;
}
function ZL(e) {
  let t = Ss + 1, n = Vn.length;
  for (; t < n; ) {
    const r = t + n >>> 1, s = Vn[r], o = Ic(s);
    o < e || o === e && s.flags & 2 ? t = r + 1 : n = r;
  }
  return t;
}
function Q_(e) {
  if (!(e.flags & 1)) {
    const t = Ic(e), n = Vn[Vn.length - 1];
    !n || !(e.flags & 2) && t >= Ic(n) ? Vn.push(e) : Vn.splice(ZL(t), 0, e), e.flags |= 1, OI();
  }
}
function OI() {
  Th || (Th = LI.then(BI));
}
function jL(e) {
  gt(e) ? sl.push(...e) : $o && e.id === -1 ? $o.splice(Ua + 1, 0, e) : e.flags & 1 || (sl.push(e), e.flags |= 1), OI();
}
function r0(e, t, n = Ss + 1) {
  for (; n < Vn.length; n++) {
    const r = Vn[n];
    if (r && r.flags & 2) {
      if (e && r.id !== e.uid) continue;
      Vn.splice(n, 1), n--, r.flags & 4 && (r.flags &= -2), r(), r.flags & 4 || (r.flags &= -2);
    }
  }
}
function FI(e) {
  if (sl.length) {
    const t = [...new Set(sl)].sort((n, r) => Ic(n) - Ic(r));
    if (sl.length = 0, $o) {
      $o.push(...t);
      return;
    }
    for ($o = t, Ua = 0; Ua < $o.length; Ua++) {
      const n = $o[Ua];
      n.flags & 4 && (n.flags &= -2), n.flags & 8 || n(), n.flags &= -2;
    }
    $o = null, Ua = 0;
  }
}
var Ic = (e) => e.id == null ? e.flags & 2 ? -1 : 1 / 0 : e.id;
function BI(e) {
  try {
    for (Ss = 0; Ss < Vn.length; Ss++) {
      const t = Vn[Ss];
      t && !(t.flags & 8) && (t.flags & 4 && (t.flags &= -2), jc(t, t.i, t.i ? 15 : 14), t.flags & 4 || (t.flags &= -2));
    }
  } finally {
    for (; Ss < Vn.length; Ss++) {
      const t = Vn[Ss];
      t && (t.flags &= -2);
    }
    Ss = -1, Vn.length = 0, FI(e), Th = null, (Vn.length || sl.length) && BI(e);
  }
}
var Gr = null, UI = null;
function Eh(e) {
  const t = Gr;
  return Gr = e, UI = e && e.type.__scopeId || null, t;
}
function eO(e, t = Gr, n) {
  if (!t || e._n) return e;
  const r = (...s) => {
    r._d && h0(-1);
    const o = Eh(t);
    let i;
    try {
      i = e(...s);
    } finally {
      Eh(o), r._d && h0(1);
    }
    return i;
  };
  return r._n = !0, r._c = !0, r._d = !0, r;
}
function wn(e, t) {
  if (Gr === null) return e;
  const n = Op(Gr), r = e.dirs || (e.dirs = []);
  for (let s = 0; s < t.length; s++) {
    let [o, i, a, l = Wt] = t[s];
    o && (St(o) && (o = {
      mounted: o,
      updated: o
    }), o.deep && no(i), r.push({
      dir: o,
      instance: n,
      value: i,
      oldValue: void 0,
      arg: a,
      modifiers: l
    }));
  }
  return e;
}
function Ti(e, t, n, r) {
  const s = e.dirs, o = t && t.dirs;
  for (let i = 0; i < s.length; i++) {
    const a = s[i];
    o && (a.oldValue = o[i].value);
    let l = a.dir[r];
    l && (uo(), ds(l, n, 8, [
      e.el,
      a,
      e,
      t
    ]), co());
  }
}
function qI(e, t) {
  if (zn) {
    let n = zn.provides;
    const r = zn.parent && zn.parent.provides;
    r === n && (n = zn.provides = Object.create(r)), n[e] = t;
  }
}
function ac(e, t, n = !1) {
  const r = JO();
  if (r || ol) {
    let s = ol ? ol._context.provides : r ? r.parent == null || r.ce ? r.vnode.appContext && r.vnode.appContext.provides : r.parent.provides : void 0;
    if (s && e in s) return s[e];
    if (arguments.length > 1) return n && St(t) ? t.call(r && r.proxy) : t;
  }
}
var tO = /* @__PURE__ */ Symbol.for("v-scx"), nO = () => {
  {
    const e = ac(tO);
    return e;
  }
};
function yt(e, t, n) {
  return HI(e, t, n);
}
function HI(e, t, n = Wt) {
  const { immediate: r, deep: s, flush: o, once: i } = n, a = fn({}, n), l = t && r || !t && o !== "post";
  let u;
  if (kc) {
    if (o === "sync") {
      const y = nO();
      u = y.__watcherHandles || (y.__watcherHandles = []);
    } else if (!l) {
      const y = () => {
      };
      return y.stop = Rs, y.resume = Rs, y.pause = Rs, y;
    }
  }
  const d = zn;
  a.call = (y, w, E) => ds(y, d, w, E);
  let h = !1;
  o === "post" ? a.scheduler = (y) => {
    Zn(y, d && d.suspense);
  } : o !== "sync" && (h = !0, a.scheduler = (y, w) => {
    w ? y() : Q_(y);
  }), a.augmentJob = (y) => {
    t && (y.flags |= 4), h && (y.flags |= 2, d && (y.id = d.uid, y.i = d));
  };
  const p = XL(e, t, a);
  return kc && (u ? u.push(p) : l && p()), p;
}
function rO(e, t, n) {
  const r = this.proxy, s = tn(e) ? e.includes(".") ? GI(r, e) : () => r[e] : e.bind(r, r);
  let o;
  St(t) ? o = t : (o = t.handler, n = t);
  const i = ed(this), a = HI(s, o.bind(r), n);
  return i(), a;
}
function GI(e, t) {
  const n = t.split(".");
  return () => {
    let r = e;
    for (let s = 0; s < n.length && r; s++) r = r[n[s]];
    return r;
  };
}
var sO = /* @__PURE__ */ Symbol("_vte"), oO = (e) => e.__isTeleport, Tg = /* @__PURE__ */ Symbol("_leaveCb");
function Z_(e, t) {
  e.shapeFlag & 6 && e.component ? (e.transition = t, Z_(e.component.subTree, t)) : e.shapeFlag & 128 ? (e.ssContent.transition = t.clone(e.ssContent), e.ssFallback.transition = t.clone(e.ssFallback)) : e.transition = t;
}
// @__NO_SIDE_EFFECTS__
function jt(e, t) {
  return St(e) ? fn({ name: e.name }, t, { setup: e }) : e;
}
function WI(e) {
  e.ids = [
    e.ids[0] + e.ids[2]++ + "-",
    0,
    0
  ];
}
function s0(e, t) {
  let n;
  return !!((n = Object.getOwnPropertyDescriptor(e, t)) && !n.configurable);
}
var Ch = /* @__PURE__ */ new WeakMap();
function lc(e, t, n, r, s = !1) {
  if (gt(e)) {
    e.forEach((E, C) => lc(E, t && (gt(t) ? t[C] : t), n, r, s));
    return;
  }
  if (uc(r) && !s) {
    r.shapeFlag & 512 && r.type.__asyncResolved && r.component.subTree.component && lc(e, t, n, r.component.subTree);
    return;
  }
  const o = r.shapeFlag & 4 ? Op(r.component) : r.el, i = s ? null : o, { i: a, r: l } = e, u = t && t.r, d = a.refs === Wt ? a.refs = {} : a.refs, h = a.setupState, p = /* @__PURE__ */ Lt(h), y = h === Wt ? dI : (E) => s0(d, E) ? !1 : Ot(p, E), w = (E, C) => !(C && s0(d, C));
  if (u != null && u !== l) {
    if (o0(t), tn(u))
      d[u] = null, y(u) && (h[u] = null);
    else if (/* @__PURE__ */ Xt(u)) {
      const E = t;
      w(u, E.k) && (u.value = null), E.k && (d[E.k] = null);
    }
  }
  if (St(l)) jc(l, a, 12, [i, d]);
  else {
    const E = tn(l), C = /* @__PURE__ */ Xt(l);
    if (E || C) {
      const M = () => {
        if (e.f) {
          const k = E ? y(l) ? h[l] : d[l] : w(l) || !e.k ? l.value : d[e.k];
          if (s) gt(k) && q_(k, o);
          else if (gt(k)) k.includes(o) || k.push(o);
          else if (E)
            d[l] = [o], y(l) && (h[l] = d[l]);
          else {
            const R = [o];
            w(l, e.k) && (l.value = R), e.k && (d[e.k] = R);
          }
        } else E ? (d[l] = i, y(l) && (h[l] = i)) : C && (w(l, e.k) && (l.value = i), e.k && (d[e.k] = i));
      };
      if (i) {
        const k = () => {
          M(), Ch.delete(e);
        };
        k.id = -1, Ch.set(e, k), Zn(k, n);
      } else
        o0(e), M();
    }
  }
}
function o0(e) {
  const t = Ch.get(e);
  t && (t.flags |= 8, Ch.delete(e));
}
var eae = Pp().requestIdleCallback || ((e) => setTimeout(e, 1)), tae = Pp().cancelIdleCallback || ((e) => clearTimeout(e)), uc = (e) => !!e.type.__asyncLoader, VI = (e) => e.type.__isKeepAlive;
function iO(e, t) {
  KI(e, "a", t);
}
function aO(e, t) {
  KI(e, "da", t);
}
function KI(e, t, n = zn) {
  const r = e.__wdc || (e.__wdc = () => {
    let s = n;
    for (; s; ) {
      if (s.isDeactivated) return;
      s = s.parent;
    }
    return e();
  });
  if ($p(t, r, n), n) {
    let s = n.parent;
    for (; s && s.parent; )
      VI(s.parent.vnode) && lO(r, t, n, s), s = s.parent;
  }
}
function lO(e, t, n, r) {
  const s = $p(t, e, r, !0);
  j_(() => {
    q_(r[t], s);
  }, n);
}
function $p(e, t, n = zn, r = !1) {
  if (n) {
    const s = n[e] || (n[e] = []), o = t.__weh || (t.__weh = (...i) => {
      uo();
      const a = ed(n), l = ds(t, n, e, i);
      return a(), co(), l;
    });
    return r ? s.unshift(o) : s.push(o), o;
  }
}
var vo = (e) => (t, n = zn) => {
  (!kc || e === "sp") && $p(e, (...r) => t(...r), n);
}, uO = vo("bm"), zI = vo("m"), JI = vo("bu"), YI = vo("u"), vl = vo("bum"), j_ = vo("um"), cO = vo("sp"), dO = vo("rtg"), fO = vo("rtc");
function hO(e, t = zn) {
  $p("ec", e, t);
}
var pO = /* @__PURE__ */ Symbol.for("v-ndc");
function ut(e, t, n, r) {
  let s;
  const o = n && n[r], i = gt(e);
  if (i || tn(e)) {
    const a = i && /* @__PURE__ */ Ji(e);
    let l = !1, u = !1;
    a && (l = !/* @__PURE__ */ zr(e), u = /* @__PURE__ */ fo(e), e = Rp(e)), s = new Array(e.length);
    for (let d = 0, h = e.length; d < h; d++) s[d] = t(l ? u ? gl(cs(e[d])) : cs(e[d]) : e[d], d, void 0, o && o[d]);
  } else if (typeof e == "number") {
    s = new Array(e);
    for (let a = 0; a < e; a++) s[a] = t(a + 1, a, void 0, o && o[a]);
  } else if (Gt(e)) if (e[Symbol.iterator]) s = Array.from(e, (a, l) => t(a, l, void 0, o && o[l]));
  else {
    const a = Object.keys(e);
    s = new Array(a.length);
    for (let l = 0, u = a.length; l < u; l++) {
      const d = a[l];
      s[l] = t(e[d], d, l, o && o[l]);
    }
  }
  else s = [];
  return n && (n[r] = s), s;
}
var Fv = (e) => e ? mx(e) ? Op(e) : Fv(e.parent) : null, cc = /* @__PURE__ */ fn(/* @__PURE__ */ Object.create(null), {
  $: (e) => e,
  $el: (e) => e.vnode.el,
  $data: (e) => e.data,
  $props: (e) => e.props,
  $attrs: (e) => e.attrs,
  $slots: (e) => e.slots,
  $refs: (e) => e.refs,
  $parent: (e) => Fv(e.parent),
  $root: (e) => Fv(e.root),
  $host: (e) => e.ce,
  $emit: (e) => e.emit,
  $options: (e) => eb(e),
  $forceUpdate: (e) => e.f || (e.f = () => {
    Q_(e.update);
  }),
  $nextTick: (e) => e.n || (e.n = Jt.bind(e.proxy)),
  $watch: (e) => rO.bind(e)
}), Eg = (e, t) => e !== Wt && !e.__isScriptSetup && Ot(e, t), mO = {
  get({ _: e }, t) {
    if (t === "__v_skip") return !0;
    const { ctx: n, setupState: r, data: s, props: o, accessCache: i, type: a, appContext: l } = e;
    if (t[0] !== "$") {
      const p = i[t];
      if (p !== void 0) switch (p) {
        case 1:
          return r[t];
        case 2:
          return s[t];
        case 4:
          return n[t];
        case 3:
          return o[t];
      }
      else {
        if (Eg(r, t))
          return i[t] = 1, r[t];
        if (s !== Wt && Ot(s, t))
          return i[t] = 2, s[t];
        if (Ot(o, t))
          return i[t] = 3, o[t];
        if (n !== Wt && Ot(n, t))
          return i[t] = 4, n[t];
        Bv && (i[t] = 0);
      }
    }
    const u = cc[t];
    let d, h;
    if (u)
      return t === "$attrs" && $n(e.attrs, "get", ""), u(e);
    if ((d = a.__cssModules) && (d = d[t])) return d;
    if (n !== Wt && Ot(n, t))
      return i[t] = 4, n[t];
    if (h = l.config.globalProperties, Ot(h, t)) return h[t];
  },
  set({ _: e }, t, n) {
    const { data: r, setupState: s, ctx: o } = e;
    return Eg(s, t) ? (s[t] = n, !0) : r !== Wt && Ot(r, t) ? (r[t] = n, !0) : Ot(e.props, t) || t[0] === "$" && t.slice(1) in e ? !1 : (o[t] = n, !0);
  },
  has({ _: { data: e, setupState: t, accessCache: n, ctx: r, appContext: s, props: o, type: i } }, a) {
    let l;
    return !!(n[a] || e !== Wt && a[0] !== "$" && Ot(e, a) || Eg(t, a) || Ot(o, a) || Ot(r, a) || Ot(cc, a) || Ot(s.config.globalProperties, a) || (l = i.__cssModules) && l[a]);
  },
  defineProperty(e, t, n) {
    return n.get != null ? e._.accessCache[t] = 0 : Ot(n, "value") && this.set(e, t, n.value, null), Reflect.defineProperty(e, t, n);
  }
};
function i0(e) {
  return gt(e) ? e.reduce((t, n) => (t[n] = null, t), {}) : e;
}
var Bv = !0;
function gO(e) {
  const t = eb(e), n = e.proxy, r = e.ctx;
  Bv = !1, t.beforeCreate && a0(t.beforeCreate, e, "bc");
  const { data: s, computed: o, methods: i, watch: a, provide: l, inject: u, created: d, beforeMount: h, mounted: p, beforeUpdate: y, updated: w, activated: E, deactivated: C, beforeDestroy: M, beforeUnmount: k, destroyed: R, unmounted: D, render: m, renderTracked: b, renderTriggered: v, errorCaptured: T, serverPrefetch: I, expose: x, inheritAttrs: P, components: L, directives: G, filters: z } = t;
  if (u && vO(u, r, null), i) for (const O in i) {
    const Y = i[O];
    St(Y) && (r[O] = Y.bind(n));
  }
  if (s) {
    const O = s.call(n, n);
    Gt(O) && (e.data = /* @__PURE__ */ J_(O));
  }
  if (Bv = !0, o) for (const O in o) {
    const Y = o[O], X = ie({
      get: St(Y) ? Y.bind(n, n) : St(Y.get) ? Y.get.bind(n, n) : Rs,
      set: !St(Y) && St(Y.set) ? Y.set.bind(n) : Rs
    });
    Object.defineProperty(r, O, {
      enumerable: !0,
      configurable: !0,
      get: () => X.value,
      set: (j) => X.value = j
    });
  }
  if (a) for (const O in a) XI(a[O], r, n, O);
  if (l) {
    const O = St(l) ? l.call(n) : l;
    Reflect.ownKeys(O).forEach((Y) => {
      qI(Y, O[Y]);
    });
  }
  d && a0(d, e, "c");
  function Z(O, Y) {
    gt(Y) ? Y.forEach((X) => O(X.bind(n))) : Y && O(Y.bind(n));
  }
  if (Z(uO, h), Z(zI, p), Z(JI, y), Z(YI, w), Z(iO, E), Z(aO, C), Z(hO, T), Z(fO, b), Z(dO, v), Z(vl, k), Z(j_, D), Z(cO, I), gt(x))
    if (x.length) {
      const O = e.exposed || (e.exposed = {});
      x.forEach((Y) => {
        Object.defineProperty(O, Y, {
          get: () => n[Y],
          set: (X) => n[Y] = X,
          enumerable: !0
        });
      });
    } else e.exposed || (e.exposed = {});
  m && e.render === Rs && (e.render = m), P != null && (e.inheritAttrs = P), L && (e.components = L), G && (e.directives = G), I && WI(e);
}
function vO(e, t, n = Rs) {
  gt(e) && (e = Uv(e));
  for (const r in e) {
    const s = e[r];
    let o;
    Gt(s) ? "default" in s ? o = ac(s.from || r, s.default, !0) : o = ac(s.from || r) : o = ac(s), /* @__PURE__ */ Xt(o) ? Object.defineProperty(t, r, {
      enumerable: !0,
      configurable: !0,
      get: () => o.value,
      set: (i) => o.value = i
    }) : t[r] = o;
  }
}
function a0(e, t, n) {
  ds(gt(e) ? e.map((r) => r.bind(t.proxy)) : e.bind(t.proxy), t, n);
}
function XI(e, t, n, r) {
  let s = r.includes(".") ? GI(n, r) : () => n[r];
  if (tn(e)) {
    const o = t[e];
    St(o) && yt(s, o);
  } else if (St(e)) yt(s, e.bind(n));
  else if (Gt(e)) if (gt(e)) e.forEach((o) => XI(o, t, n, r));
  else {
    const o = St(e.handler) ? e.handler.bind(n) : t[e.handler];
    St(o) && yt(s, o, e);
  }
}
function eb(e) {
  const t = e.type, { mixins: n, extends: r } = t, { mixins: s, optionsCache: o, config: { optionMergeStrategies: i } } = e.appContext, a = o.get(t);
  let l;
  return a ? l = a : !s.length && !n && !r ? l = t : (l = {}, s.length && s.forEach((u) => Ah(l, u, i, !0)), Ah(l, t, i)), Gt(t) && o.set(t, l), l;
}
function Ah(e, t, n, r = !1) {
  const { mixins: s, extends: o } = t;
  o && Ah(e, o, n, !0), s && s.forEach((i) => Ah(e, i, n, !0));
  for (const i in t) if (!(r && i === "expose")) {
    const a = yO[i] || n && n[i];
    e[i] = a ? a(e[i], t[i]) : t[i];
  }
  return e;
}
var yO = {
  data: l0,
  props: u0,
  emits: u0,
  methods: Lu,
  computed: Lu,
  beforeCreate: qn,
  created: qn,
  beforeMount: qn,
  mounted: qn,
  beforeUpdate: qn,
  updated: qn,
  beforeDestroy: qn,
  beforeUnmount: qn,
  destroyed: qn,
  unmounted: qn,
  activated: qn,
  deactivated: qn,
  errorCaptured: qn,
  serverPrefetch: qn,
  components: Lu,
  directives: Lu,
  watch: bO,
  provide: l0,
  inject: _O
};
function l0(e, t) {
  return t ? e ? function() {
    return fn(St(e) ? e.call(this, this) : e, St(t) ? t.call(this, this) : t);
  } : t : e;
}
function _O(e, t) {
  return Lu(Uv(e), Uv(t));
}
function Uv(e) {
  if (gt(e)) {
    const t = {};
    for (let n = 0; n < e.length; n++) t[e[n]] = e[n];
    return t;
  }
  return e;
}
function qn(e, t) {
  return e ? [...new Set([].concat(e, t))] : t;
}
function Lu(e, t) {
  return e ? fn(/* @__PURE__ */ Object.create(null), e, t) : t;
}
function u0(e, t) {
  return e ? gt(e) && gt(t) ? [.../* @__PURE__ */ new Set([...e, ...t])] : fn(/* @__PURE__ */ Object.create(null), i0(e), i0(t ?? {})) : t;
}
function bO(e, t) {
  if (!e) return t;
  if (!t) return e;
  const n = fn(/* @__PURE__ */ Object.create(null), e);
  for (const r in t) n[r] = qn(e[r], t[r]);
  return n;
}
function QI() {
  return {
    app: null,
    config: {
      isNativeTag: dI,
      performance: !1,
      globalProperties: {},
      optionMergeStrategies: {},
      errorHandler: void 0,
      warnHandler: void 0,
      compilerOptions: {}
    },
    mixins: [],
    components: {},
    directives: {},
    provides: /* @__PURE__ */ Object.create(null),
    optionsCache: /* @__PURE__ */ new WeakMap(),
    propsCache: /* @__PURE__ */ new WeakMap(),
    emitsCache: /* @__PURE__ */ new WeakMap()
  };
}
var wO = 0;
function SO(e, t) {
  return function(r, s = null) {
    St(r) || (r = fn({}, r)), s != null && !Gt(s) && (s = null);
    const o = QI(), i = /* @__PURE__ */ new WeakSet(), a = [];
    let l = !1;
    const u = o.app = {
      _uid: wO++,
      _component: r,
      _props: s,
      _container: null,
      _context: o,
      _instance: null,
      version: eF,
      get config() {
        return o.config;
      },
      set config(d) {
      },
      use(d, ...h) {
        return i.has(d) || (d && St(d.install) ? (i.add(d), d.install(u, ...h)) : St(d) && (i.add(d), d(u, ...h))), u;
      },
      mixin(d) {
        return o.mixins.includes(d) || o.mixins.push(d), u;
      },
      component(d, h) {
        return h ? (o.components[d] = h, u) : o.components[d];
      },
      directive(d, h) {
        return h ? (o.directives[d] = h, u) : o.directives[d];
      },
      mount(d, h, p) {
        if (!l) {
          const y = u._ceVNode || Ft(r, s);
          return y.appContext = o, p === !0 ? p = "svg" : p === !1 && (p = void 0), h && t ? t(y, d) : e(y, d, p), l = !0, u._container = d, d.__vue_app__ = u, Op(y.component);
        }
      },
      onUnmount(d) {
        a.push(d);
      },
      unmount() {
        l && (ds(a, u._instance, 16), e(null, u._container), delete u._container.__vue_app__);
      },
      provide(d, h) {
        return o.provides[d] = h, u;
      },
      runWithContext(d) {
        const h = ol;
        ol = u;
        try {
          return d();
        } finally {
          ol = h;
        }
      }
    };
    return u;
  };
}
var ol = null, TO = (e, t) => t === "modelValue" || t === "model-value" ? e.modelModifiers : e[`${t}Modifiers`] || e[`${os(t)}Modifiers`] || e[`${si(t)}Modifiers`];
function EO(e, t, ...n) {
  if (e.isUnmounted) return;
  const r = e.vnode.props || Wt;
  let s = n;
  const o = t.startsWith("update:"), i = o && TO(r, t.slice(7));
  i && (i.trim && (s = n.map((d) => tn(d) ? d.trim() : d)), i.number && (s = n.map(Mp)));
  let a, l = r[a = yg(t)] || r[a = yg(os(t))];
  !l && o && (l = r[a = yg(si(t))]), l && ds(l, e, 6, s);
  const u = r[a + "Once"];
  if (u) {
    if (!e.emitted) e.emitted = {};
    else if (e.emitted[a]) return;
    e.emitted[a] = !0, ds(u, e, 6, s);
  }
}
var CO = /* @__PURE__ */ new WeakMap();
function ZI(e, t, n = !1) {
  const r = n ? CO : t.emitsCache, s = r.get(e);
  if (s !== void 0) return s;
  const o = e.emits;
  let i = {}, a = !1;
  if (!St(e)) {
    const l = (u) => {
      const d = ZI(u, t, !0);
      d && (a = !0, fn(i, d));
    };
    !n && t.mixins.length && t.mixins.forEach(l), e.extends && l(e.extends), e.mixins && e.mixins.forEach(l);
  }
  return !o && !a ? (Gt(e) && r.set(e, null), null) : (gt(o) ? o.forEach((l) => i[l] = null) : fn(i, o), Gt(e) && r.set(e, i), i);
}
function Dp(e, t) {
  return !e || !Ap(t) ? !1 : (t = t.slice(2).replace(/Once$/, ""), Ot(e, t[0].toLowerCase() + t.slice(1)) || Ot(e, si(t)) || Ot(e, t));
}
function Cg(e) {
  const { type: t, vnode: n, proxy: r, withProxy: s, propsOptions: [o], slots: i, attrs: a, emit: l, render: u, renderCache: d, props: h, data: p, setupState: y, ctx: w, inheritAttrs: E } = e, C = Eh(e);
  let M, k;
  try {
    if (n.shapeFlag & 4) {
      const D = s || r, m = D;
      M = As(u.call(m, D, d, h, y, p, w)), k = a;
    } else {
      const D = t;
      M = As(D.length > 1 ? D(h, {
        attrs: a,
        slots: i,
        emit: l
      }) : D(h, null)), k = t.props ? a : AO(a);
    }
  } catch (D) {
    dc.length = 0, Np(D, e, 1), M = Ft(Qo);
  }
  let R = M;
  if (k && E !== !1) {
    const D = Object.keys(k), { shapeFlag: m } = R;
    D.length && m & 7 && (o && D.some(Ip) && (k = IO(k, o)), R = yl(R, k, !1, !0));
  }
  return n.dirs && (R = yl(R, null, !1, !0), R.dirs = R.dirs ? R.dirs.concat(n.dirs) : n.dirs), n.transition && Z_(R, n.transition), M = R, Eh(C), M;
}
var AO = (e) => {
  let t;
  for (const n in e) (n === "class" || n === "style" || Ap(n)) && ((t || (t = {}))[n] = e[n]);
  return t;
}, IO = (e, t) => {
  const n = {};
  for (const r in e) (!Ip(r) || !(r.slice(9) in t)) && (n[r] = e[r]);
  return n;
};
function xO(e, t, n) {
  const { props: r, children: s, component: o } = e, { props: i, children: a, patchFlag: l } = t, u = o.emitsOptions;
  if (t.dirs || t.transition) return !0;
  if (n && l >= 0) {
    if (l & 1024) return !0;
    if (l & 16)
      return r ? c0(r, i, u) : !!i;
    if (l & 8) {
      const d = t.dynamicProps;
      for (let h = 0; h < d.length; h++) {
        const p = d[h];
        if (jI(i, r, p) && !Dp(u, p)) return !0;
      }
    }
  } else
    return (s || a) && (!a || !a.$stable) ? !0 : r === i ? !1 : r ? i ? c0(r, i, u) : !0 : !!i;
  return !1;
}
function c0(e, t, n) {
  const r = Object.keys(t);
  if (r.length !== Object.keys(e).length) return !0;
  for (let s = 0; s < r.length; s++) {
    const o = r[s];
    if (jI(t, e, o) && !Dp(n, o)) return !0;
  }
  return !1;
}
function jI(e, t, n) {
  const r = e[n], s = t[n];
  return n === "style" && Gt(r) && Gt(s) ? !Zc(r, s) : r !== s;
}
function kO({ vnode: e, parent: t, suspense: n }, r) {
  for (; t; ) {
    const s = t.subTree;
    if (s.suspense && s.suspense.activeBranch === e && (s.suspense.vnode.el = s.el = r, e = s), s === e)
      (e = t.vnode).el = r, t = t.parent;
    else break;
  }
  n && n.activeBranch === e && (n.vnode.el = r);
}
var ex = {}, tx = () => Object.create(ex), nx = (e) => Object.getPrototypeOf(e) === ex;
function MO(e, t, n, r = !1) {
  const s = {}, o = tx();
  e.propsDefaults = /* @__PURE__ */ Object.create(null), rx(e, t, s, o);
  for (const i in e.propsOptions[0]) i in s || (s[i] = void 0);
  n ? e.props = r ? s : /* @__PURE__ */ HL(s) : e.type.props ? e.props = s : e.props = o, e.attrs = o;
}
function PO(e, t, n, r) {
  const { props: s, attrs: o, vnode: { patchFlag: i } } = e, a = /* @__PURE__ */ Lt(s), [l] = e.propsOptions;
  let u = !1;
  if ((r || i > 0) && !(i & 16)) {
    if (i & 8) {
      const d = e.vnode.dynamicProps;
      for (let h = 0; h < d.length; h++) {
        let p = d[h];
        if (Dp(e.emitsOptions, p)) continue;
        const y = t[p];
        if (l) if (Ot(o, p))
          y !== o[p] && (o[p] = y, u = !0);
        else {
          const w = os(p);
          s[w] = qv(l, a, w, y, e, !1);
        }
        else y !== o[p] && (o[p] = y, u = !0);
      }
    }
  } else {
    rx(e, t, s, o) && (u = !0);
    let d;
    for (const h in a) (!t || !Ot(t, h) && ((d = si(h)) === h || !Ot(t, d))) && (l ? n && (n[h] !== void 0 || n[d] !== void 0) && (s[h] = qv(l, a, h, void 0, e, !0)) : delete s[h]);
    if (o !== a)
      for (const h in o) (!t || !Ot(t, h)) && (delete o[h], u = !0);
  }
  u && to(e.attrs, "set", "");
}
function rx(e, t, n, r) {
  const [s, o] = e.propsOptions;
  let i = !1, a;
  if (t) for (let l in t) {
    if (sc(l)) continue;
    const u = t[l];
    let d;
    s && Ot(s, d = os(l)) ? !o || !o.includes(d) ? n[d] = u : (a || (a = {}))[d] = u : Dp(e.emitsOptions, l) || (!(l in r) || u !== r[l]) && (r[l] = u, i = !0);
  }
  if (o) {
    const l = /* @__PURE__ */ Lt(n), u = a || Wt;
    for (let d = 0; d < o.length; d++) {
      const h = o[d];
      n[h] = qv(s, l, h, u[h], e, !Ot(u, h));
    }
  }
  return i;
}
function qv(e, t, n, r, s, o) {
  const i = e[n];
  if (i != null) {
    const a = Ot(i, "default");
    if (a && r === void 0) {
      const l = i.default;
      if (i.type !== Function && !i.skipFactory && St(l)) {
        const { propsDefaults: u } = s;
        if (n in u) r = u[n];
        else {
          const d = ed(s);
          r = u[n] = l.call(null, t), d();
        }
      } else r = l;
      s.ce && s.ce._setProp(n, r);
    }
    i[0] && (o && !a ? r = !1 : i[1] && (r === "" || r === si(n)) && (r = !0));
  }
  return r;
}
var RO = /* @__PURE__ */ new WeakMap();
function sx(e, t, n = !1) {
  const r = n ? RO : t.propsCache, s = r.get(e);
  if (s) return s;
  const o = e.props, i = {}, a = [];
  let l = !1;
  if (!St(e)) {
    const d = (h) => {
      l = !0;
      const [p, y] = sx(h, t, !0);
      fn(i, p), y && a.push(...y);
    };
    !n && t.mixins.length && t.mixins.forEach(d), e.extends && d(e.extends), e.mixins && e.mixins.forEach(d);
  }
  if (!o && !l)
    return Gt(e) && r.set(e, nl), nl;
  if (gt(o)) for (let d = 0; d < o.length; d++) {
    const h = os(o[d]);
    d0(h) && (i[h] = Wt);
  }
  else if (o) for (const d in o) {
    const h = os(d);
    if (d0(h)) {
      const p = o[d], y = i[h] = gt(p) || St(p) ? { type: p } : fn({}, p), w = y.type;
      let E = !1, C = !0;
      if (gt(w)) for (let M = 0; M < w.length; ++M) {
        const k = w[M], R = St(k) && k.name;
        if (R === "Boolean") {
          E = !0;
          break;
        } else R === "String" && (C = !1);
      }
      else E = St(w) && w.name === "Boolean";
      y[0] = E, y[1] = C, (E || Ot(y, "default")) && a.push(h);
    }
  }
  const u = [i, a];
  return Gt(e) && r.set(e, u), u;
}
function d0(e) {
  return e[0] !== "$" && !sc(e);
}
var tb = (e) => e === "_" || e === "_ctx" || e === "$stable", nb = (e) => gt(e) ? e.map(As) : [As(e)], NO = (e, t, n) => {
  if (t._n) return t;
  const r = eO((...s) => nb(t(...s)), n);
  return r._c = !1, r;
}, ox = (e, t, n) => {
  const r = e._ctx;
  for (const s in e) {
    if (tb(s)) continue;
    const o = e[s];
    if (St(o)) t[s] = NO(s, o, r);
    else if (o != null) {
      const i = nb(o);
      t[s] = () => i;
    }
  }
}, ix = (e, t) => {
  const n = nb(t);
  e.slots.default = () => n;
}, ax = (e, t, n) => {
  for (const r in t) (n || !tb(r)) && (e[r] = t[r]);
}, $O = (e, t, n) => {
  const r = e.slots = tx();
  if (e.vnode.shapeFlag & 32) {
    const s = t._;
    s ? (ax(r, t, n), n && gI(r, "_", s, !0)) : ox(t, r);
  } else t && ix(e, t);
}, DO = (e, t, n) => {
  const { vnode: r, slots: s } = e;
  let o = !0, i = Wt;
  if (r.shapeFlag & 32) {
    const a = t._;
    a ? n && a === 1 ? o = !1 : ax(s, t, n) : (o = !t.$stable, ox(t, s)), i = t;
  } else t && (ix(e, t), i = { default: 1 });
  if (o)
    for (const a in s) !tb(a) && i[a] == null && delete s[a];
};
var Zn = UO;
function LO(e) {
  return OO(e);
}
function OO(e, t) {
  const n = Pp();
  n.__VUE__ = !0;
  const { insert: r, remove: s, patchProp: o, createElement: i, createText: a, createComment: l, setText: u, setElementText: d, parentNode: h, nextSibling: p, setScopeId: y = Rs, insertStaticContent: w } = e, E = (W, ee, ue, Se = null, be = null, _e = null, Pe = void 0, xe = null, ke = !!ee.dynamicChildren) => {
    if (W === ee) return;
    W && !cu(W, ee) && (Se = Ie(W), pe(W, be, _e, !0), W = null), ee.patchFlag === -2 && (ke = !1, ee.dynamicChildren = null);
    const { type: we, ref: Ve, shapeFlag: Re } = ee;
    switch (we) {
      case Lp:
        C(W, ee, ue, Se);
        break;
      case Qo:
        M(W, ee, ue, Se);
        break;
      case Wf:
        W == null && k(ee, ue, Se, Pe);
        break;
      case Ge:
        L(W, ee, ue, Se, be, _e, Pe, xe, ke);
        break;
      default:
        Re & 1 ? m(W, ee, ue, Se, be, _e, Pe, xe, ke) : Re & 6 ? G(W, ee, ue, Se, be, _e, Pe, xe, ke) : (Re & 64 || Re & 128) && we.process(W, ee, ue, Se, be, _e, Pe, xe, ke, it);
    }
    Ve != null && be ? lc(Ve, W && W.ref, _e, ee || W, !ee) : Ve == null && W && W.ref != null && lc(W.ref, null, _e, W, !0);
  }, C = (W, ee, ue, Se) => {
    if (W == null) r(ee.el = a(ee.children), ue, Se);
    else {
      const be = ee.el = W.el;
      ee.children !== W.children && u(be, ee.children);
    }
  }, M = (W, ee, ue, Se) => {
    W == null ? r(ee.el = l(ee.children || ""), ue, Se) : ee.el = W.el;
  }, k = (W, ee, ue, Se) => {
    [W.el, W.anchor] = w(W.children, ee, ue, Se, W.el, W.anchor);
  }, R = ({ el: W, anchor: ee }, ue, Se) => {
    let be;
    for (; W && W !== ee; )
      be = p(W), r(W, ue, Se), W = be;
    r(ee, ue, Se);
  }, D = ({ el: W, anchor: ee }) => {
    let ue;
    for (; W && W !== ee; )
      ue = p(W), s(W), W = ue;
    s(ee);
  }, m = (W, ee, ue, Se, be, _e, Pe, xe, ke) => {
    if (ee.type === "svg" ? Pe = "svg" : ee.type === "math" && (Pe = "mathml"), W == null) b(ee, ue, Se, be, _e, Pe, xe, ke);
    else {
      const we = W.el && W.el._isVueCE ? W.el : null;
      try {
        we && we._beginPatch(), I(W, ee, be, _e, Pe, xe, ke);
      } finally {
        we && we._endPatch();
      }
    }
  }, b = (W, ee, ue, Se, be, _e, Pe, xe) => {
    let ke, we;
    const { props: Ve, shapeFlag: Re, transition: $e, dirs: Le } = W;
    if (ke = W.el = i(W.type, _e, Ve && Ve.is, Ve), Re & 8 ? d(ke, W.children) : Re & 16 && T(W.children, ke, null, Se, be, Ag(W, _e), Pe, xe), Le && Ti(W, null, Se, "created"), v(ke, W, W.scopeId, Pe, Se), Ve) {
      for (const nt in Ve) nt !== "value" && !sc(nt) && o(ke, nt, null, Ve[nt], _e, Se);
      "value" in Ve && o(ke, "value", null, Ve.value, _e), (we = Ve.onVnodeBeforeMount) && ys(we, Se, W);
    }
    Le && Ti(W, null, Se, "beforeMount");
    const tt = FO(be, $e);
    tt && $e.beforeEnter(ke), r(ke, ee, ue), ((we = Ve && Ve.onVnodeMounted) || tt || Le) && Zn(() => {
      we && ys(we, Se, W), tt && $e.enter(ke), Le && Ti(W, null, Se, "mounted");
    }, be);
  }, v = (W, ee, ue, Se, be) => {
    if (ue && y(W, ue), Se) for (let _e = 0; _e < Se.length; _e++) y(W, Se[_e]);
    if (be) {
      let _e = be.subTree;
      if (ee === _e || dx(_e.type) && (_e.ssContent === ee || _e.ssFallback === ee)) {
        const Pe = be.vnode;
        v(W, Pe, Pe.scopeId, Pe.slotScopeIds, be.parent);
      }
    }
  }, T = (W, ee, ue, Se, be, _e, Pe, xe, ke = 0) => {
    for (let we = ke; we < W.length; we++) E(null, W[we] = xe ? js(W[we]) : As(W[we]), ee, ue, Se, be, _e, Pe, xe);
  }, I = (W, ee, ue, Se, be, _e, Pe) => {
    const xe = ee.el = W.el;
    let { patchFlag: ke, dynamicChildren: we, dirs: Ve } = ee;
    ke |= W.patchFlag & 16;
    const Re = W.props || Wt, $e = ee.props || Wt;
    let Le;
    if (ue && Ei(ue, !1), (Le = $e.onVnodeBeforeUpdate) && ys(Le, ue, ee, W), Ve && Ti(ee, W, ue, "beforeUpdate"), ue && Ei(ue, !0), (Re.innerHTML && $e.innerHTML == null || Re.textContent && $e.textContent == null) && d(xe, ""), we ? x(W.dynamicChildren, we, xe, ue, Se, Ag(ee, be), _e) : Pe || Y(W, ee, xe, null, ue, Se, Ag(ee, be), _e, !1), ke > 0) {
      if (ke & 16) P(xe, Re, $e, ue, be);
      else if (ke & 2 && Re.class !== $e.class && o(xe, "class", null, $e.class, be), ke & 4 && o(xe, "style", Re.style, $e.style, be), ke & 8) {
        const tt = ee.dynamicProps;
        for (let nt = 0; nt < tt.length; nt++) {
          const at = tt[nt], mt = Re[at], ft = $e[at];
          (ft !== mt || at === "value") && o(xe, at, mt, ft, be, ue);
        }
      }
      ke & 1 && W.children !== ee.children && d(xe, ee.children);
    } else !Pe && we == null && P(xe, Re, $e, ue, be);
    ((Le = $e.onVnodeUpdated) || Ve) && Zn(() => {
      Le && ys(Le, ue, ee, W), Ve && Ti(ee, W, ue, "updated");
    }, Se);
  }, x = (W, ee, ue, Se, be, _e, Pe) => {
    for (let xe = 0; xe < ee.length; xe++) {
      const ke = W[xe], we = ee[xe];
      E(ke, we, ke.el && (ke.type === Ge || !cu(ke, we) || ke.shapeFlag & 198) ? h(ke.el) : ue, null, Se, be, _e, Pe, !0);
    }
  }, P = (W, ee, ue, Se, be) => {
    if (ee !== ue) {
      if (ee !== Wt)
        for (const _e in ee) !sc(_e) && !(_e in ue) && o(W, _e, ee[_e], null, be, Se);
      for (const _e in ue) {
        if (sc(_e)) continue;
        const Pe = ue[_e], xe = ee[_e];
        Pe !== xe && _e !== "value" && o(W, _e, xe, Pe, be, Se);
      }
      "value" in ue && o(W, "value", ee.value, ue.value, be);
    }
  }, L = (W, ee, ue, Se, be, _e, Pe, xe, ke) => {
    const we = ee.el = W ? W.el : a(""), Ve = ee.anchor = W ? W.anchor : a("");
    let { patchFlag: Re, dynamicChildren: $e, slotScopeIds: Le } = ee;
    Le && (xe = xe ? xe.concat(Le) : Le), W == null ? (r(we, ue, Se), r(Ve, ue, Se), T(ee.children || [], ue, Ve, be, _e, Pe, xe, ke)) : Re > 0 && Re & 64 && $e && W.dynamicChildren && W.dynamicChildren.length === $e.length ? (x(W.dynamicChildren, $e, ue, be, _e, Pe, xe), (ee.key != null || be && ee === be.subTree) && lx(W, ee, !0)) : Y(W, ee, ue, Ve, be, _e, Pe, xe, ke);
  }, G = (W, ee, ue, Se, be, _e, Pe, xe, ke) => {
    ee.slotScopeIds = xe, W == null ? ee.shapeFlag & 512 ? be.ctx.activate(ee, ue, Se, Pe, ke) : z(ee, ue, Se, be, _e, Pe, ke) : V(W, ee, ke);
  }, z = (W, ee, ue, Se, be, _e, Pe) => {
    const xe = W.component = zO(W, Se, be);
    if (VI(W) && (xe.ctx.renderer = it), YO(xe, !1, Pe), xe.asyncDep) {
      if (be && be.registerDep(xe, Z, Pe), !W.el) {
        const ke = xe.subTree = Ft(Qo);
        M(null, ke, ee, ue), W.placeholder = ke.el;
      }
    } else Z(xe, W, ee, ue, be, _e, Pe);
  }, V = (W, ee, ue) => {
    const Se = ee.component = W.component;
    if (xO(W, ee, ue)) if (Se.asyncDep && !Se.asyncResolved) {
      O(Se, ee, ue);
      return;
    } else
      Se.next = ee, Se.update();
    else
      ee.el = W.el, Se.vnode = ee;
  }, Z = (W, ee, ue, Se, be, _e, Pe) => {
    const xe = () => {
      if (W.isMounted) {
        let { next: Re, bu: $e, u: Le, parent: tt, vnode: nt } = W;
        {
          const At = ux(W);
          if (At) {
            Re && (Re.el = nt.el, O(W, Re, Pe)), At.asyncDep.then(() => {
              Zn(() => {
                W.isUnmounted || we();
              }, be);
            });
            return;
          }
        }
        let at = Re, mt;
        Ei(W, !1), Re ? (Re.el = nt.el, O(W, Re, Pe)) : Re = nt, $e && Gf($e), (mt = Re.props && Re.props.onVnodeBeforeUpdate) && ys(mt, tt, Re, nt), Ei(W, !0);
        const ft = Cg(W), Pt = W.subTree;
        W.subTree = ft, E(Pt, ft, h(Pt.el), Ie(Pt), W, be, _e), Re.el = ft.el, at === null && kO(W, ft.el), Le && Zn(Le, be), (mt = Re.props && Re.props.onVnodeUpdated) && Zn(() => ys(mt, tt, Re, nt), be);
      } else {
        let Re;
        const { el: $e, props: Le } = ee, { bm: tt, m: nt, parent: at, root: mt, type: ft } = W, Pt = uc(ee);
        if (Ei(W, !1), tt && Gf(tt), !Pt && (Re = Le && Le.onVnodeBeforeMount) && ys(Re, at, ee), Ei(W, !0), $e && je) {
          const At = () => {
            W.subTree = Cg(W), je($e, W.subTree, W, be, null);
          };
          Pt && ft.__asyncHydrate ? ft.__asyncHydrate($e, W, At) : At();
        } else {
          mt.ce && mt.ce._hasShadowRoot() && mt.ce._injectChildStyle(ft, W.parent ? W.parent.type : void 0);
          const At = W.subTree = Cg(W);
          E(null, At, ue, Se, W, be, _e), ee.el = At.el;
        }
        if (nt && Zn(nt, be), !Pt && (Re = Le && Le.onVnodeMounted)) {
          const At = ee;
          Zn(() => ys(Re, at, At), be);
        }
        (ee.shapeFlag & 256 || at && uc(at.vnode) && at.vnode.shapeFlag & 256) && W.a && Zn(W.a, be), W.isMounted = !0, ee = ue = Se = null;
      }
    };
    W.scope.on();
    const ke = W.effect = new wI(xe);
    W.scope.off();
    const we = W.update = ke.run.bind(ke), Ve = W.job = ke.runIfDirty.bind(ke);
    Ve.i = W, Ve.id = W.uid, ke.scheduler = () => Q_(Ve), Ei(W, !0), we();
  }, O = (W, ee, ue) => {
    ee.component = W;
    const Se = W.vnode.props;
    W.vnode = ee, W.next = null, PO(W, ee.props, Se, ue), DO(W, ee.children, ue), uo(), r0(W), co();
  }, Y = (W, ee, ue, Se, be, _e, Pe, xe, ke = !1) => {
    const we = W && W.children, Ve = W ? W.shapeFlag : 0, Re = ee.children, { patchFlag: $e, shapeFlag: Le } = ee;
    if ($e > 0) {
      if ($e & 128) {
        j(we, Re, ue, Se, be, _e, Pe, xe, ke);
        return;
      } else if ($e & 256) {
        X(we, Re, ue, Se, be, _e, Pe, xe, ke);
        return;
      }
    }
    Le & 8 ? (Ve & 16 && le(we, be, _e), Re !== we && d(ue, Re)) : Ve & 16 ? Le & 16 ? j(we, Re, ue, Se, be, _e, Pe, xe, ke) : le(we, be, _e, !0) : (Ve & 8 && d(ue, ""), Le & 16 && T(Re, ue, Se, be, _e, Pe, xe, ke));
  }, X = (W, ee, ue, Se, be, _e, Pe, xe, ke) => {
    W = W || nl, ee = ee || nl;
    const we = W.length, Ve = ee.length, Re = Math.min(we, Ve);
    let $e;
    for ($e = 0; $e < Re; $e++) {
      const Le = ee[$e] = ke ? js(ee[$e]) : As(ee[$e]);
      E(W[$e], Le, ue, null, be, _e, Pe, xe, ke);
    }
    we > Ve ? le(W, be, _e, !0, !1, Re) : T(ee, ue, Se, be, _e, Pe, xe, ke, Re);
  }, j = (W, ee, ue, Se, be, _e, Pe, xe, ke) => {
    let we = 0;
    const Ve = ee.length;
    let Re = W.length - 1, $e = Ve - 1;
    for (; we <= Re && we <= $e; ) {
      const Le = W[we], tt = ee[we] = ke ? js(ee[we]) : As(ee[we]);
      if (cu(Le, tt)) E(Le, tt, ue, null, be, _e, Pe, xe, ke);
      else break;
      we++;
    }
    for (; we <= Re && we <= $e; ) {
      const Le = W[Re], tt = ee[$e] = ke ? js(ee[$e]) : As(ee[$e]);
      if (cu(Le, tt)) E(Le, tt, ue, null, be, _e, Pe, xe, ke);
      else break;
      Re--, $e--;
    }
    if (we > Re) {
      if (we <= $e) {
        const Le = $e + 1, tt = Le < Ve ? ee[Le].el : Se;
        for (; we <= $e; )
          E(null, ee[we] = ke ? js(ee[we]) : As(ee[we]), ue, tt, be, _e, Pe, xe, ke), we++;
      }
    } else if (we > $e) for (; we <= Re; )
      pe(W[we], be, _e, !0), we++;
    else {
      const Le = we, tt = we, nt = /* @__PURE__ */ new Map();
      for (we = tt; we <= $e; we++) {
        const qt = ee[we] = ke ? js(ee[we]) : As(ee[we]);
        qt.key != null && nt.set(qt.key, we);
      }
      let at, mt = 0;
      const ft = $e - tt + 1;
      let Pt = !1, At = 0;
      const Ut = new Array(ft);
      for (we = 0; we < ft; we++) Ut[we] = 0;
      for (we = Le; we <= Re; we++) {
        const qt = W[we];
        if (mt >= ft) {
          pe(qt, be, _e, !0);
          continue;
        }
        let Nt;
        if (qt.key != null) Nt = nt.get(qt.key);
        else for (at = tt; at <= $e; at++) if (Ut[at - tt] === 0 && cu(qt, ee[at])) {
          Nt = at;
          break;
        }
        Nt === void 0 ? pe(qt, be, _e, !0) : (Ut[Nt - tt] = we + 1, Nt >= At ? At = Nt : Pt = !0, E(qt, ee[Nt], ue, null, be, _e, Pe, xe, ke), mt++);
      }
      const Mt = Pt ? BO(Ut) : nl;
      for (at = Mt.length - 1, we = ft - 1; we >= 0; we--) {
        const qt = tt + we, Nt = ee[qt], Yn = ee[qt + 1], Or = qt + 1 < Ve ? Yn.el || cx(Yn) : Se;
        Ut[we] === 0 ? E(null, Nt, ue, Or, be, _e, Pe, xe, ke) : Pt && (at < 0 || we !== Mt[at] ? re(Nt, ue, Or, 2) : at--);
      }
    }
  }, re = (W, ee, ue, Se, be = null) => {
    const { el: _e, type: Pe, transition: xe, children: ke, shapeFlag: we } = W;
    if (we & 6) {
      re(W.component.subTree, ee, ue, Se);
      return;
    }
    if (we & 128) {
      W.suspense.move(ee, ue, Se);
      return;
    }
    if (we & 64) {
      Pe.move(W, ee, ue, it);
      return;
    }
    if (Pe === Ge) {
      r(_e, ee, ue);
      for (let Ve = 0; Ve < ke.length; Ve++) re(ke[Ve], ee, ue, Se);
      r(W.anchor, ee, ue);
      return;
    }
    if (Pe === Wf) {
      R(W, ee, ue);
      return;
    }
    if (Se !== 2 && we & 1 && xe) if (Se === 0) xe.persisted && !_e[Tg] ? r(_e, ee, ue) : (xe.beforeEnter(_e), r(_e, ee, ue), Zn(() => xe.enter(_e), be));
    else {
      const { leave: Ve, delayLeave: Re, afterLeave: $e } = xe, Le = () => {
        W.ctx.isUnmounted ? s(_e) : r(_e, ee, ue);
      }, tt = () => {
        const nt = _e._isLeaving || !!_e[Tg];
        _e._isLeaving && _e[Tg](!0), xe.persisted && !nt ? Le() : Ve(_e, () => {
          Le(), $e && $e();
        });
      };
      Re ? Re(_e, Le, tt) : tt();
    }
    else r(_e, ee, ue);
  }, pe = (W, ee, ue, Se = !1, be = !1) => {
    const { type: _e, props: Pe, ref: xe, children: ke, dynamicChildren: we, shapeFlag: Ve, patchFlag: Re, dirs: $e, cacheIndex: Le, memo: tt } = W;
    if (Re === -2 && (be = !1), xe != null && (uo(), lc(xe, null, ue, W, !0), co()), Le != null && (ee.renderCache[Le] = void 0), Ve & 256) {
      ee.ctx.deactivate(W);
      return;
    }
    const nt = Ve & 1 && $e, at = !uc(W);
    let mt;
    if (at && (mt = Pe && Pe.onVnodeBeforeUnmount) && ys(mt, ee, W), Ve & 6) H(W.component, ue, Se);
    else {
      if (Ve & 128) {
        W.suspense.unmount(ue, Se);
        return;
      }
      nt && Ti(W, null, ee, "beforeUnmount"), Ve & 64 ? W.type.remove(W, ee, ue, it, Se) : we && !we.hasOnce && (_e !== Ge || Re > 0 && Re & 64) ? le(we, ee, ue, !1, !0) : (_e === Ge && Re & 384 || !be && Ve & 16) && le(ke, ee, ue), Se && B(W);
    }
    const ft = tt != null && Le == null;
    (at && (mt = Pe && Pe.onVnodeUnmounted) || nt || ft) && Zn(() => {
      mt && ys(mt, ee, W), nt && Ti(W, null, ee, "unmounted"), ft && (W.el = null);
    }, ue);
  }, B = (W) => {
    const { type: ee, el: ue, anchor: Se, transition: be } = W;
    if (ee === Ge) {
      N(ue, Se);
      return;
    }
    if (ee === Wf) {
      D(W);
      return;
    }
    const _e = () => {
      s(ue), be && !be.persisted && be.afterLeave && be.afterLeave();
    };
    if (W.shapeFlag & 1 && be && !be.persisted) {
      const { leave: Pe, delayLeave: xe } = be, ke = () => Pe(ue, _e);
      xe ? xe(W.el, _e, ke) : ke();
    } else _e();
  }, N = (W, ee) => {
    let ue;
    for (; W !== ee; )
      ue = p(W), s(W), W = ue;
    s(ee);
  }, H = (W, ee, ue) => {
    const { bum: Se, scope: be, job: _e, subTree: Pe, um: xe, m: ke, a: we } = W;
    f0(ke), f0(we), Se && Gf(Se), be.stop(), _e && (_e.flags |= 8, pe(Pe, W, ee, ue)), xe && Zn(xe, ee), Zn(() => {
      W.isUnmounted = !0;
    }, ee);
  }, le = (W, ee, ue, Se = !1, be = !1, _e = 0) => {
    for (let Pe = _e; Pe < W.length; Pe++) pe(W[Pe], ee, ue, Se, be);
  }, Ie = (W) => {
    if (W.shapeFlag & 6) return Ie(W.component.subTree);
    if (W.shapeFlag & 128) return W.suspense.next();
    const ee = p(W.anchor || W.el), ue = ee && ee[sO];
    return ue ? p(ue) : ee;
  };
  let De = !1;
  const Be = (W, ee, ue) => {
    let Se;
    W == null ? ee._vnode && (pe(ee._vnode, null, null, !0), Se = ee._vnode.component) : E(ee._vnode || null, W, ee, null, null, null, ue), ee._vnode = W, De || (De = !0, r0(Se), FI(), De = !1);
  }, it = {
    p: E,
    um: pe,
    m: re,
    r: B,
    mt: z,
    mc: T,
    pc: Y,
    pbc: x,
    n: Ie,
    o: e
  };
  let Qe, je;
  return t && ([Qe, je] = t(it)), {
    render: Be,
    hydrate: Qe,
    createApp: SO(Be, Qe)
  };
}
function Ag({ type: e, props: t }, n) {
  return n === "svg" && e === "foreignObject" || n === "mathml" && e === "annotation-xml" && t && t.encoding && t.encoding.includes("html") ? void 0 : n;
}
function Ei({ effect: e, job: t }, n) {
  n ? (e.flags |= 32, t.flags |= 4) : (e.flags &= -33, t.flags &= -5);
}
function FO(e, t) {
  return (!e || e && !e.pendingBranch) && t && !t.persisted;
}
function lx(e, t, n = !1) {
  const r = e.children, s = t.children;
  if (gt(r) && gt(s)) for (let o = 0; o < r.length; o++) {
    const i = r[o];
    let a = s[o];
    a.shapeFlag & 1 && !a.dynamicChildren && ((a.patchFlag <= 0 || a.patchFlag === 32) && (a = s[o] = js(s[o]), a.el = i.el), !n && a.patchFlag !== -2 && lx(i, a)), a.type === Lp && (a.patchFlag === -1 && (a = s[o] = js(a)), a.el = i.el), a.type === Qo && !a.el && (a.el = i.el);
  }
}
function BO(e) {
  const t = e.slice(), n = [0];
  let r, s, o, i, a;
  const l = e.length;
  for (r = 0; r < l; r++) {
    const u = e[r];
    if (u !== 0) {
      if (s = n[n.length - 1], e[s] < u) {
        t[r] = s, n.push(r);
        continue;
      }
      for (o = 0, i = n.length - 1; o < i; )
        a = o + i >> 1, e[n[a]] < u ? o = a + 1 : i = a;
      u < e[n[o]] && (o > 0 && (t[r] = n[o - 1]), n[o] = r);
    }
  }
  for (o = n.length, i = n[o - 1]; o-- > 0; )
    n[o] = i, i = t[i];
  return n;
}
function ux(e) {
  const t = e.subTree.component;
  if (t) return t.asyncDep && !t.asyncResolved ? t : ux(t);
}
function f0(e) {
  if (e) for (let t = 0; t < e.length; t++) e[t].flags |= 8;
}
function cx(e) {
  if (e.placeholder) return e.placeholder;
  const t = e.component;
  return t ? cx(t.subTree) : null;
}
var dx = (e) => e.__isSuspense;
function UO(e, t) {
  t && t.pendingBranch ? gt(e) ? t.effects.push(...e) : t.effects.push(e) : jL(e);
}
var Ge = /* @__PURE__ */ Symbol.for("v-fgt"), Lp = /* @__PURE__ */ Symbol.for("v-txt"), Qo = /* @__PURE__ */ Symbol.for("v-cmt"), Wf = /* @__PURE__ */ Symbol.for("v-stc"), dc = [], Mr = null;
function U(e = !1) {
  dc.push(Mr = e ? null : []);
}
function qO() {
  dc.pop(), Mr = dc[dc.length - 1] || null;
}
var xc = 1;
function h0(e, t = !1) {
  xc += e, e < 0 && Mr && t && (Mr.hasOnce = !0);
}
function fx(e) {
  return e.dynamicChildren = xc > 0 ? Mr || nl : null, qO(), xc > 0 && Mr && Mr.push(e), e;
}
function q(e, t, n, r, s, o) {
  return fx(_(e, t, n, r, s, o, !0));
}
function Zs(e, t, n, r, s) {
  return fx(Ft(e, t, n, r, s, !0));
}
function hx(e) {
  return e ? e.__v_isVNode === !0 : !1;
}
function cu(e, t) {
  return e.type === t.type && e.key === t.key;
}
var px = ({ key: e }) => e ?? null, Vf = ({ ref: e, ref_key: t, ref_for: n }) => (typeof e == "number" && (e = "" + e), e != null ? tn(e) || /* @__PURE__ */ Xt(e) || St(e) ? {
  i: Gr,
  r: e,
  k: t,
  f: !!n
} : e : null);
function _(e, t = null, n = null, r = 0, s = null, o = e === Ge ? 0 : 1, i = !1, a = !1) {
  const l = {
    __v_isVNode: !0,
    __v_skip: !0,
    type: e,
    props: t,
    key: t && px(t),
    ref: t && Vf(t),
    scopeId: UI,
    slotScopeIds: null,
    children: n,
    component: null,
    suspense: null,
    ssContent: null,
    ssFallback: null,
    dirs: null,
    transition: null,
    el: null,
    anchor: null,
    target: null,
    targetStart: null,
    targetAnchor: null,
    staticCount: 0,
    shapeFlag: o,
    patchFlag: r,
    dynamicProps: s,
    dynamicChildren: null,
    appContext: null,
    ctx: Gr
  };
  return a ? (rb(l, n), o & 128 && e.normalize(l)) : n && (l.shapeFlag |= tn(n) ? 8 : 16), xc > 0 && !i && Mr && (l.patchFlag > 0 || o & 6) && l.patchFlag !== 32 && Mr.push(l), l;
}
var Ft = HO;
function HO(e, t = null, n = null, r = 0, s = null, o = !1) {
  if ((!e || e === pO) && (e = Qo), hx(e)) {
    const a = yl(e, t, !0);
    return n && rb(a, n), xc > 0 && !o && Mr && (a.shapeFlag & 6 ? Mr[Mr.indexOf(e)] = a : Mr.push(a)), a.patchFlag = -2, a;
  }
  if (jO(e) && (e = e.__vccOpts), t) {
    t = GO(t);
    let { class: a, style: l } = t;
    a && !tn(a) && (t.class = Fe(a)), Gt(l) && (/* @__PURE__ */ X_(l) && !gt(l) && (l = fn({}, l)), t.style = Ir(l));
  }
  const i = tn(e) ? 1 : dx(e) ? 128 : oO(e) ? 64 : Gt(e) ? 4 : St(e) ? 2 : 0;
  return _(e, t, n, r, s, i, o, !0);
}
function GO(e) {
  return e ? /* @__PURE__ */ X_(e) || nx(e) ? fn({}, e) : e : null;
}
function yl(e, t, n = !1, r = !1) {
  const { props: s, ref: o, patchFlag: i, children: a, transition: l } = e, u = t ? WO(s || {}, t) : s, d = {
    __v_isVNode: !0,
    __v_skip: !0,
    type: e.type,
    props: u,
    key: u && px(u),
    ref: t && t.ref ? n && o ? gt(o) ? o.concat(Vf(t)) : [o, Vf(t)] : Vf(t) : o,
    scopeId: e.scopeId,
    slotScopeIds: e.slotScopeIds,
    children: a,
    target: e.target,
    targetStart: e.targetStart,
    targetAnchor: e.targetAnchor,
    staticCount: e.staticCount,
    shapeFlag: e.shapeFlag,
    patchFlag: t && e.type !== Ge ? i === -1 ? 16 : i | 16 : i,
    dynamicProps: e.dynamicProps,
    dynamicChildren: e.dynamicChildren,
    appContext: e.appContext,
    dirs: e.dirs,
    transition: l,
    component: e.component,
    suspense: e.suspense,
    ssContent: e.ssContent && yl(e.ssContent),
    ssFallback: e.ssFallback && yl(e.ssFallback),
    placeholder: e.placeholder,
    el: e.el,
    anchor: e.anchor,
    ctx: e.ctx,
    ce: e.ce
  };
  return l && r && Z_(d, l.clone(d)), d;
}
function er(e = " ", t = 0) {
  return Ft(Lp, null, e, t);
}
function kl(e, t) {
  const n = Ft(Wf, null, e);
  return n.staticCount = t, n;
}
function ve(e = "", t = !1) {
  return t ? (U(), Zs(Qo, null, e)) : Ft(Qo, null, e);
}
function As(e) {
  return e == null || typeof e == "boolean" ? Ft(Qo) : gt(e) ? Ft(Ge, null, e.slice()) : hx(e) ? js(e) : Ft(Lp, null, String(e));
}
function js(e) {
  return e.el === null && e.patchFlag !== -1 || e.memo ? e : yl(e);
}
function rb(e, t) {
  let n = 0;
  const { shapeFlag: r } = e;
  if (t == null) t = null;
  else if (gt(t)) n = 16;
  else if (typeof t == "object") if (r & 65) {
    const s = t.default;
    s && (s._c && (s._d = !1), rb(e, s()), s._c && (s._d = !0));
    return;
  } else {
    n = 32;
    const s = t._;
    !s && !nx(t) ? t._ctx = Gr : s === 3 && Gr && (Gr.slots._ === 1 ? t._ = 1 : (t._ = 2, e.patchFlag |= 1024));
  }
  else St(t) ? (t = {
    default: t,
    _ctx: Gr
  }, n = 32) : (t = String(t), r & 64 ? (n = 16, t = [er(t)]) : n = 8);
  e.children = t, e.shapeFlag |= n;
}
function WO(...e) {
  const t = {};
  for (let n = 0; n < e.length; n++) {
    const r = e[n];
    for (const s in r) if (s === "class")
      t.class !== r.class && (t.class = Fe([t.class, r.class]));
    else if (s === "style") t.style = Ir([t.style, r.style]);
    else if (Ap(s)) {
      const o = t[s], i = r[s];
      i && o !== i && !(gt(o) && o.includes(i)) ? t[s] = o ? [].concat(o, i) : i : i == null && o == null && !Ip(s) && (t[s] = i);
    } else s !== "" && (t[s] = r[s]);
  }
  return t;
}
function ys(e, t, n, r = null) {
  ds(e, t, 7, [n, r]);
}
var VO = QI(), KO = 0;
function zO(e, t, n) {
  const r = e.type, s = (t ? t.appContext : e.appContext) || VO, o = {
    uid: KO++,
    vnode: e,
    type: r,
    parent: t,
    appContext: s,
    root: null,
    next: null,
    subTree: null,
    effect: null,
    update: null,
    job: null,
    scope: new TL(!0),
    render: null,
    proxy: null,
    exposed: null,
    exposeProxy: null,
    withProxy: null,
    provides: t ? t.provides : Object.create(s.provides),
    ids: t ? t.ids : [
      "",
      0,
      0
    ],
    accessCache: null,
    renderCache: [],
    components: null,
    directives: null,
    propsOptions: sx(r, s),
    emitsOptions: ZI(r, s),
    emit: null,
    emitted: null,
    propsDefaults: Wt,
    inheritAttrs: r.inheritAttrs,
    ctx: Wt,
    data: Wt,
    props: Wt,
    attrs: Wt,
    slots: Wt,
    refs: Wt,
    setupState: Wt,
    setupContext: null,
    suspense: n,
    suspenseId: n ? n.pendingId : 0,
    asyncDep: null,
    asyncResolved: !1,
    isMounted: !1,
    isUnmounted: !1,
    isDeactivated: !1,
    bc: null,
    c: null,
    bm: null,
    m: null,
    bu: null,
    u: null,
    um: null,
    bum: null,
    da: null,
    a: null,
    rtg: null,
    rtc: null,
    ec: null,
    sp: null
  };
  return o.ctx = { _: o }, o.root = t ? t.root : o, o.emit = EO.bind(null, o), e.ce && e.ce(o), o;
}
var zn = null, JO = () => zn || Gr, Ih, Hv;
{
  const e = Pp(), t = (n, r) => {
    let s;
    return (s = e[n]) || (s = e[n] = []), s.push(r), (o) => {
      s.length > 1 ? s.forEach((i) => i(o)) : s[0](o);
    };
  };
  Ih = t("__VUE_INSTANCE_SETTERS__", (n) => zn = n), Hv = t("__VUE_SSR_SETTERS__", (n) => kc = n);
}
var ed = (e) => {
  const t = zn;
  return Ih(e), e.scope.on(), () => {
    e.scope.off(), Ih(t);
  };
}, p0 = () => {
  zn && zn.scope.off(), Ih(null);
};
function mx(e) {
  return e.vnode.shapeFlag & 4;
}
var kc = !1;
function YO(e, t = !1, n = !1) {
  t && Hv(t);
  const { props: r, children: s } = e.vnode, o = mx(e);
  MO(e, r, o, t), $O(e, s, n || t);
  const i = o ? XO(e, t) : void 0;
  return t && Hv(!1), i;
}
function XO(e, t) {
  const n = e.type;
  e.accessCache = /* @__PURE__ */ Object.create(null), e.proxy = new Proxy(e.ctx, mO);
  const { setup: r } = n;
  if (r) {
    uo();
    const s = e.setupContext = r.length > 1 ? ZO(e) : null, o = ed(e), i = jc(r, e, 0, [e.props, s]), a = fI(i);
    if (co(), o(), (a || e.sp) && !uc(e) && WI(e), a) {
      if (i.then(p0, p0), t) return i.then((l) => {
        m0(e, l, t);
      }).catch((l) => {
        Np(l, e, 0);
      });
      e.asyncDep = i;
    } else m0(e, i, t);
  } else gx(e, t);
}
function m0(e, t, n) {
  St(t) ? e.type.__ssrInlineRender ? e.ssrRender = t : e.render = t : Gt(t) && (e.setupState = DI(t)), gx(e, n);
}
var g0, v0;
function gx(e, t, n) {
  const r = e.type;
  if (!e.render) {
    if (!t && g0 && !r.render) {
      const s = r.template || eb(e).template;
      if (s) {
        const { isCustomElement: o, compilerOptions: i } = e.appContext.config, { delimiters: a, compilerOptions: l } = r, u = fn(fn({
          isCustomElement: o,
          delimiters: a
        }, i), l);
        r.render = g0(s, u);
      }
    }
    e.render = r.render || Rs, v0 && v0(e);
  }
  {
    const s = ed(e);
    uo();
    try {
      gO(e);
    } finally {
      co(), s();
    }
  }
}
var QO = { get(e, t) {
  return $n(e, "get", ""), e[t];
} };
function ZO(e) {
  const t = (n) => {
    e.exposed = n || {};
  };
  return {
    attrs: new Proxy(e.attrs, QO),
    slots: e.slots,
    emit: e.emit,
    expose: t
  };
}
function Op(e) {
  return e.exposed ? e.exposeProxy || (e.exposeProxy = new Proxy(DI(GL(e.exposed)), {
    get(t, n) {
      if (n in t) return t[n];
      if (n in cc) return cc[n](e);
    },
    has(t, n) {
      return n in t || n in cc;
    }
  })) : e.proxy;
}
function jO(e) {
  return St(e) && "__vccOpts" in e;
}
var ie = (e, t) => /* @__PURE__ */ JL(e, t, kc), eF = "3.5.35", Gv = void 0, y0 = typeof window < "u" && window.trustedTypes;
if (y0) try {
  Gv = /* @__PURE__ */ y0.createPolicy("vue", { createHTML: (e) => e });
} catch {
}
var vx = Gv ? (e) => Gv.createHTML(e) : (e) => e, tF = "http://www.w3.org/2000/svg", nF = "http://www.w3.org/1998/Math/MathML", Xs = typeof document < "u" ? document : null, _0 = Xs && /* @__PURE__ */ Xs.createElement("template"), rF = {
  insert: (e, t, n) => {
    t.insertBefore(e, n || null);
  },
  remove: (e) => {
    const t = e.parentNode;
    t && t.removeChild(e);
  },
  createElement: (e, t, n, r) => {
    const s = t === "svg" ? Xs.createElementNS(tF, e) : t === "mathml" ? Xs.createElementNS(nF, e) : n ? Xs.createElement(e, { is: n }) : Xs.createElement(e);
    return e === "select" && r && r.multiple != null && s.setAttribute("multiple", r.multiple), s;
  },
  createText: (e) => Xs.createTextNode(e),
  createComment: (e) => Xs.createComment(e),
  setText: (e, t) => {
    e.nodeValue = t;
  },
  setElementText: (e, t) => {
    e.textContent = t;
  },
  parentNode: (e) => e.parentNode,
  nextSibling: (e) => e.nextSibling,
  querySelector: (e) => Xs.querySelector(e),
  setScopeId(e, t) {
    e.setAttribute(t, "");
  },
  insertStaticContent(e, t, n, r, s, o) {
    const i = n ? n.previousSibling : t.lastChild;
    if (s && (s === o || s.nextSibling)) for (; t.insertBefore(s.cloneNode(!0), n), !(s === o || !(s = s.nextSibling)); )
      ;
    else {
      _0.innerHTML = vx(r === "svg" ? `<svg>${e}</svg>` : r === "mathml" ? `<math>${e}</math>` : e);
      const a = _0.content;
      if (r === "svg" || r === "mathml") {
        const l = a.firstChild;
        for (; l.firstChild; ) a.appendChild(l.firstChild);
        a.removeChild(l);
      }
      t.insertBefore(a, n);
    }
    return [i ? i.nextSibling : t.firstChild, n ? n.previousSibling : t.lastChild];
  }
}, sF = /* @__PURE__ */ Symbol("_vtc");
function oF(e, t, n) {
  const r = e[sF];
  r && (t = (t ? [t, ...r] : [...r]).join(" ")), t == null ? e.removeAttribute("class") : n ? e.setAttribute("class", t) : e.className = t;
}
var xh = /* @__PURE__ */ Symbol("_vod"), yx = /* @__PURE__ */ Symbol("_vsh"), Zo = {
  name: "show",
  beforeMount(e, { value: t }, { transition: n }) {
    e[xh] = e.style.display === "none" ? "" : e.style.display, n && t ? n.beforeEnter(e) : du(e, t);
  },
  mounted(e, { value: t }, { transition: n }) {
    n && t && n.enter(e);
  },
  updated(e, { value: t, oldValue: n }, { transition: r }) {
    !t != !n && (r ? t ? (r.beforeEnter(e), du(e, !0), r.enter(e)) : r.leave(e, () => {
      du(e, !1);
    }) : du(e, t));
  },
  beforeUnmount(e, { value: t }) {
    du(e, t);
  }
};
function du(e, t) {
  e.style.display = t ? e[xh] : "none", e[yx] = !t;
}
var iF = /* @__PURE__ */ Symbol(""), aF = /(?:^|;)\s*display\s*:/;
function lF(e, t, n) {
  const r = e.style, s = tn(n);
  let o = !1;
  if (n && !s) {
    if (t) if (tn(t))
      for (const i of t.split(";")) {
        const a = i.slice(0, i.indexOf(":")).trim();
        n[a] == null && Ou(r, a, "");
      }
    else for (const i in t) n[i] == null && Ou(r, i, "");
    for (const i in n) {
      i === "display" && (o = !0);
      const a = n[i];
      a != null ? cF(e, i, !tn(t) && t ? t[i] : void 0, a) || Ou(r, i, a) : Ou(r, i, "");
    }
  } else if (s) {
    if (t !== n) {
      const i = r[iF];
      i && (n += ";" + i), r.cssText = n, o = aF.test(n);
    }
  } else t && e.removeAttribute("style");
  xh in e && (e[xh] = o ? r.display : "", e[yx] && (r.display = "none"));
}
var b0 = /\s*!important$/;
function Ou(e, t, n) {
  if (gt(n)) n.forEach((r) => Ou(e, t, r));
  else if (n == null && (n = ""), t.startsWith("--")) e.setProperty(t, n);
  else {
    const r = uF(e, t);
    b0.test(n) ? e.setProperty(si(r), n.replace(b0, ""), "important") : e[r] = n;
  }
}
var w0 = [
  "Webkit",
  "Moz",
  "ms"
], Ig = {};
function uF(e, t) {
  const n = Ig[t];
  if (n) return n;
  let r = os(t);
  if (r !== "filter" && r in e) return Ig[t] = r;
  r = mI(r);
  for (let s = 0; s < w0.length; s++) {
    const o = w0[s] + r;
    if (o in e) return Ig[t] = o;
  }
  return t;
}
function cF(e, t, n, r) {
  return e.tagName === "TEXTAREA" && (t === "width" || t === "height") && tn(r) && n === r;
}
var S0 = "http://www.w3.org/1999/xlink";
function T0(e, t, n, r, s, o = bL(t)) {
  r && t.startsWith("xlink:") ? n == null ? e.removeAttributeNS(S0, t.slice(6, t.length)) : e.setAttributeNS(S0, t, n) : n == null || o && !yI(n) ? e.removeAttribute(t) : e.setAttribute(t, o ? "" : Ns(n) ? String(n) : n);
}
function E0(e, t, n, r, s) {
  if (t === "innerHTML" || t === "textContent") {
    n != null && (e[t] = t === "innerHTML" ? vx(n) : n);
    return;
  }
  const o = e.tagName;
  if (t === "value" && o !== "PROGRESS" && !o.includes("-")) {
    const a = o === "OPTION" ? e.getAttribute("value") || "" : e.value, l = n == null ? e.type === "checkbox" ? "on" : "" : String(n);
    (a !== l || !("_value" in e)) && (e.value = l), n == null && e.removeAttribute(t), e._value = n;
    return;
  }
  let i = !1;
  if (n === "" || n == null) {
    const a = typeof e[t];
    a === "boolean" ? n = yI(n) : n == null && a === "string" ? (n = "", i = !0) : a === "number" && (n = 0, i = !0);
  }
  try {
    e[t] = n;
  } catch {
  }
  i && e.removeAttribute(s || t);
}
function Di(e, t, n, r) {
  e.addEventListener(t, n, r);
}
function dF(e, t, n, r) {
  e.removeEventListener(t, n, r);
}
var C0 = /* @__PURE__ */ Symbol("_vei");
function fF(e, t, n, r, s = null) {
  const o = e[C0] || (e[C0] = {}), i = o[t];
  if (r && i) i.value = r;
  else {
    const [a, l] = hF(t);
    r ? Di(e, a, o[t] = gF(r, s), l) : i && (dF(e, a, i, l), o[t] = void 0);
  }
}
var A0 = /(?:Once|Passive|Capture)$/;
function hF(e) {
  let t;
  if (A0.test(e)) {
    t = {};
    let n;
    for (; n = e.match(A0); )
      e = e.slice(0, e.length - n[0].length), t[n[0].toLowerCase()] = !0;
  }
  return [e[2] === ":" ? e.slice(3) : si(e.slice(2)), t];
}
var xg = 0, pF = /* @__PURE__ */ Promise.resolve(), mF = () => xg || (pF.then(() => xg = 0), xg = Date.now());
function gF(e, t) {
  const n = (r) => {
    if (!r._vts) r._vts = Date.now();
    else if (r._vts <= n.attached) return;
    const s = n.value;
    if (gt(s)) {
      const o = r.stopImmediatePropagation;
      r.stopImmediatePropagation = () => {
        o.call(r), r._stopped = !0;
      };
      const i = s.slice(), a = [r];
      for (let l = 0; l < i.length && !r._stopped; l++) {
        const u = i[l];
        u && ds(u, t, 5, a);
      }
    } else ds(s, t, 5, [r]);
  };
  return n.value = e, n.attached = mF(), n;
}
var I0 = (e) => e.charCodeAt(0) === 111 && e.charCodeAt(1) === 110 && e.charCodeAt(2) > 96 && e.charCodeAt(2) < 123, vF = (e, t, n, r, s, o) => {
  const i = s === "svg";
  t === "class" ? oF(e, r, i) : t === "style" ? lF(e, n, r) : Ap(t) ? Ip(t) || fF(e, t, n, r, o) : (t[0] === "." ? (t = t.slice(1), !0) : t[0] === "^" ? (t = t.slice(1), !1) : yF(e, t, r, i)) ? (E0(e, t, r), !e.tagName.includes("-") && (t === "value" || t === "checked" || t === "selected") && T0(e, t, r, i, o, t !== "value")) : e._isVueCE && (_F(e, t) || e._def.__asyncLoader && (/[A-Z]/.test(t) || !tn(r))) ? E0(e, os(t), r, o, t) : (t === "true-value" ? e._trueValue = r : t === "false-value" && (e._falseValue = r), T0(e, t, r, i));
};
function yF(e, t, n, r) {
  if (r)
    return !!(t === "innerHTML" || t === "textContent" || t in e && I0(t) && St(n));
  if (t === "spellcheck" || t === "draggable" || t === "translate" || t === "autocorrect" || t === "sandbox" && e.tagName === "IFRAME" || t === "form" || t === "list" && e.tagName === "INPUT" || t === "type" && e.tagName === "TEXTAREA") return !1;
  if (t === "width" || t === "height") {
    const s = e.tagName;
    if (s === "IMG" || s === "VIDEO" || s === "CANVAS" || s === "SOURCE") return !1;
  }
  return I0(t) && tn(n) ? !1 : t in e;
}
function _F(e, t) {
  const n = e._def.props;
  if (!n) return !1;
  const r = os(t);
  return Array.isArray(n) ? n.some((s) => os(s) === r) : Object.keys(n).some((s) => os(s) === r);
}
var kh = (e) => {
  const t = e.props["onUpdate:modelValue"] || !1;
  return gt(t) ? (n) => Gf(t, n) : t;
};
function bF(e) {
  e.target.composing = !0;
}
function x0(e) {
  const t = e.target;
  t.composing && (t.composing = !1, t.dispatchEvent(new Event("input")));
}
var il = /* @__PURE__ */ Symbol("_assign");
function k0(e, t, n) {
  return t && (e = e.trim()), n && (e = Mp(e)), e;
}
var ho = {
  created(e, { modifiers: { lazy: t, trim: n, number: r } }, s) {
    e[il] = kh(s);
    const o = r || s.props && s.props.type === "number";
    Di(e, t ? "change" : "input", (i) => {
      i.target.composing || e[il](k0(e.value, n, o));
    }), (n || o) && Di(e, "change", () => {
      e.value = k0(e.value, n, o);
    }), t || (Di(e, "compositionstart", bF), Di(e, "compositionend", x0), Di(e, "change", x0));
  },
  mounted(e, { value: t }) {
    e.value = t ?? "";
  },
  beforeUpdate(e, { value: t, oldValue: n, modifiers: { lazy: r, trim: s, number: o } }, i) {
    if (e[il] = kh(i), e.composing) return;
    const a = (o || e.type === "number") && !/^0\d/.test(e.value) ? Mp(e.value) : e.value, l = t ?? "";
    if (a === l) return;
    const u = e.getRootNode();
    (u instanceof Document || u instanceof ShadowRoot) && u.activeElement === e && e.type !== "range" && (r && t === n || s && e.value.trim() === l) || (e.value = l);
  }
}, wF = {
  deep: !0,
  created(e, { value: t, modifiers: { number: n } }, r) {
    const s = xp(t);
    Di(e, "change", () => {
      const o = Array.prototype.filter.call(e.options, (i) => i.selected).map((i) => n ? Mp(Mh(i)) : Mh(i));
      e[il](e.multiple ? s ? new Set(o) : o : o[0]), e._assigning = !0, Jt(() => {
        e._assigning = !1;
      });
    }), e[il] = kh(r);
  },
  mounted(e, { value: t }) {
    M0(e, t);
  },
  beforeUpdate(e, t, n) {
    e[il] = kh(n);
  },
  updated(e, { value: t }) {
    e._assigning || M0(e, t);
  }
};
function M0(e, t) {
  const n = e.multiple, r = gt(t);
  if (!(n && !r && !xp(t))) {
    for (let s = 0, o = e.options.length; s < o; s++) {
      const i = e.options[s], a = Mh(i);
      if (n) if (r) {
        const l = typeof a;
        l === "string" || l === "number" ? i.selected = t.some((u) => String(u) === String(a)) : i.selected = SL(t, a) > -1;
      } else i.selected = t.has(a);
      else if (Zc(Mh(i), t)) {
        e.selectedIndex !== s && (e.selectedIndex = s);
        return;
      }
    }
    !n && e.selectedIndex !== -1 && (e.selectedIndex = -1);
  }
}
function Mh(e) {
  return "_value" in e ? e._value : e.value;
}
var SF = [
  "ctrl",
  "shift",
  "alt",
  "meta"
], TF = {
  stop: (e) => e.stopPropagation(),
  prevent: (e) => e.preventDefault(),
  self: (e) => e.target !== e.currentTarget,
  ctrl: (e) => !e.ctrlKey,
  shift: (e) => !e.shiftKey,
  alt: (e) => !e.altKey,
  meta: (e) => !e.metaKey,
  left: (e) => "button" in e && e.button !== 0,
  middle: (e) => "button" in e && e.button !== 1,
  right: (e) => "button" in e && e.button !== 2,
  exact: (e, t) => SF.some((n) => e[`${n}Key`] && !t.includes(n))
}, Pn = (e, t) => {
  if (!e) return e;
  const n = e._withMods || (e._withMods = {}), r = t.join(".");
  return n[r] || (n[r] = ((s, ...o) => {
    for (let i = 0; i < t.length; i++) {
      const a = TF[t[i]];
      if (a && a(s, t)) return;
    }
    return e(s, ...o);
  }));
}, EF = {
  esc: "escape",
  space: " ",
  up: "arrow-up",
  left: "arrow-left",
  right: "arrow-right",
  down: "arrow-down",
  delete: "backspace"
}, _l = (e, t) => {
  const n = e._withKeys || (e._withKeys = {}), r = t.join(".");
  return n[r] || (n[r] = ((s) => {
    if (!("key" in s)) return;
    const o = si(s.key);
    if (t.some((i) => i === o || EF[i] === o)) return e(s);
  }));
}, CF = /* @__PURE__ */ fn({ patchProp: vF }, rF), P0;
function AF() {
  return P0 || (P0 = LO(CF));
}
var IF = ((...e) => {
  const t = AF().createApp(...e), { mount: n } = t;
  return t.mount = (r) => {
    const s = kF(r);
    if (!s) return;
    const o = t._component;
    !St(o) && !o.render && !o.template && (o.template = s.innerHTML), s.nodeType === 1 && (s.textContent = "");
    const i = n(s, !1, xF(s));
    return s instanceof Element && (s.removeAttribute("v-cloak"), s.setAttribute("data-v-app", "")), i;
  }, t;
});
function xF(e) {
  if (e instanceof SVGElement) return "svg";
  if (typeof MathMLElement == "function" && e instanceof MathMLElement) return "mathml";
}
function kF(e) {
  return tn(e) ? document.querySelector(e) : e;
}
function MF(e, t) {
  const n = Number(e);
  return Number.isFinite(n) && n > 0 ? Math.floor(n) : t;
}
function Do(e, t = 5) {
  const n = Number(e);
  return Number.isFinite(n) ? Math.min(20, Math.max(1, Math.floor(n))) : Math.min(20, Math.max(1, Math.floor(t)));
}
function sb(e, t = 20) {
  const n = Number(e);
  return Number.isFinite(n) ? Math.min(50, Math.max(5, Math.round(n / 5) * 5)) : Math.min(50, Math.max(5, Math.round(t / 5) * 5));
}
function Wv(e = {}, t = 0, n = {}) {
  const r = Math.max(0, Number(t) || 0), s = Do(n.defaultLimit, 5), o = MF(e.uiMessageWindowLimit, s), i = Math.min(Math.max(s, o), Math.max(r, s));
  e.uiMessageWindowLimit = i;
  const a = Math.max(0, r - i);
  return {
    startIndex: a,
    hiddenBefore: a,
    visibleCount: r - a,
    limit: i,
    total: r
  };
}
function PF(e = {}, t = 0, n = {}) {
  const r = Math.max(0, Number(t) || 0), s = Wv(e, r, n);
  if (!s.hiddenBefore) return !1;
  const o = sb(n.chunk, 20);
  return e.uiMessageWindowLimit = Math.min(r, s.limit + o), !0;
}
function RF(e = {}, t = {}) {
  e.uiMessageWindowLimit = Do(t.defaultLimit, 5);
}
function NF(e) {
  return e && typeof e == "object" && !Array.isArray(e) ? e : {};
}
function _x(e, t, n, r) {
  const s = Number(e);
  return Number.isFinite(s) ? Math.min(r, Math.max(n, Math.floor(s))) : t;
}
function $F(e, t = 20) {
  const n = _x(e, t, 5, 50);
  return Math.min(50, Math.max(5, Math.round(n / 5) * 5));
}
function Ya(e = {}) {
  const t = NF(e);
  return {
    hiddenOutsideCount: _x(t.hiddenOutsideCount, 5, 1, 20),
    loadBatchSize: $F(t.loadBatchSize, 20)
  };
}
var DF = /* @__PURE__ */ Xc(((e, t) => {
  (function() {
    function n(m) {
      "use strict";
      var b = {
        omitExtraWLInCodeBlocks: {
          defaultValue: !1,
          describe: "Omit the default extra whiteline added to code blocks",
          type: "boolean"
        },
        noHeaderId: {
          defaultValue: !1,
          describe: "Turn on/off generated header id",
          type: "boolean"
        },
        prefixHeaderId: {
          defaultValue: !1,
          describe: "Add a prefix to the generated header ids. Passing a string will prefix that string to the header id. Setting to true will add a generic 'section-' prefix",
          type: "string"
        },
        rawPrefixHeaderId: {
          defaultValue: !1,
          describe: 'Setting this option to true will prevent showdown from modifying the prefix. This might result in malformed IDs (if, for instance, the " char is used in the prefix)',
          type: "boolean"
        },
        ghCompatibleHeaderId: {
          defaultValue: !1,
          describe: "Generate header ids compatible with github style (spaces are replaced with dashes, a bunch of non alphanumeric chars are removed)",
          type: "boolean"
        },
        rawHeaderId: {
          defaultValue: !1,
          describe: `Remove only spaces, ' and " from generated header ids (including prefixes), replacing them with dashes (-). WARNING: This might result in malformed ids`,
          type: "boolean"
        },
        headerLevelStart: {
          defaultValue: !1,
          describe: "The header blocks level start",
          type: "integer"
        },
        parseImgDimensions: {
          defaultValue: !1,
          describe: "Turn on/off image dimension parsing",
          type: "boolean"
        },
        simplifiedAutoLink: {
          defaultValue: !1,
          describe: "Turn on/off GFM autolink style",
          type: "boolean"
        },
        excludeTrailingPunctuationFromURLs: {
          defaultValue: !1,
          describe: "Excludes trailing punctuation from links generated with autoLinking",
          type: "boolean"
        },
        literalMidWordUnderscores: {
          defaultValue: !1,
          describe: "Parse midword underscores as literal underscores",
          type: "boolean"
        },
        literalMidWordAsterisks: {
          defaultValue: !1,
          describe: "Parse midword asterisks as literal asterisks",
          type: "boolean"
        },
        strikethrough: {
          defaultValue: !1,
          describe: "Turn on/off strikethrough support",
          type: "boolean"
        },
        tables: {
          defaultValue: !1,
          describe: "Turn on/off tables support",
          type: "boolean"
        },
        tablesHeaderId: {
          defaultValue: !1,
          describe: "Add an id to table headers",
          type: "boolean"
        },
        ghCodeBlocks: {
          defaultValue: !0,
          describe: "Turn on/off GFM fenced code blocks support",
          type: "boolean"
        },
        tasklists: {
          defaultValue: !1,
          describe: "Turn on/off GFM tasklist support",
          type: "boolean"
        },
        smoothLivePreview: {
          defaultValue: !1,
          describe: "Prevents weird effects in live previews due to incomplete input",
          type: "boolean"
        },
        smartIndentationFix: {
          defaultValue: !1,
          describe: "Tries to smartly fix indentation in es6 strings",
          type: "boolean"
        },
        disableForced4SpacesIndentedSublists: {
          defaultValue: !1,
          describe: "Disables the requirement of indenting nested sublists by 4 spaces",
          type: "boolean"
        },
        simpleLineBreaks: {
          defaultValue: !1,
          describe: "Parses simple line breaks as <br> (GFM Style)",
          type: "boolean"
        },
        requireSpaceBeforeHeadingText: {
          defaultValue: !1,
          describe: "Makes adding a space between `#` and the header text mandatory (GFM Style)",
          type: "boolean"
        },
        ghMentions: {
          defaultValue: !1,
          describe: "Enables github @mentions",
          type: "boolean"
        },
        ghMentionsLink: {
          defaultValue: "https://github.com/{u}",
          describe: "Changes the link generated by @mentions. Only applies if ghMentions option is enabled.",
          type: "string"
        },
        encodeEmails: {
          defaultValue: !0,
          describe: "Encode e-mail addresses through the use of Character Entities, transforming ASCII e-mail addresses into its equivalent decimal entities",
          type: "boolean"
        },
        openLinksInNewWindow: {
          defaultValue: !1,
          describe: "Open all links in new windows",
          type: "boolean"
        },
        backslashEscapesHTMLTags: {
          defaultValue: !1,
          describe: "Support for HTML Tag escaping. ex: <div>foo</div>",
          type: "boolean"
        },
        emoji: {
          defaultValue: !1,
          describe: "Enable emoji support. Ex: `this is a :smile: emoji`",
          type: "boolean"
        },
        underline: {
          defaultValue: !1,
          describe: "Enable support for underline. Syntax is double or triple underscores: `__underline word__`. With this option enabled, underscores no longer parses into `<em>` and `<strong>`",
          type: "boolean"
        },
        ellipsis: {
          defaultValue: !0,
          describe: "Replaces three dots with the ellipsis unicode character",
          type: "boolean"
        },
        completeHTMLDocument: {
          defaultValue: !1,
          describe: "Outputs a complete html document, including `<html>`, `<head>` and `<body>` tags",
          type: "boolean"
        },
        metadata: {
          defaultValue: !1,
          describe: "Enable support for document metadata (defined at the top of the document between `«««` and `»»»` or between `---` and `---`).",
          type: "boolean"
        },
        splitAdjacentBlockquotes: {
          defaultValue: !1,
          describe: "Split adjacent blockquote blocks",
          type: "boolean"
        }
      };
      if (m === !1) return JSON.parse(JSON.stringify(b));
      var v = {};
      for (var T in b) b.hasOwnProperty(T) && (v[T] = b[T].defaultValue);
      return v;
    }
    function r() {
      "use strict";
      var m = n(!0), b = {};
      for (var v in m) m.hasOwnProperty(v) && (b[v] = !0);
      return b;
    }
    var s = {}, o = {}, i = {}, a = n(!0), l = "vanilla", u = {
      github: {
        omitExtraWLInCodeBlocks: !0,
        simplifiedAutoLink: !0,
        excludeTrailingPunctuationFromURLs: !0,
        literalMidWordUnderscores: !0,
        strikethrough: !0,
        tables: !0,
        tablesHeaderId: !0,
        ghCodeBlocks: !0,
        tasklists: !0,
        disableForced4SpacesIndentedSublists: !0,
        simpleLineBreaks: !0,
        requireSpaceBeforeHeadingText: !0,
        ghCompatibleHeaderId: !0,
        ghMentions: !0,
        backslashEscapesHTMLTags: !0,
        emoji: !0,
        splitAdjacentBlockquotes: !0
      },
      original: {
        noHeaderId: !0,
        ghCodeBlocks: !1
      },
      ghost: {
        omitExtraWLInCodeBlocks: !0,
        parseImgDimensions: !0,
        simplifiedAutoLink: !0,
        excludeTrailingPunctuationFromURLs: !0,
        literalMidWordUnderscores: !0,
        strikethrough: !0,
        tables: !0,
        tablesHeaderId: !0,
        ghCodeBlocks: !0,
        tasklists: !0,
        smoothLivePreview: !0,
        simpleLineBreaks: !0,
        requireSpaceBeforeHeadingText: !0,
        ghMentions: !1,
        encodeEmails: !0
      },
      vanilla: n(!0),
      allOn: r()
    };
    s.helper = {}, s.extensions = {}, s.setOption = function(m, b) {
      "use strict";
      return a[m] = b, this;
    }, s.getOption = function(m) {
      "use strict";
      return a[m];
    }, s.getOptions = function() {
      "use strict";
      return a;
    }, s.resetOptions = function() {
      "use strict";
      a = n(!0);
    }, s.setFlavor = function(m) {
      "use strict";
      if (!u.hasOwnProperty(m)) throw Error(m + " flavor was not found");
      s.resetOptions();
      var b = u[m];
      l = m;
      for (var v in b) b.hasOwnProperty(v) && (a[v] = b[v]);
    }, s.getFlavor = function() {
      "use strict";
      return l;
    }, s.getFlavorOptions = function(m) {
      "use strict";
      if (u.hasOwnProperty(m)) return u[m];
    }, s.getDefaultOptions = function(m) {
      "use strict";
      return n(m);
    }, s.subParser = function(m, b) {
      "use strict";
      if (s.helper.isString(m)) if (typeof b < "u") o[m] = b;
      else {
        if (o.hasOwnProperty(m)) return o[m];
        throw Error("SubParser named " + m + " not registered!");
      }
    }, s.extension = function(m, b) {
      "use strict";
      if (!s.helper.isString(m)) throw Error("Extension 'name' must be a string");
      if (m = s.helper.stdExtName(m), s.helper.isUndefined(b)) {
        if (!i.hasOwnProperty(m)) throw Error("Extension named " + m + " is not registered!");
        return i[m];
      } else {
        typeof b == "function" && (b = b()), s.helper.isArray(b) || (b = [b]);
        var v = d(b, m);
        if (v.valid) i[m] = b;
        else throw Error(v.error);
      }
    }, s.getAllExtensions = function() {
      "use strict";
      return i;
    }, s.removeExtension = function(m) {
      "use strict";
      delete i[m];
    }, s.resetExtensions = function() {
      "use strict";
      i = {};
    };
    function d(m, b) {
      "use strict";
      var v = b ? "Error in " + b + " extension->" : "Error in unnamed extension", T = {
        valid: !0,
        error: ""
      };
      s.helper.isArray(m) || (m = [m]);
      for (var I = 0; I < m.length; ++I) {
        var x = v + " sub-extension " + I + ": ", P = m[I];
        if (typeof P != "object")
          return T.valid = !1, T.error = x + "must be an object, but " + typeof P + " given", T;
        if (!s.helper.isString(P.type))
          return T.valid = !1, T.error = x + 'property "type" must be a string, but ' + typeof P.type + " given", T;
        var L = P.type = P.type.toLowerCase();
        if (L === "language" && (L = P.type = "lang"), L === "html" && (L = P.type = "output"), L !== "lang" && L !== "output" && L !== "listener")
          return T.valid = !1, T.error = x + "type " + L + ' is not recognized. Valid values: "lang/language", "output/html" or "listener"', T;
        if (L === "listener") {
          if (s.helper.isUndefined(P.listeners))
            return T.valid = !1, T.error = x + '. Extensions of type "listener" must have a property called "listeners"', T;
        } else if (s.helper.isUndefined(P.filter) && s.helper.isUndefined(P.regex))
          return T.valid = !1, T.error = x + L + ' extensions must define either a "regex" property or a "filter" method', T;
        if (P.listeners) {
          if (typeof P.listeners != "object")
            return T.valid = !1, T.error = x + '"listeners" property must be an object but ' + typeof P.listeners + " given", T;
          for (var G in P.listeners) if (P.listeners.hasOwnProperty(G) && typeof P.listeners[G] != "function")
            return T.valid = !1, T.error = x + '"listeners" property must be an hash of [event name]: [callback]. listeners.' + G + " must be a function but " + typeof P.listeners[G] + " given", T;
        }
        if (P.filter) {
          if (typeof P.filter != "function")
            return T.valid = !1, T.error = x + '"filter" must be a function, but ' + typeof P.filter + " given", T;
        } else if (P.regex) {
          if (s.helper.isString(P.regex) && (P.regex = new RegExp(P.regex, "g")), !(P.regex instanceof RegExp))
            return T.valid = !1, T.error = x + '"regex" property must either be a string or a RegExp object, but ' + typeof P.regex + " given", T;
          if (s.helper.isUndefined(P.replace))
            return T.valid = !1, T.error = x + '"regex" extensions must implement a replace string or function', T;
        }
      }
      return T;
    }
    s.validateExtension = function(m) {
      "use strict";
      var b = d(m, null);
      return b.valid ? !0 : (console.warn(b.error), !1);
    }, s.hasOwnProperty("helper") || (s.helper = {}), s.helper.isString = function(m) {
      "use strict";
      return typeof m == "string" || m instanceof String;
    }, s.helper.isFunction = function(m) {
      "use strict";
      return m && {}.toString.call(m) === "[object Function]";
    }, s.helper.isArray = function(m) {
      "use strict";
      return Array.isArray(m);
    }, s.helper.isUndefined = function(m) {
      "use strict";
      return typeof m > "u";
    }, s.helper.forEach = function(m, b) {
      "use strict";
      if (s.helper.isUndefined(m)) throw new Error("obj param is required");
      if (s.helper.isUndefined(b)) throw new Error("callback param is required");
      if (!s.helper.isFunction(b)) throw new Error("callback param must be a function/closure");
      if (typeof m.forEach == "function") m.forEach(b);
      else if (s.helper.isArray(m)) for (var v = 0; v < m.length; v++) b(m[v], v, m);
      else if (typeof m == "object")
        for (var T in m) m.hasOwnProperty(T) && b(m[T], T, m);
      else throw new Error("obj does not seem to be an array or an iterable object");
    }, s.helper.stdExtName = function(m) {
      "use strict";
      return m.replace(/[_?*+\/\\.^-]/g, "").replace(/\s/g, "").toLowerCase();
    };
    function h(m, b) {
      "use strict";
      return "¨E" + b.charCodeAt(0) + "E";
    }
    s.helper.escapeCharactersCallback = h, s.helper.escapeCharacters = function(m, b, v) {
      "use strict";
      var T = "([" + b.replace(/([\[\]\\])/g, "\\$1") + "])";
      v && (T = "\\\\" + T);
      var I = new RegExp(T, "g");
      return m = m.replace(I, h), m;
    }, s.helper.unescapeHTMLEntities = function(m) {
      "use strict";
      return m.replace(/&quot;/g, '"').replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&amp;/g, "&");
    };
    var p = function(m, b, v, T) {
      "use strict";
      var I = T || "", x = I.indexOf("g") > -1, P = new RegExp(b + "|" + v, "g" + I.replace(/g/g, "")), L = new RegExp(b, I.replace(/g/g, "")), G = [], z, V, Z, O, Y;
      do
        for (z = 0; Z = P.exec(m); ) if (L.test(Z[0]))
          z++ || (V = P.lastIndex, O = V - Z[0].length);
        else if (z && !--z) {
          Y = Z.index + Z[0].length;
          var X = {
            left: {
              start: O,
              end: V
            },
            match: {
              start: V,
              end: Z.index
            },
            right: {
              start: Z.index,
              end: Y
            },
            wholeMatch: {
              start: O,
              end: Y
            }
          };
          if (G.push(X), !x) return G;
        }
      while (z && (P.lastIndex = V));
      return G;
    };
    s.helper.matchRecursiveRegExp = function(m, b, v, T) {
      "use strict";
      for (var I = p(m, b, v, T), x = [], P = 0; P < I.length; ++P) x.push([
        m.slice(I[P].wholeMatch.start, I[P].wholeMatch.end),
        m.slice(I[P].match.start, I[P].match.end),
        m.slice(I[P].left.start, I[P].left.end),
        m.slice(I[P].right.start, I[P].right.end)
      ]);
      return x;
    }, s.helper.replaceRecursiveRegExp = function(m, b, v, T, I) {
      "use strict";
      if (!s.helper.isFunction(b)) {
        var x = b;
        b = function() {
          return x;
        };
      }
      var P = p(m, v, T, I), L = m, G = P.length;
      if (G > 0) {
        var z = [];
        P[0].wholeMatch.start !== 0 && z.push(m.slice(0, P[0].wholeMatch.start));
        for (var V = 0; V < G; ++V)
          z.push(b(m.slice(P[V].wholeMatch.start, P[V].wholeMatch.end), m.slice(P[V].match.start, P[V].match.end), m.slice(P[V].left.start, P[V].left.end), m.slice(P[V].right.start, P[V].right.end))), V < G - 1 && z.push(m.slice(P[V].wholeMatch.end, P[V + 1].wholeMatch.start));
        P[G - 1].wholeMatch.end < m.length && z.push(m.slice(P[G - 1].wholeMatch.end)), L = z.join("");
      }
      return L;
    }, s.helper.regexIndexOf = function(m, b, v) {
      "use strict";
      if (!s.helper.isString(m)) throw "InvalidArgumentError: first parameter of showdown.helper.regexIndexOf function must be a string";
      if (!(b instanceof RegExp)) throw "InvalidArgumentError: second parameter of showdown.helper.regexIndexOf function must be an instance of RegExp";
      var T = m.substring(v || 0).search(b);
      return T >= 0 ? T + (v || 0) : T;
    }, s.helper.splitAtIndex = function(m, b) {
      "use strict";
      if (!s.helper.isString(m)) throw "InvalidArgumentError: first parameter of showdown.helper.regexIndexOf function must be a string";
      return [m.substring(0, b), m.substring(b)];
    }, s.helper.encodeEmailAddress = function(m) {
      "use strict";
      var b = [
        function(v) {
          return "&#" + v.charCodeAt(0) + ";";
        },
        function(v) {
          return "&#x" + v.charCodeAt(0).toString(16) + ";";
        },
        function(v) {
          return v;
        }
      ];
      return m = m.replace(/./g, function(v) {
        if (v === "@") v = b[Math.floor(Math.random() * 2)](v);
        else {
          var T = Math.random();
          v = T > 0.9 ? b[2](v) : T > 0.45 ? b[1](v) : b[0](v);
        }
        return v;
      }), m;
    }, s.helper.padEnd = function(b, v, T) {
      "use strict";
      return v = v >> 0, T = String(T || " "), b.length > v ? String(b) : (v = v - b.length, v > T.length && (T += T.repeat(v / T.length)), String(b) + T.slice(0, v));
    }, typeof console > "u" && (console = {
      warn: function(m) {
        "use strict";
        alert(m);
      },
      log: function(m) {
        "use strict";
        alert(m);
      },
      error: function(m) {
        "use strict";
        throw m;
      }
    }), s.helper.regexes = { asteriskDashAndColon: /([*_:~])/g }, s.helper.emojis = {
      "+1": "👍",
      "-1": "👎",
      100: "💯",
      1234: "🔢",
      "1st_place_medal": "🥇",
      "2nd_place_medal": "🥈",
      "3rd_place_medal": "🥉",
      "8ball": "🎱",
      a: "🅰️",
      ab: "🆎",
      abc: "🔤",
      abcd: "🔡",
      accept: "🉑",
      aerial_tramway: "🚡",
      airplane: "✈️",
      alarm_clock: "⏰",
      alembic: "⚗️",
      alien: "👽",
      ambulance: "🚑",
      amphora: "🏺",
      anchor: "⚓️",
      angel: "👼",
      anger: "💢",
      angry: "😠",
      anguished: "😧",
      ant: "🐜",
      apple: "🍎",
      aquarius: "♒️",
      aries: "♈️",
      arrow_backward: "◀️",
      arrow_double_down: "⏬",
      arrow_double_up: "⏫",
      arrow_down: "⬇️",
      arrow_down_small: "🔽",
      arrow_forward: "▶️",
      arrow_heading_down: "⤵️",
      arrow_heading_up: "⤴️",
      arrow_left: "⬅️",
      arrow_lower_left: "↙️",
      arrow_lower_right: "↘️",
      arrow_right: "➡️",
      arrow_right_hook: "↪️",
      arrow_up: "⬆️",
      arrow_up_down: "↕️",
      arrow_up_small: "🔼",
      arrow_upper_left: "↖️",
      arrow_upper_right: "↗️",
      arrows_clockwise: "🔃",
      arrows_counterclockwise: "🔄",
      art: "🎨",
      articulated_lorry: "🚛",
      artificial_satellite: "🛰",
      astonished: "😲",
      athletic_shoe: "👟",
      atm: "🏧",
      atom_symbol: "⚛️",
      avocado: "🥑",
      b: "🅱️",
      baby: "👶",
      baby_bottle: "🍼",
      baby_chick: "🐤",
      baby_symbol: "🚼",
      back: "🔙",
      bacon: "🥓",
      badminton: "🏸",
      baggage_claim: "🛄",
      baguette_bread: "🥖",
      balance_scale: "⚖️",
      balloon: "🎈",
      ballot_box: "🗳",
      ballot_box_with_check: "☑️",
      bamboo: "🎍",
      banana: "🍌",
      bangbang: "‼️",
      bank: "🏦",
      bar_chart: "📊",
      barber: "💈",
      baseball: "⚾️",
      basketball: "🏀",
      basketball_man: "⛹️",
      basketball_woman: "⛹️&zwj;♀️",
      bat: "🦇",
      bath: "🛀",
      bathtub: "🛁",
      battery: "🔋",
      beach_umbrella: "🏖",
      bear: "🐻",
      bed: "🛏",
      bee: "🐝",
      beer: "🍺",
      beers: "🍻",
      beetle: "🐞",
      beginner: "🔰",
      bell: "🔔",
      bellhop_bell: "🛎",
      bento: "🍱",
      biking_man: "🚴",
      bike: "🚲",
      biking_woman: "🚴&zwj;♀️",
      bikini: "👙",
      biohazard: "☣️",
      bird: "🐦",
      birthday: "🎂",
      black_circle: "⚫️",
      black_flag: "🏴",
      black_heart: "🖤",
      black_joker: "🃏",
      black_large_square: "⬛️",
      black_medium_small_square: "◾️",
      black_medium_square: "◼️",
      black_nib: "✒️",
      black_small_square: "▪️",
      black_square_button: "🔲",
      blonde_man: "👱",
      blonde_woman: "👱&zwj;♀️",
      blossom: "🌼",
      blowfish: "🐡",
      blue_book: "📘",
      blue_car: "🚙",
      blue_heart: "💙",
      blush: "😊",
      boar: "🐗",
      boat: "⛵️",
      bomb: "💣",
      book: "📖",
      bookmark: "🔖",
      bookmark_tabs: "📑",
      books: "📚",
      boom: "💥",
      boot: "👢",
      bouquet: "💐",
      bowing_man: "🙇",
      bow_and_arrow: "🏹",
      bowing_woman: "🙇&zwj;♀️",
      bowling: "🎳",
      boxing_glove: "🥊",
      boy: "👦",
      bread: "🍞",
      bride_with_veil: "👰",
      bridge_at_night: "🌉",
      briefcase: "💼",
      broken_heart: "💔",
      bug: "🐛",
      building_construction: "🏗",
      bulb: "💡",
      bullettrain_front: "🚅",
      bullettrain_side: "🚄",
      burrito: "🌯",
      bus: "🚌",
      business_suit_levitating: "🕴",
      busstop: "🚏",
      bust_in_silhouette: "👤",
      busts_in_silhouette: "👥",
      butterfly: "🦋",
      cactus: "🌵",
      cake: "🍰",
      calendar: "📆",
      call_me_hand: "🤙",
      calling: "📲",
      camel: "🐫",
      camera: "📷",
      camera_flash: "📸",
      camping: "🏕",
      cancer: "♋️",
      candle: "🕯",
      candy: "🍬",
      canoe: "🛶",
      capital_abcd: "🔠",
      capricorn: "♑️",
      car: "🚗",
      card_file_box: "🗃",
      card_index: "📇",
      card_index_dividers: "🗂",
      carousel_horse: "🎠",
      carrot: "🥕",
      cat: "🐱",
      cat2: "🐈",
      cd: "💿",
      chains: "⛓",
      champagne: "🍾",
      chart: "💹",
      chart_with_downwards_trend: "📉",
      chart_with_upwards_trend: "📈",
      checkered_flag: "🏁",
      cheese: "🧀",
      cherries: "🍒",
      cherry_blossom: "🌸",
      chestnut: "🌰",
      chicken: "🐔",
      children_crossing: "🚸",
      chipmunk: "🐿",
      chocolate_bar: "🍫",
      christmas_tree: "🎄",
      church: "⛪️",
      cinema: "🎦",
      circus_tent: "🎪",
      city_sunrise: "🌇",
      city_sunset: "🌆",
      cityscape: "🏙",
      cl: "🆑",
      clamp: "🗜",
      clap: "👏",
      clapper: "🎬",
      classical_building: "🏛",
      clinking_glasses: "🥂",
      clipboard: "📋",
      clock1: "🕐",
      clock10: "🕙",
      clock1030: "🕥",
      clock11: "🕚",
      clock1130: "🕦",
      clock12: "🕛",
      clock1230: "🕧",
      clock130: "🕜",
      clock2: "🕑",
      clock230: "🕝",
      clock3: "🕒",
      clock330: "🕞",
      clock4: "🕓",
      clock430: "🕟",
      clock5: "🕔",
      clock530: "🕠",
      clock6: "🕕",
      clock630: "🕡",
      clock7: "🕖",
      clock730: "🕢",
      clock8: "🕗",
      clock830: "🕣",
      clock9: "🕘",
      clock930: "🕤",
      closed_book: "📕",
      closed_lock_with_key: "🔐",
      closed_umbrella: "🌂",
      cloud: "☁️",
      cloud_with_lightning: "🌩",
      cloud_with_lightning_and_rain: "⛈",
      cloud_with_rain: "🌧",
      cloud_with_snow: "🌨",
      clown_face: "🤡",
      clubs: "♣️",
      cocktail: "🍸",
      coffee: "☕️",
      coffin: "⚰️",
      cold_sweat: "😰",
      comet: "☄️",
      computer: "💻",
      computer_mouse: "🖱",
      confetti_ball: "🎊",
      confounded: "😖",
      confused: "😕",
      congratulations: "㊗️",
      construction: "🚧",
      construction_worker_man: "👷",
      construction_worker_woman: "👷&zwj;♀️",
      control_knobs: "🎛",
      convenience_store: "🏪",
      cookie: "🍪",
      cool: "🆒",
      policeman: "👮",
      copyright: "©️",
      corn: "🌽",
      couch_and_lamp: "🛋",
      couple: "👫",
      couple_with_heart_woman_man: "💑",
      couple_with_heart_man_man: "👨&zwj;❤️&zwj;👨",
      couple_with_heart_woman_woman: "👩&zwj;❤️&zwj;👩",
      couplekiss_man_man: "👨&zwj;❤️&zwj;💋&zwj;👨",
      couplekiss_man_woman: "💏",
      couplekiss_woman_woman: "👩&zwj;❤️&zwj;💋&zwj;👩",
      cow: "🐮",
      cow2: "🐄",
      cowboy_hat_face: "🤠",
      crab: "🦀",
      crayon: "🖍",
      credit_card: "💳",
      crescent_moon: "🌙",
      cricket: "🏏",
      crocodile: "🐊",
      croissant: "🥐",
      crossed_fingers: "🤞",
      crossed_flags: "🎌",
      crossed_swords: "⚔️",
      crown: "👑",
      cry: "😢",
      crying_cat_face: "😿",
      crystal_ball: "🔮",
      cucumber: "🥒",
      cupid: "💘",
      curly_loop: "➰",
      currency_exchange: "💱",
      curry: "🍛",
      custard: "🍮",
      customs: "🛃",
      cyclone: "🌀",
      dagger: "🗡",
      dancer: "💃",
      dancing_women: "👯",
      dancing_men: "👯&zwj;♂️",
      dango: "🍡",
      dark_sunglasses: "🕶",
      dart: "🎯",
      dash: "💨",
      date: "📅",
      deciduous_tree: "🌳",
      deer: "🦌",
      department_store: "🏬",
      derelict_house: "🏚",
      desert: "🏜",
      desert_island: "🏝",
      desktop_computer: "🖥",
      male_detective: "🕵️",
      diamond_shape_with_a_dot_inside: "💠",
      diamonds: "♦️",
      disappointed: "😞",
      disappointed_relieved: "😥",
      dizzy: "💫",
      dizzy_face: "😵",
      do_not_litter: "🚯",
      dog: "🐶",
      dog2: "🐕",
      dollar: "💵",
      dolls: "🎎",
      dolphin: "🐬",
      door: "🚪",
      doughnut: "🍩",
      dove: "🕊",
      dragon: "🐉",
      dragon_face: "🐲",
      dress: "👗",
      dromedary_camel: "🐪",
      drooling_face: "🤤",
      droplet: "💧",
      drum: "🥁",
      duck: "🦆",
      dvd: "📀",
      "e-mail": "📧",
      eagle: "🦅",
      ear: "👂",
      ear_of_rice: "🌾",
      earth_africa: "🌍",
      earth_americas: "🌎",
      earth_asia: "🌏",
      egg: "🥚",
      eggplant: "🍆",
      eight_pointed_black_star: "✴️",
      eight_spoked_asterisk: "✳️",
      electric_plug: "🔌",
      elephant: "🐘",
      email: "✉️",
      end: "🔚",
      envelope_with_arrow: "📩",
      euro: "💶",
      european_castle: "🏰",
      european_post_office: "🏤",
      evergreen_tree: "🌲",
      exclamation: "❗️",
      expressionless: "😑",
      eye: "👁",
      eye_speech_bubble: "👁&zwj;🗨",
      eyeglasses: "👓",
      eyes: "👀",
      face_with_head_bandage: "🤕",
      face_with_thermometer: "🤒",
      fist_oncoming: "👊",
      factory: "🏭",
      fallen_leaf: "🍂",
      family_man_woman_boy: "👪",
      family_man_boy: "👨&zwj;👦",
      family_man_boy_boy: "👨&zwj;👦&zwj;👦",
      family_man_girl: "👨&zwj;👧",
      family_man_girl_boy: "👨&zwj;👧&zwj;👦",
      family_man_girl_girl: "👨&zwj;👧&zwj;👧",
      family_man_man_boy: "👨&zwj;👨&zwj;👦",
      family_man_man_boy_boy: "👨&zwj;👨&zwj;👦&zwj;👦",
      family_man_man_girl: "👨&zwj;👨&zwj;👧",
      family_man_man_girl_boy: "👨&zwj;👨&zwj;👧&zwj;👦",
      family_man_man_girl_girl: "👨&zwj;👨&zwj;👧&zwj;👧",
      family_man_woman_boy_boy: "👨&zwj;👩&zwj;👦&zwj;👦",
      family_man_woman_girl: "👨&zwj;👩&zwj;👧",
      family_man_woman_girl_boy: "👨&zwj;👩&zwj;👧&zwj;👦",
      family_man_woman_girl_girl: "👨&zwj;👩&zwj;👧&zwj;👧",
      family_woman_boy: "👩&zwj;👦",
      family_woman_boy_boy: "👩&zwj;👦&zwj;👦",
      family_woman_girl: "👩&zwj;👧",
      family_woman_girl_boy: "👩&zwj;👧&zwj;👦",
      family_woman_girl_girl: "👩&zwj;👧&zwj;👧",
      family_woman_woman_boy: "👩&zwj;👩&zwj;👦",
      family_woman_woman_boy_boy: "👩&zwj;👩&zwj;👦&zwj;👦",
      family_woman_woman_girl: "👩&zwj;👩&zwj;👧",
      family_woman_woman_girl_boy: "👩&zwj;👩&zwj;👧&zwj;👦",
      family_woman_woman_girl_girl: "👩&zwj;👩&zwj;👧&zwj;👧",
      fast_forward: "⏩",
      fax: "📠",
      fearful: "😨",
      feet: "🐾",
      female_detective: "🕵️&zwj;♀️",
      ferris_wheel: "🎡",
      ferry: "⛴",
      field_hockey: "🏑",
      file_cabinet: "🗄",
      file_folder: "📁",
      film_projector: "📽",
      film_strip: "🎞",
      fire: "🔥",
      fire_engine: "🚒",
      fireworks: "🎆",
      first_quarter_moon: "🌓",
      first_quarter_moon_with_face: "🌛",
      fish: "🐟",
      fish_cake: "🍥",
      fishing_pole_and_fish: "🎣",
      fist_raised: "✊",
      fist_left: "🤛",
      fist_right: "🤜",
      flags: "🎏",
      flashlight: "🔦",
      fleur_de_lis: "⚜️",
      flight_arrival: "🛬",
      flight_departure: "🛫",
      floppy_disk: "💾",
      flower_playing_cards: "🎴",
      flushed: "😳",
      fog: "🌫",
      foggy: "🌁",
      football: "🏈",
      footprints: "👣",
      fork_and_knife: "🍴",
      fountain: "⛲️",
      fountain_pen: "🖋",
      four_leaf_clover: "🍀",
      fox_face: "🦊",
      framed_picture: "🖼",
      free: "🆓",
      fried_egg: "🍳",
      fried_shrimp: "🍤",
      fries: "🍟",
      frog: "🐸",
      frowning: "😦",
      frowning_face: "☹️",
      frowning_man: "🙍&zwj;♂️",
      frowning_woman: "🙍",
      middle_finger: "🖕",
      fuelpump: "⛽️",
      full_moon: "🌕",
      full_moon_with_face: "🌝",
      funeral_urn: "⚱️",
      game_die: "🎲",
      gear: "⚙️",
      gem: "💎",
      gemini: "♊️",
      ghost: "👻",
      gift: "🎁",
      gift_heart: "💝",
      girl: "👧",
      globe_with_meridians: "🌐",
      goal_net: "🥅",
      goat: "🐐",
      golf: "⛳️",
      golfing_man: "🏌️",
      golfing_woman: "🏌️&zwj;♀️",
      gorilla: "🦍",
      grapes: "🍇",
      green_apple: "🍏",
      green_book: "📗",
      green_heart: "💚",
      green_salad: "🥗",
      grey_exclamation: "❕",
      grey_question: "❔",
      grimacing: "😬",
      grin: "😁",
      grinning: "😀",
      guardsman: "💂",
      guardswoman: "💂&zwj;♀️",
      guitar: "🎸",
      gun: "🔫",
      haircut_woman: "💇",
      haircut_man: "💇&zwj;♂️",
      hamburger: "🍔",
      hammer: "🔨",
      hammer_and_pick: "⚒",
      hammer_and_wrench: "🛠",
      hamster: "🐹",
      hand: "✋",
      handbag: "👜",
      handshake: "🤝",
      hankey: "💩",
      hatched_chick: "🐥",
      hatching_chick: "🐣",
      headphones: "🎧",
      hear_no_evil: "🙉",
      heart: "❤️",
      heart_decoration: "💟",
      heart_eyes: "😍",
      heart_eyes_cat: "😻",
      heartbeat: "💓",
      heartpulse: "💗",
      hearts: "♥️",
      heavy_check_mark: "✔️",
      heavy_division_sign: "➗",
      heavy_dollar_sign: "💲",
      heavy_heart_exclamation: "❣️",
      heavy_minus_sign: "➖",
      heavy_multiplication_x: "✖️",
      heavy_plus_sign: "➕",
      helicopter: "🚁",
      herb: "🌿",
      hibiscus: "🌺",
      high_brightness: "🔆",
      high_heel: "👠",
      hocho: "🔪",
      hole: "🕳",
      honey_pot: "🍯",
      horse: "🐴",
      horse_racing: "🏇",
      hospital: "🏥",
      hot_pepper: "🌶",
      hotdog: "🌭",
      hotel: "🏨",
      hotsprings: "♨️",
      hourglass: "⌛️",
      hourglass_flowing_sand: "⏳",
      house: "🏠",
      house_with_garden: "🏡",
      houses: "🏘",
      hugs: "🤗",
      hushed: "😯",
      ice_cream: "🍨",
      ice_hockey: "🏒",
      ice_skate: "⛸",
      icecream: "🍦",
      id: "🆔",
      ideograph_advantage: "🉐",
      imp: "👿",
      inbox_tray: "📥",
      incoming_envelope: "📨",
      tipping_hand_woman: "💁",
      information_source: "ℹ️",
      innocent: "😇",
      interrobang: "⁉️",
      iphone: "📱",
      izakaya_lantern: "🏮",
      jack_o_lantern: "🎃",
      japan: "🗾",
      japanese_castle: "🏯",
      japanese_goblin: "👺",
      japanese_ogre: "👹",
      jeans: "👖",
      joy: "😂",
      joy_cat: "😹",
      joystick: "🕹",
      kaaba: "🕋",
      key: "🔑",
      keyboard: "⌨️",
      keycap_ten: "🔟",
      kick_scooter: "🛴",
      kimono: "👘",
      kiss: "💋",
      kissing: "😗",
      kissing_cat: "😽",
      kissing_closed_eyes: "😚",
      kissing_heart: "😘",
      kissing_smiling_eyes: "😙",
      kiwi_fruit: "🥝",
      koala: "🐨",
      koko: "🈁",
      label: "🏷",
      large_blue_circle: "🔵",
      large_blue_diamond: "🔷",
      large_orange_diamond: "🔶",
      last_quarter_moon: "🌗",
      last_quarter_moon_with_face: "🌜",
      latin_cross: "✝️",
      laughing: "😆",
      leaves: "🍃",
      ledger: "📒",
      left_luggage: "🛅",
      left_right_arrow: "↔️",
      leftwards_arrow_with_hook: "↩️",
      lemon: "🍋",
      leo: "♌️",
      leopard: "🐆",
      level_slider: "🎚",
      libra: "♎️",
      light_rail: "🚈",
      link: "🔗",
      lion: "🦁",
      lips: "👄",
      lipstick: "💄",
      lizard: "🦎",
      lock: "🔒",
      lock_with_ink_pen: "🔏",
      lollipop: "🍭",
      loop: "➿",
      loud_sound: "🔊",
      loudspeaker: "📢",
      love_hotel: "🏩",
      love_letter: "💌",
      low_brightness: "🔅",
      lying_face: "🤥",
      m: "Ⓜ️",
      mag: "🔍",
      mag_right: "🔎",
      mahjong: "🀄️",
      mailbox: "📫",
      mailbox_closed: "📪",
      mailbox_with_mail: "📬",
      mailbox_with_no_mail: "📭",
      man: "👨",
      man_artist: "👨&zwj;🎨",
      man_astronaut: "👨&zwj;🚀",
      man_cartwheeling: "🤸&zwj;♂️",
      man_cook: "👨&zwj;🍳",
      man_dancing: "🕺",
      man_facepalming: "🤦&zwj;♂️",
      man_factory_worker: "👨&zwj;🏭",
      man_farmer: "👨&zwj;🌾",
      man_firefighter: "👨&zwj;🚒",
      man_health_worker: "👨&zwj;⚕️",
      man_in_tuxedo: "🤵",
      man_judge: "👨&zwj;⚖️",
      man_juggling: "🤹&zwj;♂️",
      man_mechanic: "👨&zwj;🔧",
      man_office_worker: "👨&zwj;💼",
      man_pilot: "👨&zwj;✈️",
      man_playing_handball: "🤾&zwj;♂️",
      man_playing_water_polo: "🤽&zwj;♂️",
      man_scientist: "👨&zwj;🔬",
      man_shrugging: "🤷&zwj;♂️",
      man_singer: "👨&zwj;🎤",
      man_student: "👨&zwj;🎓",
      man_teacher: "👨&zwj;🏫",
      man_technologist: "👨&zwj;💻",
      man_with_gua_pi_mao: "👲",
      man_with_turban: "👳",
      tangerine: "🍊",
      mans_shoe: "👞",
      mantelpiece_clock: "🕰",
      maple_leaf: "🍁",
      martial_arts_uniform: "🥋",
      mask: "😷",
      massage_woman: "💆",
      massage_man: "💆&zwj;♂️",
      meat_on_bone: "🍖",
      medal_military: "🎖",
      medal_sports: "🏅",
      mega: "📣",
      melon: "🍈",
      memo: "📝",
      men_wrestling: "🤼&zwj;♂️",
      menorah: "🕎",
      mens: "🚹",
      metal: "🤘",
      metro: "🚇",
      microphone: "🎤",
      microscope: "🔬",
      milk_glass: "🥛",
      milky_way: "🌌",
      minibus: "🚐",
      minidisc: "💽",
      mobile_phone_off: "📴",
      money_mouth_face: "🤑",
      money_with_wings: "💸",
      moneybag: "💰",
      monkey: "🐒",
      monkey_face: "🐵",
      monorail: "🚝",
      moon: "🌔",
      mortar_board: "🎓",
      mosque: "🕌",
      motor_boat: "🛥",
      motor_scooter: "🛵",
      motorcycle: "🏍",
      motorway: "🛣",
      mount_fuji: "🗻",
      mountain: "⛰",
      mountain_biking_man: "🚵",
      mountain_biking_woman: "🚵&zwj;♀️",
      mountain_cableway: "🚠",
      mountain_railway: "🚞",
      mountain_snow: "🏔",
      mouse: "🐭",
      mouse2: "🐁",
      movie_camera: "🎥",
      moyai: "🗿",
      mrs_claus: "🤶",
      muscle: "💪",
      mushroom: "🍄",
      musical_keyboard: "🎹",
      musical_note: "🎵",
      musical_score: "🎼",
      mute: "🔇",
      nail_care: "💅",
      name_badge: "📛",
      national_park: "🏞",
      nauseated_face: "🤢",
      necktie: "👔",
      negative_squared_cross_mark: "❎",
      nerd_face: "🤓",
      neutral_face: "😐",
      new: "🆕",
      new_moon: "🌑",
      new_moon_with_face: "🌚",
      newspaper: "📰",
      newspaper_roll: "🗞",
      next_track_button: "⏭",
      ng: "🆖",
      no_good_man: "🙅&zwj;♂️",
      no_good_woman: "🙅",
      night_with_stars: "🌃",
      no_bell: "🔕",
      no_bicycles: "🚳",
      no_entry: "⛔️",
      no_entry_sign: "🚫",
      no_mobile_phones: "📵",
      no_mouth: "😶",
      no_pedestrians: "🚷",
      no_smoking: "🚭",
      "non-potable_water": "🚱",
      nose: "👃",
      notebook: "📓",
      notebook_with_decorative_cover: "📔",
      notes: "🎶",
      nut_and_bolt: "🔩",
      o: "⭕️",
      o2: "🅾️",
      ocean: "🌊",
      octopus: "🐙",
      oden: "🍢",
      office: "🏢",
      oil_drum: "🛢",
      ok: "🆗",
      ok_hand: "👌",
      ok_man: "🙆&zwj;♂️",
      ok_woman: "🙆",
      old_key: "🗝",
      older_man: "👴",
      older_woman: "👵",
      om: "🕉",
      on: "🔛",
      oncoming_automobile: "🚘",
      oncoming_bus: "🚍",
      oncoming_police_car: "🚔",
      oncoming_taxi: "🚖",
      open_file_folder: "📂",
      open_hands: "👐",
      open_mouth: "😮",
      open_umbrella: "☂️",
      ophiuchus: "⛎",
      orange_book: "📙",
      orthodox_cross: "☦️",
      outbox_tray: "📤",
      owl: "🦉",
      ox: "🐂",
      package: "📦",
      page_facing_up: "📄",
      page_with_curl: "📃",
      pager: "📟",
      paintbrush: "🖌",
      palm_tree: "🌴",
      pancakes: "🥞",
      panda_face: "🐼",
      paperclip: "📎",
      paperclips: "🖇",
      parasol_on_ground: "⛱",
      parking: "🅿️",
      part_alternation_mark: "〽️",
      partly_sunny: "⛅️",
      passenger_ship: "🛳",
      passport_control: "🛂",
      pause_button: "⏸",
      peace_symbol: "☮️",
      peach: "🍑",
      peanuts: "🥜",
      pear: "🍐",
      pen: "🖊",
      pencil2: "✏️",
      penguin: "🐧",
      pensive: "😔",
      performing_arts: "🎭",
      persevere: "😣",
      person_fencing: "🤺",
      pouting_woman: "🙎",
      phone: "☎️",
      pick: "⛏",
      pig: "🐷",
      pig2: "🐖",
      pig_nose: "🐽",
      pill: "💊",
      pineapple: "🍍",
      ping_pong: "🏓",
      pisces: "♓️",
      pizza: "🍕",
      place_of_worship: "🛐",
      plate_with_cutlery: "🍽",
      play_or_pause_button: "⏯",
      point_down: "👇",
      point_left: "👈",
      point_right: "👉",
      point_up: "☝️",
      point_up_2: "👆",
      police_car: "🚓",
      policewoman: "👮&zwj;♀️",
      poodle: "🐩",
      popcorn: "🍿",
      post_office: "🏣",
      postal_horn: "📯",
      postbox: "📮",
      potable_water: "🚰",
      potato: "🥔",
      pouch: "👝",
      poultry_leg: "🍗",
      pound: "💷",
      rage: "😡",
      pouting_cat: "😾",
      pouting_man: "🙎&zwj;♂️",
      pray: "🙏",
      prayer_beads: "📿",
      pregnant_woman: "🤰",
      previous_track_button: "⏮",
      prince: "🤴",
      princess: "👸",
      printer: "🖨",
      purple_heart: "💜",
      purse: "👛",
      pushpin: "📌",
      put_litter_in_its_place: "🚮",
      question: "❓",
      rabbit: "🐰",
      rabbit2: "🐇",
      racehorse: "🐎",
      racing_car: "🏎",
      radio: "📻",
      radio_button: "🔘",
      radioactive: "☢️",
      railway_car: "🚃",
      railway_track: "🛤",
      rainbow: "🌈",
      rainbow_flag: "🏳️&zwj;🌈",
      raised_back_of_hand: "🤚",
      raised_hand_with_fingers_splayed: "🖐",
      raised_hands: "🙌",
      raising_hand_woman: "🙋",
      raising_hand_man: "🙋&zwj;♂️",
      ram: "🐏",
      ramen: "🍜",
      rat: "🐀",
      record_button: "⏺",
      recycle: "♻️",
      red_circle: "🔴",
      registered: "®️",
      relaxed: "☺️",
      relieved: "😌",
      reminder_ribbon: "🎗",
      repeat: "🔁",
      repeat_one: "🔂",
      rescue_worker_helmet: "⛑",
      restroom: "🚻",
      revolving_hearts: "💞",
      rewind: "⏪",
      rhinoceros: "🦏",
      ribbon: "🎀",
      rice: "🍚",
      rice_ball: "🍙",
      rice_cracker: "🍘",
      rice_scene: "🎑",
      right_anger_bubble: "🗯",
      ring: "💍",
      robot: "🤖",
      rocket: "🚀",
      rofl: "🤣",
      roll_eyes: "🙄",
      roller_coaster: "🎢",
      rooster: "🐓",
      rose: "🌹",
      rosette: "🏵",
      rotating_light: "🚨",
      round_pushpin: "📍",
      rowing_man: "🚣",
      rowing_woman: "🚣&zwj;♀️",
      rugby_football: "🏉",
      running_man: "🏃",
      running_shirt_with_sash: "🎽",
      running_woman: "🏃&zwj;♀️",
      sa: "🈂️",
      sagittarius: "♐️",
      sake: "🍶",
      sandal: "👡",
      santa: "🎅",
      satellite: "📡",
      saxophone: "🎷",
      school: "🏫",
      school_satchel: "🎒",
      scissors: "✂️",
      scorpion: "🦂",
      scorpius: "♏️",
      scream: "😱",
      scream_cat: "🙀",
      scroll: "📜",
      seat: "💺",
      secret: "㊙️",
      see_no_evil: "🙈",
      seedling: "🌱",
      selfie: "🤳",
      shallow_pan_of_food: "🥘",
      shamrock: "☘️",
      shark: "🦈",
      shaved_ice: "🍧",
      sheep: "🐑",
      shell: "🐚",
      shield: "🛡",
      shinto_shrine: "⛩",
      ship: "🚢",
      shirt: "👕",
      shopping: "🛍",
      shopping_cart: "🛒",
      shower: "🚿",
      shrimp: "🦐",
      signal_strength: "📶",
      six_pointed_star: "🔯",
      ski: "🎿",
      skier: "⛷",
      skull: "💀",
      skull_and_crossbones: "☠️",
      sleeping: "😴",
      sleeping_bed: "🛌",
      sleepy: "😪",
      slightly_frowning_face: "🙁",
      slightly_smiling_face: "🙂",
      slot_machine: "🎰",
      small_airplane: "🛩",
      small_blue_diamond: "🔹",
      small_orange_diamond: "🔸",
      small_red_triangle: "🔺",
      small_red_triangle_down: "🔻",
      smile: "😄",
      smile_cat: "😸",
      smiley: "😃",
      smiley_cat: "😺",
      smiling_imp: "😈",
      smirk: "😏",
      smirk_cat: "😼",
      smoking: "🚬",
      snail: "🐌",
      snake: "🐍",
      sneezing_face: "🤧",
      snowboarder: "🏂",
      snowflake: "❄️",
      snowman: "⛄️",
      snowman_with_snow: "☃️",
      sob: "😭",
      soccer: "⚽️",
      soon: "🔜",
      sos: "🆘",
      sound: "🔉",
      space_invader: "👾",
      spades: "♠️",
      spaghetti: "🍝",
      sparkle: "❇️",
      sparkler: "🎇",
      sparkles: "✨",
      sparkling_heart: "💖",
      speak_no_evil: "🙊",
      speaker: "🔈",
      speaking_head: "🗣",
      speech_balloon: "💬",
      speedboat: "🚤",
      spider: "🕷",
      spider_web: "🕸",
      spiral_calendar: "🗓",
      spiral_notepad: "🗒",
      spoon: "🥄",
      squid: "🦑",
      stadium: "🏟",
      star: "⭐️",
      star2: "🌟",
      star_and_crescent: "☪️",
      star_of_david: "✡️",
      stars: "🌠",
      station: "🚉",
      statue_of_liberty: "🗽",
      steam_locomotive: "🚂",
      stew: "🍲",
      stop_button: "⏹",
      stop_sign: "🛑",
      stopwatch: "⏱",
      straight_ruler: "📏",
      strawberry: "🍓",
      stuck_out_tongue: "😛",
      stuck_out_tongue_closed_eyes: "😝",
      stuck_out_tongue_winking_eye: "😜",
      studio_microphone: "🎙",
      stuffed_flatbread: "🥙",
      sun_behind_large_cloud: "🌥",
      sun_behind_rain_cloud: "🌦",
      sun_behind_small_cloud: "🌤",
      sun_with_face: "🌞",
      sunflower: "🌻",
      sunglasses: "😎",
      sunny: "☀️",
      sunrise: "🌅",
      sunrise_over_mountains: "🌄",
      surfing_man: "🏄",
      surfing_woman: "🏄&zwj;♀️",
      sushi: "🍣",
      suspension_railway: "🚟",
      sweat: "😓",
      sweat_drops: "💦",
      sweat_smile: "😅",
      sweet_potato: "🍠",
      swimming_man: "🏊",
      swimming_woman: "🏊&zwj;♀️",
      symbols: "🔣",
      synagogue: "🕍",
      syringe: "💉",
      taco: "🌮",
      tada: "🎉",
      tanabata_tree: "🎋",
      taurus: "♉️",
      taxi: "🚕",
      tea: "🍵",
      telephone_receiver: "📞",
      telescope: "🔭",
      tennis: "🎾",
      tent: "⛺️",
      thermometer: "🌡",
      thinking: "🤔",
      thought_balloon: "💭",
      ticket: "🎫",
      tickets: "🎟",
      tiger: "🐯",
      tiger2: "🐅",
      timer_clock: "⏲",
      tipping_hand_man: "💁&zwj;♂️",
      tired_face: "😫",
      tm: "™️",
      toilet: "🚽",
      tokyo_tower: "🗼",
      tomato: "🍅",
      tongue: "👅",
      top: "🔝",
      tophat: "🎩",
      tornado: "🌪",
      trackball: "🖲",
      tractor: "🚜",
      traffic_light: "🚥",
      train: "🚋",
      train2: "🚆",
      tram: "🚊",
      triangular_flag_on_post: "🚩",
      triangular_ruler: "📐",
      trident: "🔱",
      triumph: "😤",
      trolleybus: "🚎",
      trophy: "🏆",
      tropical_drink: "🍹",
      tropical_fish: "🐠",
      truck: "🚚",
      trumpet: "🎺",
      tulip: "🌷",
      tumbler_glass: "🥃",
      turkey: "🦃",
      turtle: "🐢",
      tv: "📺",
      twisted_rightwards_arrows: "🔀",
      two_hearts: "💕",
      two_men_holding_hands: "👬",
      two_women_holding_hands: "👭",
      u5272: "🈹",
      u5408: "🈴",
      u55b6: "🈺",
      u6307: "🈯️",
      u6708: "🈷️",
      u6709: "🈶",
      u6e80: "🈵",
      u7121: "🈚️",
      u7533: "🈸",
      u7981: "🈲",
      u7a7a: "🈳",
      umbrella: "☔️",
      unamused: "😒",
      underage: "🔞",
      unicorn: "🦄",
      unlock: "🔓",
      up: "🆙",
      upside_down_face: "🙃",
      v: "✌️",
      vertical_traffic_light: "🚦",
      vhs: "📼",
      vibration_mode: "📳",
      video_camera: "📹",
      video_game: "🎮",
      violin: "🎻",
      virgo: "♍️",
      volcano: "🌋",
      volleyball: "🏐",
      vs: "🆚",
      vulcan_salute: "🖖",
      walking_man: "🚶",
      walking_woman: "🚶&zwj;♀️",
      waning_crescent_moon: "🌘",
      waning_gibbous_moon: "🌖",
      warning: "⚠️",
      wastebasket: "🗑",
      watch: "⌚️",
      water_buffalo: "🐃",
      watermelon: "🍉",
      wave: "👋",
      wavy_dash: "〰️",
      waxing_crescent_moon: "🌒",
      wc: "🚾",
      weary: "😩",
      wedding: "💒",
      weight_lifting_man: "🏋️",
      weight_lifting_woman: "🏋️&zwj;♀️",
      whale: "🐳",
      whale2: "🐋",
      wheel_of_dharma: "☸️",
      wheelchair: "♿️",
      white_check_mark: "✅",
      white_circle: "⚪️",
      white_flag: "🏳️",
      white_flower: "💮",
      white_large_square: "⬜️",
      white_medium_small_square: "◽️",
      white_medium_square: "◻️",
      white_small_square: "▫️",
      white_square_button: "🔳",
      wilted_flower: "🥀",
      wind_chime: "🎐",
      wind_face: "🌬",
      wine_glass: "🍷",
      wink: "😉",
      wolf: "🐺",
      woman: "👩",
      woman_artist: "👩&zwj;🎨",
      woman_astronaut: "👩&zwj;🚀",
      woman_cartwheeling: "🤸&zwj;♀️",
      woman_cook: "👩&zwj;🍳",
      woman_facepalming: "🤦&zwj;♀️",
      woman_factory_worker: "👩&zwj;🏭",
      woman_farmer: "👩&zwj;🌾",
      woman_firefighter: "👩&zwj;🚒",
      woman_health_worker: "👩&zwj;⚕️",
      woman_judge: "👩&zwj;⚖️",
      woman_juggling: "🤹&zwj;♀️",
      woman_mechanic: "👩&zwj;🔧",
      woman_office_worker: "👩&zwj;💼",
      woman_pilot: "👩&zwj;✈️",
      woman_playing_handball: "🤾&zwj;♀️",
      woman_playing_water_polo: "🤽&zwj;♀️",
      woman_scientist: "👩&zwj;🔬",
      woman_shrugging: "🤷&zwj;♀️",
      woman_singer: "👩&zwj;🎤",
      woman_student: "👩&zwj;🎓",
      woman_teacher: "👩&zwj;🏫",
      woman_technologist: "👩&zwj;💻",
      woman_with_turban: "👳&zwj;♀️",
      womans_clothes: "👚",
      womans_hat: "👒",
      women_wrestling: "🤼&zwj;♀️",
      womens: "🚺",
      world_map: "🗺",
      worried: "😟",
      wrench: "🔧",
      writing_hand: "✍️",
      x: "❌",
      yellow_heart: "💛",
      yen: "💴",
      yin_yang: "☯️",
      yum: "😋",
      zap: "⚡️",
      zipper_mouth_face: "🤐",
      zzz: "💤",
      octocat: '<img alt=":octocat:" height="20" width="20" align="absmiddle" src="https://assets-cdn.github.com/images/icons/emoji/octocat.png">',
      showdown: `<span style="font-family: 'Anonymous Pro', monospace; text-decoration: underline; text-decoration-style: dashed; text-decoration-color: #3e8b8a;text-underline-position: under;">S</span>`
    }, s.Converter = function(m) {
      "use strict";
      var b = {}, v = [], T = [], I = {}, x = l, P = {
        parsed: {},
        raw: "",
        format: ""
      };
      L();
      function L() {
        m = m || {};
        for (var O in a) a.hasOwnProperty(O) && (b[O] = a[O]);
        if (typeof m == "object")
          for (var Y in m) m.hasOwnProperty(Y) && (b[Y] = m[Y]);
        else throw Error("Converter expects the passed parameter to be an object, but " + typeof m + " was passed instead.");
        b.extensions && s.helper.forEach(b.extensions, G);
      }
      function G(O, Y) {
        if (Y = Y || null, s.helper.isString(O))
          if (O = s.helper.stdExtName(O), Y = O, s.extensions[O]) {
            console.warn("DEPRECATION WARNING: " + O + " is an old extension that uses a deprecated loading method.Please inform the developer that the extension should be updated!"), z(s.extensions[O], O);
            return;
          } else if (!s.helper.isUndefined(i[O])) O = i[O];
          else throw Error('Extension "' + O + '" could not be loaded. It was either not found or is not a valid extension.');
        typeof O == "function" && (O = O()), s.helper.isArray(O) || (O = [O]);
        var X = d(O, Y);
        if (!X.valid) throw Error(X.error);
        for (var j = 0; j < O.length; ++j) {
          switch (O[j].type) {
            case "lang":
              v.push(O[j]);
              break;
            case "output":
              T.push(O[j]);
              break;
          }
          if (O[j].hasOwnProperty("listeners"))
            for (var re in O[j].listeners) O[j].listeners.hasOwnProperty(re) && V(re, O[j].listeners[re]);
        }
      }
      function z(O, Y) {
        typeof O == "function" && (O = O(new s.Converter())), s.helper.isArray(O) || (O = [O]);
        var X = d(O, Y);
        if (!X.valid) throw Error(X.error);
        for (var j = 0; j < O.length; ++j) switch (O[j].type) {
          case "lang":
            v.push(O[j]);
            break;
          case "output":
            T.push(O[j]);
            break;
          default:
            throw Error("Extension loader error: Type unrecognized!!!");
        }
      }
      function V(O, Y) {
        if (!s.helper.isString(O)) throw Error("Invalid argument in converter.listen() method: name must be a string, but " + typeof O + " given");
        if (typeof Y != "function") throw Error("Invalid argument in converter.listen() method: callback must be a function, but " + typeof Y + " given");
        I.hasOwnProperty(O) || (I[O] = []), I[O].push(Y);
      }
      function Z(O) {
        var Y = O.match(/^\s*/)[0].length, X = new RegExp("^\\s{0," + Y + "}", "gm");
        return O.replace(X, "");
      }
      this._dispatch = function(Y, X, j, re) {
        if (I.hasOwnProperty(Y)) for (var pe = 0; pe < I[Y].length; ++pe) {
          var B = I[Y][pe](Y, X, this, j, re);
          B && typeof B < "u" && (X = B);
        }
        return X;
      }, this.listen = function(O, Y) {
        return V(O, Y), this;
      }, this.makeHtml = function(O) {
        if (!O) return O;
        var Y = {
          gHtmlBlocks: [],
          gHtmlMdBlocks: [],
          gHtmlSpans: [],
          gUrls: {},
          gTitles: {},
          gDimensions: {},
          gListLevel: 0,
          hashLinkCounts: {},
          langExtensions: v,
          outputModifiers: T,
          converter: this,
          ghCodeBlocks: [],
          metadata: {
            parsed: {},
            raw: "",
            format: ""
          }
        };
        return O = O.replace(/¨/g, "¨T"), O = O.replace(/\$/g, "¨D"), O = O.replace(/\r\n/g, `
`), O = O.replace(/\r/g, `
`), O = O.replace(/\u00A0/g, "&nbsp;"), b.smartIndentationFix && (O = Z(O)), O = `

` + O + `

`, O = s.subParser("detab")(O, b, Y), O = O.replace(/^[ \t]+$/gm, ""), s.helper.forEach(v, function(X) {
          O = s.subParser("runExtension")(X, O, b, Y);
        }), O = s.subParser("metadata")(O, b, Y), O = s.subParser("hashPreCodeTags")(O, b, Y), O = s.subParser("githubCodeBlocks")(O, b, Y), O = s.subParser("hashHTMLBlocks")(O, b, Y), O = s.subParser("hashCodeTags")(O, b, Y), O = s.subParser("stripLinkDefinitions")(O, b, Y), O = s.subParser("blockGamut")(O, b, Y), O = s.subParser("unhashHTMLSpans")(O, b, Y), O = s.subParser("unescapeSpecialChars")(O, b, Y), O = O.replace(/¨D/g, "$$"), O = O.replace(/¨T/g, "¨"), O = s.subParser("completeHTMLDocument")(O, b, Y), s.helper.forEach(T, function(X) {
          O = s.subParser("runExtension")(X, O, b, Y);
        }), P = Y.metadata, O;
      }, this.makeMarkdown = this.makeMd = function(O, Y) {
        if (O = O.replace(/\r\n/g, `
`), O = O.replace(/\r/g, `
`), O = O.replace(/>[ \t]+</, ">¨NBSP;<"), !Y) if (window && window.document) Y = window.document;
        else throw new Error("HTMLParser is undefined. If in a webworker or nodejs environment, you need to provide a WHATWG DOM and HTML such as JSDOM");
        var X = Y.createElement("div");
        X.innerHTML = O;
        var j = { preList: H(X) };
        N(X);
        for (var re = X.childNodes, pe = "", B = 0; B < re.length; B++) pe += s.subParser("makeMarkdown.node")(re[B], j);
        function N(le) {
          for (var Ie = 0; Ie < le.childNodes.length; ++Ie) {
            var De = le.childNodes[Ie];
            De.nodeType === 3 ? !/\S/.test(De.nodeValue) && !/^[ ]+$/.test(De.nodeValue) ? (le.removeChild(De), --Ie) : (De.nodeValue = De.nodeValue.split(`
`).join(" "), De.nodeValue = De.nodeValue.replace(/(\s)+/g, "$1")) : De.nodeType === 1 && N(De);
          }
        }
        function H(le) {
          for (var Ie = le.querySelectorAll("pre"), De = [], Be = 0; Be < Ie.length; ++Be) if (Ie[Be].childElementCount === 1 && Ie[Be].firstChild.tagName.toLowerCase() === "code") {
            var it = Ie[Be].firstChild.innerHTML.trim(), Qe = Ie[Be].firstChild.getAttribute("data-language") || "";
            if (Qe === "")
              for (var je = Ie[Be].firstChild.className.split(" "), W = 0; W < je.length; ++W) {
                var ee = je[W].match(/^language-(.+)$/);
                if (ee !== null) {
                  Qe = ee[1];
                  break;
                }
              }
            it = s.helper.unescapeHTMLEntities(it), De.push(it), Ie[Be].outerHTML = '<precode language="' + Qe + '" precodenum="' + Be.toString() + '"></precode>';
          } else
            De.push(Ie[Be].innerHTML), Ie[Be].innerHTML = "", Ie[Be].setAttribute("prenum", Be.toString());
          return De;
        }
        return pe;
      }, this.setOption = function(O, Y) {
        b[O] = Y;
      }, this.getOption = function(O) {
        return b[O];
      }, this.getOptions = function() {
        return b;
      }, this.addExtension = function(O, Y) {
        Y = Y || null, G(O, Y);
      }, this.useExtension = function(O) {
        G(O);
      }, this.setFlavor = function(O) {
        if (!u.hasOwnProperty(O)) throw Error(O + " flavor was not found");
        var Y = u[O];
        x = O;
        for (var X in Y) Y.hasOwnProperty(X) && (b[X] = Y[X]);
      }, this.getFlavor = function() {
        return x;
      }, this.removeExtension = function(O) {
        s.helper.isArray(O) || (O = [O]);
        for (var Y = 0; Y < O.length; ++Y) {
          for (var X = O[Y], j = 0; j < v.length; ++j) v[j] === X && v.splice(j, 1);
          for (var re = 0; re < T.length; ++re) T[re] === X && T.splice(re, 1);
        }
      }, this.getAllExtensions = function() {
        return {
          language: v,
          output: T
        };
      }, this.getMetadata = function(O) {
        return O ? P.raw : P.parsed;
      }, this.getMetadataFormat = function() {
        return P.format;
      }, this._setMetadataPair = function(O, Y) {
        P.parsed[O] = Y;
      }, this._setMetadataFormat = function(O) {
        P.format = O;
      }, this._setMetadataRaw = function(O) {
        P.raw = O;
      };
    }, s.subParser("anchors", function(m, b, v) {
      "use strict";
      m = v.converter._dispatch("anchors.before", m, b, v);
      var T = function(I, x, P, L, G, z, V) {
        if (s.helper.isUndefined(V) && (V = ""), P = P.toLowerCase(), I.search(/\(<?\s*>? ?(['"].*['"])?\)$/m) > -1) L = "";
        else if (!L)
          if (P || (P = x.toLowerCase().replace(/ ?\n/g, " ")), L = "#" + P, !s.helper.isUndefined(v.gUrls[P]))
            L = v.gUrls[P], s.helper.isUndefined(v.gTitles[P]) || (V = v.gTitles[P]);
          else return I;
        L = L.replace(s.helper.regexes.asteriskDashAndColon, s.helper.escapeCharactersCallback);
        var Z = '<a href="' + L + '"';
        return V !== "" && V !== null && (V = V.replace(/"/g, "&quot;"), V = V.replace(s.helper.regexes.asteriskDashAndColon, s.helper.escapeCharactersCallback), Z += ' title="' + V + '"'), b.openLinksInNewWindow && !/^#/.test(L) && (Z += ' rel="noopener noreferrer" target="¨E95Eblank"'), Z += ">" + x + "</a>", Z;
      };
      return m = m.replace(/\[((?:\[[^\]]*]|[^\[\]])*)] ?(?:\n *)?\[(.*?)]()()()()/g, T), m = m.replace(/\[((?:\[[^\]]*]|[^\[\]])*)]()[ \t]*\([ \t]?<([^>]*)>(?:[ \t]*((["'])([^"]*?)\5))?[ \t]?\)/g, T), m = m.replace(/\[((?:\[[^\]]*]|[^\[\]])*)]()[ \t]*\([ \t]?<?([\S]+?(?:\([\S]*?\)[\S]*?)?)>?(?:[ \t]*((["'])([^"]*?)\5))?[ \t]?\)/g, T), m = m.replace(/\[([^\[\]]+)]()()()()()/g, T), b.ghMentions && (m = m.replace(/(^|\s)(\\)?(@([a-z\d]+(?:[a-z\d.-]+?[a-z\d]+)*))/gim, function(I, x, P, L, G) {
        if (P === "\\") return x + L;
        if (!s.helper.isString(b.ghMentionsLink)) throw new Error("ghMentionsLink option must be a string");
        var z = b.ghMentionsLink.replace(/\{u}/g, G), V = "";
        return b.openLinksInNewWindow && (V = ' rel="noopener noreferrer" target="¨E95Eblank"'), x + '<a href="' + z + '"' + V + ">" + L + "</a>";
      })), m = v.converter._dispatch("anchors.after", m, b, v), m;
    });
    var y = /([*~_]+|\b)(((https?|ftp|dict):\/\/|www\.)[^'">\s]+?\.[^'">\s]+?)()(\1)?(?=\s|$)(?!["<>])/gi, w = /([*~_]+|\b)(((https?|ftp|dict):\/\/|www\.)[^'">\s]+\.[^'">\s]+?)([.!?,()\[\]])?(\1)?(?=\s|$)(?!["<>])/gi, E = /()<(((https?|ftp|dict):\/\/|www\.)[^'">\s]+)()>()/gi, C = /(^|\s)(?:mailto:)?([A-Za-z0-9!#$%&'*+-/=?^_`{|}~.]+@[-a-z0-9]+(\.[-a-z0-9]+)*\.[a-z]+)(?=$|\s)/gim, M = /<()(?:mailto:)?([-.\w]+@[-a-z0-9]+(\.[-a-z0-9]+)*\.[a-z]+)>/gi, k = function(m) {
      "use strict";
      return function(b, v, T, I, x, P, L) {
        T = T.replace(s.helper.regexes.asteriskDashAndColon, s.helper.escapeCharactersCallback);
        var G = T, z = "", V = "", Z = v || "", O = L || "";
        return /^www\./i.test(T) && (T = T.replace(/^www\./i, "http://www.")), m.excludeTrailingPunctuationFromURLs && P && (z = P), m.openLinksInNewWindow && (V = ' rel="noopener noreferrer" target="¨E95Eblank"'), Z + '<a href="' + T + '"' + V + ">" + G + "</a>" + z + O;
      };
    }, R = function(m, b) {
      "use strict";
      return function(v, T, I) {
        var x = "mailto:";
        return T = T || "", I = s.subParser("unescapeSpecialChars")(I, m, b), m.encodeEmails ? (x = s.helper.encodeEmailAddress(x + I), I = s.helper.encodeEmailAddress(I)) : x = x + I, T + '<a href="' + x + '">' + I + "</a>";
      };
    };
    s.subParser("autoLinks", function(m, b, v) {
      "use strict";
      return m = v.converter._dispatch("autoLinks.before", m, b, v), m = m.replace(E, k(b)), m = m.replace(M, R(b, v)), m = v.converter._dispatch("autoLinks.after", m, b, v), m;
    }), s.subParser("simplifiedAutoLinks", function(m, b, v) {
      "use strict";
      return b.simplifiedAutoLink && (m = v.converter._dispatch("simplifiedAutoLinks.before", m, b, v), b.excludeTrailingPunctuationFromURLs ? m = m.replace(w, k(b)) : m = m.replace(y, k(b)), m = m.replace(C, R(b, v)), m = v.converter._dispatch("simplifiedAutoLinks.after", m, b, v)), m;
    }), s.subParser("blockGamut", function(m, b, v) {
      "use strict";
      return m = v.converter._dispatch("blockGamut.before", m, b, v), m = s.subParser("blockQuotes")(m, b, v), m = s.subParser("headers")(m, b, v), m = s.subParser("horizontalRule")(m, b, v), m = s.subParser("lists")(m, b, v), m = s.subParser("codeBlocks")(m, b, v), m = s.subParser("tables")(m, b, v), m = s.subParser("hashHTMLBlocks")(m, b, v), m = s.subParser("paragraphs")(m, b, v), m = v.converter._dispatch("blockGamut.after", m, b, v), m;
    }), s.subParser("blockQuotes", function(m, b, v) {
      "use strict";
      m = v.converter._dispatch("blockQuotes.before", m, b, v), m = m + `

`;
      var T = /(^ {0,3}>[ \t]?.+\n(.+\n)*\n*)+/gm;
      return b.splitAdjacentBlockquotes && (T = /^ {0,3}>[\s\S]*?(?:\n\n)/gm), m = m.replace(T, function(I) {
        return I = I.replace(/^[ \t]*>[ \t]?/gm, ""), I = I.replace(/¨0/g, ""), I = I.replace(/^[ \t]+$/gm, ""), I = s.subParser("githubCodeBlocks")(I, b, v), I = s.subParser("blockGamut")(I, b, v), I = I.replace(/(^|\n)/g, "$1  "), I = I.replace(/(\s*<pre>[^\r]+?<\/pre>)/gm, function(x, P) {
          var L = P;
          return L = L.replace(/^  /gm, "¨0"), L = L.replace(/¨0/g, ""), L;
        }), s.subParser("hashBlock")(`<blockquote>
` + I + `
</blockquote>`, b, v);
      }), m = v.converter._dispatch("blockQuotes.after", m, b, v), m;
    }), s.subParser("codeBlocks", function(m, b, v) {
      "use strict";
      return m = v.converter._dispatch("codeBlocks.before", m, b, v), m += "¨0", m = m.replace(/(?:\n\n|^)((?:(?:[ ]{4}|\t).*\n+)+)(\n*[ ]{0,3}[^ \t\n]|(?=¨0))/g, function(T, I, x) {
        var P = I, L = x, G = `
`;
        return P = s.subParser("outdent")(P, b, v), P = s.subParser("encodeCode")(P, b, v), P = s.subParser("detab")(P, b, v), P = P.replace(/^\n+/g, ""), P = P.replace(/\n+$/g, ""), b.omitExtraWLInCodeBlocks && (G = ""), P = "<pre><code>" + P + G + "</code></pre>", s.subParser("hashBlock")(P, b, v) + L;
      }), m = m.replace(/¨0/, ""), m = v.converter._dispatch("codeBlocks.after", m, b, v), m;
    }), s.subParser("codeSpans", function(m, b, v) {
      "use strict";
      return m = v.converter._dispatch("codeSpans.before", m, b, v), typeof m > "u" && (m = ""), m = m.replace(/(^|[^\\])(`+)([^\r]*?[^`])\2(?!`)/gm, function(T, I, x, P) {
        var L = P;
        return L = L.replace(/^([ \t]*)/g, ""), L = L.replace(/[ \t]*$/g, ""), L = s.subParser("encodeCode")(L, b, v), L = I + "<code>" + L + "</code>", L = s.subParser("hashHTMLSpans")(L, b, v), L;
      }), m = v.converter._dispatch("codeSpans.after", m, b, v), m;
    }), s.subParser("completeHTMLDocument", function(m, b, v) {
      "use strict";
      if (!b.completeHTMLDocument) return m;
      m = v.converter._dispatch("completeHTMLDocument.before", m, b, v);
      var T = "html", I = `<!DOCTYPE HTML>
`, x = "", P = `<meta charset="utf-8">
`, L = "", G = "";
      typeof v.metadata.parsed.doctype < "u" && (I = "<!DOCTYPE " + v.metadata.parsed.doctype + `>
`, T = v.metadata.parsed.doctype.toString().toLowerCase(), (T === "html" || T === "html5") && (P = '<meta charset="utf-8">'));
      for (var z in v.metadata.parsed) if (v.metadata.parsed.hasOwnProperty(z)) switch (z.toLowerCase()) {
        case "doctype":
          break;
        case "title":
          x = "<title>" + v.metadata.parsed.title + `</title>
`;
          break;
        case "charset":
          T === "html" || T === "html5" ? P = '<meta charset="' + v.metadata.parsed.charset + `">
` : P = '<meta name="charset" content="' + v.metadata.parsed.charset + `">
`;
          break;
        case "language":
        case "lang":
          L = ' lang="' + v.metadata.parsed[z] + '"', G += '<meta name="' + z + '" content="' + v.metadata.parsed[z] + `">
`;
          break;
        default:
          G += '<meta name="' + z + '" content="' + v.metadata.parsed[z] + `">
`;
      }
      return m = I + "<html" + L + `>
<head>
` + x + P + G + `</head>
<body>
` + m.trim() + `
</body>
</html>`, m = v.converter._dispatch("completeHTMLDocument.after", m, b, v), m;
    }), s.subParser("detab", function(m, b, v) {
      "use strict";
      return m = v.converter._dispatch("detab.before", m, b, v), m = m.replace(/\t(?=\t)/g, "    "), m = m.replace(/\t/g, "¨A¨B"), m = m.replace(/¨B(.+?)¨A/g, function(T, I) {
        for (var x = I, P = 4 - x.length % 4, L = 0; L < P; L++) x += " ";
        return x;
      }), m = m.replace(/¨A/g, "    "), m = m.replace(/¨B/g, ""), m = v.converter._dispatch("detab.after", m, b, v), m;
    }), s.subParser("ellipsis", function(m, b, v) {
      "use strict";
      return b.ellipsis && (m = v.converter._dispatch("ellipsis.before", m, b, v), m = m.replace(/\.\.\./g, "…"), m = v.converter._dispatch("ellipsis.after", m, b, v)), m;
    }), s.subParser("emoji", function(m, b, v) {
      "use strict";
      return b.emoji && (m = v.converter._dispatch("emoji.before", m, b, v), m = m.replace(/:([\S]+?):/g, function(T, I) {
        return s.helper.emojis.hasOwnProperty(I) ? s.helper.emojis[I] : T;
      }), m = v.converter._dispatch("emoji.after", m, b, v)), m;
    }), s.subParser("encodeAmpsAndAngles", function(m, b, v) {
      "use strict";
      return m = v.converter._dispatch("encodeAmpsAndAngles.before", m, b, v), m = m.replace(/&(?!#?[xX]?(?:[0-9a-fA-F]+|\w+);)/g, "&amp;"), m = m.replace(/<(?![a-z\/?$!])/gi, "&lt;"), m = m.replace(/</g, "&lt;"), m = m.replace(/>/g, "&gt;"), m = v.converter._dispatch("encodeAmpsAndAngles.after", m, b, v), m;
    }), s.subParser("encodeBackslashEscapes", function(m, b, v) {
      "use strict";
      return m = v.converter._dispatch("encodeBackslashEscapes.before", m, b, v), m = m.replace(/\\(\\)/g, s.helper.escapeCharactersCallback), m = m.replace(/\\([`*_{}\[\]()>#+.!~=|:-])/g, s.helper.escapeCharactersCallback), m = v.converter._dispatch("encodeBackslashEscapes.after", m, b, v), m;
    }), s.subParser("encodeCode", function(m, b, v) {
      "use strict";
      return m = v.converter._dispatch("encodeCode.before", m, b, v), m = m.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/([*_{}\[\]\\=~-])/g, s.helper.escapeCharactersCallback), m = v.converter._dispatch("encodeCode.after", m, b, v), m;
    }), s.subParser("escapeSpecialCharsWithinTagAttributes", function(m, b, v) {
      "use strict";
      m = v.converter._dispatch("escapeSpecialCharsWithinTagAttributes.before", m, b, v);
      var T = /<\/?[a-z\d_:-]+(?:[\s]+[\s\S]+?)?>/gi, I = /<!(--(?:(?:[^>-]|-[^>])(?:[^-]|-[^-])*)--)>/gi;
      return m = m.replace(T, function(x) {
        return x.replace(/(.)<\/?code>(?=.)/g, "$1`").replace(/([\\`*_~=|])/g, s.helper.escapeCharactersCallback);
      }), m = m.replace(I, function(x) {
        return x.replace(/([\\`*_~=|])/g, s.helper.escapeCharactersCallback);
      }), m = v.converter._dispatch("escapeSpecialCharsWithinTagAttributes.after", m, b, v), m;
    }), s.subParser("githubCodeBlocks", function(m, b, v) {
      "use strict";
      return b.ghCodeBlocks ? (m = v.converter._dispatch("githubCodeBlocks.before", m, b, v), m += "¨0", m = m.replace(/(?:^|\n)(?: {0,3})(```+|~~~+)(?: *)([^\s`~]*)\n([\s\S]*?)\n(?: {0,3})\1/g, function(T, I, x, P) {
        var L = b.omitExtraWLInCodeBlocks ? "" : `
`;
        return P = s.subParser("encodeCode")(P, b, v), P = s.subParser("detab")(P, b, v), P = P.replace(/^\n+/g, ""), P = P.replace(/\n+$/g, ""), P = "<pre><code" + (x ? ' class="' + x + " language-" + x + '"' : "") + ">" + P + L + "</code></pre>", P = s.subParser("hashBlock")(P, b, v), `

¨G` + (v.ghCodeBlocks.push({
          text: T,
          codeblock: P
        }) - 1) + `G

`;
      }), m = m.replace(/¨0/, ""), v.converter._dispatch("githubCodeBlocks.after", m, b, v)) : m;
    }), s.subParser("hashBlock", function(m, b, v) {
      "use strict";
      return m = v.converter._dispatch("hashBlock.before", m, b, v), m = m.replace(/(^\n+|\n+$)/g, ""), m = `

¨K` + (v.gHtmlBlocks.push(m) - 1) + `K

`, m = v.converter._dispatch("hashBlock.after", m, b, v), m;
    }), s.subParser("hashCodeTags", function(m, b, v) {
      "use strict";
      m = v.converter._dispatch("hashCodeTags.before", m, b, v);
      var T = function(I, x, P, L) {
        var G = P + s.subParser("encodeCode")(x, b, v) + L;
        return "¨C" + (v.gHtmlSpans.push(G) - 1) + "C";
      };
      return m = s.helper.replaceRecursiveRegExp(m, T, "<code\\b[^>]*>", "</code>", "gim"), m = v.converter._dispatch("hashCodeTags.after", m, b, v), m;
    }), s.subParser("hashElement", function(m, b, v) {
      "use strict";
      return function(T, I) {
        var x = I;
        return x = x.replace(/\n\n/g, `
`), x = x.replace(/^\n/, ""), x = x.replace(/\n+$/g, ""), x = `

¨K` + (v.gHtmlBlocks.push(x) - 1) + `K

`, x;
      };
    }), s.subParser("hashHTMLBlocks", function(m, b, v) {
      "use strict";
      m = v.converter._dispatch("hashHTMLBlocks.before", m, b, v);
      var T = [
        "pre",
        "div",
        "h1",
        "h2",
        "h3",
        "h4",
        "h5",
        "h6",
        "blockquote",
        "table",
        "dl",
        "ol",
        "ul",
        "script",
        "noscript",
        "form",
        "fieldset",
        "iframe",
        "math",
        "style",
        "section",
        "header",
        "footer",
        "nav",
        "article",
        "aside",
        "address",
        "audio",
        "canvas",
        "figure",
        "hgroup",
        "output",
        "video",
        "p"
      ], I = function(O, Y, X, j) {
        var re = O;
        return X.search(/\bmarkdown\b/) !== -1 && (re = X + v.converter.makeHtml(Y) + j), `

¨K` + (v.gHtmlBlocks.push(re) - 1) + `K

`;
      };
      b.backslashEscapesHTMLTags && (m = m.replace(/\\<(\/?[^>]+?)>/g, function(O, Y) {
        return "&lt;" + Y + "&gt;";
      }));
      for (var x = 0; x < T.length; ++x)
        for (var P, L = new RegExp("^ {0,3}(<" + T[x] + "\\b[^>]*>)", "im"), G = "<" + T[x] + "\\b[^>]*>", z = "</" + T[x] + ">"; (P = s.helper.regexIndexOf(m, L)) !== -1; ) {
          var V = s.helper.splitAtIndex(m, P), Z = s.helper.replaceRecursiveRegExp(V[1], I, G, z, "im");
          if (Z === V[1]) break;
          m = V[0].concat(Z);
        }
      return m = m.replace(/(\n {0,3}(<(hr)\b([^<>])*?\/?>)[ \t]*(?=\n{2,}))/g, s.subParser("hashElement")(m, b, v)), m = s.helper.replaceRecursiveRegExp(m, function(O) {
        return `

¨K` + (v.gHtmlBlocks.push(O) - 1) + `K

`;
      }, "^ {0,3}<!--", "-->", "gm"), m = m.replace(/(?:\n\n)( {0,3}(?:<([?%])[^\r]*?\2>)[ \t]*(?=\n{2,}))/g, s.subParser("hashElement")(m, b, v)), m = v.converter._dispatch("hashHTMLBlocks.after", m, b, v), m;
    }), s.subParser("hashHTMLSpans", function(m, b, v) {
      "use strict";
      m = v.converter._dispatch("hashHTMLSpans.before", m, b, v);
      function T(I) {
        return "¨C" + (v.gHtmlSpans.push(I) - 1) + "C";
      }
      return m = m.replace(/<[^>]+?\/>/gi, function(I) {
        return T(I);
      }), m = m.replace(/<([^>]+?)>[\s\S]*?<\/\1>/g, function(I) {
        return T(I);
      }), m = m.replace(/<([^>]+?)\s[^>]+?>[\s\S]*?<\/\1>/g, function(I) {
        return T(I);
      }), m = m.replace(/<[^>]+?>/gi, function(I) {
        return T(I);
      }), m = v.converter._dispatch("hashHTMLSpans.after", m, b, v), m;
    }), s.subParser("unhashHTMLSpans", function(m, b, v) {
      "use strict";
      m = v.converter._dispatch("unhashHTMLSpans.before", m, b, v);
      for (var T = 0; T < v.gHtmlSpans.length; ++T) {
        for (var I = v.gHtmlSpans[T], x = 0; /¨C(\d+)C/.test(I); ) {
          var P = RegExp.$1;
          if (I = I.replace("¨C" + P + "C", v.gHtmlSpans[P]), x === 10) {
            console.error("maximum nesting of 10 spans reached!!!");
            break;
          }
          ++x;
        }
        m = m.replace("¨C" + T + "C", I);
      }
      return m = v.converter._dispatch("unhashHTMLSpans.after", m, b, v), m;
    }), s.subParser("hashPreCodeTags", function(m, b, v) {
      "use strict";
      m = v.converter._dispatch("hashPreCodeTags.before", m, b, v);
      var T = function(I, x, P, L) {
        var G = P + s.subParser("encodeCode")(x, b, v) + L;
        return `

¨G` + (v.ghCodeBlocks.push({
          text: I,
          codeblock: G
        }) - 1) + `G

`;
      };
      return m = s.helper.replaceRecursiveRegExp(m, T, "^ {0,3}<pre\\b[^>]*>\\s*<code\\b[^>]*>", "^ {0,3}</code>\\s*</pre>", "gim"), m = v.converter._dispatch("hashPreCodeTags.after", m, b, v), m;
    }), s.subParser("headers", function(m, b, v) {
      "use strict";
      m = v.converter._dispatch("headers.before", m, b, v);
      var T = isNaN(parseInt(b.headerLevelStart)) ? 1 : parseInt(b.headerLevelStart), I = b.smoothLivePreview ? /^(.+)[ \t]*\n={2,}[ \t]*\n+/gm : /^(.+)[ \t]*\n=+[ \t]*\n+/gm, x = b.smoothLivePreview ? /^(.+)[ \t]*\n-{2,}[ \t]*\n+/gm : /^(.+)[ \t]*\n-+[ \t]*\n+/gm;
      m = m.replace(I, function(G, z) {
        var V = s.subParser("spanGamut")(z, b, v), Z = b.noHeaderId ? "" : ' id="' + L(z) + '"', O = T, Y = "<h" + O + Z + ">" + V + "</h" + O + ">";
        return s.subParser("hashBlock")(Y, b, v);
      }), m = m.replace(x, function(G, z) {
        var V = s.subParser("spanGamut")(z, b, v), Z = b.noHeaderId ? "" : ' id="' + L(z) + '"', O = T + 1, Y = "<h" + O + Z + ">" + V + "</h" + O + ">";
        return s.subParser("hashBlock")(Y, b, v);
      });
      var P = b.requireSpaceBeforeHeadingText ? /^(#{1,6})[ \t]+(.+?)[ \t]*#*\n+/gm : /^(#{1,6})[ \t]*(.+?)[ \t]*#*\n+/gm;
      m = m.replace(P, function(G, z, V) {
        var Z = V;
        b.customizedHeaderId && (Z = V.replace(/\s?\{([^{]+?)}\s*$/, ""));
        var O = s.subParser("spanGamut")(Z, b, v), Y = b.noHeaderId ? "" : ' id="' + L(V) + '"', X = T - 1 + z.length, j = "<h" + X + Y + ">" + O + "</h" + X + ">";
        return s.subParser("hashBlock")(j, b, v);
      });
      function L(G) {
        var z, V;
        if (b.customizedHeaderId) {
          var Z = G.match(/\{([^{]+?)}\s*$/);
          Z && Z[1] && (G = Z[1]);
        }
        return z = G, s.helper.isString(b.prefixHeaderId) ? V = b.prefixHeaderId : b.prefixHeaderId === !0 ? V = "section-" : V = "", b.rawPrefixHeaderId || (z = V + z), b.ghCompatibleHeaderId ? z = z.replace(/ /g, "-").replace(/&amp;/g, "").replace(/¨T/g, "").replace(/¨D/g, "").replace(/[&+$,\/:;=?@"#{}|^¨~\[\]`\\*)(%.!'<>]/g, "").toLowerCase() : b.rawHeaderId ? z = z.replace(/ /g, "-").replace(/&amp;/g, "&").replace(/¨T/g, "¨").replace(/¨D/g, "$").replace(/["']/g, "-").toLowerCase() : z = z.replace(/[^\w]/g, "").toLowerCase(), b.rawPrefixHeaderId && (z = V + z), v.hashLinkCounts[z] ? z = z + "-" + v.hashLinkCounts[z]++ : v.hashLinkCounts[z] = 1, z;
      }
      return m = v.converter._dispatch("headers.after", m, b, v), m;
    }), s.subParser("horizontalRule", function(m, b, v) {
      "use strict";
      m = v.converter._dispatch("horizontalRule.before", m, b, v);
      var T = s.subParser("hashBlock")("<hr />", b, v);
      return m = m.replace(/^ {0,2}( ?-){3,}[ \t]*$/gm, T), m = m.replace(/^ {0,2}( ?\*){3,}[ \t]*$/gm, T), m = m.replace(/^ {0,2}( ?_){3,}[ \t]*$/gm, T), m = v.converter._dispatch("horizontalRule.after", m, b, v), m;
    }), s.subParser("images", function(m, b, v) {
      "use strict";
      m = v.converter._dispatch("images.before", m, b, v);
      var T = /!\[([^\]]*?)][ \t]*()\([ \t]?<?([\S]+?(?:\([\S]*?\)[\S]*?)?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(["'])([^"]*?)\6)?[ \t]?\)/g, I = /!\[([^\]]*?)][ \t]*()\([ \t]?<([^>]*)>(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(?:(["'])([^"]*?)\6))?[ \t]?\)/g, x = /!\[([^\]]*?)][ \t]*()\([ \t]?<?(data:.+?\/.+?;base64,[A-Za-z0-9+/=\n]+?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*(?:(["'])([^"]*?)\6)?[ \t]?\)/g, P = /!\[([^\]]*?)] ?(?:\n *)?\[([\s\S]*?)]()()()()()/g, L = /!\[([^\[\]]+)]()()()()()/g;
      function G(V, Z, O, Y, X, j, re, pe) {
        return Y = Y.replace(/\s/g, ""), z(V, Z, O, Y, X, j, re, pe);
      }
      function z(V, Z, O, Y, X, j, re, pe) {
        var B = v.gUrls, N = v.gTitles, H = v.gDimensions;
        if (O = O.toLowerCase(), pe || (pe = ""), V.search(/\(<?\s*>? ?(['"].*['"])?\)$/m) > -1) Y = "";
        else if (Y === "" || Y === null)
          if ((O === "" || O === null) && (O = Z.toLowerCase().replace(/ ?\n/g, " ")), Y = "#" + O, !s.helper.isUndefined(B[O]))
            Y = B[O], s.helper.isUndefined(N[O]) || (pe = N[O]), s.helper.isUndefined(H[O]) || (X = H[O].width, j = H[O].height);
          else return V;
        Z = Z.replace(/"/g, "&quot;").replace(s.helper.regexes.asteriskDashAndColon, s.helper.escapeCharactersCallback), Y = Y.replace(s.helper.regexes.asteriskDashAndColon, s.helper.escapeCharactersCallback);
        var le = '<img src="' + Y + '" alt="' + Z + '"';
        return pe && s.helper.isString(pe) && (pe = pe.replace(/"/g, "&quot;").replace(s.helper.regexes.asteriskDashAndColon, s.helper.escapeCharactersCallback), le += ' title="' + pe + '"'), X && j && (X = X === "*" ? "auto" : X, j = j === "*" ? "auto" : j, le += ' width="' + X + '"', le += ' height="' + j + '"'), le += " />", le;
      }
      return m = m.replace(P, z), m = m.replace(x, G), m = m.replace(I, z), m = m.replace(T, z), m = m.replace(L, z), m = v.converter._dispatch("images.after", m, b, v), m;
    }), s.subParser("italicsAndBold", function(m, b, v) {
      "use strict";
      m = v.converter._dispatch("italicsAndBold.before", m, b, v);
      function T(I, x, P) {
        return x + I + P;
      }
      return b.literalMidWordUnderscores ? (m = m.replace(/\b___(\S[\s\S]*?)___\b/g, function(I, x) {
        return T(x, "<strong><em>", "</em></strong>");
      }), m = m.replace(/\b__(\S[\s\S]*?)__\b/g, function(I, x) {
        return T(x, "<strong>", "</strong>");
      }), m = m.replace(/\b_(\S[\s\S]*?)_\b/g, function(I, x) {
        return T(x, "<em>", "</em>");
      })) : (m = m.replace(/___(\S[\s\S]*?)___/g, function(I, x) {
        return /\S$/.test(x) ? T(x, "<strong><em>", "</em></strong>") : I;
      }), m = m.replace(/__(\S[\s\S]*?)__/g, function(I, x) {
        return /\S$/.test(x) ? T(x, "<strong>", "</strong>") : I;
      }), m = m.replace(/_([^\s_][\s\S]*?)_/g, function(I, x) {
        return /\S$/.test(x) ? T(x, "<em>", "</em>") : I;
      })), b.literalMidWordAsterisks ? (m = m.replace(/([^*]|^)\B\*\*\*(\S[\s\S]*?)\*\*\*\B(?!\*)/g, function(I, x, P) {
        return T(P, x + "<strong><em>", "</em></strong>");
      }), m = m.replace(/([^*]|^)\B\*\*(\S[\s\S]*?)\*\*\B(?!\*)/g, function(I, x, P) {
        return T(P, x + "<strong>", "</strong>");
      }), m = m.replace(/([^*]|^)\B\*(\S[\s\S]*?)\*\B(?!\*)/g, function(I, x, P) {
        return T(P, x + "<em>", "</em>");
      })) : (m = m.replace(/\*\*\*(\S[\s\S]*?)\*\*\*/g, function(I, x) {
        return /\S$/.test(x) ? T(x, "<strong><em>", "</em></strong>") : I;
      }), m = m.replace(/\*\*(\S[\s\S]*?)\*\*/g, function(I, x) {
        return /\S$/.test(x) ? T(x, "<strong>", "</strong>") : I;
      }), m = m.replace(/\*([^\s*][\s\S]*?)\*/g, function(I, x) {
        return /\S$/.test(x) ? T(x, "<em>", "</em>") : I;
      })), m = v.converter._dispatch("italicsAndBold.after", m, b, v), m;
    }), s.subParser("lists", function(m, b, v) {
      "use strict";
      function T(P, L) {
        v.gListLevel++, P = P.replace(/\n{2,}$/, `
`), P += "¨0";
        var G = /(\n)?(^ {0,3})([*+-]|\d+[.])[ \t]+((\[(x|X| )?])?[ \t]*[^\r]+?(\n{1,2}))(?=\n*(¨0| {0,3}([*+-]|\d+[.])[ \t]+))/gm, z = /\n[ \t]*\n(?!¨0)/.test(P);
        return b.disableForced4SpacesIndentedSublists && (G = /(\n)?(^ {0,3})([*+-]|\d+[.])[ \t]+((\[(x|X| )?])?[ \t]*[^\r]+?(\n{1,2}))(?=\n*(¨0|\2([*+-]|\d+[.])[ \t]+))/gm), P = P.replace(G, function(V, Z, O, Y, X, j, re) {
          re = re && re.trim() !== "";
          var pe = s.subParser("outdent")(X, b, v), B = "";
          return j && b.tasklists && (B = ' class="task-list-item" style="list-style-type: none;"', pe = pe.replace(/^[ \t]*\[(x|X| )?]/m, function() {
            var N = '<input type="checkbox" disabled style="margin: 0px 0.35em 0.25em -1.6em; vertical-align: middle;"';
            return re && (N += " checked"), N += ">", N;
          })), pe = pe.replace(/^([-*+]|\d\.)[ \t]+[\S\n ]*/g, function(N) {
            return "¨A" + N;
          }), Z || pe.search(/\n{2,}/) > -1 ? (pe = s.subParser("githubCodeBlocks")(pe, b, v), pe = s.subParser("blockGamut")(pe, b, v)) : (pe = s.subParser("lists")(pe, b, v), pe = pe.replace(/\n$/, ""), pe = s.subParser("hashHTMLBlocks")(pe, b, v), pe = pe.replace(/\n\n+/g, `

`), z ? pe = s.subParser("paragraphs")(pe, b, v) : pe = s.subParser("spanGamut")(pe, b, v)), pe = pe.replace("¨A", ""), pe = "<li" + B + ">" + pe + `</li>
`, pe;
        }), P = P.replace(/¨0/g, ""), v.gListLevel--, L && (P = P.replace(/\s+$/, "")), P;
      }
      function I(P, L) {
        if (L === "ol") {
          var G = P.match(/^ *(\d+)\./);
          if (G && G[1] !== "1") return ' start="' + G[1] + '"';
        }
        return "";
      }
      function x(P, L, G) {
        var z = b.disableForced4SpacesIndentedSublists ? /^ ?\d+\.[ \t]/gm : /^ {0,3}\d+\.[ \t]/gm, V = b.disableForced4SpacesIndentedSublists ? /^ ?[*+-][ \t]/gm : /^ {0,3}[*+-][ \t]/gm, Z = L === "ul" ? z : V, O = "";
        if (P.search(Z) !== -1) (function X(j) {
          var re = j.search(Z), pe = I(P, L);
          re !== -1 ? (O += `

<` + L + pe + `>
` + T(j.slice(0, re), !!G) + "</" + L + `>
`, L = L === "ul" ? "ol" : "ul", Z = L === "ul" ? z : V, X(j.slice(re))) : O += `

<` + L + pe + `>
` + T(j, !!G) + "</" + L + `>
`;
        })(P);
        else {
          var Y = I(P, L);
          O = `

<` + L + Y + `>
` + T(P, !!G) + "</" + L + `>
`;
        }
        return O;
      }
      return m = v.converter._dispatch("lists.before", m, b, v), m += "¨0", v.gListLevel ? m = m.replace(/^(( {0,3}([*+-]|\d+[.])[ \t]+)[^\r]+?(¨0|\n{2,}(?=\S)(?![ \t]*(?:[*+-]|\d+[.])[ \t]+)))/gm, function(P, L, G) {
        return x(L, G.search(/[*+-]/g) > -1 ? "ul" : "ol", !0);
      }) : m = m.replace(/(\n\n|^\n?)(( {0,3}([*+-]|\d+[.])[ \t]+)[^\r]+?(¨0|\n{2,}(?=\S)(?![ \t]*(?:[*+-]|\d+[.])[ \t]+)))/gm, function(P, L, G, z) {
        return x(G, z.search(/[*+-]/g) > -1 ? "ul" : "ol", !1);
      }), m = m.replace(/¨0/, ""), m = v.converter._dispatch("lists.after", m, b, v), m;
    }), s.subParser("metadata", function(m, b, v) {
      "use strict";
      if (!b.metadata) return m;
      m = v.converter._dispatch("metadata.before", m, b, v);
      function T(I) {
        v.metadata.raw = I, I = I.replace(/&/g, "&amp;").replace(/"/g, "&quot;"), I = I.replace(/\n {4}/g, " "), I.replace(/^([\S ]+): +([\s\S]+?)$/gm, function(x, P, L) {
          return v.metadata.parsed[P] = L, "";
        });
      }
      return m = m.replace(/^\s*«««+(\S*?)\n([\s\S]+?)\n»»»+\n/, function(I, x, P) {
        return T(P), "¨M";
      }), m = m.replace(/^\s*---+(\S*?)\n([\s\S]+?)\n---+\n/, function(I, x, P) {
        return x && (v.metadata.format = x), T(P), "¨M";
      }), m = m.replace(/¨M/g, ""), m = v.converter._dispatch("metadata.after", m, b, v), m;
    }), s.subParser("outdent", function(m, b, v) {
      "use strict";
      return m = v.converter._dispatch("outdent.before", m, b, v), m = m.replace(/^(\t|[ ]{1,4})/gm, "¨0"), m = m.replace(/¨0/g, ""), m = v.converter._dispatch("outdent.after", m, b, v), m;
    }), s.subParser("paragraphs", function(m, b, v) {
      "use strict";
      m = v.converter._dispatch("paragraphs.before", m, b, v), m = m.replace(/^\n+/g, ""), m = m.replace(/\n+$/g, "");
      for (var T = m.split(/\n{2,}/g), I = [], x = T.length, P = 0; P < x; P++) {
        var L = T[P];
        L.search(/¨(K|G)(\d+)\1/g) >= 0 ? I.push(L) : L.search(/\S/) >= 0 && (L = s.subParser("spanGamut")(L, b, v), L = L.replace(/^([ \t]*)/g, "<p>"), L += "</p>", I.push(L));
      }
      for (x = I.length, P = 0; P < x; P++) {
        for (var G = "", z = I[P], V = !1; /¨(K|G)(\d+)\1/.test(z); ) {
          var Z = RegExp.$1, O = RegExp.$2;
          Z === "K" ? G = v.gHtmlBlocks[O] : V ? G = s.subParser("encodeCode")(v.ghCodeBlocks[O].text, b, v) : G = v.ghCodeBlocks[O].codeblock, G = G.replace(/\$/g, "$$$$"), z = z.replace(/(\n\n)?¨(K|G)\d+\2(\n\n)?/, G), /^<pre\b[^>]*>\s*<code\b[^>]*>/.test(z) && (V = !0);
        }
        I[P] = z;
      }
      return m = I.join(`
`), m = m.replace(/^\n+/g, ""), m = m.replace(/\n+$/g, ""), v.converter._dispatch("paragraphs.after", m, b, v);
    }), s.subParser("runExtension", function(m, b, v, T) {
      "use strict";
      if (m.filter) b = m.filter(b, T.converter, v);
      else if (m.regex) {
        var I = m.regex;
        I instanceof RegExp || (I = new RegExp(I, "g")), b = b.replace(I, m.replace);
      }
      return b;
    }), s.subParser("spanGamut", function(m, b, v) {
      "use strict";
      return m = v.converter._dispatch("spanGamut.before", m, b, v), m = s.subParser("codeSpans")(m, b, v), m = s.subParser("escapeSpecialCharsWithinTagAttributes")(m, b, v), m = s.subParser("encodeBackslashEscapes")(m, b, v), m = s.subParser("images")(m, b, v), m = s.subParser("anchors")(m, b, v), m = s.subParser("autoLinks")(m, b, v), m = s.subParser("simplifiedAutoLinks")(m, b, v), m = s.subParser("emoji")(m, b, v), m = s.subParser("underline")(m, b, v), m = s.subParser("italicsAndBold")(m, b, v), m = s.subParser("strikethrough")(m, b, v), m = s.subParser("ellipsis")(m, b, v), m = s.subParser("hashHTMLSpans")(m, b, v), m = s.subParser("encodeAmpsAndAngles")(m, b, v), b.simpleLineBreaks ? /\n\n¨K/.test(m) || (m = m.replace(/\n+/g, `<br />
`)) : m = m.replace(/  +\n/g, `<br />
`), m = v.converter._dispatch("spanGamut.after", m, b, v), m;
    }), s.subParser("strikethrough", function(m, b, v) {
      "use strict";
      function T(I) {
        return b.simplifiedAutoLink && (I = s.subParser("simplifiedAutoLinks")(I, b, v)), "<del>" + I + "</del>";
      }
      return b.strikethrough && (m = v.converter._dispatch("strikethrough.before", m, b, v), m = m.replace(/(?:~){2}([\s\S]+?)(?:~){2}/g, function(I, x) {
        return T(x);
      }), m = v.converter._dispatch("strikethrough.after", m, b, v)), m;
    }), s.subParser("stripLinkDefinitions", function(m, b, v) {
      "use strict";
      var T = /^ {0,3}\[([^\]]+)]:[ \t]*\n?[ \t]*<?([^>\s]+)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*\n?[ \t]*(?:(\n*)["|'(](.+?)["|')][ \t]*)?(?:\n+|(?=¨0))/gm, I = /^ {0,3}\[([^\]]+)]:[ \t]*\n?[ \t]*<?(data:.+?\/.+?;base64,[A-Za-z0-9+/=\n]+?)>?(?: =([*\d]+[A-Za-z%]{0,4})x([*\d]+[A-Za-z%]{0,4}))?[ \t]*\n?[ \t]*(?:(\n*)["|'(](.+?)["|')][ \t]*)?(?:\n\n|(?=¨0)|(?=\n\[))/gm;
      m += "¨0";
      var x = function(P, L, G, z, V, Z, O) {
        return L = L.toLowerCase(), m.toLowerCase().split(L).length - 1 < 2 ? P : (G.match(/^data:.+?\/.+?;base64,/) ? v.gUrls[L] = G.replace(/\s/g, "") : v.gUrls[L] = s.subParser("encodeAmpsAndAngles")(G, b, v), Z ? Z + O : (O && (v.gTitles[L] = O.replace(/"|'/g, "&quot;")), b.parseImgDimensions && z && V && (v.gDimensions[L] = {
          width: z,
          height: V
        }), ""));
      };
      return m = m.replace(I, x), m = m.replace(T, x), m = m.replace(/¨0/, ""), m;
    }), s.subParser("tables", function(m, b, v) {
      "use strict";
      if (!b.tables) return m;
      var T = /^ {0,3}\|?.+\|.+\n {0,3}\|?[ \t]*:?[ \t]*(?:[-=]){2,}[ \t]*:?[ \t]*\|[ \t]*:?[ \t]*(?:[-=]){2,}[\s\S]+?(?:\n\n|¨0)/gm, I = /^ {0,3}\|.+\|[ \t]*\n {0,3}\|[ \t]*:?[ \t]*(?:[-=]){2,}[ \t]*:?[ \t]*\|[ \t]*\n( {0,3}\|.+\|[ \t]*\n)*(?:\n|¨0)/gm;
      function x(V) {
        return /^:[ \t]*--*$/.test(V) ? ' style="text-align:left;"' : /^--*[ \t]*:[ \t]*$/.test(V) ? ' style="text-align:right;"' : /^:[ \t]*--*[ \t]*:$/.test(V) ? ' style="text-align:center;"' : "";
      }
      function P(V, Z) {
        var O = "";
        return V = V.trim(), (b.tablesHeaderId || b.tableHeaderId) && (O = ' id="' + V.replace(/ /g, "_").toLowerCase() + '"'), V = s.subParser("spanGamut")(V, b, v), "<th" + O + Z + ">" + V + `</th>
`;
      }
      function L(V, Z) {
        var O = s.subParser("spanGamut")(V, b, v);
        return "<td" + Z + ">" + O + `</td>
`;
      }
      function G(V, Z) {
        for (var O = `<table>
<thead>
<tr>
`, Y = V.length, X = 0; X < Y; ++X) O += V[X];
        for (O += `</tr>
</thead>
<tbody>
`, X = 0; X < Z.length; ++X) {
          O += `<tr>
`;
          for (var j = 0; j < Y; ++j) O += Z[X][j];
          O += `</tr>
`;
        }
        return O += `</tbody>
</table>
`, O;
      }
      function z(V) {
        var Z, O = V.split(`
`);
        for (Z = 0; Z < O.length; ++Z)
          /^ {0,3}\|/.test(O[Z]) && (O[Z] = O[Z].replace(/^ {0,3}\|/, "")), /\|[ \t]*$/.test(O[Z]) && (O[Z] = O[Z].replace(/\|[ \t]*$/, "")), O[Z] = s.subParser("codeSpans")(O[Z], b, v);
        var Y = O[0].split("|").map(function(le) {
          return le.trim();
        }), X = O[1].split("|").map(function(le) {
          return le.trim();
        }), j = [], re = [], pe = [], B = [];
        for (O.shift(), O.shift(), Z = 0; Z < O.length; ++Z)
          O[Z].trim() !== "" && j.push(O[Z].split("|").map(function(le) {
            return le.trim();
          }));
        if (Y.length < X.length) return V;
        for (Z = 0; Z < X.length; ++Z) pe.push(x(X[Z]));
        for (Z = 0; Z < Y.length; ++Z)
          s.helper.isUndefined(pe[Z]) && (pe[Z] = ""), re.push(P(Y[Z], pe[Z]));
        for (Z = 0; Z < j.length; ++Z) {
          for (var N = [], H = 0; H < re.length; ++H)
            s.helper.isUndefined(j[Z][H]), N.push(L(j[Z][H], pe[H]));
          B.push(N);
        }
        return G(re, B);
      }
      return m = v.converter._dispatch("tables.before", m, b, v), m = m.replace(/\\(\|)/g, s.helper.escapeCharactersCallback), m = m.replace(T, z), m = m.replace(I, z), m = v.converter._dispatch("tables.after", m, b, v), m;
    }), s.subParser("underline", function(m, b, v) {
      "use strict";
      return b.underline && (m = v.converter._dispatch("underline.before", m, b, v), b.literalMidWordUnderscores ? (m = m.replace(/\b___(\S[\s\S]*?)___\b/g, function(T, I) {
        return "<u>" + I + "</u>";
      }), m = m.replace(/\b__(\S[\s\S]*?)__\b/g, function(T, I) {
        return "<u>" + I + "</u>";
      })) : (m = m.replace(/___(\S[\s\S]*?)___/g, function(T, I) {
        return /\S$/.test(I) ? "<u>" + I + "</u>" : T;
      }), m = m.replace(/__(\S[\s\S]*?)__/g, function(T, I) {
        return /\S$/.test(I) ? "<u>" + I + "</u>" : T;
      })), m = m.replace(/(_)/g, s.helper.escapeCharactersCallback), m = v.converter._dispatch("underline.after", m, b, v)), m;
    }), s.subParser("unescapeSpecialChars", function(m, b, v) {
      "use strict";
      return m = v.converter._dispatch("unescapeSpecialChars.before", m, b, v), m = m.replace(/¨E(\d+)E/g, function(T, I) {
        var x = parseInt(I);
        return String.fromCharCode(x);
      }), m = v.converter._dispatch("unescapeSpecialChars.after", m, b, v), m;
    }), s.subParser("makeMarkdown.blockquote", function(m, b) {
      "use strict";
      var v = "";
      if (m.hasChildNodes())
        for (var T = m.childNodes, I = T.length, x = 0; x < I; ++x) {
          var P = s.subParser("makeMarkdown.node")(T[x], b);
          P !== "" && (v += P);
        }
      return v = v.trim(), v = "> " + v.split(`
`).join(`
> `), v;
    }), s.subParser("makeMarkdown.codeBlock", function(m, b) {
      "use strict";
      var v = m.getAttribute("language"), T = m.getAttribute("precodenum");
      return "```" + v + `
` + b.preList[T] + "\n```";
    }), s.subParser("makeMarkdown.codeSpan", function(m) {
      "use strict";
      return "`" + m.innerHTML + "`";
    }), s.subParser("makeMarkdown.emphasis", function(m, b) {
      "use strict";
      var v = "";
      if (m.hasChildNodes()) {
        v += "*";
        for (var T = m.childNodes, I = T.length, x = 0; x < I; ++x) v += s.subParser("makeMarkdown.node")(T[x], b);
        v += "*";
      }
      return v;
    }), s.subParser("makeMarkdown.header", function(m, b, v) {
      "use strict";
      var T = new Array(v + 1).join("#"), I = "";
      if (m.hasChildNodes()) {
        I = T + " ";
        for (var x = m.childNodes, P = x.length, L = 0; L < P; ++L) I += s.subParser("makeMarkdown.node")(x[L], b);
      }
      return I;
    }), s.subParser("makeMarkdown.hr", function() {
      "use strict";
      return "---";
    }), s.subParser("makeMarkdown.image", function(m) {
      "use strict";
      var b = "";
      return m.hasAttribute("src") && (b += "![" + m.getAttribute("alt") + "](", b += "<" + m.getAttribute("src") + ">", m.hasAttribute("width") && m.hasAttribute("height") && (b += " =" + m.getAttribute("width") + "x" + m.getAttribute("height")), m.hasAttribute("title") && (b += ' "' + m.getAttribute("title") + '"'), b += ")"), b;
    }), s.subParser("makeMarkdown.links", function(m, b) {
      "use strict";
      var v = "";
      if (m.hasChildNodes() && m.hasAttribute("href")) {
        var T = m.childNodes, I = T.length;
        v = "[";
        for (var x = 0; x < I; ++x) v += s.subParser("makeMarkdown.node")(T[x], b);
        v += "](", v += "<" + m.getAttribute("href") + ">", m.hasAttribute("title") && (v += ' "' + m.getAttribute("title") + '"'), v += ")";
      }
      return v;
    }), s.subParser("makeMarkdown.list", function(m, b, v) {
      "use strict";
      var T = "";
      if (!m.hasChildNodes()) return "";
      for (var I = m.childNodes, x = I.length, P = m.getAttribute("start") || 1, L = 0; L < x; ++L)
        if (!(typeof I[L].tagName > "u" || I[L].tagName.toLowerCase() !== "li")) {
          var G = "";
          v === "ol" ? G = P.toString() + ". " : G = "- ", T += G + s.subParser("makeMarkdown.listItem")(I[L], b), ++P;
        }
      return T += `
<!-- -->
`, T.trim();
    }), s.subParser("makeMarkdown.listItem", function(m, b) {
      "use strict";
      for (var v = "", T = m.childNodes, I = T.length, x = 0; x < I; ++x) v += s.subParser("makeMarkdown.node")(T[x], b);
      return /\n$/.test(v) ? v = v.split(`
`).join(`
    `).replace(/^ {4}$/gm, "").replace(/\n\n+/g, `

`) : v += `
`, v;
    }), s.subParser("makeMarkdown.node", function(m, b, v) {
      "use strict";
      v = v || !1;
      var T = "";
      if (m.nodeType === 3) return s.subParser("makeMarkdown.txt")(m, b);
      if (m.nodeType === 8) return "<!--" + m.data + `-->

`;
      if (m.nodeType !== 1) return "";
      switch (m.tagName.toLowerCase()) {
        case "h1":
          v || (T = s.subParser("makeMarkdown.header")(m, b, 1) + `

`);
          break;
        case "h2":
          v || (T = s.subParser("makeMarkdown.header")(m, b, 2) + `

`);
          break;
        case "h3":
          v || (T = s.subParser("makeMarkdown.header")(m, b, 3) + `

`);
          break;
        case "h4":
          v || (T = s.subParser("makeMarkdown.header")(m, b, 4) + `

`);
          break;
        case "h5":
          v || (T = s.subParser("makeMarkdown.header")(m, b, 5) + `

`);
          break;
        case "h6":
          v || (T = s.subParser("makeMarkdown.header")(m, b, 6) + `

`);
          break;
        case "p":
          v || (T = s.subParser("makeMarkdown.paragraph")(m, b) + `

`);
          break;
        case "blockquote":
          v || (T = s.subParser("makeMarkdown.blockquote")(m, b) + `

`);
          break;
        case "hr":
          v || (T = s.subParser("makeMarkdown.hr")(m, b) + `

`);
          break;
        case "ol":
          v || (T = s.subParser("makeMarkdown.list")(m, b, "ol") + `

`);
          break;
        case "ul":
          v || (T = s.subParser("makeMarkdown.list")(m, b, "ul") + `

`);
          break;
        case "precode":
          v || (T = s.subParser("makeMarkdown.codeBlock")(m, b) + `

`);
          break;
        case "pre":
          v || (T = s.subParser("makeMarkdown.pre")(m, b) + `

`);
          break;
        case "table":
          v || (T = s.subParser("makeMarkdown.table")(m, b) + `

`);
          break;
        case "code":
          T = s.subParser("makeMarkdown.codeSpan")(m, b);
          break;
        case "em":
        case "i":
          T = s.subParser("makeMarkdown.emphasis")(m, b);
          break;
        case "strong":
        case "b":
          T = s.subParser("makeMarkdown.strong")(m, b);
          break;
        case "del":
          T = s.subParser("makeMarkdown.strikethrough")(m, b);
          break;
        case "a":
          T = s.subParser("makeMarkdown.links")(m, b);
          break;
        case "img":
          T = s.subParser("makeMarkdown.image")(m, b);
          break;
        default:
          T = m.outerHTML + `

`;
      }
      return T;
    }), s.subParser("makeMarkdown.paragraph", function(m, b) {
      "use strict";
      var v = "";
      if (m.hasChildNodes())
        for (var T = m.childNodes, I = T.length, x = 0; x < I; ++x) v += s.subParser("makeMarkdown.node")(T[x], b);
      return v = v.trim(), v;
    }), s.subParser("makeMarkdown.pre", function(m, b) {
      "use strict";
      var v = m.getAttribute("prenum");
      return "<pre>" + b.preList[v] + "</pre>";
    }), s.subParser("makeMarkdown.strikethrough", function(m, b) {
      "use strict";
      var v = "";
      if (m.hasChildNodes()) {
        v += "~~";
        for (var T = m.childNodes, I = T.length, x = 0; x < I; ++x) v += s.subParser("makeMarkdown.node")(T[x], b);
        v += "~~";
      }
      return v;
    }), s.subParser("makeMarkdown.strong", function(m, b) {
      "use strict";
      var v = "";
      if (m.hasChildNodes()) {
        v += "**";
        for (var T = m.childNodes, I = T.length, x = 0; x < I; ++x) v += s.subParser("makeMarkdown.node")(T[x], b);
        v += "**";
      }
      return v;
    }), s.subParser("makeMarkdown.table", function(m, b) {
      "use strict";
      var v = "", T = [[], []], I = m.querySelectorAll("thead>tr>th"), x = m.querySelectorAll("tbody>tr"), P, L;
      for (P = 0; P < I.length; ++P) {
        var G = s.subParser("makeMarkdown.tableCell")(I[P], b), z = "---";
        if (I[P].hasAttribute("style")) switch (I[P].getAttribute("style").toLowerCase().replace(/\s/g, "")) {
          case "text-align:left;":
            z = ":---";
            break;
          case "text-align:right;":
            z = "---:";
            break;
          case "text-align:center;":
            z = ":---:";
            break;
        }
        T[0][P] = G.trim(), T[1][P] = z;
      }
      for (P = 0; P < x.length; ++P) {
        var V = T.push([]) - 1, Z = x[P].getElementsByTagName("td");
        for (L = 0; L < I.length; ++L) {
          var O = " ";
          typeof Z[L] < "u" && (O = s.subParser("makeMarkdown.tableCell")(Z[L], b)), T[V].push(O);
        }
      }
      var Y = 3;
      for (P = 0; P < T.length; ++P) for (L = 0; L < T[P].length; ++L) {
        var X = T[P][L].length;
        X > Y && (Y = X);
      }
      for (P = 0; P < T.length; ++P) {
        for (L = 0; L < T[P].length; ++L) P === 1 ? T[P][L].slice(-1) === ":" ? T[P][L] = s.helper.padEnd(T[P][L].slice(-1), Y - 1, "-") + ":" : T[P][L] = s.helper.padEnd(T[P][L], Y, "-") : T[P][L] = s.helper.padEnd(T[P][L], Y);
        v += "| " + T[P].join(" | ") + ` |
`;
      }
      return v.trim();
    }), s.subParser("makeMarkdown.tableCell", function(m, b) {
      "use strict";
      var v = "";
      if (!m.hasChildNodes()) return "";
      for (var T = m.childNodes, I = T.length, x = 0; x < I; ++x) v += s.subParser("makeMarkdown.node")(T[x], b, !0);
      return v.trim();
    }), s.subParser("makeMarkdown.txt", function(m) {
      "use strict";
      var b = m.nodeValue;
      return b = b.replace(/ +/g, " "), b = b.replace(/¨NBSP;/g, " "), b = s.helper.unescapeHTMLEntities(b), b = b.replace(/([*_~|`])/g, "\\$1"), b = b.replace(/^(\s*)>/g, "\\$1>"), b = b.replace(/^#/gm, "\\#"), b = b.replace(/^(\s*)([-=]{3,})(\s*)$/, "$1\\$2$3"), b = b.replace(/^( {0,3}\d+)\./gm, "$1\\."), b = b.replace(/^( {0,3})([+-])/gm, "$1\\$2"), b = b.replace(/]([\s]*)\(/g, "\\]$1\\("), b = b.replace(/^ {0,3}\[([\S \t]*?)]:/gm, "\\[$1]:"), b;
    });
    var D = this;
    typeof define == "function" && define.amd ? define(function() {
      "use strict";
      return s;
    }) : typeof t < "u" && t.exports ? t.exports = s : D.showdown = s;
  }).call(e);
})), LF = /* @__PURE__ */ cI(DF(), 1), kg = null, R0 = 0, Vv = /* @__PURE__ */ new Map(), OF = /* @__PURE__ */ new Set([
  "html",
  "htm",
  "xhtml",
  "xml",
  "svg",
  "vue",
  "svelte"
]), FF = /* @__PURE__ */ new Set(["details", "summary"]), BF = "allow-scripts";
function UF(e) {
  return String(e || "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
}
function qF() {
  return R0 += 1, `html-${Date.now().toString(36)}-${R0.toString(36)}`;
}
function HF(e = "") {
  const t = String(e || "").length;
  if (t >= 1e3) {
    const n = t / 1e3;
    return `${n >= 10 ? Math.round(n) : n.toFixed(1)}k 字符`;
  }
  return `${t} 字符`;
}
function GF(e = "") {
  return OF.has(String(e || "").trim().toLowerCase());
}
function WF(e = "") {
  const t = String(e || "").trim();
  return t ? /^<!doctype\s+html/i.test(t) || /^<html[\s>]/i.test(t) ? !0 : (t.match(/<\/?[a-z][\w:-]*(?:\s[^<>]*)?>/gi) || []).length >= 3 && /<\/[a-z][\w:-]*>/i.test(t) : !1;
}
function VF(e = "") {
  const t = String(e || "").trim();
  return /^<!doctype\s+html/i.test(t) || /^<html[\s>]/i.test(t);
}
function bx(e = "", t = "html") {
  const n = qF();
  return Vv.set(n, {
    code: String(e || ""),
    language: String(t || "html").trim() || "html"
  }), `@@XBHTMLBLOCK:${n}@@`;
}
function KF(e = "", t = {}) {
  const n = t.allowSafeHtmlTags === !0;
  return String(e || "").replace(/<\/?([a-z][\w:-]*)(?:\s[^<>]*)?>/gi, (r, s) => {
    const o = String(s || "").toLowerCase();
    return n && FF.has(o) ? /^<\s*\//.test(r) ? `</${o}>` : o === "details" && /\sopen(?:\s|=|>|\/)/i.test(r) ? "<details open>" : `<${o}>` : r.replace(/</g, "&lt;").replace(/>/g, "&gt;");
  });
}
function N0(e = "", t = {}) {
  const n = String(e || ""), r = n.trim();
  if (VF(r)) {
    const s = n.match(/^\s*/)?.[0] || "", o = n.match(/\s*$/)?.[0] || "";
    return `${s}${bx(r, "html")}${o}`;
  }
  return KF(n, t);
}
function zF(e = "", t = {}) {
  const n = String(e || ""), r = /(^|\n)(`{3,}|~{3,})[ \t]*([^\n]*)\n([\s\S]*?)\n\2[ \t]*(?=\n|$)/g;
  let s = "", o = 0, i = null;
  for (; (i = r.exec(n)) !== null; ) {
    const a = i[1] || "", l = i.index + a.length, u = r.lastIndex, d = String(i[3] || "").trim().split(/\s+/)[0] || "", h = String(i[4] || ""), p = GF(d) || !d && WF(h);
    s += N0(n.slice(o, l), t), p ? s += bx(h, d || "html") : s += n.slice(l, u), o = u;
  }
  return s += N0(n.slice(o), t), s;
}
function $0(e = "") {
  return String(e || "").replace(/@@XBHTMLBLOCK:([a-z0-9-]+)@@|@@XB_HTML_BLOCK_([a-z0-9-]+)@@/g, (t, n, r) => `<span class="xb-markdown-html-placeholder" data-xb-html-block-id="${n || r}"></span>`);
}
function JF(e = "") {
  return String(e || "").replace(/<p>\s*(<details\b[^>]*>\s*(?:<summary\b[^>]*>[\s\S]*?<\/summary>\s*)?)<\/p>/gi, "$1").replace(/<p>\s*(<summary\b[^>]*>[\s\S]*?<\/summary>)\s*<\/p>/gi, "$1").replace(/<p>\s*(<\/details>)\s*<\/p>/gi, "$1");
}
function YF(e = "") {
  return String(e || "").replace(/&#x([0-9a-f]+);?/gi, (t, n) => String.fromCodePoint(Number.parseInt(n, 16) || 0)).replace(/&#([0-9]+);?/g, (t, n) => String.fromCodePoint(Number.parseInt(n, 10) || 0)).replace(/&colon;?/gi, ":").replace(/&tab;?/gi, "	").replace(/&newline;?/gi, `
`).replace(/&amp;?/gi, "&");
}
function XF(e = "") {
  const t = YF(e).trim().replace(/[\u0000-\u001F\u007F\s]+/g, "").toLowerCase();
  return /^(?:javascript|vbscript|data):/.test(t);
}
function QF(e = "") {
  return String(e || "").replace(/<\/?(?:script|style|iframe|object|embed|link|meta|base|form|input|button|textarea|select|option)[^>]*>/gi, "").replace(/\s+on[a-z0-9_-]+\s*=\s*(?:"[^"]*"|'[^']*'|[^\s"'=<>`]+)/gi, "").replace(/\s+(href|src|xlink:href)\s*=\s*("[^"]*"|'[^']*'|[^\s"'=<>`]+)/gi, (t, n, r) => XF(String(r || "").replace(/^["']|["']$/g, "")) ? "" : ` ${n}=${r}`);
}
function ZF(e) {
  const t = String(e || "").trim();
  if (!t) return "";
  const n = zF(t, { allowSafeHtmlTags: !0 });
  try {
    return kg || (kg = new LF.default.Converter({
      simpleLineBreaks: !0,
      strikethrough: !0,
      tables: !0,
      tasklists: !0,
      ghCodeBlocks: !0,
      simplifiedAutoLink: !0,
      openLinksInNewWindow: !0,
      emoji: !1
    })), $0(QF(JF(kg.makeHtml(n))));
  } catch {
  }
  return $0(UF(n).replace(/\n/g, "<br>"));
}
async function jF(e = "") {
  const t = String(e || "");
  if (!t) return !1;
  try {
    if (navigator.clipboard?.writeText)
      return await navigator.clipboard.writeText(t), !0;
  } catch {
  }
  try {
    const n = document.createElement("textarea");
    n.value = t, n.setAttribute("readonly", "readonly"), n.style.position = "fixed", n.style.opacity = "0", n.style.pointerEvents = "none", document.body.appendChild(n), n.select(), n.setSelectionRange(0, n.value.length);
    const r = document.execCommand("copy");
    return n.remove(), r;
  } catch {
    return !1;
  }
}
function e2(e, t = {}) {
  if (!e || typeof t.onPathClick != "function") return;
  const n = e.ownerDocument || globalThis.document, r = n?.defaultView?.NodeFilter || globalThis.NodeFilter;
  if (!n?.createTreeWalker || !r) return;
  const s = t.pathRegex || /local\/[^\s`"'<>，。；：！？（）()[\]{}]+/g, o = String(t.linkClassName || "xb-markdown-local-path-link"), i = n.createTreeWalker(e, r.SHOW_TEXT), a = [];
  for (; i.nextNode(); ) {
    const l = i.currentNode;
    !l?.nodeValue || !String(l.nodeValue).includes("local/") || l.parentElement?.closest?.("button, a, textarea, input") || a.push(l);
  }
  a.forEach((l) => {
    const u = String(l.nodeValue || ""), d = n.createDocumentFragment();
    let h = null, p = 0, y = !1;
    for (s.lastIndex = 0; (h = s.exec(u)) !== null; ) {
      y = !0, h.index > p && d.appendChild(n.createTextNode(u.slice(p, h.index)));
      const w = h[0], E = n.createElement("button");
      E.type = "button", E.className = o, E.textContent = w, E.addEventListener("click", () => {
        t.onPathClick(w);
      }), d.appendChild(E), p = h.index + w.length;
    }
    y && (p < u.length && d.appendChild(n.createTextNode(u.slice(p))), l.parentNode?.replaceChild(d, l));
  });
}
function t2(e, t = {}) {
  if (!e?.querySelectorAll) return;
  const n = e.ownerDocument || globalThis.document;
  if (!n?.createElement) return;
  const r = String(t.codeBlockClassName || "xb-markdown-codeblock"), s = String(t.codeCopyClassName || "xb-markdown-code-copy"), o = String(t.copyButtonTitle || "复制代码"), i = String(t.copySuccessTitle || "已复制"), a = String(t.copyFailureTitle || "复制失败");
  Array.from(e.querySelectorAll("pre")).forEach((l) => {
    if (l.closest(`.${r}`)) return;
    const u = n.createElement("div");
    u.className = r;
    const d = n.createElement("button");
    d.type = "button", d.className = s, d.textContent = "⧉", d.title = o, d.setAttribute("aria-label", o), d.addEventListener("click", async () => {
      const h = await jF(l.querySelector("code")?.textContent || l.textContent || "");
      d.textContent = h ? "✓" : "!", d.title = h ? i : a, setTimeout(() => {
        d.textContent = "⧉", d.title = o;
      }, 1200);
    }), l.parentNode?.insertBefore(u, l), u.append(d, l);
  });
}
function D0(e = [], t = null) {
  e.forEach((n) => {
    n.classList.toggle("is-active", n === t), n.setAttribute("aria-pressed", n === t ? "true" : "false");
  });
}
function n2(e, t = "") {
  const n = e.createElement("pre");
  return n.className = "xb-markdown-html-code", n.textContent = String(t || ""), n;
}
function L0(e, t = "") {
  const n = e.createElement("iframe");
  return n.className = "xb-markdown-html-preview", n.setAttribute("sandbox", BF), n.referrerPolicy = "no-referrer", n.title = "HTML 渲染预览", n.srcdoc = String(t || ""), n;
}
function r2(e, t = {}, n = {}) {
  const r = String(t.code || "");
  if (n.htmlBlockMode === "preview") {
    const E = e.createElement("div");
    E.className = "xb-markdown-html-block xb-markdown-html-block-auto";
    const C = e.createElement("div");
    return C.className = "xb-markdown-html-body", C.append(L0(e, r)), E.append(C), E;
  }
  const s = e.createElement("div");
  s.className = "xb-markdown-html-block";
  const o = e.createElement("div");
  o.className = "xb-markdown-html-head";
  const i = e.createElement("div");
  i.className = "xb-markdown-html-title", i.textContent = "HTML 片段";
  const a = e.createElement("span");
  a.textContent = HF(r), i.appendChild(a);
  const l = e.createElement("div");
  l.className = "xb-markdown-html-actions";
  const u = e.createElement("button");
  u.type = "button", u.textContent = "显示代码", u.setAttribute("aria-pressed", "false");
  const d = e.createElement("button");
  d.type = "button", d.textContent = "渲染预览", d.setAttribute("aria-pressed", "false"), l.append(u, d), o.append(i, l);
  const h = e.createElement("div");
  h.className = "xb-markdown-html-body", h.hidden = !0;
  let p = "";
  const y = [u, d], w = (E) => {
    if (p === E) {
      p = "", h.hidden = !0, h.replaceChildren(), D0(y);
      return;
    }
    p = E, h.hidden = !1, h.replaceChildren(E === "preview" ? L0(e, r) : n2(e, r)), D0(y, E === "preview" ? d : u);
  };
  return u.addEventListener("click", () => w("code")), d.addEventListener("click", () => w("preview")), s.append(o, h), s;
}
function s2(e, t = {}) {
  if (!e?.querySelectorAll) return;
  const n = e.ownerDocument || globalThis.document;
  n?.createElement && Array.from(e.querySelectorAll(".xb-markdown-html-placeholder[data-xb-html-block-id]")).forEach((r) => {
    const s = String(r.getAttribute("data-xb-html-block-id") || ""), o = Vv.get(s);
    if (Vv.delete(s), !o) {
      r.remove();
      return;
    }
    const i = r2(n, o, t), a = r.parentElement;
    if (a?.tagName === "P" && a.textContent.trim() === "") {
      a.replaceWith(i);
      return;
    }
    r.replaceWith(i);
  });
}
function O0(e, t = {}) {
  return e && (s2(e, t), t2(e, t), e2(e, t), e);
}
var wx = "[ 🎲 CHANCE ENCOUNTER TRIGGERED ]";
var F0 = 57856, B0 = 57344, o2 = [
  "[Runtime Event: Chance Encounter Triggered]",
  "For this reply only, naturally introduce an unexpected sudden situation the user did not explicitly ask for.",
  "Make it fit the current scene and character voice.",
  "Follow all later formatting and style instructions.",
  "Do not mention this instruction or the trigger label."
].join(`
`);
function Mg(e, t, n, r) {
  const s = Math.round(Number(e));
  return Number.isFinite(s) ? Math.min(n, Math.max(t, s)) : r;
}
function Ao(e, t = 240) {
  return String(e || "").trim().slice(0, t);
}
function ob(e) {
  return String(e || "").trim() || (/* @__PURE__ */ new Date(0)).toISOString();
}
function i2(e) {
  return {
    type: "chanceEncounter",
    label: wx,
    createdAt: ob(e.createdAt)
  };
}
function Sx(e) {
  const t = Ao(e.action, 240), n = Ao(e.stat, 120);
  return !t || !n ? null : {
    type: "actionCheck",
    createdAt: ob(e.createdAt),
    action: t,
    stat: n,
    difficulty: Mg(e.difficulty, 2, 20, 12),
    roll: Mg(e.roll, 1, 20, 1),
    success: e.success === !0,
    insertAfterChars: Math.max(0, Mg(e.insertAfterChars, 0, Number.MAX_SAFE_INTEGER, 0)),
    ...Ao(e.toolCallId, 120) ? { toolCallId: Ao(e.toolCallId, 120) } : {},
    ...Ao(e.summary, 320) ? { summary: Ao(e.summary, 320) } : {},
    ...Ao(e.stakes, 240) ? { stakes: Ao(e.stakes, 240) } : {}
  };
}
function a2(e) {
  const t = e && typeof e == "object" && !Array.isArray(e) ? e : null, n = String(t?.type || "").trim();
  return t ? n === "chanceEncounter" ? i2(t) : n === "actionCheck" ? Sx(t) : null : null;
}
function l2(e) {
  return e.type === "chanceEncounter" ? e.type : [
    e.type,
    e.toolCallId || "",
    e.createdAt,
    String(e.insertAfterChars),
    e.stat,
    e.action,
    String(e.roll),
    String(e.difficulty)
  ].join("\0");
}
function ib(e) {
  const t = Array.isArray(e) ? e : [], n = [], r = /* @__PURE__ */ new Set();
  return t.forEach((s) => {
    const o = a2(s);
    if (!o) return;
    const i = l2(o);
    r.has(i) || (r.add(i), n.push(o));
  }), n;
}
function u2(e = (/* @__PURE__ */ new Date()).toISOString()) {
  return {
    type: "chanceEncounter",
    label: wx,
    createdAt: ob(e)
  };
}
function c2(e) {
  return Sx({
    type: "actionCheck",
    createdAt: e.createdAt || (/* @__PURE__ */ new Date()).toISOString(),
    action: e.action,
    stat: e.stat,
    difficulty: e.difficulty,
    roll: e.roll,
    success: e.success,
    insertAfterChars: e.insertAfterChars,
    toolCallId: e.toolCallId,
    summary: e.summary,
    stakes: e.stakes
  });
}
function ab(e) {
  return ib(e).find((t) => t.type === "chanceEncounter") || null;
}
function d2(e) {
  return !!ab(e);
}
function Wo(e) {
  return ib(e).filter((t) => t.type === "actionCheck");
}
function f2(e) {
  return (Array.isArray(e) ? e : []).map((t) => {
    const n = t && typeof t == "object" && !Array.isArray(t) ? t : {}, r = String(n.marker || "").slice(0, 1), s = Wo(n.events);
    return r && s.length ? {
      marker: r,
      events: s
    } : null;
  }).filter((t) => !!t);
}
function h2(e, t = []) {
  const n = Wo(t).slice().sort((a, l) => a.insertAfterChars - l.insertAfterChars);
  if (!n.length) return {
    text: e,
    groups: []
  };
  const r = [];
  if (n.forEach((a) => {
    const l = Math.max(0, Math.min(e.length, Number(a.insertAfterChars) || 0)), u = r[r.length - 1];
    if (u && u.offset === l) {
      u.events.push(a);
      return;
    }
    r.push({
      offset: l,
      events: [a]
    });
  }), r.length > 63743 - F0) return {
    text: e,
    groups: []
  };
  let s = 0, o = "";
  const i = r.map((a, l) => {
    const u = String.fromCharCode(F0 + l);
    return o += e.slice(s, a.offset) + u, s = a.offset, {
      marker: u,
      events: a.events
    };
  });
  return o += e.slice(s), {
    text: o,
    groups: i
  };
}
function Kv(e, t = []) {
  const n = String(e || ""), r = [...new Set(Wo(t).map((a) => Math.max(0, Math.min(n.length, Number(a.insertAfterChars) || 0))))].sort((a, l) => a - l);
  if (!r.length || r.length > 63743 - B0) return {
    text: n,
    boundaries: []
  };
  let s = 0, o = "";
  const i = r.map((a, l) => {
    const u = String.fromCharCode(B0 + l);
    return o += n.slice(s, a) + u, s = a, {
      originalOffset: a,
      marker: u
    };
  });
  return o += n.slice(s), {
    text: o,
    boundaries: i
  };
}
function Tx(e, t = [], n = []) {
  const r = String(e || ""), s = Wo(t);
  if (!n.length || !s.length) return {
    text: r,
    events: s
  };
  const o = new Map(n.map((l) => [l.marker, l.originalOffset])), i = /* @__PURE__ */ new Map();
  let a = "";
  for (let l = 0; l < r.length; l += 1) {
    const u = r[l], d = o.get(u);
    if (d !== void 0) {
      i.has(d) || i.set(d, a.length);
      continue;
    }
    a += u;
  }
  return {
    text: a,
    events: s.map((l) => {
      const u = Math.max(0, Number(l.insertAfterChars) || 0);
      return {
        ...l,
        insertAfterChars: i.has(u) ? i.get(u) : Math.max(0, Math.min(a.length, u))
      };
    })
  };
}
function p2() {
  return {
    role: "system",
    content: o2
  };
}
function m2(e = [], t) {
  if (!t) return [...e];
  const n = [...e], r = p2();
  for (let s = n.length - 1; s >= 0; s -= 1) if (n[s]?.role === "user")
    return n.splice(s + 1, 0, r), n;
  return n.push(r), n;
}
var Ci = /\[tavern-image:([a-z0-9\-_]+)\]/gi, g2 = /* @__PURE__ */ new Set(["details", "summary"]);
function U0(e = "", t = "") {
  return String(e || "").trim() || t;
}
function v2(e = "", t = {}) {
  const n = U0(t.userName, "User"), r = U0(t.characterName, "角色");
  return String(e || "").replace(/<\s*user\s*>/gi, n).replace(/<\s*(?:char|bot)\s*>/gi, r).replace(/\{\{\s*user\s*\}\}/gi, n).replace(/\{\{\s*(?:char|bot)\s*\}\}/gi, r);
}
function q0(e = "") {
  return String(e || "").replace(/<\/?([a-z][\w:-]*)(?:\s[^<>]*)?>/gi, (t, n) => {
    const r = String(n || "").toLowerCase();
    return g2.has(r) ? t : "";
  });
}
function y2(e = "", t = {}) {
  const n = v2(e, t), r = /(^|\n)(`{3,}|~{3,})[ \t]*([^\n]*)\n([\s\S]*?)\n\2[ \t]*(?=\n|$)/g;
  let s = "", o = 0, i = null;
  for (; (i = r.exec(n)) !== null; ) {
    const a = i[1] || "", l = i.index + a.length, u = r.lastIndex;
    s += q0(n.slice(o, l)), s += n.slice(l, u), o = u;
  }
  return s += q0(n.slice(o)), s;
}
function _2(e) {
  const t = /* @__PURE__ */ new Map();
  function n(T = "") {
    const I = String(T || "");
    let x = 0;
    for (let P = 0; P < I.length; P += 1) x = x * 31 + I.charCodeAt(P) >>> 0;
    return `${I.length}:${x.toString(36)}`;
  }
  function r(T = "", I = {}) {
    const x = I.roleplay ? y2(T, I) : String(T || ""), P = !/(^|\n)(`{3,}|~{3,})[ \t]*(html|htm|xhtml|xml|svg|vue|svelte)?\b/i.test(x) && !/^<!doctype\s+html/i.test(x.trim()) && !/^<html[\s>]/i.test(x.trim()), L = n(x);
    if (P && t.has(L)) return t.get(L) || "";
    const G = ZF(x);
    if (P && !G.includes("xb-markdown-html-placeholder") && (t.set(L, G), t.size > 160)) {
      const z = t.keys().next().value;
      z && t.delete(z);
    }
    return G;
  }
  function s(T = "") {
    return String(T || "").replace(Ci, "").trim();
  }
  function o() {
    t.clear();
  }
  const i = {
    '"': '"',
    "“": "”",
    "「": "」",
    "『": "』"
  }, a = new Set(Object.keys(i)), l = /* @__PURE__ */ new Set([
    "A",
    "BUTTON",
    "CODE",
    "KBD",
    "PRE",
    "SCRIPT",
    "STYLE",
    "TEXTAREA"
  ]), u = 600;
  function d(T) {
    let I = T.parentElement;
    for (; I; ) {
      if (l.has(I.tagName) || I.classList.contains("xb-rp-dialogue")) return !0;
      I = I.parentElement;
    }
    return !1;
  }
  function h(T) {
    const I = [];
    let x = 0;
    for (; x < T.length; ) {
      const P = T[x];
      if (!a.has(P)) {
        x += 1;
        continue;
      }
      const L = i[P];
      let G = T.indexOf(L, x + 1);
      if (G < 0 || G === x + 1 || G - x > u) {
        x += 1;
        continue;
      }
      const z = T.slice(x + 1, G);
      if (z.includes(`
`) || !z.trim()) {
        x += 1;
        continue;
      }
      I.push({
        start: x,
        end: G + 1
      }), x = G + 1;
    }
    return I;
  }
  function p(T) {
    const I = document.createTreeWalker(T, NodeFilter.SHOW_TEXT, { acceptNode(P) {
      const L = P;
      return !L.data || d(L) ? NodeFilter.FILTER_REJECT : h(L.data).length ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP;
    } }), x = [];
    for (; I.nextNode(); ) x.push(I.currentNode);
    x.forEach((P) => {
      const L = h(P.data);
      if (!L.length) return;
      const G = document.createDocumentFragment();
      let z = 0;
      L.forEach((V) => {
        V.start > z && G.append(document.createTextNode(P.data.slice(z, V.start)));
        const Z = document.createElement("span");
        Z.className = "xb-rp-dialogue", Z.textContent = P.data.slice(V.start, V.end), G.append(Z), z = V.end;
      }), z < P.data.length && G.append(document.createTextNode(P.data.slice(z))), P.replaceWith(G);
    });
  }
  function y(T, I = "") {
    return !!(T && T.isConnected !== !1 && String(T.dataset.tavernImageSlot || "").trim() === I);
  }
  function w(T) {
    const I = String(T.dataset.tavernImageSlot || "").trim();
    !I || T.dataset.tavernImageHydrating === "true" || T.dataset.tavernImageLoaded === "true" || (T.dataset.tavernImageHydrating = "true", e.requestHost("xb-tavern:draw-image", { payload: { slotId: I } }, { timeoutMs: e.imageRequestTimeoutMs }).then((x) => {
      if (!y(T, I)) return;
      const P = x.result || x;
      if (T.dataset.tavernImageHydrating = "false", !P.hasData || !P.url) {
        T.classList.add("is-failed");
        const G = document.createElement("span");
        G.className = "xb-tavern-image-placeholder", G.textContent = P.isFailed ? String(P.errorMessage || "配图生成失败") : "配图未找到", T.replaceChildren(G);
        return;
      }
      T.classList.add("is-loaded"), T.dataset.tavernImageLoaded = "true";
      const L = document.createElement("img");
      L.src = String(P.url || ""), L.alt = P.tags ? `配图：${String(P.tags)}` : "配图", L.loading = "lazy", T.replaceChildren(L);
    }).catch(() => {
      if (!y(T, I)) return;
      T.dataset.tavernImageHydrating = "false", T.classList.add("is-failed");
      const x = document.createElement("span");
      x.className = "xb-tavern-image-placeholder", x.textContent = "配图加载失败", T.replaceChildren(x);
    }));
  }
  function E(T = "") {
    const I = document.createElement("span");
    I.className = "xb-tavern-image", I.dataset.tavernImageSlot = T;
    const x = document.createElement("span");
    return x.className = "xb-tavern-image-placeholder", x.textContent = "配图加载中", I.append(x), I;
  }
  function C(T) {
    const I = document.createTreeWalker(T, NodeFilter.SHOW_TEXT, { acceptNode(P) {
      const L = P;
      return !L.data || !Ci.test(L.data) ? (Ci.lastIndex = 0, NodeFilter.FILTER_SKIP) : (Ci.lastIndex = 0, L.parentElement?.closest?.("a, button, code, kbd, pre, script, style, textarea, .xb-tavern-image") ? NodeFilter.FILTER_REJECT : NodeFilter.FILTER_ACCEPT);
    } }), x = [];
    for (; I.nextNode(); ) x.push(I.currentNode);
    x.forEach((P) => {
      const L = P.data;
      Ci.lastIndex = 0;
      let G, z = 0, V = !1;
      const Z = document.createDocumentFragment();
      for (; (G = Ci.exec(L)) !== null; )
        V = !0, G.index > z && Z.append(document.createTextNode(L.slice(z, G.index))), Z.append(E(G[1] || "")), z = Ci.lastIndex;
      V && (z < L.length && Z.append(document.createTextNode(L.slice(z))), P.replaceWith(Z));
    }), T.querySelectorAll("[data-tavern-image-slot]").forEach((P) => w(P));
  }
  function M(T) {
    const I = T.success ? "判定成功" : "判定失败", x = String(T.action || "").trim();
    return [
      `行动判定：${T.stat}。`,
      `掷骰 ${T.roll} 对抗难度 ${T.difficulty}。`,
      `${I}.`,
      x ? `行动意图：${x}。` : ""
    ].filter(Boolean).join(" ");
  }
  function k(T) {
    const I = document.createElement("span");
    I.className = `action-check-card ${T.success ? "is-success" : "is-failure"}`, I.setAttribute("role", "group"), I.setAttribute("aria-label", M(T));
    const x = document.createElement("span");
    x.className = "action-check-card-head";
    const P = document.createElement("strong");
    P.textContent = T.stat, x.append(P);
    const L = document.createElement("span");
    L.className = "action-check-card-outcome", L.textContent = T.success ? "判定成功" : "判定失败", x.append(L);
    const G = document.createElement("span");
    G.className = "action-check-card-grid", [{
      className: "action-check-card-roll",
      label: "掷骰",
      value: String(T.roll)
    }, {
      className: "action-check-card-dc",
      label: "难度",
      value: String(T.difficulty)
    }].forEach((V) => {
      const Z = document.createElement("span");
      Z.className = V.className;
      const O = document.createElement("small");
      O.textContent = V.label;
      const Y = document.createElement("strong");
      Y.textContent = V.value, Z.append(O, Y), G.append(Z);
    });
    const z = document.createElement("span");
    return z.className = "action-check-card-copy", z.textContent = T.action, I.append(x, G, z), I;
  }
  function R(T = []) {
    const I = document.createElement("span");
    return I.className = "assistant-runtime-event-stack", I.setAttribute("role", "group"), I.setAttribute("aria-label", T.length > 1 ? `${T.length} action check results.` : "Action check result."), T.forEach((x) => {
      I.append(k(x));
    }), I;
  }
  function D(T) {
    try {
      return f2(JSON.parse(String(T || "[]")));
    } catch {
      return [];
    }
  }
  function m(T) {
    const I = D(T.dataset.actionCheckGroups);
    if (!I.length) return;
    const x = new Map(I.map((z) => [z.marker, z.events])), P = new Set(I.map((z) => z.marker)), L = document.createTreeWalker(T, NodeFilter.SHOW_TEXT, { acceptNode(z) {
      const V = z;
      return V.data ? V.parentElement?.closest?.("a, button, code, kbd, pre, script, style, textarea, .assistant-runtime-event-stack") ? NodeFilter.FILTER_REJECT : [...V.data].some((Z) => P.has(Z)) ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP : NodeFilter.FILTER_SKIP;
    } }), G = [];
    for (; L.nextNode(); ) G.push(L.currentNode);
    G.forEach((z) => {
      const V = document.createDocumentFragment();
      let Z = !1;
      [...z.data].forEach((O) => {
        const Y = x.get(O);
        if (Y?.length) {
          V.append(R(Y)), Z = !0;
          return;
        }
        V.append(document.createTextNode(O));
      }), Z && z.replaceWith(V);
    });
  }
  function b() {
    const T = e.chatScrollRef.value;
    T?.querySelectorAll && T.querySelectorAll(".xb-tavern-markdown").forEach((I) => {
      const x = I.dataset.markdownSignature || "";
      I.dataset.markdownEnhanced !== x && (O0(I, {
        codeBlockClassName: "xb-tavern-codeblock",
        codeCopyClassName: "xb-tavern-code-copy",
        htmlBlockMode: "preview"
      }), C(I), p(I), m(I), I.dataset.markdownEnhanced = x);
    });
  }
  function v() {
    const T = e.managerScrollRef.value;
    T?.querySelectorAll && T.querySelectorAll(".xb-tavern-markdown").forEach((I) => {
      const x = I.dataset.markdownSignature || "";
      I.dataset.markdownEnhanced !== x && (O0(I, {
        codeBlockClassName: "xb-tavern-codeblock",
        codeCopyClassName: "xb-tavern-code-copy"
      }), I.dataset.markdownEnhanced = x);
    });
  }
  return {
    clearMarkdownCache: o,
    enhanceChatMarkdown: b,
    enhanceManagerMarkdown: v,
    markdownSignature: n,
    renderChatMarkdown: r,
    stripTavernImageMarkers: s
  };
}
function H0(e) {
  const t = /* @__PURE__ */ se(null), n = /* @__PURE__ */ se(!0), r = /* @__PURE__ */ se(!1), s = /* @__PURE__ */ se(!1), o = /* @__PURE__ */ se(!1), i = /* @__PURE__ */ se(Do(S(e.defaultLimit), 5));
  let a = null, l = !1, u = null, d = 0;
  function h() {
    const I = { uiMessageWindowLimit: i.value };
    RF(I, { defaultLimit: Do(S(e.defaultLimit), 5) }), i.value = Number(I.uiMessageWindowLimit || 5);
  }
  function p(I = !1) {
    const x = t.value;
    if (!I && n.value !== !1 || !x || !I && x.scrollTop > 64) return !1;
    const P = { uiMessageWindowLimit: i.value };
    return PF(P, e.totalItems(), {
      defaultLimit: Do(S(e.defaultLimit), 5),
      chunk: sb(S(e.loadBatchSize), 20)
    }) ? (i.value = Number(P.uiMessageWindowLimit || i.value), n.value = !1, !0) : !1;
  }
  function y() {
    const I = t.value;
    if (!I) {
      r.value = !1, s.value = !1;
      return;
    }
    const x = 80;
    r.value = I.scrollTop > x, s.value = I.scrollHeight - I.scrollTop - I.clientHeight > x;
  }
  function w() {
    o.value = !0, t.value?.classList.add("is-scrolling"), a && window.clearTimeout(a), a = window.setTimeout(() => {
      o.value = !1, t.value?.classList.remove("is-scrolling"), a = null;
    }, 1500);
  }
  function E(I = 56) {
    const x = t.value;
    return x ? x.scrollHeight - x.scrollTop - x.clientHeight <= I : !0;
  }
  function C(I = !1) {
    const x = Do(S(e.defaultLimit), 5);
    return i.value <= x || !I && !E(8) ? !1 : (h(), !0);
  }
  yt(() => Do(S(e.defaultLimit), 5), () => {
    h();
  });
  function M(I = !1, x = {}) {
    !I && !n.value || (I && (n.value = !0), (x.collapseWindow || n.value) && C(!0), Jt(() => {
      const P = t.value;
      if (!P) return;
      const L = () => {
        P.scrollTop = P.scrollHeight;
      };
      L(), requestAnimationFrame(() => {
        L(), requestAnimationFrame(() => {
          L(), y(), x.revealHelpers && w();
        });
      });
    }));
  }
  function k() {
    const I = t.value;
    I && (n.value = !1, d = 0, I.scrollTo?.({
      top: 0,
      behavior: "smooth"
    }), I.scrollTop = 0, y(), w());
  }
  function R() {
    const I = t.value;
    if (!I || p()) return;
    const x = Number(I.scrollTop || 0), P = x > d;
    d = x, E() ? (n.value !== !1 || P) && (n.value = !0, C()) : n.value = !1, !l && (l = !0, requestAnimationFrame(() => {
      y(), w(), l = !1;
    }));
  }
  function D(I) {
    Number(I.deltaY || 0) < 0 && (n.value = !1);
  }
  function m(I) {
    u = Number(I.touches?.[0]?.clientY);
  }
  function b(I) {
    const x = Number(I.touches?.[0]?.clientY);
    if (!Number.isFinite(Number(u)) || !Number.isFinite(x)) {
      n.value = !1;
      return;
    }
    u !== null && (x > u + 4 || !E()) && (n.value = !1);
  }
  function v() {
    d = 0;
  }
  function T() {
    a && (window.clearTimeout(a), a = null), t.value?.classList.remove("is-scrolling");
  }
  return {
    scrollRef: t,
    autoScroll: n,
    showScrollTop: r,
    showScrollBottom: s,
    scrollControlsActive: o,
    messageWindowLimit: i,
    resetWindowState: h,
    revealOlderMessages: p,
    updateScrollButtons: y,
    isNearBottom: E,
    collapseMessageWindowIfBottom: C,
    scrollToBottom: M,
    scrollToTop: k,
    handleScroll: R,
    handleWheel: D,
    handleTouchStart: m,
    handleTouchMove: b,
    resetPositionState: v,
    cleanup: T
  };
}
async function b2(e, t) {
  const n = e.body?.getReader?.();
  if (!n) throw new Error("host_chat_completions_stream_missing_body");
  const r = new TextDecoder();
  let s = "";
  const o = /\r?\n\r?\n/, i = (l) => {
    const u = l.split(/\r?\n/).filter((d) => d.startsWith("data:")).map((d) => d.slice(5).trimStart()).join(`
`).trim();
    !u || u === "[DONE]" || t(JSON.parse(u));
  };
  for (; ; ) {
    const { done: l, value: u } = await n.read();
    if (l) break;
    for (s += r.decode(u, { stream: !0 }); ; ) {
      const d = s.match(o);
      if (!d || typeof d.index != "number") break;
      const h = s.slice(0, d.index);
      s = s.slice(d.index + d[0].length), i(h);
    }
  }
  const a = s.trim();
  a && i(a);
}
var Ml = "openai", lb = "claude", ub = "makersuite", w2 = "/api/backends/chat-completions/status", S2 = "/api/backends/chat-completions/generate", Ex = Object.freeze({
  [lb]: "https://api.anthropic.com/v1",
  [ub]: "https://generativelanguage.googleapis.com"
}), Cx = null;
function T2(e) {
  return String(e || "").trim().replace(/\/+$/, "");
}
function E2(e, t) {
  const n = T2(e);
  return t === "claude" ? !n || /\/v\d[\w.-]*$/i.test(n) ? n : `${n}/v1` : t === "makersuite" ? n.replace(/\/v\d[\w.-]*$/i, "") : n;
}
function G0(e) {
  Cx = typeof e == "function" ? e : null;
}
async function Ax() {
  return {
    "Content-Type": "application/json",
    ...await Promise.resolve(Cx?.() || {}),
    Accept: "application/json"
  };
}
function C2(e = {}) {
  const t = {};
  return Object.entries(e || {}).forEach(([n, r]) => {
    t[n] = /authorization|csrf|token|api[-_]?key/i.test(n) ? "[redacted]" : r;
  }), t;
}
async function td(e = {}, t = !1) {
  const n = await Ax(), r = {
    url: S2,
    method: "POST",
    headers: C2(n),
    body: {
      ...e,
      stream: !!t
    }
  };
  return Object.defineProperty(r, "rawHeaders", {
    value: n,
    enumerable: !1
  }), r;
}
function A2(e = "") {
  return /^\s*<!DOCTYPE\s+html/i.test(String(e || ""));
}
function I2(e = "") {
  return /invalid csrf token/i.test(String(e || ""));
}
function x2() {
  return "酒馆当前页面的 CSRF token 已失效，请按 F5 刷新并重新进入酒馆后再试。";
}
function bl(e = "", t = "") {
  return I2(e) || A2(e) ? x2() : String(e || t || "").trim();
}
function Ix(e = {}, t = Ml) {
  const n = E2(e.baseUrl, t), r = String(e.apiKey || "").trim(), s = Ex[t] || "", o = n || (r ? s : ""), i = { chat_completion_source: t || "openai" };
  return o && (i.reverse_proxy = o), r && (i.proxy_password = r), i;
}
function k2(e = {}) {
  return Object.keys(e).forEach((t) => {
    (e[t] === void 0 || e[t] === "") && delete e[t];
  }), e;
}
function M2(e = {}, t = Ml) {
  return Ix(e, t);
}
function cb(e = {}, t = {}, n = [], r = !1, s = Ml) {
  return k2({
    ...Ix(e, s),
    stream: !!r,
    messages: n,
    model: e.model,
    max_tokens: t.maxTokens,
    temperature: t.reasoning?.enabled ? void 0 : t.temperature,
    tools: Array.isArray(t.tools) && t.tools.length ? t.tools : void 0,
    tool_choice: Array.isArray(t.tools) && t.tools.length ? t.toolChoice || "auto" : void 0,
    use_sysprompt: s === "openai" ? void 0 : !0,
    reasoning_effort: t.reasoning?.enabled ? t.reasoning.effort : void 0,
    include_reasoning: s === "openai" ? void 0 : t.reasoning?.enabled ? !0 : void 0
  });
}
function P2(e = {}, t = {}, n = [], r = !1) {
  return cb(e, t, n, r, Ml);
}
function R2(e = {}, t = {}, n = [], r = !1) {
  return cb(e, t, n, r, lb);
}
function N2(e = {}, t = {}, n = [], r = !1) {
  return cb(e, t, n, r, ub);
}
async function $2(e = {}, t = Ml, n = {}) {
  const r = await fetch(w2, {
    method: "POST",
    headers: await Ax(),
    body: JSON.stringify(M2(e, t)),
    signal: n.signal
  }), s = await r.text();
  let o = null;
  try {
    o = s ? JSON.parse(s) : {};
  } catch (a) {
    throw new Error(`酒馆后端模型列表拉取失败：${bl(s, String(a?.message || a))}`);
  }
  if (!r.ok || o?.error) {
    const a = bl(o?.message || o?.error?.message || s, `HTTP ${r.status}`);
    throw new Error(`酒馆后端模型列表拉取失败：${a}`);
  }
  const i = Array.isArray(o?.data) ? o.data.map((a) => String(a?.id || a?.name || "").trim()).filter(Boolean) : [];
  return [...new Set(i)];
}
async function db(e = {}, t = {}) {
  const n = await td(e, !1);
  typeof t.onRequest == "function" && t.onRequest(n);
  const r = await fetch(n.url, {
    method: n.method,
    headers: n.rawHeaders || n.headers,
    body: JSON.stringify(n.body),
    signal: t.signal
  }), s = await r.text();
  let o = null;
  try {
    o = s ? JSON.parse(s) : {};
  } catch (i) {
    throw new Error(`酒馆后端生成失败：${bl(s, String(i?.message || i))}`);
  }
  if (!r.ok || o?.error) {
    const i = bl(o?.error?.message || o?.message || s, `HTTP ${r.status}`);
    throw new Error(`酒馆后端生成失败：${i}`);
  }
  return o;
}
async function fb(e = {}, t, n = {}) {
  const r = await td(e, !0);
  typeof n.onRequest == "function" && n.onRequest(r);
  const s = await fetch(r.url, {
    method: r.method,
    headers: r.rawHeaders || r.headers,
    body: JSON.stringify(r.body),
    signal: n.signal
  });
  if (!s.ok) {
    const o = await s.text().catch(() => "");
    throw new Error(bl(o, `酒馆后端流式生成失败：HTTP ${s.status}`));
  }
  await b2(s, (o) => {
    if (o?.error) {
      const i = bl(o.error?.message || o.message || JSON.stringify(o.error), "酒馆后端流式生成失败");
      throw new Error(i);
    }
    t(o);
  });
}
var dt = /* @__PURE__ */ (function(e) {
  return e[e.before = 0] = "before", e[e.after = 1] = "after", e[e.ANTop = 2] = "ANTop", e[e.ANBottom = 3] = "ANBottom", e[e.atDepth = 4] = "atDepth", e[e.EMTop = 5] = "EMTop", e[e.EMBottom = 6] = "EMBottom", e[e.outlet = 7] = "outlet", e;
})({}), Pg = /* @__PURE__ */ (function(e) {
  return e[e.SYSTEM = 0] = "SYSTEM", e[e.USER = 1] = "USER", e[e.ASSISTANT = 2] = "ASSISTANT", e;
})({}), Lo = /* @__PURE__ */ (function(e) {
  return e[e.AND_ANY = 0] = "AND_ANY", e[e.NOT_ALL = 1] = "NOT_ALL", e[e.NOT_ANY = 2] = "NOT_ANY", e[e.AND_ALL = 3] = "AND_ALL", e;
})({}), zv = {
  [Pg.SYSTEM]: "system",
  [Pg.USER]: "user",
  [Pg.ASSISTANT]: "assistant"
}, W0 = {
  before: dt.before,
  before_char: dt.before,
  beforeCharacter: dt.before,
  after: dt.after,
  after_char: dt.after,
  afterCharacter: dt.after,
  atDepth: dt.atDepth,
  depth: dt.atDepth,
  outlet: dt.outlet,
  ANTop: dt.ANTop,
  ANBottom: dt.ANBottom,
  EMTop: dt.EMTop,
  EMBottom: dt.EMBottom
}, D2 = {
  [dt.before]: "before character",
  [dt.after]: "after character",
  [dt.ANTop]: "author note top",
  [dt.ANBottom]: "author note bottom",
  [dt.atDepth]: "depth",
  [dt.EMTop]: "example top",
  [dt.EMBottom]: "example bottom",
  [dt.outlet]: "outlet"
}, L2 = [
  "top",
  "beforeCharacter",
  "afterCharacter",
  "beforeHistory",
  "afterHistory",
  "assistantPrefill"
];
function Oe(e = "") {
  return String(e || "").trim();
}
function kn(e, t) {
  if (!e || typeof e != "object") return "";
  const n = e;
  for (const r of t) {
    const s = Oe(n[r]);
    if (s) return s;
  }
  return "";
}
function oi(e, t = "system") {
  if (typeof e == "number" && zv[e]) return zv[e];
  const n = String(e || "").trim().toLowerCase();
  return n === "model" ? "assistant" : n === "sys" ? "system" : [
    "system",
    "user",
    "assistant",
    "tool"
  ].includes(n) ? n : t;
}
function la(e, t, n = {}) {
  const r = Oe(t);
  return r ? {
    role: oi(e),
    content: r,
    ...n
  } : null;
}
function Jv(e) {
  return Array.isArray(e) ? e.map((t, n) => {
    const r = t && typeof t == "object" ? t : {}, s = Oe(r.text);
    return s ? {
      label: Oe(r.label) || `reasoning ${n + 1}`,
      text: s
    } : null;
  }).filter((t) => !!t) : [];
}
function O2(e = []) {
  const t = Jv(e);
  return t.length ? [
    "<reasoning>",
    ...t.map((n) => [
      `<thought label="${Oe(n.label).replace(/"/g, "&quot;")}">`,
      n.text,
      "</thought>"
    ].join(`
`)),
    "</reasoning>"
  ].join(`
`) : "";
}
function F2(e) {
  const { thoughts: t, ...n } = e;
  if (e.role !== "assistant") return n;
  const r = O2(e.thoughts || []);
  return r ? {
    ...n,
    content: [e.content, r].filter(Boolean).join(`

`)
  } : n;
}
function hb(e) {
  return e.filter((t) => !!t && !!Oe(t.content));
}
function Vt(e, t, n = "unknown", r = "", s = {}, o = "") {
  return {
    message: la(e, t, s),
    layer: n,
    label: r || n,
    sourceId: Oe(o)
  };
}
function B2(e = []) {
  const t = [], n = [];
  return e.forEach((r) => {
    if (!r.message || !Oe(r.message.content)) return;
    const s = t.length;
    t.push(r.message);
    const o = r.message.content.length;
    n.push({
      index: s,
      role: r.message.role,
      layer: r.layer,
      label: r.label,
      sourceId: r.sourceId || void 0,
      chars: o,
      tokenEstimate: Math.max(1, Math.ceil(o / 4))
    });
  }), {
    messages: t,
    messageLayers: n
  };
}
function Oo(e) {
  if (Array.isArray(e)) return e.map((n) => Oe(n)).filter(Boolean);
  const t = Oe(e);
  return t ? [t] : [];
}
function U2(e = "") {
  const t = [], n = String(e || "").split(`
`);
  let r = 0;
  for (; r < n.length && n[r].startsWith("@@"); ) {
    const s = n[r].trim();
    s && t.push(s.startsWith("@@@") ? s.slice(1) : s), r += 1;
  }
  return {
    decorators: t,
    content: n.slice(r).join(`
`).trim()
  };
}
function q2(e) {
  if (typeof e == "number" && Object.values(dt).includes(e)) return e;
  const t = String(e || "").trim();
  return Object.prototype.hasOwnProperty.call(W0, t) ? W0[t] : dt.after;
}
function pb(e = {}, t = 0) {
  const n = e, r = e.extensions || {}, s = (d, h) => Object.prototype.hasOwnProperty.call(r, d) ? r[d] : n[h], o = U2(e.content || ""), i = e.uid ?? e.id ?? e.comment ?? e.name ?? t + 1, a = Oe(e.sourceWorldBook || e.worldName || e.world), l = o.content || Oe(e.content), u = n.order ?? n.insertion_order;
  return {
    ...e,
    uid: i,
    activationKey: H2(a, i, t),
    content: l,
    decorators: [...Oo(e.decorators), ...o.decorators],
    key: Oo(e.key),
    keysecondary: [...Oo(e.keysecondary), ...Oo(e.secondary_keys)],
    order: Number(u) || 0,
    disable: n.disable === !0 || n.disabled === !0 || n.enabled === !1,
    enabled: n.enabled === !1 ? !1 : n.disable !== !0 && n.disabled !== !0,
    vectorized: s("vectorized", "vectorized") === !0,
    depth: Number.isFinite(Number(s("depth", "depth"))) ? Number(s("depth", "depth")) : 4,
    role: oi(s("role", "role"), "system"),
    position: q2(s("position", "position")),
    selectiveLogic: s("selectiveLogic", "selectiveLogic"),
    scanDepth: Number.isFinite(Number(s("scan_depth", "scanDepth"))) ? Number(s("scan_depth", "scanDepth")) : null,
    caseSensitive: s("case_sensitive", "caseSensitive"),
    matchWholeWords: s("match_whole_words", "matchWholeWords"),
    characterFilter: s("character_filter", "characterFilter") || n.character_filter,
    ignoreBudget: s("ignore_budget", "ignoreBudget") === !0,
    excludeRecursion: s("exclude_recursion", "excludeRecursion") === !0,
    preventRecursion: s("prevent_recursion", "preventRecursion") === !0,
    delayUntilRecursion: s("delay_until_recursion", "delayUntilRecursion"),
    group: Oe(s("group", "group")),
    groupOverride: s("group_override", "groupOverride") === !0,
    groupWeight: Number.isFinite(Number(s("group_weight", "groupWeight"))) ? Number(s("group_weight", "groupWeight")) : void 0,
    useGroupScoring: s("use_group_scoring", "useGroupScoring"),
    probability: s("probability", "probability"),
    useProbability: s("useProbability", "useProbability"),
    sticky: s("sticky", "sticky"),
    cooldown: s("cooldown", "cooldown"),
    delay: s("delay", "delay"),
    triggers: Oo(s("triggers", "triggers")),
    outletName: Oe(s("outlet_name", "outletName")),
    matchPersonaDescription: s("match_persona_description", "matchPersonaDescription"),
    matchCharacterDescription: s("match_character_description", "matchCharacterDescription"),
    matchCharacterPersonality: s("match_character_personality", "matchCharacterPersonality"),
    matchCharacterDepthPrompt: s("match_character_depth_prompt", "matchCharacterDepthPrompt"),
    matchScenario: s("match_scenario", "matchScenario"),
    matchCreatorNotes: s("match_creator_notes", "matchCreatorNotes"),
    activationReason: "",
    sourceWorldBook: a,
    worldSourceType: Oe(e.worldSourceType),
    worldSourceIndex: Number.isFinite(Number(e.worldSourceIndex)) ? Number(e.worldSourceIndex) : t,
    contentChars: l.length
  };
}
function H2(e, t, n = 0) {
  return `${Oe(e) || "direct"}\0${Oe(t) || `index:${n}`}`;
}
function mb(e) {
  return D2[e] || "after character";
}
function Mc(e, t = 2) {
  const n = Number(e);
  return Number.isFinite(n) ? Math.max(0, Math.floor(n)) : t;
}
function xx(e = [], t = 2) {
  const n = Mc(t, 2);
  return n <= 0 ? "" : e.slice(-n).map((r) => String(r || "")).filter(Boolean).join(`
`);
}
function G2(e = {}, t) {
  const n = t?.scanDepth === null || t?.scanDepth === void 0 ? Mc(e.scanDepth ?? 2, 2) : Mc(t.scanDepth, 2), r = [Array.isArray(e.scanMessages) ? xx(e.scanMessages, n) : String(e.scanText || ""), String(e.recursionText || "")], s = e.globalScanData || {};
  return t?.matchPersonaDescription && s.personaDescription && r.push(s.personaDescription), t?.matchCharacterDescription && s.characterDescription && r.push(s.characterDescription), t?.matchCharacterPersonality && s.characterPersonality && r.push(s.characterPersonality), t?.matchCharacterDepthPrompt && s.characterDepthPrompt && r.push(s.characterDepthPrompt), t?.matchScenario && s.scenario && r.push(s.scenario), t?.matchCreatorNotes && s.creatorNotes && r.push(s.creatorNotes), r.filter(Boolean).join(`
`);
}
function Fp(e = {}, t) {
  const n = !!(t?.caseSensitive ?? t?.case_sensitive ?? e.caseSensitive), r = !!(t?.matchWholeWords ?? t?.match_whole_words ?? e.matchWholeWords), s = G2(e, t), o = n ? s : s.toLowerCase();
  return (i = "") => {
    const a = String(i || "").trim();
    if (!a) return !1;
    const l = W2(a);
    if (l) return l.test(s);
    const u = n ? a : a.toLowerCase();
    if (!r) return o.includes(u);
    const d = u.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    return new RegExp(`(^|[^\\p{L}\\p{N}_])${d}($|[^\\p{L}\\p{N}_])`, n ? "u" : "iu").test(o);
  };
}
function W2(e = "") {
  const t = String(e || "").match(/^\/([\w\W]+?)\/([gimsuy]*)$/);
  if (!t) return null;
  let n = t[1] || "";
  const r = t[2] || "";
  if (/(^|[^\\])\//.test(n)) return null;
  n = n.replace("\\/", "/");
  try {
    return new RegExp(n, r);
  } catch {
    return null;
  }
}
function kx(e, t) {
  if (e.selective === !1 || !e.keysecondary.length) return !0;
  const n = e.keysecondary.map((o) => t(o)), r = n.some(Boolean), s = n.every(Boolean);
  switch (Number(e.selectiveLogic ?? e.selective_logic ?? Lo.AND_ANY)) {
    case Lo.NOT_ALL:
      return !s;
    case Lo.NOT_ANY:
      return !r;
    case Lo.AND_ALL:
      return s;
    case Lo.AND_ANY:
    default:
      return r;
  }
}
function gb(e, t) {
  return e.entryStates?.[t.activationKey] || e.entryStates?.[String(t.uid)] || {};
}
function vb(e, t) {
  const n = Number(t.turn) || 0, r = gb(t, e);
  return Number(r.stickyUntilTurn) >= n;
}
function V2(e, t) {
  if (e.useProbability === !1 || e.useProbabilityGlobal === !1 || vb(e, t)) return !0;
  const n = Number(e.probability);
  if (!Number.isFinite(n) || n <= 0) return n !== 0;
  const r = n > 1 ? n / 100 : n;
  return r >= 1 ? !0 : (t.random || Math.random)() <= r;
}
function Mx(e) {
  return Math.max(0, Number(e.recursionPass) || 0);
}
function Px(e, t) {
  const n = e.delayUntilRecursion ?? e.delay_until_recursion;
  return n ? t <= 0 ? !1 : n === !0 ? !0 : t >= Math.max(1, Number(n) || 1) : !0;
}
function Rx(e, t) {
  const n = e.characterFilter;
  if (!n || typeof n != "object") return !0;
  const r = Array.isArray(n.names) ? n.names.map((d) => Oe(d)).filter(Boolean) : [], s = Array.isArray(n.tags) ? n.tags.map((d) => Oe(d)).filter(Boolean) : [];
  if (!r.length && !s.length) return !0;
  const o = new Set((t.characterFilterData?.names || []).map((d) => Oe(d)).filter(Boolean)), i = new Set((t.characterFilterData?.tags || []).map((d) => Oe(d)).filter(Boolean)), a = r.some((d) => o.has(d)), l = s.some((d) => i.has(d)), u = a || l;
  return n.isExclude ? !u : u;
}
function K2(e, t) {
  if (e.disable === !0 || e.disabled === !0 || e.triggers?.length && !e.triggers.includes(Oe(t.trigger) || "normal") || !Rx(e, t)) return "";
  const n = Number(t.turn) || 0, r = gb(t, e), s = vb(e, t), o = Mx(t);
  if (Number(r.delayUntilTurn) > n || Number(e.delay) > 0 && n < Number(e.delay) || Number(r.cooldownUntilTurn) > n && !s || o > 0 && e.excludeRecursion && !s || !Px(e, o) && !s) return "";
  if (e.decorators.includes("@@activate")) return "decorator";
  if (e.decorators.includes("@@dont_activate")) return "";
  if (e.constant === !0) return "constant";
  if (s) return "sticky";
  const i = Fp(t, e);
  return !e.key.some((a) => i(a)) || !kx(e, i) ? "" : "keyword";
}
function z2(e, t) {
  if (e.disable === !0 || e.disabled === !0) return {
    status: "disabled",
    activationReason: ""
  };
  if (e.triggers?.length && !e.triggers.includes(Oe(t.trigger) || "normal")) return {
    status: "trigger_filtered",
    activationReason: ""
  };
  if (!Rx(e, t)) return {
    status: "character_filtered",
    activationReason: ""
  };
  const n = Number(t.turn) || 0, r = gb(t, e), s = vb(e, t), o = Mx(t);
  if (Number(r.delayUntilTurn) > n || Number(e.delay) > 0 && n < Number(e.delay)) return {
    status: "delay",
    activationReason: ""
  };
  if (Number(r.cooldownUntilTurn) > n && !s) return {
    status: "cooldown",
    activationReason: ""
  };
  if (o > 0 && e.excludeRecursion && !s) return {
    status: "excluded_from_recursion",
    activationReason: ""
  };
  if (!Px(e, o) && !s) return {
    status: "delay_until_recursion",
    activationReason: ""
  };
  if (e.decorators.includes("@@activate")) return {
    status: "activated",
    activationReason: "decorator"
  };
  if (e.decorators.includes("@@dont_activate")) return {
    status: "suppressed_by_decorator",
    activationReason: ""
  };
  if (e.constant === !0) return {
    status: "activated",
    activationReason: "constant"
  };
  if (s) return {
    status: "activated",
    activationReason: "sticky"
  };
  const i = Fp(t, e);
  return e.key.some((a) => i(a)) ? kx(e, i) ? {
    status: "activated",
    activationReason: "keyword"
  } : {
    status: "secondary_not_matched",
    activationReason: ""
  } : {
    status: "not_matched",
    activationReason: ""
  };
}
function V0(e, t = {}) {
  const n = Oe(e.worldSourceType);
  if (n === "chat") return 0;
  if (n === "persona") return 1;
  const r = Number(t.insertionStrategy ?? 1);
  return n === "character" || n === "embedded" ? r === 2 ? 3 : 2 : n === "global" ? r === 1 ? 3 : 2 : 4;
}
function K0(e, t = {}) {
  const n = Oe(e.worldSourceType);
  if (Number(t.insertionStrategy ?? 1) === 0) {
    if (n === "global") return 0;
    if (n === "character" || n === "embedded") return 1;
  }
  return 0;
}
function qa(e, t, n = {}) {
  return V0(e, n) - V0(t, n) || t.order - e.order || K0(e, n) - K0(t, n) || Number(e.worldSourceIndex ?? 0) - Number(t.worldSourceIndex ?? 0) || e.activationKey.localeCompare(t.activationKey, "en");
}
function J2(e) {
  const t = Number(e.budgetChars);
  return Number.isFinite(t) && t > 0 ? t : 0;
}
function Kf(e = [], t = {}) {
  const n = J2(t), r = n > 0, s = /* @__PURE__ */ new Set(), o = /* @__PURE__ */ new Map(), i = [];
  let a = 0, l = 0, u = !1;
  return e.forEach((d) => {
    const h = d.content.length;
    if (!h) return;
    const p = r ? Math.max(0, n - a) : Number.POSITIVE_INFINITY;
    if (o.set(d.activationKey, {
      budgetUsedBefore: a,
      budgetRemainingBefore: r ? p : void 0,
      budgetShortfall: r && !d.ignoreBudget && (u || a + h >= n) ? Math.max(1, a + h - n) : void 0
    }), d.ignoreBudget) {
      i.push(d), s.add(d.activationKey);
      return;
    }
    if (u || r && a + h >= n) {
      u = r, l += h;
      return;
    }
    i.push(d), s.add(d.activationKey), a += h;
  }), {
    includedKeys: s,
    byKey: o,
    enabled: r,
    limit: n,
    used: a,
    remaining: r ? Math.max(0, n - a) : 0,
    activatedChars: a,
    skippedChars: l
  };
}
function z0(e) {
  return Oe(e.group).split(/,\s*/).map((t) => t.trim()).filter(Boolean);
}
function Y2(e, t = {}) {
  const n = Fp(t, e), r = e.key.filter((o) => n(o)).length;
  if (!e.key.length || !r) return 0;
  const s = e.keysecondary.filter((o) => n(o)).length;
  if (e.keysecondary.length > 0) {
    const o = Number(e.selectiveLogic ?? e.selective_logic ?? Lo.AND_ANY);
    if (o === Lo.AND_ANY || o === Lo.AND_ALL && s === e.keysecondary.length) return r + s;
  }
  return r;
}
function X2(e = [], t, n = {}) {
  const r = new Set(e), s = /* @__PURE__ */ new Set();
  t.forEach((i) => {
    z0(i).forEach((a) => s.add(a));
  });
  const o = /* @__PURE__ */ new Map();
  return e.forEach((i) => {
    z0(i).forEach((a) => {
      const l = o.get(a) || [];
      l.push(i), o.set(a, l);
    });
  }), o.forEach((i, a) => {
    const l = i.filter((h) => r.has(h));
    if (!l.length) return;
    if (s.has(a)) {
      l.forEach((h) => r.delete(h));
      return;
    }
    if (n.useGroupScoring === !0 || l.some((h) => h.useGroupScoring === !0)) {
      const h = l.map((y) => Y2(y, n)), p = Math.max(...h);
      l.forEach((y, w) => {
        (y.useGroupScoring ?? n.useGroupScoring) && h[w] < p && r.delete(y);
      });
    }
    const u = l.filter((h) => r.has(h));
    if (u.length <= 1) return;
    const d = u.filter((h) => h.groupOverride).sort((h, p) => qa(h, p, n))[0] || Q2(u, n);
    u.forEach((h) => {
      h !== d && r.delete(h);
    });
  }), e.filter((i) => r.has(i));
}
function Q2(e = [], t = {}) {
  if (!e.length) return null;
  const n = e.map((o) => ({
    entry: o,
    weight: Math.max(1, Number(o.groupWeight) || 100)
  })), r = n.reduce((o, i) => o + i.weight, 0);
  let s = (t.random || Math.random)() * r;
  for (const o of n)
    if (s -= o.weight, s <= 0) return o.entry;
  return n[n.length - 1]?.entry || null;
}
function Z2(e = [], t = {}, n = {}) {
  const r = {
    ...t,
    ...n,
    scanText: n.scanText ?? t.scanText ?? ""
  }, s = (Array.isArray(e) ? e : []).map((I, x) => pb(I, x)).sort((I, x) => qa(I, x, r)), o = Math.max(0, Number(r.recursionLimit) || 0), i = !!r.recursion, a = i ? o > 0 ? Math.max(0, o - 1) : Math.max(1, s.length) : 0, l = /* @__PURE__ */ new Map(), u = /* @__PURE__ */ new Map(), d = /* @__PURE__ */ new Set(), h = String(r.scanText || ""), p = Mc(r.scanDepth ?? 2, 2), y = Math.max(0, Number(r.minActivations) || 0), w = Math.max(0, Number(r.minActivationsDepthMax) || 0), E = Array.isArray(r.scanMessages) ? r.scanMessages.length : 0, C = Math.max(p, w > 0 ? w : E);
  let M = "", k = 0, R = 0, D = "initial", m = 0;
  const b = Math.max(1, s.length + a + C + 4);
  for (; m < b; ) {
    m += 1;
    const I = p + R, x = D === "minActivations" ? "" : M, P = [h, x].filter(Boolean).join(`
`), L = {
      ...r,
      scanDepth: I,
      scanText: P,
      recursionText: x,
      recursionPass: D === "recursion" ? k : 0
    };
    let G = !1;
    const z = [];
    s.forEach((Y) => {
      const X = Y.activationKey;
      if (u.has(X) || d.has(X)) return;
      const j = K2(Y, L);
      j && z.push({
        ...Y,
        activationReason: j
      });
    }), X2(z, u, r).forEach((Y) => {
      if (!V2(Y, L)) {
        d.add(Y.activationKey);
        return;
      }
      u.set(Y.activationKey, Y);
      const X = Kf([...u.values()].sort((j, re) => qa(j, re, r)), r);
      (!X.enabled || X.includedKeys.has(Y.activationKey)) && (l.set(Y.activationKey, Y), Y.preventRecursion || (M = [M, Y.content].filter(Boolean).join(`
`)), G = !0);
    });
    const V = Kf([...u.values()].sort((Y, X) => qa(Y, X, r)), r), Z = V.enabled && V.skippedChars > 0;
    if (i && G && !Z && k < a) {
      k += 1, D = "recursion";
      continue;
    }
    const O = y > 0 && l.size < y;
    if (!Z && O && E > 0 && I < C && I < E) {
      R += 1, D = "minActivations";
      continue;
    }
    break;
  }
  const v = [...u.values()].sort((I, x) => qa(I, x, r)), T = Kf(v, r);
  return {
    activatedBeforeBudget: v,
    activatedEntries: [...l.values()].sort((I, x) => qa(I, x, r)).filter((I) => !T.enabled || T.includedKeys.has(I.activationKey)),
    budgetDebug: T
  };
}
function j2(e = [], t = [], n = {}, r = Kf(t, n)) {
  const s = new Map(t.map((i) => [i.activationKey, i])), o = r.includedKeys;
  return (Array.isArray(e) ? e : []).map((i, a) => {
    const l = pb(i, a), u = s.get(l.activationKey), d = r.byKey.get(l.activationKey) || {}, h = u ? {
      status: "activated",
      activationReason: u.activationReason
    } : z2(l, n), p = Fp(n, l), y = u ? r.enabled && !o.has(l.activationKey) ? "budget_skipped" : "activated" : h.status === "activated" ? "probability_failed" : h.status;
    return {
      uid: l.uid,
      activationKey: l.activationKey,
      title: Oe(l.comment || l.title || l.name || l.uid),
      sourceWorldBook: l.sourceWorldBook,
      worldSourceType: l.worldSourceType,
      worldSourceIndex: l.worldSourceIndex,
      content: l.content,
      contentChars: l.contentChars,
      key: l.key,
      keysecondary: l.keysecondary,
      matchedKeys: l.key.filter((w) => p(w)),
      matchedSecondaryKeys: l.keysecondary.filter((w) => p(w)),
      decorators: l.decorators,
      position: l.position,
      positionLabel: mb(l.position),
      role: l.role,
      order: l.order,
      depth: l.depth,
      status: y,
      activationReason: u?.activationReason || h.activationReason,
      insertionTarget: Bp(l),
      ...d
    };
  });
}
function eB(e = []) {
  return (Array.isArray(e) ? e : []).map((t, n) => ({
    ...pb(t, n),
    activationReason: Oe(t.activationReason) || "native"
  })).filter((t) => !!t.content);
}
function tB(e = []) {
  return e.map((t) => ({
    uid: t.uid,
    activationKey: t.activationKey,
    title: Oe(t.comment || t.title || t.name || t.uid),
    sourceWorldBook: t.sourceWorldBook,
    worldSourceType: t.worldSourceType,
    worldSourceIndex: t.worldSourceIndex,
    content: t.content,
    contentChars: t.contentChars,
    key: t.key,
    keysecondary: t.keysecondary,
    matchedKeys: [],
    matchedSecondaryKeys: [],
    decorators: t.decorators,
    position: t.position,
    positionLabel: mb(t.position),
    role: t.role,
    order: t.order,
    depth: t.depth,
    status: "activated",
    activationReason: t.activationReason || "native",
    insertionTarget: Bp(t)
  }));
}
function nB(e = {}) {
  const t = [];
  let n = 0;
  const r = (o, i, a = {}) => {
    const l = Oe(o);
    if (!l) return;
    const u = `native:${i}:${n}`;
    t.push({
      uid: u,
      activationKey: u,
      content: l,
      contentChars: l.length,
      key: [],
      keysecondary: [],
      decorators: [],
      position: i,
      role: a.role || "system",
      order: n,
      depth: Number.isFinite(Number(a.depth)) ? Number(a.depth) : 4,
      activationReason: "native",
      sourceWorldBook: "",
      title: "",
      comment: "",
      ...a
    }), n += 1;
  };
  r(e.worldInfoBefore, dt.before), r(e.worldInfoAfter, dt.after), (Array.isArray(e.worldInfoExamples) ? e.worldInfoExamples : []).forEach((o) => {
    const i = Oe(o?.position) === "after" ? dt.EMBottom : dt.EMTop;
    r(o?.content, i);
  }), (Array.isArray(e.anBefore) ? e.anBefore : []).forEach((o) => {
    r(o, dt.ANTop);
  }), (Array.isArray(e.anAfter) ? e.anAfter : []).forEach((o) => {
    r(o, dt.ANBottom);
  }), (Array.isArray(e.worldInfoDepth) ? e.worldInfoDepth : []).forEach((o) => {
    const i = Number.isFinite(Number(o?.depth)) ? Number(o.depth) : 4, a = oi(zv[Number(o?.role)] || "system", "system");
    (Array.isArray(o?.entries) ? o.entries : []).forEach((l) => {
      r(l, dt.atDepth, {
        depth: i,
        role: a
      });
    });
  });
  const s = e.outlets && typeof e.outlets == "object" ? e.outlets : {};
  return Object.entries(s).forEach(([o, i]) => {
    (Array.isArray(i) ? i : []).forEach((a) => {
      r(a, dt.outlet, { outletName: o });
    });
  }), t;
}
function Bp(e) {
  switch (e.position) {
    case dt.before:
      return "before character card";
    case dt.after:
      return "after character card";
    case dt.atDepth:
      return `history depth ${Math.max(0, Number(e.depth) || 0)}`;
    case dt.ANTop:
      return "author note top";
    case dt.ANBottom:
      return "author note bottom";
    case dt.EMTop:
      return "example messages top";
    case dt.EMBottom:
      return "example messages bottom";
    case dt.outlet:
      return `outlet:${Oe(e.outletName || e.outlet || "default")}`;
    default:
      return mb(e.position);
  }
}
function rB(e = []) {
  const t = {
    before: [],
    after: [],
    atDepth: [],
    outlet: {},
    examplesTop: [],
    examplesBottom: [],
    authorNoteTop: [],
    authorNoteBottom: []
  };
  return e.forEach((n) => {
    if (n.content)
      switch (n.position) {
        case dt.before:
          t.before.push(n);
          break;
        case dt.atDepth:
          t.atDepth.push(n);
          break;
        case dt.outlet: {
          const r = Oe(n.outletName || n.outlet || "default");
          t.outlet[r] = t.outlet[r] || [], t.outlet[r].push(n);
          break;
        }
        case dt.EMTop:
          t.examplesTop.push(n);
          break;
        case dt.EMBottom:
          t.examplesBottom.push(n);
          break;
        case dt.ANTop:
          t.authorNoteTop.push(n);
          break;
        case dt.ANBottom:
          t.authorNoteBottom.push(n);
          break;
        case dt.after:
        default:
          t.after.push(n);
          break;
      }
  }), t;
}
function sB(e = []) {
  const t = {};
  return e.forEach((n) => {
    const r = Bp(n);
    t[r] = (t[r] || 0) + 1;
  }), t;
}
function oB(e = [], t = {}) {
  const n = Number(t.turn) || 0, r = {};
  return e.forEach((s) => {
    const o = s.activationKey, i = {}, a = s.sticky === !0 ? 1 : Number(s.sticky), l = Number(s.cooldown), u = Number(s.delay);
    Number.isFinite(a) && a > 0 && (i.stickyUntilTurn = n + a), Number.isFinite(l) && l > 0 && (i.cooldownUntilTurn = n + l), Number.isFinite(u) && u > 0 && (i.delayUntilTurn = n + u), Object.keys(i).length && (r[o] = i);
  }), r;
}
function Nx(e = []) {
  return [...e].sort((t, n) => t.order - n.order || Number(n.worldSourceIndex ?? 0) - Number(t.worldSourceIndex ?? 0) || n.activationKey.localeCompare(t.activationKey, "en"));
}
function Nn(e, t = []) {
  const n = Nx(t).map((r) => r.content).filter(Boolean).join(`

`);
  return n ? `<${e}>
${n}
</${e}>` : "";
}
function $x(e = {}, t = {}) {
  const n = e.data || {}, r = [
    ["Character", e.name || kn(n, ["name"])],
    ["User", t.name],
    ["Description", e.description || kn(n, ["description"])],
    ["Personality", e.personality || kn(n, ["personality"])],
    ["Scenario", e.scenario || kn(n, ["scenario"])],
    ["Creator Notes", e.creatorNotes || e.creator_notes || kn(n, ["creator_notes"])],
    ["First Message", e.firstMessage || e.first_mes || kn(n, ["first_mes"])],
    ["Message Examples", e.mesExample || e.mes_example || kn(n, ["mes_example"])],
    ["User Persona", t.persona || t.description]
  ].map(([s, o]) => {
    const i = Oe(o);
    return i ? `## ${s}
${i}` : "";
  }).filter(Boolean);
  return r.length ? `<character_card>
${r.join(`

`)}
</character_card>` : "";
}
function fu(e, t) {
  const n = Oe(t);
  return n ? `## ${e}
${n}` : "";
}
function Yv(e = {}) {
  const t = Array.isArray(e.memoryFiles) ? e.memoryFiles : [], n = Array.isArray(e.structuredStates) ? e.structuredStates : [], r = [], s = t.map((i) => {
    const a = Oe(i.path), l = Oe(i.title) || a || "记忆档案", u = Oe(i.content);
    return u ? `### ${l}${a ? ` (${a})` : ""}
${u}` : "";
  }).filter(Boolean);
  s.length && r.push(`## 固定记忆档案
${s.join(`

`)}`);
  const o = n.map((i) => {
    const a = Oe(i.docType), l = Oe(i.docId), u = Oe(i.title) || `${a}/${l}`, d = Oe(i.digest);
    return d ? `### ${u}${a || l ? ` (${a}/${l}, revision ${Number(i.revision) || 0})` : ""}
${d}` : "";
  }).filter(Boolean);
  return o.length && r.push(`## 可视化结构状态摘要
${o.join(`

`)}`), r.length ? `<session_memory>
${r.join(`

`)}
</session_memory>` : "";
}
function iB(e = {}) {
  return (Array.isArray(e.sections) ? e.sections : []).map((t) => ({
    id: Oe(t.id),
    label: Oe(t.label),
    enabled: t.enabled !== !1,
    marker: t.marker === !0,
    role: oi(t.role, "system"),
    content: Oe(t.content),
    placement: L2.includes(t.placement) ? t.placement : "beforeHistory",
    source: Oe(t.source)
  })).filter((t) => t.enabled && (t.content || t.marker));
}
function Aa(e = [], t = "") {
  return e.filter((n) => n.placement === t);
}
function Ia(e = [], t, n = "preset") {
  return e.map((r, s) => ({
    message: la(r.role, r.content),
    layer: n,
    label: r.label || `preset ${t} ${s + 1}`,
    sourceId: r.id || void 0
  }));
}
function aB(e = {}) {
  return Oe(e.id).replace(/^prompt-manager:/, "");
}
function lB(e) {
  const { presetSections: t, character: n, user: r, worldBuckets: s, historyUnits: o, currentUserUnits: i, memoryContext: a } = e, l = n.data || {}, u = /* @__PURE__ */ new Set(), d = [], h = (p, y) => {
    u.add(p), d.push(...y);
  };
  return t.forEach((p, y) => {
    const w = aB(p);
    if (p.marker) switch (w) {
      case "worldInfoBefore":
        h(w, [Vt("system", Nn("world_info_before_character", s.before), "world-before", p.label || "World Info (before)", {}, p.id)]);
        return;
      case "worldInfoAfter":
        h(w, [Vt("system", Nn("world_info_after_character", s.after), "world-after", p.label || "World Info (after)", {}, p.id)]);
        return;
      case "charDescription":
        h(w, [Vt(p.role, fu("Description", n.description || kn(l, ["description"])), "character-card", p.label || "Char Description", {}, p.id)]);
        return;
      case "charPersonality":
        h(w, [Vt(p.role, fu("Personality", n.personality || kn(l, ["personality"])), "character-card", p.label || "Char Personality", {}, p.id)]);
        return;
      case "scenario":
        h(w, [Vt(p.role, fu("Scenario", n.scenario || kn(l, ["scenario"])), "character-card", p.label || "Scenario", {}, p.id)]);
        return;
      case "personaDescription":
        h(w, [Vt(p.role, fu("User Persona", r.persona || r.description), "character-card", p.label || "Persona Description", {}, p.id)]);
        return;
      case "dialogueExamples":
        h(w, [Vt(p.role, fu("Message Examples", n.mesExample || n.mes_example || kn(l, ["mes_example"])), "character-card", p.label || "Chat Examples", {}, p.id)]);
        return;
      case "chatHistory":
        h(w, [
          Vt("system", Nn("world_info_examples_top", s.examplesTop), "world-examples", "world info examples top"),
          Vt("system", Nn("world_info_author_note_top", s.authorNoteTop), "world-author-note", "world info author note top"),
          ...o,
          Vt("system", Yv(a), "memory", "session memory"),
          ...i
        ]);
        return;
      default:
        return;
    }
    d.push({
      message: la(p.role, p.content),
      layer: (p.source === "promptManager", "preset"),
      label: p.label || `preset ordered ${y + 1}`,
      sourceId: p.id || void 0
    });
  }), u.has("worldInfoBefore") || d.push(Vt("system", Nn("world_info_before_character", s.before), "world-before", "world info before character")), !u.has("charDescription") && !u.has("charPersonality") && !u.has("scenario") && !u.has("personaDescription") && !u.has("dialogueExamples") && d.push(Vt("system", $x(n, r), "character-card", "character card")), u.has("worldInfoAfter") || d.push(Vt("system", Nn("world_info_after_character", s.after), "world-after", "world info after character")), u.has("chatHistory") || d.push(Vt("system", Nn("world_info_examples_top", s.examplesTop), "world-examples", "world info examples top"), Vt("system", Nn("world_info_author_note_top", s.authorNoteTop), "world-author-note", "world info author note top"), ...o, Vt("system", Yv(a), "memory", "session memory"), ...i), d.push(Vt("system", Nn("world_info_author_note_bottom", s.authorNoteBottom), "world-author-note", "world info author note bottom"), Vt("system", Nn("world_info_examples_bottom", s.examplesBottom), "world-examples", "world info examples bottom")), d;
}
function Dx(e = {}) {
  const t = e.is_user === !0 ? "user" : oi(e.role, "assistant");
  return t === "tool" ? null : la(t, e.content || e.mes || e.message, {
    ...e.name ? { name: String(e.name) } : {},
    ...Jv(e.thoughts).length ? { thoughts: Jv(e.thoughts) } : {}
  });
}
function uB(e = [], t = {}) {
  const n = (Array.isArray(e) ? e : []).map((s) => Dx(s)).filter((s) => !!s);
  if (!n.length) return [];
  const r = t.separator || `

`;
  return [la(oi(t.role, "system"), `<conversation_history>
${n.map((s) => `<message role="${s.role === "user" ? "user" : "assistant"}">
${s.content}
</message>`).join(r)}
</conversation_history>`)].filter((s) => !!s);
}
function cB(e = [], t = {}) {
  return t.mode === "raw" ? (Array.isArray(e) ? e : []).map((n) => Dx(n)).filter((n) => !!n) : uB(e, t);
}
function dB(e = []) {
  const t = /* @__PURE__ */ new Map();
  return Nx(e).forEach((n) => {
    const r = Math.max(0, Number(n.depth) || 0), s = oi(n.role, "system"), o = `${r}\0${s}`, i = t.get(o) || {
      depth: r,
      role: s,
      entries: []
    };
    i.entries.push(n.content), t.set(o, i);
  }), [...t.values()].map((n) => ({
    depth: n.depth,
    message: la(n.role, `<world_info_depth depth="${n.depth}">
${n.entries.join(`

`)}
</world_info_depth>`)
  })).filter((n) => !!n.message);
}
function fB(e = [], t = []) {
  if (!t.length) return e;
  const n = Array.from({ length: e.length + 1 }, () => []);
  t.forEach((s) => {
    const o = Math.max(0, Number(s.depth) || 0);
    n[(e.length ? Math.max(-1, e.length - 1 - o) : -1) + 1].push(s.message);
  });
  const r = [...n[0]];
  return e.forEach((s, o) => {
    r.push(s, ...n[o + 1]);
  }), r;
}
function hB(e = {}) {
  const t = e.character || {}, n = e.user || {}, r = t.data || {};
  return {
    personaDescription: Oe(n.persona || n.description),
    characterDescription: Oe(t.description || kn(r, ["description"])),
    characterPersonality: Oe(t.personality || kn(r, ["personality"])),
    characterDepthPrompt: Oe(kn(r, ["character_depth_prompt"]) || kn(r, ["depth_prompt"])),
    scenario: Oe(t.scenario || kn(r, ["scenario"])),
    creatorNotes: Oe(t.creatorNotes || t.creator_notes || kn(r, ["creator_notes"]))
  };
}
function pB(e = {}) {
  const t = e.character || {}, n = Oe(t.avatar), r = n.split(/[\\/]/).pop() || n, s = Array.isArray(t.tags) ? t.tags : [];
  return {
    names: [
      t.name,
      t.id,
      n,
      r
    ].map((o) => Oe(o)).filter(Boolean),
    tags: s.map((o) => Oe(o)).filter(Boolean)
  };
}
function mB(e = {}, t = {}) {
  const n = String(e.content || e.mes || e.message || "");
  if (!n) return "";
  const r = (e.is_user === !0 ? "user" : oi(e.role, "assistant")) === "user" ? t.user?.name : t.character?.name, s = Oe(e.name || r);
  return s ? `${s}: ${n}` : n;
}
function gB(e = {}, t = "", n = !1) {
  const r = e.history || [], s = n && t ? `${Oe(e.user?.name) || "User"}: ${t}` : t;
  return [...r.map((o) => n ? mB(o, e) : o.content || o.mes || o.message || ""), s].map((o) => String(o || "")).filter(Boolean);
}
function vB(e = {}) {
  const t = !(Array.isArray(e.worldBooks) && e.worldBooks.length > 0) && Array.isArray(e.worldEntries) ? e.worldEntries.map((r) => ({
    ...r,
    sourceWorldBook: r.sourceWorldBook || r.worldName || r.world || ""
  })) : [], n = (Array.isArray(e.worldBooks) ? e.worldBooks : []).flatMap((r) => {
    const s = Oe(r.worldSourceType), o = Number(r.worldSourceIndex);
    return Array.isArray(r.entries) ? r.entries.map((i) => ({
      ...i,
      sourceWorldBook: i.sourceWorldBook || i.worldName || i.world || r.name,
      worldSourceType: Oe(i.worldSourceType) || s,
      worldSourceIndex: Number.isFinite(Number(i.worldSourceIndex)) ? Number(i.worldSourceIndex) : Number.isFinite(o) ? o : void 0
    })) : [];
  });
  return _B([...t, ...n]);
}
function yB(e = {}) {
  const t = Oe(e.sourceWorldBook || e.worldName || e.world) || "direct", n = Oe(e.uid ?? e.id);
  if (n) return `${t}\0uid\0${n}`;
  const r = Oe(e.comment || e.title || e.name), s = Oe(e.content);
  return `${t}\0body\0${r}\0${Oo(e.key).join("")}\0${[...Oo(e.keysecondary), ...Oo(e.secondary_keys)].join("")}\0${s}`;
}
function _B(e = []) {
  const t = /* @__PURE__ */ new Set(), n = [];
  return e.forEach((r) => {
    const s = yB(r);
    t.has(s) || (t.add(s), n.push(r));
  }), n;
}
function Lx(e = {}, t = {}, n = {}) {
  const r = e.character || {}, s = e.user || {}, o = e.history || [], i = n.currentUserMessage || "", a = n.historyMode || "raw", l = n.memoryContext || {}, u = iB(t), d = n.worldSettings || {}, h = typeof n.worldScanText == "string", p = gB(e, i, d.includeNames === !0), y = h ? String(n.worldScanText || "") : xx(p, Mc(d.scanDepth ?? 2, 2)), w = eB(e.nativeWorldInfo?.activatedEntries), E = nB(e.nativeWorldInfo), C = {
    ...d,
    scanText: y,
    scanMessages: h ? void 0 : p,
    globalScanData: d.globalScanData || hB(e),
    characterFilterData: d.characterFilterData || pB(e),
    turn: n.turn ?? n.worldSettings?.turn,
    entryStates: n.entryStates ?? n.worldSettings?.entryStates
  }, M = vB(e), k = e.nativeWorldInfo ? M.filter((T) => Oe(T.worldSourceType) === "embedded") : M, R = Z2(k, C), D = R.activatedBeforeBudget, m = R.budgetDebug, b = j2(k, D, C, m), v = R.activatedEntries;
  if (w.length || E.length || e.nativeWorldInfo) {
    const T = E.length ? E : w;
    return {
      character: r,
      user: s,
      history: o,
      currentUserMessage: i,
      historyMode: a,
      squashRole: n.squashRole,
      memoryContext: l,
      presetSections: u,
      scanText: y,
      worldSettings: C,
      worldEntryCandidates: [...tB(w), ...b],
      activatedWorldEntries: [...T, ...v],
      localStateWorldEntries: v,
      budgetDebug: m
    };
  }
  return {
    character: r,
    user: s,
    history: o,
    currentUserMessage: i,
    historyMode: a,
    squashRole: n.squashRole,
    memoryContext: l,
    presetSections: u,
    scanText: y,
    worldSettings: C,
    worldEntryCandidates: b,
    activatedWorldEntries: v,
    localStateWorldEntries: v,
    budgetDebug: m
  };
}
function Ox(e, t = {}) {
  const n = cB(e.history, {
    mode: e.historyMode,
    role: e.squashRole || "system",
    userName: e.user.name,
    characterName: e.character.name,
    separator: t.historySeparator
  }), r = la("user", e.currentUserMessage);
  return hb([...n, r]);
}
function Fx(e = {}, t, n) {
  const { character: r, user: s, history: o, currentUserMessage: i, historyMode: a, squashRole: l, memoryContext: u, presetSections: d, scanText: h, worldSettings: p, worldEntryCandidates: y, activatedWorldEntries: w, budgetDebug: E } = t, C = rB(w), M = fB((t.promptConversationMessages || Ox(t, e)).map(F2), dB(C.atDepth)), k = Ia(Aa(d, "top"), "top"), R = Ia(Aa(d, "beforeCharacter"), "before character"), D = Ia(Aa(d, "afterCharacter"), "after character"), m = Ia(Aa(d, "beforeHistory"), "before history"), b = Ia(Aa(d, "afterHistory"), "after history"), v = Ia(Aa(d, "assistantPrefill"), "assistant prefill", "assistant-prefill"), T = d.some((V) => V.source === "promptManager"), I = M.map((V, Z) => ({
    message: V,
    layer: V.role === "user" ? "current-user/history" : "history",
    label: `history ${Z + 1}`
  }));
  let x = -1;
  for (let V = I.length - 1; V >= 0; V -= 1) if (I[V]?.message?.role === "user") {
    x = V, I[V].label = "current user message";
    break;
  }
  const P = x >= 0 ? I.slice(0, x) : I, L = x >= 0 ? I.slice(x) : [], G = B2((T ? lB({
    presetSections: d,
    character: r,
    user: s,
    worldBuckets: C,
    historyUnits: P,
    currentUserUnits: L,
    memoryContext: u
  }) : null) || [
    ...k,
    Vt("system", Nn("world_info_before_character", C.before), "world-before", "world info before character"),
    ...R,
    Vt("system", $x(r, s), "character-card", "character card"),
    Vt("system", Nn("world_info_after_character", C.after), "world-after", "world info after character"),
    ...D,
    Vt("system", Nn("world_info_examples_top", C.examplesTop), "world-examples", "world info examples top"),
    Vt("system", Nn("world_info_author_note_top", C.authorNoteTop), "world-author-note", "world info author note top"),
    ...m,
    ...P,
    Vt("system", Yv(u), "memory", "session memory"),
    ...L,
    ...b,
    Vt("system", Nn("world_info_author_note_bottom", C.authorNoteBottom), "world-author-note", "world info author note bottom"),
    Vt("system", Nn("world_info_examples_bottom", C.examplesBottom), "world-examples", "world info examples bottom"),
    ...v
  ]), z = G.messages;
  return {
    messages: z,
    messageLayers: G.messageLayers,
    activatedWorldEntries: w,
    worldEntryCandidates: y,
    outlets: Object.fromEntries(Object.entries(C.outlet).map(([V, Z]) => [V, Z.map((O) => O.content).join(`

`)])),
    meta: {
      scanText: h,
      scanTextChars: h.length,
      historyMode: a,
      squashedHistory: a !== "raw",
      rawMessagesJson: JSON.stringify(z, null, 2),
      ...n ? { regexApplications: n } : {},
      ...u.structuredStates?.length ? { structuredStates: u.structuredStates } : {},
      worldBudget: {
        enabled: E.enabled,
        limit: E.limit,
        used: E.used,
        remaining: E.remaining,
        activatedChars: E.activatedChars,
        skippedChars: E.skippedChars
      },
      worldPositionCounts: sB(w),
      worldEntryStateUpdates: oB(t.localStateWorldEntries, p)
    }
  };
}
function bB(e, t = []) {
  const n = hb(t.map((r) => ({
    ...r,
    content: Oe(r.content)
  })));
  return {
    ...e,
    messages: n,
    messageLayers: e.messageLayers.map((r, s) => {
      const o = n[s];
      if (!o) return r;
      const i = o.content.length;
      return {
        ...r,
        role: o.role,
        chars: i,
        tokenEstimate: Math.max(1, Math.ceil(i / 4))
      };
    }),
    meta: {
      ...e.meta,
      rawMessagesJson: JSON.stringify(n, null, 2)
    }
  };
}
function wB(e = {}, t = {}, n = {}) {
  return Fx(t, Lx(e, t, n));
}
async function SB(e = {}, t = {}, n = {}, r = {}) {
  const s = Lx(e, t, n);
  if (r.transformWorldEntries) {
    s.activatedWorldEntries = (await r.transformWorldEntries(s.activatedWorldEntries)).map((a) => ({
      ...a,
      content: Oe(a.content),
      contentChars: Oe(a.content).length
    })).filter((a) => !!a.content);
    const i = new Map(s.activatedWorldEntries.map((a) => [a.activationKey, a]));
    s.localStateWorldEntries = s.localStateWorldEntries.map((a) => i.get(a.activationKey) || a).filter((a) => !!a.content);
  }
  r.transformConversationMessages && (s.promptConversationMessages = hb((await r.transformConversationMessages(Ox(s, t))).map((i) => ({
    ...i,
    content: Oe(i.content)
  }))));
  const o = Fx(t, s, r.regexApplications);
  return r.transformFinalMessages ? bB(o, await r.transformFinalMessages(o.messages)) : o;
}
function Bx(e = {}, t = {}, n, r = void 0) {
  const s = e.character || {}, o = e.user || {}, i = Array.isArray(e.worldBooks) ? e.worldBooks : [], a = new Map(n.worldEntryCandidates.map((l) => [l.activationKey, l]));
  return {
    chatPresetId: Oe(t.id),
    chatPresetName: Oe(t.name),
    presetId: Oe(t.id),
    presetName: Oe(t.name),
    characterId: Oe(s.id),
    characterName: Oe(s.name),
    userName: Oe(o.name),
    historyCount: Array.isArray(e.history) ? e.history.length : 0,
    worldBooks: i.map((l) => ({
      name: Oe(l.name),
      entries: Array.isArray(l.entries) ? l.entries.length : 0,
      ...l.error ? { error: Oe(l.error) } : {}
    })),
    messageCount: n.messages.length,
    messageChars: n.messages.reduce((l, u) => l + String(u.content || "").length, 0),
    messageLayers: n.messageLayers,
    rawMessagesJson: n.meta.rawMessagesJson,
    ...n.meta.regexApplications ? { regexApplications: n.meta.regexApplications } : {},
    activatedWorldEntries: n.activatedWorldEntries.map((l) => {
      const u = a.get(l.activationKey);
      return {
        uid: l.uid,
        sourceWorldBook: l.sourceWorldBook,
        title: u?.title || Oe(l.comment || l.title || l.name || l.uid),
        activationReason: l.activationReason,
        insertionTarget: u?.insertionTarget || Bp(l),
        contentChars: l.contentChars
      };
    }),
    ...e.nativeWorldInfo ? { nativeWorldInfo: {
      trigger: Oe(e.nativeWorldInfo.trigger),
      sourceNames: Array.isArray(e.nativeWorldInfo.sourceNames) ? e.nativeWorldInfo.sourceNames.map((l) => ({
        name: Oe(l.name),
        ...l.sourceType ? { sourceType: l.sourceType } : {},
        ...Number.isFinite(Number(l.sourceIndex)) ? { sourceIndex: Number(l.sourceIndex) } : {}
      })) : []
    } } : {},
    ...n.meta.structuredStates?.length ? { structuredStates: n.meta.structuredStates.map((l) => ({
      docType: Oe(l.docType),
      docId: Oe(l.docId),
      revision: Number(l.revision) || 0,
      digestChars: Oe(l.digest).length
    })) } : {},
    worldBudget: n.meta.worldBudget,
    worldPositionCounts: n.meta.worldPositionCounts,
    scanTextChars: n.meta.scanTextChars,
    ...r === void 0 ? {} : { diagnostics: r }
  };
}
var TB = Object.freeze({
  recursion: !0,
  recursionLimit: 4,
  budgetChars: 24e3
});
function EB(e = {}) {
  const t = e.worldSettings || {};
  return {
    ...TB,
    ...t,
    turn: Math.max(0, Number(e.turn) || 0),
    entryStates: e.entryStates || {}
  };
}
function Ux(e) {
  return {
    currentUserMessage: String(e.currentUserMessage || ""),
    historyMode: e.historyMode || "raw",
    worldSettings: EB({
      worldSettings: e.context?.worldSettings,
      turn: e.turn,
      entryStates: e.entryStates
    }),
    memoryContext: e.memoryContext
  };
}
function qx(e) {
  const t = e.context || {}, n = e.chatPreset || e.preset || {}, r = Ux(e), s = wB(t, n, r);
  return {
    runtimeState: r,
    buildResult: s,
    buildSnapshot: Bx(t, n, s, e.diagnostics),
    rawMessagesJson: s.meta.rawMessagesJson
  };
}
async function yb(e) {
  const t = e.context || {}, n = e.chatPreset || e.preset || {}, r = Ux(e), s = await SB(t, n, r, {
    transformWorldEntries: e.transformWorldEntries,
    transformConversationMessages: e.transformConversationMessages,
    transformFinalMessages: e.transformFinalMessages,
    regexApplications: e.regexApplications
  });
  return {
    runtimeState: r,
    buildResult: s,
    buildSnapshot: Bx(t, n, s, e.diagnostics),
    rawMessagesJson: s.meta.rawMessagesJson
  };
}
var J0 = "LittleWhiteBox", CB = (() => {
  try {
    const e = new URL(import.meta.url).pathname.match(/\/scripts\/extensions\/third-party\/([^/]+)\//);
    return e?.[1] ? decodeURIComponent(e[1]) : J0;
  } catch {
    return J0;
  }
})(), Rg = `scripts/extensions/third-party/${CB}`, Y0 = 24, AB = 24, IB = 240, xB = /* @__PURE__ */ new Map([
  ["“", '"'],
  ["”", '"'],
  ["＂", '"'],
  ["「", '"'],
  ["」", '"'],
  ["『", '"'],
  ["』", '"'],
  ["‘", "'"],
  ["’", "'"],
  ["，", ","],
  ["。", "."],
  ["：", ":"],
  ["；", ";"],
  ["？", "?"],
  ["！", "!"],
  ["（", "("],
  ["）", ")"]
]);
function Pc(e = "") {
  return Array.from(String(e ?? ""), (t) => xB.get(t) || t).join("");
}
function Xv(e = "") {
  let t = "";
  const n = [];
  let r = 0;
  for (const s of String(e ?? "")) {
    const o = r;
    r += s.length, !/\s/u.test(s) && (t += Pc(s), n.push({
      start: o,
      end: r
    }));
  }
  return {
    compact: t,
    positions: n
  };
}
function _b(e = "", t = "") {
  if (!t) return [];
  const n = [];
  let r = 0;
  for (; r <= e.length; ) {
    const s = e.indexOf(t, r);
    if (s < 0) break;
    n.push({
      start: s,
      end: s + t.length,
      equivalent: !1
    }), r = s + Math.max(t.length, 1);
  }
  return n;
}
function kB(e = "", t = "") {
  const n = Pc(e), r = Pc(t);
  return !r || r === t && n === e ? [] : _b(n, r).map((s) => ({
    ...s,
    equivalent: !0
  }));
}
function MB(e = "", t = "") {
  const n = Xv(e), r = Xv(t);
  return r.compact.length < AB ? [] : !r.compact || r.compact === t && n.compact === e ? [] : _b(n.compact, r.compact).map((s) => {
    const o = n.positions[s.start], i = n.positions[s.end - 1];
    return !o || !i ? null : {
      start: o.start,
      end: i.end,
      equivalent: !0,
      flexibleWhitespace: !0
    };
  }).filter(Boolean);
}
function PB(e = "", t = 0) {
  let n = 1;
  const r = Math.max(0, Math.min(t, e.length));
  for (let s = 0; s < r; s += 1) e[s] === `
` && (n += 1);
  return n;
}
function RB(e = "", t = {}) {
  const n = Math.max(0, Number(t.start) - Y0), r = Math.min(e.length, Number(t.end) + Y0), s = n > 0 ? "..." : "", o = r < e.length ? "..." : "";
  return `${s}${e.slice(n, r).replace(/\s+/g, " ")}${o}`;
}
function al(e = [], t = 1, n = 8) {
  const r = e.slice(0, n), s = e.length > n ? `
...` : "";
  return `${r.map((o, i) => `${t + i}: ${o}`).join(`
`)}${s}`;
}
function Zd(e = "", t = IB) {
  const n = String(e ?? "").replace(/\r\n/g, `
`);
  return n.length > t ? `${n.slice(0, t)}...` : n;
}
function NB(e = "", t = []) {
  return t.slice(0, 8).map((n) => ({
    line: PB(e, n.start),
    context: RB(e, n)
  }));
}
function jd(e = "", t = /"/g, n = "“", r = "”") {
  let s = !0;
  return e.replace(t, () => {
    const o = s ? n : r;
    return s = !s, o;
  });
}
function $B(e = "", t = "") {
  let n = String(e ?? "");
  return /[“”]/.test(t) ? n = jd(n, /"/g, "“", "”") : /[「」]/.test(t) ? n = jd(n, /"/g, "「", "」") : /[『』]/.test(t) && (n = jd(n, /"/g, "『", "』")), /[‘’]/.test(t) && (n = jd(n, /'/g, "‘", "’")), t.includes("，") && (n = n.replaceAll(",", "，")), t.includes("。") && (n = n.replaceAll(".", "。")), t.includes("：") && (n = n.replaceAll(":", "：")), t.includes("；") && (n = n.replaceAll(";", "；")), t.includes("？") && (n = n.replaceAll("?", "？")), t.includes("！") && (n = n.replaceAll("!", "！")), t.includes("（") && (n = n.replaceAll("(", "（")), t.includes("）") && (n = n.replaceAll(")", "）")), n;
}
function Ph(e = "", t = "") {
  const n = _b(e, t);
  if (n.length) return n;
  const r = kB(e, t);
  return r.length ? r : MB(e, t);
}
function DB(e = "") {
  return Xv(e).compact.length;
}
function Qv(e = "", t = 8) {
  return DB(e) >= t;
}
function Ng(e = "", t = "") {
  return t ? Ph(e, t).length > 0 : !1;
}
function LB(e = {}, t = []) {
  const n = typeof e.oldString == "string" ? e.oldString : String(e.oldString ?? ""), r = typeof e.newString == "string" ? e.newString : String(e.newString ?? "");
  return t.some((s = {}) => {
    const o = String(s.matchedText ?? ""), i = String(s.replacement ?? "");
    return o === n && i === r ? !0 : !Qv(n, 8) || r !== "" && !Qv(r, 8) || !Ng(o, n) || Ng(i, n) ? !1 : r === "" || Ng(i, r);
  });
}
function OB(e = "", t = "", n = "") {
  if (!n || !Qv(n, 16)) return {};
  const r = t ? Ph(e, t).length : 0, s = Ph(e, n).length;
  return r !== 0 || s <= 0 ? {} : {
    uncertain: !0,
    possibleAlreadyApplied: !0,
    newStringMatches: s
  };
}
function FB(e = "", t = [], n = "") {
  let r = e;
  const s = [], o = [];
  return t.slice().sort((i, a) => a.start - i.start).forEach((i) => {
    const a = e.slice(i.start, i.end), l = i.equivalent ? $B(n, a) : n;
    s.push(l), o.push({
      matchedText: a,
      replacement: l
    }), r = `${r.slice(0, i.start)}${l}${r.slice(i.end)}`;
  }), {
    content: r,
    replacements: s,
    replacementDetails: o,
    matchedBy: t.some((i) => i.flexibleWhitespace) ? "flexible_whitespace" : t.some((i) => i.equivalent) ? "punctuation_equivalent" : "exact"
  };
}
function BB(e = "", t = "", n = {}) {
  return {
    ok: !1,
    error: e,
    message: t,
    ...n
  };
}
function Yt(e = "", t = "", n = "", r = {}) {
  return BB(e, t, n ? {
    suggestion: n,
    ...r
  } : r);
}
var UB = /* @__PURE__ */ new Set([
  "oldString",
  "newString",
  "startLine",
  "endLine",
  "insertAtLine",
  "replaceAll"
]);
function ef(e = [], t = "") {
  for (let n = e.length - 1; n >= 0; n -= 1) if (e[n]?.[1] === t) return e[n];
  return null;
}
function qB(e = [], t = 0) {
  const n = ef(e, "startLine"), r = ef(e, "endLine"), s = ef(e, "insertAtLine"), o = ef(e, "oldString"), i = [];
  return n && r ? i.push(n, r) : s ? i.push(s) : o && i.push(o), i.filter((a) => a.index > t).sort((a, l) => a.index - l.index)[0] || null;
}
function HB(e = "") {
  let t = String(e ?? "").trim();
  return t.endsWith(",") && (t = t.slice(0, -1).trimEnd()), t.startsWith('"') && (t = t.slice(1)), t.endsWith('"') && (t = t.slice(0, -1)), t.replace(/\\r/g, "\r").replace(/\\n/g, `
`).replace(/\\t/g, "	").replace(/\\"/g, '"').replace(/\\\\/g, "\\");
}
function GB(e = "") {
  const t = String(e || "").trim().replace(/^\{/, "").replace(/\}$/, ""), n = [...t.matchAll(/"?([A-Za-z][A-Za-z0-9_]*)"?\s*:/g)].filter((i) => UB.has(i[1]));
  if (!n.length) return null;
  const r = n.filter((i) => i[1] !== "newString"), s = r[r.length - 1] || null, o = {};
  for (let i = 0; i < n.length; i += 1) {
    const a = n[i], l = a[1], u = a.index + a[0].length, d = Nh(o) || $h(o) || Dh(o), h = l === "newString" ? !d && s && s.index > a.index ? qB(n, a.index) : null : n[i + 1];
    let p = h ? h.index : t.length;
    if (h) {
      const w = t.lastIndexOf(",", h.index);
      w >= u && (p = w);
    }
    const y = t.slice(u, p).trim();
    if (l === "startLine" || l === "endLine" || l === "insertAtLine") {
      const w = y.match(/-?\d+/);
      w && (o[l] = Number(w[0]));
      continue;
    }
    if (l === "replaceAll") {
      o[l] = /^true\b/i.test(y);
      continue;
    }
    if (o[l] = HB(y), l === "newString" && !h) break;
  }
  return (Nh(o) || $h(o) || Dh(o)) && Object.hasOwn(o, "newString") ? o : null;
}
function WB(e) {
  return e && typeof e == "object" && !Array.isArray(e) && Object.hasOwn(e, "newString") && (Nh(e) || $h(e) || Dh(e));
}
function X0(e = "") {
  return /"?newString"?\s*:/i.test(e) && /"?(?:oldString|startLine|endLine|insertAtLine)"?\s*:/i.test(e);
}
function VB(e = "") {
  const t = String(e || "").trim();
  if (!t.startsWith("[") || !t.endsWith("]")) return [];
  const n = t.slice(1, -1).trim();
  if (!n) return [];
  const r = [...n.matchAll(/\}\s*,\s*\{(?=\s*"?(?:oldString|newString|startLine|endLine|insertAtLine|replaceAll)"?\s*:)/g)].map((i) => ({
    index: i.index,
    splitEnd: i.index + 1,
    nextStart: i.index + i[0].lastIndexOf("{")
  })), s = [];
  let o = 0;
  return r.forEach((i, a) => {
    const l = r[a + 1], u = n.slice(o, i.splitEnd), d = n.slice(i.nextStart, l?.splitEnd ?? n.length);
    !X0(u) || !X0(d) || (s.push(u), o = i.nextStart);
  }), s.push(n.slice(o)), s.map((i) => i.trim()).filter(Boolean);
}
function KB(e = "") {
  const t = VB(e);
  if (!t.length) return null;
  const n = t.map((r) => {
    try {
      const s = JSON.parse(r);
      return WB(s) ? s : null;
    } catch {
      return GB(r);
    }
  });
  return n.every((r) => r && typeof r == "object") ? n : null;
}
function Rh(e = {}, t = "") {
  if (!Object.hasOwn(e, t)) return !1;
  const n = e[t];
  return n != null && n !== "";
}
function Nh(e = {}) {
  return Rh(e, "oldString");
}
function $h(e = {}) {
  return Rh(e, "startLine") || Rh(e, "endLine");
}
function Dh(e = {}) {
  return Rh(e, "insertAtLine");
}
function io(e) {
  const t = Number(e);
  return Number.isInteger(t) && t > 0 ? t : 0;
}
function Hx(e = {}) {
  const t = io(e.startLine), n = io(e.endLine);
  return t && n ? "line_range" : io(e.insertAtLine) ? "line_insert" : Nh(e) ? "old_string" : $h(e) ? "line_range" : Dh(e) ? "line_insert" : "old_string";
}
function tf(e = {}, t = "") {
  const n = typeof e.newString == "string" ? e.newString : String(e.newString ?? "");
  return t === "line_range" ? {
    startLine: e.startLine,
    endLine: e.endLine,
    newString: n
  } : t === "line_insert" ? {
    insertAtLine: e.insertAtLine,
    newString: n
  } : {
    oldString: typeof e.oldString == "string" ? e.oldString : String(e.oldString ?? ""),
    newString: n,
    replaceAll: !!e.replaceAll
  };
}
function zB(e = {}, t = "") {
  return t === "line_range" ? {
    mode: t,
    startLine: e.startLine,
    endLine: e.endLine,
    newPreview: Zd(e.newString)
  } : t === "line_insert" ? {
    mode: t,
    insertAtLine: e.insertAtLine,
    newPreview: Zd(e.newString)
  } : {
    mode: t,
    oldPreview: Zd(e.oldString),
    newPreview: Zd(e.newString),
    replaceAll: !!e.replaceAll || void 0
  };
}
function Rc(e = [], t = [], n = []) {
  return e.map((r, s) => {
    if (!r || typeof r != "object") return r;
    const o = n[s] || Hx(t[s] || {});
    if (r.ok) return Object.hasOwn(r, "index") ? r : {
      ...r,
      index: s
    };
    const i = Object.hasOwn(r, "index") ? r.index : s;
    return {
      ...zB(t[s] || {}, o),
      ...r,
      index: i
    };
  });
}
function Up(e = "") {
  return String(e ?? "").replace(/\r\n/g, `
`).split(`
`);
}
function Lh(e = "") {
  if (e === "") return [];
  const t = String(e ?? "").replace(/\r\n/g, `
`).split(`
`);
  t.length > 1 && t[t.length - 1] === "" && t.pop();
  const n = t.filter((r) => r.trim());
  return n.length > 0 && n.every((r) => /^\s*\d+:\s?/.test(r)) ? t.map((r) => r.trim() ? r.replace(/^\s*\d+:\s?/, "") : r) : t;
}
function JB(e = "", t = []) {
  const n = Up(e), r = new Array(t.length), s = t.map((u = {}, d) => {
    const h = io(u.startLine), p = io(u.endLine), y = typeof u.newString == "string" ? u.newString : String(u.newString ?? "");
    return {
      edit: u,
      index: d,
      startLine: h,
      endLine: p,
      newString: y,
      replacementLines: Lh(y)
    };
  });
  s.forEach((u) => {
    if (!u.startLine || !u.endLine) {
      r[u.index] = Yt("invalid_line_range", "startLine and endLine must be positive integers", "Use line numbers from the latest Read result, with startLine <= endLine.");
      return;
    }
    if (u.endLine < u.startLine) {
      r[u.index] = Yt("invalid_line_range", "endLine must be greater than or equal to startLine", "Use an inclusive line range from the latest Read result.");
      return;
    }
    u.endLine > n.length && (r[u.index] = Yt("line_range_out_of_bounds", `Line range ${u.startLine}-${u.endLine} is outside the file`, `Read the current file again. This file has ${n.length} lines.`));
  });
  const o = s.slice().sort((u, d) => u.startLine - d.startLine || u.endLine - d.endLine);
  o.forEach((u, d) => {
    if (r[u.index]) return;
    const h = o[d - 1];
    h && !r[h.index] && u.startLine <= h.endLine && (r[h.index] = Yt("overlapping_line_ranges", `Line range ${h.startLine}-${h.endLine} overlaps ${u.startLine}-${u.endLine}`, "Merge overlapping line edits into one larger startLine/endLine replacement."), r[u.index] = Yt("overlapping_line_ranges", `Line range ${u.startLine}-${u.endLine} overlaps ${h.startLine}-${h.endLine}`, "Merge overlapping line edits into one larger startLine/endLine replacement."));
  });
  const i = n.slice();
  s.slice().sort((u, d) => d.startLine - u.startLine || d.endLine - u.endLine).forEach((u) => {
    if (r[u.index]) return;
    const d = n.slice(u.startLine - 1, u.endLine);
    i.splice(u.startLine - 1, u.endLine - u.startLine + 1, ...u.replacementLines), r[u.index] = {
      ok: !0,
      index: u.index,
      startLine: u.startLine,
      endLine: u.endLine,
      replacements: 1,
      matchedBy: "line_range",
      oldPreview: al(d, u.startLine),
      newPreview: al(u.replacementLines, u.startLine)
    };
  });
  const a = r.filter((u) => u && !u.ok).length, l = r.filter((u) => u && u.ok).length;
  return {
    ok: a === 0,
    partial: l > 0 && a > 0 ? !0 : void 0,
    content: i.join(`
`),
    results: Rc(r, t, t.map(() => "line_range"))
  };
}
function Gx(e = "") {
  return e === "" ? 0 : Up(e).length;
}
function YB(e = "", t = []) {
  const n = e === "" ? [] : Up(e), r = Gx(e), s = new Array(t.length), o = t.map((u = {}, d) => {
    const h = io(u.insertAtLine), p = typeof u.newString == "string" ? u.newString : String(u.newString ?? "");
    return {
      edit: u,
      index: d,
      insertAtLine: h,
      newString: p,
      insertionLines: Lh(p)
    };
  });
  o.forEach((u) => {
    if (!u.insertAtLine) {
      s[u.index] = Yt("invalid_insert_line", "insertAtLine must be a positive integer", "Use a line number from the latest Read result. insertAtLine inserts before that line; totalLines + 1 appends to the end.");
      return;
    }
    if (u.insertAtLine > r + 1) {
      s[u.index] = Yt("insert_line_out_of_bounds", `Insert line ${u.insertAtLine} is outside the file`, `Read the current file again. This file has ${r} lines, so valid insertAtLine values are 1-${r + 1}.`);
      return;
    }
    u.newString || (s[u.index] = Yt("no_changes", "No changes to make", "Provide non-empty newString text to insert."));
  });
  const i = n.slice();
  o.slice().sort((u, d) => d.insertAtLine - u.insertAtLine || d.index - u.index).forEach((u) => {
    s[u.index] || (i.splice(u.insertAtLine - 1, 0, ...u.insertionLines), s[u.index] = {
      ok: !0,
      index: u.index,
      insertAtLine: u.insertAtLine,
      replacements: 0,
      matchedBy: "line_insert",
      newPreview: al(u.insertionLines, u.insertAtLine)
    });
  });
  const a = s.filter((u) => u && !u.ok).length, l = s.filter((u) => u && u.ok).length;
  return {
    ok: a === 0,
    partial: l > 0 && a > 0 ? !0 : void 0,
    content: i.join(`
`),
    results: Rc(s, t, t.map(() => "line_insert"))
  };
}
function XB(e = "", t = [], n = []) {
  const r = n.some((p) => p === "line_range"), s = e === "" && !r ? [] : Up(e), o = Gx(e), i = new Array(t.length), a = t.map((p = {}, y) => {
    const w = n[y];
    if (w === "line_insert") {
      const k = io(p.insertAtLine), R = typeof p.newString == "string" ? p.newString : String(p.newString ?? "");
      return {
        mode: w,
        edit: p,
        index: y,
        insertAtLine: k,
        newString: R,
        insertionLines: Lh(R)
      };
    }
    const E = io(p.startLine), C = io(p.endLine), M = typeof p.newString == "string" ? p.newString : String(p.newString ?? "");
    return {
      mode: w,
      edit: p,
      index: y,
      startLine: E,
      endLine: C,
      newString: M,
      replacementLines: Lh(M)
    };
  });
  a.forEach((p) => {
    if (p.mode === "line_insert") {
      if (!p.insertAtLine) {
        i[p.index] = Yt("invalid_insert_line", "insertAtLine must be a positive integer", "Use a line number from the latest Read result. insertAtLine inserts before that line; totalLines + 1 appends to the end.");
        return;
      }
      if (p.insertAtLine > o + 1) {
        i[p.index] = Yt("insert_line_out_of_bounds", `Insert line ${p.insertAtLine} is outside the file`, `Read the current file again. This file has ${o} lines, so valid insertAtLine values are 1-${o + 1}.`);
        return;
      }
      p.newString || (i[p.index] = Yt("no_changes", "No changes to make", "Provide non-empty newString text to insert."));
      return;
    }
    if (!p.startLine || !p.endLine) {
      i[p.index] = Yt("invalid_line_range", "startLine and endLine must be positive integers", "Use line numbers from the latest Read result, with startLine <= endLine.");
      return;
    }
    if (p.endLine < p.startLine) {
      i[p.index] = Yt("invalid_line_range", "endLine must be greater than or equal to startLine", "Use an inclusive line range from the latest Read result.");
      return;
    }
    p.endLine > s.length && (i[p.index] = Yt("line_range_out_of_bounds", `Line range ${p.startLine}-${p.endLine} is outside the file`, `Read the current file again. This file has ${s.length} lines.`));
  });
  const l = a.filter((p) => p.mode === "line_range").slice().sort((p, y) => p.startLine - y.startLine || p.endLine - y.endLine);
  l.forEach((p, y) => {
    if (i[p.index]) return;
    const w = l[y - 1];
    w && !i[w.index] && p.startLine <= w.endLine && (i[w.index] = Yt("overlapping_line_ranges", `Line range ${w.startLine}-${w.endLine} overlaps ${p.startLine}-${p.endLine}`, "Merge overlapping line edits into one larger startLine/endLine replacement."), i[p.index] = Yt("overlapping_line_ranges", `Line range ${p.startLine}-${p.endLine} overlaps ${w.startLine}-${w.endLine}`, "Merge overlapping line edits into one larger startLine/endLine replacement."));
  }), a.filter((p) => p.mode === "line_insert").forEach((p) => {
    if (i[p.index]) return;
    const y = l.find((E) => !i[E.index] && E.startLine < p.insertAtLine && p.insertAtLine <= E.endLine);
    if (!y) return;
    const w = Yt("insert_inside_line_range", `Insert line ${p.insertAtLine} falls inside replaced line range ${y.startLine}-${y.endLine}`, "Merge the insertion into that startLine/endLine replacement, or insert at the range start/end boundary.");
    i[p.index] = w, i[y.index] = w;
  });
  const u = s.slice();
  a.slice().sort((p, y) => {
    const w = p.mode === "line_insert" ? p.insertAtLine : p.startLine, E = y.mode === "line_insert" ? y.insertAtLine : y.startLine;
    return w !== E ? E - w : p.mode !== y.mode ? p.mode === "line_range" ? -1 : 1 : p.mode === "line_insert" ? y.index - p.index : y.endLine - p.endLine || y.index - p.index;
  }).forEach((p) => {
    if (i[p.index]) return;
    if (p.mode === "line_insert") {
      u.splice(p.insertAtLine - 1, 0, ...p.insertionLines), i[p.index] = {
        ok: !0,
        index: p.index,
        insertAtLine: p.insertAtLine,
        replacements: 0,
        matchedBy: "line_insert",
        newPreview: al(p.insertionLines, p.insertAtLine)
      };
      return;
    }
    const y = s.slice(p.startLine - 1, p.endLine);
    u.splice(p.startLine - 1, p.endLine - p.startLine + 1, ...p.replacementLines), i[p.index] = {
      ok: !0,
      index: p.index,
      startLine: p.startLine,
      endLine: p.endLine,
      replacements: 1,
      matchedBy: "line_range",
      oldPreview: al(y, p.startLine),
      newPreview: al(p.replacementLines, p.startLine)
    };
  });
  const d = i.filter((p) => p && !p.ok).length, h = i.filter((p) => p && p.ok).length;
  return {
    ok: d === 0,
    partial: h > 0 && d > 0 ? !0 : void 0,
    content: u.join(`
`),
    results: Rc(i, t, n)
  };
}
function QB(e) {
  if (Array.isArray(e)) return {
    ok: !0,
    edits: e,
    parsedFromString: !1
  };
  if (typeof e == "string") {
    const t = e.trim();
    if (!t) return {
      ok: !1,
      error: "edits_must_be_array",
      message: "edits must be a JSON array, but received an empty string",
      suggestion: 'Pass edits as an array, not a quoted JSON string. Correct: "edits":[{"oldString":"old","newString":"new"}].'
    };
    try {
      const n = JSON.parse(t);
      return Array.isArray(n) ? {
        ok: !0,
        edits: n,
        parsedFromString: !0,
        warning: "提醒：模型把 edits 发成了字符串；插件已解析并继续执行。下次请把 edits 直接作为数组传入，不要作为字符串。"
      } : {
        ok: !1,
        error: "edits_must_be_array",
        message: `edits must be a JSON array, but the JSON string parsed to ${Array.isArray(n) ? "array" : typeof n}`,
        suggestion: 'Pass edits directly as an array. Correct: "edits":[{"startLine":10,"endLine":50,"newString":"..."}]. Wrong: "edits":"[{\\"startLine\\":10,...}]".'
      };
    } catch (n) {
      const r = KB(t);
      return r ? {
        ok: !0,
        edits: r,
        parsedFromString: !0,
        warning: "提醒：模型把 edits 发成了字符串，且内容不是标准 JSON；插件已尝试修复并继续执行。下次请把 edits 直接作为数组传入，不要作为字符串。"
      } : {
        ok: !1,
        error: "invalid_edits_json_string",
        message: `edits must be a JSON array, but received a string that is not valid JSON: ${n?.message || "parse failed"}`,
        suggestion: 'Do not JSON-stringify edits. Pass an array value: "edits":[{"oldString":"old","newString":"new"}].'
      };
    }
  }
  return e == null ? {
    ok: !1,
    error: "missing_edits_array",
    message: "edits is required and must be a JSON array, but it was missing",
    suggestion: 'Include an edits array with at least one item. Correct: "edits":[{"startLine":10,"endLine":50,"newString":"..."}].'
  } : {
    ok: !1,
    error: "edits_must_be_array",
    message: `edits must be a JSON array, but received ${typeof e}`,
    suggestion: 'Pass edits as an array, not an object/string. Correct: "edits":[{"oldString":"old","newString":"new"}].'
  };
}
function ZB(e = "", t) {
  let n = String(e ?? "");
  const r = QB(t);
  if (!r.ok) return {
    ok: !1,
    content: n,
    results: [Yt(r.error, r.message, r.suggestion)]
  };
  const s = r.edits, o = [], i = [], a = [], l = [];
  let u = 0;
  if (!s.length) return {
    ok: !1,
    content: n,
    results: [Yt("invalid_edits", "No edits provided", "Provide a non-empty edits array. For same-file multi-spot changes, put all replacements in this one Edit call.")]
  };
  const d = s.map((M) => Hx(M)), h = d.filter((M) => M === "line_range").length, p = d.filter((M) => M === "line_insert").length, y = d.filter((M) => M === "old_string").length;
  if (h + p && y) return {
    ok: !1,
    content: n,
    results: Rc(s.map(() => Yt("mixed_edit_modes", "Do not mix Edit modes in one Edit call", "Use separate Edit calls for oldString replacements, line-range replacements, and insertAtLine insertions.")), s, d)
  };
  if (h && p) {
    const M = XB(n, s.map((k, R) => tf(k, d[R])), d);
    return r.warning && (M.warning = r.warning), M;
  }
  if (h) {
    const M = JB(n, s.map((k, R) => tf(k, d[R])));
    return r.warning && (M.warning = r.warning), M;
  }
  if (p) {
    const M = YB(n, s.map((k, R) => tf(k, d[R])));
    return r.warning && (M.warning = r.warning), M;
  }
  const w = s.map((M, k) => tf(M, d[k]));
  w.forEach((M = {}, k) => {
    l[k] = a.slice();
    const R = typeof M.oldString == "string" ? M.oldString : String(M.oldString ?? ""), D = typeof M.newString == "string" ? M.newString : String(M.newString ?? ""), m = !!M.replaceAll;
    if (R === D) {
      o.push(Yt("no_changes", "No changes to make", "Change newString or remove this edit item."));
      return;
    }
    const b = Pc(R);
    if (R && i.some((x) => x.includes(R) || Pc(x).includes(b))) {
      o.push(Yt("old_string_matches_previous_new_string", "old_string is a substring of a new_string from a previous edit.", "This edit may match text inserted earlier in the same Edit call. Merge overlapping changes into one larger replacement, or read the updated file and run a later Edit."));
      return;
    }
    if (!R) {
      o.push(Yt("empty_old_string", "oldString is empty; provide text to replace", "Use Write to create files. For Edit, provide the exact current fragment to replace, or use Write for a full rewrite."));
      return;
    }
    const v = Ph(n, R);
    if (!v.length) {
      const x = OB(n, R, D);
      o.push(Yt("not_found", x.possibleAlreadyApplied ? "String to replace not found, but the requested newString already exists in the file" : "String to replace not found in file", x.possibleAlreadyApplied ? "Read the current file and verify whether the intended location is already correct. Edit will not mark this as successful without a same-call replacement proof." : "Edit already retries long oldString values with common punctuation and whitespace differences ignored. If it still fails, Read the current file and copy the exact current text into oldString. If this overlaps another same-file edit, merge them into one larger replacement; for very large rewrites, use Write.", x));
      return;
    }
    if (v.length > 1 && !m) {
      o.push(Yt("multiple_matches", `找到 ${v.length} 处匹配，需要更多上下文或使用 replaceAll`, "Use the returned line contexts to expand oldString with unique surrounding text, or set replaceAll: true only if every match should change.", { matches: NB(n, v) }));
      return;
    }
    const T = m ? v : v.slice(0, 1), I = FB(n, T, D);
    n = I.content, i.push(...I.replacements), a.push(...I.replacementDetails), u += 1, o.push({
      ok: !0,
      index: k,
      replacements: T.length,
      matchedBy: I.matchedBy
    });
  }), o.forEach((M, k) => {
    !M || M.ok || ["not_found", "old_string_matches_previous_new_string"].includes(M.error) && LB(w[k], l[k] || []) && (o[k] = {
      ok: !0,
      index: k,
      replacements: 0,
      matchedBy: "already_satisfied",
      satisfied: !0,
      previousError: M.error,
      message: "Desired edit is already present in the file"
    });
  });
  const E = o.filter((M) => !M.ok).length, C = o.filter((M) => M?.satisfied).length;
  return {
    ok: E === 0,
    partial: (u > 0 || C > 0) && E > 0 ? !0 : void 0,
    content: n,
    results: Rc(o, w, d),
    warning: r.warning
  };
}
var Zv = function(e, t) {
  return Zv = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(n, r) {
    n.__proto__ = r;
  } || function(n, r) {
    for (var s in r) Object.prototype.hasOwnProperty.call(r, s) && (n[s] = r[s]);
  }, Zv(e, t);
};
function jB(e, t) {
  if (typeof t != "function" && t !== null) throw new TypeError("Class extends value " + String(t) + " is not a constructor or null");
  Zv(e, t);
  function n() {
    this.constructor = e;
  }
  e.prototype = t === null ? Object.create(t) : (n.prototype = t.prototype, new n());
}
var wt = function() {
  return wt = Object.assign || function(t) {
    for (var n, r = 1, s = arguments.length; r < s; r++) {
      n = arguments[r];
      for (var o in n) Object.prototype.hasOwnProperty.call(n, o) && (t[o] = n[o]);
    }
    return t;
  }, wt.apply(this, arguments);
};
function Oh(e, t, n) {
  if (n || arguments.length === 2)
    for (var r = 0, s = t.length, o; r < s; r++) (o || !(r in t)) && (o || (o = Array.prototype.slice.call(t, 0, r)), o[r] = t[r]);
  return e.concat(o || Array.prototype.slice.call(t));
}
var Mn = typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : typeof window < "u" ? window : globalThis, Sn = Object.keys, en = Array.isArray;
typeof Promise < "u" && !Mn.Promise && (Mn.Promise = Promise);
function Dr(e, t) {
  return typeof t != "object" || Sn(t).forEach(function(n) {
    e[n] = t[n];
  }), e;
}
var wl = Object.getPrototypeOf, eU = {}.hasOwnProperty;
function ir(e, t) {
  return eU.call(e, t);
}
function Sl(e, t) {
  typeof t == "function" && (t = t(wl(e))), (typeof Reflect > "u" ? Sn : Reflect.ownKeys)(t).forEach(function(n) {
    jo(e, n, t[n]);
  });
}
var Wx = Object.defineProperty;
function jo(e, t, n, r) {
  Wx(e, t, Dr(n && ir(n, "get") && typeof n.get == "function" ? {
    get: n.get,
    set: n.set,
    configurable: !0
  } : {
    value: n,
    configurable: !0,
    writable: !0
  }, r));
}
function Pl(e) {
  return { from: function(t) {
    return e.prototype = Object.create(t.prototype), jo(e.prototype, "constructor", e), { extend: Sl.bind(null, e.prototype) };
  } };
}
var tU = Object.getOwnPropertyDescriptor;
function Vx(e, t) {
  var n = tU(e, t), r;
  return n || (r = wl(e)) && Vx(r, t);
}
var nU = [].slice;
function qp(e, t, n) {
  return nU.call(e, t, n);
}
function Kx(e, t) {
  return t(e);
}
function Fu(e) {
  if (!e) throw new Error("Assertion Failed");
}
function zx(e) {
  Mn.setImmediate ? setImmediate(e) : setTimeout(e, 0);
}
function rU(e, t) {
  return e.reduce(function(n, r, s) {
    var o = t(r, s);
    return o && (n[o[0]] = o[1]), n;
  }, {});
}
function ao(e, t) {
  if (typeof t == "string" && ir(e, t)) return e[t];
  if (!t) return e;
  if (typeof t != "string") {
    for (var n = [], r = 0, s = t.length; r < s; ++r) {
      var o = ao(e, t[r]);
      n.push(o);
    }
    return n;
  }
  var i = t.indexOf(".");
  if (i !== -1) {
    var a = e[t.substr(0, i)];
    return a == null ? void 0 : ao(a, t.substr(i + 1));
  }
}
function Pr(e, t, n) {
  if (!(!e || t === void 0) && !("isFrozen" in Object && Object.isFrozen(e)))
    if (typeof t != "string" && "length" in t) {
      Fu(typeof n != "string" && "length" in n);
      for (var r = 0, s = t.length; r < s; ++r) Pr(e, t[r], n[r]);
    } else {
      var o = t.indexOf(".");
      if (o !== -1) {
        var i = t.substr(0, o), a = t.substr(o + 1);
        if (a === "") n === void 0 ? en(e) && !isNaN(parseInt(i)) ? e.splice(i, 1) : delete e[i] : e[i] = n;
        else {
          var l = e[i];
          (!l || !ir(e, i)) && (l = e[i] = {}), Pr(l, a, n);
        }
      } else n === void 0 ? en(e) && !isNaN(parseInt(t)) ? e.splice(t, 1) : delete e[t] : e[t] = n;
    }
}
function sU(e, t) {
  typeof t == "string" ? Pr(e, t, void 0) : "length" in t && [].map.call(t, function(n) {
    Pr(e, n, void 0);
  });
}
function Jx(e) {
  var t = {};
  for (var n in e) ir(e, n) && (t[n] = e[n]);
  return t;
}
var oU = [].concat;
function Yx(e) {
  return oU.apply([], e);
}
var iU = "BigUint64Array,BigInt64Array,Array,Boolean,String,Date,RegExp,Blob,File,FileList,FileSystemFileHandle,FileSystemDirectoryHandle,ArrayBuffer,DataView,Uint8ClampedArray,ImageBitmap,ImageData,Map,Set,CryptoKey".split(",").concat(Yx([
  8,
  16,
  32,
  64
].map(function(e) {
  return [
    "Int",
    "Uint",
    "Float"
  ].map(function(t) {
    return t + e + "Array";
  });
}))).filter(function(e) {
  return Mn[e];
}), Xx = new Set(iU.map(function(e) {
  return Mn[e];
}));
function Qx(e) {
  var t = {};
  for (var n in e) if (ir(e, n)) {
    var r = e[n];
    t[n] = !r || typeof r != "object" || Xx.has(r.constructor) ? r : Qx(r);
  }
  return t;
}
function aU(e) {
  for (var t in e) if (ir(e, t)) return !1;
  return !0;
}
var fc = null;
function ji(e) {
  fc = /* @__PURE__ */ new WeakMap();
  var t = jv(e);
  return fc = null, t;
}
function jv(e) {
  if (!e || typeof e != "object") return e;
  var t = fc.get(e);
  if (t) return t;
  if (en(e)) {
    t = [], fc.set(e, t);
    for (var n = 0, r = e.length; n < r; ++n) t.push(jv(e[n]));
  } else if (Xx.has(e.constructor)) t = e;
  else {
    var s = wl(e);
    t = s === Object.prototype ? {} : Object.create(s), fc.set(e, t);
    for (var o in e) ir(e, o) && (t[o] = jv(e[o]));
  }
  return t;
}
var lU = {}.toString;
function ey(e) {
  return lU.call(e).slice(8, -1);
}
var ty = typeof Symbol < "u" ? Symbol.iterator : "@@iterator", uU = typeof ty == "symbol" ? function(e) {
  var t;
  return e != null && (t = e[ty]) && t.apply(e);
} : function() {
  return null;
};
function Mi(e, t) {
  var n = e.indexOf(t);
  return n >= 0 && e.splice(n, 1), n >= 0;
}
var Ha = {};
function ro(e) {
  var t, n, r, s;
  if (arguments.length === 1) {
    if (en(e)) return e.slice();
    if (this === Ha && typeof e == "string") return [e];
    if (s = uU(e)) {
      for (n = []; r = s.next(), !r.done; ) n.push(r.value);
      return n;
    }
    if (e == null) return [e];
    if (t = e.length, typeof t == "number") {
      for (n = new Array(t); t--; ) n[t] = e[t];
      return n;
    }
    return [e];
  }
  for (t = arguments.length, n = new Array(t); t--; ) n[t] = arguments[t];
  return n;
}
var bb = typeof Symbol < "u" ? function(e) {
  return e[Symbol.toStringTag] === "AsyncFunction";
} : function() {
  return !1;
}, cU = [
  "Modify",
  "Bulk",
  "OpenFailed",
  "VersionChange",
  "Schema",
  "Upgrade",
  "InvalidTable",
  "MissingAPI",
  "NoSuchDatabase",
  "InvalidArgument",
  "SubTransaction",
  "Unsupported",
  "Internal",
  "DatabaseClosed",
  "PrematureCommit",
  "ForeignAwait"
], Zx = [
  "Unknown",
  "Constraint",
  "Data",
  "TransactionInactive",
  "ReadOnly",
  "Version",
  "NotFound",
  "InvalidState",
  "InvalidAccess",
  "Abort",
  "Timeout",
  "QuotaExceeded",
  "Syntax",
  "DataClone"
], wb = cU.concat(Zx), dU = {
  VersionChanged: "Database version changed by other database connection",
  DatabaseClosed: "Database has been closed",
  Abort: "Transaction aborted",
  TransactionInactive: "Transaction has already completed or failed",
  MissingAPI: "IndexedDB API missing. Please visit https://tinyurl.com/y2uuvskb"
};
function Rl(e, t) {
  this.name = e, this.message = t;
}
Pl(Rl).from(Error).extend({ toString: function() {
  return this.name + ": " + this.message;
} });
function jx(e, t) {
  return e + ". Errors: " + Object.keys(t).map(function(n) {
    return t[n].toString();
  }).filter(function(n, r, s) {
    return s.indexOf(n) === r;
  }).join(`
`);
}
function Fh(e, t, n, r) {
  this.failures = t, this.failedKeys = r, this.successCount = n, this.message = jx(e, t);
}
Pl(Fh).from(Rl);
function Xa(e, t) {
  this.name = "BulkError", this.failures = Object.keys(t).map(function(n) {
    return t[n];
  }), this.failuresByPos = t, this.message = jx(e, this.failures);
}
Pl(Xa).from(Rl);
var Sb = wb.reduce(function(e, t) {
  return e[t] = t + "Error", e;
}, {}), fU = Rl, lt = wb.reduce(function(e, t) {
  var n = t + "Error";
  function r(s, o) {
    this.name = n, s ? typeof s == "string" ? (this.message = "".concat(s).concat(o ? `
 ` + o : ""), this.inner = o || null) : typeof s == "object" && (this.message = "".concat(s.name, " ").concat(s.message), this.inner = s) : (this.message = dU[t] || n, this.inner = null);
  }
  return Pl(r).from(fU), e[t] = r, e;
}, {});
lt.Syntax = SyntaxError;
lt.Type = TypeError;
lt.Range = RangeError;
var Q0 = Zx.reduce(function(e, t) {
  return e[t + "Error"] = lt[t], e;
}, {});
function hU(e, t) {
  if (!e || e instanceof Rl || e instanceof TypeError || e instanceof SyntaxError || !e.name || !Q0[e.name]) return e;
  var n = new Q0[e.name](t || e.message, e);
  return "stack" in e && jo(n, "stack", { get: function() {
    return this.inner.stack;
  } }), n;
}
var Hp = wb.reduce(function(e, t) {
  return [
    "Syntax",
    "Type",
    "Range"
  ].indexOf(t) === -1 && (e[t + "Error"] = lt[t]), e;
}, {});
Hp.ModifyError = Fh;
Hp.DexieError = Rl;
Hp.BulkError = Xa;
function Ht() {
}
function nd(e) {
  return e;
}
function pU(e, t) {
  return e == null || e === nd ? t : function(n) {
    return t(e(n));
  };
}
function ea(e, t) {
  return function() {
    e.apply(this, arguments), t.apply(this, arguments);
  };
}
function mU(e, t) {
  return e === Ht ? t : function() {
    var n = e.apply(this, arguments);
    n !== void 0 && (arguments[0] = n);
    var r = this.onsuccess, s = this.onerror;
    this.onsuccess = null, this.onerror = null;
    var o = t.apply(this, arguments);
    return r && (this.onsuccess = this.onsuccess ? ea(r, this.onsuccess) : r), s && (this.onerror = this.onerror ? ea(s, this.onerror) : s), o !== void 0 ? o : n;
  };
}
function gU(e, t) {
  return e === Ht ? t : function() {
    e.apply(this, arguments);
    var n = this.onsuccess, r = this.onerror;
    this.onsuccess = this.onerror = null, t.apply(this, arguments), n && (this.onsuccess = this.onsuccess ? ea(n, this.onsuccess) : n), r && (this.onerror = this.onerror ? ea(r, this.onerror) : r);
  };
}
function vU(e, t) {
  return e === Ht ? t : function(n) {
    var r = e.apply(this, arguments);
    Dr(n, r);
    var s = this.onsuccess, o = this.onerror;
    this.onsuccess = null, this.onerror = null;
    var i = t.apply(this, arguments);
    return s && (this.onsuccess = this.onsuccess ? ea(s, this.onsuccess) : s), o && (this.onerror = this.onerror ? ea(o, this.onerror) : o), r === void 0 ? i === void 0 ? void 0 : i : Dr(r, i);
  };
}
function yU(e, t) {
  return e === Ht ? t : function() {
    return t.apply(this, arguments) === !1 ? !1 : e.apply(this, arguments);
  };
}
function Tb(e, t) {
  return e === Ht ? t : function() {
    var n = e.apply(this, arguments);
    if (n && typeof n.then == "function") {
      for (var r = this, s = arguments.length, o = new Array(s); s--; ) o[s] = arguments[s];
      return n.then(function() {
        return t.apply(r, o);
      });
    }
    return t.apply(this, arguments);
  };
}
var $s = typeof location < "u" && /^(http|https):\/\/(localhost|127\.0\.0\.1)/.test(location.href);
function ek(e, t) {
  $s = e;
}
var Nc = {}, tk = 100, Eb = typeof Promise > "u" ? [] : (function() {
  var e = Promise.resolve();
  if (typeof crypto > "u" || !crypto.subtle) return [
    e,
    wl(e),
    e
  ];
  var t = crypto.subtle.digest("SHA-512", new Uint8Array([0]));
  return [
    t,
    wl(t),
    e
  ];
})(), Z0 = Eb[0], j0 = Eb[1], _U = Eb[2], bU = j0 && j0.then, Li = Z0 && Z0.constructor, Cb = !!_U;
function wU() {
  queueMicrotask(TU);
}
var $c = function(e, t) {
  Bu.push([e, t]), Bh && (wU(), Bh = !1);
}, ny = !0, Bh = !0, Yi = [], zf = [], ry = nd, Vo = {
  id: "global",
  global: !0,
  ref: 0,
  unhandleds: [],
  onunhandled: Ht,
  pgp: !1,
  env: {},
  finalize: Ht
}, rt = Vo, Bu = [], Xi = 0, Jf = [];
function We(e) {
  if (typeof this != "object") throw new TypeError("Promises must be constructed via new");
  this._listeners = [], this._lib = !1;
  var t = this._PSD = rt;
  if (typeof e != "function") {
    if (e !== Nc) throw new TypeError("Not a function");
    this._state = arguments[1], this._value = arguments[2], this._state === !1 && oy(this, this._value);
    return;
  }
  this._state = null, this._value = null, ++t.ref, rk(this, e);
}
var sy = {
  get: function() {
    var e = rt, t = Uh;
    function n(r, s) {
      var o = this, i = !e.global && (e !== rt || t !== Uh), a = i && !ti(), l = new We(function(u, d) {
        Ab(o, new nk(eT(r, e, i, a), eT(s, e, i, a), u, d, e));
      });
      return this._consoleTask && (l._consoleTask = this._consoleTask), l;
    }
    return n.prototype = Nc, n;
  },
  set: function(e) {
    jo(this, "then", e && e.prototype === Nc ? sy : {
      get: function() {
        return e;
      },
      set: sy.set
    });
  }
};
Sl(We.prototype, {
  then: sy,
  _then: function(e, t) {
    Ab(this, new nk(null, null, e, t, rt));
  },
  catch: function(e) {
    if (arguments.length === 1) return this.then(null, e);
    var t = arguments[0], n = arguments[1];
    return typeof t == "function" ? this.then(null, function(r) {
      return r instanceof t ? n(r) : Yf(r);
    }) : this.then(null, function(r) {
      return r && r.name === t ? n(r) : Yf(r);
    });
  },
  finally: function(e) {
    return this.then(function(t) {
      return We.resolve(e()).then(function() {
        return t;
      });
    }, function(t) {
      return We.resolve(e()).then(function() {
        return Yf(t);
      });
    });
  },
  timeout: function(e, t) {
    var n = this;
    return e < 1 / 0 ? new We(function(r, s) {
      var o = setTimeout(function() {
        return s(new lt.Timeout(t));
      }, e);
      n.then(r, s).finally(clearTimeout.bind(null, o));
    }) : this;
  }
});
typeof Symbol < "u" && Symbol.toStringTag && jo(We.prototype, Symbol.toStringTag, "Dexie.Promise");
Vo.env = ok();
function nk(e, t, n, r, s) {
  this.onFulfilled = typeof e == "function" ? e : null, this.onRejected = typeof t == "function" ? t : null, this.resolve = n, this.reject = r, this.psd = s;
}
Sl(We, {
  all: function() {
    var e = ro.apply(null, arguments).map(qh);
    return new We(function(t, n) {
      e.length === 0 && t([]);
      var r = e.length;
      e.forEach(function(s, o) {
        return We.resolve(s).then(function(i) {
          e[o] = i, --r || t(e);
        }, n);
      });
    });
  },
  resolve: function(e) {
    return e instanceof We ? e : e && typeof e.then == "function" ? new We(function(t, n) {
      e.then(t, n);
    }) : new We(Nc, !0, e);
  },
  reject: Yf,
  race: function() {
    var e = ro.apply(null, arguments).map(qh);
    return new We(function(t, n) {
      e.map(function(r) {
        return We.resolve(r).then(t, n);
      });
    });
  },
  PSD: {
    get: function() {
      return rt;
    },
    set: function(e) {
      return rt = e;
    }
  },
  totalEchoes: { get: function() {
    return Uh;
  } },
  newPSD: ei,
  usePSD: ta,
  scheduler: {
    get: function() {
      return $c;
    },
    set: function(e) {
      $c = e;
    }
  },
  rejectionMapper: {
    get: function() {
      return ry;
    },
    set: function(e) {
      ry = e;
    }
  },
  follow: function(e, t) {
    return new We(function(n, r) {
      return ei(function(s, o) {
        var i = rt;
        i.unhandleds = [], i.onunhandled = o, i.finalize = ea(function() {
          var a = this;
          EU(function() {
            a.unhandleds.length === 0 ? s() : o(a.unhandleds[0]);
          });
        }, i.finalize), e();
      }, t, n, r);
    });
  }
});
Li && (Li.allSettled && jo(We, "allSettled", function() {
  var e = ro.apply(null, arguments).map(qh);
  return new We(function(t) {
    e.length === 0 && t([]);
    var n = e.length, r = new Array(n);
    e.forEach(function(s, o) {
      return We.resolve(s).then(function(i) {
        return r[o] = {
          status: "fulfilled",
          value: i
        };
      }, function(i) {
        return r[o] = {
          status: "rejected",
          reason: i
        };
      }).then(function() {
        return --n || t(r);
      });
    });
  });
}), Li.any && typeof AggregateError < "u" && jo(We, "any", function() {
  var e = ro.apply(null, arguments).map(qh);
  return new We(function(t, n) {
    e.length === 0 && n(/* @__PURE__ */ new AggregateError([]));
    var r = e.length, s = new Array(r);
    e.forEach(function(o, i) {
      return We.resolve(o).then(function(a) {
        return t(a);
      }, function(a) {
        s[i] = a, --r || n(new AggregateError(s));
      });
    });
  });
}), Li.withResolvers && (We.withResolvers = Li.withResolvers));
function rk(e, t) {
  try {
    t(function(n) {
      if (e._state === null) {
        if (n === e) throw new TypeError("A promise cannot be resolved with itself.");
        var r = e._lib && Nl();
        n && typeof n.then == "function" ? rk(e, function(s, o) {
          n instanceof We ? n._then(s, o) : n.then(s, o);
        }) : (e._state = !0, e._value = n, sk(e)), r && $l();
      }
    }, oy.bind(null, e));
  } catch (n) {
    oy(e, n);
  }
}
function oy(e, t) {
  if (zf.push(t), e._state === null) {
    var n = e._lib && Nl();
    t = ry(t), e._state = !1, e._value = t, CU(e), sk(e), n && $l();
  }
}
function sk(e) {
  var t = e._listeners;
  e._listeners = [];
  for (var n = 0, r = t.length; n < r; ++n) Ab(e, t[n]);
  var s = e._PSD;
  --s.ref || s.finalize(), Xi === 0 && (++Xi, $c(function() {
    --Xi === 0 && Ib();
  }, []));
}
function Ab(e, t) {
  if (e._state === null) {
    e._listeners.push(t);
    return;
  }
  var n = e._state ? t.onFulfilled : t.onRejected;
  if (n === null) return (e._state ? t.resolve : t.reject)(e._value);
  ++t.psd.ref, ++Xi, $c(SU, [
    n,
    e,
    t
  ]);
}
function SU(e, t, n) {
  try {
    var r, s = t._value;
    !t._state && zf.length && (zf = []), r = $s && t._consoleTask ? t._consoleTask.run(function() {
      return e(s);
    }) : e(s), !t._state && zf.indexOf(s) === -1 && AU(t), n.resolve(r);
  } catch (o) {
    n.reject(o);
  } finally {
    --Xi === 0 && Ib(), --n.psd.ref || n.psd.finalize();
  }
}
function TU() {
  ta(Vo, function() {
    Nl() && $l();
  });
}
function Nl() {
  var e = ny;
  return ny = !1, Bh = !1, e;
}
function $l() {
  var e, t, n;
  do
    for (; Bu.length > 0; )
      for (e = Bu, Bu = [], n = e.length, t = 0; t < n; ++t) {
        var r = e[t];
        r[0].apply(null, r[1]);
      }
  while (Bu.length > 0);
  ny = !0, Bh = !0;
}
function Ib() {
  var e = Yi;
  Yi = [], e.forEach(function(r) {
    r._PSD.onunhandled.call(null, r._value, r);
  });
  for (var t = Jf.slice(0), n = t.length; n; ) t[--n]();
}
function EU(e) {
  function t() {
    e(), Jf.splice(Jf.indexOf(t), 1);
  }
  Jf.push(t), ++Xi, $c(function() {
    --Xi === 0 && Ib();
  }, []);
}
function CU(e) {
  Yi.some(function(t) {
    return t._value === e._value;
  }) || Yi.push(e);
}
function AU(e) {
  for (var t = Yi.length; t; ) if (Yi[--t]._value === e._value) {
    Yi.splice(t, 1);
    return;
  }
}
function Yf(e) {
  return new We(Nc, !1, e);
}
function Zt(e, t) {
  var n = rt;
  return function() {
    var r = Nl(), s = rt;
    try {
      return ni(n, !0), e.apply(this, arguments);
    } catch (o) {
      t && t(o);
    } finally {
      ni(s, !1), r && $l();
    }
  };
}
var _n = {
  awaits: 0,
  echoes: 0,
  id: 0
}, IU = 0, Xf = [], Qf = 0, Uh = 0, xU = 0;
function ei(e, t, n, r) {
  var s = rt, o = Object.create(s);
  o.parent = s, o.ref = 0, o.global = !1, o.id = ++xU, Vo.env, o.env = Cb ? {
    Promise: We,
    PromiseProp: {
      value: We,
      configurable: !0,
      writable: !0
    },
    all: We.all,
    race: We.race,
    allSettled: We.allSettled,
    any: We.any,
    resolve: We.resolve,
    reject: We.reject
  } : {}, t && Dr(o, t), ++s.ref, o.finalize = function() {
    --this.parent.ref || this.parent.finalize();
  };
  var i = ta(o, e, n, r);
  return o.ref === 0 && o.finalize(), i;
}
function Dl() {
  return _n.id || (_n.id = ++IU), ++_n.awaits, _n.echoes += tk, _n.id;
}
function ti() {
  return _n.awaits ? (--_n.awaits === 0 && (_n.id = 0), _n.echoes = _n.awaits * tk, !0) : !1;
}
("" + bU).indexOf("[native code]") === -1 && (Dl = ti = Ht);
function qh(e) {
  return _n.echoes && e && e.constructor === Li ? (Dl(), e.then(function(t) {
    return ti(), t;
  }, function(t) {
    return ti(), sn(t);
  })) : e;
}
function kU(e) {
  ++Uh, (!_n.echoes || --_n.echoes === 0) && (_n.echoes = _n.awaits = _n.id = 0), Xf.push(rt), ni(e, !0);
}
function MU() {
  var e = Xf[Xf.length - 1];
  Xf.pop(), ni(e, !1);
}
function ni(e, t) {
  var n = rt;
  if ((t ? _n.echoes && (!Qf++ || e !== rt) : Qf && (!--Qf || e !== rt)) && queueMicrotask(t ? kU.bind(null, e) : MU), e !== rt && (rt = e, n === Vo && (Vo.env = ok()), Cb)) {
    var r = Vo.env.Promise, s = e.env;
    (n.global || e.global) && (Object.defineProperty(Mn, "Promise", s.PromiseProp), r.all = s.all, r.race = s.race, r.resolve = s.resolve, r.reject = s.reject, s.allSettled && (r.allSettled = s.allSettled), s.any && (r.any = s.any));
  }
}
function ok() {
  var e = Mn.Promise;
  return Cb ? {
    Promise: e,
    PromiseProp: Object.getOwnPropertyDescriptor(Mn, "Promise"),
    all: e.all,
    race: e.race,
    allSettled: e.allSettled,
    any: e.any,
    resolve: e.resolve,
    reject: e.reject
  } : {};
}
function ta(e, t, n, r, s) {
  var o = rt;
  try {
    return ni(e, !0), t(n, r, s);
  } finally {
    ni(o, !1);
  }
}
function eT(e, t, n, r) {
  return typeof e != "function" ? e : function() {
    var s = rt;
    n && Dl(), ni(t, !0);
    try {
      return e.apply(this, arguments);
    } finally {
      ni(s, !1), r && queueMicrotask(ti);
    }
  };
}
function $g(e) {
  Promise === Li && _n.echoes === 0 ? Qf === 0 ? e() : enqueueNativeMicroTask(e) : setTimeout(e, 0);
}
var sn = We.reject;
function iy(e, t, n, r) {
  if (!e.idbdb || !e._state.openComplete && !rt.letThrough && !e._vip) {
    if (e._state.openComplete) return sn(new lt.DatabaseClosed(e._state.dbOpenError));
    if (!e._state.isBeingOpened) {
      if (!e._state.autoOpen) return sn(new lt.DatabaseClosed());
      e.open().catch(Ht);
    }
    return e._state.dbReadyPromise.then(function() {
      return iy(e, t, n, r);
    });
  } else {
    var s = e._createTransaction(t, n, e._dbSchema);
    try {
      s.create(), e._state.PR1398_maxLoop = 3;
    } catch (o) {
      return o.name === Sb.InvalidState && e.isOpen() && --e._state.PR1398_maxLoop > 0 ? (console.warn("Dexie: Need to reopen db"), e.close({ disableAutoOpen: !1 }), e.open().then(function() {
        return iy(e, t, n, r);
      })) : sn(o);
    }
    return s._promise(t, function(o, i) {
      return ei(function() {
        return rt.trans = s, r(o, i, s);
      });
    }).then(function(o) {
      if (t === "readwrite") try {
        s.idbtrans.commit();
      } catch {
      }
      return t === "readonly" ? o : s._completion.then(function() {
        return o;
      });
    });
  }
}
var tT = "4.0.10", Gi = "￿", ay = -1 / 0, Gs = "Invalid key provided. Keys must be of type string, number, Date or Array<string | number | Date>.", ik = "String expected.", ll = [], Gp = "__dbnames", Dg = "readonly", Lg = "readwrite";
function na(e, t) {
  return e ? t ? function() {
    return e.apply(this, arguments) && t.apply(this, arguments);
  } : e : t;
}
var ak = {
  type: 3,
  lower: -1 / 0,
  lowerOpen: !1,
  upper: [[]],
  upperOpen: !1
};
function nf(e) {
  return typeof e == "string" && !/\./.test(e) ? function(t) {
    return t[e] === void 0 && e in t && (t = ji(t), delete t[e]), t;
  } : function(t) {
    return t;
  };
}
function PU() {
  throw lt.Type();
}
function xt(e, t) {
  try {
    var n = nT(e), r = nT(t);
    if (n !== r)
      return n === "Array" ? 1 : r === "Array" ? -1 : n === "binary" ? 1 : r === "binary" ? -1 : n === "string" ? 1 : r === "string" ? -1 : n === "Date" ? 1 : r !== "Date" ? NaN : -1;
    switch (n) {
      case "number":
      case "Date":
      case "string":
        return e > t ? 1 : e < t ? -1 : 0;
      case "binary":
        return NU(rT(e), rT(t));
      case "Array":
        return RU(e, t);
    }
  } catch {
  }
  return NaN;
}
function RU(e, t) {
  for (var n = e.length, r = t.length, s = n < r ? n : r, o = 0; o < s; ++o) {
    var i = xt(e[o], t[o]);
    if (i !== 0) return i;
  }
  return n === r ? 0 : n < r ? -1 : 1;
}
function NU(e, t) {
  for (var n = e.length, r = t.length, s = n < r ? n : r, o = 0; o < s; ++o) if (e[o] !== t[o]) return e[o] < t[o] ? -1 : 1;
  return n === r ? 0 : n < r ? -1 : 1;
}
function nT(e) {
  var t = typeof e;
  if (t !== "object") return t;
  if (ArrayBuffer.isView(e)) return "binary";
  var n = ey(e);
  return n === "ArrayBuffer" ? "binary" : n;
}
function rT(e) {
  return e instanceof Uint8Array ? e : ArrayBuffer.isView(e) ? new Uint8Array(e.buffer, e.byteOffset, e.byteLength) : new Uint8Array(e);
}
var lk = (function() {
  function e() {
  }
  return e.prototype._trans = function(t, n, r) {
    var s = this._tx || rt.trans, o = this.name, i = $s && typeof console < "u" && console.createTask && console.createTask("Dexie: ".concat(t === "readonly" ? "read" : "write", " ").concat(this.name));
    function a(d, h, p) {
      if (!p.schema[o]) throw new lt.NotFound("Table " + o + " not part of transaction");
      return n(p.idbtrans, p);
    }
    var l = Nl();
    try {
      var u = s && s.db._novip === this.db._novip ? s === rt.trans ? s._promise(t, a, r) : ei(function() {
        return s._promise(t, a, r);
      }, {
        trans: s,
        transless: rt.transless || rt
      }) : iy(this.db, t, [this.name], a);
      return i && (u._consoleTask = i, u = u.catch(function(d) {
        return console.trace(d), sn(d);
      })), u;
    } finally {
      l && $l();
    }
  }, e.prototype.get = function(t, n) {
    var r = this;
    return t && t.constructor === Object ? this.where(t).first(n) : t == null ? sn(new lt.Type("Invalid argument to Table.get()")) : this._trans("readonly", function(s) {
      return r.core.get({
        trans: s,
        key: t
      }).then(function(o) {
        return r.hook.reading.fire(o);
      });
    }).then(n);
  }, e.prototype.where = function(t) {
    if (typeof t == "string") return new this.db.WhereClause(this, t);
    if (en(t)) return new this.db.WhereClause(this, "[".concat(t.join("+"), "]"));
    var n = Sn(t);
    if (n.length === 1) return this.where(n[0]).equals(t[n[0]]);
    var r = this.schema.indexes.concat(this.schema.primKey).filter(function(d) {
      if (d.compound && n.every(function(p) {
        return d.keyPath.indexOf(p) >= 0;
      })) {
        for (var h = 0; h < n.length; ++h) if (n.indexOf(d.keyPath[h]) === -1) return !1;
        return !0;
      }
      return !1;
    }).sort(function(d, h) {
      return d.keyPath.length - h.keyPath.length;
    })[0];
    if (r && this.db._maxKey !== Gi) {
      var s = r.keyPath.slice(0, n.length);
      return this.where(s).equals(s.map(function(d) {
        return t[d];
      }));
    }
    !r && $s && console.warn("The query ".concat(JSON.stringify(t), " on ").concat(this.name, " would benefit from a ") + "compound index [".concat(n.join("+"), "]"));
    var o = this.schema.idxByName;
    function i(d, h) {
      return xt(d, h) === 0;
    }
    var a = n.reduce(function(d, h) {
      var p = d[0], y = d[1], w = o[h], E = t[h];
      return [p || w, p || !w ? na(y, w && w.multi ? function(C) {
        var M = ao(C, h);
        return en(M) && M.some(function(k) {
          return i(E, k);
        });
      } : function(C) {
        return i(E, ao(C, h));
      }) : y];
    }, [null, null]), l = a[0], u = a[1];
    return l ? this.where(l.name).equals(t[l.keyPath]).filter(u) : r ? this.filter(u) : this.where(n).equals("");
  }, e.prototype.filter = function(t) {
    return this.toCollection().and(t);
  }, e.prototype.count = function(t) {
    return this.toCollection().count(t);
  }, e.prototype.offset = function(t) {
    return this.toCollection().offset(t);
  }, e.prototype.limit = function(t) {
    return this.toCollection().limit(t);
  }, e.prototype.each = function(t) {
    return this.toCollection().each(t);
  }, e.prototype.toArray = function(t) {
    return this.toCollection().toArray(t);
  }, e.prototype.toCollection = function() {
    return new this.db.Collection(new this.db.WhereClause(this));
  }, e.prototype.orderBy = function(t) {
    return new this.db.Collection(new this.db.WhereClause(this, en(t) ? "[".concat(t.join("+"), "]") : t));
  }, e.prototype.reverse = function() {
    return this.toCollection().reverse();
  }, e.prototype.mapToClass = function(t) {
    var n = this, r = n.db, s = n.name;
    this.schema.mappedClass = t, t.prototype instanceof PU && (t = (function(l) {
      jB(u, l);
      function u() {
        return l !== null && l.apply(this, arguments) || this;
      }
      return Object.defineProperty(u.prototype, "db", {
        get: function() {
          return r;
        },
        enumerable: !1,
        configurable: !0
      }), u.prototype.table = function() {
        return s;
      }, u;
    })(t));
    for (var o = /* @__PURE__ */ new Set(), i = t.prototype; i; i = wl(i)) Object.getOwnPropertyNames(i).forEach(function(l) {
      return o.add(l);
    });
    var a = function(l) {
      if (!l) return l;
      var u = Object.create(t.prototype);
      for (var d in l) if (!o.has(d)) try {
        u[d] = l[d];
      } catch {
      }
      return u;
    };
    return this.schema.readHook && this.hook.reading.unsubscribe(this.schema.readHook), this.schema.readHook = a, this.hook("reading", a), t;
  }, e.prototype.defineClass = function() {
    function t(n) {
      Dr(this, n);
    }
    return this.mapToClass(t);
  }, e.prototype.add = function(t, n) {
    var r = this, s = this.schema.primKey, o = s.auto, i = s.keyPath, a = t;
    return i && o && (a = nf(i)(t)), this._trans("readwrite", function(l) {
      return r.core.mutate({
        trans: l,
        type: "add",
        keys: n != null ? [n] : null,
        values: [a]
      });
    }).then(function(l) {
      return l.numFailures ? We.reject(l.failures[0]) : l.lastResult;
    }).then(function(l) {
      if (i) try {
        Pr(t, i, l);
      } catch {
      }
      return l;
    });
  }, e.prototype.update = function(t, n) {
    if (typeof t == "object" && !en(t)) {
      var r = ao(t, this.schema.primKey.keyPath);
      return r === void 0 ? sn(new lt.InvalidArgument("Given object does not contain its primary key")) : this.where(":id").equals(r).modify(n);
    } else return this.where(":id").equals(t).modify(n);
  }, e.prototype.put = function(t, n) {
    var r = this, s = this.schema.primKey, o = s.auto, i = s.keyPath, a = t;
    return i && o && (a = nf(i)(t)), this._trans("readwrite", function(l) {
      return r.core.mutate({
        trans: l,
        type: "put",
        values: [a],
        keys: n != null ? [n] : null
      });
    }).then(function(l) {
      return l.numFailures ? We.reject(l.failures[0]) : l.lastResult;
    }).then(function(l) {
      if (i) try {
        Pr(t, i, l);
      } catch {
      }
      return l;
    });
  }, e.prototype.delete = function(t) {
    var n = this;
    return this._trans("readwrite", function(r) {
      return n.core.mutate({
        trans: r,
        type: "delete",
        keys: [t]
      });
    }).then(function(r) {
      return r.numFailures ? We.reject(r.failures[0]) : void 0;
    });
  }, e.prototype.clear = function() {
    var t = this;
    return this._trans("readwrite", function(n) {
      return t.core.mutate({
        trans: n,
        type: "deleteRange",
        range: ak
      });
    }).then(function(n) {
      return n.numFailures ? We.reject(n.failures[0]) : void 0;
    });
  }, e.prototype.bulkGet = function(t) {
    var n = this;
    return this._trans("readonly", function(r) {
      return n.core.getMany({
        keys: t,
        trans: r
      }).then(function(s) {
        return s.map(function(o) {
          return n.hook.reading.fire(o);
        });
      });
    });
  }, e.prototype.bulkAdd = function(t, n, r) {
    var s = this, o = Array.isArray(n) ? n : void 0;
    r = r || (o ? void 0 : n);
    var i = r ? r.allKeys : void 0;
    return this._trans("readwrite", function(a) {
      var l = s.schema.primKey, u = l.auto, d = l.keyPath;
      if (d && o) throw new lt.InvalidArgument("bulkAdd(): keys argument invalid on tables with inbound keys");
      if (o && o.length !== t.length) throw new lt.InvalidArgument("Arguments objects and keys must have the same length");
      var h = t.length, p = d && u ? t.map(nf(d)) : t;
      return s.core.mutate({
        trans: a,
        type: "add",
        keys: o,
        values: p,
        wantResults: i
      }).then(function(y) {
        var w = y.numFailures, E = y.results, C = y.lastResult, M = y.failures, k = i ? E : C;
        if (w === 0) return k;
        throw new Xa("".concat(s.name, ".bulkAdd(): ").concat(w, " of ").concat(h, " operations failed"), M);
      });
    });
  }, e.prototype.bulkPut = function(t, n, r) {
    var s = this, o = Array.isArray(n) ? n : void 0;
    r = r || (o ? void 0 : n);
    var i = r ? r.allKeys : void 0;
    return this._trans("readwrite", function(a) {
      var l = s.schema.primKey, u = l.auto, d = l.keyPath;
      if (d && o) throw new lt.InvalidArgument("bulkPut(): keys argument invalid on tables with inbound keys");
      if (o && o.length !== t.length) throw new lt.InvalidArgument("Arguments objects and keys must have the same length");
      var h = t.length, p = d && u ? t.map(nf(d)) : t;
      return s.core.mutate({
        trans: a,
        type: "put",
        keys: o,
        values: p,
        wantResults: i
      }).then(function(y) {
        var w = y.numFailures, E = y.results, C = y.lastResult, M = y.failures, k = i ? E : C;
        if (w === 0) return k;
        throw new Xa("".concat(s.name, ".bulkPut(): ").concat(w, " of ").concat(h, " operations failed"), M);
      });
    });
  }, e.prototype.bulkUpdate = function(t) {
    var n = this, r = this.core, s = t.map(function(a) {
      return a.key;
    }), o = t.map(function(a) {
      return a.changes;
    }), i = [];
    return this._trans("readwrite", function(a) {
      return r.getMany({
        trans: a,
        keys: s,
        cache: "clone"
      }).then(function(l) {
        var u = [], d = [];
        t.forEach(function(p, y) {
          var w = p.key, E = p.changes, C = l[y];
          if (C) {
            for (var M = 0, k = Object.keys(E); M < k.length; M++) {
              var R = k[M], D = E[R];
              if (R === n.schema.primKey.keyPath) {
                if (xt(D, w) !== 0) throw new lt.Constraint("Cannot update primary key in bulkUpdate()");
              } else Pr(C, R, D);
            }
            i.push(y), u.push(w), d.push(C);
          }
        });
        var h = u.length;
        return r.mutate({
          trans: a,
          type: "put",
          keys: u,
          values: d,
          updates: {
            keys: s,
            changeSpecs: o
          }
        }).then(function(p) {
          var y = p.numFailures, w = p.failures;
          if (y === 0) return h;
          for (var E = 0, C = Object.keys(w); E < C.length; E++) {
            var M = C[E], k = i[Number(M)];
            if (k != null) {
              var R = w[M];
              delete w[M], w[k] = R;
            }
          }
          throw new Xa("".concat(n.name, ".bulkUpdate(): ").concat(y, " of ").concat(h, " operations failed"), w);
        });
      });
    });
  }, e.prototype.bulkDelete = function(t) {
    var n = this, r = t.length;
    return this._trans("readwrite", function(s) {
      return n.core.mutate({
        trans: s,
        type: "delete",
        keys: t
      });
    }).then(function(s) {
      var o = s.numFailures, i = s.lastResult, a = s.failures;
      if (o === 0) return i;
      throw new Xa("".concat(n.name, ".bulkDelete(): ").concat(o, " of ").concat(r, " operations failed"), a);
    });
  }, e;
})();
function rd(e) {
  var t = {}, n = function(a, l) {
    if (l) {
      for (var u = arguments.length, d = new Array(u - 1); --u; ) d[u - 1] = arguments[u];
      return t[a].subscribe.apply(null, d), e;
    } else if (typeof a == "string") return t[a];
  };
  n.addEventType = o;
  for (var r = 1, s = arguments.length; r < s; ++r) o(arguments[r]);
  return n;
  function o(a, l, u) {
    if (typeof a == "object") return i(a);
    l || (l = yU), u || (u = Ht);
    var d = {
      subscribers: [],
      fire: u,
      subscribe: function(h) {
        d.subscribers.indexOf(h) === -1 && (d.subscribers.push(h), d.fire = l(d.fire, h));
      },
      unsubscribe: function(h) {
        d.subscribers = d.subscribers.filter(function(p) {
          return p !== h;
        }), d.fire = d.subscribers.reduce(l, u);
      }
    };
    return t[a] = n[a] = d, d;
  }
  function i(a) {
    Sn(a).forEach(function(l) {
      var u = a[l];
      if (en(u)) o(l, a[l][0], a[l][1]);
      else if (u === "asap") var d = o(l, nd, function() {
        for (var p = arguments.length, y = new Array(p); p--; ) y[p] = arguments[p];
        d.subscribers.forEach(function(w) {
          zx(function() {
            w.apply(null, y);
          });
        });
      });
      else throw new lt.InvalidArgument("Invalid event config");
    });
  }
}
function sd(e, t) {
  return Pl(t).from({ prototype: e }), t;
}
function $U(e) {
  return sd(lk.prototype, function(n, r, s) {
    this.db = e, this._tx = s, this.name = n, this.schema = r, this.hook = e._allTables[n] ? e._allTables[n].hook : rd(null, {
      creating: [mU, Ht],
      reading: [pU, nd],
      updating: [vU, Ht],
      deleting: [gU, Ht]
    });
  });
}
function xa(e, t) {
  return !(e.filter || e.algorithm || e.or) && (t ? e.justLimit : !e.replayFilter);
}
function Og(e, t) {
  e.filter = na(e.filter, t);
}
function Fg(e, t, n) {
  var r = e.replayFilter;
  e.replayFilter = r ? function() {
    return na(r(), t());
  } : t, e.justLimit = n && !r;
}
function DU(e, t) {
  e.isMatch = na(e.isMatch, t);
}
function Zf(e, t) {
  if (e.isPrimKey) return t.primaryKey;
  var n = t.getIndexByKeyPath(e.index);
  if (!n) throw new lt.Schema("KeyPath " + e.index + " on object store " + t.name + " is not indexed");
  return n;
}
function sT(e, t, n) {
  var r = Zf(e, t.schema);
  return t.openCursor({
    trans: n,
    values: !e.keysOnly,
    reverse: e.dir === "prev",
    unique: !!e.unique,
    query: {
      index: r,
      range: e.range
    }
  });
}
function rf(e, t, n, r) {
  var s = e.replayFilter ? na(e.filter, e.replayFilter()) : e.filter;
  if (e.or) {
    var o = {}, i = function(a, l, u) {
      if (!s || s(l, u, function(p) {
        return l.stop(p);
      }, function(p) {
        return l.fail(p);
      })) {
        var d = l.primaryKey, h = "" + d;
        h === "[object ArrayBuffer]" && (h = "" + new Uint8Array(d)), ir(o, h) || (o[h] = !0, t(a, l, u));
      }
    };
    return Promise.all([e.or._iterate(i, n), oT(sT(e, r, n), e.algorithm, i, !e.keysOnly && e.valueMapper)]);
  } else
    return oT(sT(e, r, n), na(e.algorithm, s), t, !e.keysOnly && e.valueMapper);
}
function oT(e, t, n, r) {
  var s = Zt(r ? function(o, i, a) {
    return n(r(o), i, a);
  } : n);
  return e.then(function(o) {
    if (o) return o.start(function() {
      var i = function() {
        return o.continue();
      };
      (!t || t(o, function(a) {
        return i = a;
      }, function(a) {
        o.stop(a), i = Ht;
      }, function(a) {
        o.fail(a), i = Ht;
      })) && s(o.value, o, function(a) {
        return i = a;
      }), i();
    });
  });
}
var LU = (function() {
  function e(t) {
    Object.assign(this, t);
  }
  return e.prototype.execute = function(t) {
    var n;
    if (this.add !== void 0) {
      var r = this.add;
      if (en(r)) return Oh(Oh([], en(t) ? t : [], !0), r, !0).sort();
      if (typeof r == "number") return (Number(t) || 0) + r;
      if (typeof r == "bigint") try {
        return BigInt(t) + r;
      } catch {
        return BigInt(0) + r;
      }
      throw new TypeError("Invalid term ".concat(r));
    }
    if (this.remove !== void 0) {
      var s = this.remove;
      if (en(s)) return en(t) ? t.filter(function(i) {
        return !s.includes(i);
      }).sort() : [];
      if (typeof s == "number") return Number(t) - s;
      if (typeof s == "bigint") try {
        return BigInt(t) - s;
      } catch {
        return BigInt(0) - s;
      }
      throw new TypeError("Invalid subtrahend ".concat(s));
    }
    var o = (n = this.replacePrefix) === null || n === void 0 ? void 0 : n[0];
    return o && typeof t == "string" && t.startsWith(o) ? this.replacePrefix[1] + t.substring(o.length) : t;
  }, e;
})(), OU = (function() {
  function e() {
  }
  return e.prototype._read = function(t, n) {
    var r = this._ctx;
    return r.error ? r.table._trans(null, sn.bind(null, r.error)) : r.table._trans("readonly", t).then(n);
  }, e.prototype._write = function(t) {
    var n = this._ctx;
    return n.error ? n.table._trans(null, sn.bind(null, n.error)) : n.table._trans("readwrite", t, "locked");
  }, e.prototype._addAlgorithm = function(t) {
    var n = this._ctx;
    n.algorithm = na(n.algorithm, t);
  }, e.prototype._iterate = function(t, n) {
    return rf(this._ctx, t, n, this._ctx.table.core);
  }, e.prototype.clone = function(t) {
    var n = Object.create(this.constructor.prototype), r = Object.create(this._ctx);
    return t && Dr(r, t), n._ctx = r, n;
  }, e.prototype.raw = function() {
    return this._ctx.valueMapper = null, this;
  }, e.prototype.each = function(t) {
    var n = this._ctx;
    return this._read(function(r) {
      return rf(n, t, r, n.table.core);
    });
  }, e.prototype.count = function(t) {
    var n = this;
    return this._read(function(r) {
      var s = n._ctx, o = s.table.core;
      if (xa(s, !0)) return o.count({
        trans: r,
        query: {
          index: Zf(s, o.schema),
          range: s.range
        }
      }).then(function(a) {
        return Math.min(a, s.limit);
      });
      var i = 0;
      return rf(s, function() {
        return ++i, !1;
      }, r, o).then(function() {
        return i;
      });
    }).then(t);
  }, e.prototype.sortBy = function(t, n) {
    var r = t.split(".").reverse(), s = r[0], o = r.length - 1;
    function i(u, d) {
      return d ? i(u[r[d]], d - 1) : u[s];
    }
    var a = this._ctx.dir === "next" ? 1 : -1;
    function l(u, d) {
      return xt(i(u, o), i(d, o)) * a;
    }
    return this.toArray(function(u) {
      return u.sort(l);
    }).then(n);
  }, e.prototype.toArray = function(t) {
    var n = this;
    return this._read(function(r) {
      var s = n._ctx;
      if (s.dir === "next" && xa(s, !0) && s.limit > 0) {
        var o = s.valueMapper, i = Zf(s, s.table.core.schema);
        return s.table.core.query({
          trans: r,
          limit: s.limit,
          values: !0,
          query: {
            index: i,
            range: s.range
          }
        }).then(function(l) {
          var u = l.result;
          return o ? u.map(o) : u;
        });
      } else {
        var a = [];
        return rf(s, function(l) {
          return a.push(l);
        }, r, s.table.core).then(function() {
          return a;
        });
      }
    }, t);
  }, e.prototype.offset = function(t) {
    var n = this._ctx;
    return t <= 0 ? this : (n.offset += t, xa(n) ? Fg(n, function() {
      var r = t;
      return function(s, o) {
        return r === 0 ? !0 : r === 1 ? (--r, !1) : (o(function() {
          s.advance(r), r = 0;
        }), !1);
      };
    }) : Fg(n, function() {
      var r = t;
      return function() {
        return --r < 0;
      };
    }), this);
  }, e.prototype.limit = function(t) {
    return this._ctx.limit = Math.min(this._ctx.limit, t), Fg(this._ctx, function() {
      var n = t;
      return function(r, s, o) {
        return --n <= 0 && s(o), n >= 0;
      };
    }, !0), this;
  }, e.prototype.until = function(t, n) {
    return Og(this._ctx, function(r, s, o) {
      return t(r.value) ? (s(o), n) : !0;
    }), this;
  }, e.prototype.first = function(t) {
    return this.limit(1).toArray(function(n) {
      return n[0];
    }).then(t);
  }, e.prototype.last = function(t) {
    return this.reverse().first(t);
  }, e.prototype.filter = function(t) {
    return Og(this._ctx, function(n) {
      return t(n.value);
    }), DU(this._ctx, t), this;
  }, e.prototype.and = function(t) {
    return this.filter(t);
  }, e.prototype.or = function(t) {
    return new this.db.WhereClause(this._ctx.table, t, this);
  }, e.prototype.reverse = function() {
    return this._ctx.dir = this._ctx.dir === "prev" ? "next" : "prev", this._ondirectionchange && this._ondirectionchange(this._ctx.dir), this;
  }, e.prototype.desc = function() {
    return this.reverse();
  }, e.prototype.eachKey = function(t) {
    var n = this._ctx;
    return n.keysOnly = !n.isMatch, this.each(function(r, s) {
      t(s.key, s);
    });
  }, e.prototype.eachUniqueKey = function(t) {
    return this._ctx.unique = "unique", this.eachKey(t);
  }, e.prototype.eachPrimaryKey = function(t) {
    var n = this._ctx;
    return n.keysOnly = !n.isMatch, this.each(function(r, s) {
      t(s.primaryKey, s);
    });
  }, e.prototype.keys = function(t) {
    var n = this._ctx;
    n.keysOnly = !n.isMatch;
    var r = [];
    return this.each(function(s, o) {
      r.push(o.key);
    }).then(function() {
      return r;
    }).then(t);
  }, e.prototype.primaryKeys = function(t) {
    var n = this._ctx;
    if (n.dir === "next" && xa(n, !0) && n.limit > 0) return this._read(function(s) {
      var o = Zf(n, n.table.core.schema);
      return n.table.core.query({
        trans: s,
        values: !1,
        limit: n.limit,
        query: {
          index: o,
          range: n.range
        }
      });
    }).then(function(s) {
      return s.result;
    }).then(t);
    n.keysOnly = !n.isMatch;
    var r = [];
    return this.each(function(s, o) {
      r.push(o.primaryKey);
    }).then(function() {
      return r;
    }).then(t);
  }, e.prototype.uniqueKeys = function(t) {
    return this._ctx.unique = "unique", this.keys(t);
  }, e.prototype.firstKey = function(t) {
    return this.limit(1).keys(function(n) {
      return n[0];
    }).then(t);
  }, e.prototype.lastKey = function(t) {
    return this.reverse().firstKey(t);
  }, e.prototype.distinct = function() {
    var t = this._ctx, n = t.index && t.table.schema.idxByName[t.index];
    if (!n || !n.multi) return this;
    var r = {};
    return Og(this._ctx, function(s) {
      var o = s.primaryKey.toString(), i = ir(r, o);
      return r[o] = !0, !i;
    }), this;
  }, e.prototype.modify = function(t) {
    var n = this, r = this._ctx;
    return this._write(function(s) {
      var o;
      if (typeof t == "function") o = t;
      else {
        var i = Sn(t), a = i.length;
        o = function(k) {
          for (var R = !1, D = 0; D < a; ++D) {
            var m = i[D], b = t[m], v = ao(k, m);
            b instanceof LU ? (Pr(k, m, b.execute(v)), R = !0) : v !== b && (Pr(k, m, b), R = !0);
          }
          return R;
        };
      }
      var l = r.table.core, u = l.schema.primaryKey, d = u.outbound, h = u.extractKey, p = 200, y = n.db._options.modifyChunkSize;
      y && (typeof y == "object" ? p = y[l.name] || y["*"] || 200 : p = y);
      var w = [], E = 0, C = [], M = function(k, R) {
        var D = R.failures, m = R.numFailures;
        E += k - m;
        for (var b = 0, v = Sn(D); b < v.length; b++) {
          var T = v[b];
          w.push(D[T]);
        }
      };
      return n.clone().primaryKeys().then(function(k) {
        var R = xa(r) && r.limit === 1 / 0 && (typeof t != "function" || t === Bg) && {
          index: r.index,
          range: r.range
        }, D = function(m) {
          var b = Math.min(p, k.length - m);
          return l.getMany({
            trans: s,
            keys: k.slice(m, m + b),
            cache: "immutable"
          }).then(function(v) {
            for (var T = [], I = [], x = d ? [] : null, P = [], L = 0; L < b; ++L) {
              var G = v[L], z = {
                value: ji(G),
                primKey: k[m + L]
              };
              o.call(z, z.value, z) !== !1 && (z.value == null ? P.push(k[m + L]) : !d && xt(h(G), h(z.value)) !== 0 ? (P.push(k[m + L]), T.push(z.value)) : (I.push(z.value), d && x.push(k[m + L])));
            }
            return Promise.resolve(T.length > 0 && l.mutate({
              trans: s,
              type: "add",
              values: T
            }).then(function(V) {
              for (var Z in V.failures) P.splice(parseInt(Z), 1);
              M(T.length, V);
            })).then(function() {
              return (I.length > 0 || R && typeof t == "object") && l.mutate({
                trans: s,
                type: "put",
                keys: x,
                values: I,
                criteria: R,
                changeSpec: typeof t != "function" && t,
                isAdditionalChunk: m > 0
              }).then(function(V) {
                return M(I.length, V);
              });
            }).then(function() {
              return (P.length > 0 || R && t === Bg) && l.mutate({
                trans: s,
                type: "delete",
                keys: P,
                criteria: R,
                isAdditionalChunk: m > 0
              }).then(function(V) {
                return M(P.length, V);
              });
            }).then(function() {
              return k.length > m + b && D(m + p);
            });
          });
        };
        return D(0).then(function() {
          if (w.length > 0) throw new Fh("Error modifying one or more objects", w, E, C);
          return k.length;
        });
      });
    });
  }, e.prototype.delete = function() {
    var t = this._ctx, n = t.range;
    return xa(t) && (t.isPrimKey || n.type === 3) ? this._write(function(r) {
      var s = t.table.core.schema.primaryKey, o = n;
      return t.table.core.count({
        trans: r,
        query: {
          index: s,
          range: o
        }
      }).then(function(i) {
        return t.table.core.mutate({
          trans: r,
          type: "deleteRange",
          range: o
        }).then(function(a) {
          var l = a.failures;
          a.lastResult, a.results;
          var u = a.numFailures;
          if (u) throw new Fh("Could not delete some values", Object.keys(l).map(function(d) {
            return l[d];
          }), i - u);
          return i - u;
        });
      });
    }) : this.modify(Bg);
  }, e;
})(), Bg = function(e, t) {
  return t.value = null;
};
function FU(e) {
  return sd(OU.prototype, function(n, r) {
    this.db = e;
    var s = ak, o = null;
    if (r) try {
      s = r();
    } catch (u) {
      o = u;
    }
    var i = n._ctx, a = i.table, l = a.hook.reading.fire;
    this._ctx = {
      table: a,
      index: i.index,
      isPrimKey: !i.index || a.schema.primKey.keyPath && i.index === a.schema.primKey.name,
      range: s,
      keysOnly: !1,
      dir: "next",
      unique: "",
      algorithm: null,
      filter: null,
      replayFilter: null,
      justLimit: !0,
      isMatch: null,
      offset: 0,
      limit: 1 / 0,
      error: o,
      or: i.or,
      valueMapper: l !== nd ? l : null
    };
  });
}
function BU(e, t) {
  return e < t ? -1 : e === t ? 0 : 1;
}
function UU(e, t) {
  return e > t ? -1 : e === t ? 0 : 1;
}
function yr(e, t, n) {
  var r = e instanceof ck ? new e.Collection(e) : e;
  return r._ctx.error = n ? new n(t) : new TypeError(t), r;
}
function ka(e) {
  return new e.Collection(e, function() {
    return uk("");
  }).limit(0);
}
function qU(e) {
  return e === "next" ? function(t) {
    return t.toUpperCase();
  } : function(t) {
    return t.toLowerCase();
  };
}
function HU(e) {
  return e === "next" ? function(t) {
    return t.toLowerCase();
  } : function(t) {
    return t.toUpperCase();
  };
}
function GU(e, t, n, r, s, o) {
  for (var i = Math.min(e.length, r.length), a = -1, l = 0; l < i; ++l) {
    var u = t[l];
    if (u !== r[l])
      return s(e[l], n[l]) < 0 ? e.substr(0, l) + n[l] + n.substr(l + 1) : s(e[l], r[l]) < 0 ? e.substr(0, l) + r[l] + n.substr(l + 1) : a >= 0 ? e.substr(0, a) + t[a] + n.substr(a + 1) : null;
    s(e[l], u) < 0 && (a = l);
  }
  return i < r.length && o === "next" ? e + n.substr(e.length) : i < e.length && o === "prev" ? e.substr(0, n.length) : a < 0 ? null : e.substr(0, a) + r[a] + n.substr(a + 1);
}
function sf(e, t, n, r) {
  var s, o, i, a, l, u, d, h = n.length;
  if (!n.every(function(E) {
    return typeof E == "string";
  })) return yr(e, ik);
  function p(E) {
    s = qU(E), o = HU(E), i = E === "next" ? BU : UU;
    var C = n.map(function(M) {
      return {
        lower: o(M),
        upper: s(M)
      };
    }).sort(function(M, k) {
      return i(M.lower, k.lower);
    });
    a = C.map(function(M) {
      return M.upper;
    }), l = C.map(function(M) {
      return M.lower;
    }), u = E, d = E === "next" ? "" : r;
  }
  p("next");
  var y = new e.Collection(e, function() {
    return Ro(a[0], l[h - 1] + r);
  });
  y._ondirectionchange = function(E) {
    p(E);
  };
  var w = 0;
  return y._addAlgorithm(function(E, C, M) {
    var k = E.key;
    if (typeof k != "string") return !1;
    var R = o(k);
    if (t(R, l, w)) return !0;
    for (var D = null, m = w; m < h; ++m) {
      var b = GU(k, R, a[m], l[m], i, u);
      b === null && D === null ? w = m + 1 : (D === null || i(D, b) > 0) && (D = b);
    }
    return C(D !== null ? function() {
      E.continue(D + d);
    } : M), !1;
  }), y;
}
function Ro(e, t, n, r) {
  return {
    type: 2,
    lower: e,
    upper: t,
    lowerOpen: n,
    upperOpen: r
  };
}
function uk(e) {
  return {
    type: 1,
    lower: e,
    upper: e
  };
}
var ck = (function() {
  function e() {
  }
  return Object.defineProperty(e.prototype, "Collection", {
    get: function() {
      return this._ctx.table.db.Collection;
    },
    enumerable: !1,
    configurable: !0
  }), e.prototype.between = function(t, n, r, s) {
    r = r !== !1, s = s === !0;
    try {
      return this._cmp(t, n) > 0 || this._cmp(t, n) === 0 && (r || s) && !(r && s) ? ka(this) : new this.Collection(this, function() {
        return Ro(t, n, !r, !s);
      });
    } catch {
      return yr(this, Gs);
    }
  }, e.prototype.equals = function(t) {
    return t == null ? yr(this, Gs) : new this.Collection(this, function() {
      return uk(t);
    });
  }, e.prototype.above = function(t) {
    return t == null ? yr(this, Gs) : new this.Collection(this, function() {
      return Ro(t, void 0, !0);
    });
  }, e.prototype.aboveOrEqual = function(t) {
    return t == null ? yr(this, Gs) : new this.Collection(this, function() {
      return Ro(t, void 0, !1);
    });
  }, e.prototype.below = function(t) {
    return t == null ? yr(this, Gs) : new this.Collection(this, function() {
      return Ro(void 0, t, !1, !0);
    });
  }, e.prototype.belowOrEqual = function(t) {
    return t == null ? yr(this, Gs) : new this.Collection(this, function() {
      return Ro(void 0, t);
    });
  }, e.prototype.startsWith = function(t) {
    return typeof t != "string" ? yr(this, ik) : this.between(t, t + Gi, !0, !0);
  }, e.prototype.startsWithIgnoreCase = function(t) {
    return t === "" ? this.startsWith(t) : sf(this, function(n, r) {
      return n.indexOf(r[0]) === 0;
    }, [t], Gi);
  }, e.prototype.equalsIgnoreCase = function(t) {
    return sf(this, function(n, r) {
      return n === r[0];
    }, [t], "");
  }, e.prototype.anyOfIgnoreCase = function() {
    var t = ro.apply(Ha, arguments);
    return t.length === 0 ? ka(this) : sf(this, function(n, r) {
      return r.indexOf(n) !== -1;
    }, t, "");
  }, e.prototype.startsWithAnyOfIgnoreCase = function() {
    var t = ro.apply(Ha, arguments);
    return t.length === 0 ? ka(this) : sf(this, function(n, r) {
      return r.some(function(s) {
        return n.indexOf(s) === 0;
      });
    }, t, Gi);
  }, e.prototype.anyOf = function() {
    var t = this, n = ro.apply(Ha, arguments), r = this._cmp;
    try {
      n.sort(r);
    } catch {
      return yr(this, Gs);
    }
    if (n.length === 0) return ka(this);
    var s = new this.Collection(this, function() {
      return Ro(n[0], n[n.length - 1]);
    });
    s._ondirectionchange = function(i) {
      r = i === "next" ? t._ascending : t._descending, n.sort(r);
    };
    var o = 0;
    return s._addAlgorithm(function(i, a, l) {
      for (var u = i.key; r(u, n[o]) > 0; )
        if (++o, o === n.length)
          return a(l), !1;
      return r(u, n[o]) === 0 ? !0 : (a(function() {
        i.continue(n[o]);
      }), !1);
    }), s;
  }, e.prototype.notEqual = function(t) {
    return this.inAnyRange([[ay, t], [t, this.db._maxKey]], {
      includeLowers: !1,
      includeUppers: !1
    });
  }, e.prototype.noneOf = function() {
    var t = ro.apply(Ha, arguments);
    if (t.length === 0) return new this.Collection(this);
    try {
      t.sort(this._ascending);
    } catch {
      return yr(this, Gs);
    }
    var n = t.reduce(function(r, s) {
      return r ? r.concat([[r[r.length - 1][1], s]]) : [[ay, s]];
    }, null);
    return n.push([t[t.length - 1], this.db._maxKey]), this.inAnyRange(n, {
      includeLowers: !1,
      includeUppers: !1
    });
  }, e.prototype.inAnyRange = function(t, n) {
    var r = this, s = this._cmp, o = this._ascending, i = this._descending, a = this._min, l = this._max;
    if (t.length === 0) return ka(this);
    if (!t.every(function(m) {
      return m[0] !== void 0 && m[1] !== void 0 && o(m[0], m[1]) <= 0;
    })) return yr(this, "First argument to inAnyRange() must be an Array of two-value Arrays [lower,upper] where upper must not be lower than lower", lt.InvalidArgument);
    var u = !n || n.includeLowers !== !1, d = n && n.includeUppers === !0;
    function h(m, b) {
      for (var v = 0, T = m.length; v < T; ++v) {
        var I = m[v];
        if (s(b[0], I[1]) < 0 && s(b[1], I[0]) > 0) {
          I[0] = a(I[0], b[0]), I[1] = l(I[1], b[1]);
          break;
        }
      }
      return v === T && m.push(b), m;
    }
    var p = o;
    function y(m, b) {
      return p(m[0], b[0]);
    }
    var w;
    try {
      w = t.reduce(h, []), w.sort(y);
    } catch {
      return yr(this, Gs);
    }
    var E = 0, C = d ? function(m) {
      return o(m, w[E][1]) > 0;
    } : function(m) {
      return o(m, w[E][1]) >= 0;
    }, M = u ? function(m) {
      return i(m, w[E][0]) > 0;
    } : function(m) {
      return i(m, w[E][0]) >= 0;
    };
    function k(m) {
      return !C(m) && !M(m);
    }
    var R = C, D = new this.Collection(this, function() {
      return Ro(w[0][0], w[w.length - 1][1], !u, !d);
    });
    return D._ondirectionchange = function(m) {
      m === "next" ? (R = C, p = o) : (R = M, p = i), w.sort(y);
    }, D._addAlgorithm(function(m, b, v) {
      for (var T = m.key; R(T); )
        if (++E, E === w.length)
          return b(v), !1;
      return k(T) ? !0 : (r._cmp(T, w[E][1]) === 0 || r._cmp(T, w[E][0]) === 0 || b(function() {
        p === o ? m.continue(w[E][0]) : m.continue(w[E][1]);
      }), !1);
    }), D;
  }, e.prototype.startsWithAnyOf = function() {
    var t = ro.apply(Ha, arguments);
    return t.every(function(n) {
      return typeof n == "string";
    }) ? t.length === 0 ? ka(this) : this.inAnyRange(t.map(function(n) {
      return [n, n + Gi];
    })) : yr(this, "startsWithAnyOf() only works with strings");
  }, e;
})();
function WU(e) {
  return sd(ck.prototype, function(n, r, s) {
    if (this.db = e, this._ctx = {
      table: n,
      index: r === ":id" ? null : r,
      or: s
    }, this._cmp = this._ascending = xt, this._descending = function(o, i) {
      return xt(i, o);
    }, this._max = function(o, i) {
      return xt(o, i) > 0 ? o : i;
    }, this._min = function(o, i) {
      return xt(o, i) < 0 ? o : i;
    }, this._IDBKeyRange = e._deps.IDBKeyRange, !this._IDBKeyRange) throw new lt.MissingAPI();
  });
}
function Ts(e) {
  return Zt(function(t) {
    return Dc(t), e(t.target.error), !1;
  });
}
function Dc(e) {
  e.stopPropagation && e.stopPropagation(), e.preventDefault && e.preventDefault();
}
var od = "storagemutated", ly = "x-storagemutated-1", ri = rd(null, od), VU = (function() {
  function e() {
  }
  return e.prototype._lock = function() {
    return Fu(!rt.global), ++this._reculock, this._reculock === 1 && !rt.global && (rt.lockOwnerFor = this), this;
  }, e.prototype._unlock = function() {
    if (Fu(!rt.global), --this._reculock === 0)
      for (rt.global || (rt.lockOwnerFor = null); this._blockedFuncs.length > 0 && !this._locked(); ) {
        var t = this._blockedFuncs.shift();
        try {
          ta(t[1], t[0]);
        } catch {
        }
      }
    return this;
  }, e.prototype._locked = function() {
    return this._reculock && rt.lockOwnerFor !== this;
  }, e.prototype.create = function(t) {
    var n = this;
    if (!this.mode) return this;
    var r = this.db.idbdb, s = this.db._state.dbOpenError;
    if (Fu(!this.idbtrans), !t && !r) switch (s && s.name) {
      case "DatabaseClosedError":
        throw new lt.DatabaseClosed(s);
      case "MissingAPIError":
        throw new lt.MissingAPI(s.message, s);
      default:
        throw new lt.OpenFailed(s);
    }
    if (!this.active) throw new lt.TransactionInactive();
    return Fu(this._completion._state === null), t = this.idbtrans = t || (this.db.core ? this.db.core.transaction(this.storeNames, this.mode, { durability: this.chromeTransactionDurability }) : r.transaction(this.storeNames, this.mode, { durability: this.chromeTransactionDurability })), t.onerror = Zt(function(o) {
      Dc(o), n._reject(t.error);
    }), t.onabort = Zt(function(o) {
      Dc(o), n.active && n._reject(new lt.Abort(t.error)), n.active = !1, n.on("abort").fire(o);
    }), t.oncomplete = Zt(function() {
      n.active = !1, n._resolve(), "mutatedParts" in t && ri.storagemutated.fire(t.mutatedParts);
    }), this;
  }, e.prototype._promise = function(t, n, r) {
    var s = this;
    if (t === "readwrite" && this.mode !== "readwrite") return sn(new lt.ReadOnly("Transaction is readonly"));
    if (!this.active) return sn(new lt.TransactionInactive());
    if (this._locked()) return new We(function(i, a) {
      s._blockedFuncs.push([function() {
        s._promise(t, n, r).then(i, a);
      }, rt]);
    });
    if (r) return ei(function() {
      var i = new We(function(a, l) {
        s._lock();
        var u = n(a, l, s);
        u && u.then && u.then(a, l);
      });
      return i.finally(function() {
        return s._unlock();
      }), i._lib = !0, i;
    });
    var o = new We(function(i, a) {
      var l = n(i, a, s);
      l && l.then && l.then(i, a);
    });
    return o._lib = !0, o;
  }, e.prototype._root = function() {
    return this.parent ? this.parent._root() : this;
  }, e.prototype.waitFor = function(t) {
    var n = this._root(), r = We.resolve(t);
    if (n._waitingFor) n._waitingFor = n._waitingFor.then(function() {
      return r;
    });
    else {
      n._waitingFor = r, n._waitingQueue = [];
      var s = n.idbtrans.objectStore(n.storeNames[0]);
      (function i() {
        for (++n._spinCount; n._waitingQueue.length; ) n._waitingQueue.shift()();
        n._waitingFor && (s.get(-1 / 0).onsuccess = i);
      })();
    }
    var o = n._waitingFor;
    return new We(function(i, a) {
      r.then(function(l) {
        return n._waitingQueue.push(Zt(i.bind(null, l)));
      }, function(l) {
        return n._waitingQueue.push(Zt(a.bind(null, l)));
      }).finally(function() {
        n._waitingFor === o && (n._waitingFor = null);
      });
    });
  }, e.prototype.abort = function() {
    this.active && (this.active = !1, this.idbtrans && this.idbtrans.abort(), this._reject(new lt.Abort()));
  }, e.prototype.table = function(t) {
    var n = this._memoizedTables || (this._memoizedTables = {});
    if (ir(n, t)) return n[t];
    var r = this.schema[t];
    if (!r) throw new lt.NotFound("Table " + t + " not part of transaction");
    var s = new this.db.Table(t, r, this);
    return s.core = this.db.core.table(t), n[t] = s, s;
  }, e;
})();
function KU(e) {
  return sd(VU.prototype, function(n, r, s, o, i) {
    var a = this;
    this.db = e, this.mode = n, this.storeNames = r, this.schema = s, this.chromeTransactionDurability = o, this.idbtrans = null, this.on = rd(this, "complete", "error", "abort"), this.parent = i || null, this.active = !0, this._reculock = 0, this._blockedFuncs = [], this._resolve = null, this._reject = null, this._waitingFor = null, this._waitingQueue = null, this._spinCount = 0, this._completion = new We(function(l, u) {
      a._resolve = l, a._reject = u;
    }), this._completion.then(function() {
      a.active = !1, a.on.complete.fire();
    }, function(l) {
      var u = a.active;
      return a.active = !1, a.on.error.fire(l), a.parent ? a.parent._reject(l) : u && a.idbtrans && a.idbtrans.abort(), sn(l);
    });
  });
}
function uy(e, t, n, r, s, o, i) {
  return {
    name: e,
    keyPath: t,
    unique: n,
    multi: r,
    auto: s,
    compound: o,
    src: (n && !i ? "&" : "") + (r ? "*" : "") + (s ? "++" : "") + dk(t)
  };
}
function dk(e) {
  return typeof e == "string" ? e : e ? "[" + [].join.call(e, "+") + "]" : "";
}
function xb(e, t, n) {
  return {
    name: e,
    primKey: t,
    indexes: n,
    mappedClass: null,
    idxByName: rU(n, function(r) {
      return [r.name, r];
    })
  };
}
function zU(e) {
  return e.length === 1 ? e[0] : e;
}
var Lc = function(e) {
  try {
    return e.only([[]]), Lc = function() {
      return [[]];
    }, [[]];
  } catch {
    return Lc = function() {
      return Gi;
    }, Gi;
  }
};
function cy(e) {
  return e == null ? function() {
  } : typeof e == "string" ? JU(e) : function(t) {
    return ao(t, e);
  };
}
function JU(e) {
  return e.split(".").length === 1 ? function(t) {
    return t[e];
  } : function(t) {
    return ao(t, e);
  };
}
function iT(e) {
  return [].slice.call(e);
}
var YU = 0;
function hc(e) {
  return e == null ? ":id" : typeof e == "string" ? e : "[".concat(e.join("+"), "]");
}
function XU(e, t, n) {
  function r(h, p) {
    var y = iT(h.objectStoreNames);
    return {
      schema: {
        name: h.name,
        tables: y.map(function(w) {
          return p.objectStore(w);
        }).map(function(w) {
          var E = w.keyPath, C = w.autoIncrement, M = en(E), k = E == null, R = {}, D = {
            name: w.name,
            primaryKey: {
              name: null,
              isPrimaryKey: !0,
              outbound: k,
              compound: M,
              keyPath: E,
              autoIncrement: C,
              unique: !0,
              extractKey: cy(E)
            },
            indexes: iT(w.indexNames).map(function(m) {
              return w.index(m);
            }).map(function(m) {
              var b = m.name, v = m.unique, T = m.multiEntry, I = m.keyPath, x = {
                name: b,
                compound: en(I),
                keyPath: I,
                unique: v,
                multiEntry: T,
                extractKey: cy(I)
              };
              return R[hc(I)] = x, x;
            }),
            getIndexByKeyPath: function(m) {
              return R[hc(m)];
            }
          };
          return R[":id"] = D.primaryKey, E != null && (R[hc(E)] = D.primaryKey), D;
        })
      },
      hasGetAll: y.length > 0 && "getAll" in p.objectStore(y[0]) && !(typeof navigator < "u" && /Safari/.test(navigator.userAgent) && !/(Chrome\/|Edge\/)/.test(navigator.userAgent) && [].concat(navigator.userAgent.match(/Safari\/(\d*)/))[1] < 604)
    };
  }
  function s(h) {
    if (h.type === 3) return null;
    if (h.type === 4) throw new Error("Cannot convert never type to IDBKeyRange");
    var p = h.lower, y = h.upper, w = h.lowerOpen, E = h.upperOpen;
    return p === void 0 ? y === void 0 ? null : t.upperBound(y, !!E) : y === void 0 ? t.lowerBound(p, !!w) : t.bound(p, y, !!w, !!E);
  }
  function o(h) {
    var p = h.name;
    function y(C) {
      var M = C.trans, k = C.type, R = C.keys, D = C.values, m = C.range;
      return new Promise(function(b, v) {
        b = Zt(b);
        var T = M.objectStore(p), I = T.keyPath == null, x = k === "put" || k === "add";
        if (!x && k !== "delete" && k !== "deleteRange") throw new Error("Invalid operation type: " + k);
        var P = (R || D || { length: 1 }).length;
        if (R && D && R.length !== D.length) throw new Error("Given keys array must have same length as given values array.");
        if (P === 0) return b({
          numFailures: 0,
          failures: {},
          results: [],
          lastResult: void 0
        });
        var L, G = [], z = [], V = 0, Z = function(pe) {
          ++V, Dc(pe);
        };
        if (k === "deleteRange") {
          if (m.type === 4) return b({
            numFailures: V,
            failures: z,
            results: [],
            lastResult: void 0
          });
          m.type === 3 ? G.push(L = T.clear()) : G.push(L = T.delete(s(m)));
        } else {
          var O = x ? I ? [D, R] : [D, null] : [R, null], Y = O[0], X = O[1];
          if (x) for (var j = 0; j < P; ++j)
            G.push(L = X && X[j] !== void 0 ? T[k](Y[j], X[j]) : T[k](Y[j])), L.onerror = Z;
          else for (var j = 0; j < P; ++j)
            G.push(L = T[k](Y[j])), L.onerror = Z;
        }
        var re = function(pe) {
          var B = pe.target.result;
          G.forEach(function(N, H) {
            return N.error != null && (z[H] = N.error);
          }), b({
            numFailures: V,
            failures: z,
            results: k === "delete" ? R : G.map(function(N) {
              return N.result;
            }),
            lastResult: B
          });
        };
        L.onerror = function(pe) {
          Z(pe), re(pe);
        }, L.onsuccess = re;
      });
    }
    function w(C) {
      var M = C.trans, k = C.values, R = C.query, D = C.reverse, m = C.unique;
      return new Promise(function(b, v) {
        b = Zt(b);
        var T = R.index, I = R.range, x = M.objectStore(p), P = T.isPrimaryKey ? x : x.index(T.name), L = D ? m ? "prevunique" : "prev" : m ? "nextunique" : "next", G = k || !("openKeyCursor" in P) ? P.openCursor(s(I), L) : P.openKeyCursor(s(I), L);
        G.onerror = Ts(v), G.onsuccess = Zt(function(z) {
          var V = G.result;
          if (!V) {
            b(null);
            return;
          }
          V.___id = ++YU, V.done = !1;
          var Z = V.continue.bind(V), O = V.continuePrimaryKey;
          O && (O = O.bind(V));
          var Y = V.advance.bind(V), X = function() {
            throw new Error("Cursor not started");
          }, j = function() {
            throw new Error("Cursor not stopped");
          };
          V.trans = M, V.stop = V.continue = V.continuePrimaryKey = V.advance = X, V.fail = Zt(v), V.next = function() {
            var re = this, pe = 1;
            return this.start(function() {
              return pe-- ? re.continue() : re.stop();
            }).then(function() {
              return re;
            });
          }, V.start = function(re) {
            var pe = new Promise(function(N, H) {
              N = Zt(N), G.onerror = Ts(H), V.fail = H, V.stop = function(le) {
                V.stop = V.continue = V.continuePrimaryKey = V.advance = j, N(le);
              };
            }), B = function() {
              if (G.result) try {
                re();
              } catch (N) {
                V.fail(N);
              }
              else
                V.done = !0, V.start = function() {
                  throw new Error("Cursor behind last entry");
                }, V.stop();
            };
            return G.onsuccess = Zt(function(N) {
              G.onsuccess = B, B();
            }), V.continue = Z, V.continuePrimaryKey = O, V.advance = Y, B(), pe;
          }, b(V);
        }, v);
      });
    }
    function E(C) {
      return function(M) {
        return new Promise(function(k, R) {
          k = Zt(k);
          var D = M.trans, m = M.values, b = M.limit, v = M.query, T = b === 1 / 0 ? void 0 : b, I = v.index, x = v.range, P = D.objectStore(p), L = I.isPrimaryKey ? P : P.index(I.name), G = s(x);
          if (b === 0) return k({ result: [] });
          if (C) {
            var z = m ? L.getAll(G, T) : L.getAllKeys(G, T);
            z.onsuccess = function(Y) {
              return k({ result: Y.target.result });
            }, z.onerror = Ts(R);
          } else {
            var V = 0, Z = m || !("openKeyCursor" in L) ? L.openCursor(G) : L.openKeyCursor(G), O = [];
            Z.onsuccess = function(Y) {
              var X = Z.result;
              if (!X) return k({ result: O });
              if (O.push(m ? X.value : X.primaryKey), ++V === b) return k({ result: O });
              X.continue();
            }, Z.onerror = Ts(R);
          }
        });
      };
    }
    return {
      name: p,
      schema: h,
      mutate: y,
      getMany: function(C) {
        var M = C.trans, k = C.keys;
        return new Promise(function(R, D) {
          R = Zt(R);
          for (var m = M.objectStore(p), b = k.length, v = new Array(b), T = 0, I = 0, x, P = function(z) {
            var V = z.target;
            (v[V._pos] = V.result) != null, ++I === T && R(v);
          }, L = Ts(D), G = 0; G < b; ++G) k[G] != null && (x = m.get(k[G]), x._pos = G, x.onsuccess = P, x.onerror = L, ++T);
          T === 0 && R(v);
        });
      },
      get: function(C) {
        var M = C.trans, k = C.key;
        return new Promise(function(R, D) {
          R = Zt(R);
          var m = M.objectStore(p).get(k);
          m.onsuccess = function(b) {
            return R(b.target.result);
          }, m.onerror = Ts(D);
        });
      },
      query: E(l),
      openCursor: w,
      count: function(C) {
        var M = C.query, k = C.trans, R = M.index, D = M.range;
        return new Promise(function(m, b) {
          var v = k.objectStore(p), T = R.isPrimaryKey ? v : v.index(R.name), I = s(D), x = I ? T.count(I) : T.count();
          x.onsuccess = Zt(function(P) {
            return m(P.target.result);
          }), x.onerror = Ts(b);
        });
      }
    };
  }
  var i = r(e, n), a = i.schema, l = i.hasGetAll, u = a.tables.map(function(h) {
    return o(h);
  }), d = {};
  return u.forEach(function(h) {
    return d[h.name] = h;
  }), {
    stack: "dbcore",
    transaction: e.transaction.bind(e),
    table: function(h) {
      if (!d[h]) throw new Error("Table '".concat(h, "' not found"));
      return d[h];
    },
    MIN_KEY: -1 / 0,
    MAX_KEY: Lc(t),
    schema: a
  };
}
function QU(e, t) {
  return t.reduce(function(n, r) {
    var s = r.create;
    return wt(wt({}, n), s(n));
  }, e);
}
function ZU(e, t, n, r) {
  var s = n.IDBKeyRange;
  return n.indexedDB, { dbcore: QU(XU(t, s, r), e.dbcore) };
}
function Hh(e, t) {
  var n = t.db;
  e.core = ZU(e._middlewares, n, e._deps, t).dbcore, e.tables.forEach(function(r) {
    var s = r.name;
    e.core.schema.tables.some(function(o) {
      return o.name === s;
    }) && (r.core = e.core.table(s), e[s] instanceof e.Table && (e[s].core = r.core));
  });
}
function Gh(e, t, n, r) {
  n.forEach(function(s) {
    var o = r[s];
    t.forEach(function(i) {
      var a = Vx(i, s);
      (!a || "value" in a && a.value === void 0) && (i === e.Transaction.prototype || i instanceof e.Transaction ? jo(i, s, {
        get: function() {
          return this.table(s);
        },
        set: function(l) {
          Wx(this, s, {
            value: l,
            writable: !0,
            configurable: !0,
            enumerable: !0
          });
        }
      }) : i[s] = new e.Table(s, o));
    });
  });
}
function dy(e, t) {
  t.forEach(function(n) {
    for (var r in n) n[r] instanceof e.Table && delete n[r];
  });
}
function jU(e, t) {
  return e._cfg.version - t._cfg.version;
}
function eq(e, t, n, r) {
  var s = e._dbSchema;
  n.objectStoreNames.contains("$meta") && !s.$meta && (s.$meta = xb("$meta", hk("")[0], []), e._storeNames.push("$meta"));
  var o = e._createTransaction("readwrite", e._storeNames, s);
  o.create(n), o._completion.catch(r);
  var i = o._reject.bind(o), a = rt.transless || rt;
  ei(function() {
    if (rt.trans = o, rt.transless = a, t === 0)
      Sn(s).forEach(function(l) {
        Mb(n, l, s[l].primKey, s[l].indexes);
      }), Hh(e, n), We.follow(function() {
        return e.on.populate.fire(o);
      }).catch(i);
    else
      return Hh(e, n), nq(e, o, t).then(function(l) {
        return rq(e, l, o, n);
      }).catch(i);
  });
}
function tq(e, t) {
  fk(e._dbSchema, t), t.db.version % 10 === 0 && !t.objectStoreNames.contains("$meta") && t.db.createObjectStore("$meta").add(Math.ceil(t.db.version / 10 - 1), "version");
  var n = Wp(e, e.idbdb, t);
  Vh(e, e._dbSchema, t);
  for (var r = kb(n, e._dbSchema), s = function(u) {
    if (u.change.length || u.recreate)
      return console.warn("Unable to patch indexes of table ".concat(u.name, " because it has changes on the type of index or primary key.")), { value: void 0 };
    var d = t.objectStore(u.name);
    u.add.forEach(function(h) {
      $s && console.debug("Dexie upgrade patch: Creating missing index ".concat(u.name, ".").concat(h.src)), Wh(d, h);
    });
  }, o = 0, i = r.change; o < i.length; o++) {
    var a = i[o], l = s(a);
    if (typeof l == "object") return l.value;
  }
}
function nq(e, t, n) {
  return t.storeNames.includes("$meta") ? t.table("$meta").get("version").then(function(r) {
    return r ?? n;
  }) : We.resolve(n);
}
function rq(e, t, n, r) {
  var s = [], o = e._versions, i = e._dbSchema = Wp(e, e.idbdb, r), a = o.filter(function(u) {
    return u._cfg.version >= t;
  });
  if (a.length === 0) return We.resolve();
  a.forEach(function(u) {
    s.push(function() {
      var d = i, h = u._cfg.dbschema;
      Vh(e, d, r), Vh(e, h, r), i = e._dbSchema = h;
      var p = kb(d, h);
      p.add.forEach(function(k) {
        Mb(r, k[0], k[1].primKey, k[1].indexes);
      }), p.change.forEach(function(k) {
        if (k.recreate) throw new lt.Upgrade("Not yet support for changing primary key");
        var R = r.objectStore(k.name);
        k.add.forEach(function(D) {
          return Wh(R, D);
        }), k.change.forEach(function(D) {
          R.deleteIndex(D.name), Wh(R, D);
        }), k.del.forEach(function(D) {
          return R.deleteIndex(D);
        });
      });
      var y = u._cfg.contentUpgrade;
      if (y && u._cfg.version > t) {
        Hh(e, r), n._memoizedTables = {};
        var w = Jx(h);
        p.del.forEach(function(k) {
          w[k] = d[k];
        }), dy(e, [e.Transaction.prototype]), Gh(e, [e.Transaction.prototype], Sn(w), w), n.schema = w;
        var E = bb(y);
        E && Dl();
        var C, M = We.follow(function() {
          if (C = y(n), C && E) {
            var k = ti.bind(null, null);
            C.then(k, k);
          }
        });
        return C && typeof C.then == "function" ? We.resolve(C) : M.then(function() {
          return C;
        });
      }
    }), s.push(function(d) {
      var h = u._cfg.dbschema;
      sq(h, d), dy(e, [e.Transaction.prototype]), Gh(e, [e.Transaction.prototype], e._storeNames, e._dbSchema), n.schema = e._dbSchema;
    }), s.push(function(d) {
      e.idbdb.objectStoreNames.contains("$meta") && (Math.ceil(e.idbdb.version / 10) === u._cfg.version ? (e.idbdb.deleteObjectStore("$meta"), delete e._dbSchema.$meta, e._storeNames = e._storeNames.filter(function(h) {
        return h !== "$meta";
      })) : d.objectStore("$meta").put(u._cfg.version, "version"));
    });
  });
  function l() {
    return s.length ? We.resolve(s.shift()(n.idbtrans)).then(l) : We.resolve();
  }
  return l().then(function() {
    fk(i, r);
  });
}
function kb(e, t) {
  var n = {
    del: [],
    add: [],
    change: []
  }, r;
  for (r in e) t[r] || n.del.push(r);
  for (r in t) {
    var s = e[r], o = t[r];
    if (!s) n.add.push([r, o]);
    else {
      var i = {
        name: r,
        def: o,
        recreate: !1,
        del: [],
        add: [],
        change: []
      };
      if ("" + (s.primKey.keyPath || "") != "" + (o.primKey.keyPath || "") || s.primKey.auto !== o.primKey.auto)
        i.recreate = !0, n.change.push(i);
      else {
        var a = s.idxByName, l = o.idxByName, u = void 0;
        for (u in a) l[u] || i.del.push(u);
        for (u in l) {
          var d = a[u], h = l[u];
          d ? d.src !== h.src && i.change.push(h) : i.add.push(h);
        }
        (i.del.length > 0 || i.add.length > 0 || i.change.length > 0) && n.change.push(i);
      }
    }
  }
  return n;
}
function Mb(e, t, n, r) {
  var s = e.db.createObjectStore(t, n.keyPath ? {
    keyPath: n.keyPath,
    autoIncrement: n.auto
  } : { autoIncrement: n.auto });
  return r.forEach(function(o) {
    return Wh(s, o);
  }), s;
}
function fk(e, t) {
  Sn(e).forEach(function(n) {
    t.db.objectStoreNames.contains(n) || ($s && console.debug("Dexie: Creating missing table", n), Mb(t, n, e[n].primKey, e[n].indexes));
  });
}
function sq(e, t) {
  [].slice.call(t.db.objectStoreNames).forEach(function(n) {
    return e[n] == null && t.db.deleteObjectStore(n);
  });
}
function Wh(e, t) {
  e.createIndex(t.name, t.keyPath, {
    unique: t.unique,
    multiEntry: t.multi
  });
}
function Wp(e, t, n) {
  var r = {};
  return qp(t.objectStoreNames, 0).forEach(function(s) {
    for (var o = n.objectStore(s), i = o.keyPath, a = uy(dk(i), i || "", !0, !1, !!o.autoIncrement, i && typeof i != "string", !0), l = [], u = 0; u < o.indexNames.length; ++u) {
      var d = o.index(o.indexNames[u]);
      i = d.keyPath;
      var h = uy(d.name, i, !!d.unique, !!d.multiEntry, !1, i && typeof i != "string", !1);
      l.push(h);
    }
    r[s] = xb(s, a, l);
  }), r;
}
function oq(e, t, n) {
  e.verno = t.version / 10;
  var r = e._dbSchema = Wp(e, t, n);
  e._storeNames = qp(t.objectStoreNames, 0), Gh(e, [e._allTables], Sn(r), r);
}
function iq(e, t) {
  var n = kb(Wp(e, e.idbdb, t), e._dbSchema);
  return !(n.add.length || n.change.some(function(r) {
    return r.add.length || r.change.length;
  }));
}
function Vh(e, t, n) {
  for (var r = n.db.objectStoreNames, s = 0; s < r.length; ++s) {
    var o = r[s], i = n.objectStore(o);
    e._hasGetAll = "getAll" in i;
    for (var a = 0; a < i.indexNames.length; ++a) {
      var l = i.indexNames[a], u = i.index(l).keyPath, d = typeof u == "string" ? u : "[" + qp(u).join("+") + "]";
      if (t[o]) {
        var h = t[o].idxByName[d];
        h && (h.name = l, delete t[o].idxByName[d], t[o].idxByName[l] = h);
      }
    }
  }
  typeof navigator < "u" && /Safari/.test(navigator.userAgent) && !/(Chrome\/|Edge\/)/.test(navigator.userAgent) && Mn.WorkerGlobalScope && Mn instanceof Mn.WorkerGlobalScope && [].concat(navigator.userAgent.match(/Safari\/(\d*)/))[1] < 604 && (e._hasGetAll = !1);
}
function hk(e) {
  return e.split(",").map(function(t, n) {
    t = t.trim();
    var r = t.replace(/([&*]|\+\+)/g, ""), s = /^\[/.test(r) ? r.match(/^\[(.*)\]$/)[1].split("+") : r;
    return uy(r, s || null, /\&/.test(t), /\*/.test(t), /\+\+/.test(t), en(s), n === 0);
  });
}
var aq = (function() {
  function e() {
  }
  return e.prototype._parseStoresSpec = function(t, n) {
    Sn(t).forEach(function(r) {
      if (t[r] !== null) {
        var s = hk(t[r]), o = s.shift();
        if (o.unique = !0, o.multi) throw new lt.Schema("Primary key cannot be multi-valued");
        s.forEach(function(i) {
          if (i.auto) throw new lt.Schema("Only primary key can be marked as autoIncrement (++)");
          if (!i.keyPath) throw new lt.Schema("Index must have a name and cannot be an empty string");
        }), n[r] = xb(r, o, s);
      }
    });
  }, e.prototype.stores = function(t) {
    var n = this.db;
    this._cfg.storesSource = this._cfg.storesSource ? Dr(this._cfg.storesSource, t) : t;
    var r = n._versions, s = {}, o = {};
    return r.forEach(function(i) {
      Dr(s, i._cfg.storesSource), o = i._cfg.dbschema = {}, i._parseStoresSpec(s, o);
    }), n._dbSchema = o, dy(n, [
      n._allTables,
      n,
      n.Transaction.prototype
    ]), Gh(n, [
      n._allTables,
      n,
      n.Transaction.prototype,
      this._cfg.tables
    ], Sn(o), o), n._storeNames = Sn(o), this;
  }, e.prototype.upgrade = function(t) {
    return this._cfg.contentUpgrade = Tb(this._cfg.contentUpgrade || Ht, t), this;
  }, e;
})();
function lq(e) {
  return sd(aq.prototype, function(n) {
    this.db = e, this._cfg = {
      version: n,
      storesSource: null,
      dbschema: {},
      tables: {},
      contentUpgrade: null
    };
  });
}
function Pb(e, t) {
  var n = e._dbNamesDB;
  return n || (n = e._dbNamesDB = new Fc(Gp, {
    addons: [],
    indexedDB: e,
    IDBKeyRange: t
  }), n.version(1).stores({ dbnames: "name" })), n.table("dbnames");
}
function Rb(e) {
  return e && typeof e.databases == "function";
}
function uq(e) {
  var t = e.indexedDB, n = e.IDBKeyRange;
  return Rb(t) ? Promise.resolve(t.databases()).then(function(r) {
    return r.map(function(s) {
      return s.name;
    }).filter(function(s) {
      return s !== Gp;
    });
  }) : Pb(t, n).toCollection().primaryKeys();
}
function cq(e, t) {
  var n = e.indexedDB, r = e.IDBKeyRange;
  !Rb(n) && t !== Gp && Pb(n, r).put({ name: t }).catch(Ht);
}
function dq(e, t) {
  var n = e.indexedDB, r = e.IDBKeyRange;
  !Rb(n) && t !== Gp && Pb(n, r).delete(t).catch(Ht);
}
function fy(e) {
  return ei(function() {
    return rt.letThrough = !0, e();
  });
}
function fq() {
  if (!(!navigator.userAgentData && /Safari\//.test(navigator.userAgent) && !/Chrom(e|ium)\//.test(navigator.userAgent)) || !indexedDB.databases) return Promise.resolve();
  var e;
  return new Promise(function(t) {
    var n = function() {
      return indexedDB.databases().finally(t);
    };
    e = setInterval(n, 100), n();
  }).finally(function() {
    return clearInterval(e);
  });
}
var Ug;
function Nb(e) {
  return !("from" in e);
}
var Kn = function(e, t) {
  if (this) Dr(this, arguments.length ? {
    d: 1,
    from: e,
    to: arguments.length > 1 ? t : e
  } : { d: 0 });
  else {
    var n = new Kn();
    return e && "d" in e && Dr(n, e), n;
  }
};
Sl(Kn.prototype, (Ug = {
  add: function(e) {
    return Kh(this, e), this;
  },
  addKey: function(e) {
    return Oc(this, e, e), this;
  },
  addKeys: function(e) {
    var t = this;
    return e.forEach(function(n) {
      return Oc(t, n, n);
    }), this;
  },
  hasKey: function(e) {
    var t = zh(this).next(e).value;
    return t && xt(t.from, e) <= 0 && xt(t.to, e) >= 0;
  }
}, Ug[ty] = function() {
  return zh(this);
}, Ug));
function Oc(e, t, n) {
  var r = xt(t, n);
  if (!isNaN(r)) {
    if (r > 0) throw RangeError();
    if (Nb(e)) return Dr(e, {
      from: t,
      to: n,
      d: 1
    });
    var s = e.l, o = e.r;
    if (xt(n, e.from) < 0)
      return s ? Oc(s, t, n) : e.l = {
        from: t,
        to: n,
        d: 1,
        l: null,
        r: null
      }, aT(e);
    if (xt(t, e.to) > 0)
      return o ? Oc(o, t, n) : e.r = {
        from: t,
        to: n,
        d: 1,
        l: null,
        r: null
      }, aT(e);
    xt(t, e.from) < 0 && (e.from = t, e.l = null, e.d = o ? o.d + 1 : 1), xt(n, e.to) > 0 && (e.to = n, e.r = null, e.d = e.l ? e.l.d + 1 : 1);
    var i = !e.r;
    s && !e.l && Kh(e, s), o && i && Kh(e, o);
  }
}
function Kh(e, t) {
  function n(r, s) {
    var o = s.from, i = s.to, a = s.l, l = s.r;
    Oc(r, o, i), a && n(r, a), l && n(r, l);
  }
  Nb(t) || n(e, t);
}
function hq(e, t) {
  var n = zh(t), r = n.next();
  if (r.done) return !1;
  for (var s = r.value, o = zh(e), i = o.next(s.from), a = i.value; !r.done && !i.done; ) {
    if (xt(a.from, s.to) <= 0 && xt(a.to, s.from) >= 0) return !0;
    xt(s.from, a.from) < 0 ? s = (r = n.next(a.from)).value : a = (i = o.next(s.from)).value;
  }
  return !1;
}
function zh(e) {
  var t = Nb(e) ? null : {
    s: 0,
    n: e
  };
  return { next: function(n) {
    for (var r = arguments.length > 0; t; ) switch (t.s) {
      case 0:
        if (t.s = 1, r) for (; t.n.l && xt(n, t.n.from) < 0; ) t = {
          up: t,
          n: t.n.l,
          s: 1
        };
        else for (; t.n.l; ) t = {
          up: t,
          n: t.n.l,
          s: 1
        };
      case 1:
        if (t.s = 2, !r || xt(n, t.n.to) <= 0) return {
          value: t.n,
          done: !1
        };
      case 2:
        if (t.n.r) {
          t.s = 3, t = {
            up: t,
            n: t.n.r,
            s: 0
          };
          continue;
        }
      case 3:
        t = t.up;
    }
    return { done: !0 };
  } };
}
function aT(e) {
  var t, n, r = (((t = e.r) === null || t === void 0 ? void 0 : t.d) || 0) - (((n = e.l) === null || n === void 0 ? void 0 : n.d) || 0), s = r > 1 ? "r" : r < -1 ? "l" : "";
  if (s) {
    var o = s === "r" ? "l" : "r", i = wt({}, e), a = e[s];
    e.from = a.from, e.to = a.to, e[s] = a[s], i[s] = a[o], e[o] = i, i.d = lT(i);
  }
  e.d = lT(e);
}
function lT(e) {
  var t = e.r, n = e.l;
  return (t ? n ? Math.max(t.d, n.d) : t.d : n ? n.d : 0) + 1;
}
function Vp(e, t) {
  return Sn(t).forEach(function(n) {
    e[n] ? Kh(e[n], t[n]) : e[n] = Qx(t[n]);
  }), e;
}
function $b(e, t) {
  return e.all || t.all || Object.keys(e).some(function(n) {
    return t[n] && hq(t[n], e[n]);
  });
}
var Qi = {}, qg = {}, Hg = !1;
function of(e, t) {
  Vp(qg, e), Hg || (Hg = !0, setTimeout(function() {
    Hg = !1;
    var n = qg;
    qg = {}, Db(n, !1);
  }, 0));
}
function Db(e, t) {
  t === void 0 && (t = !1);
  var n = /* @__PURE__ */ new Set();
  if (e.all) for (var r = 0, s = Object.values(Qi); r < s.length; r++) {
    var o = s[r];
    uT(o, e, n, t);
  }
  else for (var i in e) {
    var a = /^idb\:\/\/(.*)\/(.*)\//.exec(i);
    if (a) {
      var l = a[1], u = a[2], o = Qi["idb://".concat(l, "/").concat(u)];
      o && uT(o, e, n, t);
    }
  }
  n.forEach(function(d) {
    return d();
  });
}
function uT(e, t, n, r) {
  for (var s = [], o = 0, i = Object.entries(e.queries.query); o < i.length; o++) {
    for (var a = i[o], l = a[0], u = a[1], d = [], h = 0, p = u; h < p.length; h++) {
      var y = p[h];
      $b(t, y.obsSet) ? y.subscribers.forEach(function(M) {
        return n.add(M);
      }) : r && d.push(y);
    }
    r && s.push([l, d]);
  }
  if (r) for (var w = 0, E = s; w < E.length; w++) {
    var C = E[w], l = C[0], d = C[1];
    e.queries.query[l] = d;
  }
}
function pq(e) {
  var t = e._state, n = e._deps.indexedDB;
  if (t.isBeingOpened || e.idbdb) return t.dbReadyPromise.then(function() {
    return t.dbOpenError ? sn(t.dbOpenError) : e;
  });
  t.isBeingOpened = !0, t.dbOpenError = null, t.openComplete = !1;
  var r = t.openCanceller, s = Math.round(e.verno * 10), o = !1;
  function i() {
    if (t.openCanceller !== r) throw new lt.DatabaseClosed("db.open() was cancelled");
  }
  var a = t.dbReadyResolve, l = null, u = !1, d = function() {
    return new We(function(h, p) {
      if (i(), !n) throw new lt.MissingAPI();
      var y = e.name, w = t.autoSchema || !s ? n.open(y) : n.open(y, s);
      if (!w) throw new lt.MissingAPI();
      w.onerror = Ts(p), w.onblocked = Zt(e._fireOnBlocked), w.onupgradeneeded = Zt(function(E) {
        if (l = w.transaction, t.autoSchema && !e._options.allowEmptyDB) {
          w.onerror = Dc, l.abort(), w.result.close();
          var C = n.deleteDatabase(y);
          C.onsuccess = C.onerror = Zt(function() {
            p(new lt.NoSuchDatabase("Database ".concat(y, " doesnt exist")));
          });
        } else {
          l.onerror = Ts(p);
          var M = E.oldVersion > Math.pow(2, 62) ? 0 : E.oldVersion;
          u = M < 1, e.idbdb = w.result, o && tq(e, l), eq(e, M / 10, l, p);
        }
      }, p), w.onsuccess = Zt(function() {
        l = null;
        var E = e.idbdb = w.result, C = qp(E.objectStoreNames);
        if (C.length > 0) try {
          var M = E.transaction(zU(C), "readonly");
          if (t.autoSchema) oq(e, E, M);
          else if (Vh(e, e._dbSchema, M), !iq(e, M) && !o)
            return console.warn("Dexie SchemaDiff: Schema was extended without increasing the number passed to db.version(). Dexie will add missing parts and increment native version number to workaround this."), E.close(), s = E.version + 1, o = !0, h(d());
          Hh(e, M);
        } catch {
        }
        ll.push(e), E.onversionchange = Zt(function(k) {
          t.vcFired = !0, e.on("versionchange").fire(k);
        }), E.onclose = Zt(function(k) {
          e.on("close").fire(k);
        }), u && cq(e._deps, y), h();
      }, p);
    }).catch(function(h) {
      switch (h?.name) {
        case "UnknownError":
          if (t.PR1398_maxLoop > 0)
            return t.PR1398_maxLoop--, console.warn("Dexie: Workaround for Chrome UnknownError on open()"), d();
          break;
        case "VersionError":
          if (s > 0)
            return s = 0, d();
          break;
      }
      return We.reject(h);
    });
  };
  return We.race([r, (typeof navigator > "u" ? We.resolve() : fq()).then(d)]).then(function() {
    return i(), t.onReadyBeingFired = [], We.resolve(fy(function() {
      return e.on.ready.fire(e.vip);
    })).then(function h() {
      if (t.onReadyBeingFired.length > 0) {
        var p = t.onReadyBeingFired.reduce(Tb, Ht);
        return t.onReadyBeingFired = [], We.resolve(fy(function() {
          return p(e.vip);
        })).then(h);
      }
    });
  }).finally(function() {
    t.openCanceller === r && (t.onReadyBeingFired = null, t.isBeingOpened = !1);
  }).catch(function(h) {
    t.dbOpenError = h;
    try {
      l && l.abort();
    } catch {
    }
    return r === t.openCanceller && e._close(), sn(h);
  }).finally(function() {
    t.openComplete = !0, a();
  }).then(function() {
    if (u) {
      var h = {};
      e.tables.forEach(function(p) {
        p.schema.indexes.forEach(function(y) {
          y.name && (h["idb://".concat(e.name, "/").concat(p.name, "/").concat(y.name)] = new Kn(-1 / 0, [[[]]]));
        }), h["idb://".concat(e.name, "/").concat(p.name, "/")] = h["idb://".concat(e.name, "/").concat(p.name, "/:dels")] = new Kn(-1 / 0, [[[]]]);
      }), ri(od).fire(h), Db(h, !0);
    }
    return e;
  });
}
function hy(e) {
  var t = function(i) {
    return e.next(i);
  }, n = function(i) {
    return e.throw(i);
  }, r = o(t), s = o(n);
  function o(i) {
    return function(a) {
      var l = i(a), u = l.value;
      return l.done ? u : !u || typeof u.then != "function" ? en(u) ? Promise.all(u).then(r, s) : r(u) : u.then(r, s);
    };
  }
  return o(t)();
}
function mq(e, t, n) {
  var r = arguments.length;
  if (r < 2) throw new lt.InvalidArgument("Too few arguments");
  for (var s = new Array(r - 1); --r; ) s[r - 1] = arguments[r];
  return n = s.pop(), [
    e,
    Yx(s),
    n
  ];
}
function pk(e, t, n, r, s) {
  return We.resolve().then(function() {
    var o = rt.transless || rt, i = e._createTransaction(t, n, e._dbSchema, r);
    i.explicit = !0;
    var a = {
      trans: i,
      transless: o
    };
    if (r) i.idbtrans = r.idbtrans;
    else try {
      i.create(), i.idbtrans._explicit = !0, e._state.PR1398_maxLoop = 3;
    } catch (h) {
      return h.name === Sb.InvalidState && e.isOpen() && --e._state.PR1398_maxLoop > 0 ? (console.warn("Dexie: Need to reopen db"), e.close({ disableAutoOpen: !1 }), e.open().then(function() {
        return pk(e, t, n, null, s);
      })) : sn(h);
    }
    var l = bb(s);
    l && Dl();
    var u, d = We.follow(function() {
      if (u = s.call(i, i), u)
        if (l) {
          var h = ti.bind(null, null);
          u.then(h, h);
        } else typeof u.next == "function" && typeof u.throw == "function" && (u = hy(u));
    }, a);
    return (u && typeof u.then == "function" ? We.resolve(u).then(function(h) {
      return i.active ? h : sn(new lt.PrematureCommit("Transaction committed too early. See http://bit.ly/2kdckMn"));
    }) : d.then(function() {
      return u;
    })).then(function(h) {
      return r && i._resolve(), i._completion.then(function() {
        return h;
      });
    }).catch(function(h) {
      return i._reject(h), sn(h);
    });
  });
}
function af(e, t, n) {
  for (var r = en(e) ? e.slice() : [e], s = 0; s < n; ++s) r.push(t);
  return r;
}
function gq(e) {
  return wt(wt({}, e), { table: function(t) {
    var n = e.table(t), r = n.schema, s = {}, o = [];
    function i(w, E, C) {
      var M = hc(w), k = s[M] = s[M] || [], R = w == null ? 0 : typeof w == "string" ? 1 : w.length, D = E > 0, m = wt(wt({}, C), {
        name: D ? "".concat(M, "(virtual-from:").concat(C.name, ")") : C.name,
        lowLevelIndex: C,
        isVirtual: D,
        keyTail: E,
        keyLength: R,
        extractKey: cy(w),
        unique: !D && C.unique
      });
      return k.push(m), m.isPrimaryKey || o.push(m), R > 1 && i(R === 2 ? w[0] : w.slice(0, R - 1), E + 1, C), k.sort(function(b, v) {
        return b.keyTail - v.keyTail;
      }), m;
    }
    var a = i(r.primaryKey.keyPath, 0, r.primaryKey);
    s[":id"] = [a];
    for (var l = 0, u = r.indexes; l < u.length; l++) {
      var d = u[l];
      i(d.keyPath, 0, d);
    }
    function h(w) {
      var E = s[hc(w)];
      return E && E[0];
    }
    function p(w, E) {
      return {
        type: w.type === 1 ? 2 : w.type,
        lower: af(w.lower, w.lowerOpen ? e.MAX_KEY : e.MIN_KEY, E),
        lowerOpen: !0,
        upper: af(w.upper, w.upperOpen ? e.MIN_KEY : e.MAX_KEY, E),
        upperOpen: !0
      };
    }
    function y(w) {
      var E = w.query.index;
      return E.isVirtual ? wt(wt({}, w), { query: {
        index: E.lowLevelIndex,
        range: p(w.query.range, E.keyTail)
      } }) : w;
    }
    return wt(wt({}, n), {
      schema: wt(wt({}, r), {
        primaryKey: a,
        indexes: o,
        getIndexByKeyPath: h
      }),
      count: function(w) {
        return n.count(y(w));
      },
      query: function(w) {
        return n.query(y(w));
      },
      openCursor: function(w) {
        var E = w.query.index, C = E.keyTail, M = E.isVirtual, k = E.keyLength;
        if (!M) return n.openCursor(w);
        function R(D) {
          function m(b) {
            b != null ? D.continue(af(b, w.reverse ? e.MAX_KEY : e.MIN_KEY, C)) : w.unique ? D.continue(D.key.slice(0, k).concat(w.reverse ? e.MIN_KEY : e.MAX_KEY, C)) : D.continue();
          }
          return Object.create(D, {
            continue: { value: m },
            continuePrimaryKey: { value: function(b, v) {
              D.continuePrimaryKey(af(b, e.MAX_KEY, C), v);
            } },
            primaryKey: { get: function() {
              return D.primaryKey;
            } },
            key: { get: function() {
              var b = D.key;
              return k === 1 ? b[0] : b.slice(0, k);
            } },
            value: { get: function() {
              return D.value;
            } }
          });
        }
        return n.openCursor(y(w)).then(function(D) {
          return D && R(D);
        });
      }
    });
  } });
}
var vq = {
  stack: "dbcore",
  name: "VirtualIndexMiddleware",
  level: 1,
  create: gq
};
function Lb(e, t, n, r) {
  return n = n || {}, r = r || "", Sn(e).forEach(function(s) {
    if (!ir(t, s)) n[r + s] = void 0;
    else {
      var o = e[s], i = t[s];
      if (typeof o == "object" && typeof i == "object" && o && i) {
        var a = ey(o);
        a !== ey(i) ? n[r + s] = t[s] : a === "Object" ? Lb(o, i, n, r + s + ".") : o !== i && (n[r + s] = t[s]);
      } else o !== i && (n[r + s] = t[s]);
    }
  }), Sn(t).forEach(function(s) {
    ir(e, s) || (n[r + s] = t[s]);
  }), n;
}
function Ob(e, t) {
  return t.type === "delete" ? t.keys : t.keys || t.values.map(e.extractKey);
}
var yq = {
  stack: "dbcore",
  name: "HooksMiddleware",
  level: 2,
  create: function(e) {
    return wt(wt({}, e), { table: function(t) {
      var n = e.table(t), r = n.schema.primaryKey;
      return wt(wt({}, n), { mutate: function(s) {
        var o = rt.trans, i = o.table(t).hook, a = i.deleting, l = i.creating, u = i.updating;
        switch (s.type) {
          case "add":
            if (l.fire === Ht) break;
            return o._promise("readwrite", function() {
              return d(s);
            }, !0);
          case "put":
            if (l.fire === Ht && u.fire === Ht) break;
            return o._promise("readwrite", function() {
              return d(s);
            }, !0);
          case "delete":
            if (a.fire === Ht) break;
            return o._promise("readwrite", function() {
              return d(s);
            }, !0);
          case "deleteRange":
            if (a.fire === Ht) break;
            return o._promise("readwrite", function() {
              return h(s);
            }, !0);
        }
        return n.mutate(s);
        function d(y) {
          var w = rt.trans, E = y.keys || Ob(r, y);
          if (!E) throw new Error("Keys missing");
          return y = y.type === "add" || y.type === "put" ? wt(wt({}, y), { keys: E }) : wt({}, y), y.type !== "delete" && (y.values = Oh([], y.values, !0)), y.keys && (y.keys = Oh([], y.keys, !0)), _q(n, y, E).then(function(C) {
            var M = E.map(function(k, R) {
              var D = C[R], m = {
                onerror: null,
                onsuccess: null
              };
              if (y.type === "delete") a.fire.call(m, k, D, w);
              else if (y.type === "add" || D === void 0) {
                var b = l.fire.call(m, k, y.values[R], w);
                k == null && b != null && (k = b, y.keys[R] = k, r.outbound || Pr(y.values[R], r.keyPath, k));
              } else {
                var v = Lb(D, y.values[R]), T = u.fire.call(m, v, k, D, w);
                if (T) {
                  var I = y.values[R];
                  Object.keys(T).forEach(function(x) {
                    ir(I, x) ? I[x] = T[x] : Pr(I, x, T[x]);
                  });
                }
              }
              return m;
            });
            return n.mutate(y).then(function(k) {
              for (var R = k.failures, D = k.results, m = k.numFailures, b = k.lastResult, v = 0; v < E.length; ++v) {
                var T = D ? D[v] : E[v], I = M[v];
                T == null ? I.onerror && I.onerror(R[v]) : I.onsuccess && I.onsuccess(y.type === "put" && C[v] ? y.values[v] : T);
              }
              return {
                failures: R,
                results: D,
                numFailures: m,
                lastResult: b
              };
            }).catch(function(k) {
              return M.forEach(function(R) {
                return R.onerror && R.onerror(k);
              }), Promise.reject(k);
            });
          });
        }
        function h(y) {
          return p(y.trans, y.range, 1e4);
        }
        function p(y, w, E) {
          return n.query({
            trans: y,
            values: !1,
            query: {
              index: r,
              range: w
            },
            limit: E
          }).then(function(C) {
            var M = C.result;
            return d({
              type: "delete",
              keys: M,
              trans: y
            }).then(function(k) {
              return k.numFailures > 0 ? Promise.reject(k.failures[0]) : M.length < E ? {
                failures: [],
                numFailures: 0,
                lastResult: void 0
              } : p(y, wt(wt({}, w), {
                lower: M[M.length - 1],
                lowerOpen: !0
              }), E);
            });
          });
        }
      } });
    } });
  }
};
function _q(e, t, n) {
  return t.type === "add" ? Promise.resolve([]) : e.getMany({
    trans: t.trans,
    keys: n,
    cache: "immutable"
  });
}
function mk(e, t, n) {
  try {
    if (!t || t.keys.length < e.length) return null;
    for (var r = [], s = 0, o = 0; s < t.keys.length && o < e.length; ++s)
      xt(t.keys[s], e[o]) === 0 && (r.push(n ? ji(t.values[s]) : t.values[s]), ++o);
    return r.length === e.length ? r : null;
  } catch {
    return null;
  }
}
var bq = {
  stack: "dbcore",
  level: -1,
  create: function(e) {
    return { table: function(t) {
      var n = e.table(t);
      return wt(wt({}, n), {
        getMany: function(r) {
          if (!r.cache) return n.getMany(r);
          var s = mk(r.keys, r.trans._cache, r.cache === "clone");
          return s ? We.resolve(s) : n.getMany(r).then(function(o) {
            return r.trans._cache = {
              keys: r.keys,
              values: r.cache === "clone" ? ji(o) : o
            }, o;
          });
        },
        mutate: function(r) {
          return r.type !== "add" && (r.trans._cache = null), n.mutate(r);
        }
      });
    } };
  }
};
function gk(e, t) {
  return e.trans.mode === "readonly" && !!e.subscr && !e.trans.explicit && e.trans.db._options.cache !== "disabled" && !t.schema.primaryKey.outbound;
}
function vk(e, t) {
  switch (e) {
    case "query":
      return t.values && !t.unique;
    case "get":
      return !1;
    case "getMany":
      return !1;
    case "count":
      return !1;
    case "openCursor":
      return !1;
  }
}
var wq = {
  stack: "dbcore",
  level: 0,
  name: "Observability",
  create: function(e) {
    var t = e.schema.name, n = new Kn(e.MIN_KEY, e.MAX_KEY);
    return wt(wt({}, e), {
      transaction: function(r, s, o) {
        if (rt.subscr && s !== "readonly") throw new lt.ReadOnly("Readwrite transaction in liveQuery context. Querier source: ".concat(rt.querier));
        return e.transaction(r, s, o);
      },
      table: function(r) {
        var s = e.table(r), o = s.schema, i = o.primaryKey, a = o.indexes, l = i.extractKey, u = i.outbound, d = i.autoIncrement && a.filter(function(w) {
          return w.compound && w.keyPath.includes(i.keyPath);
        }), h = wt(wt({}, s), { mutate: function(w) {
          var E, C, M = w.trans, k = w.mutatedParts || (w.mutatedParts = {}), R = function(G) {
            var z = "idb://".concat(t, "/").concat(r, "/").concat(G);
            return k[z] || (k[z] = new Kn());
          }, D = R(""), m = R(":dels"), b = w.type, v = w.type === "deleteRange" ? [w.range] : w.type === "delete" ? [w.keys] : w.values.length < 50 ? [Ob(i, w).filter(function(G) {
            return G;
          }), w.values] : [], T = v[0], I = v[1], x = w.trans._cache;
          if (en(T)) {
            D.addKeys(T);
            var P = b === "delete" || T.length === I.length ? mk(T, x) : null;
            P || m.addKeys(T), (P || I) && Sq(R, o, P, I);
          } else if (T) {
            var L = {
              from: (E = T.lower) !== null && E !== void 0 ? E : e.MIN_KEY,
              to: (C = T.upper) !== null && C !== void 0 ? C : e.MAX_KEY
            };
            m.add(L), D.add(L);
          } else
            D.add(n), m.add(n), o.indexes.forEach(function(G) {
              return R(G.name).add(n);
            });
          return s.mutate(w).then(function(G) {
            return T && (w.type === "add" || w.type === "put") && (D.addKeys(G.results), d && d.forEach(function(z) {
              for (var V = w.values.map(function(X) {
                return z.extractKey(X);
              }), Z = z.keyPath.findIndex(function(X) {
                return X === i.keyPath;
              }), O = 0, Y = G.results.length; O < Y; ++O) V[O][Z] = G.results[O];
              R(z.name).addKeys(V);
            })), M.mutatedParts = Vp(M.mutatedParts || {}, k), G;
          });
        } }), p = function(w) {
          var E, C, M = w.query, k = M.index, R = M.range;
          return [k, new Kn((E = R.lower) !== null && E !== void 0 ? E : e.MIN_KEY, (C = R.upper) !== null && C !== void 0 ? C : e.MAX_KEY)];
        }, y = {
          get: function(w) {
            return [i, new Kn(w.key)];
          },
          getMany: function(w) {
            return [i, new Kn().addKeys(w.keys)];
          },
          count: p,
          query: p,
          openCursor: p
        };
        return Sn(y).forEach(function(w) {
          h[w] = function(E) {
            var C = rt.subscr, M = !!C, k = gk(rt, s) && vk(w, E) ? E.obsSet = {} : C;
            if (M) {
              var R = function(x) {
                var P = "idb://".concat(t, "/").concat(r, "/").concat(x);
                return k[P] || (k[P] = new Kn());
              }, D = R(""), m = R(":dels"), b = y[w](E), v = b[0], T = b[1];
              if (w === "query" && v.isPrimaryKey && !E.values ? m.add(T) : R(v.name || "").add(T), !v.isPrimaryKey) if (w === "count") m.add(n);
              else {
                var I = w === "query" && u && E.values && s.query(wt(wt({}, E), { values: !1 }));
                return s[w].apply(this, arguments).then(function(x) {
                  if (w === "query") {
                    if (u && E.values) return I.then(function(z) {
                      var V = z.result;
                      return D.addKeys(V), x;
                    });
                    var P = E.values ? x.result.map(l) : x.result;
                    E.values ? D.addKeys(P) : m.addKeys(P);
                  } else if (w === "openCursor") {
                    var L = x, G = E.values;
                    return L && Object.create(L, {
                      key: { get: function() {
                        return m.addKey(L.primaryKey), L.key;
                      } },
                      primaryKey: { get: function() {
                        var z = L.primaryKey;
                        return m.addKey(z), z;
                      } },
                      value: { get: function() {
                        return G && D.addKey(L.primaryKey), L.value;
                      } }
                    });
                  }
                  return x;
                });
              }
            }
            return s[w].apply(this, arguments);
          };
        }), h;
      }
    });
  }
};
function Sq(e, t, n, r) {
  function s(o) {
    var i = e(o.name || "");
    function a(u) {
      return u != null ? o.extractKey(u) : null;
    }
    var l = function(u) {
      return o.multiEntry && en(u) ? u.forEach(function(d) {
        return i.addKey(d);
      }) : i.addKey(u);
    };
    (n || r).forEach(function(u, d) {
      var h = n && a(n[d]), p = r && a(r[d]);
      xt(h, p) !== 0 && (h != null && l(h), p != null && l(p));
    });
  }
  t.indexes.forEach(s);
}
function cT(e, t, n) {
  if (n.numFailures === 0) return t;
  if (t.type === "deleteRange") return null;
  var r = t.keys ? t.keys.length : "values" in t && t.values ? t.values.length : 1;
  if (n.numFailures === r) return null;
  var s = wt({}, t);
  return en(s.keys) && (s.keys = s.keys.filter(function(o, i) {
    return !(i in n.failures);
  })), "values" in s && en(s.values) && (s.values = s.values.filter(function(o, i) {
    return !(i in n.failures);
  })), s;
}
function Tq(e, t) {
  return t.lower === void 0 ? !0 : t.lowerOpen ? xt(e, t.lower) > 0 : xt(e, t.lower) >= 0;
}
function Eq(e, t) {
  return t.upper === void 0 ? !0 : t.upperOpen ? xt(e, t.upper) < 0 : xt(e, t.upper) <= 0;
}
function Gg(e, t) {
  return Tq(e, t) && Eq(e, t);
}
function dT(e, t, n, r, s, o) {
  if (!n || n.length === 0) return e;
  var i = t.query.index, a = i.multiEntry, l = t.query.range, u = r.schema.primaryKey.extractKey, d = i.extractKey, h = (i.lowLevelIndex || i).extractKey, p = n.reduce(function(y, w) {
    var E = y, C = [];
    if (w.type === "add" || w.type === "put")
      for (var M = new Kn(), k = w.values.length - 1; k >= 0; --k) {
        var R = w.values[k], D = u(R);
        if (!M.hasKey(D)) {
          var m = d(R);
          (a && en(m) ? m.some(function(x) {
            return Gg(x, l);
          }) : Gg(m, l)) && (M.addKey(D), C.push(R));
        }
      }
    switch (w.type) {
      case "add":
        var b = new Kn().addKeys(t.values ? y.map(function(x) {
          return u(x);
        }) : y);
        E = y.concat(t.values ? C.filter(function(x) {
          var P = u(x);
          return b.hasKey(P) ? !1 : (b.addKey(P), !0);
        }) : C.map(function(x) {
          return u(x);
        }).filter(function(x) {
          return b.hasKey(x) ? !1 : (b.addKey(x), !0);
        }));
        break;
      case "put":
        var v = new Kn().addKeys(w.values.map(function(x) {
          return u(x);
        }));
        E = y.filter(function(x) {
          return !v.hasKey(t.values ? u(x) : x);
        }).concat(t.values ? C : C.map(function(x) {
          return u(x);
        }));
        break;
      case "delete":
        var T = new Kn().addKeys(w.keys);
        E = y.filter(function(x) {
          return !T.hasKey(t.values ? u(x) : x);
        });
        break;
      case "deleteRange":
        var I = w.range;
        E = y.filter(function(x) {
          return !Gg(u(x), I);
        });
        break;
    }
    return E;
  }, e);
  return p === e ? e : (p.sort(function(y, w) {
    return xt(h(y), h(w)) || xt(u(y), u(w));
  }), t.limit && t.limit < 1 / 0 && (p.length > t.limit ? p.length = t.limit : e.length === t.limit && p.length < t.limit && (s.dirty = !0)), o ? Object.freeze(p) : p);
}
function fT(e, t) {
  return xt(e.lower, t.lower) === 0 && xt(e.upper, t.upper) === 0 && !!e.lowerOpen == !!t.lowerOpen && !!e.upperOpen == !!t.upperOpen;
}
function Cq(e, t, n, r) {
  if (e === void 0) return t !== void 0 ? -1 : 0;
  if (t === void 0) return 1;
  var s = xt(e, t);
  if (s === 0) {
    if (n && r) return 0;
    if (n) return 1;
    if (r) return -1;
  }
  return s;
}
function Aq(e, t, n, r) {
  if (e === void 0) return t !== void 0 ? 1 : 0;
  if (t === void 0) return -1;
  var s = xt(e, t);
  if (s === 0) {
    if (n && r) return 0;
    if (n) return -1;
    if (r) return 1;
  }
  return s;
}
function Iq(e, t) {
  return Cq(e.lower, t.lower, e.lowerOpen, t.lowerOpen) <= 0 && Aq(e.upper, t.upper, e.upperOpen, t.upperOpen) >= 0;
}
function xq(e, t, n, r) {
  var s = Qi["idb://".concat(e, "/").concat(t)];
  if (!s) return [];
  var o = s.queries[n];
  if (!o) return [
    null,
    !1,
    s,
    null
  ];
  var i = o[(r.query ? r.query.index.name : null) || ""];
  if (!i) return [
    null,
    !1,
    s,
    null
  ];
  switch (n) {
    case "query":
      var a = i.find(function(u) {
        return u.req.limit === r.limit && u.req.values === r.values && fT(u.req.query.range, r.query.range);
      });
      return a ? [
        a,
        !0,
        s,
        i
      ] : [
        i.find(function(u) {
          return ("limit" in u.req ? u.req.limit : 1 / 0) >= r.limit && (r.values ? u.req.values : !0) && Iq(u.req.query.range, r.query.range);
        }),
        !1,
        s,
        i
      ];
    case "count":
      var l = i.find(function(u) {
        return fT(u.req.query.range, r.query.range);
      });
      return [
        l,
        !!l,
        s,
        i
      ];
  }
}
function kq(e, t, n, r) {
  e.subscribers.add(n), r.addEventListener("abort", function() {
    e.subscribers.delete(n), e.subscribers.size === 0 && Mq(e, t);
  });
}
function Mq(e, t) {
  setTimeout(function() {
    e.subscribers.size === 0 && Mi(t, e);
  }, 3e3);
}
var Pq = {
  stack: "dbcore",
  level: 0,
  name: "Cache",
  create: function(e) {
    var t = e.schema.name;
    return wt(wt({}, e), {
      transaction: function(n, r, s) {
        var o = e.transaction(n, r, s);
        if (r === "readwrite") {
          var i = new AbortController(), a = i.signal, l = function(u) {
            return function() {
              if (i.abort(), r === "readwrite") {
                for (var d = /* @__PURE__ */ new Set(), h = 0, p = n; h < p.length; h++) {
                  var y = p[h], w = Qi["idb://".concat(t, "/").concat(y)];
                  if (w) {
                    var E = e.table(y), C = w.optimisticOps.filter(function(G) {
                      return G.trans === o;
                    });
                    if (o._explicit && u && o.mutatedParts) for (var M = 0, k = Object.values(w.queries.query); M < k.length; M++)
                      for (var R = k[M], D = 0, m = R.slice(); D < m.length; D++) {
                        var b = m[D];
                        $b(b.obsSet, o.mutatedParts) && (Mi(R, b), b.subscribers.forEach(function(G) {
                          return d.add(G);
                        }));
                      }
                    else if (C.length > 0) {
                      w.optimisticOps = w.optimisticOps.filter(function(G) {
                        return G.trans !== o;
                      });
                      for (var v = 0, T = Object.values(w.queries.query); v < T.length; v++)
                        for (var R = T[v], I = 0, x = R.slice(); I < x.length; I++) {
                          var b = x[I];
                          if (b.res != null && o.mutatedParts) if (u && !b.dirty) {
                            var P = Object.isFrozen(b.res), L = dT(b.res, b.req, C, E, b, P);
                            b.dirty ? (Mi(R, b), b.subscribers.forEach(function(V) {
                              return d.add(V);
                            })) : L !== b.res && (b.res = L, b.promise = We.resolve({ result: L }));
                          } else
                            b.dirty && Mi(R, b), b.subscribers.forEach(function(V) {
                              return d.add(V);
                            });
                        }
                    }
                  }
                }
                d.forEach(function(G) {
                  return G();
                });
              }
            };
          };
          o.addEventListener("abort", l(!1), { signal: a }), o.addEventListener("error", l(!1), { signal: a }), o.addEventListener("complete", l(!0), { signal: a });
        }
        return o;
      },
      table: function(n) {
        var r = e.table(n), s = r.schema.primaryKey;
        return wt(wt({}, r), {
          mutate: function(o) {
            var i = rt.trans;
            if (s.outbound || i.db._options.cache === "disabled" || i.explicit || i.idbtrans.mode !== "readwrite") return r.mutate(o);
            var a = Qi["idb://".concat(t, "/").concat(n)];
            if (!a) return r.mutate(o);
            var l = r.mutate(o);
            return (o.type === "add" || o.type === "put") && (o.values.length >= 50 || Ob(s, o).some(function(u) {
              return u == null;
            })) ? l.then(function(u) {
              var d = cT(a, wt(wt({}, o), { values: o.values.map(function(h, p) {
                var y;
                if (u.failures[p]) return h;
                var w = !((y = s.keyPath) === null || y === void 0) && y.includes(".") ? ji(h) : wt({}, h);
                return Pr(w, s.keyPath, u.results[p]), w;
              }) }), u);
              a.optimisticOps.push(d), queueMicrotask(function() {
                return o.mutatedParts && of(o.mutatedParts);
              });
            }) : (a.optimisticOps.push(o), o.mutatedParts && of(o.mutatedParts), l.then(function(u) {
              if (u.numFailures > 0) {
                Mi(a.optimisticOps, o);
                var d = cT(a, o, u);
                d && a.optimisticOps.push(d), o.mutatedParts && of(o.mutatedParts);
              }
            }), l.catch(function() {
              Mi(a.optimisticOps, o), o.mutatedParts && of(o.mutatedParts);
            })), l;
          },
          query: function(o) {
            var i;
            if (!gk(rt, r) || !vk("query", o)) return r.query(o);
            var a = ((i = rt.trans) === null || i === void 0 ? void 0 : i.db._options.cache) === "immutable", l = rt, u = l.requery, d = l.signal, h = xq(t, n, "query", o), p = h[0], y = h[1], w = h[2], E = h[3];
            if (p && y) p.obsSet = o.obsSet;
            else {
              var C = r.query(o).then(function(M) {
                var k = M.result;
                if (p && (p.res = k), a) {
                  for (var R = 0, D = k.length; R < D; ++R) Object.freeze(k[R]);
                  Object.freeze(k);
                } else M.result = ji(k);
                return M;
              }).catch(function(M) {
                return E && p && Mi(E, p), Promise.reject(M);
              });
              p = {
                obsSet: o.obsSet,
                promise: C,
                subscribers: /* @__PURE__ */ new Set(),
                type: "query",
                req: o,
                dirty: !1
              }, E ? E.push(p) : (E = [p], w || (w = Qi["idb://".concat(t, "/").concat(n)] = {
                queries: {
                  query: {},
                  count: {}
                },
                objs: /* @__PURE__ */ new Map(),
                optimisticOps: [],
                unsignaledParts: {}
              }), w.queries.query[o.query.index.name || ""] = E);
            }
            return kq(p, E, u, d), p.promise.then(function(M) {
              return { result: dT(M.result, o, w?.optimisticOps, r, p, a) };
            });
          }
        });
      }
    });
  }
};
function lf(e, t) {
  return new Proxy(e, { get: function(n, r, s) {
    return r === "db" ? t : Reflect.get(n, r, s);
  } });
}
var Fc = (function() {
  function e(t, n) {
    var r = this;
    this._middlewares = {}, this.verno = 0;
    var s = e.dependencies;
    this._options = n = wt({
      addons: e.addons,
      autoOpen: !0,
      indexedDB: s.indexedDB,
      IDBKeyRange: s.IDBKeyRange,
      cache: "cloned"
    }, n), this._deps = {
      indexedDB: n.indexedDB,
      IDBKeyRange: n.IDBKeyRange
    };
    var o = n.addons;
    this._dbSchema = {}, this._versions = [], this._storeNames = [], this._allTables = {}, this.idbdb = null, this._novip = this;
    var i = {
      dbOpenError: null,
      isBeingOpened: !1,
      onReadyBeingFired: null,
      openComplete: !1,
      dbReadyResolve: Ht,
      dbReadyPromise: null,
      cancelOpen: Ht,
      openCanceller: null,
      autoSchema: !0,
      PR1398_maxLoop: 3,
      autoOpen: n.autoOpen
    };
    i.dbReadyPromise = new We(function(l) {
      i.dbReadyResolve = l;
    }), i.openCanceller = new We(function(l, u) {
      i.cancelOpen = u;
    }), this._state = i, this.name = t, this.on = rd(this, "populate", "blocked", "versionchange", "close", { ready: [Tb, Ht] }), this.on.ready.subscribe = Kx(this.on.ready.subscribe, function(l) {
      return function(u, d) {
        e.vip(function() {
          var h = r._state;
          if (h.openComplete)
            h.dbOpenError || We.resolve().then(u), d && l(u);
          else if (h.onReadyBeingFired)
            h.onReadyBeingFired.push(u), d && l(u);
          else {
            l(u);
            var p = r;
            d || l(function y() {
              p.on.ready.unsubscribe(u), p.on.ready.unsubscribe(y);
            });
          }
        });
      };
    }), this.Collection = FU(this), this.Table = $U(this), this.Transaction = KU(this), this.Version = lq(this), this.WhereClause = WU(this), this.on("versionchange", function(l) {
      l.newVersion > 0 ? console.warn("Another connection wants to upgrade database '".concat(r.name, "'. Closing db now to resume the upgrade.")) : console.warn("Another connection wants to delete database '".concat(r.name, "'. Closing db now to resume the delete request.")), r.close({ disableAutoOpen: !1 });
    }), this.on("blocked", function(l) {
      !l.newVersion || l.newVersion < l.oldVersion ? console.warn("Dexie.delete('".concat(r.name, "') was blocked")) : console.warn("Upgrade '".concat(r.name, "' blocked by other connection holding version ").concat(l.oldVersion / 10));
    }), this._maxKey = Lc(n.IDBKeyRange), this._createTransaction = function(l, u, d, h) {
      return new r.Transaction(l, u, d, r._options.chromeTransactionDurability, h);
    }, this._fireOnBlocked = function(l) {
      r.on("blocked").fire(l), ll.filter(function(u) {
        return u.name === r.name && u !== r && !u._state.vcFired;
      }).map(function(u) {
        return u.on("versionchange").fire(l);
      });
    }, this.use(bq), this.use(Pq), this.use(wq), this.use(vq), this.use(yq);
    var a = new Proxy(this, { get: function(l, u, d) {
      if (u === "_vip") return !0;
      if (u === "table") return function(p) {
        return lf(r.table(p), a);
      };
      var h = Reflect.get(l, u, d);
      return h instanceof lk ? lf(h, a) : u === "tables" ? h.map(function(p) {
        return lf(p, a);
      }) : u === "_createTransaction" ? function() {
        return lf(h.apply(this, arguments), a);
      } : h;
    } });
    this.vip = a, o.forEach(function(l) {
      return l(r);
    });
  }
  return e.prototype.version = function(t) {
    if (isNaN(t) || t < 0.1) throw new lt.Type("Given version is not a positive number");
    if (t = Math.round(t * 10) / 10, this.idbdb || this._state.isBeingOpened) throw new lt.Schema("Cannot add version when database is open");
    this.verno = Math.max(this.verno, t);
    var n = this._versions, r = n.filter(function(s) {
      return s._cfg.version === t;
    })[0];
    return r || (r = new this.Version(t), n.push(r), n.sort(jU), r.stores({}), this._state.autoSchema = !1, r);
  }, e.prototype._whenReady = function(t) {
    var n = this;
    return this.idbdb && (this._state.openComplete || rt.letThrough || this._vip) ? t() : new We(function(r, s) {
      if (n._state.openComplete) return s(new lt.DatabaseClosed(n._state.dbOpenError));
      if (!n._state.isBeingOpened) {
        if (!n._state.autoOpen) {
          s(new lt.DatabaseClosed());
          return;
        }
        n.open().catch(Ht);
      }
      n._state.dbReadyPromise.then(r, s);
    }).then(t);
  }, e.prototype.use = function(t) {
    var n = t.stack, r = t.create, s = t.level, o = t.name;
    o && this.unuse({
      stack: n,
      name: o
    });
    var i = this._middlewares[n] || (this._middlewares[n] = []);
    return i.push({
      stack: n,
      create: r,
      level: s ?? 10,
      name: o
    }), i.sort(function(a, l) {
      return a.level - l.level;
    }), this;
  }, e.prototype.unuse = function(t) {
    var n = t.stack, r = t.name, s = t.create;
    return n && this._middlewares[n] && (this._middlewares[n] = this._middlewares[n].filter(function(o) {
      return s ? o.create !== s : r ? o.name !== r : !1;
    })), this;
  }, e.prototype.open = function() {
    var t = this;
    return ta(Vo, function() {
      return pq(t);
    });
  }, e.prototype._close = function() {
    var t = this._state, n = ll.indexOf(this);
    if (n >= 0 && ll.splice(n, 1), this.idbdb) {
      try {
        this.idbdb.close();
      } catch {
      }
      this.idbdb = null;
    }
    t.isBeingOpened || (t.dbReadyPromise = new We(function(r) {
      t.dbReadyResolve = r;
    }), t.openCanceller = new We(function(r, s) {
      t.cancelOpen = s;
    }));
  }, e.prototype.close = function(t) {
    var n = (t === void 0 ? { disableAutoOpen: !0 } : t).disableAutoOpen, r = this._state;
    n ? (r.isBeingOpened && r.cancelOpen(new lt.DatabaseClosed()), this._close(), r.autoOpen = !1, r.dbOpenError = new lt.DatabaseClosed()) : (this._close(), r.autoOpen = this._options.autoOpen || r.isBeingOpened, r.openComplete = !1, r.dbOpenError = null);
  }, e.prototype.delete = function(t) {
    var n = this;
    t === void 0 && (t = { disableAutoOpen: !0 });
    var r = arguments.length > 0 && typeof arguments[0] != "object", s = this._state;
    return new We(function(o, i) {
      var a = function() {
        n.close(t);
        var l = n._deps.indexedDB.deleteDatabase(n.name);
        l.onsuccess = Zt(function() {
          dq(n._deps, n.name), o();
        }), l.onerror = Ts(i), l.onblocked = n._fireOnBlocked;
      };
      if (r) throw new lt.InvalidArgument("Invalid closeOptions argument to db.delete()");
      s.isBeingOpened ? s.dbReadyPromise.then(a) : a();
    });
  }, e.prototype.backendDB = function() {
    return this.idbdb;
  }, e.prototype.isOpen = function() {
    return this.idbdb !== null;
  }, e.prototype.hasBeenClosed = function() {
    var t = this._state.dbOpenError;
    return t && t.name === "DatabaseClosed";
  }, e.prototype.hasFailed = function() {
    return this._state.dbOpenError !== null;
  }, e.prototype.dynamicallyOpened = function() {
    return this._state.autoSchema;
  }, Object.defineProperty(e.prototype, "tables", {
    get: function() {
      var t = this;
      return Sn(this._allTables).map(function(n) {
        return t._allTables[n];
      });
    },
    enumerable: !1,
    configurable: !0
  }), e.prototype.transaction = function() {
    var t = mq.apply(this, arguments);
    return this._transaction.apply(this, t);
  }, e.prototype._transaction = function(t, n, r) {
    var s = this, o = rt.trans;
    (!o || o.db !== this || t.indexOf("!") !== -1) && (o = null);
    var i = t.indexOf("?") !== -1;
    t = t.replace("!", "").replace("?", "");
    var a, l;
    try {
      if (l = n.map(function(d) {
        var h = d instanceof s.Table ? d.name : d;
        if (typeof h != "string") throw new TypeError("Invalid table argument to Dexie.transaction(). Only Table or String are allowed");
        return h;
      }), t == "r" || t === Dg) a = Dg;
      else if (t == "rw" || t == Lg) a = Lg;
      else throw new lt.InvalidArgument("Invalid transaction mode: " + t);
      if (o) {
        if (o.mode === Dg && a === Lg) if (i) o = null;
        else throw new lt.SubTransaction("Cannot enter a sub-transaction with READWRITE mode when parent transaction is READONLY");
        o && l.forEach(function(d) {
          if (o && o.storeNames.indexOf(d) === -1) if (i) o = null;
          else throw new lt.SubTransaction("Table " + d + " not included in parent transaction.");
        }), i && o && !o.active && (o = null);
      }
    } catch (d) {
      return o ? o._promise(null, function(h, p) {
        p(d);
      }) : sn(d);
    }
    var u = pk.bind(null, this, a, l, o, r);
    return o ? o._promise(a, u, "lock") : rt.trans ? ta(rt.transless, function() {
      return s._whenReady(u);
    }) : this._whenReady(u);
  }, e.prototype.table = function(t) {
    if (!ir(this._allTables, t)) throw new lt.InvalidTable("Table ".concat(t, " does not exist"));
    return this._allTables[t];
  }, e;
})(), Rq = typeof Symbol < "u" && "observable" in Symbol ? Symbol.observable : "@@observable", Nq = (function() {
  function e(t) {
    this._subscribe = t;
  }
  return e.prototype.subscribe = function(t, n, r) {
    return this._subscribe(!t || typeof t == "function" ? {
      next: t,
      error: n,
      complete: r
    } : t);
  }, e.prototype[Rq] = function() {
    return this;
  }, e;
})(), Jh;
try {
  Jh = {
    indexedDB: Mn.indexedDB || Mn.mozIndexedDB || Mn.webkitIndexedDB || Mn.msIndexedDB,
    IDBKeyRange: Mn.IDBKeyRange || Mn.webkitIDBKeyRange
  };
} catch {
  Jh = {
    indexedDB: null,
    IDBKeyRange: null
  };
}
function $q(e) {
  var t = !1, n, r = new Nq(function(s) {
    var o = bb(e);
    function i(M) {
      var k = Nl();
      try {
        o && Dl();
        var R = ei(e, M);
        return o && (R = R.finally(ti)), R;
      } finally {
        k && $l();
      }
    }
    var a = !1, l, u = {}, d = {}, h = {
      get closed() {
        return a;
      },
      unsubscribe: function() {
        a || (a = !0, l && l.abort(), p && ri.storagemutated.unsubscribe(E));
      }
    };
    s.start && s.start(h);
    var p = !1, y = function() {
      return $g(C);
    };
    function w() {
      return $b(d, u);
    }
    var E = function(M) {
      Vp(u, M), w() && y();
    }, C = function() {
      if (!(a || !Jh.indexedDB)) {
        u = {};
        var M = {};
        l && l.abort(), l = new AbortController();
        var k = {
          subscr: M,
          signal: l.signal,
          requery: y,
          querier: e,
          trans: null
        }, R = i(k);
        Promise.resolve(R).then(function(D) {
          t = !0, n = D, !(a || k.signal.aborted) && (u = {}, d = M, !aU(d) && !p && (ri(od, E), p = !0), $g(function() {
            return !a && s.next && s.next(D);
          }));
        }, function(D) {
          t = !1, ["DatabaseClosedError", "AbortError"].includes(D?.name) || a || $g(function() {
            a || s.error && s.error(D);
          });
        });
      }
    };
    return setTimeout(y, 0), h;
  });
  return r.hasValue = function() {
    return t;
  }, r.getValue = function() {
    return n;
  }, r;
}
var Oi = Fc;
Sl(Oi, wt(wt({}, Hp), {
  delete: function(e) {
    return new Oi(e, { addons: [] }).delete();
  },
  exists: function(e) {
    return new Oi(e, { addons: [] }).open().then(function(t) {
      return t.close(), !0;
    }).catch("NoSuchDatabaseError", function() {
      return !1;
    });
  },
  getDatabaseNames: function(e) {
    try {
      return uq(Oi.dependencies).then(e);
    } catch {
      return sn(new lt.MissingAPI());
    }
  },
  defineClass: function() {
    function e(t) {
      Dr(this, t);
    }
    return e;
  },
  ignoreTransaction: function(e) {
    return rt.trans ? ta(rt.transless, e) : e();
  },
  vip: fy,
  async: function(e) {
    return function() {
      try {
        var t = hy(e.apply(this, arguments));
        return !t || typeof t.then != "function" ? We.resolve(t) : t;
      } catch (n) {
        return sn(n);
      }
    };
  },
  spawn: function(e, t, n) {
    try {
      var r = hy(e.apply(n, t || []));
      return !r || typeof r.then != "function" ? We.resolve(r) : r;
    } catch (s) {
      return sn(s);
    }
  },
  currentTransaction: { get: function() {
    return rt.trans || null;
  } },
  waitFor: function(e, t) {
    var n = We.resolve(typeof e == "function" ? Oi.ignoreTransaction(e) : e).timeout(t || 6e4);
    return rt.trans ? rt.trans.waitFor(n) : n;
  },
  Promise: We,
  debug: {
    get: function() {
      return $s;
    },
    set: function(e) {
      ek(e);
    }
  },
  derive: Pl,
  extend: Dr,
  props: Sl,
  override: Kx,
  Events: rd,
  on: ri,
  liveQuery: $q,
  extendObservabilitySet: Vp,
  getByKeyPath: ao,
  setByKeyPath: Pr,
  delByKeyPath: sU,
  shallowClone: Jx,
  deepClone: ji,
  getObjectDiff: Lb,
  cmp: xt,
  asap: zx,
  minKey: ay,
  addons: [],
  connections: ll,
  errnames: Sb,
  dependencies: Jh,
  cache: Qi,
  semVer: tT,
  version: tT.split(".").map(function(e) {
    return parseInt(e);
  }).reduce(function(e, t, n) {
    return e + t / Math.pow(10, n * 2);
  })
}));
Oi.maxKey = Lc(Oi.dependencies.IDBKeyRange);
typeof dispatchEvent < "u" && typeof addEventListener < "u" && (ri(od, function(e) {
  if (!qo) {
    var t = new CustomEvent(ly, { detail: e });
    qo = !0, dispatchEvent(t), qo = !1;
  }
}), addEventListener(ly, function(e) {
  var t = e.detail;
  qo || Fb(t);
}));
function Fb(e) {
  var t = qo;
  try {
    qo = !0, ri.storagemutated.fire(e), Db(e, !0);
  } finally {
    qo = t;
  }
}
var qo = !1, Fo, py = function() {
};
typeof BroadcastChannel < "u" && (py = function() {
  Fo = new BroadcastChannel(ly), Fo.onmessage = function(e) {
    return e.data && Fb(e.data);
  };
}, py(), typeof Fo.unref == "function" && Fo.unref(), ri(od, function(e) {
  qo || Fo.postMessage(e);
}));
typeof addEventListener < "u" && (addEventListener("pagehide", function(e) {
  if (!Fc.disableBfCache && e.persisted) {
    $s && console.debug("Dexie: handling persisted pagehide"), Fo?.close();
    for (var t = 0, n = ll; t < n.length; t++) n[t].close({ disableAutoOpen: !1 });
  }
}), addEventListener("pageshow", function(e) {
  !Fc.disableBfCache && e.persisted && ($s && console.debug("Dexie: handling persisted pageshow"), py(), Fb({ all: new Kn(-1 / 0, [[]]) }));
}));
We.rejectionMapper = hU;
ek($s);
var Kp = "littlewhitebox-assistant-default";
function yk(e = {}) {
  return {
    includeMemory: e.includeMemory !== !1,
    includeCartography: e.includeCartography !== !1
  };
}
function _s(e, t = []) {
  const n = t.map((r) => Zi(r)).filter(Boolean);
  return n.length ? ["## " + e, n.join(`
`)].join(`
`) : "";
}
function Dq(e = {}) {
  const { includeMemory: t, includeCartography: n } = yk(e), r = [t ? "memory Markdown files" : "", n ? "structured spatial state" : ""].filter(Boolean), s = t && n ? "facts, continuity, long-lived state, turn notes, and spatial state." : t ? "facts, continuity, long-lived state, and turn notes." : n ? "spatial boundaries, positions, routes, and scene geometry." : "evidence and session context.", o = t && n ? "Maintain continuity, state, places, time, possessions, hooks, turn notes, and spatial changes when they actually matter." : t ? "Maintain continuity, state, places, time, possessions, hooks, and turn notes when they actually matter." : n ? "Maintain the current scene map with boundaries, routes, positions, and hazards." : "Help the user inspect backstage materials with evidence when asked.", i = t && n ? "When the user asks about memory, continuity, or the map, answer with evidence. If uncertain, check source text instead of bluffing." : t ? "When the user asks about memory or continuity, answer with evidence. If uncertain, check source text instead of bluffing." : n ? "When the user asks about the map or spatial state, answer with evidence. If uncertain, check source text instead of bluffing." : "When the user asks about backstage materials, answer with evidence. If uncertain, check source text instead of bluffing.", a = [
    "You are the backstage steward for the current LittleWhiteTavern RP session.",
    `The user and character stay on stage; you organize what has already happened behind the stage: ${s}`,
    r.length ? `You maintain the current session's ${r.join(" and ")}. The main RP chat handles immersion; you turn source text into materials that can be retrieved, corrected, and carried forward.` : "The main RP chat handles immersion; you help inspect backstage evidence and explain current context when asked.",
    "Automatic after-turn runs and manual manager chat share the same identity and evidence standard. Only the trigger and this-turn input differ."
  ], l = [
    t ? "Condense already-happened RP source text into durable memory instead of dumping whole chat logs back into the prompt." : "",
    o,
    r.length ? "Keep records readable, editable, and traceable. Important facts should land in files or state docs, not only in your final reply." : "Use evidence first. Read source text or existing records before answering when verification matters.",
    i
  ], u = [
    "The current session is the only work scope.",
    [
      t ? "current-session `memory/...` files" : "",
      n ? "structured state" : "",
      "manager chat",
      "RP source evidence"
    ].filter(Boolean).join(", ") + " all belong to this work scope.",
    r.length ? "RP source text is the factual source. Background memory and structured state are derived materials. If they conflict, verify with ChatHistory first and then repair the derived materials." : "RP source text is the factual source. Existing files are only supporting materials, and source text wins when they conflict.",
    "Resident injections are only a snapshot of current materials, not the full chat history. Treat any source text you have not read as unverified until you check it."
  ], d = [
    t ? "`[Resident Memory Files]` usually contains `memory/session.md` and `memory/state.md`." : "",
    t ? "Automatic after-turn runs also receive this turn's user/assistant source text and a suggested turn-note path. If this turn needs a turn file, maintain it with normal MemoryWrite or MemoryEdit Markdown updates." : "",
    "Manual manager chat receives the manager's own conversation history and the current user question. RP source text is not fully preloaded; use ChatHistory when needed."
  ], h = t ? [
    "You may operate on current-session `memory/...` Markdown files only through Memory tools.",
    "`memory/session.md` tracks long-running plot continuity and pressure. `memory/state.md` tracks facts and states that still hold. `memory/turns/*.md` are lightweight per-turn notes and history fallback, not another durable summary layer.",
    "Automatic after-turn runs receive a suggested turn-note path. When the user asks to correct memory, manager chat may also read and modify existing turn files.",
    "These Markdown files are for future reading and retrieval, not a fixed database schema. Headings, paragraphs, and style are controlled by the assistant preset."
  ] : [], p = [
    [
      "First decide what this turn is: automatic after-turn maintenance, a user asking for manager help",
      t ? ", a user asking to correct memory" : "",
      n ? ", or a user asking to inspect or change the map" : "",
      "."
    ].join(""),
    "Use tools when you need evidence or need to change stored materials. Save only through the tools that are currently available.",
    "Read the current state or the source RP first, then make the smallest necessary change. Do not blindly repeat a failed tool call.",
    t ? "In automatic after-turn runs, create or update the suggested turn note when useful, then sync `memory/session.md` and `memory/state.md` only when the turn changed durable continuity or durable state." : "",
    t && n ? "The map is extra spatial state. It does not replace written memory, and turn notes do not replace durable memory." : "",
    n ? "For automatic map maintenance, always start with StateRead summary and inspect `meta.status`. If it is still `uninitialized`, initialize as soon as this turn establishes a clear current scene. If it is already `active`, apply incremental map changes only for confirmed spatial changes this turn." : "",
    "In manual manager chat, answer the user question first. Write memory or state only when the user asks for a change, or when you verified a real error or omission.",
    "When a tool fails, adjust the path, arguments, or strategy before trying again."
  ], y = [
    t ? "When explaining existing memory or answering a user question, read the relevant memory files first. Write only when the user explicitly asks for changes, or when you confirm a real error or omission." : "",
    t ? "To verify memory against the RP source text, gather evidence with ChatHistory recent/range/grep first, then repair the file with MemoryRead or MemoryEdit." : "",
    t ? "Use MemoryGrep to ask whether a fact already exists in memory. Use ChatHistory grep to ask whether something happened in the RP source text. Match the search scope to the question." : "",
    "If you know message order, use ChatHistory range. If you only know a keyword, use ChatHistory grep. If you only need recent continuity, use ChatHistory recent.",
    n ? "When maintaining structured state, start with StateRead summary. Use elements or element when you need current ids; read document only when you truly need the full structure." : "",
    n ? 'StateRead summary tells you whether the map is `uninitialized` or `active`, and whether this turn calls for initialization or incremental maintenance. Do not decide whether to read based only on your own guess about "whether anything changed." ' : "",
    n ? "Structured state should record only confirmed spatial changes from this turn. Unknown rooms, future routes, and unconfirmed details should stay unwritten until the RP actually confirms them." : "",
    t ? "Keep durable Markdown files clear. Update facts that still hold, and do not write uncertain claims as if they were settled facts." : ""
  ], w = n ? [
    "State tools maintain structured spatial state for the current session. The default document is `tavern.map/main`. New sessions already include a seed map. Read StateRead first, inspect `meta.status` and `meta.hint`, then decide whether to initialize or maintain incrementally. Do not skip the read step.",
    "The map is a spatial relation view of the current scene, not a board of floating text labels. Your job is to project what already happened into a flat layout with boundaries, direction, focus, and proportion.",
    "When you read spatial information, answer these questions first: what defines the boundary, where are the entrances and exits, where is the current player or viewpoint focus, what occupies interactive space, and which directions remain unexplored.",
    "Place the most certain anchors first, such as outer room walls, a main road, a river, corridor edges, or the current location. Place other elements relative to those anchors.",
    "Use the default orientation north-up: north = smaller y, south = larger y, west = smaller x, east = larger x. If the narration only says left/right/front/back, choose a reasonable facing and keep it consistent inside one map.",
    "For indoor scenes, use an outer-wall rect as the anchor and place furniture and doors inside it. For outdoor scenes, use roads or rivers as the backbone and scatter buildings or terrain around them. For passage scenes, use two parallel boundary lines and a stretched `viewBox` aspect ratio.",
    "`meta.viewBox` is the camera. It does not move map elements. Move the player by changing the player `at`, then adjust `meta.viewBox` only if the camera should follow.",
    'If the map is still `uninitialized`, initialize it with one `meta + add` transaction as soon as the current turn clearly establishes a scene or place. First appearance does not require a prior "change".',
    "Initialization must include at least one spatial geometry element (`rect`, `circle`, `path`, `curve`, or `icon`) plus a player marker. `text` is only for short labels and cannot replace geometry. Name, `viewBox`, and labels alone are not a valid map.",
    "When a map already exists, prefer growing the same map for connected space: add adjacent rooms, paths, yards, landmarks, forest edges, or districts, and enlarge `meta.viewBox` when the visible scope needs more room.",
    "Let `meta.name` grow with the map scope. For example, a bedroom map may become a house map, then a glade or town-edge map as confirmed connected areas appear.",
    "Replace the whole map only when the story moves to a clearly separate place with no useful spatial continuity, such as a far-away city, teleportation, dream space, or unrelated interior. If nothing changes spatially, skip the map update. If you are unsure, do not change the map yet.",
    "Common updates: character movement = modify `at`; discovering a door or route = add door/road; an item appearing = add icon + label; an item being taken away = remove; a door opening = modify style; a new danger = add danger; camera follow = meta viewBox.",
    "Use `meta.name` as the map scope title. If you add an area label such as a room, house, road, or district name, attach it to that visible region instead of placing it at the top edge as a second title.",
    "Keep at least 20 units of spacing between elements to avoid overlap. Place text labels 15-25 units beside what they describe instead of on top of the shape center.",
    "Before submitting, sanity-check the map: clear anchors, a clear focus, drawable elements, a well-defined `viewBox` as the camera, and enough geometry to carry the map body.",
    "The map should record only spatial facts that already happened and are worth visualizing. Uncertain spatial information stays unwritten until confirmed."
  ] : [], E = t ? [
    "Write memory like a case file for your future self: specific, restrained, and easy to carry forward.",
    "Character psychology, secrets, and future plans become facts only after the RP source text clearly establishes them.",
    "Separate what happened, what the user requested, what you inferred, and what is still unconfirmed. Only established facts belong in durable memory."
  ] : [];
  return [
    "# 小白酒馆后台管理员",
    "",
    _s("Role", a),
    "",
    _s("Responsibilities", l),
    "",
    _s("Current Session", u),
    "",
    _s("Injected Context", d),
    h.length ? `
` + _s("File Discipline", h) : "",
    "",
    _s("Work Loop", p),
    "",
    _s("Source Strategy", y),
    w.length ? `
` + _s("Structured State", w) : "",
    E.length ? `
` + _s("Memory Tone", E) : "",
    "",
    _s("Output", [
      "Reply like an ebook file-operation confirmation: short, clear, and user-facing.",
      "Say what you verified, wrote, skipped, or left unchanged this turn. If nothing changed, say that you checked and why.",
      "Only expand tool arguments, raw JSON, full Markdown, or protocol details when the user explicitly asks for debugging detail."
    ])
  ].filter(Boolean).join(`
`).replace(/\n{3,}/g, `

`).trim();
}
function Zi(e = "") {
  return String(e || "").trim();
}
function Ur(e = []) {
  return e.join(`
`).trim();
}
function _k() {
  return {
    storyArcPrompt: Lq(),
    statePrompt: Oq(),
    turnPrompt: Fq()
  };
}
function Lq() {
  return Ur([
    "职责：维护“故事为什么走到现在”的长期脉络，只记录会影响后续剧情理解的内容。",
    "",
    "推荐格式：",
    "# 剧情脉络",
    "## 当前主线",
    "- 主角/角色正在围绕什么目标、危机或矛盾行动。",
    "## 长期压力",
    "- 仍在逼近、尚未解除、会改变后续选择的压力。",
    "## 未解伏笔",
    "- 已经出现但尚未解决的线索、承诺、秘密或隐患。",
    "## 关系变化",
    "- 角色之间已经形成的信任、敌意、亏欠、误会或依赖。",
    "",
    "规则：",
    "- 只在主线方向、核心矛盾、长期伏笔、关键关系发生变化时更新。",
    "- 不写逐轮流水账，不重复临时动作，不记录已经失去后续意义的细节。"
  ]);
}
function Oq() {
  return Ur([
    "职责：维护“此刻仍然成立”的当前事实，让下一轮对话能自然接上现场。",
    "",
    "推荐格式：",
    "# 状态栏",
    "## 当前局面",
    "- 时间/地点：",
    "- 正在发生：",
    "## 角色状态",
    "- 角色名：身体、情绪、立场、可见目标。",
    "## 关系与约定",
    "- 谁和谁之间存在明确关系、承诺、敌意、误会或共同秘密。",
    "## 持有物与限制",
    "- 物品、资源、权限、伤势、规则、风险、倒计时。",
    "",
    "规则：",
    "- 只保留现在仍为真的事实；事实变化时改写旧状态，不在后面追加矛盾说法。",
    "- 临时事件结束后删除或降级，不让过期状态污染后续判断。"
  ]);
}
function Fq() {
  return Ur([
    "职责：为本轮剧情写轻量小记，方便长篇故事在很久以后仍能回溯。",
    "",
    "推荐格式：",
    "# 楼层小记",
    "## 本轮变化",
    "- 本轮真正改变了什么。",
    "## 新信息",
    "- 新揭示的事实、线索、人物态度或世界设定。",
    "## 后续钩子",
    "- 下一轮可能继续影响剧情的问题、风险、承诺或机会。",
    "",
    "规则：",
    "- 每轮只写 3-6 条高价值信息，短句优先。",
    "- 已经写进剧情脉络或状态栏的长期信息，不要在楼层小记里再堆一遍。"
  ]);
}
var Wg = {
  storyArcPrompt: [
    Ur([
      "Use `memory/session.md` for why the story has reached the current point.",
      "Keep long-running direction, current pressure, and unresolved threads that still matter later.",
      "Do not turn it into a turn-by-turn log."
    ]),
    Ur([
      "`memory/session.md` writes why the story has reached the current point.",
      "Keep long-running direction, current pressure, and unresolved threads that still matter later.",
      "This is not a turn log. Update it when higher-level continuity changes instead of stuffing all long-term information into turns."
    ]),
    Ur([
      "记录故事为什么走到现在，而不是复述每一轮聊天。",
      "保留长期方向、当前压力、仍会影响后续的未解线索和关系变化。",
      "只在剧情走向、核心矛盾、长期伏笔发生变化时更新，避免堆成流水账。"
    ])
  ],
  statePrompt: [
    Ur([
      "Use `memory/state.md` for facts and states that are still true right now.",
      "Keep character state, relationships, places, time, possessions, and ongoing constraints.",
      "Do not keep transient events after they stop being true."
    ]),
    Ur([
      "`memory/state.md` writes facts and states that are still true right now.",
      "Include character state, relationships, places, time, possessions, constraints, and clear changes that still affect later turns.",
      'This answers "what is true now." Do not keep transient events after they stop being true.'
    ]),
    Ur([
      "记录此刻仍然成立的事实和状态，让后续对话能直接接上当前局面。",
      "保留角色状态、关系、地点、时间、持有物、限制条件、正在生效的约定和风险。",
      "临时事件结束后要移除或改写，不要把已经失效的状态留在这里。"
    ])
  ],
  turnPrompt: [
    Ur([
      "Use `memory/turns/*.md` for lightweight per-turn notes and history fallback.",
      "Capture what this turn changed, revealed, or set up, so very long stories remain recoverable even if chat history becomes inaccessible.",
      "Do not duplicate durable facts that already belong in `memory/session.md` or `memory/state.md`."
    ]),
    Ur([
      "`memory/turns/*.md` writes lightweight per-turn notes.",
      "Use it as long-story history fallback: what changed, what was revealed, and what was set up this turn.",
      "Do not turn turn notes into a second durable summary layer, and do not repeat settled facts already captured in session or state."
    ]),
    Ur([
      "记录本轮发生了什么关键变化，作为长篇剧情的轻量回溯。",
      "重点写新揭示的信息、做出的选择、状态变化、埋下的后续钩子。",
      "已经进入剧情脉络或状态栏的长期信息，不要在这里重复堆一遍。"
    ])
  ]
};
function Vg(e, t, n = []) {
  const r = Zi(e);
  return !r || n.includes(r) ? t : r;
}
function Bb(e = {}, t = {}) {
  const { includeMemory: n } = yk(t), r = _k(), s = n ? [
    ["Story Arc Duties", Zi(e.storyArcPrompt) || r.storyArcPrompt],
    ["State Duties and Format", Zi(e.statePrompt) || r.statePrompt],
    ["Turn Notes Duties", Zi(e.turnPrompt) || r.turnPrompt]
  ].filter(([, i]) => i) : [], o = [Dq(t)];
  return s.forEach(([i, a]) => {
    o.push("", `## ${i}`, String(a));
  }), o.join(`
`).trim();
}
function Bq(e = {}, t = {}) {
  return Bb(e, t);
}
function Bc() {
  const e = _k();
  return {
    id: Kp,
    name: "默认助手预设",
    description: "记忆管理员的默认维护规则。",
    ...e,
    memoryManagerPrompt: Bb(e)
  };
}
function Ub(e = {}) {
  const t = Bc(), n = {
    id: Zi(e.id) || t.id,
    name: Zi(e.name) || t.name,
    description: String(e.description || ""),
    storyArcPrompt: Vg(e.storyArcPrompt, t.storyArcPrompt, Wg.storyArcPrompt),
    statePrompt: Vg(e.statePrompt, t.statePrompt, Wg.statePrompt),
    turnPrompt: Vg(e.turnPrompt, t.turnPrompt, Wg.turnPrompt),
    updatedAt: Number(e.updatedAt) || void 0
  };
  return n.memoryManagerPrompt = Bb(n), n;
}
var Uq = "sillytavern-current-chat-prompt";
function bk() {
  return {
    id: Uq,
    name: "酒馆当前聊天预设",
    source: "sillytavern",
    selected: !0,
    sections: []
  };
}
function qq(e = {}) {
  return {
    id: String(e.id || "").trim(),
    label: String(e.label || "").trim(),
    enabled: e.enabled !== !1,
    marker: e.marker === !0,
    role: e.role || "system",
    content: String(e.content || "").trim(),
    placement: e.placement || "beforeHistory",
    source: String(e.source || "manual").trim()
  };
}
function Kg(e = {}) {
  const t = bk();
  return {
    id: String(e.id || t.id).trim() || t.id,
    name: String(e.name || t.name).trim() || t.name,
    description: String(e.description || ""),
    version: String(e.version || ""),
    source: String(e.source || t.source || "sillytavern"),
    selected: e.selected !== !1,
    promptManager: e.promptManager,
    systemPrompt: e.systemPrompt,
    contextTemplate: e.contextTemplate,
    instructTemplate: e.instructTemplate,
    historySeparator: String(e.historySeparator || ""),
    sections: Array.isArray(e.sections) ? e.sections.map(qq).filter((n) => n.content || n.marker) : [],
    updatedAt: Number(e.updatedAt) || void 0
  };
}
var my = "tavern.map", gy = "main", wk = "__label__";
function vy() {
  return [
    "Empty map. If this turn establishes a clear current scene, initialize it with one minimal StatePatch like the examples below. Replace <angle brackets> with scene facts, then add or remove elements as needed.",
    'Indoor example: {"ops":[{"op":"meta","set":{"name":"<Place Name>","viewBox":[0,0,400,300],"status":"active"}},{"op":"add","element":{"id":"wall","at":[40,40],"rect":[320,220],"cat":"wall"}},{"op":"add","element":{"id":"door","at":[200,260],"icon":"o","cat":"door"}},{"op":"add","element":{"id":"player","at":[200,180],"icon":"o","cat":"marker"}}]}',
    'Outdoor example: {"ops":[{"op":"meta","set":{"name":"<Place Name>","viewBox":[0,0,800,600],"status":"active"}},{"op":"add","element":{"id":"ground","at":[400,300],"circle":150,"cat":"terrain"}},{"op":"add","element":{"id":"path","at":[0,300],"path":[[800,0]],"cat":"road"}},{"op":"add","element":{"id":"player","at":[400,320],"icon":"o","cat":"marker"}}]}',
    "Requirement: include at least one spatial geometry element (`rect`, `circle`, `path`, `curve`, or `icon`). Do not send only `text` labels. After initialization, use only incremental `add` / `modify` / `remove` ops. If nothing changes spatially, skip the map update instead of resetting the whole map.",
    "For connected places, grow the same map by adding adjacent spaces and enlarging `meta.viewBox`; reset only for a clearly separate location with no useful spatial continuity.",
    "Use `meta.name` as the map scope title. Area labels are allowed only when they mark a concrete visible region; place them inside or next to that region, not at the top edge as a second title.",
    "`viewBox` is the camera. Changing it does not move any element. Move the player by updating the player `at`, then adjust `meta.viewBox` only if the camera should follow.",
    "Elements use `at:[x,y]` plus one shape field: `rect`, `circle`, `path`, `curve`, `icon`, or `text`. With `at`, `path` and `curve` points are relative offsets. Without `at`, points are absolute coordinates and the first point becomes the anchor."
  ].join(" ");
}
function Uc() {
  return {
    meta: {
      name: null,
      viewBox: null,
      theme: "parchment",
      status: "uninitialized",
      hint: vy()
    },
    elements: []
  };
}
function Yh(e) {
  return String(e || "").startsWith(wk);
}
function yy(e) {
  return `${wk}${e}`;
}
function Hq(e) {
  return !e || typeof e != "object" || Array.isArray(e) ? !1 : e.meta?.status === "uninitialized";
}
var hu = {
  memoryArchiving: !0,
  cartographyEngine: !0,
  actionChecks: !0,
  randomEncounters: !1,
  questOrchestration: !1
}, Sk = [
  "memoryArchiving",
  "cartographyEngine",
  "actionChecks",
  "randomEncounters",
  "questOrchestration"
], Gq = {
  "silent-operations": "Silent Operations",
  "fate-arbitration": "Fate Arbitration",
  "narrative-orchestration": "Narrative Orchestration"
}, _y = [
  {
    key: "memoryArchiving",
    tier: "silent-operations",
    icon: "🧠",
    title: "Memory Archiving",
    summary: "",
    description: "Grant the Agent authority over your session's long-term memory. It will silently maintain, compress, and recall context on your behalf — ensuring the story never forgets."
  },
  {
    key: "cartographyEngine",
    tier: "silent-operations",
    icon: "🗺️",
    title: "Cartography Engine",
    summary: "",
    description: "Allow the Agent to generate, update, and render spatial data as you explore. Regions, landmarks, and points of interest will materialize in the Appendix panel as the narrative unfolds."
  },
  {
    key: "actionChecks",
    tier: "fate-arbitration",
    icon: "🎲",
    title: "Action Checks",
    summary: "",
    description: "When you attempt something uncertain — persuading a stranger, scaling a wall, deciphering a rune — the dice decide, not the AI. A true random roll arbitrates the outcome, and the story bends to fate."
  },
  {
    key: "randomEncounters",
    tier: "fate-arbitration",
    icon: "⚔️",
    title: "Random Encounters",
    summary: "",
    description: "Permit the world to surprise you. At intervals, an unseen die rolls in the background. When it lands, the unexpected arrives — an ambush on the road, a strange omen in the sky, a letter slipped under your door."
  },
  {
    key: "questOrchestration",
    tier: "narrative-orchestration",
    icon: "📜",
    title: "Quest Orchestration",
    summary: "",
    description: "Allow the Agent to weave structured quest arcs into the narrative. Objectives are tracked, updated, and resolved as the story progresses. A living quest log appears in the Appendix, evolving with your choices."
  }
], Wq = {
  memoryArchiving: {
    includeMemoryFiles: !0,
    automaticManagerWork: !0,
    managerPromptMemory: !0
  },
  cartographyEngine: {
    includeStructuredStates: !0,
    automaticManagerWork: !0,
    managerPromptCartography: !0
  },
  actionChecks: { includeActionChecks: !0 },
  randomEncounters: { includeRandomEncounters: !0 },
  questOrchestration: {}
};
function Wr(e) {
  const t = e && typeof e == "object" && !Array.isArray(e) ? e : {};
  return {
    memoryArchiving: "memoryArchiving" in t ? !!t.memoryArchiving : hu.memoryArchiving,
    cartographyEngine: "cartographyEngine" in t ? !!t.cartographyEngine : hu.cartographyEngine,
    actionChecks: "actionChecks" in t ? !!t.actionChecks : hu.actionChecks,
    randomEncounters: "randomEncounters" in t ? !!t.randomEncounters : hu.randomEncounters,
    questOrchestration: "questOrchestration" in t ? !!t.questOrchestration : hu.questOrchestration
  };
}
function zp(e) {
  return !e || typeof e != "object" || Array.isArray(e) ? !1 : Sk.some((t) => Object.prototype.hasOwnProperty.call(e, t));
}
function Xh(e, t) {
  return Wr({
    ...Wr(e),
    ...zp(t) ? t : {}
  });
}
function Vq(e) {
  const t = Wr(e);
  return Object.values(t).filter(Boolean).length;
}
function Kq(e) {
  return _y.find((t) => t.key === e);
}
function Jp(e) {
  const t = Wr(e), n = Sk.filter((r) => t[r]);
  return n.reduce((r, s) => {
    const o = Wq[s];
    return o ? {
      ...r,
      includeMemoryFiles: r.includeMemoryFiles || o.includeMemoryFiles === !0,
      includeStructuredStates: r.includeStructuredStates || o.includeStructuredStates === !0,
      includeActionChecks: r.includeActionChecks || o.includeActionChecks === !0,
      includeRandomEncounters: r.includeRandomEncounters || o.includeRandomEncounters === !0,
      hasAutomaticManagerWork: r.hasAutomaticManagerWork || o.automaticManagerWork === !0,
      managerPromptOptions: {
        includeMemory: r.managerPromptOptions.includeMemory || o.managerPromptMemory === !0,
        includeCartography: r.managerPromptOptions.includeCartography || o.managerPromptCartography === !0
      }
    } : r;
  }, {
    contract: t,
    enabledKeys: n,
    includeMemoryFiles: !1,
    includeStructuredStates: !1,
    includeActionChecks: !1,
    includeRandomEncounters: !1,
    hasAutomaticManagerWork: !1,
    managerPromptOptions: {
      includeMemory: !1,
      includeCartography: !1
    }
  });
}
var zq = class extends Fc {
  sessions;
  messages;
  managerMessages;
  meta;
  presets;
  managerRuns;
  memoryFiles;
  memoryIndexes;
  assistantPresets;
  managerMemorySnapshots;
  stateDocuments;
  statePatches;
  managerStateSnapshots;
  constructor() {
    super("LittleWhiteBox_Tavern"), this.version(1).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      turnSummaries: "id, sessionId, episodeId, turn, userOrder, assistantOrder, status, updatedAt",
      episodeSummaries: "id, sessionId, status, updatedAt, startTurn, endTurn",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt"
    }), this.version(2).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      turnSummaries: "id, sessionId, turn, userOrder, assistantOrder, status, updatedAt",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt"
    }), this.version(3).stores({
      sessions: "id, updatedAt, characterId, characterName",
      messages: "[sessionId+order], sessionId, order",
      managerMessages: "[sessionId+order], sessionId, order",
      meta: "key",
      presets: "id, updatedAt, sourcePresetId",
      managerRuns: "id, sessionId, status, turn, updatedAt",
      memoryFiles: "[sessionId+path], sessionId, path, status, updatedAt",
      memoryIndexes: "[sessionId+kind], sessionId, kind, status, updatedAt",
      assistantPresets: "id, updatedAt",
      managerMemorySnapshots: "[managerRunId+path], managerRunId, sessionId, path, updatedAt",
      stateDocuments: "[sessionId+docType+docId], sessionId, docType, docId, status, updatedAt",
      statePatches: "id, sessionId, docType, docId, managerRunId, revision, status, updatedAt",
      managerStateSnapshots: "[managerRunId+docType+docId], managerRunId, sessionId, docType, docId, updatedAt"
    });
  }
}, rn = new zq(), kt = rn.sessions, or = rn.messages, Rr = rn.managerMessages, lo = rn.meta, nae = rn.presets, Jn = rn.managerRuns, Rn = rn.memoryFiles, Nr = rn.memoryIndexes, ra = rn.assistantPresets, as = rn.managerMemorySnapshots, $r = rn.stateDocuments, Ko = rn.statePatches, Jr = rn.managerStateSnapshots;
function Ct() {
  return Date.now();
}
function Yp(e = "tavern-session") {
  return `${e}-${Ct()}-${Math.random().toString(36).slice(2, 8)}`;
}
function Jq(e = "", t = "小白酒馆会话") {
  return String(e || "").trim().slice(0, 120) || t;
}
function hT(e = "") {
  const t = String(e || "").replace(/\s*[·-]\s*小白酒馆\s*$/g, "").replace(/\s*[·-]\s*会话\s*$/g, "").replace(/^小白酒馆会话$/g, "").replace(/\s*·\s*第\s*\d+\s*轮\s*·\s*\d+\s*条可用消息\s*$/g, "").trim();
  return /^(sillytavern\s+system|system)\b/i.test(t) ? "" : t;
}
function Yq(e) {
  return JSON.parse(JSON.stringify(e));
}
function Bt(e, t) {
  if (e === void 0) return t;
  try {
    return JSON.parse(JSON.stringify(e));
  } catch {
    return t;
  }
}
function qc(e, t = 12) {
  return (Array.isArray(e) ? e : typeof e == "string" ? [e] : []).map((n) => String(n || "").trim()).filter(Boolean).slice(0, t);
}
function Xq(e) {
  const t = String(e || "").trim();
  return t === "assistant" || t === "tool" || t === "system" ? t : "user";
}
function Tk(e) {
  const t = Array.isArray(e.toolCalls) ? e.toolCalls : [], n = "tool_calls" in e && Array.isArray(e.tool_calls) ? e.tool_calls : [], r = [...t.map((s) => ({
    id: String(s?.id || ""),
    name: String(s?.name || ""),
    arguments: String(s?.arguments || "{}")
  })), ...n.map((s) => ({
    id: String(s?.id || ""),
    name: String(s?.function?.name || ""),
    arguments: String(s?.function?.arguments || "{}")
  }))].filter((s) => s.name);
  return r.length ? r : void 0;
}
function qb(e) {
  return ib(e);
}
function Hb(e) {
  return {
    ...e,
    runtimeEvents: qb(e.runtimeEvents)
  };
}
function Ek(e) {
  return [
    "queued",
    "running",
    "completed",
    "failed",
    "cancelled",
    "superseded",
    "rolled_back"
  ].includes(String(e || "")) ? e : "queued";
}
function Qq(e) {
  if (!e || typeof e != "object" || Array.isArray(e)) return {};
  const t = {};
  return Object.entries(e).forEach(([n, r]) => {
    if (!n || !r || typeof r != "object" || Array.isArray(r)) return;
    const s = {}, o = r;
    [
      "stickyUntilTurn",
      "cooldownUntilTurn",
      "delayUntilTurn"
    ].forEach((i) => {
      const a = Number(o[i]);
      Number.isFinite(a) && (s[i] = a);
    }), Object.keys(s).length && (t[n] = s);
  }), t;
}
function Zq(e) {
  if (!e || typeof e != "object" || Array.isArray(e)) return null;
  const t = e, n = {}, r = Number(t.hash), s = Number(t.start), o = Number(t.end);
  return Number.isFinite(r) && (n.hash = r), Number.isFinite(s) && (n.start = s), Number.isFinite(o) && (n.end = o), t.protected === !0 && (n.protected = !0), n.hash !== void 0 || n.start !== void 0 || n.end !== void 0 || n.protected ? n : null;
}
function jq(e) {
  if (!e || typeof e != "object" || Array.isArray(e)) return {
    sticky: {},
    cooldown: {}
  };
  const t = e, n = (r) => {
    if (!r || typeof r != "object" || Array.isArray(r)) return {};
    const s = {};
    return Object.entries(r).forEach(([o, i]) => {
      const a = Zq(i);
      o && a && (s[o] = a);
    }), s;
  };
  return {
    sticky: n(t.sticky),
    cooldown: n(t.cooldown)
  };
}
function nr(e) {
  const t = e && typeof e == "object" && !Array.isArray(e) ? e : {};
  return {
    ...t,
    turn: Math.max(0, Number(t.turn) || 0),
    contextWindowStartOrder: Math.max(0, Math.floor(Number(t.contextWindowStartOrder) || 0)),
    contract: Wr(t.contract),
    worldEntryStates: Qq(t.worldEntryStates),
    nativeWorldInfoTimedState: jq(t.nativeWorldInfoTimedState)
  };
}
function by(e, t) {
  return !!e && typeof e == "object" && !Array.isArray(e) && Object.prototype.hasOwnProperty.call(e, t);
}
function Gb(e = {}, t = {}) {
  const n = Yq(e || {});
  return Object.entries(t || {}).forEach(([r, s]) => {
    !r || !s || typeof s != "object" || (n[r] = {
      ...n[r] || {},
      ...s
    });
  }), n;
}
async function Ck(e = {}) {
  const t = Ct(), n = hT(e.characterName || ""), r = hT(e.title || ""), s = {
    id: String(e.id || Yp()),
    title: Jq(r, n || "未选择角色"),
    characterId: String(e.characterId || ""),
    characterName: n,
    createdAt: Number(e.createdAt) || t,
    updatedAt: t,
    contextSnapshot: Bt(e.contextSnapshot, void 0),
    buildSnapshot: Bt(e.buildSnapshot, void 0),
    chatPresetId: String(e.chatPresetId || e.presetId || ""),
    chatPresetName: String(e.chatPresetName || e.presetName || ""),
    presetId: String(e.presetId || ""),
    presetName: String(e.presetName || ""),
    summary: String(e.summary || ""),
    state: Bt(nr(e.state || {}), {})
  };
  return await rn.transaction("rw", kt, lo, $r, async () => {
    await kt.put(s), await lo.put({
      key: "selectedSessionId",
      value: s.id,
      updatedAt: t
    }), await Rk(s.id, { touchSession: !1 });
  }), s;
}
async function eH() {
  return kt.orderBy("updatedAt").reverse().toArray();
}
async function tH() {
  const e = await lo.get("selectedSessionId");
  return String(e?.value || "").trim();
}
async function uf(e = "") {
  const t = String(e || "").trim();
  return await lo.put({
    key: "selectedSessionId",
    value: t,
    updatedAt: Ct()
  }), t;
}
async function po(e = "") {
  const t = String(e || "").trim();
  return t && await kt.get(t) || null;
}
async function nH(e = "") {
  const t = String(e || "").trim();
  return !t || !await po(t) ? 0 : (await rn.transaction("rw", kt, or, Rr, Jn, as, Jr, Rn, Nr, $r, Ko, lo, async () => {
    const [n, r, s, o, i, a, l, u, d] = await Promise.all([
      or.where("sessionId").equals(t).toArray(),
      Rr.where("sessionId").equals(t).toArray(),
      Jn.where("sessionId").equals(t).toArray(),
      as.where("sessionId").equals(t).toArray(),
      Jr.where("sessionId").equals(t).toArray(),
      Rn.where("sessionId").equals(t).toArray(),
      Nr.where("sessionId").equals(t).toArray(),
      $r.where("sessionId").equals(t).toArray(),
      Ko.where("sessionId").equals(t).toArray()
    ]);
    await Promise.all([
      n.length ? or.bulkDelete(n.map((p) => [p.sessionId, p.order])) : 0,
      r.length ? Rr.bulkDelete(r.map((p) => [p.sessionId, p.order])) : 0,
      s.length ? Jn.bulkDelete(s.map((p) => p.id)) : 0,
      o.length ? as.bulkDelete(o.map((p) => [p.managerRunId, p.path])) : 0,
      i.length ? Jr.bulkDelete(i.map((p) => [
        p.managerRunId,
        p.docType,
        p.docId
      ])) : 0,
      a.length ? Rn.bulkDelete(a.map((p) => [p.sessionId, p.path])) : 0,
      l.length ? Nr.bulkDelete(l.map((p) => [p.sessionId, p.kind])) : 0,
      u.length ? $r.bulkDelete(u.map((p) => [
        p.sessionId,
        p.docType,
        p.docId
      ])) : 0,
      d.length ? Ko.bulkDelete(d.map((p) => p.id)) : 0
    ]), await kt.delete(t);
    const h = await lo.get("selectedSessionId");
    if (String(h?.value || "") === t) {
      const [p] = await kt.orderBy("updatedAt").reverse().toArray();
      await lo.put({
        key: "selectedSessionId",
        value: p?.id || "",
        updatedAt: Ct()
      });
    }
  }), 1);
}
async function Ak(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = await po(n);
  if (!r) return null;
  const s = Ct(), o = nr(r.state || {}), i = nr(t), a = {
    ...o,
    ...t,
    turn: Math.max(0, Number(t.turn ?? o.turn) || 0),
    contract: by(t, "contract") ? Xh(o.contract, zp(t.contract) ? t.contract : void 0) : o.contract,
    worldEntryStates: Gb(o.worldEntryStates || {}, i.worldEntryStates || {}),
    nativeWorldInfoTimedState: by(t, "nativeWorldInfoTimedState") ? i.nativeWorldInfoTimedState : o.nativeWorldInfoTimedState
  };
  return await kt.update(n, {
    state: Bt(a, {}),
    updatedAt: s,
    buildSnapshot: Bt(t.lastBuildSnapshot || r.buildSnapshot, void 0)
  }), await po(n);
}
async function Ik(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = await po(n);
  if (!r) return null;
  const s = Ct(), o = nr(r.state || {}), i = nr(t), a = {
    ...t,
    turn: Math.max(0, Number(i.turn) || 0),
    contract: by(t, "contract") ? Xh(o.contract, zp(t.contract) ? t.contract : void 0) : o.contract,
    worldEntryStates: i.worldEntryStates || {},
    nativeWorldInfoTimedState: i.nativeWorldInfoTimedState
  };
  return await kt.update(n, {
    state: Bt(a, {}),
    updatedAt: s,
    buildSnapshot: Bt(a.lastBuildSnapshot || r.buildSnapshot, void 0)
  }), await po(n);
}
async function xk(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = await po(n);
  if (!r) return null;
  const s = "contextSnapshot" in t ? t.contextSnapshot : r.contextSnapshot, o = s?.character || {}, i = {
    updatedAt: Ct(),
    contextSnapshot: Bt(s, void 0),
    buildSnapshot: Bt("buildSnapshot" in t ? t.buildSnapshot : r.buildSnapshot, void 0),
    chatPresetId: "chatPresetId" in t ? String(t.chatPresetId || "") : r.chatPresetId,
    chatPresetName: "chatPresetName" in t ? String(t.chatPresetName || "") : r.chatPresetName,
    presetId: "presetId" in t ? String(t.presetId || "") : r.presetId,
    presetName: "presetName" in t ? String(t.presetName || "") : r.presetName,
    characterId: "characterId" in t ? String(t.characterId || "") : String(o.id || r.characterId || ""),
    characterName: "characterName" in t ? String(t.characterName || "") : String(o.name || r.characterName || "")
  };
  return await kt.update(n, i), await po(n);
}
async function Uu(e, t) {
  const n = String(e || "").trim();
  if (!n) throw new Error("session_required");
  const r = await or.where("sessionId").equals(n).toArray(), s = Math.max(-1, ...r.map((a) => Number(a.order) || 0)) + 1, o = Ct(), i = {
    sessionId: n,
    order: s,
    role: String(t.role || ""),
    content: String(t.content || ""),
    name: t.name ? String(t.name) : void 0,
    error: t.error === !0,
    createdAt: o,
    provider: "provider" in t ? String(t.provider || "") : void 0,
    model: "model" in t ? String(t.model || "") : void 0,
    finishReason: "finishReason" in t ? String(t.finishReason || "") : void 0,
    thoughts: "thoughts" in t ? Bt(t.thoughts, void 0) : void 0,
    providerPayload: "providerPayload" in t ? Bt(t.providerPayload, void 0) : void 0,
    contextSnapshot: "contextSnapshot" in t ? Bt(t.contextSnapshot, void 0) : void 0,
    buildSnapshot: "buildSnapshot" in t ? Bt(t.buildSnapshot, void 0) : void 0,
    chatPresetId: "chatPresetId" in t ? String(t.chatPresetId || "") : String(t.presetId || ""),
    chatPresetName: "chatPresetName" in t ? String(t.chatPresetName || "") : String(t.presetName || ""),
    presetId: "presetId" in t ? String(t.presetId || "") : void 0,
    presetName: "presetName" in t ? String(t.presetName || "") : void 0,
    requestSnapshot: "requestSnapshot" in t ? Bt(t.requestSnapshot, void 0) : void 0,
    runtimeEvents: "runtimeEvents" in t ? qb(t.runtimeEvents) : []
  };
  return await rn.transaction("rw", or, kt, async () => {
    await or.put(i), await kt.update(n, { updatedAt: o });
  }), Hb(i);
}
async function wy(e = "", t = -1, n) {
  const r = String(e || "").trim(), s = Number(t);
  if (!r || !Number.isInteger(s) || s < 0 || !await or.get([r, s])) return null;
  const o = {};
  "content" in n && (o.content = String(n.content || "")), "error" in n && (o.error = n.error === !0), "thoughts" in n && (o.thoughts = Bt(n.thoughts, void 0)), "runtimeEvents" in n && (o.runtimeEvents = qb(n.runtimeEvents)), await or.update([r, s], o), await kt.update(r, { updatedAt: Ct() });
  const i = await or.get([r, s]);
  return i ? Hb(i) : null;
}
async function kk(e = "", t = []) {
  const n = String(e || "").trim(), r = [...new Set((Array.isArray(t) ? t : []).map((o) => Number(o)).filter((o) => Number.isInteger(o) && o >= 0))];
  if (!n || !r.length) return 0;
  const s = [];
  return await Promise.all(r.map(async (o) => {
    await or.get([n, o]) && s.push([n, o]);
  })), s.length ? (await rn.transaction("rw", or, kt, async () => {
    await or.bulkDelete(s), await kt.update(n, { updatedAt: Ct() });
  }), s.length) : 0;
}
async function Hr(e = "") {
  const t = String(e || "").trim();
  return t ? (await or.where("sessionId").equals(t).sortBy("order")).map(Hb) : [];
}
async function rH(e = "") {
  const t = String(e || "").trim();
  return t ? (await or.where("sessionId").equals(t).toArray()).length : 0;
}
async function cf(e, t) {
  const n = String(e || "").trim();
  if (!n) throw new Error("session_required");
  const r = await Rr.where("sessionId").equals(n).toArray(), s = Math.max(-1, ...r.map((a) => Number(a.order) || 0)) + 1, o = Ct(), i = {
    sessionId: n,
    order: s,
    role: Xq(t.role),
    content: String(t.content || ""),
    name: t.name ? String(t.name) : void 0,
    error: t.error === !0,
    createdAt: o,
    updatedAt: o,
    provider: "provider" in t ? String(t.provider || "") : void 0,
    model: "model" in t ? String(t.model || "") : void 0,
    finishReason: "finishReason" in t ? String(t.finishReason || "") : void 0,
    thoughts: "thoughts" in t ? Bt(t.thoughts, void 0) : void 0,
    providerPayload: "providerPayload" in t ? Bt(t.providerPayload, void 0) : void 0,
    toolCalls: Tk(t),
    toolCallId: String(t.toolCallId || t.tool_call_id || "").trim() || void 0,
    toolName: String(t.toolName || "").trim() || void 0,
    toolDisplay: "toolDisplay" in t ? Bt(t.toolDisplay, void 0) : void 0
  };
  return await rn.transaction("rw", Rr, kt, async () => {
    await Rr.put(i), await kt.update(n, { updatedAt: o });
  }), i;
}
async function sH(e = "", t = -1, n) {
  const r = String(e || "").trim(), s = Number(t);
  if (!r || !Number.isInteger(s) || s < 0 || !await Rr.get([r, s])) return null;
  const o = Ct(), i = { updatedAt: o };
  return "content" in n && (i.content = String(n.content || "")), "error" in n && (i.error = n.error === !0), "provider" in n && (i.provider = String(n.provider || "")), "model" in n && (i.model = String(n.model || "")), "finishReason" in n && (i.finishReason = String(n.finishReason || "")), "thoughts" in n && (i.thoughts = Bt(n.thoughts, void 0)), "providerPayload" in n && (i.providerPayload = Bt(n.providerPayload, void 0)), "toolCalls" in n && (i.toolCalls = Tk(n)), "toolCallId" in n && (i.toolCallId = String(n.toolCallId || "").trim()), "toolName" in n && (i.toolName = String(n.toolName || "").trim()), "toolDisplay" in n && (i.toolDisplay = Bt(n.toolDisplay, void 0)), n.clearProtocolPayload === !0 && (i.providerPayload = void 0, i.toolCalls = void 0, i.toolCallId = void 0, i.toolName = void 0, i.toolDisplay = void 0), await Rr.update([r, s], i), await kt.update(r, { updatedAt: o }), await Rr.get([r, s]) || null;
}
async function Sy(e = "", t = []) {
  const n = String(e || "").trim(), r = [...new Set((Array.isArray(t) ? t : []).map((o) => Number(o)).filter((o) => Number.isInteger(o) && o >= 0))];
  if (!n || !r.length) return 0;
  const s = [];
  return await Promise.all(r.map(async (o) => {
    await Rr.get([n, o]) && s.push([n, o]);
  })), s.length ? (await rn.transaction("rw", Rr, kt, async () => {
    await Rr.bulkDelete(s), await kt.update(n, { updatedAt: Ct() });
  }), s.length) : 0;
}
async function qr(e = "") {
  const t = String(e || "").trim();
  return t ? Rr.where("sessionId").equals(t).sortBy("order") : [];
}
async function Qh(e = {}) {
  const t = String(e.sessionId || "").trim();
  if (!t) throw new Error("manager_run_session_required");
  const n = Ct(), r = {
    id: String(e.id || Yp("manager-run")),
    sessionId: t,
    turn: Math.max(0, Number(e.turn) || 0),
    userOrder: Number.isInteger(Number(e.userOrder)) ? Number(e.userOrder) : -1,
    assistantOrder: Number.isInteger(Number(e.assistantOrder)) ? Number(e.assistantOrder) : -1,
    trigger: String(e.trigger || "after_turn"),
    status: Ek(e.status),
    provider: String(e.provider || ""),
    model: String(e.model || ""),
    inputSummary: String(e.inputSummary || ""),
    outputText: String(e.outputText || ""),
    parsedAction: String(e.parsedAction || ""),
    toolTrace: "toolTrace" in e ? Bt(e.toolTrace, void 0) : void 0,
    changedFiles: qc(e.changedFiles, 100),
    changedStates: qc(e.changedStates, 100),
    error: String(e.error || ""),
    createdAt: Number(e.createdAt) || n,
    updatedAt: n
  };
  return await Jn.put(r), await kt.update(t, { updatedAt: n }), r;
}
async function Ds(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = await Jn.get(n);
  if (!r) return null;
  const s = { updatedAt: Ct() };
  return "status" in t && (s.status = Ek(t.status)), [
    "provider",
    "model",
    "inputSummary",
    "outputText",
    "parsedAction",
    "error",
    "trigger"
  ].forEach((o) => {
    o in t && (s[o] = String(t[o] || ""));
  }), "toolTrace" in t && (s.toolTrace = Bt(t.toolTrace, void 0)), "changedFiles" in t && (s.changedFiles = qc(t.changedFiles, 100)), "changedStates" in t && (s.changedStates = qc(t.changedStates, 100)), "turn" in t && (s.turn = Math.max(0, Number(t.turn) || 0)), "userOrder" in t && (s.userOrder = Number(t.userOrder)), "assistantOrder" in t && (s.assistantOrder = Number(t.assistantOrder)), await Jn.update(n, s), await kt.update(r.sessionId, { updatedAt: Ct() }), await Jn.get(n) || null;
}
async function oH(e = "") {
  const t = String(e || "").trim();
  if (!t) return null;
  const n = await Jn.get(t);
  if (!n || n.status !== "running") return n || null;
  const r = Ct();
  return await Jn.update(t, { updatedAt: r }), await kt.update(n.sessionId, { updatedAt: r }), await Jn.get(t) || null;
}
async function iH(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return [];
  const r = await Jn.where("sessionId").equals(n).sortBy("updatedAt"), s = Math.max(0, Number(t.limit) || 0);
  return (s ? r.slice(-s) : r).reverse();
}
function Wb(e) {
  const t = JSON.stringify({
    content: String(e?.content || ""),
    status: String(e?.status || ""),
    source: String(e?.source || ""),
    staleFromOrder: Number.isFinite(Number(e?.staleFromOrder)) ? Number(e?.staleFromOrder) : null
  });
  let n = 2166136261;
  for (let r = 0; r < t.length; r += 1)
    n ^= t.charCodeAt(r), n = Math.imul(n, 16777619) >>> 0;
  return n.toString(16);
}
function aH(e) {
  const t = JSON.stringify(e ?? null);
  let n = 2166136261;
  for (let r = 0; r < t.length; r += 1)
    n ^= t.charCodeAt(r), n = Math.imul(n, 16777619) >>> 0;
  return n.toString(16);
}
function Vb(e) {
  return aH(e ? {
    docType: e.docType,
    docId: e.docId,
    title: String(e.title || ""),
    revision: Number(e.revision) || 0,
    data: e.data ?? null,
    digest: String(e.digest || ""),
    status: String(e.status || ""),
    source: String(e.source || ""),
    staleFromOrder: Number.isFinite(Number(e.staleFromOrder)) ? Number(e.staleFromOrder) : null
  } : null);
}
function Mk(e = "", t = []) {
  const n = String(e || "").trim();
  if (!t.length) return n;
  const r = "rollback_conflict:", s = n.startsWith(r) ? n.slice(18).split(",").map((o) => o.trim()).filter(Boolean) : [];
  return `${r}${[.../* @__PURE__ */ new Set([...s, ...t])].join(",")}`;
}
async function Pk(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return [];
  const r = await $r.where("sessionId").equals(n).sortBy("updatedAt"), s = String(t.docType || "").trim();
  return r.filter((o) => !s || o.docType === s).filter((o) => t.includeStale || o.status !== "stale");
}
async function Tl(e = "", t = "tavern.map", n = "main") {
  const r = String(e || "").trim(), s = String(t || "").trim(), o = String(n || "main").trim() || "main";
  return !r || !s || !o ? null : await $r.get([
    r,
    s,
    o
  ]) || null;
}
async function Rk(e = "", t = {}) {
  const n = String(e || "").trim();
  return n ? await rn.transaction("rw", $r, kt, async () => {
    const r = await $r.get([
      n,
      my,
      gy
    ]);
    if (r) return r;
    const s = Ct(), o = {
      sessionId: n,
      docType: my,
      docId: gy,
      title: "地图",
      revision: 0,
      data: Uc(),
      digest: "",
      status: "active",
      source: "system-seed",
      staleFromOrder: void 0,
      createdAt: s,
      updatedAt: s
    };
    return await $r.put(o), t.touchSession !== !1 && await kt.update(n, { updatedAt: s }), o;
  }) : null;
}
async function lH(e) {
  const t = Ct(), n = {
    ...Bt(e, e),
    sessionId: String(e.sessionId || "").trim(),
    docType: String(e.docType || "tavern.map"),
    docId: String(e.docId || "main").trim() || "main",
    title: String(e.title || ""),
    revision: Math.max(0, Number(e.revision) || 0),
    digest: String(e.digest || ""),
    status: e.status === "stale" ? "stale" : "active",
    createdAt: Number(e.createdAt) || t,
    updatedAt: t
  };
  if (!n.sessionId) throw new Error("state_session_required");
  return await $r.put(n), await kt.update(n.sessionId, { updatedAt: t }), n;
}
async function uH(e = {}) {
  const t = String(e.sessionId || "").trim(), n = String(e.docType || "tavern.map"), r = String(e.docId || "main").trim() || "main";
  if (!t) throw new Error("state_patch_session_required");
  const s = Ct(), o = {
    id: String(e.id || Yp("state-patch")),
    sessionId: t,
    docType: n,
    docId: r,
    revision: Math.max(0, Number(e.revision) || 0),
    status: e.status === "rolled_back" ? "rolled_back" : "active",
    managerRunId: String(e.managerRunId || ""),
    sourceUserOrder: Number.isFinite(Number(e.sourceUserOrder)) ? Number(e.sourceUserOrder) : void 0,
    sourceAssistantOrder: Number.isFinite(Number(e.sourceAssistantOrder)) ? Number(e.sourceAssistantOrder) : void 0,
    source: String(e.source || ""),
    summary: String(e.summary || ""),
    ops: Array.isArray(e.ops) ? Bt(e.ops, []) : [],
    changedIds: qc(e.changedIds, 200),
    removedElements: Array.isArray(e.removedElements) ? Bt(e.removedElements, []) : [],
    createdAt: Number(e.createdAt) || s,
    updatedAt: s
  };
  return await Ko.put(o), await kt.update(t, { updatedAt: s }), o;
}
async function Nk(e = {}) {
  const t = String(e.sessionId || "").trim();
  if (!t) return [];
  const n = String(e.docType || "").trim(), r = String(e.docId || "").trim(), s = (await Ko.where("sessionId").equals(t).sortBy("revision")).filter((i) => (!n || i.docType === n) && (!r || i.docId === r) && (e.includeRolledBack || i.status !== "rolled_back")), o = Math.max(0, Number(e.limit) || 0);
  return o ? s.slice(-o) : s;
}
async function Ty(e) {
  const t = String(e.managerRunId || "").trim(), n = String(e.sessionId || "").trim(), r = String(e.path || "").trim();
  if (!t || !n || !r) return null;
  const s = await as.get([t, r]);
  if (s) return s;
  const o = Ct(), i = await Rn.get([n, r]) || null, a = {
    managerRunId: t,
    sessionId: n,
    path: r,
    beforeExists: !!i,
    beforeFile: i ? Bt(i, void 0) : void 0,
    beforeHash: Wb(i),
    afterHash: "",
    rollbackStatus: "pending",
    error: "",
    createdAt: o,
    updatedAt: o
  };
  return await as.put(a), a;
}
async function pT(e) {
  const t = String(e.managerRunId || "").trim(), n = String(e.sessionId || "").trim(), r = String(e.path || "").trim();
  if (!t || !n || !r || !await Ty({
    managerRunId: t,
    sessionId: n,
    path: r
  })) return null;
  const s = await Rn.get([n, r]) || null;
  return await as.update([t, r], {
    afterHash: Wb(s),
    updatedAt: Ct()
  }), await as.get([t, r]) || null;
}
async function $k(e) {
  const t = String(e.managerRunId || "").trim(), n = String(e.sessionId || "").trim(), r = String(e.docType || "tavern.map"), s = String(e.docId || "main").trim() || "main";
  if (!t || !n || !r || !s) return null;
  const o = await Jr.get([
    t,
    r,
    s
  ]);
  if (o) return o;
  const i = Ct(), a = await Tl(n, r, s), l = {
    managerRunId: t,
    sessionId: n,
    docType: r,
    docId: s,
    beforeExists: !!a,
    beforeDocument: a ? Bt(a, void 0) : void 0,
    beforeHash: Vb(a),
    afterHash: "",
    rollbackStatus: "pending",
    error: "",
    createdAt: i,
    updatedAt: i
  };
  return await Jr.put(l), l;
}
async function cH(e) {
  const t = String(e.managerRunId || "").trim(), n = String(e.sessionId || "").trim(), r = String(e.docType || "tavern.map"), s = String(e.docId || "main").trim() || "main";
  if (!t || !n || !r || !s || !await $k({
    managerRunId: t,
    sessionId: n,
    docType: r,
    docId: s
  })) return null;
  const o = await Tl(n, r, s);
  return await Jr.update([
    t,
    r,
    s
  ], {
    afterHash: Vb(o),
    updatedAt: Ct()
  }), await Jr.get([
    t,
    r,
    s
  ]) || null;
}
async function Kb(e = "") {
  const t = String(e || "").trim();
  return t ? await as.where("managerRunId").equals(t).sortBy("updatedAt") : [];
}
async function zb(e = "") {
  const t = String(e || "").trim();
  return t ? await Jr.where("managerRunId").equals(t).sortBy("updatedAt") : [];
}
async function Dk(e = "") {
  const t = String(e || "").trim();
  if (!t) return {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const n = await Jn.get(t);
  if (!n) return {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const r = (await Kb(t)).reverse();
  let s = 0, o = 0;
  const i = [];
  for (const l of r) {
    if (l.rollbackStatus === "rolled_back" || l.rollbackStatus === "skipped") {
      o += 1;
      continue;
    }
    if (l.rollbackStatus === "conflict") {
      i.push(l.path), o += 1;
      continue;
    }
    const u = await Rn.get([l.sessionId, l.path]) || null;
    if (!l.afterHash) {
      o += 1, await as.update([l.managerRunId, l.path], {
        rollbackStatus: "skipped",
        error: "snapshot_after_hash_missing",
        updatedAt: Ct()
      });
      continue;
    }
    if (Wb(u) !== l.afterHash) {
      i.push(l.path), await as.update([l.managerRunId, l.path], {
        rollbackStatus: "conflict",
        error: "rollback_conflict_current_file_changed",
        updatedAt: Ct()
      });
      continue;
    }
    l.beforeExists && l.beforeFile ? await Rn.put(Bt(l.beforeFile, l.beforeFile)) : await Rn.delete([l.sessionId, l.path]), s += 1, await as.update([l.managerRunId, l.path], {
      rollbackStatus: "rolled_back",
      error: "",
      updatedAt: Ct()
    });
  }
  const a = await Nr.get([n.sessionId, "markdown-derived"]);
  return await Nr.put({
    sessionId: n.sessionId,
    kind: "markdown-derived",
    status: "stale",
    error: i.length ? `rollback_conflict:${i.join(",")}` : "",
    sourceFingerprint: "",
    derivedAt: Ct(),
    updatedAt: Ct(),
    files: Array.isArray(a?.files) ? a.files : []
  }), await Ds(t, {
    status: "rolled_back",
    error: Mk(n.error, i)
  }), await kt.update(n.sessionId, { updatedAt: Ct() }), {
    rolledBack: s,
    conflicts: i,
    skipped: o
  };
}
async function Lk(e = "") {
  const t = String(e || "").trim();
  if (!t) return {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const n = await Jn.get(t);
  if (!n) return {
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const r = (await zb(t)).reverse();
  let s = 0, o = 0;
  const i = [];
  for (const a of r) {
    const l = `${a.docType}/${a.docId}`;
    if (a.rollbackStatus === "rolled_back" || a.rollbackStatus === "skipped") {
      o += 1;
      continue;
    }
    if (a.rollbackStatus === "conflict") {
      i.push(l), o += 1;
      continue;
    }
    const u = await Tl(a.sessionId, a.docType, a.docId);
    if (!a.afterHash) {
      o += 1, await Jr.update([
        a.managerRunId,
        a.docType,
        a.docId
      ], {
        rollbackStatus: "skipped",
        error: "snapshot_after_hash_missing",
        updatedAt: Ct()
      });
      continue;
    }
    if (Vb(u) !== a.afterHash) {
      i.push(l), await Jr.update([
        a.managerRunId,
        a.docType,
        a.docId
      ], {
        rollbackStatus: "conflict",
        error: "rollback_conflict_current_state_changed",
        updatedAt: Ct()
      });
      continue;
    }
    a.beforeExists && a.beforeDocument ? await $r.put(Bt(a.beforeDocument, a.beforeDocument)) : await $r.delete([
      a.sessionId,
      a.docType,
      a.docId
    ]);
    const d = await Ko.where("managerRunId").equals(a.managerRunId).toArray();
    await Promise.all(d.filter((h) => h.docType === a.docType && h.docId === a.docId).map((h) => Ko.update(h.id, {
      status: "rolled_back",
      updatedAt: Ct()
    }))), s += 1, await Jr.update([
      a.managerRunId,
      a.docType,
      a.docId
    ], {
      rollbackStatus: "rolled_back",
      error: "",
      updatedAt: Ct()
    });
  }
  return await Ds(t, {
    status: "rolled_back",
    error: Mk(n.error, i)
  }), await kt.update(n.sessionId, { updatedAt: Ct() }), {
    rolledBack: s,
    conflicts: i,
    skipped: o
  };
}
async function dH(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  if (!n || !Number.isFinite(r)) return {
    runIds: [],
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const s = (await Jn.where("sessionId").equals(n).toArray()).filter((l) => l.trigger === "after_turn" && (Number(l.userOrder) >= r || Number(l.assistantOrder) >= r)).sort((l, u) => u.updatedAt - l.updatedAt);
  let o = 0, i = 0;
  const a = [];
  for (const l of s) {
    if ((await Kb(l.id)).some((u) => String(u.afterHash || "").trim())) {
      const u = await Dk(l.id);
      o += u.rolledBack, i += u.skipped, a.push(...u.conflicts);
      continue;
    }
    await Ds(l.id, {
      status: ["queued", "running"].includes(l.status) ? "cancelled" : "superseded",
      error: "manager_source_messages_superseded"
    });
  }
  return {
    runIds: s.map((l) => l.id),
    rolledBack: o,
    conflicts: a,
    skipped: i
  };
}
async function fH(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  if (!n || !Number.isFinite(r)) return {
    runIds: [],
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  const s = (await Jn.where("sessionId").equals(n).toArray()).filter((l) => l.trigger === "after_turn" && (Number(l.userOrder) >= r || Number(l.assistantOrder) >= r)).sort((l, u) => u.updatedAt - l.updatedAt);
  let o = 0, i = 0;
  const a = [];
  for (const l of s) {
    if (!(await zb(l.id)).some((d) => String(d.afterHash || "").trim())) continue;
    const u = await Lk(l.id);
    o += u.rolledBack, i += u.skipped, a.push(...u.conflicts);
  }
  return {
    runIds: s.map((l) => l.id),
    rolledBack: o,
    conflicts: a,
    skipped: i
  };
}
async function Ey(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  if (!n || !Number.isFinite(r)) return 0;
  const s = Ct(), o = (await Rn.where("sessionId").equals(n).toArray()).filter((i) => {
    const a = String(i.path || "").match(/^memory\/turns\/.+-(\d+)\.md$/), l = Number(a ? a[1] : i.staleFromOrder);
    return Number.isFinite(l) && l >= r;
  });
  return o.length ? (await rn.transaction("rw", Rn, Nr, kt, async () => {
    await Promise.all(o.map((a) => Rn.update([n, a.path], {
      status: "stale",
      staleFromOrder: r,
      updatedAt: s
    })));
    const i = await Nr.get([n, "markdown-derived"]);
    await Nr.put({
      sessionId: n,
      kind: "markdown-derived",
      status: "stale",
      error: "",
      sourceFingerprint: "",
      derivedAt: s,
      updatedAt: s,
      files: Array.isArray(i?.files) ? i.files : []
    }), await kt.update(n, { updatedAt: s });
  }), o.length) : 0;
}
async function Zh() {
  const e = await lo.get("activeAssistantPresetId");
  return String(e?.value || "littlewhitebox-assistant-default").trim() || "littlewhitebox-assistant-default";
}
async function Ga(e = Kp) {
  const t = String(e || "littlewhitebox-assistant-default").trim() || "littlewhitebox-assistant-default";
  return await lo.put({
    key: "activeAssistantPresetId",
    value: t,
    updatedAt: Ct()
  }), t;
}
async function qu(e, t = {}) {
  const n = Ct(), r = String(e.id || Yp("assistant-preset")), s = await ra.get(r), o = Ub({
    ...e,
    id: r,
    updatedAt: n
  }), i = {
    id: r,
    name: o.name,
    description: o.description,
    version: "",
    isBuiltIn: t.isBuiltIn === !0,
    createdAt: Number(s?.createdAt) || n,
    updatedAt: n,
    preset: o
  };
  return await ra.put(i), i;
}
async function hH(e = "") {
  const t = String(e || "").trim();
  return !t || t === "littlewhitebox-assistant-default" ? !1 : (await ra.delete(t), await Zh() === t && await Ga(Kp), !0);
}
async function Ok() {
  const e = await ra.get(Kp);
  return e || qu(Bc(), { isBuiltIn: !0 });
}
async function Ma() {
  return await Ok(), ra.orderBy("updatedAt").reverse().toArray();
}
async function zg() {
  await Ok();
  const e = await Zh();
  return Ub((await ra.get(e) || await ra.get("littlewhitebox-assistant-default"))?.preset || Bc());
}
function Jb(e = []) {
  return e.some((t) => !!t.rect || typeof t.circle == "number" || Array.isArray(t.path) || Array.isArray(t.curve) || !!t.icon);
}
function pH(e) {
  return e ? e.meta.status === "active" && Jb(e.elements) : !1;
}
var Fk = [
  "rect",
  "circle",
  "path",
  "curve",
  "icon",
  "text"
];
function oo(e) {
  return JSON.parse(JSON.stringify(e));
}
function Qs(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function Bk(e, t) {
  const n = { ...e };
  return Object.entries(t).forEach(([r, s]) => {
    if (s == null) {
      delete n[r];
      return;
    }
    if (Qs(s) && Qs(n[r])) {
      n[r] = Bk(n[r], s);
      return;
    }
    n[r] = oo(s);
  }), n;
}
function mH(e) {
  return Fk.filter((t) => t === "circle" ? typeof e.circle == "number" : t === "text" ? typeof e.text == "string" && !!e.text.trim() : Array.isArray(e[t]) || typeof e[t] == "string" && !!e[t]);
}
function gH(e, t) {
  return t === void 0 ? e ? oo(e) : void 0 : Object.keys(t).length ? {
    ...e || {},
    ...t
  } : void 0;
}
function Uk(e, t) {
  const n = mH(t).length > 0, r = oo(e);
  return n && (Fk.forEach((s) => {
    delete r[s];
  }), delete r.style, delete r.fill, delete r.closed), Object.entries(t).forEach(([s, o]) => {
    if (s !== "style") {
      if (o === void 0) {
        delete r[s];
        return;
      }
      r[s] = oo(o);
    }
  }), "style" in t && (r.style = n ? oo(t.style) : gH(e.style, t.style)), r;
}
function vH(e, t) {
  let n = oo(e);
  return t.forEach((r) => {
    const s = String(r.op || "").trim();
    if (s === "meta") {
      const o = r.set && Qs(r.set) ? r.set : r.changes && Qs(r.changes) ? r.changes : null;
      o && (n.meta = Bk(n.meta, o));
      return;
    }
    if (s === "add") {
      const o = r.element;
      if (!o || !Qs(o) || typeof o.id != "string") return;
      const i = oo(o), a = n.elements.findIndex((l) => l.id === i.id);
      if (a >= 0) {
        n.elements[a] = i;
        return;
      }
      n.elements.push(i);
      return;
    }
    if (s === "remove") {
      const o = String(r.id || "").trim();
      if (!o) return;
      n.elements = n.elements.filter((i) => i.id !== o);
      return;
    }
    if (s === "modify") {
      const o = String(r.id || "").trim(), i = r.set && Qs(r.set) ? r.set : r.changes && Qs(r.changes) ? r.changes : null;
      if (!o || !i) return;
      n.elements = n.elements.map((a) => a.id === o ? Uk(a, i) : a);
      return;
    }
    if (s === "init" || s === "reset") {
      const o = r.document;
      o && Qs(o) && (n = oo(o));
      return;
    }
    if (s === "replace") {
      const o = String(r.id || "").trim(), i = r.element;
      if (!o || !i || !Qs(i)) return;
      n.elements = n.elements.map((a) => a.id === o ? oo(i) : a);
    }
  }), n;
}
var zo = {
  LIST: "StateList",
  READ: "StateRead",
  PATCH: "StatePatch"
}, Vr = my, Jo = gy, Cy = 2e3, yH = 1e3, ul = 300, Yb = [
  "rect",
  "circle",
  "path",
  "curve",
  "icon",
  "text"
], _H = [
  "rect",
  "circle",
  "path",
  "curve",
  "icon"
], Xb = /* @__PURE__ */ new Set([
  "wall",
  "road",
  "water",
  "terrain",
  "furniture",
  "door",
  "danger",
  "marker",
  "label",
  "grid",
  "magic",
  "secret"
]), qk = /* @__PURE__ */ new Set([
  "x",
  "o",
  "+",
  "star",
  "tree",
  "skull",
  "arrow-n",
  "arrow-s",
  "arrow-e",
  "arrow-w",
  "stairs-up",
  "stairs-down"
]), bH = /* @__PURE__ */ new Set([
  "parchment",
  "paper",
  "dark",
  "blueprint",
  "grid"
]), wH = /* @__PURE__ */ new Set(["uninitialized", "active"]);
function SH() {
  return Date.now();
}
function ks(e) {
  return JSON.parse(JSON.stringify(e));
}
function mo(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function zt(e = "", t = 400) {
  const n = String(e ?? "").replace(/\s+/g, " ").trim();
  return n.length > t ? n.slice(0, t) : n;
}
function mT(e) {
  return JSON.stringify(e ?? null);
}
function Jg(e, t) {
  return mT(e) === mT(t);
}
function TH(e = Vr) {
  if (String(e || Vr).trim() !== Vr) throw new Error("state_doc_type_not_supported");
  return Vr;
}
function EH(e = Jo) {
  const t = String(e || Jo).trim() || Jo;
  if (!/^[\w.-]{1,80}$/i.test(t)) throw new Error("state_doc_id_invalid");
  return t;
}
function Hk(e, t = "parchment") {
  return bH.has(String(e || "").trim()) ? String(e).trim() : t;
}
function Gk(e, t = "active") {
  return wH.has(String(e || "").trim()) ? String(e).trim() : t;
}
function Ay(e) {
  if (e === null || !Array.isArray(e) || e.length !== 4) return null;
  const t = e.map((n) => Number(n));
  return t.every((n) => Number.isFinite(n)) ? [
    t[0],
    t[1],
    Math.max(1, t[2]),
    Math.max(1, t[3])
  ] : null;
}
function Yo(e) {
  if (Array.isArray(e) && e.length >= 2) {
    const t = Number(e[0]), n = Number(e[1]);
    return Number.isFinite(t) && Number.isFinite(n) ? [t, n] : null;
  }
  if (mo(e)) {
    const t = Number(e.x ?? e.cx), n = Number(e.y ?? e.cy);
    return Number.isFinite(t) && Number.isFinite(n) ? [t, n] : null;
  }
  return null;
}
function Wk(e, t) {
  const n = Yo(e);
  if (!n) throw new Error(t);
  return n;
}
function Iy(e, t, n = 2) {
  if (!Array.isArray(e) || e.length < n) throw new Error(t);
  const r = e.map((s) => Yo(s));
  if (r.some((s) => !s)) throw new Error(t);
  return r;
}
function Xp(e) {
  const t = Number(e);
  return Number.isFinite(t) && t > 0 ? t : null;
}
function Qb(e) {
  if (!Array.isArray(e) || e.length < 2) return null;
  const t = Number(e[0]), n = Number(e[1]);
  return Number.isFinite(t) && Number.isFinite(n) ? [t, n] : null;
}
function jh(e, t) {
  const n = String(e || "").trim();
  return n && Xb.has(n) ? n : t;
}
function Zb(e) {
  const t = String(e || "").trim();
  return qk.has(t) ? t : void 0;
}
function jb(e) {
  if (e === null) return;
  const t = mo(e) ? e : {}, n = {}, r = zt(t.color, 80), s = zt(t.dash, 80), o = Xp(t.width);
  return r && (n.color = r), s && (n.dash = s), o !== null && (n.width = o), Object.keys(n).length ? n : void 0;
}
function Vk(e, t = {}) {
  const n = mo(e) ? e : {}, r = Gk(n.status ?? t.status ?? "active", t.status ?? "active"), s = "name" in n ? n.name : t.name, o = "hint" in n ? n.hint : t.hint;
  return {
    name: s === null ? null : zt(s, 120) || null,
    viewBox: "viewBox" in n ? Ay(n.viewBox) : Ay(t.viewBox ?? null),
    theme: Hk(n.theme ?? t.theme ?? "parchment", t.theme ?? "parchment"),
    status: r,
    ...r === "uninitialized" ? { hint: zt(o ?? vy(), 1200) || vy() } : {}
  };
}
function CH(e) {
  if (!mo(e)) throw new Error("map_meta_set_required");
  const t = {};
  return "name" in e && (t.name = e.name === null ? null : zt(e.name, 120) || null), "viewBox" in e && (t.viewBox = Ay(e.viewBox)), "theme" in e && (t.theme = Hk(e.theme)), "status" in e && (t.status = Gk(e.status)), "hint" in e && (t.hint = zt(e.hint, 1200) || void 0), t;
}
function xy(e, t) {
  return Vk({
    ...e,
    ...t,
    ...t.hint === void 0 && "hint" in t ? { hint: null } : {}
  }, e);
}
function Kk(e) {
  for (const t of Yb) {
    if (t === "circle") {
      if (typeof e.circle == "number") return t;
      continue;
    }
    if (t === "text") {
      if (typeof e.text == "string" && e.text.trim()) return t;
      continue;
    }
    if (Array.isArray(e[t]) || typeof e[t] == "string" && e[t]) return t;
  }
  return null;
}
function ky(e) {
  return e === "text" ? "label" : e === "icon" ? "marker" : "wall";
}
function zk(e, t) {
  const n = Wk(e.center ?? e.pos ?? {
    x: e.cx,
    y: e.cy
  }, `map_element_center_required:${t}`), r = Xp(e.r ?? e.radius);
  if (r === null) throw new Error(`map_element_radius_required:${t}`);
  const s = Number(e.startAngle), o = Number(e.endAngle);
  if (!Number.isFinite(s) || !Number.isFinite(o)) throw new Error(`map_element_arc_angles_required:${t}`);
  const i = o - s, a = Math.max(4, Math.min(24, Math.ceil(Math.abs(i) / 30)));
  return Array.from({ length: a + 1 }, (l, u) => {
    const d = (s + i * u / a) * Math.PI / 180;
    return [Number((n[0] + r * Math.cos(d)).toFixed(2)), Number((n[1] + r * Math.sin(d)).toFixed(2))];
  });
}
function Is(e, t, n) {
  const r = Iy(e, `map_element_points_required:${t}`, 2);
  if (n) return {
    at: n,
    points: r
  };
  const s = r[0];
  return {
    at: s,
    points: r.map(([o, i]) => [Number((o - s[0]).toFixed(2)), Number((i - s[1]).toFixed(2))])
  };
}
function AH(e) {
  const [t, n] = e.at;
  return e.rect ? [t + Math.max(18, e.rect[0] / 2), n - 18] : typeof e.circle == "number" ? [t + e.circle + 18, n - 6] : [t + 18, n - 18];
}
function Jk(e, t = {}) {
  if (!e || !/^[\w:.-]{1,120}$/i.test(e)) throw new Error("map_element_id_invalid");
  if (!t.allowReserved && Yh(e)) throw new Error(`map_element_id_reserved:${e}`);
  return e;
}
function IH(e, t) {
  const n = [...new Set(t)];
  if (!n.length) throw new Error(`map_element_shape_required:${e}`);
  if (n.length !== 1 && !(n.length === 2 && n.includes("text") && n.some((r) => _H.includes(r))))
    throw new Error(`map_element_shape_conflict:${e}`);
}
function jf(e, t, n = {}) {
  const r = Jk(t, { allowReserved: n.allowReservedId === !0 }), s = Kk(e);
  if (!s) throw new Error(`map_element_shape_required:${r}`);
  const o = {
    id: r,
    at: Wk(e.at, `map_element_at_required:${r}`),
    cat: jh(e.cat, ky(s))
  };
  if (e.rect) {
    const i = Qb(e.rect);
    if (!i || i[0] <= 0 || i[1] <= 0) throw new Error(`map_element_rect_invalid:${r}`);
    o.rect = i;
  }
  if (typeof e.circle == "number") {
    if (!(e.circle > 0)) throw new Error(`map_element_radius_required:${r}`);
    o.circle = e.circle;
  }
  if (e.path && (o.path = Iy(e.path, `map_element_points_required:${r}`, 2)), e.curve && (o.curve = Iy(e.curve, `map_element_points_required:${r}`, 2)), e.icon) {
    const i = Zb(e.icon);
    if (!i) throw new Error(`map_element_icon_invalid:${r}`);
    o.icon = i;
  }
  if (e.text !== void 0) {
    const i = zt(e.text, 240);
    if (!i) throw new Error(`map_element_text_required:${r}`);
    o.text = i;
  }
  if (e.closed === !0 && (o.closed = !0), zt(e.fill, 120) && (o.fill = zt(e.fill, 120)), e.style !== void 0) {
    const i = jb(e.style);
    i && (o.style = i);
  }
  return o;
}
function My(e, t = {}) {
  if (!mo(e)) throw new Error("map_element_must_be_object");
  const n = t.source || "model-input", r = Jk(t.forcedId || String(e.id || "").trim(), { allowReserved: t.allowReservedId === !0 || n === "stored-document" });
  let s = Yo(e.at ?? e.pos ?? e.center ?? e.position);
  !s && (e.x !== void 0 || e.y !== void 0) && (s = Yo({
    x: e.x,
    y: e.y
  })), !s && (e.cx !== void 0 || e.cy !== void 0) && (s = Yo({
    x: e.cx,
    y: e.cy
  }));
  const o = {}, i = String(e.type || "").trim(), a = Qb(e.rect ?? e.size ?? (e.width !== void 0 || e.height !== void 0 ? [e.width, e.height] : null));
  a && (o.rect = a);
  const l = i === "arc" ? null : Xp(e.circle ?? e.r ?? e.radius);
  l !== null && (o.circle = l);
  const u = e.path ?? e.points ?? e.line ?? (e.x1 !== void 0 || e.y1 !== void 0 || e.x2 !== void 0 || e.y2 !== void 0 ? [[e.x1, e.y1], [e.x2, e.y2]] : null), d = e.curve, h = zt(e.text ?? e.content ?? e.label ?? e.value, 240);
  h && (o.text = h);
  const p = Zb(e.icon);
  if (p && (o.icon = p), i === "line" && u) {
    const C = Is(u, r, s);
    s = C.at, o.path = C.points;
  } else if (i === "curve" && (d ?? u)) {
    const C = Is(d ?? u, r, s);
    s = C.at, o.curve = C.points;
  } else if (i === "fill" && u) {
    const C = Is(u, r, s);
    s = C.at, o.path = C.points, o.closed = !0, zt(e.fill, 120) && (o.fill = zt(e.fill, 120));
  } else if (i === "arc") {
    const C = Is(zk(e, r), r, s);
    s = C.at, o.curve = C.points;
  } else {
    if (u && !o.path && !o.curve) {
      const C = Is(u, r, s);
      s = C.at, o.path = C.points;
    }
    if (d && !o.curve) {
      const C = Is(d, r, s);
      s = C.at, o.curve = C.points;
    }
  }
  const y = Yb.filter((C) => C === "circle" ? typeof o.circle == "number" : C === "text" ? typeof o.text == "string" && !!o.text.trim() : C in o);
  IH(r, y);
  const w = jh(e.cat, ky(y.find((C) => C !== "text") || y[0] || null)), E = {
    id: r,
    at: s || void 0,
    cat: w,
    closed: e.closed === !0 || o.closed === !0,
    fill: zt(e.fill ?? o.fill, 120) || void 0,
    style: jb(e.style),
    ...o
  };
  if (y.length === 2 && t.splitGeometryText !== !1) {
    const C = y.find((R) => R !== "text"), M = jf({
      id: r,
      at: E.at,
      cat: jh(e.cat, ky(C)),
      closed: E.closed,
      fill: E.fill,
      style: E.style,
      [C]: E[C]
    }, r, { allowReservedId: t.allowReservedId === !0 || n === "stored-document" }), k = yy(r);
    return [M, jf({
      id: k,
      at: AH(M),
      cat: "label",
      text: E.text
    }, k, { allowReservedId: !0 })];
  }
  return [jf(E, r, { allowReservedId: t.allowReservedId === !0 || n === "stored-document" })];
}
function ep(e, t = {}, n = "stored-document") {
  const r = mo(e) ? e : {}, s = Vk(r.meta, t), o = Array.isArray(r.elements) ? r.elements.flatMap((a) => My(a, { source: n })) : [];
  if (o.length > Cy) throw new Error("map_elements_limit_exceeded");
  const i = /* @__PURE__ */ new Set();
  return o.forEach((a) => {
    if (i.has(a.id)) throw new Error(`map_element_duplicate:${a.id}`);
    i.add(a.id);
  }), {
    meta: s,
    elements: o
  };
}
function Yk() {
  return ep(Uc(), Uc().meta, "stored-document");
}
function tp(e) {
  return e?.data ? ep(e.data, Uc().meta, "stored-document") : Yk();
}
function ew(e, t = 0) {
  if (e.meta.status !== "active" || !Jb(e.elements)) return "";
  const n = zt(e.meta.name || "Map", 80) || "Map", r = /* @__PURE__ */ new Map();
  e.elements.forEach((a) => {
    const l = r.get(a.cat) || [];
    l.push(a), r.set(a.cat, l);
  });
  const s = e.elements.filter((a) => typeof a.text == "string" && a.text.trim()).map((a) => zt(a.text, 40)).filter(Boolean).slice(0, 8), o = (r.get("door") || []).map((a) => a.id).slice(0, 8), i = (r.get("danger") || []).map((a) => a.id).slice(0, 8);
  return [
    `Map: ${n} (revision ${t}, ${e.elements.length} elements)`,
    s.length ? `Labels: ${s.join(", ")}` : "",
    o.length ? `Doors/Routes: ${o.join(", ")}` : "",
    i.length ? `Danger/Markers: ${i.join(", ")}` : ""
  ].filter(Boolean).join(`
`);
}
function np(e) {
  return zt(e.meta.name || "Map", 120) || "Map";
}
function Xk(e) {
  return ks(e);
}
function tw(e = "") {
  const [t, n = "element"] = String(e || "").split(":"), r = n || "element";
  switch (t) {
    case "map_element_id_invalid":
      return "Invalid element id. Use a short stable id containing only letters, numbers, underscores, dots, colons, and hyphens.";
    case "map_element_id_reserved":
      return `${r} uses the reserved \`__label__\` prefix. Use a normal id instead.`;
    case "map_element_at_required":
      return `${r} is missing a position. Provide \`at:[x,y]\`. Legacy aliases such as pos/center/x+y are also accepted.`;
    case "map_element_rect_invalid":
      return `${r} has an invalid rect. Use positive dimensions such as \`rect:[120,80]\`.`;
    case "map_element_radius_required":
      return `${r} is missing a valid radius. \`circle\` must be a number greater than 0.`;
    case "map_element_points_required":
      return `${r} is missing a valid point array. \`path\` and \`curve\` need at least two points. With \`at\`, points are relative offsets; without \`at\`, the first point becomes the anchor.`;
    case "map_element_text_required":
      return `${r} is missing text content. \`text\` must be a short non-empty label.`;
    case "map_element_icon_invalid":
      return `${r} has an invalid icon. Use one of x/o/+/star/tree/skull/arrow-n/arrow-s/arrow-e/arrow-w/stairs-up/stairs-down.`;
    case "map_element_shape_required":
      return `${r} is missing a shape field. Every element must provide exactly one of rect/circle/path/curve/icon/text.`;
    case "map_element_shape_conflict":
      return `${r} has multiple shape fields. Only the special "geometry + text" input is allowed, and it will be split into a derived label automatically.`;
    case "map_element_duplicate":
      return `${r} is duplicated. Use a stable unique id.`;
    case "map_element_not_found":
      return `${r} does not exist. Use StateRead summary/elements first to find existing ids.`;
    case "map_element_already_exists":
      return `${r} already exists. Use \`modify\` to change it. If it is already identical, do not repeat the \`add\`.`;
    case "map_element_id_cannot_change":
      return `${r} cannot change id inside a \`modify\` op.`;
    case "map_meta_set_required":
      return "`meta` requires a `set` object.";
    case "map_modify_set_required":
      return `${r} is missing a \`set\` object. \`modify\` requires \`set\`.`;
    case "map_op_not_supported":
      return `Unsupported op: ${r}. Supported canonical ops are meta/add/modify/remove. Legacy init/reset/replace input is still absorbed at runtime.`;
    case "map_element_center_required":
      return `${r} is missing a center point.`;
    case "map_element_arc_angles_required":
      return `${r} is missing arc start/end angles.`;
    default:
      return e;
  }
}
function xH(e) {
  const t = e.slice(0, 3).map((r) => `${r.index >= 0 ? `op ${r.index + 1}` : "transaction"}: ${r.hint || tw(r.error) || r.error}`), n = e.length > t.length ? `; plus ${e.length - t.length} more failures` : "";
  return t.length ? `${t.join("; ")}${n}` : "";
}
function kH(e, t = []) {
  return Jb(e.elements) ? e.meta.status === "uninitialized" ? {
    document: {
      ...e,
      meta: xy(e.meta, { status: "active" })
    },
    statusChanged: !0
  } : {
    document: e,
    statusChanged: !1
  } : e.meta.status === "active" ? (t.push("Map stays uninitialized: it needs at least one spatial geometry element. Name, viewBox, or text labels alone are not enough."), {
    document: {
      ...e,
      meta: xy(e.meta, { status: "uninitialized" })
    },
    statusChanged: !0
  }) : {
    document: e,
    statusChanged: !1
  };
}
function MH(e, t = {}) {
  const n = zt(t.query, 120).toLowerCase(), r = String(t.elementType || t.type || "").trim(), s = String(t.category || t.cat || "").trim(), o = Math.max(0, Number(t.offset) || 0), i = Math.max(1, Math.min(ul, Number(t.limit) || 30)), a = e.elements.filter((d) => {
    const h = Kk(d) || "";
    return r && h !== r || s && d.cat !== s ? !1 : n ? [
      d.id,
      d.cat,
      h,
      d.text,
      d.icon
    ].map((p) => String(p || "").toLowerCase()).join(`
`).includes(n) : !0;
  }), l = a.slice(o, o + i).map(Xk), u = o + l.length < a.length ? o + l.length : 0;
  return {
    elements: l,
    count: a.length,
    truncated: u > 0,
    nextOffset: u
  };
}
function PH(e, t) {
  if (!mo(e)) throw new Error(`map_modify_set_required:${t}`);
  const n = {};
  if ("id" in e && zt(e.id, 120) && zt(e.id, 120) !== t) throw new Error(`map_element_id_cannot_change:${t}`);
  if ("at" in e || "pos" in e || "center" in e || "position" in e || "x" in e || "y" in e || "cx" in e || "cy" in e) {
    const s = Yo(e.at ?? e.pos ?? e.center ?? e.position) || Yo({
      x: e.x,
      y: e.y
    }) || Yo({
      x: e.cx,
      y: e.cy
    });
    if (!s) throw new Error(`map_element_at_required:${t}`);
    n.at = s;
  }
  if ("cat" in e && (n.cat = jh(e.cat, "wall")), "rect" in e || "size" in e || "width" in e || "height" in e) {
    const s = Qb(e.rect ?? e.size ?? [e.width, e.height]);
    if (!s || s[0] <= 0 || s[1] <= 0) throw new Error(`map_element_rect_invalid:${t}`);
    n.rect = s;
  }
  if ("circle" in e || "r" in e || "radius" in e) {
    const s = Xp(e.circle ?? e.r ?? e.radius);
    if (s === null) throw new Error(`map_element_radius_required:${t}`);
    n.circle = s;
  }
  if ("path" in e || "points" in e || "line" in e) {
    const s = n.at || null, o = Is(e.path ?? e.points ?? e.line, t, s);
    n.at || (n.at = o.at), n.path = o.points;
  }
  if ("curve" in e) {
    const s = n.at || null, o = Is(e.curve, t, s);
    n.at || (n.at = o.at), n.curve = o.points;
  }
  if ("icon" in e) {
    const s = Zb(e.icon);
    if (!s) throw new Error(`map_element_icon_invalid:${t}`);
    n.icon = s;
  }
  if ("text" in e || "content" in e || "label" in e || "value" in e) {
    const s = zt(e.text ?? e.content ?? e.label ?? e.value, 240);
    if (!s) throw new Error(`map_element_text_required:${t}`);
    n.text = s;
  }
  "closed" in e && (n.closed = e.closed === !0), "fill" in e && (n.fill = e.fill === null ? void 0 : zt(e.fill, 120) || void 0), "style" in e && (n.style = e.style === null ? void 0 : jb(e.style));
  const r = String(e.type || "").trim();
  if (r === "fill" && !n.path) {
    const s = Is(e.points, t, n.at || null);
    n.at = s.at, n.path = s.points, n.closed = !0;
  }
  if (r === "arc" && !n.curve) {
    const s = Is(zk(e, t), t, n.at || null);
    n.at = s.at, n.curve = s.points;
  }
  return n;
}
function gT(e, t) {
  return jf(Uk(e, t), e.id, { allowReservedId: Yh(e.id) });
}
async function Qk(e = "", t = Vr, n = Jo) {
  if (t !== Vr || n !== Jo) return await Tl(e, t, n);
  const r = await Tl(e, t, n);
  return r || await Rk(e, { touchSession: !1 });
}
function RH(e, t) {
  const n = [];
  return e.elements.forEach((r) => {
    n.push({
      op: "remove",
      id: r.id,
      _internalSoft: !0
    });
  }), n.push({
    op: "meta",
    set: {
      name: t.meta.name,
      viewBox: t.meta.viewBox,
      theme: t.meta.theme,
      status: t.meta.status,
      hint: t.meta.hint
    }
  }), t.elements.forEach((r) => {
    n.push({
      op: "add",
      element: r
    });
  }), n;
}
function NH(e, t) {
  if (!Array.isArray(t)) throw new Error("state_patch_ops_must_be_array");
  if (!t.length) throw new Error("state_patch_ops_required");
  if (t.length > yH) throw new Error("state_patch_ops_limit_exceeded");
  let n = ep(e, e.meta, "stored-document");
  const r = [], s = [], o = /* @__PURE__ */ new Set(), i = [], a = [];
  let l = 0, u = 0, d = !1;
  const h = (C) => n.elements.findIndex((M) => M.id === C), p = (C) => {
    const M = xy(n.meta, C);
    if (Jg(M, n.meta)) {
      u += 1;
      return;
    }
    n.meta = M, r.push({
      op: "meta",
      set: ks(C)
    }), d = !0, l += 1, o.add("meta");
  }, y = (C) => {
    const M = h(C.id);
    if (M >= 0) {
      if (Jg(n.elements[M], C)) {
        u += 1;
        return;
      }
      throw new Error(`map_element_already_exists:${C.id}`);
    }
    n.elements.push(ks(C)), r.push({
      op: "add",
      element: ks(C)
    }), d = !0, l += 1, o.add(C.id);
  }, w = (C, M = {}) => {
    const k = h(C);
    if (k < 0) {
      if (M.soft) {
        u += 1;
        return;
      }
      throw new Error(`map_element_not_found:${C}`);
    }
    const [R] = n.elements.splice(k, 1);
    if (i.push(ks(R)), r.push({
      op: "remove",
      id: C,
      ...M.soft ? { _internalSoft: !0 } : {}
    }), d = !0, l += 1, o.add(C), M.cascadeLabel !== !1 && !Yh(C)) {
      const D = yy(C);
      h(D) >= 0 && w(D, {
        soft: !0,
        cascadeLabel: !1
      });
    }
  }, E = (C, M) => {
    const k = h(C);
    if (k < 0) throw new Error(`map_element_not_found:${C}`);
    const R = n.elements[k], D = gT(R, M);
    if (Jg(R, D)) {
      u += 1;
      return;
    }
    if (n.elements[k] = D, r.push({
      op: "modify",
      id: C,
      set: ks(M)
    }), d = !0, l += 1, o.add(C), !Yh(C) && M.at && R.at) {
      const m = yy(C), b = h(m);
      if (b >= 0) {
        const v = n.elements[b], T = [M.at[0] - R.at[0], M.at[1] - R.at[1]];
        if (T[0] || T[1]) {
          const I = [Number((v.at[0] + T[0]).toFixed(2)), Number((v.at[1] + T[1]).toFixed(2))], x = gT(v, { at: I });
          n.elements[b] = x, r.push({
            op: "modify",
            id: m,
            set: { at: I }
          }), o.add(m);
        }
      }
    }
  };
  for (let C = 0; C < t.length; C += 1) {
    const M = t[C];
    try {
      if (!mo(M)) throw new Error("op_must_be_object");
      const k = String(M.op || "").trim();
      if (!k) throw new Error("map_op_not_supported:empty");
      if (k === "meta") {
        p(CH(M.set ?? M.changes ?? M.meta));
        continue;
      }
      if (k === "add") {
        My(M.element ?? M, { source: "model-input" }).forEach((R) => y(R));
        continue;
      }
      if (k === "remove") {
        const R = zt(M.id, 120);
        if (!R) throw new Error("map_element_id_invalid");
        w(R);
        continue;
      }
      if (k === "modify") {
        const R = zt(M.id, 120);
        if (!R) throw new Error("map_element_id_invalid");
        E(R, PH(M.set ?? M.changes, R));
        continue;
      }
      if (k === "replace") {
        const R = zt(M.id, 120);
        if (!R) throw new Error("map_element_id_invalid");
        if (h(R) < 0) throw new Error(`map_element_not_found:${R}`);
        w(R, { cascadeLabel: !0 }), My(M.element, {
          forcedId: R,
          source: "model-input"
        }).forEach((D) => y(D));
        continue;
      }
      if (k === "init" || k === "reset") {
        const R = ep(M.document || {
          meta: {
            ...mo(M.meta) ? M.meta : {},
            status: "active"
          },
          elements: M.elements
        }, {
          ...n.meta,
          status: "active",
          hint: n.meta.hint
        }, "model-input");
        if (k === "init" && n.meta.status === "active" && n.elements.length && M.replaceDocument !== !0) throw new Error("state_init_existing_document_requires_reset");
        RH(n, R).forEach((D) => {
          D.op === "meta" ? p(D.set) : D.op === "add" ? y(D.element) : D.op === "remove" && w(D.id, {
            soft: D._internalSoft === !0,
            cascadeLabel: !1
          });
        });
        continue;
      }
      throw new Error(`map_op_not_supported:${k}`);
    } catch (k) {
      const R = k instanceof Error ? k.message : String(k || "map_op_failed");
      a.push({
        index: C,
        error: R,
        hint: tw(R)
      });
      break;
    }
  }
  if (!a.length) {
    const C = kH(n, s);
    n = C.document, C.statusChanged && (r.push({
      op: "meta",
      set: { status: n.meta.status }
    }), d = !0, o.add("meta"));
  }
  return !a.length && n.elements.length > Cy && a.push({
    index: -1,
    error: "map_elements_limit_exceeded",
    hint: `Map has more than ${Cy} elements. Split or reduce the element count.`
  }), {
    document: n,
    effectiveOps: r,
    appliedCount: l,
    satisfiedCount: u,
    failed: a,
    warnings: s,
    changedIds: [...o],
    removedElements: i,
    changed: d
  };
}
function $H() {
  return {
    type: "object",
    description: "One map element. It must have `id` and `cat`, plus exactly one shape field: `rect`, `circle`, `path`, `curve`, `icon`, or `text`. Most elements use `at:[x,y]`; `path` and `curve` may omit `at` and let the first point become the anchor.",
    properties: {
      id: {
        type: "string",
        description: "Stable unique id. Do not use the reserved `__label__` prefix."
      },
      at: {
        type: "array",
        items: { type: "number" },
        minItems: 2,
        maxItems: 2,
        description: "Anchor coordinate `[x,y]`. Most elements should provide `at`; `path` and `curve` may omit it and use the first point as the anchor. North = smaller y, south = larger y, west = smaller x, east = larger x."
      },
      cat: {
        type: "string",
        enum: [...Xb],
        description: "Semantic category such as wall, door, marker, terrain, road, or label."
      },
      rect: {
        type: "array",
        items: { type: "number" },
        minItems: 2,
        maxItems: 2,
        description: "Rectangle `[width,height]`. The top-left corner is at `at`."
      },
      circle: {
        type: "number",
        description: "Circle radius. The center is at `at`."
      },
      path: {
        type: "array",
        items: {
          type: "array",
          items: { type: "number" },
          minItems: 2,
          maxItems: 2
        },
        description: "Polyline point array. With `at`, points are relative offsets. Without `at`, points are absolute coordinates and the first point becomes the anchor."
      },
      curve: {
        type: "array",
        items: {
          type: "array",
          items: { type: "number" },
          minItems: 2,
          maxItems: 2
        },
        description: "Smooth curve point array. The anchor and relative/absolute rules are the same as `path`."
      },
      icon: {
        type: "string",
        enum: [...qk],
        description: "Named icon marker."
      },
      text: {
        type: "string",
        description: "Short label text. If you provide text together with geometry in an add input, the runtime will split the text into a derived label element automatically."
      },
      closed: {
        type: "boolean",
        description: "Whether a path or curve should be closed."
      },
      fill: {
        type: "string",
        description: "Fill token or color name for closed shapes."
      },
      style: {
        type: "object",
        description: "Optional visual style overrides.",
        properties: {
          color: {
            type: "string",
            description: "Stroke or text color."
          },
          width: {
            type: "number",
            description: "Stroke width."
          },
          dash: {
            type: "string",
            description: "Dash pattern string."
          }
        },
        additionalProperties: !1
      }
    },
    required: ["id", "cat"],
    additionalProperties: !1
  };
}
function DH() {
  const e = $H();
  return [
    {
      type: "function",
      function: {
        name: zo.LIST,
        description: [
          "List structured state documents in the current session.",
          "Returns document entries only. It does not read full state, elements, or patch history.",
          "Use before StateRead when you need available structured documents such as `tavern.map/main`."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: { docType: {
            type: "string",
            enum: [Vr],
            description: "Optional structured document type filter. Currently `tavern.map`."
          } },
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: zo.READ,
        description: [
          "Read a structured state document in the current session.",
          "Use `summary` first. It returns the revision plus compact meta fields: status, name, viewBox, theme, hint, and digest.",
          "For tavern maps, the default document is `tavern.map/main`. New sessions already include a seed map, so this document is readable even before first initialization.",
          "Use `elements` to browse or filter map elements, `element` for one stable id, `document` for the full current state, and `history` for saved patch transactions."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            docType: {
              type: "string",
              enum: [Vr],
              description: "Structured document type. Currently `tavern.map`."
            },
            docId: {
              type: "string",
              description: "Structured document id. Defaults to `main` for tavern maps."
            },
            mode: {
              type: "string",
              enum: [
                "summary",
                "elements",
                "document",
                "element",
                "history"
              ],
              description: "`summary` returns compact meta, `elements` pages/filter element summaries, `document` returns the full state, `element` returns one exact id, and `history` returns saved patch transactions."
            },
            elementId: {
              type: "string",
              description: "Required for `element` mode. Exact element id to read."
            },
            elementType: {
              type: "string",
              enum: [...Yb],
              description: "Optional `elements`-mode shape filter such as rect, circle, path, curve, icon, or text."
            },
            category: {
              type: "string",
              enum: [...Xb],
              description: "Optional `elements`-mode category filter such as wall, door, marker, terrain, road, or label."
            },
            query: {
              type: "string",
              description: "Optional `elements`-mode text query matched against id, category, shape, icon, and label text."
            },
            offset: {
              type: "number",
              minimum: 0,
              description: "Pagination offset for `elements` or `history` results. Default 0."
            },
            limit: {
              type: "number",
              minimum: 1,
              maximum: ul,
              description: "Maximum `elements` or `history` results to return. Default 30 for `elements`, 20 for `history`."
            },
            tail: {
              type: "number",
              minimum: 1,
              maximum: ul,
              description: "For `history` mode, return the final N patch transactions."
            }
          },
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: zo.PATCH,
        description: [
          "Apply map patch transactions to the current session.",
          "Canonical ops are `meta`, `add`, `modify`, and `remove`. One StatePatch call is one atomic transaction and becomes exactly one revision when it saves.",
          "Use `meta` to update document fields such as name, viewBox, theme, status, or hint. Use `add` to create elements, `modify` to change existing ids, and `remove` to delete ids.",
          "Each element has `id` and `cat`, plus exactly one shape field: `rect`, `circle`, `path`, `curve`, `icon`, or `text`. Most elements use `at:[x,y]`; `path` and `curve` may omit `at` and use the first point as the anchor.",
          "With `at`, `path` and `curve` points are relative offsets. Without `at`, the points are treated as absolute coordinates and the stored result becomes relative to the first point.",
          "If one add element contains geometry plus text, the runtime splits the text into a system label element automatically.",
          "`meta.viewBox` is the camera. Changing it does not move elements. Move actors by changing their `at`, then adjust `viewBox` only if the camera should follow.",
          "Legacy `init`, `reset`, and `replace` input is still absorbed at runtime, but do not rely on it in new calls."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            docType: {
              type: "string",
              enum: [Vr],
              description: "Structured document type. Currently `tavern.map`."
            },
            docId: {
              type: "string",
              description: "Structured document id. Defaults to `main` for tavern maps."
            },
            baseRevision: {
              type: "number",
              description: "Optional optimistic revision check from StateRead summary/document."
            },
            dryRun: {
              type: "boolean",
              description: "Validate and simulate the transaction without saving or incrementing the revision."
            },
            desc: {
              type: "string",
              description: "Short one-line summary of this turn’s spatial update."
            },
            ops: {
              type: "array",
              description: "Patch ops as one atomic transaction. Use canonical `meta`, `add`, `modify`, or `remove` ops.",
              items: {
                type: "object",
                properties: {
                  op: {
                    type: "string",
                    enum: [
                      "meta",
                      "add",
                      "modify",
                      "remove"
                    ],
                    description: "Operation type. Use `meta` for document fields, `add` for a new element, `modify` for an existing id, and `remove` for an existing id."
                  },
                  id: {
                    type: "string",
                    description: "Exact element id for `modify` or `remove`."
                  },
                  set: {
                    type: "object",
                    description: "For `meta`, the meta fields to update. For `modify`, the partial element fields to change. Shape-field changes replace the previous shape; `style` merges by field."
                  },
                  element: {
                    ...e,
                    description: "Full element object for `add`."
                  }
                },
                required: ["op"],
                additionalProperties: !1
              }
            }
          },
          required: ["ops"],
          additionalProperties: !1
        }
      }
    }
  ];
}
async function LH(e = "", t = "", n = {}, r = {}) {
  const s = String(e || "").trim();
  if (!s) return {
    ok: !1,
    summary: "Missing sessionId.",
    error: "state_session_required"
  };
  try {
    const o = TH(n.docType || Vr), i = EH(n.docId || Jo);
    if (t === zo.LIST) {
      const a = await Pk(s, {
        docType: o,
        includeStale: !0
      });
      return {
        ok: !0,
        summary: `Found ${a.length} structured state document(s).`,
        count: a.length,
        documents: a.map((l) => ({
          docType: l.docType,
          docId: l.docId,
          title: l.title,
          revision: l.revision,
          digest: l.digest,
          status: l.status,
          updatedAt: l.updatedAt
        }))
      };
    }
    if (t === zo.READ) {
      const a = String(n.mode || "summary").trim() || "summary", l = await Qk(s, o, i);
      if (!l) return {
        ok: !1,
        summary: `${o}/${i} does not exist.`,
        docType: o,
        docId: i,
        error: "state_document_not_found"
      };
      const u = tp(l);
      if (a === "summary") return {
        ok: !0,
        summary: `Read summary for ${l.title || i}, revision ${l.revision}, status ${u.meta.status}.`,
        docType: o,
        docId: i,
        title: l.title,
        revision: l.revision,
        digest: l.digest,
        meta: ks(u.meta),
        elementCount: u.elements.length
      };
      if (a === "document") return {
        ok: !0,
        summary: `Read full state for ${l.title || i}, revision ${l.revision}.`,
        docType: o,
        docId: i,
        title: l.title,
        revision: l.revision,
        digest: l.digest,
        meta: ks(u.meta),
        elementCount: u.elements.length,
        document: u
      };
      if (a === "element") {
        const d = zt(n.elementId, 120);
        if (!d) return {
          ok: !1,
          summary: "Missing elementId.",
          docType: o,
          docId: i,
          error: "state_element_id_required"
        };
        const h = u.elements.find((p) => p.id === d);
        return h ? {
          ok: !0,
          summary: `Read element ${d}.`,
          docType: o,
          docId: i,
          revision: l.revision,
          element: Xk(h)
        } : {
          ok: !1,
          summary: `${d} does not exist.`,
          docType: o,
          docId: i,
          revision: l.revision,
          error: "state_element_not_found"
        };
      }
      if (a === "elements") {
        const d = MH(u, n);
        return {
          ok: !0,
          summary: `Matched ${d.count} map element(s); returned ${d.elements.length}.`,
          docType: o,
          docId: i,
          revision: l.revision,
          count: d.count,
          truncated: d.truncated,
          nextOffset: d.nextOffset,
          elements: d.elements
        };
      }
      if (a === "history") {
        const d = await Nk({
          sessionId: s,
          docType: o,
          docId: i
        }), h = Math.max(0, Number(n.tail) || 0), p = Math.max(1, Math.min(ul, Number(n.limit) || 20)), y = Math.max(0, Number(n.offset) || 0), w = h > 0 ? Math.max(0, d.length - Math.min(ul, h)) : y, E = d.slice(w, w + (h > 0 ? Math.min(ul, h) : p)), C = w + E.length < d.length ? w + E.length : 0;
        return {
          ok: !0,
          summary: `Found ${d.length} saved patch transaction(s); returned ${E.length}.`,
          docType: o,
          docId: i,
          revision: l.revision,
          count: d.length,
          truncated: C > 0,
          nextOffset: C,
          patches: E
        };
      }
      return {
        ok: !1,
        summary: `Unsupported StateRead mode: ${a}`,
        docType: o,
        docId: i,
        error: "state_read_mode_invalid"
      };
    }
    return t === zo.PATCH ? Array.isArray(n.ops) ? (await r.beforeWriteGuard?.(), await rn.transaction("rw", $r, Ko, Jr, kt, async () => {
      const a = await Tl(s, o, i), l = Number(a?.revision) || 0;
      if (Number.isFinite(Number(n.baseRevision)) && Number(n.baseRevision) !== l) return {
        ok: !1,
        summary: `Revision changed: current is ${l}, but this call was based on ${Number(n.baseRevision)}. Run StateRead again before patching.`,
        docType: o,
        docId: i,
        revision: l,
        error: "state_revision_conflict"
      };
      const u = NH(a ? tp(a) : Yk(), n.ops);
      if (u.failed.length) {
        const w = xH(u.failed);
        return {
          ok: !1,
          summary: `StatePatch was not saved: ${u.failed.length} op(s) failed.${w ? ` ${w}` : ""}`,
          docType: o,
          docId: i,
          revision: l,
          changed: !1,
          appliedCount: 0,
          satisfiedCount: u.satisfiedCount,
          failedCount: u.failed.length,
          warnings: u.warnings,
          error: "state_patch_failed",
          details: u.failed
        };
      }
      if (!u.changed) return {
        ok: !0,
        summary: "State is already at the target result. No write was needed.",
        docType: o,
        docId: i,
        revision: l,
        changed: !1,
        appliedCount: 0,
        satisfiedCount: u.satisfiedCount,
        failedCount: 0,
        warnings: u.warnings
      };
      const d = l + 1, h = ew(u.document, d);
      if (n.dryRun === !0) return {
        ok: !0,
        summary: `Dry run passed: ${o}/${i} would advance to revision ${d} with ${u.appliedCount} applied op(s). Nothing was saved.`,
        docType: o,
        docId: i,
        title: np(u.document),
        revision: l,
        digest: h,
        changed: !0,
        appliedCount: u.appliedCount,
        satisfiedCount: u.satisfiedCount,
        failedCount: 0,
        changedIds: u.changedIds,
        removedElements: u.removedElements,
        warnings: u.warnings,
        meta: ks(u.document.meta),
        elementCount: u.document.elements.length
      };
      r.managerRunId && await $k({
        managerRunId: r.managerRunId,
        sessionId: s,
        docType: o,
        docId: i
      });
      const p = SH(), y = await lH({
        sessionId: s,
        docType: o,
        docId: i,
        title: np(u.document),
        revision: d,
        data: u.document,
        digest: h,
        status: "active",
        source: r.caller || "auto",
        createdAt: Number(a?.createdAt) || p,
        updatedAt: p
      });
      return await uH({
        sessionId: s,
        docType: o,
        docId: i,
        revision: d,
        status: "active",
        managerRunId: r.managerRunId,
        sourceUserOrder: r.sourceUserOrder,
        sourceAssistantOrder: r.sourceAssistantOrder,
        source: r.caller || "auto",
        summary: zt(n.desc || `StatePatch ${d}`, 400),
        ops: u.effectiveOps,
        changedIds: u.changedIds,
        removedElements: u.removedElements
      }), r.managerRunId && await cH({
        managerRunId: r.managerRunId,
        sessionId: s,
        docType: o,
        docId: i
      }), {
        ok: !0,
        summary: `Updated ${o}/${i} to revision ${y.revision} with ${u.appliedCount} applied op(s).`,
        docType: o,
        docId: i,
        title: y.title,
        revision: y.revision,
        digest: y.digest,
        changed: !0,
        appliedCount: u.appliedCount,
        satisfiedCount: u.satisfiedCount,
        failedCount: 0,
        changedIds: u.changedIds,
        removedElements: u.removedElements,
        warnings: u.warnings,
        meta: ks(u.document.meta),
        elementCount: u.document.elements.length
      };
    })) : {
      ok: !1,
      summary: "StatePatch ops must be a real array.",
      docType: o,
      docId: i,
      error: "state_patch_ops_must_be_array"
    } : {
      ok: !1,
      summary: `${t} is not available.`,
      error: "state_tool_not_available"
    };
  } catch (o) {
    const i = o instanceof Error ? o.name : "", a = o instanceof Error ? o.message : String(o || "state_tool_failed");
    if (i === "AbortError" || a === "manager_source_messages_changed" || a === "manager_aborted") throw o;
    return {
      ok: !1,
      summary: tw(a) || a,
      error: a
    };
  }
}
async function OH(e = "") {
  const t = await Qk(e, Vr, Jo), n = t ? tp(t) : null, r = t ? await Nk({
    sessionId: e,
    docType: Vr,
    docId: Jo,
    limit: 80
  }) : [];
  return !t || !n ? {
    document: null,
    patches: []
  } : {
    document: {
      ...t,
      data: n,
      title: np(n),
      digest: ew(n, t.revision)
    },
    patches: r
  };
}
async function FH(e = "") {
  return (await Pk(e, { includeStale: !1 })).map((t) => ({
    record: t,
    normalized: tp(t)
  })).filter(({ normalized: t }) => t.meta.status === "active" && !Hq(t)).map(({ record: t, normalized: n }) => ({
    docType: t.docType,
    docId: t.docId,
    title: np(n),
    revision: t.revision,
    digest: ew(n, t.revision)
  }));
}
var Dn = {
  LIST: "MemoryList",
  READ: "MemoryRead",
  WRITE: "MemoryWrite",
  EDIT: "MemoryEdit",
  GREP: "MemoryGrep",
  CHAT_HISTORY: "ChatHistory"
}, BH = 1200, vT = 2e3, UH = 100, qH = 100;
function id() {
  return Date.now();
}
function yT(e = "") {
  if (e === "memory/session.md") return "剧情脉络";
  if (e === "memory/state.md") return "状态栏";
  const t = String(e || "").match(/^memory\/turns\/(\d{8})-(\d+)\.md$/);
  if (t) {
    const n = Number(t[2]) || 0;
    return n > 0 ? `第 ${n} 楼小记` : "楼层小记";
  }
  return String(e || "").replace(/^memory\//, "").replace(/\.md$/i, "") || "记忆档案";
}
function eh(e = "", t = 400) {
  const n = String(e || "").replace(/\s+/g, " ").trim();
  return n.length > t ? n.slice(0, t) : n;
}
function rp(e = "", t = 2e4) {
  const n = String(e || "").replace(/\r\n/g, `
`).trim();
  return n.length > t ? n.slice(0, t) : n;
}
function _T(e = "") {
  return String(e ?? "").replace(/\r\n?/g, `
`).split(`
`);
}
function bT(e = [], t = 1) {
  return e.map((n, r) => `${t + r}: ${n}`).join(`
`);
}
function HH(e, t = 1) {
  const n = Math.floor(Number(e));
  return Number.isFinite(n) && n > 0 ? n : t;
}
function wT(e, t = 0) {
  const n = Math.floor(Number(e));
  return Number.isFinite(n) && n >= 0 ? n : t;
}
function ST(e, t, n) {
  const r = Math.floor(Number(e));
  return !Number.isFinite(r) || r <= 0 ? t : Math.min(n, r);
}
function GH(e = "") {
  const t = String(e || "content").trim().replace(/[\s-]/g, "_").replace(/[A-Z]/g, (n) => `_${n.toLowerCase()}`).replace(/_+/g, "_").replace(/^_+|_+$/g, "");
  return t === "files_with_matches" || t === "fileswithmatches" ? "files_with_matches" : t === "count" ? "count" : "content";
}
function Qp(e = "") {
  const t = String(e || "").replace(/\\/g, "/").trim();
  if (!t) throw new Error("memory_path_required");
  if (!t.startsWith("memory/")) throw new Error("memory_path_scope_required");
  if (t.includes("\0") || t.includes("//") || t.split("/").some((n) => n === ".." || n === ".")) throw new Error("memory_path_invalid");
  if (!t.endsWith(".md")) throw new Error("memory_path_must_be_md");
  return t;
}
function WH(e = Date.now()) {
  return new Date(e).toISOString().slice(0, 10).replace(/-/g, "");
}
function VH(e = 0) {
  return String(Math.max(0, Number(e) || 0)).padStart(4, "0");
}
function Zk(e = 0, t = Date.now()) {
  return `memory/turns/${WH(t)}-${VH(e)}.md`;
}
function KH(e = "", t = "") {
  const n = id(), r = eh(t, 120) || "当前角色", s = {
    sessionId: e,
    status: "active",
    createdAt: n,
    updatedAt: n,
    source: "default"
  };
  return [{
    ...s,
    path: "memory/session.md",
    content: [
      "# 剧情脉络",
      "",
      `- 角色：${r}`,
      "- 当前剧情段：未开始",
      "",
      "## 故事为什么走到现在",
      "- 暂无。",
      "",
      "## 当前剧情压力",
      "- 暂无。",
      "",
      "## 未解决事项",
      "- 暂无。"
    ].join(`
`)
  }, {
    ...s,
    path: "memory/state.md",
    content: [
      "# 状态栏",
      "",
      "## 当前事实",
      "- 暂无。",
      "",
      "## 当前关系",
      "- 暂无。",
      "",
      "## 当前地点",
      "- 暂无。",
      "",
      "## 当前物品",
      "- 暂无。"
    ].join(`
`)
  }];
}
async function nw(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) throw new Error("memory_session_required");
  const r = await Rn.where("sessionId").equals(n).toArray();
  if (r.length) return r.sort((o, i) => o.path.localeCompare(i.path));
  const s = KH(n, t.characterName);
  return await Rn.bulkPut(s), await kt.update(n, { updatedAt: id() }), s;
}
async function El(e = "", t = {}) {
  const n = String(e || "").trim();
  if (!n) return [];
  const r = await Rn.where("sessionId").equals(n).sortBy("path");
  return t.includeStale ? r : r.filter((s) => s.status !== "stale");
}
function zH(e) {
  const t = String(e.path || ""), n = ["memory/session.md", "memory/state.md"].includes(t) ? 2400 : 1200;
  return rp(e.content, n);
}
function JH(e) {
  return {
    path: e.path,
    status: e.status,
    createdAt: e.createdAt,
    updatedAt: e.updatedAt,
    source: e.source,
    staleFromOrder: e.staleFromOrder,
    contentLength: String(e.content || "").length,
    preview: zH(e)
  };
}
function YH(e) {
  return {
    ...JH(e),
    title: yT(e.path),
    searchText: rp([
      e.path,
      yT(e.path),
      e.content || ""
    ].filter(Boolean).join(`
`), 2e4)
  };
}
async function Qa(e = "", t = "") {
  const n = String(e || "").trim();
  if (!n) return null;
  const r = Qp(t);
  return await Rn.get([n, r]) || null;
}
async function Py(e = "", t = "", n = "", r = {}) {
  const s = String(e || "").trim();
  if (!s) throw new Error("memory_session_required");
  const o = Qp(t), i = id(), a = await Rn.get([s, o]), l = {
    sessionId: s,
    path: o,
    content: String(n || "").replace(/\r\n/g, `
`),
    status: "active",
    source: String(r.source || a?.source || "manager"),
    staleFromOrder: Number.isFinite(Number(r.staleFromOrder)) ? Number(r.staleFromOrder) : a?.staleFromOrder,
    createdAt: Number(a?.createdAt) || i,
    updatedAt: i
  };
  return await Rn.put(l), await kt.update(s, { updatedAt: i }), await XH(s), l;
}
async function XH(e = "", t = "") {
  const n = String(e || "").trim();
  if (!n) throw new Error("memory_session_required");
  const r = id(), s = await Nr.get([n, "markdown-derived"]), o = {
    sessionId: n,
    kind: "markdown-derived",
    status: "stale",
    error: String(t || ""),
    sourceFingerprint: "",
    derivedAt: r,
    updatedAt: r,
    files: Array.isArray(s?.files) ? s.files : []
  };
  return await Nr.put(o), o;
}
async function jk(e = "", t = "markdown-derived") {
  const n = String(e || "").trim();
  return n && await Nr.get([n, t]) || null;
}
function QH(e) {
  const t = e.map((r) => `${r.path}${r.status}${r.updatedAt}${r.content.length}`).join("");
  let n = 2166136261;
  for (let r = 0; r < t.length; r += 1)
    n ^= t.charCodeAt(r), n = Math.imul(n, 16777619) >>> 0;
  return `${e.length}:${n.toString(16)}`;
}
async function Xo(e = "") {
  const t = String(e || "").trim();
  if (!t) throw new Error("memory_session_required");
  const n = id();
  try {
    const r = await El(t, { includeStale: !0 }), s = {
      sessionId: t,
      kind: "markdown-derived",
      status: "ready",
      error: "",
      sourceFingerprint: QH(r),
      derivedAt: n,
      updatedAt: n,
      files: r.map(YH)
    };
    return await Nr.put(s), s;
  } catch (r) {
    const s = {
      sessionId: t,
      kind: "markdown-derived",
      status: "failed",
      error: r instanceof Error ? r.message : String(r || "memory_index_failed"),
      sourceFingerprint: "",
      derivedAt: n,
      updatedAt: n,
      files: []
    };
    return await Nr.put(s), s;
  }
}
function Yg(e) {
  return Qp(e.filePath || e.path || "");
}
function ZH() {
  return [
    {
      type: "function",
      function: {
        name: Dn.LIST,
        description: [
          "List memory Markdown files in the current session.",
          "Returns paths, status, and timestamps only; it does not read file bodies.",
          "Best for discovering which memory files exist before choosing MemoryRead, MemoryGrep, MemoryWrite, or MemoryEdit.",
          "Scope is fixed to the current session and `memory/...`; it cannot inspect RP chat history, character cards, world books, settings, or plugin source code."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {},
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: Dn.READ,
        description: [
          "Read a current-session memory Markdown file.",
          "Returns raw `content` plus line-numbered `numberedContent`. Large files include continuation hints.",
          "Use `tail` by itself when you need the end of a file. Do not combine `tail` with `offset` or `limit`.",
          "The argument name is `filePath`, not `path`. Memory file paths look like `memory/session.md`, `memory/state.md`, or `memory/turns/20260612-0042.md`.",
          "This reads memory files only. Use ChatHistory for original RP chat messages."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            filePath: {
              type: "string",
              description: "Memory file path, for example `memory/session.md`, `memory/state.md`, or `memory/turns/20260612-0042.md`. Do not pass path."
            },
            offset: {
              type: "number",
              description: "1-based line offset. Default 1."
            },
            limit: {
              type: "number",
              description: "Maximum lines to return. Default 1200, max 2000."
            },
            tail: {
              type: "number",
              description: "Return the final N lines. Use by itself when you need the end of a file; do not combine it with offset or limit."
            }
          },
          required: ["filePath"],
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: Dn.WRITE,
        description: [
          "Write a complete current-session memory Markdown file.",
          "Use for creating or replacing session, state, or turn-note files, or for whole-file rewrites where most content is new.",
          "Read the target file first when it already exists. Write overwrites the entire file, so include all original content you want to keep.",
          "The argument names are `filePath` and `content`. Write replaces the complete target file content.",
          "Use MemoryEdit instead for small corrections inside an existing file."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            filePath: {
              type: "string",
              description: "Target memory file path, for example `memory/turns/0001.md` or `memory/state.md`."
            },
            content: {
              type: "string",
              description: "Complete Markdown file content to save."
            }
          },
          required: ["filePath", "content"],
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: Dn.EDIT,
        description: [
          "Edit one existing current-session memory Markdown file by replacing original text fragments, replacing inclusive line ranges, inserting text at line positions, or removing text with empty replacements.",
          "One call edits one file.",
          "Use oldString/newString for in-sentence, small-paragraph, or multi-spot local revisions. Use startLine/endLine/newString for contiguous section replacement where copying oldString would be fragile. Use insertAtLine/newString to add new text before a line or at the end without replacing existing text. Use MemoryWrite instead for creating files or whole-file rewrites where most content is new.",
          "Read the target file first unless the exact current text is already available in the conversation or a recent tool result. Line-range and insertion edits must use line numbers from the latest MemoryRead result.",
          "Put multiple edits in the edits array. Line-range and insertion items may share one call when they use line numbers from the same MemoryRead result. Keep oldString edits separate from line-number edits unless the whole change can be expressed with line numbers.",
          'The `edits` argument must be a non-empty array value, not a JSON-stringified string. Correct: `"edits":[{"startLine":10,"endLine":50,"newString":"..."}]`. Wrong: `"edits":"[{\\"startLine\\":10,\\"endLine\\":50,\\"newString\\":\\"...\\"}]"`.',
          "Each edit item should choose exactly one mode. Omit unused mode fields when possible. If the provider/tool channel adds stray optional fields, MemoryEdit normalizes by priority: complete startLine/endLine wins, then insertAtLine, then oldString.",
          'Correct line-range item: `{"startLine":10,"endLine":50,"newString":"..."}`. If a polluted item also contains oldString or insertAtLine, the complete startLine/endLine range is used and stray fields are ignored.',
          "Do not issue multiple MemoryEdit tool calls for the same file in one assistant turn. Combine same-file changes into one call, or wait for the first result before editing that file again.",
          "",
          "## Matching Rules",
          "oldString must be an exact fragment present in the file, including spaces and newlines. To remove the matched word, sentence, or fragment, set newString to an empty string.",
          "Common punctuation equivalence is supported, such as straight/curly quotes and ASCII/full-width comma or period. Replacements preserve the file punctuation style when possible.",
          "For long oldString fragments, MemoryEdit can also tolerate whitespace-only differences such as indentation, line wrapping, or extra/missing blank lines.",
          "Each oldString must be unique by default. Multiple matches return line numbers and context.",
          "Line-range edits use 1-based inclusive startLine/endLine from MemoryRead output. A line range replaces the entire inclusive range with any length of newString; use an empty newString to remove the range. Replacement line count does not need to match the original range.",
          "Insertion edits use 1-based insertAtLine from MemoryRead output. insertAtLine inserts before that line; use totalLines + 1 to append to the end of the file.",
          "Line-range and insertion items in one call are applied by original MemoryRead line numbers from bottom to top automatically to avoid line-number shifts; do not adjust later line numbers yourself. If an insertion falls inside a line range being replaced, merge it into that line-range replacement.",
          "Do not intentionally mix oldString edits with line-number edits in one MemoryEdit call. If one item is polluted by stray optional fields, the tool will still choose one mode by priority instead of failing.",
          "",
          "## Failure Handling",
          "Not found: check whether oldString exactly matches the file content.",
          "Multiple matches: expand oldString with more context to make it unique, or set `replaceAll: true` only if every match should change.",
          "",
          "## Notes",
          "oldString edits execute in order. Line-range and insertion edits execute bottom-to-top by original line numbers. Do not let a later oldString match text just inserted by an earlier newString.",
          "If two changes overlap, merge them into one replacement for the larger fragment instead of splitting them into separate edits.",
          "Use MemoryWrite for complete file rewrites, or when most of the file should be replaced."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            filePath: {
              type: "string",
              description: "Canonical path under memory/."
            },
            edits: {
              type: "array",
              description: "List of edits as a real, non-empty JSON array, not a quoted JSON string. Each item should use exactly one mode: oldString/newString, startLine/endLine/newString, or insertAtLine/newString. Stray optional fields are ignored by mode priority when possible.",
              items: {
                type: "object",
                properties: {
                  oldString: {
                    type: "string",
                    description: "Original text fragment to replace. Must match exactly, with common punctuation/long-whitespace tolerance. Do not use an empty oldString; use MemoryWrite for full rewrites."
                  },
                  startLine: {
                    type: "number",
                    description: "1-based inclusive start line from the latest MemoryRead result. Use with endLine instead of oldString."
                  },
                  endLine: {
                    type: "number",
                    description: "1-based inclusive end line from the latest MemoryRead result. Use with startLine instead of oldString."
                  },
                  insertAtLine: {
                    type: "number",
                    description: "1-based insertion point from the latest MemoryRead result. Inserts before this line. Use totalLines + 1 to append to the end of the file."
                  },
                  newString: {
                    type: "string",
                    description: "Replacement, inserted text, or an empty string to delete the matched fragment or line range."
                  },
                  replaceAll: {
                    type: "boolean",
                    description: "Whether to replace all matches. Default false."
                  }
                },
                required: ["newString"],
                additionalProperties: !1
              }
            }
          },
          required: ["filePath", "edits"],
          additionalProperties: !1
        }
      }
    },
    {
      type: "function",
      function: {
        name: Dn.GREP,
        description: [
          "Search text inside current-session memory Markdown files.",
          "Uses literal text search by default and returns matching files plus line-level snippets.",
          "Use before reading many files to locate facts, hooks, character state, unresolved items, or summaries.",
          "`path` can be a search directory or one exact file like `memory/state.md`; `filePath` is an alias for exact-file scope. For regex search, explicitly pass `regex: true` or `useRegex: true`.",
          "Supports path/filePath scope, outputMode, offset/limit pagination, and context lines.",
          "This searches memory files only. Use ChatHistory grep to search original RP chat history."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            pattern: {
              type: "string",
              description: "Search pattern. Treated as literal text by default; use regex/useRegex only when you intentionally need regex."
            },
            path: {
              type: "string",
              description: "Optional search scope. Can be a directory like `memory/turns/` or one file like `memory/state.md`."
            },
            filePath: {
              type: "string",
              description: "Optional exact file scope. Alias for path when targeting one memory file such as memory/state.md."
            },
            outputMode: {
              type: "string",
              enum: [
                "content",
                "files_with_matches",
                "count"
              ],
              description: "`content` returns matched lines, `files_with_matches` returns files only, and `count` returns match counts. Default content."
            },
            limit: {
              type: "number",
              description: "Maximum results to return. Default 100, max 100."
            },
            offset: {
              type: "number",
              description: "Skip this many ascending results before returning matches. Default 0."
            },
            contextLines: {
              type: "number",
              description: "How many context lines to include before and after each match. Default 0, max 5."
            },
            regex: {
              type: "boolean",
              description: "Whether to treat pattern as a regex. Default false; omit it for literal text search."
            },
            useRegex: {
              type: "boolean",
              description: "Alias for regex; kept for JSON compatibility with ebook-style calls."
            }
          },
          required: ["pattern"],
          additionalProperties: !1
        }
      }
    }
  ];
}
function Ry() {
  return [
    ...ZH(),
    ...DH(),
    {
      type: "function",
      function: {
        name: Dn.CHAT_HISTORY,
        description: [
          "Read original RP chat history for the current session.",
          "This is read-only and returns message order, role, and snippet or full content.",
          "Best for checking what actually happened in the RP before correcting memory files.",
          "Use recent for current continuity, range when you know message order, and grep when you only know a keyword.",
          "recent reads the latest messages; offset pages backward from the newest messages.",
          "range reads message order ascending; if startOrder is provided and endOrder is omitted, the range continues through the latest message.",
          "grep searches message content and returns ascending earliest matches; offset/limit continue through later matches.",
          "Results include count/truncated/nextOffset for pagination. Set full:true when exact wording or evidence matters.",
          "This does not search memory Markdown files. Use MemoryGrep for memory files."
        ].join(`
`),
        parameters: {
          type: "object",
          properties: {
            mode: {
              type: "string",
              enum: [
                "recent",
                "range",
                "grep"
              ],
              description: "recent reads the latest messages; range reads messages by order; grep searches message content by keyword."
            },
            limit: {
              type: "number",
              minimum: 1,
              maximum: 100,
              description: "Maximum messages to return. Defaults to 12."
            },
            offset: {
              type: "number",
              minimum: 0,
              description: "Pagination offset. In recent mode it pages backward from the newest messages; in range/grep it skips earlier ascending results."
            },
            startOrder: {
              type: "number",
              minimum: 0,
              description: "First message order for range mode. If endOrder is omitted, the range continues through the latest message."
            },
            endOrder: {
              type: "number",
              minimum: 0,
              description: "Last message order for range mode, inclusive. Omit to read from startOrder through the latest message."
            },
            pattern: {
              type: "string",
              description: "Keyword for grep mode. Plain text by default."
            },
            regex: {
              type: "boolean",
              description: "Set true only when pattern is intended as a regular expression."
            },
            useRegex: {
              type: "boolean",
              description: "Alias for regex; kept for JSON compatibility with ebook-style calls."
            },
            full: {
              type: "boolean",
              description: "Return full message content instead of snippets when exact wording or source evidence matters."
            }
          },
          required: ["mode"],
          additionalProperties: !1
        }
      }
    }
  ];
}
function jH(e) {
  const t = e instanceof Error ? e.message : String(e || "");
  return (e instanceof Error ? e.name : "") === "AbortError" || t === "manager_source_messages_changed" || t === "manager_aborted";
}
function eG(e, t = 0, n = 12) {
  const r = Math.max(0, Number(t) || 0), s = Math.max(1, Math.min(100, Number(n) || 12)), o = Math.max(0, e.length - r), i = Math.max(0, o - s);
  return e.slice(i, o);
}
function Xg(e, t = {}) {
  const n = t.full === !0, r = rp(e.content, 8e3), s = Array.isArray(e.thoughts) ? e.thoughts.map((o, i) => ({
    label: eh(o?.label || `思考 ${i + 1}`, 80),
    text: rp(o?.text || "", 8e3)
  })).filter((o) => o.text) : [];
  return {
    order: e.order,
    role: String(e.role || ""),
    snippet: eh(e.content, 320),
    reasoningSnippet: s.length ? eh(s.map((o) => o.text).join(`
`), 320) : void 0,
    content: n ? r : void 0,
    thoughts: n && s.length ? s : void 0
  };
}
async function tG(e = "", t = "", n = {}, r = {}) {
  const s = String(e || "").trim();
  if (!s) return {
    ok: !1,
    summary: "缺少 sessionId。",
    error: "memory_session_required"
  };
  await nw(s);
  try {
    if (t === Dn.LIST) {
      const o = await El(s, { includeStale: !0 });
      return {
        ok: !0,
        summary: `找到 ${o.length} 个记忆文件。`,
        files: o.map((i) => ({
          path: i.path,
          status: i.status,
          updatedAt: i.updatedAt
        }))
      };
    }
    if (t === Dn.READ) {
      const o = Yg(n), i = await Qa(s, o);
      if (!i) return {
        ok: !1,
        summary: `${o} 不存在。`,
        path: o,
        error: "memory_file_not_found"
      };
      const a = _T(i.content), l = Math.floor(Number(n.tail) || 0);
      let u = HH(n.offset, 1), d = ST(n.limit, BH, vT);
      l > 0 && (d = Math.min(vT, l), u = Math.max(1, a.length - d + 1));
      const h = Math.max(0, u - 1), p = a.slice(h, h + d), y = h + d < a.length ? h + d + 1 : 0;
      return {
        ok: !0,
        summary: `读取 ${o} 第 ${h + 1}-${h + p.length} 行，共 ${a.length} 行。`,
        path: o,
        content: p.join(`
`),
        numberedContent: bT(p, h + 1),
        lineStart: h + 1,
        lineEnd: h + p.length,
        totalLines: a.length,
        truncated: y > 0,
        nextOffset: y
      };
    }
    if (t === Dn.WRITE) {
      const o = Yg(n);
      await r.beforeWriteGuard?.(), r.managerRunId && await Ty({
        managerRunId: r.managerRunId,
        sessionId: s,
        path: o
      });
      const i = await Py(s, o, String(n.content || ""), { source: "manager" });
      r.managerRunId && await pT({
        managerRunId: r.managerRunId,
        sessionId: s,
        path: i.path
      });
      const a = await Qa(s, i.path);
      return !a || a.content !== i.content ? {
        ok: !1,
        summary: `已生成 ${i.path} 的写入结果，但保存校验未通过；请重新读取文件确认当前内容。`,
        path: i.path,
        changed: !1,
        error: "memory_write_verification_failed"
      } : {
        ok: !0,
        summary: `已写入 ${i.path}。`,
        path: i.path,
        changed: !0
      };
    }
    if (t === Dn.EDIT) {
      const o = Yg(n), i = await Qa(s, o);
      if (!i) return {
        ok: !1,
        summary: `${o} 不存在。`,
        path: o,
        error: "memory_file_not_found"
      };
      const a = ZB(i.content, n.edits), l = Array.isArray(a.results) ? a.results : [], u = l.filter((w) => w.ok && !w.satisfied).length, d = l.filter((w) => w.ok && w.satisfied).length, h = u + d, p = Math.max(0, l.length - h), y = a.content !== i.content;
      if (y) {
        await r.beforeWriteGuard?.(), r.managerRunId && await Ty({
          managerRunId: r.managerRunId,
          sessionId: s,
          path: o
        }), await Py(s, o, a.content, { source: "manager" }), r.managerRunId && await pT({
          managerRunId: r.managerRunId,
          sessionId: s,
          path: o
        });
        const w = await Qa(s, o);
        if (!w || w.content !== a.content) return {
          ok: !1,
          partial: !0,
          summary: `已生成 ${o} 的修改结果，但保存校验未通过；请重新读取文件确认当前内容。`,
          path: o,
          changed: !1,
          appliedCount: u,
          satisfiedCount: d || void 0,
          successCount: h,
          failedCount: p,
          warning: a.warning,
          error: "memory_edit_persistence_verification_failed",
          details: l
        };
      }
      return {
        ok: !!a.ok,
        partial: !!a.partial,
        summary: a.ok ? y ? d > 0 ? `已修改 ${o}，应用 ${u} 项，另有 ${d} 项已是目标状态。` : `已修改 ${o}，应用 ${u} 项。` : `已确认 ${o} 的 ${d || u} 项修改已是目标状态，无需重复写入。` : y ? `已部分修改 ${o}：成功 ${h} 项，失败 ${p} 项。` : `未修改 ${o}：${l.find((w) => !w.ok)?.message || l.find((w) => !w.ok)?.error || "Edit failed"}。`,
        path: o,
        changed: y,
        appliedCount: u,
        satisfiedCount: d || void 0,
        successCount: h,
        failedCount: p,
        warning: a.warning,
        error: a.ok ? "" : "memory_edit_failed",
        details: l
      };
    }
    if (t === Dn.GREP) {
      const o = String(n.pattern || "").trim();
      if (!o) return {
        ok: !1,
        summary: "缺少搜索词。",
        error: "memory_grep_pattern_required"
      };
      const i = n.regex === !0 || n.useRegex === !0 ? new RegExp(o, "iu") : null, a = o.toLowerCase(), l = GH(n.outputMode), u = ST(n.limit, UH, qH), d = wT(n.offset, 0), h = Math.min(5, wT(n.contextLines, 0)), p = String(n.filePath || n.path || "").trim(), y = p && p.endsWith(".md") ? Qp(p) : "", w = p && !y ? String(p || "").replace(/\\/g, "/").trim() : "";
      if (w && (!w.startsWith("memory/") || w.includes(".."))) throw new Error("memory_path_scope_required");
      const E = [];
      (await El(s, { includeStale: !0 })).filter((M) => y ? M.path === y : w ? M.path.startsWith(w.endsWith("/") ? w : `${w}/`) : !0).forEach((M) => {
        const k = _T(M.content);
        let R = 0;
        k.forEach((D, m) => {
          if (i && (i.lastIndex = 0), (i ? i.test(D) : D.toLowerCase().includes(a)) && (R += 1, l === "content")) {
            const b = Math.max(0, m - h), v = Math.min(k.length, m + h + 1);
            E.push({
              path: M.path,
              line: m + 1,
              text: D.trim(),
              context: h > 0 ? bT(k.slice(b, v), b + 1) : void 0
            });
          }
        }), l === "files_with_matches" && R > 0 ? E.push({ path: M.path }) : l === "count" && R > 0 && E.push({
          path: M.path,
          count: R
        });
      });
      const C = E.slice(d, d + u);
      return {
        ok: !0,
        summary: `搜索到 ${E.length} 项，返回 ${C.length} 项。`,
        count: E.length,
        truncated: d + u < E.length,
        nextOffset: d + u < E.length ? d + u : 0,
        matches: C
      };
    }
    if (t === Dn.CHAT_HISTORY) {
      const o = String(n.mode || "").trim(), i = Math.max(1, Math.min(100, Number(n.limit) || 12)), a = Math.max(0, Number(n.offset) || 0), l = n.full === !0, u = await Hr(s);
      if (o === "recent") {
        const d = eG(u, a, i).map((p) => Xg(p, { full: l })), h = a + i < u.length;
        return {
          ok: !0,
          summary: `共有 ${u.length} 条原文，读取最近窗口 ${d.length} 条。`,
          count: u.length,
          truncated: h,
          nextOffset: h ? a + i : 0,
          messages: d
        };
      }
      if (o === "range") {
        const d = Math.max(0, Number(n.startOrder) || 0), h = Object.prototype.hasOwnProperty.call(n, "endOrder") && Number.isFinite(Number(n.endOrder)) ? Math.max(d, Number(n.endOrder) || d) : Math.max(d, Number(u[u.length - 1]?.order) || d), p = u.filter((E) => E.order >= d && E.order <= h), y = p.slice(a, a + i).map((E) => Xg(E, { full: l })), w = a + i < p.length;
        return {
          ok: !0,
          summary: `order ${d}-${h} 共 ${p.length} 条原文，返回 ${y.length} 条。`,
          count: p.length,
          truncated: w,
          nextOffset: w ? a + i : 0,
          messages: y
        };
      }
      if (o === "grep") {
        const d = String(n.pattern || "").trim();
        if (!d) return {
          ok: !1,
          summary: "缺少搜索词。",
          error: "chat_history_pattern_required"
        };
        const h = n.regex === !0 || n.useRegex === !0 ? new RegExp(d, "iu") : null, p = d.toLowerCase(), y = u.filter((C) => (h && (h.lastIndex = 0), h ? h.test(C.content) : C.content.toLowerCase().includes(p))), w = y.slice(a, a + i).map((C) => Xg(C, { full: l })), E = a + i < y.length;
        return {
          ok: !0,
          summary: `搜索到 ${y.length} 条原文，返回 ${w.length} 条。`,
          count: y.length,
          truncated: E,
          nextOffset: E ? a + i : 0,
          messages: w
        };
      }
      return {
        ok: !1,
        summary: `不支持的 ChatHistory 模式：${o || "empty"}`,
        error: "chat_history_mode_invalid"
      };
    }
    return {
      ok: !1,
      summary: `${t} 不可用。`,
      error: "memory_tool_not_available"
    };
  } catch (o) {
    if (jH(o)) throw o;
    return {
      ok: !1,
      summary: o instanceof Error ? o.message : String(o || "memory_tool_failed"),
      error: o instanceof Error ? o.message : String(o || "memory_tool_failed")
    };
  }
}
var nG = [
  "、",
  "。",
  "〈",
  "〉",
  "《",
  "》",
  "一",
  "一个",
  "一些",
  "一何",
  "一切",
  "一则",
  "一方面",
  "一旦",
  "一来",
  "一样",
  "一种",
  "一般",
  "一转眼",
  "七",
  "万一",
  "三",
  "上",
  "上下",
  "下",
  "不",
  "不仅",
  "不但",
  "不光",
  "不单",
  "不只",
  "不外乎",
  "不如",
  "不妨",
  "不尽",
  "不尽然",
  "不得",
  "不怕",
  "不惟",
  "不成",
  "不拘",
  "不料",
  "不是",
  "不比",
  "不然",
  "不特",
  "不独",
  "不管",
  "不至于",
  "不若",
  "不论",
  "不过",
  "不问",
  "与",
  "与其",
  "与其说",
  "与否",
  "与此同时",
  "且",
  "且不说",
  "且说",
  "两者",
  "个",
  "个别",
  "中",
  "临",
  "为",
  "为了",
  "为什么",
  "为何",
  "为止",
  "为此",
  "为着",
  "乃",
  "乃至",
  "乃至于",
  "么",
  "之",
  "之一",
  "之所以",
  "之类",
  "乌乎",
  "乎",
  "乘",
  "九",
  "也",
  "也好",
  "也罢",
  "了",
  "二",
  "二来",
  "于",
  "于是",
  "于是乎",
  "云云",
  "云尔",
  "五",
  "些",
  "亦",
  "人",
  "人们",
  "人家",
  "什",
  "什么",
  "什么样",
  "今",
  "介于",
  "仍",
  "仍旧",
  "从",
  "从此",
  "从而",
  "他",
  "他人",
  "他们",
  "他们们",
  "以",
  "以上",
  "以为",
  "以便",
  "以免",
  "以及",
  "以故",
  "以期",
  "以来",
  "以至",
  "以至于",
  "以致",
  "们",
  "任",
  "任何",
  "任凭",
  "会",
  "似的",
  "但",
  "但凡",
  "但是",
  "何",
  "何以",
  "何况",
  "何处",
  "何时",
  "余外",
  "作为",
  "你",
  "你们",
  "使",
  "使得",
  "例如",
  "依",
  "依据",
  "依照",
  "便于",
  "俺",
  "俺们",
  "倘",
  "倘使",
  "倘或",
  "倘然",
  "倘若",
  "借",
  "借傥然",
  "假使",
  "假如",
  "假若",
  "做",
  "像",
  "儿",
  "先不先",
  "光",
  "光是",
  "全体",
  "全部",
  "八",
  "六",
  "兮",
  "共",
  "关于",
  "关于具体地说",
  "其",
  "其一",
  "其中",
  "其二",
  "其他",
  "其余",
  "其它",
  "其次",
  "具体地说",
  "具体说来",
  "兼之",
  "内",
  "再",
  "再其次",
  "再则",
  "再有",
  "再者",
  "再者说",
  "再说",
  "冒",
  "冲",
  "况且",
  "几",
  "几时",
  "凡",
  "凡是",
  "凭",
  "凭借",
  "出于",
  "出来",
  "分",
  "分别",
  "则",
  "则甚",
  "别",
  "别人",
  "别处",
  "别是",
  "别的",
  "别管",
  "别说",
  "到",
  "前后",
  "前此",
  "前者",
  "加之",
  "加以",
  "区",
  "即",
  "即令",
  "即使",
  "即便",
  "即如",
  "即或",
  "即若",
  "却",
  "去",
  "又",
  "又及",
  "及",
  "及其",
  "及至",
  "反之",
  "反而",
  "反过来",
  "反过来说",
  "受到",
  "另",
  "另一方面",
  "另外",
  "另悉",
  "只",
  "只当",
  "只怕",
  "只是",
  "只有",
  "只消",
  "只要",
  "只限",
  "叫",
  "叮咚",
  "可",
  "可以",
  "可是",
  "可见",
  "各",
  "各个",
  "各位",
  "各种",
  "各自",
  "同",
  "同时",
  "后",
  "后者",
  "向",
  "向使",
  "向着",
  "吓",
  "吗",
  "否则",
  "吧",
  "吧哒",
  "含",
  "吱",
  "呀",
  "呃",
  "呕",
  "呗",
  "呜",
  "呜呼",
  "呢",
  "呵",
  "呵呵",
  "呸",
  "呼哧",
  "咋",
  "和",
  "咚",
  "咦",
  "咧",
  "咱",
  "咱们",
  "咳",
  "哇",
  "哈",
  "哈哈",
  "哉",
  "哎",
  "哎呀",
  "哎哟",
  "哗",
  "哟",
  "哦",
  "哩",
  "哪",
  "哪个",
  "哪些",
  "哪儿",
  "哪天",
  "哪年",
  "哪怕",
  "哪样",
  "哪边",
  "哪里",
  "哼",
  "哼唷",
  "唉",
  "唯有",
  "啊",
  "啐",
  "啥",
  "啦",
  "啪达",
  "啷当",
  "喂",
  "喏",
  "喔唷",
  "喽",
  "嗡",
  "嗡嗡",
  "嗬",
  "嗯",
  "嗳",
  "嘎",
  "嘎登",
  "嘘",
  "嘛",
  "嘻",
  "嘿",
  "嘿嘿",
  "四",
  "因",
  "因为",
  "因了",
  "因此",
  "因着",
  "因而",
  "固然",
  "在",
  "在下",
  "在于",
  "地",
  "基于",
  "处在",
  "多",
  "多么",
  "多少",
  "大",
  "大家",
  "她",
  "她们",
  "好",
  "如",
  "如上",
  "如上所述",
  "如下",
  "如何",
  "如其",
  "如同",
  "如是",
  "如果",
  "如此",
  "如若",
  "始而",
  "孰料",
  "孰知",
  "宁",
  "宁可",
  "宁愿",
  "宁肯",
  "它",
  "它们",
  "对",
  "对于",
  "对待",
  "对方",
  "对比",
  "将",
  "小",
  "尔",
  "尔后",
  "尔尔",
  "尚且",
  "就",
  "就是",
  "就是了",
  "就是说",
  "就算",
  "就要",
  "尽",
  "尽管",
  "尽管如此",
  "岂但",
  "己",
  "已",
  "已矣",
  "巴",
  "巴巴",
  "年",
  "并",
  "并且",
  "庶乎",
  "庶几",
  "开外",
  "开始",
  "归",
  "归齐",
  "当",
  "当地",
  "当然",
  "当着",
  "彼",
  "彼时",
  "彼此",
  "往",
  "待",
  "很",
  "得",
  "得了",
  "怎",
  "怎么",
  "怎么办",
  "怎么样",
  "怎奈",
  "怎样",
  "总之",
  "总的来看",
  "总的来说",
  "总的说来",
  "总而言之",
  "恰恰相反",
  "您",
  "惟其",
  "慢说",
  "我",
  "我们",
  "或",
  "或则",
  "或是",
  "或曰",
  "或者",
  "截至",
  "所",
  "所以",
  "所在",
  "所幸",
  "所有",
  "才",
  "才能",
  "打",
  "打从",
  "把",
  "抑或",
  "拿",
  "按",
  "按照",
  "换句话说",
  "换言之",
  "据",
  "据此",
  "接着",
  "故",
  "故此",
  "故而",
  "旁人",
  "无",
  "无宁",
  "无论",
  "既",
  "既往",
  "既是",
  "既然",
  "日",
  "时",
  "时候",
  "是",
  "是以",
  "是的",
  "更",
  "曾",
  "替",
  "替代",
  "最",
  "月",
  "有",
  "有些",
  "有关",
  "有及",
  "有时",
  "有的",
  "望",
  "朝",
  "朝着",
  "本",
  "本人",
  "本地",
  "本着",
  "本身",
  "来",
  "来着",
  "来自",
  "来说",
  "极了",
  "果然",
  "果真",
  "某",
  "某个",
  "某些",
  "某某",
  "根据",
  "欤",
  "正值",
  "正如",
  "正巧",
  "正是",
  "此",
  "此地",
  "此处",
  "此外",
  "此时",
  "此次",
  "此间",
  "毋宁",
  "每",
  "每当",
  "比",
  "比及",
  "比如",
  "比方",
  "没奈何",
  "沿",
  "沿着",
  "漫说",
  "点",
  "焉",
  "然则",
  "然后",
  "然而",
  "照",
  "照着",
  "犹且",
  "犹自",
  "甚且",
  "甚么",
  "甚或",
  "甚而",
  "甚至",
  "甚至于",
  "用",
  "用来",
  "由",
  "由于",
  "由是",
  "由此",
  "由此可见",
  "的",
  "的确",
  "的话",
  "直到",
  "相对而言",
  "省得",
  "看",
  "眨眼",
  "着",
  "着呢",
  "矣",
  "矣乎",
  "矣哉",
  "离",
  "秒",
  "称",
  "竟而",
  "第",
  "等",
  "等到",
  "等等",
  "简言之",
  "管",
  "类如",
  "紧接着",
  "纵",
  "纵令",
  "纵使",
  "纵然",
  "经",
  "经过",
  "结果",
  "给",
  "继之",
  "继后",
  "继而",
  "综上所述",
  "罢了",
  "者",
  "而",
  "而且",
  "而况",
  "而后",
  "而外",
  "而已",
  "而是",
  "而言",
  "能",
  "能否",
  "腾",
  "自",
  "自个儿",
  "自从",
  "自各儿",
  "自后",
  "自家",
  "自己",
  "自打",
  "自身",
  "至",
  "至于",
  "至今",
  "至若",
  "致",
  "般的",
  "若",
  "若夫",
  "若是",
  "若果",
  "若非",
  "莫不然",
  "莫如",
  "莫若",
  "虽",
  "虽则",
  "虽然",
  "虽说",
  "被",
  "要",
  "要不",
  "要不是",
  "要不然",
  "要么",
  "要是",
  "譬喻",
  "譬如",
  "让",
  "许多",
  "论",
  "设使",
  "设或",
  "设若",
  "诚如",
  "诚然",
  "该",
  "说",
  "说来",
  "请",
  "诸",
  "诸位",
  "诸如",
  "谁",
  "谁人",
  "谁料",
  "谁知",
  "贼死",
  "赖以",
  "赶",
  "起",
  "起见",
  "趁",
  "趁着",
  "越是",
  "距",
  "跟",
  "较",
  "较之",
  "边",
  "过",
  "还",
  "还是",
  "还有",
  "还要",
  "这",
  "这一来",
  "这个",
  "这么",
  "这么些",
  "这么样",
  "这么点儿",
  "这些",
  "这会儿",
  "这儿",
  "这就是说",
  "这时",
  "这样",
  "这次",
  "这般",
  "这边",
  "这里",
  "进而",
  "连",
  "连同",
  "逐步",
  "通过",
  "遵循",
  "遵照",
  "那",
  "那个",
  "那么",
  "那么些",
  "那么样",
  "那些",
  "那会儿",
  "那儿",
  "那时",
  "那样",
  "那般",
  "那边",
  "那里",
  "都",
  "鄙人",
  "鉴于",
  "针对",
  "阿",
  "除",
  "除了",
  "除外",
  "除开",
  "除此之外",
  "除非",
  "随",
  "随后",
  "随时",
  "随着",
  "难道说",
  "零",
  "非",
  "非但",
  "非徒",
  "非特",
  "非独",
  "靠",
  "顺",
  "顺着",
  "首先",
  "︿",
  "！",
  "＃",
  "＄",
  "％",
  "＆",
  "（",
  "）",
  "＊",
  "＋",
  "，",
  "０",
  "１",
  "２",
  "３",
  "４",
  "５",
  "６",
  "７",
  "８",
  "９",
  "：",
  "；",
  "＜",
  "＞",
  "？",
  "＠",
  "［",
  "］",
  "｛",
  "｜",
  "｝",
  "～",
  "￥",
  "あそこ",
  "あっ",
  "あの",
  "あのかた",
  "あの人",
  "あり",
  "あります",
  "ある",
  "あれ",
  "い",
  "いう",
  "います",
  "いる",
  "う",
  "うち",
  "え",
  "お",
  "および",
  "おり",
  "おります",
  "か",
  "かつて",
  "から",
  "が",
  "き",
  "ここ",
  "こちら",
  "こと",
  "この",
  "これ",
  "これら",
  "さ",
  "さらに",
  "し",
  "しかし",
  "する",
  "ず",
  "せ",
  "せる",
  "そこ",
  "そして",
  "その",
  "その他",
  "その後",
  "それ",
  "それぞれ",
  "それで",
  "た",
  "ただし",
  "たち",
  "ため",
  "たり",
  "だ",
  "だっ",
  "だれ",
  "つ",
  "て",
  "で",
  "でき",
  "できる",
  "です",
  "では",
  "でも",
  "と",
  "という",
  "といった",
  "とき",
  "ところ",
  "として",
  "とともに",
  "とも",
  "と共に",
  "どこ",
  "どの",
  "な",
  "ない",
  "なお",
  "なかっ",
  "ながら",
  "なく",
  "なっ",
  "など",
  "なに",
  "なら",
  "なり",
  "なる",
  "なん",
  "に",
  "において",
  "における",
  "について",
  "にて",
  "によって",
  "により",
  "による",
  "に対して",
  "に対する",
  "に関する",
  "の",
  "ので",
  "のみ",
  "は",
  "ば",
  "へ",
  "ほか",
  "ほとんど",
  "ほど",
  "ます",
  "また",
  "または",
  "まで",
  "も",
  "もの",
  "ものの",
  "や",
  "よう",
  "より",
  "ら",
  "られ",
  "られる",
  "れ",
  "れる",
  "を",
  "ん",
  "及び",
  "彼女",
  "我々",
  "特に",
  "私",
  "私達",
  "貴方",
  "貴方方",
  "'ll",
  "'tis",
  "'twas",
  "'ve",
  "10",
  "39",
  "a",
  "a's",
  "able",
  "ableabout",
  "about",
  "above",
  "abroad",
  "abst",
  "accordance",
  "according",
  "accordingly",
  "across",
  "act",
  "actually",
  "ad",
  "added",
  "adj",
  "adopted",
  "ae",
  "af",
  "affected",
  "affecting",
  "affects",
  "after",
  "afterwards",
  "ag",
  "again",
  "against",
  "ago",
  "ah",
  "ahead",
  "ai",
  "ain't",
  "aint",
  "al",
  "all",
  "allow",
  "allows",
  "almost",
  "alone",
  "along",
  "alongside",
  "already",
  "also",
  "although",
  "always",
  "am",
  "amid",
  "amidst",
  "among",
  "amongst",
  "amoungst",
  "amount",
  "an",
  "and",
  "announce",
  "another",
  "any",
  "anybody",
  "anyhow",
  "anymore",
  "anyone",
  "anything",
  "anyway",
  "anyways",
  "anywhere",
  "ao",
  "apart",
  "apparently",
  "appear",
  "appreciate",
  "appropriate",
  "approximately",
  "aq",
  "ar",
  "are",
  "area",
  "areas",
  "aren",
  "aren't",
  "arent",
  "arise",
  "around",
  "arpa",
  "as",
  "aside",
  "ask",
  "asked",
  "asking",
  "asks",
  "associated",
  "at",
  "au",
  "auth",
  "available",
  "aw",
  "away",
  "awfully",
  "az",
  "b",
  "ba",
  "back",
  "backed",
  "backing",
  "backs",
  "backward",
  "backwards",
  "bb",
  "bd",
  "be",
  "became",
  "because",
  "become",
  "becomes",
  "becoming",
  "been",
  "before",
  "beforehand",
  "began",
  "begin",
  "beginning",
  "beginnings",
  "begins",
  "behind",
  "being",
  "beings",
  "believe",
  "below",
  "beside",
  "besides",
  "best",
  "better",
  "between",
  "beyond",
  "bf",
  "bg",
  "bh",
  "bi",
  "big",
  "bill",
  "billion",
  "biol",
  "bj",
  "bm",
  "bn",
  "bo",
  "both",
  "bottom",
  "br",
  "brief",
  "briefly",
  "bs",
  "bt",
  "but",
  "buy",
  "bv",
  "bw",
  "by",
  "bz",
  "c",
  "c'mon",
  "c's",
  "ca",
  "call",
  "came",
  "can",
  "can't",
  "cannot",
  "cant",
  "caption",
  "case",
  "cases",
  "cause",
  "causes",
  "cc",
  "cd",
  "certain",
  "certainly",
  "cf",
  "cg",
  "ch",
  "changes",
  "ci",
  "ck",
  "cl",
  "clear",
  "clearly",
  "click",
  "cm",
  "cmon",
  "cn",
  "co",
  "co.",
  "com",
  "come",
  "comes",
  "computer",
  "con",
  "concerning",
  "consequently",
  "consider",
  "considering",
  "contain",
  "containing",
  "contains",
  "copy",
  "corresponding",
  "could",
  "could've",
  "couldn",
  "couldn't",
  "couldnt",
  "course",
  "cr",
  "cry",
  "cs",
  "cu",
  "currently",
  "cv",
  "cx",
  "cy",
  "cz",
  "d",
  "dare",
  "daren't",
  "darent",
  "date",
  "de",
  "dear",
  "definitely",
  "describe",
  "described",
  "despite",
  "detail",
  "did",
  "didn",
  "didn't",
  "didnt",
  "differ",
  "different",
  "differently",
  "directly",
  "dj",
  "dk",
  "dm",
  "do",
  "does",
  "doesn",
  "doesn't",
  "doesnt",
  "doing",
  "don",
  "don't",
  "done",
  "dont",
  "doubtful",
  "down",
  "downed",
  "downing",
  "downs",
  "downwards",
  "due",
  "during",
  "dz",
  "e",
  "each",
  "early",
  "ec",
  "ed",
  "edu",
  "ee",
  "effect",
  "eg",
  "eh",
  "eight",
  "eighty",
  "either",
  "eleven",
  "else",
  "elsewhere",
  "empty",
  "end",
  "ended",
  "ending",
  "ends",
  "enough",
  "entirely",
  "er",
  "es",
  "especially",
  "et",
  "et-al",
  "etc",
  "even",
  "evenly",
  "ever",
  "evermore",
  "every",
  "everybody",
  "everyone",
  "everything",
  "everywhere",
  "ex",
  "exactly",
  "example",
  "except",
  "f",
  "face",
  "faces",
  "fact",
  "facts",
  "fairly",
  "far",
  "farther",
  "felt",
  "few",
  "fewer",
  "ff",
  "fi",
  "fifteen",
  "fifth",
  "fifty",
  "fify",
  "fill",
  "find",
  "finds",
  "fire",
  "first",
  "five",
  "fix",
  "fj",
  "fk",
  "fm",
  "fo",
  "followed",
  "following",
  "follows",
  "for",
  "forever",
  "former",
  "formerly",
  "forth",
  "forty",
  "forward",
  "found",
  "four",
  "fr",
  "free",
  "from",
  "front",
  "full",
  "fully",
  "further",
  "furthered",
  "furthering",
  "furthermore",
  "furthers",
  "fx",
  "g",
  "ga",
  "gave",
  "gb",
  "gd",
  "ge",
  "general",
  "generally",
  "get",
  "gets",
  "getting",
  "gf",
  "gg",
  "gh",
  "gi",
  "give",
  "given",
  "gives",
  "giving",
  "gl",
  "gm",
  "gmt",
  "gn",
  "go",
  "goes",
  "going",
  "gone",
  "good",
  "goods",
  "got",
  "gotten",
  "gov",
  "gp",
  "gq",
  "gr",
  "great",
  "greater",
  "greatest",
  "greetings",
  "group",
  "grouped",
  "grouping",
  "groups",
  "gs",
  "gt",
  "gu",
  "gw",
  "gy",
  "h",
  "had",
  "hadn't",
  "hadnt",
  "half",
  "happens",
  "hardly",
  "has",
  "hasn",
  "hasn't",
  "hasnt",
  "have",
  "haven",
  "haven't",
  "havent",
  "having",
  "he",
  "he'd",
  "he'll",
  "he's",
  "hed",
  "hell",
  "hello",
  "help",
  "hence",
  "her",
  "here",
  "here's",
  "hereafter",
  "hereby",
  "herein",
  "heres",
  "hereupon",
  "hers",
  "herself",
  "herse”",
  "hes",
  "hi",
  "hid",
  "high",
  "higher",
  "highest",
  "him",
  "himself",
  "himse”",
  "his",
  "hither",
  "hk",
  "hm",
  "hn",
  "home",
  "homepage",
  "hopefully",
  "how",
  "how'd",
  "how'll",
  "how's",
  "howbeit",
  "however",
  "hr",
  "ht",
  "htm",
  "html",
  "http",
  "hu",
  "hundred",
  "i",
  "i'd",
  "i'll",
  "i'm",
  "i've",
  "i.e.",
  "id",
  "ie",
  "if",
  "ignored",
  "ii",
  "il",
  "ill",
  "im",
  "immediate",
  "immediately",
  "importance",
  "important",
  "in",
  "inasmuch",
  "inc",
  "inc.",
  "indeed",
  "index",
  "indicate",
  "indicated",
  "indicates",
  "information",
  "inner",
  "inside",
  "insofar",
  "instead",
  "int",
  "interest",
  "interested",
  "interesting",
  "interests",
  "into",
  "invention",
  "inward",
  "io",
  "iq",
  "ir",
  "is",
  "isn",
  "isn't",
  "isnt",
  "it",
  "it'd",
  "it'll",
  "it's",
  "itd",
  "itll",
  "its",
  "itself",
  "itse”",
  "ive",
  "j",
  "je",
  "jm",
  "jo",
  "join",
  "jp",
  "just",
  "k",
  "ke",
  "keep",
  "keeps",
  "kept",
  "keys",
  "kg",
  "kh",
  "ki",
  "kind",
  "km",
  "kn",
  "knew",
  "know",
  "known",
  "knows",
  "kp",
  "kr",
  "kw",
  "ky",
  "kz",
  "l",
  "la",
  "large",
  "largely",
  "last",
  "lately",
  "later",
  "latest",
  "latter",
  "latterly",
  "lb",
  "lc",
  "least",
  "length",
  "less",
  "lest",
  "let",
  "let's",
  "lets",
  "li",
  "like",
  "liked",
  "likely",
  "likewise",
  "line",
  "little",
  "lk",
  "ll",
  "long",
  "longer",
  "longest",
  "look",
  "looking",
  "looks",
  "low",
  "lower",
  "lr",
  "ls",
  "lt",
  "ltd",
  "lu",
  "lv",
  "ly",
  "m",
  "ma",
  "made",
  "mainly",
  "make",
  "makes",
  "making",
  "man",
  "many",
  "may",
  "maybe",
  "mayn't",
  "maynt",
  "mc",
  "md",
  "me",
  "mean",
  "means",
  "meantime",
  "meanwhile",
  "member",
  "members",
  "men",
  "merely",
  "mg",
  "mh",
  "microsoft",
  "might",
  "might've",
  "mightn't",
  "mightnt",
  "mil",
  "mill",
  "million",
  "mine",
  "minus",
  "miss",
  "mk",
  "ml",
  "mm",
  "mn",
  "mo",
  "more",
  "moreover",
  "most",
  "mostly",
  "move",
  "mp",
  "mq",
  "mr",
  "mrs",
  "ms",
  "msie",
  "mt",
  "mu",
  "much",
  "mug",
  "must",
  "must've",
  "mustn't",
  "mustnt",
  "mv",
  "mw",
  "mx",
  "my",
  "myself",
  "myse”",
  "mz",
  "n",
  "na",
  "name",
  "namely",
  "nay",
  "nc",
  "nd",
  "ne",
  "near",
  "nearly",
  "necessarily",
  "necessary",
  "need",
  "needed",
  "needing",
  "needn't",
  "neednt",
  "needs",
  "neither",
  "net",
  "netscape",
  "never",
  "neverf",
  "neverless",
  "nevertheless",
  "new",
  "newer",
  "newest",
  "next",
  "nf",
  "ng",
  "ni",
  "nine",
  "ninety",
  "nl",
  "no",
  "no-one",
  "nobody",
  "non",
  "none",
  "nonetheless",
  "noone",
  "nor",
  "normally",
  "nos",
  "not",
  "noted",
  "nothing",
  "notwithstanding",
  "novel",
  "now",
  "nowhere",
  "np",
  "nr",
  "nu",
  "null",
  "number",
  "numbers",
  "nz",
  "o",
  "obtain",
  "obtained",
  "obviously",
  "of",
  "off",
  "often",
  "oh",
  "ok",
  "okay",
  "old",
  "older",
  "oldest",
  "om",
  "omitted",
  "on",
  "once",
  "one",
  "one's",
  "ones",
  "only",
  "onto",
  "open",
  "opened",
  "opening",
  "opens",
  "opposite",
  "or",
  "ord",
  "order",
  "ordered",
  "ordering",
  "orders",
  "org",
  "other",
  "others",
  "otherwise",
  "ought",
  "oughtn't",
  "oughtnt",
  "our",
  "ours",
  "ourselves",
  "out",
  "outside",
  "over",
  "overall",
  "owing",
  "own",
  "p",
  "pa",
  "page",
  "pages",
  "part",
  "parted",
  "particular",
  "particularly",
  "parting",
  "parts",
  "past",
  "pe",
  "per",
  "perhaps",
  "pf",
  "pg",
  "ph",
  "pk",
  "pl",
  "place",
  "placed",
  "places",
  "please",
  "plus",
  "pm",
  "pmid",
  "pn",
  "point",
  "pointed",
  "pointing",
  "points",
  "poorly",
  "possible",
  "possibly",
  "potentially",
  "pp",
  "pr",
  "predominantly",
  "present",
  "presented",
  "presenting",
  "presents",
  "presumably",
  "previously",
  "primarily",
  "probably",
  "problem",
  "problems",
  "promptly",
  "proud",
  "provided",
  "provides",
  "pt",
  "put",
  "puts",
  "pw",
  "py",
  "q",
  "qa",
  "que",
  "quickly",
  "quite",
  "qv",
  "r",
  "ran",
  "rather",
  "rd",
  "re",
  "readily",
  "really",
  "reasonably",
  "recent",
  "recently",
  "ref",
  "refs",
  "regarding",
  "regardless",
  "regards",
  "related",
  "relatively",
  "research",
  "reserved",
  "respectively",
  "resulted",
  "resulting",
  "results",
  "right",
  "ring",
  "ro",
  "room",
  "rooms",
  "round",
  "ru",
  "run",
  "rw",
  "s",
  "sa",
  "said",
  "same",
  "saw",
  "say",
  "saying",
  "says",
  "sb",
  "sc",
  "sd",
  "se",
  "sec",
  "second",
  "secondly",
  "seconds",
  "section",
  "see",
  "seeing",
  "seem",
  "seemed",
  "seeming",
  "seems",
  "seen",
  "sees",
  "self",
  "selves",
  "sensible",
  "sent",
  "serious",
  "seriously",
  "seven",
  "seventy",
  "several",
  "sg",
  "sh",
  "shall",
  "shan't",
  "shant",
  "she",
  "she'd",
  "she'll",
  "she's",
  "shed",
  "shell",
  "shes",
  "should",
  "should've",
  "shouldn",
  "shouldn't",
  "shouldnt",
  "show",
  "showed",
  "showing",
  "shown",
  "showns",
  "shows",
  "si",
  "side",
  "sides",
  "significant",
  "significantly",
  "similar",
  "similarly",
  "since",
  "sincere",
  "site",
  "six",
  "sixty",
  "sj",
  "sk",
  "sl",
  "slightly",
  "sm",
  "small",
  "smaller",
  "smallest",
  "sn",
  "so",
  "some",
  "somebody",
  "someday",
  "somehow",
  "someone",
  "somethan",
  "something",
  "sometime",
  "sometimes",
  "somewhat",
  "somewhere",
  "soon",
  "sorry",
  "specifically",
  "specified",
  "specify",
  "specifying",
  "sr",
  "st",
  "state",
  "states",
  "still",
  "stop",
  "strongly",
  "su",
  "sub",
  "substantially",
  "successfully",
  "such",
  "sufficiently",
  "suggest",
  "sup",
  "sure",
  "sv",
  "sy",
  "system",
  "sz",
  "t",
  "t's",
  "take",
  "taken",
  "taking",
  "tc",
  "td",
  "tell",
  "ten",
  "tends",
  "test",
  "text",
  "tf",
  "tg",
  "th",
  "than",
  "thank",
  "thanks",
  "thanx",
  "that",
  "that'll",
  "that's",
  "that've",
  "thatll",
  "thats",
  "thatve",
  "the",
  "their",
  "theirs",
  "them",
  "themselves",
  "then",
  "thence",
  "there",
  "there'd",
  "there'll",
  "there're",
  "there's",
  "there've",
  "thereafter",
  "thereby",
  "thered",
  "therefore",
  "therein",
  "therell",
  "thereof",
  "therere",
  "theres",
  "thereto",
  "thereupon",
  "thereve",
  "these",
  "they",
  "they'd",
  "they'll",
  "they're",
  "they've",
  "theyd",
  "theyll",
  "theyre",
  "theyve",
  "thick",
  "thin",
  "thing",
  "things",
  "think",
  "thinks",
  "third",
  "thirty",
  "this",
  "thorough",
  "thoroughly",
  "those",
  "thou",
  "though",
  "thoughh",
  "thought",
  "thoughts",
  "thousand",
  "three",
  "throug",
  "through",
  "throughout",
  "thru",
  "thus",
  "til",
  "till",
  "tip",
  "tis",
  "tj",
  "tk",
  "tm",
  "tn",
  "to",
  "today",
  "together",
  "too",
  "took",
  "top",
  "toward",
  "towards",
  "tp",
  "tr",
  "tried",
  "tries",
  "trillion",
  "truly",
  "try",
  "trying",
  "ts",
  "tt",
  "turn",
  "turned",
  "turning",
  "turns",
  "tv",
  "tw",
  "twas",
  "twelve",
  "twenty",
  "twice",
  "two",
  "tz",
  "u",
  "ua",
  "ug",
  "uk",
  "um",
  "un",
  "under",
  "underneath",
  "undoing",
  "unfortunately",
  "unless",
  "unlike",
  "unlikely",
  "until",
  "unto",
  "up",
  "upon",
  "ups",
  "upwards",
  "us",
  "use",
  "used",
  "useful",
  "usefully",
  "usefulness",
  "uses",
  "using",
  "usually",
  "uucp",
  "uy",
  "uz",
  "v",
  "va",
  "value",
  "various",
  "vc",
  "ve",
  "versus",
  "very",
  "vg",
  "vi",
  "via",
  "viz",
  "vn",
  "vol",
  "vols",
  "vs",
  "vu",
  "w",
  "want",
  "wanted",
  "wanting",
  "wants",
  "was",
  "wasn",
  "wasn't",
  "wasnt",
  "way",
  "ways",
  "we",
  "we'd",
  "we'll",
  "we're",
  "we've",
  "web",
  "webpage",
  "website",
  "wed",
  "welcome",
  "well",
  "wells",
  "went",
  "were",
  "weren",
  "weren't",
  "werent",
  "weve",
  "wf",
  "what",
  "what'd",
  "what'll",
  "what's",
  "what've",
  "whatever",
  "whatll",
  "whats",
  "whatve",
  "when",
  "when'd",
  "when'll",
  "when's",
  "whence",
  "whenever",
  "where",
  "where'd",
  "where'll",
  "where's",
  "whereafter",
  "whereas",
  "whereby",
  "wherein",
  "wheres",
  "whereupon",
  "wherever",
  "whether",
  "which",
  "whichever",
  "while",
  "whilst",
  "whim",
  "whither",
  "who",
  "who'd",
  "who'll",
  "who's",
  "whod",
  "whoever",
  "whole",
  "wholl",
  "whom",
  "whomever",
  "whos",
  "whose",
  "why",
  "why'd",
  "why'll",
  "why's",
  "widely",
  "width",
  "will",
  "willing",
  "wish",
  "with",
  "within",
  "without",
  "won",
  "won't",
  "wonder",
  "wont",
  "words",
  "work",
  "worked",
  "working",
  "works",
  "world",
  "would",
  "would've",
  "wouldn",
  "wouldn't",
  "wouldnt",
  "ws",
  "www",
  "x",
  "y",
  "ye",
  "year",
  "years",
  "yes",
  "yet",
  "you",
  "you'd",
  "you'll",
  "you're",
  "you've",
  "youd",
  "youll",
  "young",
  "younger",
  "youngest",
  "your",
  "youre",
  "yours",
  "yourself",
  "yourselves",
  "youve",
  "yt",
  "yu",
  "z",
  "za",
  "zero",
  "zm",
  "zr"
], rG = [], sG = [], oG = new Set([...nG || [], ...rG || []].map((e) => sa(e)).filter(Boolean)), iG = new Set((sG || []).map((e) => sa(e)).filter(Boolean)), cl = null, dl = null, pu = null, Fi = "idle", Bi = "", aG = 2, lG = 3.2;
function Qg() {
  return {
    status: Fi,
    error: Bi
  };
}
function uG() {
  return Fi === "ready" && !!cl && !!dl;
}
function sp(e) {
  return e instanceof Error ? e.message : String(e || "memory_tokenizer_failed");
}
function Hu(e, t = "") {
  const n = t ? `${e}: ${t}` : e, r = new Error(n);
  return r.code = e, r;
}
function sa(e = "") {
  return String(e || "").normalize("NFKC").replace(/[\u200B-\u200D\uFEFF]/g, "").toLowerCase().replace(/\s+/g, " ").trim();
}
function cG(e = "") {
  return sa(e).replace(/\s+/g, "");
}
function eM(e) {
  return e.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function tM(e = "") {
  return cG(e);
}
function dG(e = []) {
  return new Set(e.map((t) => tM(t)).filter((t) => t.length >= 2));
}
function fG(e, t) {
  let n = String(e || "");
  return t.forEach((r) => {
    r && (n = n.replace(new RegExp(eM(r), "gi"), " "));
  }), n;
}
function hG(e, t = []) {
  const n = [
    ...t,
    "user",
    "assistant",
    "用户",
    "角色",
    "玩家",
    "旁白"
  ].map((s) => String(s || "").trim()).filter(Boolean);
  if (!n.length) return e;
  const r = n.map(eM).join("|");
  return String(e || "").split(/\r?\n/).map((s) => s.replace(new RegExp(`^\\s*(?:${r})\\s*[:：]\\s*`, "i"), "")).join(`
`);
}
function nM(e = "") {
  return String(e || "").replace(/\[tts:[^\]]*]/gi, " ").replace(/<state>[\s\S]*?<\/state>/gi, " ").replace(/\s+/g, " ").trim();
}
function rM(e) {
  return e >= 12352 && e <= 12447 || e >= 12448 && e <= 12543 || e >= 12784 && e <= 12799 || e >= 65381 && e <= 65439;
}
function sM(e) {
  return e >= 19968 && e <= 40959 || e >= 13312 && e <= 19903 || e >= 63744 && e <= 64255 || e >= 131072 && e <= 173791;
}
function pG(e) {
  return sM(e) || rM(e);
}
function mG(e) {
  return e >= 65 && e <= 90 || e >= 97 && e <= 122 || e >= 48 && e <= 57 || e >= 192 && e <= 591;
}
function gG(e) {
  let t = 0, n = 0;
  for (const s of e) {
    const o = s.codePointAt(0) || 0;
    rM(o) ? t += 1 : sM(o) && (n += 1);
  }
  const r = t + n;
  return r ? t / r > 0.3 ? "ja" : "zh" : "other";
}
function vG(e) {
  return String(e || "").split(/[\s\-_.,;:!?'"()[\]{}<>/\\|@#$%^&*+=~`]+/).map((t) => sa(t)).filter((t) => t.length >= 3);
}
function yG(e) {
  const t = gG(e);
  if (t === "ja") {
    if (!dl) throw Hu("memory_tokenizer_not_ready", "tiny_segmenter_not_ready");
    try {
      return dl.segment(e).map((n) => sa(n)).filter((n) => n.length >= 2);
    } catch (n) {
      throw Hu("memory_tokenizer_failed", sp(n));
    }
  }
  if (t === "other") return [];
  if (cl) try {
    return Array.from(cl(e, !0)).map((n) => sa(n)).filter((n) => n.length >= 2);
  } catch (n) {
    throw Hu("memory_tokenizer_failed", sp(n));
  }
  throw Hu("memory_tokenizer_not_ready", "jieba_wasm_not_ready");
}
function _G(e) {
  const t = [];
  let n = null, r = "";
  const s = () => {
    r && (n === "asian" ? t.push(...yG(r)) : n === "latin" && t.push(...vG(r)), r = "");
  };
  for (const o of String(e || "")) {
    const i = o.codePointAt(0) || 0, a = pG(i) ? "asian" : mG(i) ? "latin" : "other";
    a !== n && (s(), n = a), a !== "other" && (r += o);
  }
  return s(), t;
}
function bG(e) {
  const t = /* @__PURE__ */ new Set(), n = [];
  return _G(e).forEach((r) => {
    const s = sa(r);
    !s || s.length < 2 || oG.has(s) && !iG.has(s) || /^[\s\x00-\x1F\p{P}\p{S}]+$/u.test(s) || t.has(s) || (t.add(s), n.push(s));
  }), n;
}
async function oM() {
  if (typeof window > "u")
    return Fi = "unavailable", Bi = "browser_required", !1;
  if (uG()) return !0;
  if (pu) return await pu;
  Fi = "loading", Bi = "", pu = (async () => {
    let e = null, t = null;
    try {
      const n = await import(
        /* @vite-ignore */
        `/${Rg}/libs/tiny-segmenter.js`
      ), r = n.TinySegmenter || n.default;
      if (!r) throw new Error("TinySegmenter constructor missing");
      e = new r();
    } catch (n) {
      return Fi = "failed", Bi = `tiny_segmenter_load_failed: ${sp(n)}`, dl = null, cl = null, !1;
    }
    try {
      const n = `/${Rg}/libs/jieba-wasm/jieba_rs_wasm_bg.wasm`, r = await import(
        /* @vite-ignore */
        `/${Rg}/libs/jieba-wasm/jieba_rs_wasm.js`
      );
      if (typeof r.default == "function" && await r.default({ module_or_path: n }), typeof r.cut != "function") throw new Error("jieba cut function missing");
      t = r.cut;
    } catch (n) {
      return Fi = "failed", Bi = `jieba_wasm_load_failed: ${sp(n)}`, cl = null, dl = null, !1;
    }
    return dl = e, cl = t, Fi = "ready", Bi = "", !0;
  })();
  try {
    return await pu;
  } finally {
    pu = null;
  }
}
async function wG() {
  if (!(typeof window > "u") && !await oM())
    throw Hu("memory_tokenizer_not_ready", Bi || "preload_failed");
}
function iM(e = "", t = /* @__PURE__ */ new Set()) {
  const n = fG(nM(e), t), r = /* @__PURE__ */ new Set();
  return bG(n).map((s) => tM(s)).filter(Boolean).forEach((s) => r.add(s)), [...r].filter((s) => {
    if (s.length < 2) return !1;
    for (const o of t) if (s === o || s.includes(o) || o.includes(s)) return !1;
    return !0;
  });
}
function SG(e, t, n) {
  return e.map((r) => {
    const s = t(r), o = s.map((l) => l.text).join(`
`), i = /* @__PURE__ */ new Map();
    let a = 0;
    return s.forEach((l) => {
      const u = Math.max(0.1, Number(l.weight) || 1), d = iM(l.text);
      a += d.length * u, d.forEach((h) => {
        i.set(h, (i.get(h) || 0) + u);
      });
    }), {
      id: n(r),
      text: o,
      tokens: new Set(i.keys()),
      termWeights: i,
      weightedLength: a
    };
  });
}
function TG(e) {
  const t = /* @__PURE__ */ new Map();
  e.forEach((s) => {
    s.tokens.forEach((o) => {
      t.set(o, (t.get(o) || 0) + 1);
    });
  });
  const n = Math.max(1, e.length), r = /* @__PURE__ */ new Map();
  return t.forEach((s, o) => {
    r.set(o, Math.log((n + 1) / (s + 1)) + 1);
  }), r;
}
function EG(e, t) {
  return (e.length <= 2 ? 0.55 : e.length === 3 ? 0.9 : 1.25) * (t.get(e) || 1);
}
function CG(e) {
  return e.length ? Math.max(1, e.reduce((t, n) => t + Math.max(1, n.weightedLength), 0) / e.length) : 1;
}
function AG(e, t = [], n, r = 1) {
  if (!e || !t.length) return 0;
  const s = 1.2, o = 0.2, i = s * (1 - o + o * (Math.max(1, e.weightedLength) / Math.max(1, r)));
  return t.reduce((a, l) => {
    const u = e.termWeights.get(l) || 0;
    if (u <= 0) return a;
    const d = u * (s + 1) / (u + i);
    return a + EG(l, n) * d;
  }, 0);
}
function Zg(e, t) {
  return {
    text: String(e || ""),
    weight: t
  };
}
function aM(e) {
  if ("title" in e && e.title) return String(e.title || "").trim();
  const t = "content" in e ? String(e.content || "").match(/^\s*#\s+(.+?)\s*$/m)?.[1] : "";
  return String(t || e.path.split("/").pop() || e.path).trim();
}
function IG(e) {
  const t = String(e.path || ""), n = t === "memory/state.md" ? 2.2 : t === "memory/session.md" ? 2 : t.startsWith("memory/turns/") ? 1.6 : 1.4;
  return [
    Zg(e.path, n),
    Zg(aM(e), n),
    Zg("searchText" in e && e.searchText || "preview" in e && e.preview || ("content" in e ? e.content : ""), 1)
  ];
}
function lM(e) {
  const t = String(e.path || "");
  return e.status === "stale" ? !1 : ["memory/session.md", "memory/state.md"].includes(t) || t.startsWith("memory/turns/");
}
function rw(e = {}) {
  return [e.character?.name, e.user?.name].map((t) => String(t || "").trim()).filter((t) => t.length >= 2);
}
function uM(e = {}, t = "") {
  const n = Array.isArray(e.history) ? e.history.slice(-aG) : [], r = rw(e), s = (o) => hG(nM(o), r);
  return [s(t), ...n.map((o) => s(o.content || o.mes || o.message || ""))].filter(Boolean).join(`
`);
}
function xG(e = {}) {
  const t = [...e.memoryFiles || []].filter(lM).sort((d, h) => d.path.localeCompare(h.path)), n = dG(e.ignoredTerms || []), r = iM(e.queryText, n), s = SG(t, (d) => IG(d), (d) => d.path), o = [...s], i = TG(o), a = CG(o), l = new Map(s.map((d) => [d.id, d])), u = /* @__PURE__ */ new Set(["memory/session.md", "memory/state.md"]);
  return { memoryFiles: t.map((d) => ({
    file: d,
    id: d.path,
    score: AG(l.get(d.path), r, i, a)
  })).filter((d) => u.has(d.file.path) || d.score >= lG).sort((d, h) => d.file.path.localeCompare(h.file.path)).map((d) => ({
    ...d.file,
    recallReason: u.has(d.file.path) ? "fixed" : "matched",
    recallScore: d.score
  })).map((d) => ({
    path: d.path,
    title: aM(d),
    content: String("preview" in d && d.preview || ("content" in d ? d.content : "") || "").slice(0, 2400),
    recallReason: d.recallReason,
    recallScore: d.recallScore
  })) };
}
async function cM(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) return {};
  const n = e.includeMemoryFiles !== !1, r = e.includeStructuredStates !== !1, [s, o] = await Promise.all([n ? jk(t) : Promise.resolve(null), r ? FH(t) : Promise.resolve([])]), i = n && s?.status === "ready" && Array.isArray(s.files) || n && s?.status === "stale" && s.error && Array.isArray(s.files) ? s : n ? await Xo(t) : null, a = Array.isArray(i?.files) ? i.files : [], l = a.some(lM);
  return !l && !o.length ? {} : (l && await wG(), {
    ...l ? xG({
      memoryFiles: a,
      queryText: e.queryText,
      ignoredTerms: e.ignoredTerms
    }) : {},
    structuredStates: o
  });
}
function Ke(e, t, n, r, s) {
  if (r === "m") throw new TypeError("Private method is not writable");
  if (r === "a" && !s) throw new TypeError("Private accessor was defined without a setter");
  if (typeof t == "function" ? e !== t || !s : !t.has(e)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return r === "a" ? s.call(e, n) : s ? s.value = n : t.set(e, n), n;
}
function ce(e, t, n, r) {
  if (n === "a" && !r) throw new TypeError("Private accessor was defined without a getter");
  if (typeof t == "function" ? e !== t || !r : !t.has(e)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return n === "m" ? r : n === "a" ? r.call(e) : r ? r.value : t.get(e);
}
var dM = function() {
  const { crypto: e } = globalThis;
  if (e?.randomUUID)
    return dM = e.randomUUID.bind(e), e.randomUUID();
  const t = new Uint8Array(1), n = e ? () => e.getRandomValues(t)[0] : () => Math.random() * 255 & 255;
  return "10000000-1000-4000-8000-100000000000".replace(/[018]/g, (r) => (+r ^ n() & 15 >> +r / 4).toString(16));
};
function Hc(e) {
  return typeof e == "object" && e !== null && ("name" in e && e.name === "AbortError" || "message" in e && String(e.message).includes("FetchRequestCanceledException"));
}
var Ny = (e) => {
  if (e instanceof Error) return e;
  if (typeof e == "object" && e !== null) {
    try {
      if (Object.prototype.toString.call(e) === "[object Error]") {
        const t = new Error(e.message, e.cause ? { cause: e.cause } : {});
        return e.stack && (t.stack = e.stack), e.cause && !t.cause && (t.cause = e.cause), e.name && (t.name = e.name), t;
      }
    } catch {
    }
    try {
      return new Error(JSON.stringify(e));
    } catch {
    }
  }
  return new Error(e);
}, _t = class extends Error {
}, Lr = class $y extends _t {
  constructor(t, n, r, s, o) {
    super(`${$y.makeMessage(t, n, r)}`), this.status = t, this.headers = s, this.requestID = s?.get("request-id"), this.error = n, this.type = o ?? null;
  }
  static makeMessage(t, n, r) {
    const s = n?.message ? typeof n.message == "string" ? n.message : JSON.stringify(n.message) : n ? JSON.stringify(n) : r;
    return t && s ? `${t} ${s}` : t ? `${t} status code (no body)` : s || "(no status code or body)";
  }
  static generate(t, n, r, s) {
    if (!t || !s) return new Zp({
      message: r,
      cause: Ny(n)
    });
    const o = n, i = o?.error?.type;
    return t === 400 ? new hM(t, o, r, s, i) : t === 401 ? new pM(t, o, r, s, i) : t === 403 ? new mM(t, o, r, s, i) : t === 404 ? new gM(t, o, r, s, i) : t === 409 ? new vM(t, o, r, s, i) : t === 422 ? new yM(t, o, r, s, i) : t === 429 ? new _M(t, o, r, s, i) : t >= 500 ? new bM(t, o, r, s, i) : new $y(t, o, r, s, i);
  }
}, ss = class extends Lr {
  constructor({ message: e } = {}) {
    super(void 0, void 0, e || "Request was aborted.", void 0);
  }
}, Zp = class extends Lr {
  constructor({ message: e, cause: t }) {
    super(void 0, void 0, e || "Connection error.", void 0), t && (this.cause = t);
  }
}, fM = class extends Zp {
  constructor({ message: e } = {}) {
    super({ message: e ?? "Request timed out." });
  }
}, hM = class extends Lr {
}, pM = class extends Lr {
}, mM = class extends Lr {
}, gM = class extends Lr {
}, vM = class extends Lr {
}, yM = class extends Lr {
}, _M = class extends Lr {
}, bM = class extends Lr {
}, kG = /^[a-z][a-z0-9+.-]*:/i, MG = (e) => kG.test(e), Dy = (e) => (Dy = Array.isArray, Dy(e)), TT = Dy;
function Ly(e) {
  return typeof e != "object" ? {} : e ?? {};
}
function ET(e) {
  if (!e) return !0;
  for (const t in e) return !1;
  return !0;
}
function PG(e, t) {
  return Object.prototype.hasOwnProperty.call(e, t);
}
var RG = (e, t) => {
  if (typeof t != "number" || !Number.isInteger(t)) throw new _t(`${e} must be an integer`);
  if (t < 0) throw new _t(`${e} must be a positive integer`);
  return t;
}, wM = (e) => {
  try {
    return JSON.parse(e);
  } catch {
    return;
  }
}, NG = (e) => new Promise((t) => setTimeout(t, e)), Wa = "0.89.0", $G = () => typeof window < "u" && typeof window.document < "u" && typeof navigator < "u";
function DG() {
  return typeof Deno < "u" && Deno.build != null ? "deno" : typeof EdgeRuntime < "u" ? "edge" : Object.prototype.toString.call(typeof globalThis.process < "u" ? globalThis.process : 0) === "[object process]" ? "node" : "unknown";
}
var LG = () => {
  const e = DG();
  if (e === "deno") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Wa,
    "X-Stainless-OS": AT(Deno.build.os),
    "X-Stainless-Arch": CT(Deno.build.arch),
    "X-Stainless-Runtime": "deno",
    "X-Stainless-Runtime-Version": typeof Deno.version == "string" ? Deno.version : Deno.version?.deno ?? "unknown"
  };
  if (typeof EdgeRuntime < "u") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Wa,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": `other:${EdgeRuntime}`,
    "X-Stainless-Runtime": "edge",
    "X-Stainless-Runtime-Version": globalThis.process.version
  };
  if (e === "node") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Wa,
    "X-Stainless-OS": AT(globalThis.process.platform ?? "unknown"),
    "X-Stainless-Arch": CT(globalThis.process.arch ?? "unknown"),
    "X-Stainless-Runtime": "node",
    "X-Stainless-Runtime-Version": globalThis.process.version ?? "unknown"
  };
  const t = OG();
  return t ? {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Wa,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": "unknown",
    "X-Stainless-Runtime": `browser:${t.browser}`,
    "X-Stainless-Runtime-Version": t.version
  } : {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Wa,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": "unknown",
    "X-Stainless-Runtime": "unknown",
    "X-Stainless-Runtime-Version": "unknown"
  };
};
function OG() {
  if (typeof navigator > "u" || !navigator) return null;
  for (const { key: e, pattern: t } of [
    {
      key: "edge",
      pattern: /Edge(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "ie",
      pattern: /MSIE(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "ie",
      pattern: /Trident(?:.*rv\:(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "chrome",
      pattern: /Chrome(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "firefox",
      pattern: /Firefox(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "safari",
      pattern: /(?:Version\W+(\d+)\.(\d+)(?:\.(\d+))?)?(?:\W+Mobile\S*)?\W+Safari/
    }
  ]) {
    const n = t.exec(navigator.userAgent);
    if (n) return {
      browser: e,
      version: `${n[1] || 0}.${n[2] || 0}.${n[3] || 0}`
    };
  }
  return null;
}
var CT = (e) => e === "x32" ? "x32" : e === "x86_64" || e === "x64" ? "x64" : e === "arm" ? "arm" : e === "aarch64" || e === "arm64" ? "arm64" : e ? `other:${e}` : "unknown", AT = (e) => (e = e.toLowerCase(), e.includes("ios") ? "iOS" : e === "android" ? "Android" : e === "darwin" ? "MacOS" : e === "win32" ? "Windows" : e === "freebsd" ? "FreeBSD" : e === "openbsd" ? "OpenBSD" : e === "linux" ? "Linux" : e ? `Other:${e}` : "Unknown"), IT, FG = () => IT ?? (IT = LG());
function BG() {
  if (typeof fetch < "u") return fetch;
  throw new Error("`fetch` is not defined as a global; Either pass `fetch` to the client, `new Anthropic({ fetch })` or polyfill the global, `globalThis.fetch = fetch`");
}
function SM(...e) {
  const t = globalThis.ReadableStream;
  if (typeof t > "u") throw new Error("`ReadableStream` is not defined as a global; You will need to polyfill it, `globalThis.ReadableStream = ReadableStream`");
  return new t(...e);
}
function TM(e) {
  let t = Symbol.asyncIterator in e ? e[Symbol.asyncIterator]() : e[Symbol.iterator]();
  return SM({
    start() {
    },
    async pull(n) {
      const { done: r, value: s } = await t.next();
      r ? n.close() : n.enqueue(s);
    },
    async cancel() {
      await t.return?.();
    }
  });
}
function sw(e) {
  if (e[Symbol.asyncIterator]) return e;
  const t = e.getReader();
  return {
    async next() {
      try {
        const n = await t.read();
        return n?.done && t.releaseLock(), n;
      } catch (n) {
        throw t.releaseLock(), n;
      }
    },
    async return() {
      const n = t.cancel();
      return t.releaseLock(), await n, {
        done: !0,
        value: void 0
      };
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
async function UG(e) {
  if (e === null || typeof e != "object") return;
  if (e[Symbol.asyncIterator]) {
    await e[Symbol.asyncIterator]().return?.();
    return;
  }
  const t = e.getReader(), n = t.cancel();
  t.releaseLock(), await n;
}
var qG = ({ headers: e, body: t }) => ({
  bodyHeaders: { "content-type": "application/json" },
  body: JSON.stringify(t)
});
function HG(e) {
  return Object.entries(e).filter(([t, n]) => typeof n < "u").map(([t, n]) => {
    if (typeof n == "string" || typeof n == "number" || typeof n == "boolean") return `${encodeURIComponent(t)}=${encodeURIComponent(n)}`;
    if (n === null) return `${encodeURIComponent(t)}=`;
    throw new _t(`Cannot stringify type ${typeof n}; Expected string, number, boolean, or null. If you need to pass nested query parameters, you can manually encode them, e.g. { query: { 'foo[key1]': value1, 'foo[key2]': value2 } }, and please open a GitHub issue requesting better support for your use case.`);
  }).join("&");
}
function GG(e) {
  let t = 0;
  for (const s of e) t += s.length;
  const n = new Uint8Array(t);
  let r = 0;
  for (const s of e)
    n.set(s, r), r += s.length;
  return n;
}
var xT;
function ow(e) {
  let t;
  return (xT ?? (t = new globalThis.TextEncoder(), xT = t.encode.bind(t)))(e);
}
var kT;
function MT(e) {
  let t;
  return (kT ?? (t = new globalThis.TextDecoder(), kT = t.decode.bind(t)))(e);
}
var _r, br, ad = class {
  constructor() {
    _r.set(this, void 0), br.set(this, void 0), Ke(this, _r, new Uint8Array(), "f"), Ke(this, br, null, "f");
  }
  decode(e) {
    if (e == null) return [];
    const t = e instanceof ArrayBuffer ? new Uint8Array(e) : typeof e == "string" ? ow(e) : e;
    Ke(this, _r, GG([ce(this, _r, "f"), t]), "f");
    const n = [];
    let r;
    for (; (r = WG(ce(this, _r, "f"), ce(this, br, "f"))) != null; ) {
      if (r.carriage && ce(this, br, "f") == null) {
        Ke(this, br, r.index, "f");
        continue;
      }
      if (ce(this, br, "f") != null && (r.index !== ce(this, br, "f") + 1 || r.carriage)) {
        n.push(MT(ce(this, _r, "f").subarray(0, ce(this, br, "f") - 1))), Ke(this, _r, ce(this, _r, "f").subarray(ce(this, br, "f")), "f"), Ke(this, br, null, "f");
        continue;
      }
      const s = ce(this, br, "f") !== null ? r.preceding - 1 : r.preceding, o = MT(ce(this, _r, "f").subarray(0, s));
      n.push(o), Ke(this, _r, ce(this, _r, "f").subarray(r.index), "f"), Ke(this, br, null, "f");
    }
    return n;
  }
  flush() {
    return ce(this, _r, "f").length ? this.decode(`
`) : [];
  }
};
_r = /* @__PURE__ */ new WeakMap(), br = /* @__PURE__ */ new WeakMap();
ad.NEWLINE_CHARS = /* @__PURE__ */ new Set([`
`, "\r"]);
ad.NEWLINE_REGEXP = /\r\n|[\n\r]/g;
function WG(e, t) {
  for (let s = t ?? 0; s < e.length; s++) {
    if (e[s] === 10) return {
      preceding: s,
      index: s + 1,
      carriage: !1
    };
    if (e[s] === 13) return {
      preceding: s,
      index: s + 1,
      carriage: !0
    };
  }
  return null;
}
function VG(e) {
  for (let r = 0; r < e.length - 1; r++) {
    if (e[r] === 10 && e[r + 1] === 10 || e[r] === 13 && e[r + 1] === 13) return r + 2;
    if (e[r] === 13 && e[r + 1] === 10 && r + 3 < e.length && e[r + 2] === 13 && e[r + 3] === 10) return r + 4;
  }
  return -1;
}
var op = {
  off: 0,
  error: 200,
  warn: 300,
  info: 400,
  debug: 500
}, PT = (e, t, n) => {
  if (e) {
    if (PG(op, e)) return e;
    Gn(n).warn(`${t} was set to ${JSON.stringify(e)}, expected one of ${JSON.stringify(Object.keys(op))}`);
  }
};
function Gu() {
}
function df(e, t, n) {
  return !t || op[e] > op[n] ? Gu : t[e].bind(t);
}
var KG = {
  error: Gu,
  warn: Gu,
  info: Gu,
  debug: Gu
}, RT = /* @__PURE__ */ new WeakMap();
function Gn(e) {
  const t = e.logger, n = e.logLevel ?? "off";
  if (!t) return KG;
  const r = RT.get(t);
  if (r && r[0] === n) return r[1];
  const s = {
    error: df("error", t, n),
    warn: df("warn", t, n),
    info: df("info", t, n),
    debug: df("debug", t, n)
  };
  return RT.set(t, [n, s]), s;
}
var Pi = (e) => (e.options && (e.options = { ...e.options }, delete e.options.headers), e.headers && (e.headers = Object.fromEntries((e.headers instanceof Headers ? [...e.headers] : Object.entries(e.headers)).map(([t, n]) => [t, t.toLowerCase() === "x-api-key" || t.toLowerCase() === "authorization" || t.toLowerCase() === "cookie" || t.toLowerCase() === "set-cookie" ? "***" : n]))), "retryOfRequestLogID" in e && (e.retryOfRequestLogID && (e.retryOf = e.retryOfRequestLogID), delete e.retryOfRequestLogID), e), mu, Gc = class Wu {
  constructor(t, n, r) {
    this.iterator = t, mu.set(this, void 0), this.controller = n, Ke(this, mu, r, "f");
  }
  static fromSSEResponse(t, n, r) {
    let s = !1;
    const o = r ? Gn(r) : console;
    async function* i() {
      if (s) throw new _t("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
      s = !0;
      let a = !1;
      try {
        for await (const l of zG(t, n)) {
          if (l.event === "completion") try {
            yield JSON.parse(l.data);
          } catch (u) {
            throw o.error("Could not parse message into JSON:", l.data), o.error("From chunk:", l.raw), u;
          }
          if (l.event === "message_start" || l.event === "message_delta" || l.event === "message_stop" || l.event === "content_block_start" || l.event === "content_block_delta" || l.event === "content_block_stop" || l.event === "message" || l.event === "user.message" || l.event === "user.interrupt" || l.event === "user.tool_confirmation" || l.event === "user.custom_tool_result" || l.event === "agent.message" || l.event === "agent.thinking" || l.event === "agent.tool_use" || l.event === "agent.tool_result" || l.event === "agent.mcp_tool_use" || l.event === "agent.mcp_tool_result" || l.event === "agent.custom_tool_use" || l.event === "agent.thread_context_compacted" || l.event === "session.status_running" || l.event === "session.status_idle" || l.event === "session.status_rescheduled" || l.event === "session.status_terminated" || l.event === "session.error" || l.event === "session.deleted" || l.event === "span.model_request_start" || l.event === "span.model_request_end") try {
            yield JSON.parse(l.data);
          } catch (u) {
            throw o.error("Could not parse message into JSON:", l.data), o.error("From chunk:", l.raw), u;
          }
          if (l.event !== "ping" && l.event === "error") {
            const u = wM(l.data) ?? l.data, d = u?.error?.type;
            throw new Lr(void 0, u, void 0, t.headers, d);
          }
        }
        a = !0;
      } catch (l) {
        if (Hc(l)) return;
        throw l;
      } finally {
        a || n.abort();
      }
    }
    return new Wu(i, n, r);
  }
  static fromReadableStream(t, n, r) {
    let s = !1;
    async function* o() {
      const a = new ad(), l = sw(t);
      for await (const u of l) for (const d of a.decode(u)) yield d;
      for (const u of a.flush()) yield u;
    }
    async function* i() {
      if (s) throw new _t("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
      s = !0;
      let a = !1;
      try {
        for await (const l of o())
          a || l && (yield JSON.parse(l));
        a = !0;
      } catch (l) {
        if (Hc(l)) return;
        throw l;
      } finally {
        a || n.abort();
      }
    }
    return new Wu(i, n, r);
  }
  [(mu = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
    return this.iterator();
  }
  tee() {
    const t = [], n = [], r = this.iterator(), s = (o) => ({ next: () => {
      if (o.length === 0) {
        const i = r.next();
        t.push(i), n.push(i);
      }
      return o.shift();
    } });
    return [new Wu(() => s(t), this.controller, ce(this, mu, "f")), new Wu(() => s(n), this.controller, ce(this, mu, "f"))];
  }
  toReadableStream() {
    const t = this;
    let n;
    return SM({
      async start() {
        n = t[Symbol.asyncIterator]();
      },
      async pull(r) {
        try {
          const { value: s, done: o } = await n.next();
          if (o) return r.close();
          const i = ow(JSON.stringify(s) + `
`);
          r.enqueue(i);
        } catch (s) {
          r.error(s);
        }
      },
      async cancel() {
        await n.return?.();
      }
    });
  }
};
async function* zG(e, t) {
  if (!e.body)
    throw t.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new _t("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new _t("Attempted to iterate over a response with no body");
  const n = new YG(), r = new ad(), s = sw(e.body);
  for await (const o of JG(s)) for (const i of r.decode(o)) {
    const a = n.decode(i);
    a && (yield a);
  }
  for (const o of r.flush()) {
    const i = n.decode(o);
    i && (yield i);
  }
}
async function* JG(e) {
  let t = new Uint8Array();
  for await (const n of e) {
    if (n == null) continue;
    const r = n instanceof ArrayBuffer ? new Uint8Array(n) : typeof n == "string" ? ow(n) : n;
    let s = new Uint8Array(t.length + r.length);
    s.set(t), s.set(r, t.length), t = s;
    let o;
    for (; (o = VG(t)) !== -1; )
      yield t.slice(0, o), t = t.slice(o);
  }
  t.length > 0 && (yield t);
}
var YG = class {
  constructor() {
    this.event = null, this.data = [], this.chunks = [];
  }
  decode(e) {
    if (e.endsWith("\r") && (e = e.substring(0, e.length - 1)), !e) {
      if (!this.event && !this.data.length) return null;
      const s = {
        event: this.event,
        data: this.data.join(`
`),
        raw: this.chunks
      };
      return this.event = null, this.data = [], this.chunks = [], s;
    }
    if (this.chunks.push(e), e.startsWith(":")) return null;
    let [t, n, r] = XG(e, ":");
    return r.startsWith(" ") && (r = r.substring(1)), t === "event" ? this.event = r : t === "data" && this.data.push(r), null;
  }
};
function XG(e, t) {
  const n = e.indexOf(t);
  return n !== -1 ? [
    e.substring(0, n),
    t,
    e.substring(n + t.length)
  ] : [
    e,
    "",
    ""
  ];
}
async function EM(e, t) {
  const { response: n, requestLogID: r, retryOfRequestLogID: s, startTime: o } = t, i = await (async () => {
    if (t.options.stream)
      return Gn(e).debug("response", n.status, n.url, n.headers, n.body), t.options.__streamClass ? t.options.__streamClass.fromSSEResponse(n, t.controller) : Gc.fromSSEResponse(n, t.controller);
    if (n.status === 204) return null;
    if (t.options.__binaryResponse) return n;
    const a = n.headers.get("content-type")?.split(";")[0]?.trim();
    return a?.includes("application/json") || a?.endsWith("+json") ? n.headers.get("content-length") === "0" ? void 0 : CM(await n.json(), n) : await n.text();
  })();
  return Gn(e).debug(`[${r}] response parsed`, Pi({
    retryOfRequestLogID: s,
    url: n.url,
    status: n.status,
    body: i,
    durationMs: Date.now() - o
  })), i;
}
function CM(e, t) {
  return !e || typeof e != "object" || Array.isArray(e) ? e : Object.defineProperty(e, "_request_id", {
    value: t.headers.get("request-id"),
    enumerable: !1
  });
}
var Vu, AM = class IM extends Promise {
  constructor(t, n, r = EM) {
    super((s) => {
      s(null);
    }), this.responsePromise = n, this.parseResponse = r, Vu.set(this, void 0), Ke(this, Vu, t, "f");
  }
  _thenUnwrap(t) {
    return new IM(ce(this, Vu, "f"), this.responsePromise, async (n, r) => CM(t(await this.parseResponse(n, r), r), r.response));
  }
  asResponse() {
    return this.responsePromise.then((t) => t.response);
  }
  async withResponse() {
    const [t, n] = await Promise.all([this.parse(), this.asResponse()]);
    return {
      data: t,
      response: n,
      request_id: n.headers.get("request-id")
    };
  }
  parse() {
    return this.parsedPromise || (this.parsedPromise = this.responsePromise.then((t) => this.parseResponse(ce(this, Vu, "f"), t))), this.parsedPromise;
  }
  then(t, n) {
    return this.parse().then(t, n);
  }
  catch(t) {
    return this.parse().catch(t);
  }
  finally(t) {
    return this.parse().finally(t);
  }
};
Vu = /* @__PURE__ */ new WeakMap();
var ff, xM = class {
  constructor(e, t, n, r) {
    ff.set(this, void 0), Ke(this, ff, e, "f"), this.options = r, this.response = t, this.body = n;
  }
  hasNextPage() {
    return this.getPaginatedItems().length ? this.nextPageRequestOptions() != null : !1;
  }
  async getNextPage() {
    const e = this.nextPageRequestOptions();
    if (!e) throw new _t("No next page expected; please check `.hasNextPage()` before calling `.getNextPage()`.");
    return await ce(this, ff, "f").requestAPIList(this.constructor, e);
  }
  async *iterPages() {
    let e = this;
    for (yield e; e.hasNextPage(); )
      e = await e.getNextPage(), yield e;
  }
  async *[(ff = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
    for await (const e of this.iterPages()) for (const t of e.getPaginatedItems()) yield t;
  }
}, QG = class extends AM {
  constructor(e, t, n) {
    super(e, t, async (r, s) => new n(r, s.response, await EM(r, s), s.options));
  }
  async *[Symbol.asyncIterator]() {
    const e = await this;
    for await (const t of e) yield t;
  }
}, ld = class extends xM {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.has_more = n.has_more || !1, this.first_id = n.first_id || null, this.last_id = n.last_id || null;
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  hasNextPage() {
    return this.has_more === !1 ? !1 : super.hasNextPage();
  }
  nextPageRequestOptions() {
    if (this.options.query?.before_id) {
      const t = this.first_id;
      return t ? {
        ...this.options,
        query: {
          ...Ly(this.options.query),
          before_id: t
        }
      } : null;
    }
    const e = this.last_id;
    return e ? {
      ...this.options,
      query: {
        ...Ly(this.options.query),
        after_id: e
      }
    } : null;
  }
}, Ls = class extends xM {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.next_page = n.next_page || null;
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  nextPageRequestOptions() {
    const e = this.next_page;
    return e ? {
      ...this.options,
      query: {
        ...Ly(this.options.query),
        page: e
      }
    } : null;
  }
}, kM = () => {
  if (typeof File > "u") {
    const { process: e } = globalThis, t = typeof e?.versions?.node == "string" && parseInt(e.versions.node.split(".")) < 20;
    throw new Error("`File` is not defined as a global, which is required for file uploads." + (t ? " Update to Node 20 LTS or newer, or set `globalThis.File` to `import('node:buffer').File`." : ""));
  }
};
function fl(e, t, n) {
  return kM(), new File(e, t ?? "unknown_file", n);
}
function th(e, t) {
  const n = typeof e == "object" && e !== null && ("name" in e && e.name && String(e.name) || "url" in e && e.url && String(e.url) || "filename" in e && e.filename && String(e.filename) || "path" in e && e.path && String(e.path)) || "";
  return t ? n.split(/[\\/]/).pop() || void 0 : n;
}
var MM = (e) => e != null && typeof e == "object" && typeof e[Symbol.asyncIterator] == "function", iw = async (e, t, n = !0) => ({
  ...e,
  body: await jG(e.body, t, n)
}), NT = /* @__PURE__ */ new WeakMap();
function ZG(e) {
  const t = typeof e == "function" ? e : e.fetch, n = NT.get(t);
  if (n) return n;
  const r = (async () => {
    try {
      const s = "Response" in t ? t.Response : (await t("data:,")).constructor, o = new FormData();
      return o.toString() !== await new s(o).text();
    } catch {
      return !0;
    }
  })();
  return NT.set(t, r), r;
}
var jG = async (e, t, n = !0) => {
  if (!await ZG(t)) throw new TypeError("The provided fetch function does not support file uploads with the current global FormData class.");
  const r = new FormData();
  return await Promise.all(Object.entries(e || {}).map(([s, o]) => Oy(r, s, o, n))), r;
}, eW = (e) => e instanceof Blob && "name" in e, Oy = async (e, t, n, r) => {
  if (n !== void 0) {
    if (n == null) throw new TypeError(`Received null for "${t}"; to pass null in FormData, you must use the string 'null'`);
    if (typeof n == "string" || typeof n == "number" || typeof n == "boolean") e.append(t, String(n));
    else if (n instanceof Response) {
      let s = {};
      const o = n.headers.get("Content-Type");
      o && (s = { type: o }), e.append(t, fl([await n.blob()], th(n, r), s));
    } else if (MM(n)) e.append(t, fl([await new Response(TM(n)).blob()], th(n, r)));
    else if (eW(n)) e.append(t, fl([n], th(n, r), { type: n.type }));
    else if (Array.isArray(n)) await Promise.all(n.map((s) => Oy(e, t + "[]", s, r)));
    else if (typeof n == "object") await Promise.all(Object.entries(n).map(([s, o]) => Oy(e, `${t}[${s}]`, o, r)));
    else throw new TypeError(`Invalid value given to form, expected a string, number, boolean, object, Array, File or Blob but got ${n} instead`);
  }
}, PM = (e) => e != null && typeof e == "object" && typeof e.size == "number" && typeof e.type == "string" && typeof e.text == "function" && typeof e.slice == "function" && typeof e.arrayBuffer == "function", tW = (e) => e != null && typeof e == "object" && typeof e.name == "string" && typeof e.lastModified == "number" && PM(e), nW = (e) => e != null && typeof e == "object" && typeof e.url == "string" && typeof e.blob == "function";
async function rW(e, t, n) {
  if (kM(), e = await e, t || (t = th(e, !0)), tW(e))
    return e instanceof File && t == null && n == null ? e : fl([await e.arrayBuffer()], t ?? e.name, {
      type: e.type,
      lastModified: e.lastModified,
      ...n
    });
  if (nW(e)) {
    const s = await e.blob();
    return t || (t = new URL(e.url).pathname.split(/[\\/]/).pop()), fl(await Fy(s), t, n);
  }
  const r = await Fy(e);
  if (!n?.type) {
    const s = r.find((o) => typeof o == "object" && "type" in o && o.type);
    typeof s == "string" && (n = {
      ...n,
      type: s
    });
  }
  return fl(r, t, n);
}
async function Fy(e) {
  let t = [];
  if (typeof e == "string" || ArrayBuffer.isView(e) || e instanceof ArrayBuffer) t.push(e);
  else if (PM(e)) t.push(e instanceof Blob ? e : await e.arrayBuffer());
  else if (MM(e)) for await (const n of e) t.push(...await Fy(n));
  else {
    const n = e?.constructor?.name;
    throw new Error(`Unexpected data type: ${typeof e}${n ? `; constructor: ${n}` : ""}${sW(e)}`);
  }
  return t;
}
function sW(e) {
  return typeof e != "object" || e === null ? "" : `; props: [${Object.getOwnPropertyNames(e).map((t) => `"${t}"`).join(", ")}]`;
}
var hn = class {
  constructor(e) {
    this._client = e;
  }
}, RM = /* @__PURE__ */ Symbol.for("brand.privateNullableHeaders");
function* oW(e) {
  if (!e) return;
  if (RM in e) {
    const { values: r, nulls: s } = e;
    yield* r.entries();
    for (const o of s) yield [o, null];
    return;
  }
  let t = !1, n;
  e instanceof Headers ? n = e.entries() : TT(e) ? n = e : (t = !0, n = Object.entries(e ?? {}));
  for (let r of n) {
    const s = r[0];
    if (typeof s != "string") throw new TypeError("expected header name to be a string");
    const o = TT(r[1]) ? r[1] : [r[1]];
    let i = !1;
    for (const a of o)
      a !== void 0 && (t && !i && (i = !0, yield [s, null]), yield [s, a]);
  }
}
var qe = (e) => {
  const t = new Headers(), n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const s = /* @__PURE__ */ new Set();
    for (const [o, i] of oW(r)) {
      const a = o.toLowerCase();
      s.has(a) || (t.delete(o), s.add(a)), i === null ? (t.delete(o), n.add(a)) : (t.append(o, i), n.delete(a));
    }
  }
  return {
    [RM]: !0,
    values: t,
    nulls: n
  };
};
function NM(e) {
  return e.replace(/[^A-Za-z0-9\-._~!$&'()*+,;=:@]+/g, encodeURIComponent);
}
var $T = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.create(null)), iW = (e = NM) => function(n, ...r) {
  if (n.length === 1) return n[0];
  let s = !1;
  const o = [], i = n.reduce((d, h, p) => {
    /[?#]/.test(h) && (s = !0);
    const y = r[p];
    let w = (s ? encodeURIComponent : e)("" + y);
    return p !== r.length && (y == null || typeof y == "object" && y.toString === Object.getPrototypeOf(Object.getPrototypeOf(y.hasOwnProperty ?? $T) ?? $T)?.toString) && (w = y + "", o.push({
      start: d.length + h.length,
      length: w.length,
      error: `Value of type ${Object.prototype.toString.call(y).slice(8, -1)} is not a valid path parameter`
    })), d + h + (p === r.length ? "" : w);
  }, ""), a = i.split(/[?#]/, 1)[0], l = /(?<=^|\/)(?:\.|%2e){1,2}(?=\/|$)/gi;
  let u;
  for (; (u = l.exec(a)) !== null; ) o.push({
    start: u.index,
    length: u[0].length,
    error: `Value "${u[0]}" can't be safely passed as a path parameter`
  });
  if (o.sort((d, h) => d.start - h.start), o.length > 0) {
    let d = 0;
    const h = o.reduce((p, y) => {
      const w = " ".repeat(y.start - d), E = "^".repeat(y.length);
      return d = y.start + y.length, p + w + E;
    }, "");
    throw new _t(`Path parameters result in path with invalid segments:
${o.map((p) => p.error).join(`
`)}
${i}
${h}`);
  }
  return i;
}, pt = /* @__PURE__ */ iW(NM), $M = class extends hn {
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/environments?beta=true", {
      body: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(pt`/v1/environments/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(pt`/v1/environments/${e}?beta=true`, {
      body: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/environments?beta=true", Ls, {
      query: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(pt`/v1/environments/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  archive(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(pt`/v1/environments/${e}/archive?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, pc = /* @__PURE__ */ Symbol("anthropic.sdk.stainlessHelper");
function nh(e) {
  return typeof e == "object" && e !== null && pc in e;
}
function DM(e, t) {
  const n = /* @__PURE__ */ new Set();
  if (e)
    for (const r of e) nh(r) && n.add(r[pc]);
  if (t) {
    for (const r of t)
      if (nh(r) && n.add(r[pc]), Array.isArray(r.content))
        for (const s of r.content) nh(s) && n.add(s[pc]);
  }
  return Array.from(n);
}
function LM(e, t) {
  const n = DM(e, t);
  return n.length === 0 ? {} : { "x-stainless-helper": n.join(", ") };
}
function aW(e) {
  return nh(e) ? { "x-stainless-helper": e[pc] } : {};
}
var OM = class extends hn {
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/files", ld, {
      query: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "files-api-2025-04-14"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(pt`/v1/files/${e}`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "files-api-2025-04-14"].toString() }, n?.headers])
    });
  }
  download(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(pt`/v1/files/${e}/content`, {
      ...n,
      headers: qe([{
        "anthropic-beta": [...r ?? [], "files-api-2025-04-14"].toString(),
        Accept: "application/binary"
      }, n?.headers]),
      __binaryResponse: !0
    });
  }
  retrieveMetadata(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(pt`/v1/files/${e}`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "files-api-2025-04-14"].toString() }, n?.headers])
    });
  }
  upload(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/files", iw({
      body: r,
      ...t,
      headers: qe([
        { "anthropic-beta": [...n ?? [], "files-api-2025-04-14"].toString() },
        aW(r.file),
        t?.headers
      ])
    }, this._client));
  }
}, FM = class extends hn {
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(pt`/v1/models/${e}?beta=true`, {
      ...n,
      headers: qe([{ ...r?.toString() != null ? { "anthropic-beta": r?.toString() } : void 0 }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/models?beta=true", ld, {
      query: r,
      ...t,
      headers: qe([{ ...n?.toString() != null ? { "anthropic-beta": n?.toString() } : void 0 }, t?.headers])
    });
  }
}, BM = class extends hn {
  list(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.getAPIList(pt`/v1/agents/${e}/versions?beta=true`, Ls, {
      query: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, aw = class extends hn {
  constructor() {
    super(...arguments), this.versions = new BM(this._client);
  }
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/agents?beta=true", {
      body: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.get(pt`/v1/agents/${e}?beta=true`, {
      query: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(pt`/v1/agents/${e}?beta=true`, {
      body: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/agents?beta=true", Ls, {
      query: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  archive(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(pt`/v1/agents/${e}/archive?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
};
aw.Versions = BM;
var UM = {
  "claude-opus-4-20250514": 8192,
  "claude-opus-4-0": 8192,
  "claude-4-opus-20250514": 8192,
  "anthropic.claude-opus-4-20250514-v1:0": 8192,
  "claude-opus-4@20250514": 8192,
  "claude-opus-4-1-20250805": 8192,
  "anthropic.claude-opus-4-1-20250805-v1:0": 8192,
  "claude-opus-4-1@20250805": 8192
};
function qM(e) {
  return e?.output_format ?? e?.output_config?.format;
}
function DT(e, t, n) {
  const r = qM(t);
  return !t || !("parse" in (r ?? {})) ? {
    ...e,
    content: e.content.map((s) => {
      if (s.type === "text") {
        const o = Object.defineProperty({ ...s }, "parsed_output", {
          value: null,
          enumerable: !1
        });
        return Object.defineProperty(o, "parsed", {
          get() {
            return n.logger.warn("The `parsed` property on `text` blocks is deprecated, please use `parsed_output` instead."), null;
          },
          enumerable: !1
        });
      }
      return s;
    }),
    parsed_output: null
  } : HM(e, t, n);
}
function HM(e, t, n) {
  let r = null;
  const s = e.content.map((o) => {
    if (o.type === "text") {
      const i = lW(t, o.text);
      r === null && (r = i);
      const a = Object.defineProperty({ ...o }, "parsed_output", {
        value: i,
        enumerable: !1
      });
      return Object.defineProperty(a, "parsed", {
        get() {
          return n.logger.warn("The `parsed` property on `text` blocks is deprecated, please use `parsed_output` instead."), i;
        },
        enumerable: !1
      });
    }
    return o;
  });
  return {
    ...e,
    content: s,
    parsed_output: r
  };
}
function lW(e, t) {
  const n = qM(e);
  if (n?.type !== "json_schema") return null;
  try {
    return "parse" in n ? n.parse(t) : JSON.parse(t);
  } catch (r) {
    throw new _t(`Failed to parse structured output: ${r}`);
  }
}
var uW = (e) => {
  let t = 0, n = [];
  for (; t < e.length; ) {
    let r = e[t];
    if (r === "\\") {
      t++;
      continue;
    }
    if (r === "{") {
      n.push({
        type: "brace",
        value: "{"
      }), t++;
      continue;
    }
    if (r === "}") {
      n.push({
        type: "brace",
        value: "}"
      }), t++;
      continue;
    }
    if (r === "[") {
      n.push({
        type: "paren",
        value: "["
      }), t++;
      continue;
    }
    if (r === "]") {
      n.push({
        type: "paren",
        value: "]"
      }), t++;
      continue;
    }
    if (r === ":") {
      n.push({
        type: "separator",
        value: ":"
      }), t++;
      continue;
    }
    if (r === ",") {
      n.push({
        type: "delimiter",
        value: ","
      }), t++;
      continue;
    }
    if (r === '"') {
      let i = "", a = !1;
      for (r = e[++t]; r !== '"'; ) {
        if (t === e.length) {
          a = !0;
          break;
        }
        if (r === "\\") {
          if (t++, t === e.length) {
            a = !0;
            break;
          }
          i += r + e[t], r = e[++t];
        } else
          i += r, r = e[++t];
      }
      r = e[++t], a || n.push({
        type: "string",
        value: i
      });
      continue;
    }
    if (r && /\s/.test(r)) {
      t++;
      continue;
    }
    let s = /[0-9]/;
    if (r && s.test(r) || r === "-" || r === ".") {
      let i = "";
      for (r === "-" && (i += r, r = e[++t]); r && s.test(r) || r === "."; )
        i += r, r = e[++t];
      n.push({
        type: "number",
        value: i
      });
      continue;
    }
    let o = /[a-z]/i;
    if (r && o.test(r)) {
      let i = "";
      for (; r && o.test(r) && t !== e.length; )
        i += r, r = e[++t];
      if (i == "true" || i == "false" || i === "null") n.push({
        type: "name",
        value: i
      });
      else {
        t++;
        continue;
      }
      continue;
    }
    t++;
  }
  return n;
}, Va = (e) => {
  if (e.length === 0) return e;
  let t = e[e.length - 1];
  switch (t.type) {
    case "separator":
      return e = e.slice(0, e.length - 1), Va(e);
    case "number":
      let n = t.value[t.value.length - 1];
      if (n === "." || n === "-")
        return e = e.slice(0, e.length - 1), Va(e);
    case "string":
      let r = e[e.length - 2];
      if (r?.type === "delimiter")
        return e = e.slice(0, e.length - 1), Va(e);
      if (r?.type === "brace" && r.value === "{")
        return e = e.slice(0, e.length - 1), Va(e);
      break;
    case "delimiter":
      return e = e.slice(0, e.length - 1), Va(e);
  }
  return e;
}, cW = (e) => {
  let t = [];
  return e.map((n) => {
    n.type === "brace" && (n.value === "{" ? t.push("}") : t.splice(t.lastIndexOf("}"), 1)), n.type === "paren" && (n.value === "[" ? t.push("]") : t.splice(t.lastIndexOf("]"), 1));
  }), t.length > 0 && t.reverse().map((n) => {
    n === "}" ? e.push({
      type: "brace",
      value: "}"
    }) : n === "]" && e.push({
      type: "paren",
      value: "]"
    });
  }), e;
}, dW = (e) => {
  let t = "";
  return e.map((n) => {
    n.type === "string" ? t += '"' + n.value + '"' : t += n.value;
  }), t;
}, GM = (e) => JSON.parse(dW(cW(Va(uW(e))))), Fr, Io, Pa, gu, hf, vu, yu, pf, _u, Ws, bu, mf, gf, Ai, vf, yf, wu, jg, LT, _f, ev, tv, nv, OT, FT = "__json_buf";
function BT(e) {
  return e.type === "tool_use" || e.type === "server_tool_use" || e.type === "mcp_tool_use";
}
var fW = class By {
  constructor(t, n) {
    Fr.add(this), this.messages = [], this.receivedMessages = [], Io.set(this, void 0), Pa.set(this, null), this.controller = new AbortController(), gu.set(this, void 0), hf.set(this, () => {
    }), vu.set(this, () => {
    }), yu.set(this, void 0), pf.set(this, () => {
    }), _u.set(this, () => {
    }), Ws.set(this, {}), bu.set(this, !1), mf.set(this, !1), gf.set(this, !1), Ai.set(this, !1), vf.set(this, void 0), yf.set(this, void 0), wu.set(this, void 0), _f.set(this, (r) => {
      if (Ke(this, mf, !0, "f"), Hc(r) && (r = new ss()), r instanceof ss)
        return Ke(this, gf, !0, "f"), this._emit("abort", r);
      if (r instanceof _t) return this._emit("error", r);
      if (r instanceof Error) {
        const s = new _t(r.message);
        return s.cause = r, this._emit("error", s);
      }
      return this._emit("error", new _t(String(r)));
    }), Ke(this, gu, new Promise((r, s) => {
      Ke(this, hf, r, "f"), Ke(this, vu, s, "f");
    }), "f"), Ke(this, yu, new Promise((r, s) => {
      Ke(this, pf, r, "f"), Ke(this, _u, s, "f");
    }), "f"), ce(this, gu, "f").catch(() => {
    }), ce(this, yu, "f").catch(() => {
    }), Ke(this, Pa, t, "f"), Ke(this, wu, n?.logger ?? console, "f");
  }
  get response() {
    return ce(this, vf, "f");
  }
  get request_id() {
    return ce(this, yf, "f");
  }
  async withResponse() {
    Ke(this, Ai, !0, "f");
    const t = await ce(this, gu, "f");
    if (!t) throw new Error("Could not resolve a `Response` object");
    return {
      data: this,
      response: t,
      request_id: t.headers.get("request-id")
    };
  }
  static fromReadableStream(t) {
    const n = new By(null);
    return n._run(() => n._fromReadableStream(t)), n;
  }
  static createMessage(t, n, r, { logger: s } = {}) {
    const o = new By(n, { logger: s });
    for (const i of n.messages) o._addMessageParam(i);
    return Ke(o, Pa, {
      ...n,
      stream: !0
    }, "f"), o._run(() => o._createMessage(t, {
      ...n,
      stream: !0
    }, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), o;
  }
  _run(t) {
    t().then(() => {
      this._emitFinal(), this._emit("end");
    }, ce(this, _f, "f"));
  }
  _addMessageParam(t) {
    this.messages.push(t);
  }
  _addMessage(t, n = !0) {
    this.receivedMessages.push(t), n && this._emit("message", t);
  }
  async _createMessage(t, n, r) {
    const s = r?.signal;
    let o;
    s && (s.aborted && this.controller.abort(), o = this.controller.abort.bind(this.controller), s.addEventListener("abort", o));
    try {
      ce(this, Fr, "m", ev).call(this);
      const { response: i, data: a } = await t.create({
        ...n,
        stream: !0
      }, {
        ...r,
        signal: this.controller.signal
      }).withResponse();
      this._connected(i);
      for await (const l of a) ce(this, Fr, "m", tv).call(this, l);
      if (a.controller.signal?.aborted) throw new ss();
      ce(this, Fr, "m", nv).call(this);
    } finally {
      s && o && s.removeEventListener("abort", o);
    }
  }
  _connected(t) {
    this.ended || (Ke(this, vf, t, "f"), Ke(this, yf, t?.headers.get("request-id"), "f"), ce(this, hf, "f").call(this, t), this._emit("connect"));
  }
  get ended() {
    return ce(this, bu, "f");
  }
  get errored() {
    return ce(this, mf, "f");
  }
  get aborted() {
    return ce(this, gf, "f");
  }
  abort() {
    this.controller.abort();
  }
  on(t, n) {
    return (ce(this, Ws, "f")[t] || (ce(this, Ws, "f")[t] = [])).push({ listener: n }), this;
  }
  off(t, n) {
    const r = ce(this, Ws, "f")[t];
    if (!r) return this;
    const s = r.findIndex((o) => o.listener === n);
    return s >= 0 && r.splice(s, 1), this;
  }
  once(t, n) {
    return (ce(this, Ws, "f")[t] || (ce(this, Ws, "f")[t] = [])).push({
      listener: n,
      once: !0
    }), this;
  }
  emitted(t) {
    return new Promise((n, r) => {
      Ke(this, Ai, !0, "f"), t !== "error" && this.once("error", r), this.once(t, n);
    });
  }
  async done() {
    Ke(this, Ai, !0, "f"), await ce(this, yu, "f");
  }
  get currentMessage() {
    return ce(this, Io, "f");
  }
  async finalMessage() {
    return await this.done(), ce(this, Fr, "m", jg).call(this);
  }
  async finalText() {
    return await this.done(), ce(this, Fr, "m", LT).call(this);
  }
  _emit(t, ...n) {
    if (ce(this, bu, "f")) return;
    t === "end" && (Ke(this, bu, !0, "f"), ce(this, pf, "f").call(this));
    const r = ce(this, Ws, "f")[t];
    if (r && (ce(this, Ws, "f")[t] = r.filter((s) => !s.once), r.forEach(({ listener: s }) => s(...n))), t === "abort") {
      const s = n[0];
      !ce(this, Ai, "f") && !r?.length && Promise.reject(s), ce(this, vu, "f").call(this, s), ce(this, _u, "f").call(this, s), this._emit("end");
      return;
    }
    if (t === "error") {
      const s = n[0];
      !ce(this, Ai, "f") && !r?.length && Promise.reject(s), ce(this, vu, "f").call(this, s), ce(this, _u, "f").call(this, s), this._emit("end");
    }
  }
  _emitFinal() {
    this.receivedMessages.at(-1) && this._emit("finalMessage", ce(this, Fr, "m", jg).call(this));
  }
  async _fromReadableStream(t, n) {
    const r = n?.signal;
    let s;
    r && (r.aborted && this.controller.abort(), s = this.controller.abort.bind(this.controller), r.addEventListener("abort", s));
    try {
      ce(this, Fr, "m", ev).call(this), this._connected(null);
      const o = Gc.fromReadableStream(t, this.controller);
      for await (const i of o) ce(this, Fr, "m", tv).call(this, i);
      if (o.controller.signal?.aborted) throw new ss();
      ce(this, Fr, "m", nv).call(this);
    } finally {
      r && s && r.removeEventListener("abort", s);
    }
  }
  [(Io = /* @__PURE__ */ new WeakMap(), Pa = /* @__PURE__ */ new WeakMap(), gu = /* @__PURE__ */ new WeakMap(), hf = /* @__PURE__ */ new WeakMap(), vu = /* @__PURE__ */ new WeakMap(), yu = /* @__PURE__ */ new WeakMap(), pf = /* @__PURE__ */ new WeakMap(), _u = /* @__PURE__ */ new WeakMap(), Ws = /* @__PURE__ */ new WeakMap(), bu = /* @__PURE__ */ new WeakMap(), mf = /* @__PURE__ */ new WeakMap(), gf = /* @__PURE__ */ new WeakMap(), Ai = /* @__PURE__ */ new WeakMap(), vf = /* @__PURE__ */ new WeakMap(), yf = /* @__PURE__ */ new WeakMap(), wu = /* @__PURE__ */ new WeakMap(), _f = /* @__PURE__ */ new WeakMap(), Fr = /* @__PURE__ */ new WeakSet(), jg = function() {
    if (this.receivedMessages.length === 0) throw new _t("stream ended without producing a Message with role=assistant");
    return this.receivedMessages.at(-1);
  }, LT = function() {
    if (this.receivedMessages.length === 0) throw new _t("stream ended without producing a Message with role=assistant");
    const n = this.receivedMessages.at(-1).content.filter((r) => r.type === "text").map((r) => r.text);
    if (n.length === 0) throw new _t("stream ended without producing a content block with type=text");
    return n.join(" ");
  }, ev = function() {
    this.ended || Ke(this, Io, void 0, "f");
  }, tv = function(n) {
    if (this.ended) return;
    const r = ce(this, Fr, "m", OT).call(this, n);
    switch (this._emit("streamEvent", n, r), n.type) {
      case "content_block_delta": {
        const s = r.content.at(-1);
        switch (n.delta.type) {
          case "text_delta":
            s.type === "text" && this._emit("text", n.delta.text, s.text || "");
            break;
          case "citations_delta":
            s.type === "text" && this._emit("citation", n.delta.citation, s.citations ?? []);
            break;
          case "input_json_delta":
            BT(s) && s.input && this._emit("inputJson", n.delta.partial_json, s.input);
            break;
          case "thinking_delta":
            s.type === "thinking" && this._emit("thinking", n.delta.thinking, s.thinking);
            break;
          case "signature_delta":
            s.type === "thinking" && this._emit("signature", s.signature);
            break;
          case "compaction_delta":
            s.type === "compaction" && s.content && this._emit("compaction", s.content);
            break;
          default:
            n.delta;
        }
        break;
      }
      case "message_stop":
        this._addMessageParam(r), this._addMessage(DT(r, ce(this, Pa, "f"), { logger: ce(this, wu, "f") }), !0);
        break;
      case "content_block_stop":
        this._emit("contentBlock", r.content.at(-1));
        break;
      case "message_start":
        Ke(this, Io, r, "f");
        break;
      case "content_block_start":
      case "message_delta":
        break;
    }
  }, nv = function() {
    if (this.ended) throw new _t("stream has ended, this shouldn't happen");
    const n = ce(this, Io, "f");
    if (!n) throw new _t("request ended without sending any chunks");
    return Ke(this, Io, void 0, "f"), DT(n, ce(this, Pa, "f"), { logger: ce(this, wu, "f") });
  }, OT = function(n) {
    let r = ce(this, Io, "f");
    if (n.type === "message_start") {
      if (r) throw new _t(`Unexpected event order, got ${n.type} before receiving "message_stop"`);
      return n.message;
    }
    if (!r) throw new _t(`Unexpected event order, got ${n.type} before "message_start"`);
    switch (n.type) {
      case "message_stop":
        return r;
      case "message_delta":
        return r.container = n.delta.container, r.stop_reason = n.delta.stop_reason, r.stop_sequence = n.delta.stop_sequence, r.usage.output_tokens = n.usage.output_tokens, r.context_management = n.context_management, n.usage.input_tokens != null && (r.usage.input_tokens = n.usage.input_tokens), n.usage.cache_creation_input_tokens != null && (r.usage.cache_creation_input_tokens = n.usage.cache_creation_input_tokens), n.usage.cache_read_input_tokens != null && (r.usage.cache_read_input_tokens = n.usage.cache_read_input_tokens), n.usage.server_tool_use != null && (r.usage.server_tool_use = n.usage.server_tool_use), n.usage.iterations != null && (r.usage.iterations = n.usage.iterations), r;
      case "content_block_start":
        return r.content.push(n.content_block), r;
      case "content_block_delta": {
        const s = r.content.at(n.index);
        switch (n.delta.type) {
          case "text_delta":
            s?.type === "text" && (r.content[n.index] = {
              ...s,
              text: (s.text || "") + n.delta.text
            });
            break;
          case "citations_delta":
            s?.type === "text" && (r.content[n.index] = {
              ...s,
              citations: [...s.citations ?? [], n.delta.citation]
            });
            break;
          case "input_json_delta":
            if (s && BT(s)) {
              let o = s[FT] || "";
              o += n.delta.partial_json;
              const i = { ...s };
              if (Object.defineProperty(i, FT, {
                value: o,
                enumerable: !1,
                writable: !0
              }), o) try {
                i.input = GM(o);
              } catch (a) {
                const l = new _t(`Unable to parse tool parameter JSON from model. Please retry your request or adjust your prompt. Error: ${a}. JSON: ${o}`);
                ce(this, _f, "f").call(this, l);
              }
              r.content[n.index] = i;
            }
            break;
          case "thinking_delta":
            s?.type === "thinking" && (r.content[n.index] = {
              ...s,
              thinking: s.thinking + n.delta.thinking
            });
            break;
          case "signature_delta":
            s?.type === "thinking" && (r.content[n.index] = {
              ...s,
              signature: n.delta.signature
            });
            break;
          case "compaction_delta":
            s?.type === "compaction" && (r.content[n.index] = {
              ...s,
              content: (s.content || "") + n.delta.content
            });
            break;
          default:
            n.delta;
        }
        return r;
      }
      case "content_block_stop":
        return r;
    }
  }, Symbol.asyncIterator)]() {
    const t = [], n = [];
    let r = !1;
    return this.on("streamEvent", (s) => {
      const o = n.shift();
      o ? o.resolve(s) : t.push(s);
    }), this.on("end", () => {
      r = !0;
      for (const s of n) s.resolve(void 0);
      n.length = 0;
    }), this.on("abort", (s) => {
      r = !0;
      for (const o of n) o.reject(s);
      n.length = 0;
    }), this.on("error", (s) => {
      r = !0;
      for (const o of n) o.reject(s);
      n.length = 0;
    }), {
      next: async () => t.length ? {
        value: t.shift(),
        done: !1
      } : r ? {
        value: void 0,
        done: !0
      } : new Promise((s, o) => n.push({
        resolve: s,
        reject: o
      })).then((s) => s ? {
        value: s,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  toReadableStream() {
    return new Gc(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
  }
};
var WM = class extends Error {
  constructor(e) {
    const t = typeof e == "string" ? e : e.map((n) => n.type === "text" ? n.text : `[${n.type}]`).join(" ");
    super(t), this.name = "ToolError", this.content = e;
  }
};
var hW = `You have been working on the task described above but have not yet completed it. Write a continuation summary that will allow you (or another instance of yourself) to resume work efficiently in a future context window where the conversation history will be replaced with this summary. Your summary should be structured, concise, and actionable. Include:
1. Task Overview
The user's core request and success criteria
Any clarifications or constraints they specified
2. Current State
What has been completed so far
Files created, modified, or analyzed (with paths if relevant)
Key outputs or artifacts produced
3. Important Discoveries
Technical constraints or requirements uncovered
Decisions made and their rationale
Errors encountered and how they were resolved
What approaches were tried that didn't work (and why)
4. Next Steps
Specific actions needed to complete the task
Any blockers or open questions to resolve
Priority order if multiple steps remain
5. Context to Preserve
User preferences or style requirements
Domain-specific details that aren't obvious
Any promises made to the user
Be concise but complete—err on the side of including information that would prevent duplicate work or repeated mistakes. Write in a way that enables immediate resumption of the task.
Wrap your summary in <summary></summary> tags.`, Su, Ra, Ii, cn, jn, vr, so, xo, Tu, UT, Uy;
function qT() {
  let e, t;
  return {
    promise: new Promise((n, r) => {
      e = n, t = r;
    }),
    resolve: e,
    reject: t
  };
}
var VM = class {
  constructor(e, t, n) {
    Su.add(this), this.client = e, Ra.set(this, !1), Ii.set(this, !1), cn.set(this, void 0), jn.set(this, void 0), vr.set(this, void 0), so.set(this, void 0), xo.set(this, void 0), Tu.set(this, 0), Ke(this, cn, { params: {
      ...t,
      messages: structuredClone(t.messages)
    } }, "f");
    const r = ["BetaToolRunner", ...DM(t.tools, t.messages)].join(", ");
    Ke(this, jn, {
      ...n,
      headers: qe([{ "x-stainless-helper": r }, n?.headers])
    }, "f"), Ke(this, xo, qT(), "f"), t.compactionControl?.enabled && console.warn('Anthropic: The `compactionControl` parameter is deprecated and will be removed in a future version. Use server-side compaction instead by passing `edits: [{ type: "compact_20260112" }]` in the params passed to `toolRunner()`. See https://platform.claude.com/docs/en/build-with-claude/compaction');
  }
  async *[(Ra = /* @__PURE__ */ new WeakMap(), Ii = /* @__PURE__ */ new WeakMap(), cn = /* @__PURE__ */ new WeakMap(), jn = /* @__PURE__ */ new WeakMap(), vr = /* @__PURE__ */ new WeakMap(), so = /* @__PURE__ */ new WeakMap(), xo = /* @__PURE__ */ new WeakMap(), Tu = /* @__PURE__ */ new WeakMap(), Su = /* @__PURE__ */ new WeakSet(), UT = async function() {
    const t = ce(this, cn, "f").params.compactionControl;
    if (!t || !t.enabled) return !1;
    let n = 0;
    if (ce(this, vr, "f") !== void 0) try {
      const l = await ce(this, vr, "f");
      n = l.usage.input_tokens + (l.usage.cache_creation_input_tokens ?? 0) + (l.usage.cache_read_input_tokens ?? 0) + l.usage.output_tokens;
    } catch {
      return !1;
    }
    const r = t.contextTokenThreshold ?? 1e5;
    if (n < r) return !1;
    const s = t.model ?? ce(this, cn, "f").params.model, o = t.summaryPrompt ?? hW, i = ce(this, cn, "f").params.messages;
    if (i[i.length - 1].role === "assistant") {
      const l = i[i.length - 1];
      if (Array.isArray(l.content)) {
        const u = l.content.filter((d) => d.type !== "tool_use");
        u.length === 0 ? i.pop() : l.content = u;
      }
    }
    const a = await this.client.beta.messages.create({
      model: s,
      messages: [...i, {
        role: "user",
        content: [{
          type: "text",
          text: o
        }]
      }],
      max_tokens: ce(this, cn, "f").params.max_tokens
    }, {
      signal: ce(this, jn, "f").signal,
      headers: qe([ce(this, jn, "f").headers, { "x-stainless-helper": "compaction" }])
    });
    if (a.content[0]?.type !== "text") throw new _t("Expected text response for compaction");
    return ce(this, cn, "f").params.messages = [{
      role: "user",
      content: a.content
    }], !0;
  }, Symbol.asyncIterator)]() {
    var e;
    if (ce(this, Ra, "f")) throw new _t("Cannot iterate over a consumed stream");
    Ke(this, Ra, !0, "f"), Ke(this, Ii, !0, "f"), Ke(this, so, void 0, "f");
    try {
      for (; ; ) {
        let t;
        try {
          if (ce(this, cn, "f").params.max_iterations && ce(this, Tu, "f") >= ce(this, cn, "f").params.max_iterations) break;
          Ke(this, Ii, !1, "f"), Ke(this, so, void 0, "f"), Ke(this, Tu, (e = ce(this, Tu, "f"), e++, e), "f"), Ke(this, vr, void 0, "f");
          const { max_iterations: n, compactionControl: r, ...s } = ce(this, cn, "f").params;
          if (s.stream ? (t = this.client.beta.messages.stream({ ...s }, ce(this, jn, "f")), Ke(this, vr, t.finalMessage(), "f"), ce(this, vr, "f").catch(() => {
          }), yield t) : (Ke(this, vr, this.client.beta.messages.create({
            ...s,
            stream: !1
          }, ce(this, jn, "f")), "f"), yield ce(this, vr, "f")), !await ce(this, Su, "m", UT).call(this)) {
            if (!ce(this, Ii, "f")) {
              const { role: i, content: a } = await ce(this, vr, "f");
              ce(this, cn, "f").params.messages.push({
                role: i,
                content: a
              });
            }
            const o = await ce(this, Su, "m", Uy).call(this, ce(this, cn, "f").params.messages.at(-1));
            if (o) ce(this, cn, "f").params.messages.push(o);
            else if (!ce(this, Ii, "f")) break;
          }
        } finally {
          t && t.abort();
        }
      }
      if (!ce(this, vr, "f")) throw new _t("ToolRunner concluded without a message from the server");
      ce(this, xo, "f").resolve(await ce(this, vr, "f"));
    } catch (t) {
      throw Ke(this, Ra, !1, "f"), ce(this, xo, "f").promise.catch(() => {
      }), ce(this, xo, "f").reject(t), Ke(this, xo, qT(), "f"), t;
    }
  }
  setMessagesParams(e) {
    typeof e == "function" ? ce(this, cn, "f").params = e(ce(this, cn, "f").params) : ce(this, cn, "f").params = e, Ke(this, Ii, !0, "f"), Ke(this, so, void 0, "f");
  }
  setRequestOptions(e) {
    typeof e == "function" ? Ke(this, jn, e(ce(this, jn, "f")), "f") : Ke(this, jn, {
      ...ce(this, jn, "f"),
      ...e
    }, "f");
  }
  async generateToolResponse(e = ce(this, jn, "f").signal) {
    const t = await ce(this, vr, "f") ?? this.params.messages.at(-1);
    return t ? ce(this, Su, "m", Uy).call(this, t, e) : null;
  }
  done() {
    return ce(this, xo, "f").promise;
  }
  async runUntilDone() {
    if (!ce(this, Ra, "f")) for await (const e of this) ;
    return this.done();
  }
  get params() {
    return ce(this, cn, "f").params;
  }
  pushMessages(...e) {
    this.setMessagesParams((t) => ({
      ...t,
      messages: [...t.messages, ...e]
    }));
  }
  then(e, t) {
    return this.runUntilDone().then(e, t);
  }
};
Uy = async function(t, n = ce(this, jn, "f").signal) {
  return ce(this, so, "f") !== void 0 ? ce(this, so, "f") : (Ke(this, so, pW(ce(this, cn, "f").params, t, {
    ...ce(this, jn, "f"),
    signal: n
  }), "f"), ce(this, so, "f"));
};
async function pW(e, t = e.messages.at(-1), n) {
  if (!t || t.role !== "assistant" || !t.content || typeof t.content == "string") return null;
  const r = t.content.filter((s) => s.type === "tool_use");
  return r.length === 0 ? null : {
    role: "user",
    content: await Promise.all(r.map(async (s) => {
      const o = e.tools.find((i) => ("name" in i ? i.name : i.mcp_server_name) === s.name);
      if (!o || !("run" in o)) return {
        type: "tool_result",
        tool_use_id: s.id,
        content: `Error: Tool '${s.name}' not found`,
        is_error: !0
      };
      try {
        let i = s.input;
        "parse" in o && o.parse && (i = o.parse(i));
        const a = await o.run(i, {
          toolUseBlock: s,
          signal: n?.signal
        });
        return {
          type: "tool_result",
          tool_use_id: s.id,
          content: a
        };
      } catch (i) {
        return {
          type: "tool_result",
          tool_use_id: s.id,
          content: i instanceof WM ? i.content : `Error: ${i instanceof Error ? i.message : String(i)}`,
          is_error: !0
        };
      }
    }))
  };
}
var KM = class zM {
  constructor(t, n) {
    this.iterator = t, this.controller = n;
  }
  async *decoder() {
    const t = new ad();
    for await (const n of this.iterator) for (const r of t.decode(n)) yield JSON.parse(r);
    for (const n of t.flush()) yield JSON.parse(n);
  }
  [Symbol.asyncIterator]() {
    return this.decoder();
  }
  static fromResponse(t, n) {
    if (!t.body)
      throw n.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new _t("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new _t("Attempted to iterate over a response with no body");
    return new zM(sw(t.body), n);
  }
}, JM = class extends hn {
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/messages/batches?beta=true", {
      body: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "message-batches-2024-09-24"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(pt`/v1/messages/batches/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "message-batches-2024-09-24"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/messages/batches?beta=true", ld, {
      query: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "message-batches-2024-09-24"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(pt`/v1/messages/batches/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "message-batches-2024-09-24"].toString() }, n?.headers])
    });
  }
  cancel(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(pt`/v1/messages/batches/${e}/cancel?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "message-batches-2024-09-24"].toString() }, n?.headers])
    });
  }
  async results(e, t = {}, n) {
    const r = await this.retrieve(e);
    if (!r.results_url) throw new _t(`No batch \`results_url\`; Has it finished processing? ${r.processing_status} - ${r.id}`);
    const { betas: s } = t ?? {};
    return this._client.get(r.results_url, {
      ...n,
      headers: qe([{
        "anthropic-beta": [...s ?? [], "message-batches-2024-09-24"].toString(),
        Accept: "application/binary"
      }, n?.headers]),
      stream: !0,
      __binaryResponse: !0
    })._thenUnwrap((o, i) => KM.fromResponse(i.response, i.controller));
  }
}, HT = {
  "claude-1.3": "November 6th, 2024",
  "claude-1.3-100k": "November 6th, 2024",
  "claude-instant-1.1": "November 6th, 2024",
  "claude-instant-1.1-100k": "November 6th, 2024",
  "claude-instant-1.2": "November 6th, 2024",
  "claude-3-sonnet-20240229": "July 21st, 2025",
  "claude-3-opus-20240229": "January 5th, 2026",
  "claude-2.1": "July 21st, 2025",
  "claude-2.0": "July 21st, 2025",
  "claude-3-7-sonnet-latest": "February 19th, 2026",
  "claude-3-7-sonnet-20250219": "February 19th, 2026"
}, mW = ["claude-opus-4-6"], ud = class extends hn {
  constructor() {
    super(...arguments), this.batches = new JM(this._client);
  }
  create(e, t) {
    const n = GT(e), { betas: r, ...s } = n;
    s.model in HT && console.warn(`The model '${s.model}' is deprecated and will reach end-of-life on ${HT[s.model]}
Please migrate to a newer model. Visit https://docs.anthropic.com/en/docs/resources/model-deprecations for more information.`), s.model in mW && s.thinking && s.thinking.type === "enabled" && console.warn(`Using Claude with ${s.model} and 'thinking.type=enabled' is deprecated. Use 'thinking.type=adaptive' instead which results in better model performance in our testing: https://platform.claude.com/docs/en/build-with-claude/adaptive-thinking`);
    let o = this._client._options.timeout;
    if (!s.stream && o == null) {
      const a = UM[s.model] ?? void 0;
      o = this._client.calculateNonstreamingTimeout(s.max_tokens, a);
    }
    const i = LM(s.tools, s.messages);
    return this._client.post("/v1/messages?beta=true", {
      body: s,
      timeout: o ?? 6e5,
      ...t,
      headers: qe([
        { ...r?.toString() != null ? { "anthropic-beta": r?.toString() } : void 0 },
        i,
        t?.headers
      ]),
      stream: n.stream ?? !1
    });
  }
  parse(e, t) {
    return t = {
      ...t,
      headers: qe([{ "anthropic-beta": [...e.betas ?? [], "structured-outputs-2025-12-15"].toString() }, t?.headers])
    }, this.create(e, t).then((n) => HM(n, e, { logger: this._client.logger ?? console }));
  }
  stream(e, t) {
    return fW.createMessage(this, e, t);
  }
  countTokens(e, t) {
    const { betas: n, ...r } = GT(e);
    return this._client.post("/v1/messages/count_tokens?beta=true", {
      body: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "token-counting-2024-11-01"].toString() }, t?.headers])
    });
  }
  toolRunner(e, t) {
    return new VM(this._client, e, t);
  }
};
function GT(e) {
  if (!e.output_format) return e;
  if (e.output_config?.format) throw new _t("Both output_format and output_config.format were provided. Please use only output_config.format (output_format is deprecated).");
  const { output_format: t, ...n } = e;
  return {
    ...n,
    output_config: {
      ...e.output_config,
      format: t
    }
  };
}
ud.Batches = JM;
ud.BetaToolRunner = VM;
ud.ToolError = WM;
var YM = class extends hn {
  list(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.getAPIList(pt`/v1/sessions/${e}/events?beta=true`, Ls, {
      query: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  send(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(pt`/v1/sessions/${e}/events?beta=true`, {
      body: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  stream(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(pt`/v1/sessions/${e}/events/stream?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers]),
      stream: !0
    });
  }
}, XM = class extends hn {
  retrieve(e, t, n) {
    const { session_id: r, betas: s } = t;
    return this._client.get(pt`/v1/sessions/${r}/resources/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { session_id: r, betas: s, ...o } = t;
    return this._client.post(pt`/v1/sessions/${r}/resources/${e}?beta=true`, {
      body: o,
      ...n,
      headers: qe([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.getAPIList(pt`/v1/sessions/${e}/resources?beta=true`, Ls, {
      query: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { session_id: r, betas: s } = t;
    return this._client.delete(pt`/v1/sessions/${r}/resources/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  add(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(pt`/v1/sessions/${e}/resources?beta=true`, {
      body: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, jp = class extends hn {
  constructor() {
    super(...arguments), this.events = new YM(this._client), this.resources = new XM(this._client);
  }
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/sessions?beta=true", {
      body: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(pt`/v1/sessions/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(pt`/v1/sessions/${e}?beta=true`, {
      body: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/sessions?beta=true", Ls, {
      query: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(pt`/v1/sessions/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  archive(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(pt`/v1/sessions/${e}/archive?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
};
jp.Events = YM;
jp.Resources = XM;
var QM = class extends hn {
  create(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.post(pt`/v1/skills/${e}/versions?beta=true`, iw({
      body: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    }, this._client));
  }
  retrieve(e, t, n) {
    const { skill_id: r, betas: s } = t;
    return this._client.get(pt`/v1/skills/${r}/versions/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...s ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.getAPIList(pt`/v1/skills/${e}/versions?beta=true`, Ls, {
      query: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { skill_id: r, betas: s } = t;
    return this._client.delete(pt`/v1/skills/${r}/versions/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...s ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
}, lw = class extends hn {
  constructor() {
    super(...arguments), this.versions = new QM(this._client);
  }
  create(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.post("/v1/skills?beta=true", iw({
      body: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "skills-2025-10-02"].toString() }, t?.headers])
    }, this._client, !1));
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(pt`/v1/skills/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/skills?beta=true", Ls, {
      query: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "skills-2025-10-02"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(pt`/v1/skills/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "skills-2025-10-02"].toString() }, n?.headers])
    });
  }
};
lw.Versions = QM;
var ZM = class extends hn {
  create(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(pt`/v1/vaults/${e}/credentials?beta=true`, {
      body: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  retrieve(e, t, n) {
    const { vault_id: r, betas: s } = t;
    return this._client.get(pt`/v1/vaults/${r}/credentials/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { vault_id: r, betas: s, ...o } = t;
    return this._client.post(pt`/v1/vaults/${r}/credentials/${e}?beta=true`, {
      body: o,
      ...n,
      headers: qe([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    const { betas: r, ...s } = t ?? {};
    return this._client.getAPIList(pt`/v1/vaults/${e}/credentials?beta=true`, Ls, {
      query: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { vault_id: r, betas: s } = t;
    return this._client.delete(pt`/v1/vaults/${r}/credentials/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  archive(e, t, n) {
    const { vault_id: r, betas: s } = t;
    return this._client.post(pt`/v1/vaults/${r}/credentials/${e}/archive?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...s ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
}, uw = class extends hn {
  constructor() {
    super(...arguments), this.credentials = new ZM(this._client);
  }
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/vaults?beta=true", {
      body: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(pt`/v1/vaults/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  update(e, t, n) {
    const { betas: r, ...s } = t;
    return this._client.post(pt`/v1/vaults/${e}?beta=true`, {
      body: s,
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/vaults?beta=true", Ls, {
      query: r,
      ...t,
      headers: qe([{ "anthropic-beta": [...n ?? [], "managed-agents-2026-04-01"].toString() }, t?.headers])
    });
  }
  delete(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.delete(pt`/v1/vaults/${e}?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
  archive(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.post(pt`/v1/vaults/${e}/archive?beta=true`, {
      ...n,
      headers: qe([{ "anthropic-beta": [...r ?? [], "managed-agents-2026-04-01"].toString() }, n?.headers])
    });
  }
};
uw.Credentials = ZM;
var Os = class extends hn {
  constructor() {
    super(...arguments), this.models = new FM(this._client), this.messages = new ud(this._client), this.agents = new aw(this._client), this.environments = new $M(this._client), this.sessions = new jp(this._client), this.vaults = new uw(this._client), this.files = new OM(this._client), this.skills = new lw(this._client);
  }
};
Os.Models = FM;
Os.Messages = ud;
Os.Agents = aw;
Os.Environments = $M;
Os.Sessions = jp;
Os.Vaults = uw;
Os.Files = OM;
Os.Skills = lw;
var jM = class extends hn {
  create(e, t) {
    const { betas: n, ...r } = e;
    return this._client.post("/v1/complete", {
      body: r,
      timeout: this._client._options.timeout ?? 6e5,
      ...t,
      headers: qe([{ ...n?.toString() != null ? { "anthropic-beta": n?.toString() } : void 0 }, t?.headers]),
      stream: e.stream ?? !1
    });
  }
};
function eP(e) {
  return e?.output_config?.format;
}
function WT(e, t, n) {
  const r = eP(t);
  return !t || !("parse" in (r ?? {})) ? {
    ...e,
    content: e.content.map((s) => s.type === "text" ? Object.defineProperty({ ...s }, "parsed_output", {
      value: null,
      enumerable: !1
    }) : s),
    parsed_output: null
  } : tP(e, t, n);
}
function tP(e, t, n) {
  let r = null;
  const s = e.content.map((o) => {
    if (o.type === "text") {
      const i = gW(t, o.text);
      return r === null && (r = i), Object.defineProperty({ ...o }, "parsed_output", {
        value: i,
        enumerable: !1
      });
    }
    return o;
  });
  return {
    ...e,
    content: s,
    parsed_output: r
  };
}
function gW(e, t) {
  const n = eP(e);
  if (n?.type !== "json_schema") return null;
  try {
    return "parse" in n ? n.parse(t) : JSON.parse(t);
  } catch (r) {
    throw new _t(`Failed to parse structured output: ${r}`);
  }
}
var Br, ko, Na, Eu, bf, Cu, Au, wf, Iu, Vs, xu, Sf, Tf, xi, Ef, Cf, ku, rv, VT, sv, ov, iv, av, KT, zT = "__json_buf";
function JT(e) {
  return e.type === "tool_use" || e.type === "server_tool_use";
}
var vW = class qy {
  constructor(t, n) {
    Br.add(this), this.messages = [], this.receivedMessages = [], ko.set(this, void 0), Na.set(this, null), this.controller = new AbortController(), Eu.set(this, void 0), bf.set(this, () => {
    }), Cu.set(this, () => {
    }), Au.set(this, void 0), wf.set(this, () => {
    }), Iu.set(this, () => {
    }), Vs.set(this, {}), xu.set(this, !1), Sf.set(this, !1), Tf.set(this, !1), xi.set(this, !1), Ef.set(this, void 0), Cf.set(this, void 0), ku.set(this, void 0), sv.set(this, (r) => {
      if (Ke(this, Sf, !0, "f"), Hc(r) && (r = new ss()), r instanceof ss)
        return Ke(this, Tf, !0, "f"), this._emit("abort", r);
      if (r instanceof _t) return this._emit("error", r);
      if (r instanceof Error) {
        const s = new _t(r.message);
        return s.cause = r, this._emit("error", s);
      }
      return this._emit("error", new _t(String(r)));
    }), Ke(this, Eu, new Promise((r, s) => {
      Ke(this, bf, r, "f"), Ke(this, Cu, s, "f");
    }), "f"), Ke(this, Au, new Promise((r, s) => {
      Ke(this, wf, r, "f"), Ke(this, Iu, s, "f");
    }), "f"), ce(this, Eu, "f").catch(() => {
    }), ce(this, Au, "f").catch(() => {
    }), Ke(this, Na, t, "f"), Ke(this, ku, n?.logger ?? console, "f");
  }
  get response() {
    return ce(this, Ef, "f");
  }
  get request_id() {
    return ce(this, Cf, "f");
  }
  async withResponse() {
    Ke(this, xi, !0, "f");
    const t = await ce(this, Eu, "f");
    if (!t) throw new Error("Could not resolve a `Response` object");
    return {
      data: this,
      response: t,
      request_id: t.headers.get("request-id")
    };
  }
  static fromReadableStream(t) {
    const n = new qy(null);
    return n._run(() => n._fromReadableStream(t)), n;
  }
  static createMessage(t, n, r, { logger: s } = {}) {
    const o = new qy(n, { logger: s });
    for (const i of n.messages) o._addMessageParam(i);
    return Ke(o, Na, {
      ...n,
      stream: !0
    }, "f"), o._run(() => o._createMessage(t, {
      ...n,
      stream: !0
    }, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), o;
  }
  _run(t) {
    t().then(() => {
      this._emitFinal(), this._emit("end");
    }, ce(this, sv, "f"));
  }
  _addMessageParam(t) {
    this.messages.push(t);
  }
  _addMessage(t, n = !0) {
    this.receivedMessages.push(t), n && this._emit("message", t);
  }
  async _createMessage(t, n, r) {
    const s = r?.signal;
    let o;
    s && (s.aborted && this.controller.abort(), o = this.controller.abort.bind(this.controller), s.addEventListener("abort", o));
    try {
      ce(this, Br, "m", ov).call(this);
      const { response: i, data: a } = await t.create({
        ...n,
        stream: !0
      }, {
        ...r,
        signal: this.controller.signal
      }).withResponse();
      this._connected(i);
      for await (const l of a) ce(this, Br, "m", iv).call(this, l);
      if (a.controller.signal?.aborted) throw new ss();
      ce(this, Br, "m", av).call(this);
    } finally {
      s && o && s.removeEventListener("abort", o);
    }
  }
  _connected(t) {
    this.ended || (Ke(this, Ef, t, "f"), Ke(this, Cf, t?.headers.get("request-id"), "f"), ce(this, bf, "f").call(this, t), this._emit("connect"));
  }
  get ended() {
    return ce(this, xu, "f");
  }
  get errored() {
    return ce(this, Sf, "f");
  }
  get aborted() {
    return ce(this, Tf, "f");
  }
  abort() {
    this.controller.abort();
  }
  on(t, n) {
    return (ce(this, Vs, "f")[t] || (ce(this, Vs, "f")[t] = [])).push({ listener: n }), this;
  }
  off(t, n) {
    const r = ce(this, Vs, "f")[t];
    if (!r) return this;
    const s = r.findIndex((o) => o.listener === n);
    return s >= 0 && r.splice(s, 1), this;
  }
  once(t, n) {
    return (ce(this, Vs, "f")[t] || (ce(this, Vs, "f")[t] = [])).push({
      listener: n,
      once: !0
    }), this;
  }
  emitted(t) {
    return new Promise((n, r) => {
      Ke(this, xi, !0, "f"), t !== "error" && this.once("error", r), this.once(t, n);
    });
  }
  async done() {
    Ke(this, xi, !0, "f"), await ce(this, Au, "f");
  }
  get currentMessage() {
    return ce(this, ko, "f");
  }
  async finalMessage() {
    return await this.done(), ce(this, Br, "m", rv).call(this);
  }
  async finalText() {
    return await this.done(), ce(this, Br, "m", VT).call(this);
  }
  _emit(t, ...n) {
    if (ce(this, xu, "f")) return;
    t === "end" && (Ke(this, xu, !0, "f"), ce(this, wf, "f").call(this));
    const r = ce(this, Vs, "f")[t];
    if (r && (ce(this, Vs, "f")[t] = r.filter((s) => !s.once), r.forEach(({ listener: s }) => s(...n))), t === "abort") {
      const s = n[0];
      !ce(this, xi, "f") && !r?.length && Promise.reject(s), ce(this, Cu, "f").call(this, s), ce(this, Iu, "f").call(this, s), this._emit("end");
      return;
    }
    if (t === "error") {
      const s = n[0];
      !ce(this, xi, "f") && !r?.length && Promise.reject(s), ce(this, Cu, "f").call(this, s), ce(this, Iu, "f").call(this, s), this._emit("end");
    }
  }
  _emitFinal() {
    this.receivedMessages.at(-1) && this._emit("finalMessage", ce(this, Br, "m", rv).call(this));
  }
  async _fromReadableStream(t, n) {
    const r = n?.signal;
    let s;
    r && (r.aborted && this.controller.abort(), s = this.controller.abort.bind(this.controller), r.addEventListener("abort", s));
    try {
      ce(this, Br, "m", ov).call(this), this._connected(null);
      const o = Gc.fromReadableStream(t, this.controller);
      for await (const i of o) ce(this, Br, "m", iv).call(this, i);
      if (o.controller.signal?.aborted) throw new ss();
      ce(this, Br, "m", av).call(this);
    } finally {
      r && s && r.removeEventListener("abort", s);
    }
  }
  [(ko = /* @__PURE__ */ new WeakMap(), Na = /* @__PURE__ */ new WeakMap(), Eu = /* @__PURE__ */ new WeakMap(), bf = /* @__PURE__ */ new WeakMap(), Cu = /* @__PURE__ */ new WeakMap(), Au = /* @__PURE__ */ new WeakMap(), wf = /* @__PURE__ */ new WeakMap(), Iu = /* @__PURE__ */ new WeakMap(), Vs = /* @__PURE__ */ new WeakMap(), xu = /* @__PURE__ */ new WeakMap(), Sf = /* @__PURE__ */ new WeakMap(), Tf = /* @__PURE__ */ new WeakMap(), xi = /* @__PURE__ */ new WeakMap(), Ef = /* @__PURE__ */ new WeakMap(), Cf = /* @__PURE__ */ new WeakMap(), ku = /* @__PURE__ */ new WeakMap(), sv = /* @__PURE__ */ new WeakMap(), Br = /* @__PURE__ */ new WeakSet(), rv = function() {
    if (this.receivedMessages.length === 0) throw new _t("stream ended without producing a Message with role=assistant");
    return this.receivedMessages.at(-1);
  }, VT = function() {
    if (this.receivedMessages.length === 0) throw new _t("stream ended without producing a Message with role=assistant");
    const n = this.receivedMessages.at(-1).content.filter((r) => r.type === "text").map((r) => r.text);
    if (n.length === 0) throw new _t("stream ended without producing a content block with type=text");
    return n.join(" ");
  }, ov = function() {
    this.ended || Ke(this, ko, void 0, "f");
  }, iv = function(n) {
    if (this.ended) return;
    const r = ce(this, Br, "m", KT).call(this, n);
    switch (this._emit("streamEvent", n, r), n.type) {
      case "content_block_delta": {
        const s = r.content.at(-1);
        switch (n.delta.type) {
          case "text_delta":
            s.type === "text" && this._emit("text", n.delta.text, s.text || "");
            break;
          case "citations_delta":
            s.type === "text" && this._emit("citation", n.delta.citation, s.citations ?? []);
            break;
          case "input_json_delta":
            JT(s) && s.input && this._emit("inputJson", n.delta.partial_json, s.input);
            break;
          case "thinking_delta":
            s.type === "thinking" && this._emit("thinking", n.delta.thinking, s.thinking);
            break;
          case "signature_delta":
            s.type === "thinking" && this._emit("signature", s.signature);
            break;
          default:
            n.delta;
        }
        break;
      }
      case "message_stop":
        this._addMessageParam(r), this._addMessage(WT(r, ce(this, Na, "f"), { logger: ce(this, ku, "f") }), !0);
        break;
      case "content_block_stop":
        this._emit("contentBlock", r.content.at(-1));
        break;
      case "message_start":
        Ke(this, ko, r, "f");
        break;
      case "content_block_start":
      case "message_delta":
        break;
    }
  }, av = function() {
    if (this.ended) throw new _t("stream has ended, this shouldn't happen");
    const n = ce(this, ko, "f");
    if (!n) throw new _t("request ended without sending any chunks");
    return Ke(this, ko, void 0, "f"), WT(n, ce(this, Na, "f"), { logger: ce(this, ku, "f") });
  }, KT = function(n) {
    let r = ce(this, ko, "f");
    if (n.type === "message_start") {
      if (r) throw new _t(`Unexpected event order, got ${n.type} before receiving "message_stop"`);
      return n.message;
    }
    if (!r) throw new _t(`Unexpected event order, got ${n.type} before "message_start"`);
    switch (n.type) {
      case "message_stop":
        return r;
      case "message_delta":
        return r.stop_reason = n.delta.stop_reason, r.stop_sequence = n.delta.stop_sequence, r.usage.output_tokens = n.usage.output_tokens, n.usage.input_tokens != null && (r.usage.input_tokens = n.usage.input_tokens), n.usage.cache_creation_input_tokens != null && (r.usage.cache_creation_input_tokens = n.usage.cache_creation_input_tokens), n.usage.cache_read_input_tokens != null && (r.usage.cache_read_input_tokens = n.usage.cache_read_input_tokens), n.usage.server_tool_use != null && (r.usage.server_tool_use = n.usage.server_tool_use), r;
      case "content_block_start":
        return r.content.push({ ...n.content_block }), r;
      case "content_block_delta": {
        const s = r.content.at(n.index);
        switch (n.delta.type) {
          case "text_delta":
            s?.type === "text" && (r.content[n.index] = {
              ...s,
              text: (s.text || "") + n.delta.text
            });
            break;
          case "citations_delta":
            s?.type === "text" && (r.content[n.index] = {
              ...s,
              citations: [...s.citations ?? [], n.delta.citation]
            });
            break;
          case "input_json_delta":
            if (s && JT(s)) {
              let o = s[zT] || "";
              o += n.delta.partial_json;
              const i = { ...s };
              Object.defineProperty(i, zT, {
                value: o,
                enumerable: !1,
                writable: !0
              }), o && (i.input = GM(o)), r.content[n.index] = i;
            }
            break;
          case "thinking_delta":
            s?.type === "thinking" && (r.content[n.index] = {
              ...s,
              thinking: s.thinking + n.delta.thinking
            });
            break;
          case "signature_delta":
            s?.type === "thinking" && (r.content[n.index] = {
              ...s,
              signature: n.delta.signature
            });
            break;
          default:
            n.delta;
        }
        return r;
      }
      case "content_block_stop":
        return r;
    }
  }, Symbol.asyncIterator)]() {
    const t = [], n = [];
    let r = !1;
    return this.on("streamEvent", (s) => {
      const o = n.shift();
      o ? o.resolve(s) : t.push(s);
    }), this.on("end", () => {
      r = !0;
      for (const s of n) s.resolve(void 0);
      n.length = 0;
    }), this.on("abort", (s) => {
      r = !0;
      for (const o of n) o.reject(s);
      n.length = 0;
    }), this.on("error", (s) => {
      r = !0;
      for (const o of n) o.reject(s);
      n.length = 0;
    }), {
      next: async () => t.length ? {
        value: t.shift(),
        done: !1
      } : r ? {
        value: void 0,
        done: !0
      } : new Promise((s, o) => n.push({
        resolve: s,
        reject: o
      })).then((s) => s ? {
        value: s,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  toReadableStream() {
    return new Gc(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
  }
};
var nP = class extends hn {
  create(e, t) {
    return this._client.post("/v1/messages/batches", {
      body: e,
      ...t
    });
  }
  retrieve(e, t) {
    return this._client.get(pt`/v1/messages/batches/${e}`, t);
  }
  list(e = {}, t) {
    return this._client.getAPIList("/v1/messages/batches", ld, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(pt`/v1/messages/batches/${e}`, t);
  }
  cancel(e, t) {
    return this._client.post(pt`/v1/messages/batches/${e}/cancel`, t);
  }
  async results(e, t) {
    const n = await this.retrieve(e);
    if (!n.results_url) throw new _t(`No batch \`results_url\`; Has it finished processing? ${n.processing_status} - ${n.id}`);
    return this._client.get(n.results_url, {
      ...t,
      headers: qe([{ Accept: "application/binary" }, t?.headers]),
      stream: !0,
      __binaryResponse: !0
    })._thenUnwrap((r, s) => KM.fromResponse(s.response, s.controller));
  }
}, cw = class extends hn {
  constructor() {
    super(...arguments), this.batches = new nP(this._client);
  }
  create(e, t) {
    e.model in YT && console.warn(`The model '${e.model}' is deprecated and will reach end-of-life on ${YT[e.model]}
Please migrate to a newer model. Visit https://docs.anthropic.com/en/docs/resources/model-deprecations for more information.`), e.model in yW && e.thinking && e.thinking.type === "enabled" && console.warn(`Using Claude with ${e.model} and 'thinking.type=enabled' is deprecated. Use 'thinking.type=adaptive' instead which results in better model performance in our testing: https://platform.claude.com/docs/en/build-with-claude/adaptive-thinking`);
    let n = this._client._options.timeout;
    if (!e.stream && n == null) {
      const s = UM[e.model] ?? void 0;
      n = this._client.calculateNonstreamingTimeout(e.max_tokens, s);
    }
    const r = LM(e.tools, e.messages);
    return this._client.post("/v1/messages", {
      body: e,
      timeout: n ?? 6e5,
      ...t,
      headers: qe([r, t?.headers]),
      stream: e.stream ?? !1
    });
  }
  parse(e, t) {
    return this.create(e, t).then((n) => tP(n, e, { logger: this._client.logger ?? console }));
  }
  stream(e, t) {
    return vW.createMessage(this, e, t, { logger: this._client.logger ?? console });
  }
  countTokens(e, t) {
    return this._client.post("/v1/messages/count_tokens", {
      body: e,
      ...t
    });
  }
}, YT = {
  "claude-1.3": "November 6th, 2024",
  "claude-1.3-100k": "November 6th, 2024",
  "claude-instant-1.1": "November 6th, 2024",
  "claude-instant-1.1-100k": "November 6th, 2024",
  "claude-instant-1.2": "November 6th, 2024",
  "claude-3-sonnet-20240229": "July 21st, 2025",
  "claude-3-opus-20240229": "January 5th, 2026",
  "claude-2.1": "July 21st, 2025",
  "claude-2.0": "July 21st, 2025",
  "claude-3-7-sonnet-latest": "February 19th, 2026",
  "claude-3-7-sonnet-20250219": "February 19th, 2026",
  "claude-3-5-haiku-latest": "February 19th, 2026",
  "claude-3-5-haiku-20241022": "February 19th, 2026",
  "claude-opus-4-0": "June 15th, 2026",
  "claude-opus-4-20250514": "June 15th, 2026",
  "claude-sonnet-4-0": "June 15th, 2026",
  "claude-sonnet-4-20250514": "June 15th, 2026"
}, yW = ["claude-opus-4-6"];
cw.Batches = nP;
var rP = class extends hn {
  retrieve(e, t = {}, n) {
    const { betas: r } = t ?? {};
    return this._client.get(pt`/v1/models/${e}`, {
      ...n,
      headers: qe([{ ...r?.toString() != null ? { "anthropic-beta": r?.toString() } : void 0 }, n?.headers])
    });
  }
  list(e = {}, t) {
    const { betas: n, ...r } = e ?? {};
    return this._client.getAPIList("/v1/models", ld, {
      query: r,
      ...t,
      headers: qe([{ ...n?.toString() != null ? { "anthropic-beta": n?.toString() } : void 0 }, t?.headers])
    });
  }
}, Af = (e) => {
  if (typeof globalThis.process < "u") return globalThis.process.env?.[e]?.trim() || void 0;
  if (typeof globalThis.Deno < "u") return globalThis.Deno.env?.get?.(e)?.trim() || void 0;
}, Hy, dw, rh, sP, _W = "\\n\\nHuman:", bW = "\\n\\nAssistant:", on = class {
  constructor({ baseURL: e = Af("ANTHROPIC_BASE_URL"), apiKey: t = Af("ANTHROPIC_API_KEY") ?? null, authToken: n = Af("ANTHROPIC_AUTH_TOKEN") ?? null, ...r } = {}) {
    Hy.add(this), rh.set(this, void 0);
    const s = {
      apiKey: t,
      authToken: n,
      ...r,
      baseURL: e || "https://api.anthropic.com"
    };
    if (!s.dangerouslyAllowBrowser && $G()) throw new _t(`It looks like you're running in a browser-like environment.

This is disabled by default, as it risks exposing your secret API credentials to attackers.
If you understand the risks and have appropriate mitigations in place,
you can set the \`dangerouslyAllowBrowser\` option to \`true\`, e.g.,

new Anthropic({ apiKey, dangerouslyAllowBrowser: true });
`);
    this.baseURL = s.baseURL, this.timeout = s.timeout ?? dw.DEFAULT_TIMEOUT, this.logger = s.logger ?? console;
    const o = "warn";
    this.logLevel = o, this.logLevel = PT(s.logLevel, "ClientOptions.logLevel", this) ?? PT(Af("ANTHROPIC_LOG"), "process.env['ANTHROPIC_LOG']", this) ?? o, this.fetchOptions = s.fetchOptions, this.maxRetries = s.maxRetries ?? 2, this.fetch = s.fetch ?? BG(), Ke(this, rh, qG, "f"), this._options = s, this.apiKey = typeof t == "string" ? t : null, this.authToken = n;
  }
  withOptions(e) {
    return new this.constructor({
      ...this._options,
      baseURL: this.baseURL,
      maxRetries: this.maxRetries,
      timeout: this.timeout,
      logger: this.logger,
      logLevel: this.logLevel,
      fetch: this.fetch,
      fetchOptions: this.fetchOptions,
      apiKey: this.apiKey,
      authToken: this.authToken,
      ...e
    });
  }
  defaultQuery() {
    return this._options.defaultQuery;
  }
  validateHeaders({ values: e, nulls: t }) {
    if (!(e.get("x-api-key") || e.get("authorization")) && !(this.apiKey && e.get("x-api-key")) && !t.has("x-api-key") && !(this.authToken && e.get("authorization")) && !t.has("authorization"))
      throw new Error('Could not resolve authentication method. Expected either apiKey or authToken to be set. Or for one of the "X-Api-Key" or "Authorization" headers to be explicitly omitted');
  }
  async authHeaders(e) {
    return qe([await this.apiKeyAuth(e), await this.bearerAuth(e)]);
  }
  async apiKeyAuth(e) {
    if (this.apiKey != null)
      return qe([{ "X-Api-Key": this.apiKey }]);
  }
  async bearerAuth(e) {
    if (this.authToken != null)
      return qe([{ Authorization: `Bearer ${this.authToken}` }]);
  }
  stringifyQuery(e) {
    return HG(e);
  }
  getUserAgent() {
    return `${this.constructor.name}/JS ${Wa}`;
  }
  defaultIdempotencyKey() {
    return `stainless-node-retry-${dM()}`;
  }
  makeStatusError(e, t, n, r) {
    return Lr.generate(e, t, n, r);
  }
  buildURL(e, t, n) {
    const r = !ce(this, Hy, "m", sP).call(this) && n || this.baseURL, s = MG(e) ? new URL(e) : new URL(r + (r.endsWith("/") && e.startsWith("/") ? e.slice(1) : e)), o = this.defaultQuery(), i = Object.fromEntries(s.searchParams);
    return (!ET(o) || !ET(i)) && (t = {
      ...i,
      ...o,
      ...t
    }), typeof t == "object" && t && !Array.isArray(t) && (s.search = this.stringifyQuery(t)), s.toString();
  }
  _calculateNonstreamingTimeout(e) {
    if (3600 * e / 128e3 > 600) throw new _t("Streaming is required for operations that may take longer than 10 minutes. See https://github.com/anthropics/anthropic-sdk-typescript#streaming-responses for more details");
    return 600 * 1e3;
  }
  async prepareOptions(e) {
  }
  async prepareRequest(e, { url: t, options: n }) {
  }
  get(e, t) {
    return this.methodRequest("get", e, t);
  }
  post(e, t) {
    return this.methodRequest("post", e, t);
  }
  patch(e, t) {
    return this.methodRequest("patch", e, t);
  }
  put(e, t) {
    return this.methodRequest("put", e, t);
  }
  delete(e, t) {
    return this.methodRequest("delete", e, t);
  }
  methodRequest(e, t, n) {
    return this.request(Promise.resolve(n).then((r) => ({
      method: e,
      path: t,
      ...r
    })));
  }
  request(e, t = null) {
    return new AM(this, this.makeRequest(e, t, void 0));
  }
  async makeRequest(e, t, n) {
    const r = await e, s = r.maxRetries ?? this.maxRetries;
    t == null && (t = s), await this.prepareOptions(r);
    const { req: o, url: i, timeout: a } = await this.buildRequest(r, { retryCount: s - t });
    await this.prepareRequest(o, {
      url: i,
      options: r
    });
    const l = "log_" + (Math.random() * (1 << 24) | 0).toString(16).padStart(6, "0"), u = n === void 0 ? "" : `, retryOf: ${n}`, d = Date.now();
    if (Gn(this).debug(`[${l}] sending request`, Pi({
      retryOfRequestLogID: n,
      method: r.method,
      url: i,
      options: r,
      headers: o.headers
    })), r.signal?.aborted) throw new ss();
    const h = new AbortController(), p = await this.fetchWithTimeout(i, o, a, h).catch(Ny), y = Date.now();
    if (p instanceof globalThis.Error) {
      const E = `retrying, ${t} attempts remaining`;
      if (r.signal?.aborted) throw new ss();
      const C = Hc(p) || /timed? ?out/i.test(String(p) + ("cause" in p ? String(p.cause) : ""));
      if (t)
        return Gn(this).info(`[${l}] connection ${C ? "timed out" : "failed"} - ${E}`), Gn(this).debug(`[${l}] connection ${C ? "timed out" : "failed"} (${E})`, Pi({
          retryOfRequestLogID: n,
          url: i,
          durationMs: y - d,
          message: p.message
        })), this.retryRequest(r, t, n ?? l);
      throw Gn(this).info(`[${l}] connection ${C ? "timed out" : "failed"} - error; no more retries left`), Gn(this).debug(`[${l}] connection ${C ? "timed out" : "failed"} (error; no more retries left)`, Pi({
        retryOfRequestLogID: n,
        url: i,
        durationMs: y - d,
        message: p.message
      })), C ? new fM() : new Zp({ cause: p });
    }
    const w = `[${l}${u}${[...p.headers.entries()].filter(([E]) => E === "request-id").map(([E, C]) => ", " + E + ": " + JSON.stringify(C)).join("")}] ${o.method} ${i} ${p.ok ? "succeeded" : "failed"} with status ${p.status} in ${y - d}ms`;
    if (!p.ok) {
      const E = await this.shouldRetry(p);
      if (t && E) {
        const D = `retrying, ${t} attempts remaining`;
        return await UG(p.body), Gn(this).info(`${w} - ${D}`), Gn(this).debug(`[${l}] response error (${D})`, Pi({
          retryOfRequestLogID: n,
          url: p.url,
          status: p.status,
          headers: p.headers,
          durationMs: y - d
        })), this.retryRequest(r, t, n ?? l, p.headers);
      }
      const C = E ? "error; no more retries left" : "error; not retryable";
      Gn(this).info(`${w} - ${C}`);
      const M = await p.text().catch((D) => Ny(D).message), k = wM(M), R = k ? void 0 : M;
      throw Gn(this).debug(`[${l}] response error (${C})`, Pi({
        retryOfRequestLogID: n,
        url: p.url,
        status: p.status,
        headers: p.headers,
        message: R,
        durationMs: Date.now() - d
      })), this.makeStatusError(p.status, k, R, p.headers);
    }
    return Gn(this).info(w), Gn(this).debug(`[${l}] response start`, Pi({
      retryOfRequestLogID: n,
      url: p.url,
      status: p.status,
      headers: p.headers,
      durationMs: y - d
    })), {
      response: p,
      options: r,
      controller: h,
      requestLogID: l,
      retryOfRequestLogID: n,
      startTime: d
    };
  }
  getAPIList(e, t, n) {
    return this.requestAPIList(t, n && "then" in n ? n.then((r) => ({
      method: "get",
      path: e,
      ...r
    })) : {
      method: "get",
      path: e,
      ...n
    });
  }
  requestAPIList(e, t) {
    const n = this.makeRequest(t, null, void 0);
    return new QG(this, n, e);
  }
  async fetchWithTimeout(e, t, n, r) {
    const { signal: s, method: o, ...i } = t || {}, a = this._makeAbort(r);
    s && s.addEventListener("abort", a, { once: !0 });
    const l = setTimeout(a, n), u = globalThis.ReadableStream && i.body instanceof globalThis.ReadableStream || typeof i.body == "object" && i.body !== null && Symbol.asyncIterator in i.body, d = {
      signal: r.signal,
      ...u ? { duplex: "half" } : {},
      method: "GET",
      ...i
    };
    o && (d.method = o.toUpperCase());
    try {
      return await this.fetch.call(void 0, e, d);
    } finally {
      clearTimeout(l);
    }
  }
  async shouldRetry(e) {
    const t = e.headers.get("x-should-retry");
    return t === "true" ? !0 : t === "false" ? !1 : e.status === 408 || e.status === 409 || e.status === 429 || e.status >= 500;
  }
  async retryRequest(e, t, n, r) {
    let s;
    const o = r?.get("retry-after-ms");
    if (o) {
      const a = parseFloat(o);
      Number.isNaN(a) || (s = a);
    }
    const i = r?.get("retry-after");
    if (i && !s) {
      const a = parseFloat(i);
      Number.isNaN(a) ? s = Date.parse(i) - Date.now() : s = a * 1e3;
    }
    if (s === void 0) {
      const a = e.maxRetries ?? this.maxRetries;
      s = this.calculateDefaultRetryTimeoutMillis(t, a);
    }
    return await NG(s), this.makeRequest(e, t - 1, n);
  }
  calculateDefaultRetryTimeoutMillis(e, t) {
    const s = t - e;
    return Math.min(0.5 * Math.pow(2, s), 8) * (1 - Math.random() * 0.25) * 1e3;
  }
  calculateNonstreamingTimeout(e, t) {
    if (36e5 * e / 128e3 > 6e5 || t != null && e > t) throw new _t("Streaming is required for operations that may take longer than 10 minutes. See https://github.com/anthropics/anthropic-sdk-typescript#long-requests for more details");
    return 6e5;
  }
  async buildRequest(e, { retryCount: t = 0 } = {}) {
    const n = { ...e }, { method: r, path: s, query: o, defaultBaseURL: i } = n, a = this.buildURL(s, o, i);
    "timeout" in n && RG("timeout", n.timeout), n.timeout = n.timeout ?? this.timeout;
    const { bodyHeaders: l, body: u } = this.buildBody({ options: n });
    return {
      req: {
        method: r,
        headers: await this.buildHeaders({
          options: e,
          method: r,
          bodyHeaders: l,
          retryCount: t
        }),
        ...n.signal && { signal: n.signal },
        ...globalThis.ReadableStream && u instanceof globalThis.ReadableStream && { duplex: "half" },
        ...u && { body: u },
        ...this.fetchOptions ?? {},
        ...n.fetchOptions ?? {}
      },
      url: a,
      timeout: n.timeout
    };
  }
  async buildHeaders({ options: e, method: t, bodyHeaders: n, retryCount: r }) {
    let s = {};
    this.idempotencyHeader && t !== "get" && (e.idempotencyKey || (e.idempotencyKey = this.defaultIdempotencyKey()), s[this.idempotencyHeader] = e.idempotencyKey);
    const o = qe([
      s,
      {
        Accept: "application/json",
        "User-Agent": this.getUserAgent(),
        "X-Stainless-Retry-Count": String(r),
        ...e.timeout ? { "X-Stainless-Timeout": String(Math.trunc(e.timeout / 1e3)) } : {},
        ...FG(),
        ...this._options.dangerouslyAllowBrowser ? { "anthropic-dangerous-direct-browser-access": "true" } : void 0,
        "anthropic-version": "2023-06-01"
      },
      await this.authHeaders(e),
      this._options.defaultHeaders,
      n,
      e.headers
    ]);
    return this.validateHeaders(o), o.values;
  }
  _makeAbort(e) {
    return () => e.abort();
  }
  buildBody({ options: { body: e, headers: t } }) {
    if (!e) return {
      bodyHeaders: void 0,
      body: void 0
    };
    const n = qe([t]);
    return ArrayBuffer.isView(e) || e instanceof ArrayBuffer || e instanceof DataView || typeof e == "string" && n.values.has("content-type") || globalThis.Blob && e instanceof globalThis.Blob || e instanceof FormData || e instanceof URLSearchParams || globalThis.ReadableStream && e instanceof globalThis.ReadableStream ? {
      bodyHeaders: void 0,
      body: e
    } : typeof e == "object" && (Symbol.asyncIterator in e || Symbol.iterator in e && "next" in e && typeof e.next == "function") ? {
      bodyHeaders: void 0,
      body: TM(e)
    } : typeof e == "object" && n.values.get("content-type") === "application/x-www-form-urlencoded" ? {
      bodyHeaders: { "content-type": "application/x-www-form-urlencoded" },
      body: this.stringifyQuery(e)
    } : ce(this, rh, "f").call(this, {
      body: e,
      headers: n
    });
  }
};
dw = on, rh = /* @__PURE__ */ new WeakMap(), Hy = /* @__PURE__ */ new WeakSet(), sP = function() {
  return this.baseURL !== "https://api.anthropic.com";
};
on.Anthropic = dw;
on.HUMAN_PROMPT = _W;
on.AI_PROMPT = bW;
on.DEFAULT_TIMEOUT = 6e5;
on.AnthropicError = _t;
on.APIError = Lr;
on.APIConnectionError = Zp;
on.APIConnectionTimeoutError = fM;
on.APIUserAbortError = ss;
on.NotFoundError = gM;
on.ConflictError = vM;
on.RateLimitError = _M;
on.BadRequestError = hM;
on.AuthenticationError = pM;
on.InternalServerError = bM;
on.PermissionDeniedError = mM;
on.UnprocessableEntityError = yM;
on.toFile = rW;
var cd = class extends on {
  constructor() {
    super(...arguments), this.completions = new jM(this), this.messages = new cw(this), this.models = new rP(this), this.beta = new Os(this);
  }
};
cd.Completions = jM;
cd.Messages = cw;
cd.Models = rP;
cd.Beta = Os;
function Cl(e) {
  if (Array.isArray(e)) return e.map((n) => Cl(n));
  if (!e || typeof e != "object") return e;
  const t = {};
  return Object.entries(e).forEach(([n, r]) => {
    t[n] = /authorization|csrf|token|api[-_]?key|proxy_password|password/i.test(n) ? "[redacted]" : Cl(r);
  }), t;
}
function Wc(e = {}) {
  return {
    provider: e.provider || "",
    model: e.model || "",
    transport: e.transport || "sdk",
    request: Cl({
      url: e.url || "",
      method: e.method || "POST",
      headers: e.headers || {},
      body: e.body || {},
      sdk: e.sdk || void 0
    })
  };
}
function wW(e) {
  try {
    return JSON.parse(e || "{}");
  } catch {
    return {};
  }
}
function SW(e = "") {
  const t = String(e || "").match(/^data:([^;,]+);base64,(.+)$/);
  return t ? {
    mediaType: t[1],
    data: t[2]
  } : {
    mediaType: "",
    data: ""
  };
}
function oP(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function TW(e) {
  if (typeof e == "string") return [{
    type: "text",
    text: e
  }];
  if (!Array.isArray(e)) return [{
    type: "text",
    text: ""
  }];
  const t = e.map((n) => {
    if (!n || typeof n != "object") return null;
    if (n.type === "text") return {
      type: "text",
      text: n.text || ""
    };
    if (n.type === "image_url" && n.image_url?.url) {
      const r = SW(n.image_url.url);
      return !r.mediaType || !r.data ? null : {
        type: "image",
        source: {
          type: "base64",
          media_type: r.mediaType,
          data: r.data
        }
      };
    }
    return null;
  }).filter(Boolean);
  return t.length ? t : [{
    type: "text",
    text: ""
  }];
}
function EW(e) {
  const t = [String(e.systemPrompt || "").trim(), ...(e.messages || []).filter((n) => n.role === "system").map((n) => String(n.content || "").trim())].filter(Boolean);
  return t.length ? [...new Set(t)].join(`

`) : "";
}
function CW(e) {
  const t = e?.providerPayload?.anthropicContent;
  return Array.isArray(t) && t.length && oP(t) || null;
}
function AW(e) {
  return Array.isArray(e?.content) && e.content.length ? { anthropicContent: oP(e.content) || [] } : void 0;
}
function XT(e = {}) {
  return {
    type: "tool_result",
    tool_use_id: e.tool_call_id,
    content: e.content
  };
}
function QT(e = []) {
  return (Array.isArray(e) ? e : []).map((t) => {
    const n = String(t?.function?.name || "").trim();
    return n ? {
      type: "tool_use",
      id: t.id,
      name: n,
      input: wW(t.function.arguments)
    } : null;
  }).filter(Boolean);
}
function IW(e) {
  const t = [];
  for (let n = 0; n < e.length; n += 1) {
    const r = e[n];
    if (r.role !== "system") {
      if (r.role === "assistant") {
        const s = CW(r), o = QT(r.tool_calls);
        if (s && o.length) {
          t.push({
            role: "assistant",
            content: s.filter((i) => i?.type !== "tool_use").concat(o)
          });
          continue;
        }
        if (s) {
          t.push({
            role: "assistant",
            content: s
          });
          continue;
        }
      }
      if (r.role === "tool") {
        const s = [XT(r)];
        for (; e[n + 1]?.role === "tool"; )
          n += 1, s.push(XT(e[n]));
        t.push({
          role: "user",
          content: s
        });
        continue;
      }
      if (r.role === "assistant" && Array.isArray(r.tool_calls) && r.tool_calls.length) {
        t.push({
          role: "assistant",
          content: [...r.content ? [{
            type: "text",
            text: r.content
          }] : [], ...QT(r.tool_calls)]
        });
        continue;
      }
      t.push({
        role: r.role,
        content: TW(r.content)
      });
    }
  }
  return t;
}
function If(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function ZT(e = "") {
  return String(e || "https://api.anthropic.com").trim().replace(/\/+$/, "").replace(/\/v1$/i, "");
}
var xW = class {
  constructor(e) {
    this.config = e, this.client = new cd({
      apiKey: e.apiKey,
      baseURL: ZT(e.baseUrl),
      timeout: Number(e.timeoutMs) || 900 * 1e3,
      maxRetries: 0,
      dangerouslyAllowBrowser: !0
    });
  }
  buildRequestBody(e) {
    const t = (e.tools || []).map((s) => ({
      name: s.function.name,
      description: s.function.description,
      input_schema: s.function.parameters
    })), n = EW(e), r = {
      model: this.config.model,
      system: n,
      messages: IW(e.messages),
      tools: t,
      ...e.maxTokens ? { max_tokens: e.maxTokens } : {}
    };
    return !e.reasoning?.enabled && typeof e.temperature == "number" && (r.temperature = e.temperature), e.reasoning?.enabled && (r.thinking = {
      type: "adaptive",
      display: "summarized"
    }), r;
  }
  inspectRequest(e, t = {}) {
    const n = typeof e.onStreamProgress == "function", r = ZT(this.config.baseUrl);
    return Wc({
      provider: "anthropic",
      model: this.config.model,
      transport: "anthropic-sdk",
      url: `${r}/v1/messages`,
      headers: {
        "Content-Type": "application/json",
        "x-api-key": this.config.apiKey || ""
      },
      body: t.body || this.buildRequestBody(e),
      sdk: n ? "client.messages.stream" : "client.messages.create"
    });
  }
  async chat(e) {
    const t = this.buildRequestBody(e), n = this.inspectRequest(e, { body: t });
    let r;
    if (typeof e.onStreamProgress == "function") {
      const o = this.client.messages.stream(t, { signal: e.signal }), i = /* @__PURE__ */ new Map(), a = /* @__PURE__ */ new Map();
      let l = "";
      const u = () => Array.from(i.entries()).sort(([p], [y]) => p.localeCompare(y)).map(([p, y]) => ({
        label: p.startsWith("redacted:") ? "已脱敏思考块" : "思考块",
        text: y
      })).filter((p) => p.text), d = () => Array.from(a.entries()).sort(([p], [y]) => Number(p) - Number(y)).map(([, p]) => ({
        id: p.id || "anthropic-tool-draft",
        name: p.name || "工具调用",
        arguments: p.inputJson || "{}",
        draft: !0
      })).filter((p) => p.name), h = () => {
        const p = d();
        p.length && If(e, {
          text: l,
          thoughts: u(),
          toolCalls: p,
          toolCallDraft: !0
        });
      };
      o.on("text", (p, y) => {
        l = y || "", If(e, {
          text: l,
          thoughts: u(),
          ...d().length ? {
            toolCalls: d(),
            toolCallDraft: !0
          } : {}
        });
      }), o.on("thinking", (p, y) => {
        i.set("thinking:0", y || ""), If(e, {
          thoughts: u(),
          ...d().length ? {
            text: l,
            toolCalls: d(),
            toolCallDraft: !0
          } : {}
        });
      }), o.on("streamEvent", (p) => {
        if (p?.type === "content_block_start" && p.content_block?.type === "tool_use") {
          const y = p.content_block.input && typeof p.content_block.input == "object" ? p.content_block.input : {};
          a.set(p.index, {
            id: p.content_block.id || `anthropic-tool-draft-${p.index + 1}`,
            name: p.content_block.name || "工具调用",
            inputJson: Object.keys(y).length ? JSON.stringify(y) : ""
          }), h();
          return;
        }
        if (p?.type === "content_block_delta" && p.delta?.type === "input_json_delta") {
          const y = a.get(p.index) || {
            id: `anthropic-tool-draft-${p.index + 1}`,
            name: "工具调用",
            inputJson: ""
          };
          a.set(p.index, {
            ...y,
            inputJson: `${y.inputJson || ""}${p.delta.partial_json || ""}`
          }), h();
        }
      }), o.on("contentBlock", (p) => {
        p?.type === "redacted_thinking" && (i.set("redacted:0", p.data || ""), If(e, {
          thoughts: u(),
          ...d().length ? {
            text: l,
            toolCalls: d(),
            toolCallDraft: !0
          } : {}
        }));
      }), r = await o.finalMessage();
    } else r = await this.client.messages.create(t, { signal: e.signal });
    const s = (r.content || []).filter((o) => o.type === "tool_use" && o.name).map((o, i) => ({
      id: o.id || `anthropic-tool-${i + 1}`,
      name: o.name,
      arguments: JSON.stringify(o.input || {})
    }));
    return {
      text: (r.content || []).filter((o) => o.type === "text").map((o) => o.text || "").join(`
`),
      toolCalls: s,
      thoughts: (r.content || []).filter((o) => o.type === "thinking" || o.type === "redacted_thinking").map((o) => ({
        label: o.type === "thinking" ? "思考块" : "已脱敏思考块",
        text: o.type === "thinking" ? o.thinking || "" : o.data || ""
      })).filter((o) => o.text),
      finishReason: r.stop_reason || "stop",
      model: r.model || this.config.model,
      provider: "anthropic",
      providerPayload: AW(r),
      requestInspection: n
    };
  }
}, kW = /* @__PURE__ */ Xc(((e, t) => {
  function n(r, s) {
    typeof s == "boolean" && (s = { forever: s }), this._originalTimeouts = JSON.parse(JSON.stringify(r)), this._timeouts = r, this._options = s || {}, this._maxRetryTime = s && s.maxRetryTime || 1 / 0, this._fn = null, this._errors = [], this._attempts = 1, this._operationTimeout = null, this._operationTimeoutCb = null, this._timeout = null, this._operationStart = null, this._timer = null, this._options.forever && (this._cachedTimeouts = this._timeouts.slice(0));
  }
  t.exports = n, n.prototype.reset = function() {
    this._attempts = 1, this._timeouts = this._originalTimeouts.slice(0);
  }, n.prototype.stop = function() {
    this._timeout && clearTimeout(this._timeout), this._timer && clearTimeout(this._timer), this._timeouts = [], this._cachedTimeouts = null;
  }, n.prototype.retry = function(r) {
    if (this._timeout && clearTimeout(this._timeout), !r) return !1;
    var s = (/* @__PURE__ */ new Date()).getTime();
    if (r && s - this._operationStart >= this._maxRetryTime)
      return this._errors.push(r), this._errors.unshift(/* @__PURE__ */ new Error("RetryOperation timeout occurred")), !1;
    this._errors.push(r);
    var o = this._timeouts.shift();
    if (o === void 0) if (this._cachedTimeouts)
      this._errors.splice(0, this._errors.length - 1), o = this._cachedTimeouts.slice(-1);
    else return !1;
    var i = this;
    return this._timer = setTimeout(function() {
      i._attempts++, i._operationTimeoutCb && (i._timeout = setTimeout(function() {
        i._operationTimeoutCb(i._attempts);
      }, i._operationTimeout), i._options.unref && i._timeout.unref()), i._fn(i._attempts);
    }, o), this._options.unref && this._timer.unref(), !0;
  }, n.prototype.attempt = function(r, s) {
    this._fn = r, s && (s.timeout && (this._operationTimeout = s.timeout), s.cb && (this._operationTimeoutCb = s.cb));
    var o = this;
    this._operationTimeoutCb && (this._timeout = setTimeout(function() {
      o._operationTimeoutCb();
    }, o._operationTimeout)), this._operationStart = (/* @__PURE__ */ new Date()).getTime(), this._fn(this._attempts);
  }, n.prototype.try = function(r) {
    console.log("Using RetryOperation.try() is deprecated"), this.attempt(r);
  }, n.prototype.start = function(r) {
    console.log("Using RetryOperation.start() is deprecated"), this.attempt(r);
  }, n.prototype.start = n.prototype.try, n.prototype.errors = function() {
    return this._errors;
  }, n.prototype.attempts = function() {
    return this._attempts;
  }, n.prototype.mainError = function() {
    if (this._errors.length === 0) return null;
    for (var r = {}, s = null, o = 0, i = 0; i < this._errors.length; i++) {
      var a = this._errors[i], l = a.message, u = (r[l] || 0) + 1;
      r[l] = u, u >= o && (s = a, o = u);
    }
    return s;
  };
})), MW = /* @__PURE__ */ Xc(((e) => {
  var t = kW();
  e.operation = function(n) {
    return new t(e.timeouts(n), {
      forever: n && (n.forever || n.retries === 1 / 0),
      unref: n && n.unref,
      maxRetryTime: n && n.maxRetryTime
    });
  }, e.timeouts = function(n) {
    if (n instanceof Array) return [].concat(n);
    var r = {
      retries: 10,
      factor: 2,
      minTimeout: 1 * 1e3,
      maxTimeout: 1 / 0,
      randomize: !1
    };
    for (var s in n) r[s] = n[s];
    if (r.minTimeout > r.maxTimeout) throw new Error("minTimeout is greater than maxTimeout");
    for (var o = [], i = 0; i < r.retries; i++) o.push(this.createTimeout(i, r));
    return n && n.forever && !o.length && o.push(this.createTimeout(i, r)), o.sort(function(a, l) {
      return a - l;
    }), o;
  }, e.createTimeout = function(n, r) {
    var s = r.randomize ? Math.random() + 1 : 1, o = Math.round(s * Math.max(r.minTimeout, 1) * Math.pow(r.factor, n));
    return o = Math.min(o, r.maxTimeout), o;
  }, e.wrap = function(n, r, s) {
    if (r instanceof Array && (s = r, r = null), !s) {
      s = [];
      for (var o in n) typeof n[o] == "function" && s.push(o);
    }
    for (var i = 0; i < s.length; i++) {
      var a = s[i], l = n[a];
      n[a] = function(d) {
        var h = e.operation(r), p = Array.prototype.slice.call(arguments, 1), y = p.pop();
        p.push(function(w) {
          h.retry(w) || (w && (arguments[0] = h.mainError()), y.apply(this, arguments));
        }), h.attempt(function() {
          d.apply(n, p);
        });
      }.bind(n, l), n[a].options = r;
    }
  };
})), PW = /* @__PURE__ */ Xc(((e, t) => {
  t.exports = MW();
})), RW = /* @__PURE__ */ Xc(((e, t) => {
  var n = PW(), r = [
    "Failed to fetch",
    "NetworkError when attempting to fetch resource.",
    "The Internet connection appears to be offline.",
    "Network request failed"
  ], s = class extends Error {
    constructor(l) {
      super(), l instanceof Error ? (this.originalError = l, { message: l } = l) : (this.originalError = new Error(l), this.originalError.stack = this.stack), this.name = "AbortError", this.message = l;
    }
  }, o = (l, u, d) => {
    const h = d.retries - (u - 1);
    return l.attemptNumber = u, l.retriesLeft = h, l;
  }, i = (l) => r.includes(l), a = (l, u) => new Promise((d, h) => {
    u = {
      onFailedAttempt: () => {
      },
      retries: 10,
      ...u
    };
    const p = n.operation(u);
    p.attempt(async (y) => {
      try {
        d(await l(y));
      } catch (w) {
        if (!(w instanceof Error)) {
          h(/* @__PURE__ */ new TypeError(`Non-error was thrown: "${w}". You should only throw errors.`));
          return;
        }
        if (w instanceof s)
          p.stop(), h(w.originalError);
        else if (w instanceof TypeError && !i(w.message))
          p.stop(), h(w);
        else {
          o(w, y, u);
          try {
            await u.onFailedAttempt(w);
          } catch (E) {
            h(E);
            return;
          }
          p.retry(w) || h(p.mainError());
        }
      }
    });
  });
  t.exports = a, t.exports.default = a, t.exports.AbortError = s;
})), jT = /* @__PURE__ */ cI(RW(), 1), NW = void 0, $W = void 0;
function DW() {
  return {
    geminiUrl: NW,
    vertexUrl: $W
  };
}
function LW(e, t, n, r) {
  var s, o;
  if (!e?.baseUrl) {
    const i = DW();
    return t ? (s = i.vertexUrl) !== null && s !== void 0 ? s : n : (o = i.geminiUrl) !== null && o !== void 0 ? o : r;
  }
  return e.baseUrl;
}
var yo = class {
};
function Ue(e, t) {
  return e.replace(/\{([^}]+)\}/g, (n, r) => {
    if (Object.prototype.hasOwnProperty.call(t, r)) {
      const s = t[r];
      return s != null ? String(s) : "";
    } else throw new Error(`Key '${r}' not found in valueMap.`);
  });
}
function f(e, t, n) {
  for (let o = 0; o < t.length - 1; o++) {
    const i = t[o];
    if (i.endsWith("[]")) {
      const a = i.slice(0, -2);
      if (!(a in e)) if (Array.isArray(n)) e[a] = Array.from({ length: n.length }, () => ({}));
      else throw new Error(`Value must be a list given an array path ${i}`);
      if (Array.isArray(e[a])) {
        const l = e[a];
        if (Array.isArray(n)) for (let u = 0; u < l.length; u++) {
          const d = l[u];
          f(d, t.slice(o + 1), n[u]);
        }
        else for (const u of l) f(u, t.slice(o + 1), n);
      }
      return;
    } else if (i.endsWith("[0]")) {
      const a = i.slice(0, -3);
      a in e || (e[a] = [{}]);
      const l = e[a];
      f(l[0], t.slice(o + 1), n);
      return;
    }
    (!e[i] || typeof e[i] != "object") && (e[i] = {}), e = e[i];
  }
  const r = t[t.length - 1], s = e[r];
  if (s !== void 0) {
    if (!n || typeof n == "object" && Object.keys(n).length === 0 || n === s) return;
    if (typeof s == "object" && typeof n == "object" && s !== null && n !== null) Object.assign(s, n);
    else throw new Error(`Cannot set value for an existing key. Key: ${r}`);
  } else r === "_self" && typeof n == "object" && n !== null && !Array.isArray(n) ? Object.assign(e, n) : e[r] = n;
}
function c(e, t, n = void 0) {
  try {
    if (t.length === 1 && t[0] === "_self") return e;
    for (let r = 0; r < t.length; r++) {
      if (typeof e != "object" || e === null) return n;
      const s = t[r];
      if (s.endsWith("[]")) {
        const o = s.slice(0, -2);
        if (o in e) {
          const i = e[o];
          return Array.isArray(i) ? i.map((a) => c(a, t.slice(r + 1), n)) : n;
        } else return n;
      } else e = e[s];
    }
    return e;
  } catch (r) {
    if (r instanceof TypeError) return n;
    throw r;
  }
}
function OW(e, t) {
  for (const [n, r] of Object.entries(t)) {
    const s = n.split("."), o = r.split("."), i = /* @__PURE__ */ new Set();
    let a = -1;
    for (let l = 0; l < s.length; l++) if (s[l] === "*") {
      a = l;
      break;
    }
    if (a !== -1 && o.length > a) for (let l = a; l < o.length; l++) {
      const u = o[l];
      u !== "*" && !u.endsWith("[]") && !u.endsWith("[0]") && i.add(u);
    }
    Gy(e, s, o, 0, i);
  }
}
function Gy(e, t, n, r, s) {
  if (r >= t.length || typeof e != "object" || e === null) return;
  const o = t[r];
  if (o.endsWith("[]")) {
    const i = o.slice(0, -2), a = e;
    if (i in a && Array.isArray(a[i])) for (const l of a[i]) Gy(l, t, n, r + 1, s);
  } else if (o === "*") {
    if (typeof e == "object" && e !== null && !Array.isArray(e)) {
      const i = e, a = Object.keys(i).filter((u) => !u.startsWith("_") && !s.has(u)), l = {};
      for (const u of a) l[u] = i[u];
      for (const [u, d] of Object.entries(l)) {
        const h = [];
        for (const p of n.slice(r)) p === "*" ? h.push(u) : h.push(p);
        f(i, h, d);
      }
      for (const u of a) delete i[u];
    }
  } else {
    const i = e;
    o in i && Gy(i[o], t, n, r + 1, s);
  }
}
function fw(e) {
  if (typeof e != "string") throw new Error("fromImageBytes must be a string");
  return e;
}
function FW(e) {
  const t = {}, n = c(e, ["operationName"]);
  n != null && f(t, ["operationName"], n);
  const r = c(e, ["resourceName"]);
  return r != null && f(t, ["_url", "resourceName"], r), t;
}
function BW(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && f(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && f(t, ["metadata"], r);
  const s = c(e, ["done"]);
  s != null && f(t, ["done"], s);
  const o = c(e, ["error"]);
  o != null && f(t, ["error"], o);
  const i = c(e, ["response", "generateVideoResponse"]);
  return i != null && f(t, ["response"], qW(i)), t;
}
function UW(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && f(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && f(t, ["metadata"], r);
  const s = c(e, ["done"]);
  s != null && f(t, ["done"], s);
  const o = c(e, ["error"]);
  o != null && f(t, ["error"], o);
  const i = c(e, ["response"]);
  return i != null && f(t, ["response"], HW(i)), t;
}
function qW(e) {
  const t = {}, n = c(e, ["generatedSamples"]);
  if (n != null) {
    let o = n;
    Array.isArray(o) && (o = o.map((i) => GW(i))), f(t, ["generatedVideos"], o);
  }
  const r = c(e, ["raiMediaFilteredCount"]);
  r != null && f(t, ["raiMediaFilteredCount"], r);
  const s = c(e, ["raiMediaFilteredReasons"]);
  return s != null && f(t, ["raiMediaFilteredReasons"], s), t;
}
function HW(e) {
  const t = {}, n = c(e, ["videos"]);
  if (n != null) {
    let o = n;
    Array.isArray(o) && (o = o.map((i) => WW(i))), f(t, ["generatedVideos"], o);
  }
  const r = c(e, ["raiMediaFilteredCount"]);
  r != null && f(t, ["raiMediaFilteredCount"], r);
  const s = c(e, ["raiMediaFilteredReasons"]);
  return s != null && f(t, ["raiMediaFilteredReasons"], s), t;
}
function GW(e) {
  const t = {}, n = c(e, ["video"]);
  return n != null && f(t, ["video"], XW(n)), t;
}
function WW(e) {
  const t = {}, n = c(e, ["_self"]);
  return n != null && f(t, ["video"], QW(n)), t;
}
function VW(e) {
  const t = {}, n = c(e, ["operationName"]);
  return n != null && f(t, ["_url", "operationName"], n), t;
}
function KW(e) {
  const t = {}, n = c(e, ["operationName"]);
  return n != null && f(t, ["_url", "operationName"], n), t;
}
function zW(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && f(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && f(t, ["metadata"], r);
  const s = c(e, ["done"]);
  s != null && f(t, ["done"], s);
  const o = c(e, ["error"]);
  o != null && f(t, ["error"], o);
  const i = c(e, ["response"]);
  return i != null && f(t, ["response"], JW(i)), t;
}
function JW(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["parent"]);
  r != null && f(t, ["parent"], r);
  const s = c(e, ["documentName"]);
  return s != null && f(t, ["documentName"], s), t;
}
function iP(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && f(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && f(t, ["metadata"], r);
  const s = c(e, ["done"]);
  s != null && f(t, ["done"], s);
  const o = c(e, ["error"]);
  o != null && f(t, ["error"], o);
  const i = c(e, ["response"]);
  return i != null && f(t, ["response"], YW(i)), t;
}
function YW(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["parent"]);
  r != null && f(t, ["parent"], r);
  const s = c(e, ["documentName"]);
  return s != null && f(t, ["documentName"], s), t;
}
function XW(e) {
  const t = {}, n = c(e, ["uri"]);
  n != null && f(t, ["uri"], n);
  const r = c(e, ["encodedVideo"]);
  r != null && f(t, ["videoBytes"], fw(r));
  const s = c(e, ["encoding"]);
  return s != null && f(t, ["mimeType"], s), t;
}
function QW(e) {
  const t = {}, n = c(e, ["gcsUri"]);
  n != null && f(t, ["uri"], n);
  const r = c(e, ["bytesBase64Encoded"]);
  r != null && f(t, ["videoBytes"], fw(r));
  const s = c(e, ["mimeType"]);
  return s != null && f(t, ["mimeType"], s), t;
}
var eE;
(function(e) {
  e.LANGUAGE_UNSPECIFIED = "LANGUAGE_UNSPECIFIED", e.PYTHON = "PYTHON";
})(eE || (eE = {}));
var tE;
(function(e) {
  e.OUTCOME_UNSPECIFIED = "OUTCOME_UNSPECIFIED", e.OUTCOME_OK = "OUTCOME_OK", e.OUTCOME_FAILED = "OUTCOME_FAILED", e.OUTCOME_DEADLINE_EXCEEDED = "OUTCOME_DEADLINE_EXCEEDED";
})(tE || (tE = {}));
var nE;
(function(e) {
  e.SCHEDULING_UNSPECIFIED = "SCHEDULING_UNSPECIFIED", e.SILENT = "SILENT", e.WHEN_IDLE = "WHEN_IDLE", e.INTERRUPT = "INTERRUPT";
})(nE || (nE = {}));
var Ho;
(function(e) {
  e.TYPE_UNSPECIFIED = "TYPE_UNSPECIFIED", e.STRING = "STRING", e.NUMBER = "NUMBER", e.INTEGER = "INTEGER", e.BOOLEAN = "BOOLEAN", e.ARRAY = "ARRAY", e.OBJECT = "OBJECT", e.NULL = "NULL";
})(Ho || (Ho = {}));
var rE;
(function(e) {
  e.ENVIRONMENT_UNSPECIFIED = "ENVIRONMENT_UNSPECIFIED", e.ENVIRONMENT_BROWSER = "ENVIRONMENT_BROWSER";
})(rE || (rE = {}));
var sE;
(function(e) {
  e.AUTH_TYPE_UNSPECIFIED = "AUTH_TYPE_UNSPECIFIED", e.NO_AUTH = "NO_AUTH", e.API_KEY_AUTH = "API_KEY_AUTH", e.HTTP_BASIC_AUTH = "HTTP_BASIC_AUTH", e.GOOGLE_SERVICE_ACCOUNT_AUTH = "GOOGLE_SERVICE_ACCOUNT_AUTH", e.OAUTH = "OAUTH", e.OIDC_AUTH = "OIDC_AUTH";
})(sE || (sE = {}));
var oE;
(function(e) {
  e.HTTP_IN_UNSPECIFIED = "HTTP_IN_UNSPECIFIED", e.HTTP_IN_QUERY = "HTTP_IN_QUERY", e.HTTP_IN_HEADER = "HTTP_IN_HEADER", e.HTTP_IN_PATH = "HTTP_IN_PATH", e.HTTP_IN_BODY = "HTTP_IN_BODY", e.HTTP_IN_COOKIE = "HTTP_IN_COOKIE";
})(oE || (oE = {}));
var iE;
(function(e) {
  e.API_SPEC_UNSPECIFIED = "API_SPEC_UNSPECIFIED", e.SIMPLE_SEARCH = "SIMPLE_SEARCH", e.ELASTIC_SEARCH = "ELASTIC_SEARCH";
})(iE || (iE = {}));
var aE;
(function(e) {
  e.PHISH_BLOCK_THRESHOLD_UNSPECIFIED = "PHISH_BLOCK_THRESHOLD_UNSPECIFIED", e.BLOCK_LOW_AND_ABOVE = "BLOCK_LOW_AND_ABOVE", e.BLOCK_MEDIUM_AND_ABOVE = "BLOCK_MEDIUM_AND_ABOVE", e.BLOCK_HIGH_AND_ABOVE = "BLOCK_HIGH_AND_ABOVE", e.BLOCK_HIGHER_AND_ABOVE = "BLOCK_HIGHER_AND_ABOVE", e.BLOCK_VERY_HIGH_AND_ABOVE = "BLOCK_VERY_HIGH_AND_ABOVE", e.BLOCK_ONLY_EXTREMELY_HIGH = "BLOCK_ONLY_EXTREMELY_HIGH";
})(aE || (aE = {}));
var lE;
(function(e) {
  e.UNSPECIFIED = "UNSPECIFIED", e.BLOCKING = "BLOCKING", e.NON_BLOCKING = "NON_BLOCKING";
})(lE || (lE = {}));
var uE;
(function(e) {
  e.MODE_UNSPECIFIED = "MODE_UNSPECIFIED", e.MODE_DYNAMIC = "MODE_DYNAMIC";
})(uE || (uE = {}));
var Wy;
(function(e) {
  e.MODE_UNSPECIFIED = "MODE_UNSPECIFIED", e.AUTO = "AUTO", e.ANY = "ANY", e.NONE = "NONE", e.VALIDATED = "VALIDATED";
})(Wy || (Wy = {}));
var mc;
(function(e) {
  e.THINKING_LEVEL_UNSPECIFIED = "THINKING_LEVEL_UNSPECIFIED", e.MINIMAL = "MINIMAL", e.LOW = "LOW", e.MEDIUM = "MEDIUM", e.HIGH = "HIGH";
})(mc || (mc = {}));
var cE;
(function(e) {
  e.DONT_ALLOW = "DONT_ALLOW", e.ALLOW_ADULT = "ALLOW_ADULT", e.ALLOW_ALL = "ALLOW_ALL";
})(cE || (cE = {}));
var dE;
(function(e) {
  e.PROMINENT_PEOPLE_UNSPECIFIED = "PROMINENT_PEOPLE_UNSPECIFIED", e.ALLOW_PROMINENT_PEOPLE = "ALLOW_PROMINENT_PEOPLE", e.BLOCK_PROMINENT_PEOPLE = "BLOCK_PROMINENT_PEOPLE";
})(dE || (dE = {}));
var fE;
(function(e) {
  e.HARM_CATEGORY_UNSPECIFIED = "HARM_CATEGORY_UNSPECIFIED", e.HARM_CATEGORY_HARASSMENT = "HARM_CATEGORY_HARASSMENT", e.HARM_CATEGORY_HATE_SPEECH = "HARM_CATEGORY_HATE_SPEECH", e.HARM_CATEGORY_SEXUALLY_EXPLICIT = "HARM_CATEGORY_SEXUALLY_EXPLICIT", e.HARM_CATEGORY_DANGEROUS_CONTENT = "HARM_CATEGORY_DANGEROUS_CONTENT", e.HARM_CATEGORY_CIVIC_INTEGRITY = "HARM_CATEGORY_CIVIC_INTEGRITY", e.HARM_CATEGORY_IMAGE_HATE = "HARM_CATEGORY_IMAGE_HATE", e.HARM_CATEGORY_IMAGE_DANGEROUS_CONTENT = "HARM_CATEGORY_IMAGE_DANGEROUS_CONTENT", e.HARM_CATEGORY_IMAGE_HARASSMENT = "HARM_CATEGORY_IMAGE_HARASSMENT", e.HARM_CATEGORY_IMAGE_SEXUALLY_EXPLICIT = "HARM_CATEGORY_IMAGE_SEXUALLY_EXPLICIT", e.HARM_CATEGORY_JAILBREAK = "HARM_CATEGORY_JAILBREAK";
})(fE || (fE = {}));
var hE;
(function(e) {
  e.HARM_BLOCK_METHOD_UNSPECIFIED = "HARM_BLOCK_METHOD_UNSPECIFIED", e.SEVERITY = "SEVERITY", e.PROBABILITY = "PROBABILITY";
})(hE || (hE = {}));
var pE;
(function(e) {
  e.HARM_BLOCK_THRESHOLD_UNSPECIFIED = "HARM_BLOCK_THRESHOLD_UNSPECIFIED", e.BLOCK_LOW_AND_ABOVE = "BLOCK_LOW_AND_ABOVE", e.BLOCK_MEDIUM_AND_ABOVE = "BLOCK_MEDIUM_AND_ABOVE", e.BLOCK_ONLY_HIGH = "BLOCK_ONLY_HIGH", e.BLOCK_NONE = "BLOCK_NONE", e.OFF = "OFF";
})(pE || (pE = {}));
var mE;
(function(e) {
  e.FINISH_REASON_UNSPECIFIED = "FINISH_REASON_UNSPECIFIED", e.STOP = "STOP", e.MAX_TOKENS = "MAX_TOKENS", e.SAFETY = "SAFETY", e.RECITATION = "RECITATION", e.LANGUAGE = "LANGUAGE", e.OTHER = "OTHER", e.BLOCKLIST = "BLOCKLIST", e.PROHIBITED_CONTENT = "PROHIBITED_CONTENT", e.SPII = "SPII", e.MALFORMED_FUNCTION_CALL = "MALFORMED_FUNCTION_CALL", e.IMAGE_SAFETY = "IMAGE_SAFETY", e.UNEXPECTED_TOOL_CALL = "UNEXPECTED_TOOL_CALL", e.IMAGE_PROHIBITED_CONTENT = "IMAGE_PROHIBITED_CONTENT", e.NO_IMAGE = "NO_IMAGE", e.IMAGE_RECITATION = "IMAGE_RECITATION", e.IMAGE_OTHER = "IMAGE_OTHER";
})(mE || (mE = {}));
var gE;
(function(e) {
  e.HARM_PROBABILITY_UNSPECIFIED = "HARM_PROBABILITY_UNSPECIFIED", e.NEGLIGIBLE = "NEGLIGIBLE", e.LOW = "LOW", e.MEDIUM = "MEDIUM", e.HIGH = "HIGH";
})(gE || (gE = {}));
var vE;
(function(e) {
  e.HARM_SEVERITY_UNSPECIFIED = "HARM_SEVERITY_UNSPECIFIED", e.HARM_SEVERITY_NEGLIGIBLE = "HARM_SEVERITY_NEGLIGIBLE", e.HARM_SEVERITY_LOW = "HARM_SEVERITY_LOW", e.HARM_SEVERITY_MEDIUM = "HARM_SEVERITY_MEDIUM", e.HARM_SEVERITY_HIGH = "HARM_SEVERITY_HIGH";
})(vE || (vE = {}));
var yE;
(function(e) {
  e.URL_RETRIEVAL_STATUS_UNSPECIFIED = "URL_RETRIEVAL_STATUS_UNSPECIFIED", e.URL_RETRIEVAL_STATUS_SUCCESS = "URL_RETRIEVAL_STATUS_SUCCESS", e.URL_RETRIEVAL_STATUS_ERROR = "URL_RETRIEVAL_STATUS_ERROR", e.URL_RETRIEVAL_STATUS_PAYWALL = "URL_RETRIEVAL_STATUS_PAYWALL", e.URL_RETRIEVAL_STATUS_UNSAFE = "URL_RETRIEVAL_STATUS_UNSAFE";
})(yE || (yE = {}));
var _E;
(function(e) {
  e.BLOCKED_REASON_UNSPECIFIED = "BLOCKED_REASON_UNSPECIFIED", e.SAFETY = "SAFETY", e.OTHER = "OTHER", e.BLOCKLIST = "BLOCKLIST", e.PROHIBITED_CONTENT = "PROHIBITED_CONTENT", e.IMAGE_SAFETY = "IMAGE_SAFETY", e.MODEL_ARMOR = "MODEL_ARMOR", e.JAILBREAK = "JAILBREAK";
})(_E || (_E = {}));
var bE;
(function(e) {
  e.TRAFFIC_TYPE_UNSPECIFIED = "TRAFFIC_TYPE_UNSPECIFIED", e.ON_DEMAND = "ON_DEMAND", e.ON_DEMAND_PRIORITY = "ON_DEMAND_PRIORITY", e.ON_DEMAND_FLEX = "ON_DEMAND_FLEX", e.PROVISIONED_THROUGHPUT = "PROVISIONED_THROUGHPUT";
})(bE || (bE = {}));
var ip;
(function(e) {
  e.MODALITY_UNSPECIFIED = "MODALITY_UNSPECIFIED", e.TEXT = "TEXT", e.IMAGE = "IMAGE", e.AUDIO = "AUDIO", e.VIDEO = "VIDEO";
})(ip || (ip = {}));
var wE;
(function(e) {
  e.MODEL_STAGE_UNSPECIFIED = "MODEL_STAGE_UNSPECIFIED", e.UNSTABLE_EXPERIMENTAL = "UNSTABLE_EXPERIMENTAL", e.EXPERIMENTAL = "EXPERIMENTAL", e.PREVIEW = "PREVIEW", e.STABLE = "STABLE", e.LEGACY = "LEGACY", e.DEPRECATED = "DEPRECATED", e.RETIRED = "RETIRED";
})(wE || (wE = {}));
var SE;
(function(e) {
  e.MEDIA_RESOLUTION_UNSPECIFIED = "MEDIA_RESOLUTION_UNSPECIFIED", e.MEDIA_RESOLUTION_LOW = "MEDIA_RESOLUTION_LOW", e.MEDIA_RESOLUTION_MEDIUM = "MEDIA_RESOLUTION_MEDIUM", e.MEDIA_RESOLUTION_HIGH = "MEDIA_RESOLUTION_HIGH";
})(SE || (SE = {}));
var TE;
(function(e) {
  e.TUNING_MODE_UNSPECIFIED = "TUNING_MODE_UNSPECIFIED", e.TUNING_MODE_FULL = "TUNING_MODE_FULL", e.TUNING_MODE_PEFT_ADAPTER = "TUNING_MODE_PEFT_ADAPTER";
})(TE || (TE = {}));
var EE;
(function(e) {
  e.ADAPTER_SIZE_UNSPECIFIED = "ADAPTER_SIZE_UNSPECIFIED", e.ADAPTER_SIZE_ONE = "ADAPTER_SIZE_ONE", e.ADAPTER_SIZE_TWO = "ADAPTER_SIZE_TWO", e.ADAPTER_SIZE_FOUR = "ADAPTER_SIZE_FOUR", e.ADAPTER_SIZE_EIGHT = "ADAPTER_SIZE_EIGHT", e.ADAPTER_SIZE_SIXTEEN = "ADAPTER_SIZE_SIXTEEN", e.ADAPTER_SIZE_THIRTY_TWO = "ADAPTER_SIZE_THIRTY_TWO";
})(EE || (EE = {}));
var Vy;
(function(e) {
  e.JOB_STATE_UNSPECIFIED = "JOB_STATE_UNSPECIFIED", e.JOB_STATE_QUEUED = "JOB_STATE_QUEUED", e.JOB_STATE_PENDING = "JOB_STATE_PENDING", e.JOB_STATE_RUNNING = "JOB_STATE_RUNNING", e.JOB_STATE_SUCCEEDED = "JOB_STATE_SUCCEEDED", e.JOB_STATE_FAILED = "JOB_STATE_FAILED", e.JOB_STATE_CANCELLING = "JOB_STATE_CANCELLING", e.JOB_STATE_CANCELLED = "JOB_STATE_CANCELLED", e.JOB_STATE_PAUSED = "JOB_STATE_PAUSED", e.JOB_STATE_EXPIRED = "JOB_STATE_EXPIRED", e.JOB_STATE_UPDATING = "JOB_STATE_UPDATING", e.JOB_STATE_PARTIALLY_SUCCEEDED = "JOB_STATE_PARTIALLY_SUCCEEDED";
})(Vy || (Vy = {}));
var CE;
(function(e) {
  e.TUNING_JOB_STATE_UNSPECIFIED = "TUNING_JOB_STATE_UNSPECIFIED", e.TUNING_JOB_STATE_WAITING_FOR_QUOTA = "TUNING_JOB_STATE_WAITING_FOR_QUOTA", e.TUNING_JOB_STATE_PROCESSING_DATASET = "TUNING_JOB_STATE_PROCESSING_DATASET", e.TUNING_JOB_STATE_WAITING_FOR_CAPACITY = "TUNING_JOB_STATE_WAITING_FOR_CAPACITY", e.TUNING_JOB_STATE_TUNING = "TUNING_JOB_STATE_TUNING", e.TUNING_JOB_STATE_POST_PROCESSING = "TUNING_JOB_STATE_POST_PROCESSING";
})(CE || (CE = {}));
var AE;
(function(e) {
  e.AGGREGATION_METRIC_UNSPECIFIED = "AGGREGATION_METRIC_UNSPECIFIED", e.AVERAGE = "AVERAGE", e.MODE = "MODE", e.STANDARD_DEVIATION = "STANDARD_DEVIATION", e.VARIANCE = "VARIANCE", e.MINIMUM = "MINIMUM", e.MAXIMUM = "MAXIMUM", e.MEDIAN = "MEDIAN", e.PERCENTILE_P90 = "PERCENTILE_P90", e.PERCENTILE_P95 = "PERCENTILE_P95", e.PERCENTILE_P99 = "PERCENTILE_P99";
})(AE || (AE = {}));
var IE;
(function(e) {
  e.PAIRWISE_CHOICE_UNSPECIFIED = "PAIRWISE_CHOICE_UNSPECIFIED", e.BASELINE = "BASELINE", e.CANDIDATE = "CANDIDATE", e.TIE = "TIE";
})(IE || (IE = {}));
var xE;
(function(e) {
  e.TUNING_TASK_UNSPECIFIED = "TUNING_TASK_UNSPECIFIED", e.TUNING_TASK_I2V = "TUNING_TASK_I2V", e.TUNING_TASK_T2V = "TUNING_TASK_T2V", e.TUNING_TASK_R2V = "TUNING_TASK_R2V";
})(xE || (xE = {}));
var kE;
(function(e) {
  e.STATE_UNSPECIFIED = "STATE_UNSPECIFIED", e.STATE_PENDING = "STATE_PENDING", e.STATE_ACTIVE = "STATE_ACTIVE", e.STATE_FAILED = "STATE_FAILED";
})(kE || (kE = {}));
var ME;
(function(e) {
  e.MEDIA_RESOLUTION_UNSPECIFIED = "MEDIA_RESOLUTION_UNSPECIFIED", e.MEDIA_RESOLUTION_LOW = "MEDIA_RESOLUTION_LOW", e.MEDIA_RESOLUTION_MEDIUM = "MEDIA_RESOLUTION_MEDIUM", e.MEDIA_RESOLUTION_HIGH = "MEDIA_RESOLUTION_HIGH", e.MEDIA_RESOLUTION_ULTRA_HIGH = "MEDIA_RESOLUTION_ULTRA_HIGH";
})(ME || (ME = {}));
var PE;
(function(e) {
  e.TOOL_TYPE_UNSPECIFIED = "TOOL_TYPE_UNSPECIFIED", e.GOOGLE_SEARCH_WEB = "GOOGLE_SEARCH_WEB", e.GOOGLE_SEARCH_IMAGE = "GOOGLE_SEARCH_IMAGE", e.URL_CONTEXT = "URL_CONTEXT", e.GOOGLE_MAPS = "GOOGLE_MAPS", e.FILE_SEARCH = "FILE_SEARCH";
})(PE || (PE = {}));
var Ky;
(function(e) {
  e.COLLECTION = "COLLECTION";
})(Ky || (Ky = {}));
var RE;
(function(e) {
  e.UNSPECIFIED = "unspecified", e.FLEX = "flex", e.STANDARD = "standard", e.PRIORITY = "priority";
})(RE || (RE = {}));
var NE;
(function(e) {
  e.FEATURE_SELECTION_PREFERENCE_UNSPECIFIED = "FEATURE_SELECTION_PREFERENCE_UNSPECIFIED", e.PRIORITIZE_QUALITY = "PRIORITIZE_QUALITY", e.BALANCED = "BALANCED", e.PRIORITIZE_COST = "PRIORITIZE_COST";
})(NE || (NE = {}));
var ap;
(function(e) {
  e.PREDICT = "PREDICT", e.EMBED_CONTENT = "EMBED_CONTENT";
})(ap || (ap = {}));
var $E;
(function(e) {
  e.BLOCK_LOW_AND_ABOVE = "BLOCK_LOW_AND_ABOVE", e.BLOCK_MEDIUM_AND_ABOVE = "BLOCK_MEDIUM_AND_ABOVE", e.BLOCK_ONLY_HIGH = "BLOCK_ONLY_HIGH", e.BLOCK_NONE = "BLOCK_NONE";
})($E || ($E = {}));
var DE;
(function(e) {
  e.auto = "auto", e.en = "en", e.ja = "ja", e.ko = "ko", e.hi = "hi", e.zh = "zh", e.pt = "pt", e.es = "es";
})(DE || (DE = {}));
var LE;
(function(e) {
  e.MASK_MODE_DEFAULT = "MASK_MODE_DEFAULT", e.MASK_MODE_USER_PROVIDED = "MASK_MODE_USER_PROVIDED", e.MASK_MODE_BACKGROUND = "MASK_MODE_BACKGROUND", e.MASK_MODE_FOREGROUND = "MASK_MODE_FOREGROUND", e.MASK_MODE_SEMANTIC = "MASK_MODE_SEMANTIC";
})(LE || (LE = {}));
var OE;
(function(e) {
  e.CONTROL_TYPE_DEFAULT = "CONTROL_TYPE_DEFAULT", e.CONTROL_TYPE_CANNY = "CONTROL_TYPE_CANNY", e.CONTROL_TYPE_SCRIBBLE = "CONTROL_TYPE_SCRIBBLE", e.CONTROL_TYPE_FACE_MESH = "CONTROL_TYPE_FACE_MESH";
})(OE || (OE = {}));
var FE;
(function(e) {
  e.SUBJECT_TYPE_DEFAULT = "SUBJECT_TYPE_DEFAULT", e.SUBJECT_TYPE_PERSON = "SUBJECT_TYPE_PERSON", e.SUBJECT_TYPE_ANIMAL = "SUBJECT_TYPE_ANIMAL", e.SUBJECT_TYPE_PRODUCT = "SUBJECT_TYPE_PRODUCT";
})(FE || (FE = {}));
var BE;
(function(e) {
  e.EDIT_MODE_DEFAULT = "EDIT_MODE_DEFAULT", e.EDIT_MODE_INPAINT_REMOVAL = "EDIT_MODE_INPAINT_REMOVAL", e.EDIT_MODE_INPAINT_INSERTION = "EDIT_MODE_INPAINT_INSERTION", e.EDIT_MODE_OUTPAINT = "EDIT_MODE_OUTPAINT", e.EDIT_MODE_CONTROLLED_EDITING = "EDIT_MODE_CONTROLLED_EDITING", e.EDIT_MODE_STYLE = "EDIT_MODE_STYLE", e.EDIT_MODE_BGSWAP = "EDIT_MODE_BGSWAP", e.EDIT_MODE_PRODUCT_IMAGE = "EDIT_MODE_PRODUCT_IMAGE";
})(BE || (BE = {}));
var UE;
(function(e) {
  e.FOREGROUND = "FOREGROUND", e.BACKGROUND = "BACKGROUND", e.PROMPT = "PROMPT", e.SEMANTIC = "SEMANTIC", e.INTERACTIVE = "INTERACTIVE";
})(UE || (UE = {}));
var qE;
(function(e) {
  e.ASSET = "ASSET", e.STYLE = "STYLE";
})(qE || (qE = {}));
var HE;
(function(e) {
  e.INSERT = "INSERT", e.REMOVE = "REMOVE", e.REMOVE_STATIC = "REMOVE_STATIC", e.OUTPAINT = "OUTPAINT";
})(HE || (HE = {}));
var GE;
(function(e) {
  e.OPTIMIZED = "OPTIMIZED", e.LOSSLESS = "LOSSLESS";
})(GE || (GE = {}));
var WE;
(function(e) {
  e.SUPERVISED_FINE_TUNING = "SUPERVISED_FINE_TUNING", e.PREFERENCE_TUNING = "PREFERENCE_TUNING", e.DISTILLATION = "DISTILLATION";
})(WE || (WE = {}));
var VE;
(function(e) {
  e.STATE_UNSPECIFIED = "STATE_UNSPECIFIED", e.PROCESSING = "PROCESSING", e.ACTIVE = "ACTIVE", e.FAILED = "FAILED";
})(VE || (VE = {}));
var KE;
(function(e) {
  e.SOURCE_UNSPECIFIED = "SOURCE_UNSPECIFIED", e.UPLOADED = "UPLOADED", e.GENERATED = "GENERATED", e.REGISTERED = "REGISTERED";
})(KE || (KE = {}));
var zE;
(function(e) {
  e.TURN_COMPLETE_REASON_UNSPECIFIED = "TURN_COMPLETE_REASON_UNSPECIFIED", e.MALFORMED_FUNCTION_CALL = "MALFORMED_FUNCTION_CALL", e.RESPONSE_REJECTED = "RESPONSE_REJECTED", e.NEED_MORE_INPUT = "NEED_MORE_INPUT", e.PROHIBITED_INPUT_CONTENT = "PROHIBITED_INPUT_CONTENT", e.IMAGE_PROHIBITED_INPUT_CONTENT = "IMAGE_PROHIBITED_INPUT_CONTENT", e.INPUT_TEXT_CONTAIN_PROMINENT_PERSON_PROHIBITED = "INPUT_TEXT_CONTAIN_PROMINENT_PERSON_PROHIBITED", e.INPUT_IMAGE_CELEBRITY = "INPUT_IMAGE_CELEBRITY", e.INPUT_IMAGE_PHOTO_REALISTIC_CHILD_PROHIBITED = "INPUT_IMAGE_PHOTO_REALISTIC_CHILD_PROHIBITED", e.INPUT_TEXT_NCII_PROHIBITED = "INPUT_TEXT_NCII_PROHIBITED", e.INPUT_OTHER = "INPUT_OTHER", e.INPUT_IP_PROHIBITED = "INPUT_IP_PROHIBITED", e.BLOCKLIST = "BLOCKLIST", e.UNSAFE_PROMPT_FOR_IMAGE_GENERATION = "UNSAFE_PROMPT_FOR_IMAGE_GENERATION", e.GENERATED_IMAGE_SAFETY = "GENERATED_IMAGE_SAFETY", e.GENERATED_CONTENT_SAFETY = "GENERATED_CONTENT_SAFETY", e.GENERATED_AUDIO_SAFETY = "GENERATED_AUDIO_SAFETY", e.GENERATED_VIDEO_SAFETY = "GENERATED_VIDEO_SAFETY", e.GENERATED_CONTENT_PROHIBITED = "GENERATED_CONTENT_PROHIBITED", e.GENERATED_CONTENT_BLOCKLIST = "GENERATED_CONTENT_BLOCKLIST", e.GENERATED_IMAGE_PROHIBITED = "GENERATED_IMAGE_PROHIBITED", e.GENERATED_IMAGE_CELEBRITY = "GENERATED_IMAGE_CELEBRITY", e.GENERATED_IMAGE_PROMINENT_PEOPLE_DETECTED_BY_REWRITER = "GENERATED_IMAGE_PROMINENT_PEOPLE_DETECTED_BY_REWRITER", e.GENERATED_IMAGE_IDENTIFIABLE_PEOPLE = "GENERATED_IMAGE_IDENTIFIABLE_PEOPLE", e.GENERATED_IMAGE_MINORS = "GENERATED_IMAGE_MINORS", e.OUTPUT_IMAGE_IP_PROHIBITED = "OUTPUT_IMAGE_IP_PROHIBITED", e.GENERATED_OTHER = "GENERATED_OTHER", e.MAX_REGENERATION_REACHED = "MAX_REGENERATION_REACHED";
})(zE || (zE = {}));
var JE;
(function(e) {
  e.MODALITY_UNSPECIFIED = "MODALITY_UNSPECIFIED", e.TEXT = "TEXT", e.IMAGE = "IMAGE", e.VIDEO = "VIDEO", e.AUDIO = "AUDIO", e.DOCUMENT = "DOCUMENT";
})(JE || (JE = {}));
var YE;
(function(e) {
  e.VAD_SIGNAL_TYPE_UNSPECIFIED = "VAD_SIGNAL_TYPE_UNSPECIFIED", e.VAD_SIGNAL_TYPE_SOS = "VAD_SIGNAL_TYPE_SOS", e.VAD_SIGNAL_TYPE_EOS = "VAD_SIGNAL_TYPE_EOS";
})(YE || (YE = {}));
var XE;
(function(e) {
  e.TYPE_UNSPECIFIED = "TYPE_UNSPECIFIED", e.ACTIVITY_START = "ACTIVITY_START", e.ACTIVITY_END = "ACTIVITY_END";
})(XE || (XE = {}));
var QE;
(function(e) {
  e.START_SENSITIVITY_UNSPECIFIED = "START_SENSITIVITY_UNSPECIFIED", e.START_SENSITIVITY_HIGH = "START_SENSITIVITY_HIGH", e.START_SENSITIVITY_LOW = "START_SENSITIVITY_LOW";
})(QE || (QE = {}));
var ZE;
(function(e) {
  e.END_SENSITIVITY_UNSPECIFIED = "END_SENSITIVITY_UNSPECIFIED", e.END_SENSITIVITY_HIGH = "END_SENSITIVITY_HIGH", e.END_SENSITIVITY_LOW = "END_SENSITIVITY_LOW";
})(ZE || (ZE = {}));
var jE;
(function(e) {
  e.ACTIVITY_HANDLING_UNSPECIFIED = "ACTIVITY_HANDLING_UNSPECIFIED", e.START_OF_ACTIVITY_INTERRUPTS = "START_OF_ACTIVITY_INTERRUPTS", e.NO_INTERRUPTION = "NO_INTERRUPTION";
})(jE || (jE = {}));
var eC;
(function(e) {
  e.TURN_COVERAGE_UNSPECIFIED = "TURN_COVERAGE_UNSPECIFIED", e.TURN_INCLUDES_ONLY_ACTIVITY = "TURN_INCLUDES_ONLY_ACTIVITY", e.TURN_INCLUDES_ALL_INPUT = "TURN_INCLUDES_ALL_INPUT", e.TURN_INCLUDES_AUDIO_ACTIVITY_AND_ALL_VIDEO = "TURN_INCLUDES_AUDIO_ACTIVITY_AND_ALL_VIDEO";
})(eC || (eC = {}));
var tC;
(function(e) {
  e.SCALE_UNSPECIFIED = "SCALE_UNSPECIFIED", e.C_MAJOR_A_MINOR = "C_MAJOR_A_MINOR", e.D_FLAT_MAJOR_B_FLAT_MINOR = "D_FLAT_MAJOR_B_FLAT_MINOR", e.D_MAJOR_B_MINOR = "D_MAJOR_B_MINOR", e.E_FLAT_MAJOR_C_MINOR = "E_FLAT_MAJOR_C_MINOR", e.E_MAJOR_D_FLAT_MINOR = "E_MAJOR_D_FLAT_MINOR", e.F_MAJOR_D_MINOR = "F_MAJOR_D_MINOR", e.G_FLAT_MAJOR_E_FLAT_MINOR = "G_FLAT_MAJOR_E_FLAT_MINOR", e.G_MAJOR_E_MINOR = "G_MAJOR_E_MINOR", e.A_FLAT_MAJOR_F_MINOR = "A_FLAT_MAJOR_F_MINOR", e.A_MAJOR_G_FLAT_MINOR = "A_MAJOR_G_FLAT_MINOR", e.B_FLAT_MAJOR_G_MINOR = "B_FLAT_MAJOR_G_MINOR", e.B_MAJOR_A_FLAT_MINOR = "B_MAJOR_A_FLAT_MINOR";
})(tC || (tC = {}));
var nC;
(function(e) {
  e.MUSIC_GENERATION_MODE_UNSPECIFIED = "MUSIC_GENERATION_MODE_UNSPECIFIED", e.QUALITY = "QUALITY", e.DIVERSITY = "DIVERSITY", e.VOCALIZATION = "VOCALIZATION";
})(nC || (nC = {}));
var Za;
(function(e) {
  e.PLAYBACK_CONTROL_UNSPECIFIED = "PLAYBACK_CONTROL_UNSPECIFIED", e.PLAY = "PLAY", e.PAUSE = "PAUSE", e.STOP = "STOP", e.RESET_CONTEXT = "RESET_CONTEXT";
})(Za || (Za = {}));
var zy = class {
  constructor(e) {
    const t = {};
    for (const n of e.headers.entries()) t[n[0]] = n[1];
    this.headers = t, this.responseInternal = e;
  }
  json() {
    return this.responseInternal.json();
  }
}, Mu = class {
  get text() {
    var e, t, n, r, s, o, i, a;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning text from the first one.");
    let l = "", u = !1;
    const d = [];
    for (const h of (a = (i = (o = (s = this.candidates) === null || s === void 0 ? void 0 : s[0]) === null || o === void 0 ? void 0 : o.content) === null || i === void 0 ? void 0 : i.parts) !== null && a !== void 0 ? a : []) {
      for (const [p, y] of Object.entries(h)) p !== "text" && p !== "thought" && p !== "thoughtSignature" && (y !== null || y !== void 0) && d.push(p);
      if (typeof h.text == "string") {
        if (typeof h.thought == "boolean" && h.thought) continue;
        u = !0, l += h.text;
      }
    }
    return d.length > 0 && console.warn(`there are non-text parts ${d} in the response, returning concatenation of all text parts. Please refer to the non text parts for a full response from model.`), u ? l : void 0;
  }
  get data() {
    var e, t, n, r, s, o, i, a;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning data from the first one.");
    let l = "";
    const u = [];
    for (const d of (a = (i = (o = (s = this.candidates) === null || s === void 0 ? void 0 : s[0]) === null || o === void 0 ? void 0 : o.content) === null || i === void 0 ? void 0 : i.parts) !== null && a !== void 0 ? a : []) {
      for (const [h, p] of Object.entries(d)) h !== "inlineData" && (p !== null || p !== void 0) && u.push(h);
      d.inlineData && typeof d.inlineData.data == "string" && (l += atob(d.inlineData.data));
    }
    return u.length > 0 && console.warn(`there are non-data parts ${u} in the response, returning concatenation of all data parts. Please refer to the non data parts for a full response from model.`), l.length > 0 ? btoa(l) : void 0;
  }
  get functionCalls() {
    var e, t, n, r, s, o, i, a;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning function calls from the first one.");
    const l = (a = (i = (o = (s = this.candidates) === null || s === void 0 ? void 0 : s[0]) === null || o === void 0 ? void 0 : o.content) === null || i === void 0 ? void 0 : i.parts) === null || a === void 0 ? void 0 : a.filter((u) => u.functionCall).map((u) => u.functionCall).filter((u) => u !== void 0);
    if (l?.length !== 0)
      return l;
  }
  get executableCode() {
    var e, t, n, r, s, o, i, a, l;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning executable code from the first one.");
    const u = (a = (i = (o = (s = this.candidates) === null || s === void 0 ? void 0 : s[0]) === null || o === void 0 ? void 0 : o.content) === null || i === void 0 ? void 0 : i.parts) === null || a === void 0 ? void 0 : a.filter((d) => d.executableCode).map((d) => d.executableCode).filter((d) => d !== void 0);
    if (u?.length !== 0)
      return (l = u?.[0]) === null || l === void 0 ? void 0 : l.code;
  }
  get codeExecutionResult() {
    var e, t, n, r, s, o, i, a, l;
    if (((r = (n = (t = (e = this.candidates) === null || e === void 0 ? void 0 : e[0]) === null || t === void 0 ? void 0 : t.content) === null || n === void 0 ? void 0 : n.parts) === null || r === void 0 ? void 0 : r.length) === 0) return;
    this.candidates && this.candidates.length > 1 && console.warn("there are multiple candidates in the response, returning code execution result from the first one.");
    const u = (a = (i = (o = (s = this.candidates) === null || s === void 0 ? void 0 : s[0]) === null || o === void 0 ? void 0 : o.content) === null || i === void 0 ? void 0 : i.parts) === null || a === void 0 ? void 0 : a.filter((d) => d.codeExecutionResult).map((d) => d.codeExecutionResult).filter((d) => d !== void 0);
    if (u?.length !== 0)
      return (l = u?.[0]) === null || l === void 0 ? void 0 : l.output;
  }
}, rC = class {
}, sC = class {
}, ZW = class {
}, jW = class {
}, e4 = class {
}, t4 = class {
}, oC = class {
}, iC = class {
}, aC = class {
}, n4 = class {
}, lC = class aP {
  _fromAPIResponse({ apiResponse: t, _isVertexAI: n }) {
    const r = new aP();
    let s;
    const o = t;
    return n ? s = UW(o) : s = BW(o), Object.assign(r, s), r;
  }
}, uC = class {
}, cC = class {
}, dC = class {
}, fC = class {
}, r4 = class {
}, s4 = class {
}, o4 = class {
}, i4 = class lP {
  _fromAPIResponse({ apiResponse: t, _isVertexAI: n }) {
    const r = new lP(), s = zW(t);
    return Object.assign(r, s), r;
  }
}, a4 = class {
}, l4 = class {
}, u4 = class {
}, c4 = class {
}, hC = class {
}, d4 = class {
  get text() {
    var e, t, n;
    let r = "", s = !1;
    const o = [];
    for (const i of (n = (t = (e = this.serverContent) === null || e === void 0 ? void 0 : e.modelTurn) === null || t === void 0 ? void 0 : t.parts) !== null && n !== void 0 ? n : []) {
      for (const [a, l] of Object.entries(i)) a !== "text" && a !== "thought" && l !== null && o.push(a);
      if (typeof i.text == "string") {
        if (typeof i.thought == "boolean" && i.thought) continue;
        s = !0, r += i.text;
      }
    }
    return o.length > 0 && console.warn(`there are non-text parts ${o} in the response, returning concatenation of all text parts. Please refer to the non text parts for a full response from model.`), s ? r : void 0;
  }
  get data() {
    var e, t, n;
    let r = "";
    const s = [];
    for (const o of (n = (t = (e = this.serverContent) === null || e === void 0 ? void 0 : e.modelTurn) === null || t === void 0 ? void 0 : t.parts) !== null && n !== void 0 ? n : []) {
      for (const [i, a] of Object.entries(o)) i !== "inlineData" && a !== null && s.push(i);
      o.inlineData && typeof o.inlineData.data == "string" && (r += atob(o.inlineData.data));
    }
    return s.length > 0 && console.warn(`there are non-data parts ${s} in the response, returning concatenation of all data parts. Please refer to the non data parts for a full response from model.`), r.length > 0 ? btoa(r) : void 0;
  }
}, f4 = class {
  get audioChunk() {
    if (this.serverContent && this.serverContent.audioChunks && this.serverContent.audioChunks.length > 0) return this.serverContent.audioChunks[0];
  }
}, h4 = class uP {
  _fromAPIResponse({ apiResponse: t, _isVertexAI: n }) {
    const r = new uP(), s = iP(t);
    return Object.assign(r, s), r;
  }
};
function Rt(e, t) {
  if (!t || typeof t != "string") throw new Error("model is required and must be a string");
  if (t.includes("..") || t.includes("?") || t.includes("&")) throw new Error("invalid model parameter");
  if (e.isVertexAI()) {
    if (t.startsWith("publishers/") || t.startsWith("projects/") || t.startsWith("models/")) return t;
    if (t.indexOf("/") >= 0) {
      const n = t.split("/", 2);
      return `publishers/${n[0]}/models/${n[1]}`;
    } else return `publishers/google/models/${t}`;
  } else return t.startsWith("models/") || t.startsWith("tunedModels/") ? t : `models/${t}`;
}
function cP(e, t) {
  const n = Rt(e, t);
  return n ? n.startsWith("publishers/") && e.isVertexAI() ? `projects/${e.getProject()}/locations/${e.getLocation()}/${n}` : n.startsWith("models/") && e.isVertexAI() ? `projects/${e.getProject()}/locations/${e.getLocation()}/publishers/google/${n}` : n : "";
}
function dP(e) {
  return Array.isArray(e) ? e.map((t) => lp(t)) : [lp(e)];
}
function lp(e) {
  if (typeof e == "object" && e !== null) return e;
  throw new Error(`Could not parse input as Blob. Unsupported blob type: ${typeof e}`);
}
function fP(e) {
  const t = lp(e);
  if (t.mimeType && t.mimeType.startsWith("image/")) return t;
  throw new Error(`Unsupported mime type: ${t.mimeType}`);
}
function hP(e) {
  const t = lp(e);
  if (t.mimeType && t.mimeType.startsWith("audio/")) return t;
  throw new Error(`Unsupported mime type: ${t.mimeType}`);
}
function pC(e) {
  if (e == null) throw new Error("PartUnion is required");
  if (typeof e == "object") return e;
  if (typeof e == "string") return { text: e };
  throw new Error(`Unsupported part type: ${typeof e}`);
}
function pP(e) {
  if (e == null || Array.isArray(e) && e.length === 0) throw new Error("PartListUnion is required");
  return Array.isArray(e) ? e.map((t) => pC(t)) : [pC(e)];
}
function Jy(e) {
  return e != null && typeof e == "object" && "parts" in e && Array.isArray(e.parts);
}
function mC(e) {
  return e != null && typeof e == "object" && "functionCall" in e;
}
function gC(e) {
  return e != null && typeof e == "object" && "functionResponse" in e;
}
function bn(e) {
  if (e == null) throw new Error("ContentUnion is required");
  return Jy(e) ? e : {
    role: "user",
    parts: pP(e)
  };
}
function hw(e, t) {
  if (!t) return [];
  if (e.isVertexAI() && Array.isArray(t)) return t.flatMap((n) => {
    const r = bn(n);
    return r.parts && r.parts.length > 0 && r.parts[0].text !== void 0 ? [r.parts[0].text] : [];
  });
  if (e.isVertexAI()) {
    const n = bn(t);
    return n.parts && n.parts.length > 0 && n.parts[0].text !== void 0 ? [n.parts[0].text] : [];
  }
  return Array.isArray(t) ? t.map((n) => bn(n)) : [bn(t)];
}
function rr(e) {
  if (e == null || Array.isArray(e) && e.length === 0) throw new Error("contents are required");
  if (!Array.isArray(e)) {
    if (mC(e) || gC(e)) throw new Error("To specify functionCall or functionResponse parts, please wrap them in a Content object, specifying the role for them");
    return [bn(e)];
  }
  const t = [], n = [], r = Jy(e[0]);
  for (const s of e) {
    const o = Jy(s);
    if (o != r) throw new Error("Mixing Content and Parts is not supported, please group the parts into a the appropriate Content objects and specify the roles for them");
    if (o) t.push(s);
    else {
      if (mC(s) || gC(s)) throw new Error("To specify functionCall or functionResponse parts, please wrap them, and any other parts, in Content objects as appropriate, specifying the role for them");
      n.push(s);
    }
  }
  return r || t.push({
    role: "user",
    parts: pP(n)
  }), t;
}
function p4(e, t) {
  e.includes("null") && (t.nullable = !0);
  const n = e.filter((r) => r !== "null");
  if (n.length === 1) t.type = Object.values(Ho).includes(n[0].toUpperCase()) ? n[0].toUpperCase() : Ho.TYPE_UNSPECIFIED;
  else {
    t.anyOf = [];
    for (const r of n) t.anyOf.push({ type: Object.values(Ho).includes(r.toUpperCase()) ? r.toUpperCase() : Ho.TYPE_UNSPECIFIED });
  }
}
function hl(e) {
  const t = {}, n = ["items"], r = ["anyOf"], s = ["properties"];
  if (e.type && e.anyOf) throw new Error("type and anyOf cannot be both populated.");
  const o = e.anyOf;
  o != null && o.length == 2 && (o[0].type === "null" ? (t.nullable = !0, e = o[1]) : o[1].type === "null" && (t.nullable = !0, e = o[0])), e.type instanceof Array && p4(e.type, t);
  for (const [i, a] of Object.entries(e))
    if (a != null)
      if (i == "type") {
        if (a === "null") throw new Error("type: null can not be the only possible type for the field.");
        if (a instanceof Array) continue;
        t.type = Object.values(Ho).includes(a.toUpperCase()) ? a.toUpperCase() : Ho.TYPE_UNSPECIFIED;
      } else if (n.includes(i)) t[i] = hl(a);
      else if (r.includes(i)) {
        const l = [];
        for (const u of a) {
          if (u.type == "null") {
            t.nullable = !0;
            continue;
          }
          l.push(hl(u));
        }
        t[i] = l;
      } else if (s.includes(i)) {
        const l = {};
        for (const [u, d] of Object.entries(a)) l[u] = hl(d);
        t[i] = l;
      } else {
        if (i === "additionalProperties") continue;
        t[i] = a;
      }
  return t;
}
function pw(e) {
  return hl(e);
}
function mw(e) {
  if (typeof e == "object") return e;
  if (typeof e == "string") return { voiceConfig: { prebuiltVoiceConfig: { voiceName: e } } };
  throw new Error(`Unsupported speechConfig type: ${typeof e}`);
}
function gw(e) {
  if ("multiSpeakerVoiceConfig" in e) throw new Error("multiSpeakerVoiceConfig is not supported in the live API.");
  return e;
}
function Ll(e) {
  if (e.functionDeclarations) for (const t of e.functionDeclarations)
    t.parameters && (Object.keys(t.parameters).includes("$schema") ? t.parametersJsonSchema || (t.parametersJsonSchema = t.parameters, delete t.parameters) : t.parameters = hl(t.parameters)), t.response && (Object.keys(t.response).includes("$schema") ? t.responseJsonSchema || (t.responseJsonSchema = t.response, delete t.response) : t.response = hl(t.response));
  return e;
}
function Ol(e) {
  if (e == null) throw new Error("tools is required");
  if (!Array.isArray(e)) throw new Error("tools is required and must be an array of Tools");
  const t = [];
  for (const n of e) t.push(n);
  return t;
}
function m4(e, t, n, r = 1) {
  const s = !t.startsWith(`${n}/`) && t.split("/").length === r;
  return e.isVertexAI() ? t.startsWith("projects/") ? t : t.startsWith("locations/") ? `projects/${e.getProject()}/${t}` : t.startsWith(`${n}/`) ? `projects/${e.getProject()}/locations/${e.getLocation()}/${t}` : s ? `projects/${e.getProject()}/locations/${e.getLocation()}/${n}/${t}` : t : s ? `${n}/${t}` : t;
}
function _o(e, t) {
  if (typeof t != "string") throw new Error("name must be a string");
  return m4(e, t, "cachedContents");
}
function mP(e) {
  switch (e) {
    case "STATE_UNSPECIFIED":
      return "JOB_STATE_UNSPECIFIED";
    case "CREATING":
      return "JOB_STATE_RUNNING";
    case "ACTIVE":
      return "JOB_STATE_SUCCEEDED";
    case "FAILED":
      return "JOB_STATE_FAILED";
    default:
      return e;
  }
}
function ii(e) {
  return fw(e);
}
function g4(e) {
  return e != null && typeof e == "object" && "name" in e;
}
function v4(e) {
  return e != null && typeof e == "object" && "video" in e;
}
function y4(e) {
  return e != null && typeof e == "object" && "uri" in e;
}
function gP(e) {
  var t;
  let n;
  if (g4(e) && (n = e.name), !(y4(e) && (n = e.uri, n === void 0)) && !(v4(e) && (n = (t = e.video) === null || t === void 0 ? void 0 : t.uri, n === void 0))) {
    if (typeof e == "string" && (n = e), n === void 0) throw new Error("Could not extract file name from the provided input.");
    if (n.startsWith("https://")) {
      const r = n.split("files/")[1].match(/[a-z0-9]+/);
      if (r === null) throw new Error(`Could not extract file name from URI ${n}`);
      n = r[0];
    } else n.startsWith("files/") && (n = n.split("files/")[1]);
    return n;
  }
}
function vP(e, t) {
  let n;
  return e.isVertexAI() ? n = t ? "publishers/google/models" : "models" : n = t ? "models" : "tunedModels", n;
}
function yP(e) {
  for (const t of [
    "models",
    "tunedModels",
    "publisherModels"
  ]) if (_4(e, t)) return e[t];
  return [];
}
function _4(e, t) {
  return e !== null && typeof e == "object" && t in e;
}
function b4(e, t = {}) {
  const n = e, r = {
    name: n.name,
    description: n.description,
    parametersJsonSchema: n.inputSchema
  };
  return n.outputSchema && (r.responseJsonSchema = n.outputSchema), t.behavior && (r.behavior = t.behavior), { functionDeclarations: [r] };
}
function w4(e, t = {}) {
  const n = [], r = /* @__PURE__ */ new Set();
  for (const s of e) {
    const o = s.name;
    if (r.has(o)) throw new Error(`Duplicate function name ${o} found in MCP tools. Please ensure function names are unique.`);
    r.add(o);
    const i = b4(s, t);
    i.functionDeclarations && n.push(...i.functionDeclarations);
  }
  return { functionDeclarations: n };
}
function _P(e, t) {
  let n;
  if (typeof t == "string") if (e.isVertexAI()) if (t.startsWith("gs://")) n = {
    format: "jsonl",
    gcsUri: [t]
  };
  else if (t.startsWith("bq://")) n = {
    format: "bigquery",
    bigqueryUri: t
  };
  else throw new Error(`Unsupported string source for Vertex AI: ${t}`);
  else if (t.startsWith("files/")) n = { fileName: t };
  else throw new Error(`Unsupported string source for Gemini API: ${t}`);
  else if (Array.isArray(t)) {
    if (e.isVertexAI()) throw new Error("InlinedRequest[] is not supported in Vertex AI.");
    n = { inlinedRequests: t };
  } else n = t;
  const r = [n.gcsUri, n.bigqueryUri].filter(Boolean).length, s = [n.inlinedRequests, n.fileName].filter(Boolean).length;
  if (e.isVertexAI()) {
    if (s > 0 || r !== 1) throw new Error("Exactly one of `gcsUri` or `bigqueryUri` must be set for Vertex AI.");
  } else if (r > 0 || s !== 1) throw new Error("Exactly one of `inlinedRequests`, `fileName`, must be set for Gemini API.");
  return n;
}
function S4(e) {
  if (typeof e != "string") return e;
  const t = e;
  if (t.startsWith("gs://")) return {
    format: "jsonl",
    gcsUri: t
  };
  if (t.startsWith("bq://")) return {
    format: "bigquery",
    bigqueryUri: t
  };
  throw new Error(`Unsupported destination: ${t}`);
}
function bP(e) {
  if (typeof e != "object" || e === null) return {};
  const t = e, n = t.inlinedResponses;
  if (typeof n != "object" || n === null) return e;
  const r = n.inlinedResponses;
  if (!Array.isArray(r) || r.length === 0) return e;
  let s = !1;
  for (const o of r) {
    if (typeof o != "object" || o === null) continue;
    const i = o.response;
    if (!(typeof i != "object" || i === null) && i.embedding !== void 0) {
      s = !0;
      break;
    }
  }
  return s && (t.inlinedEmbedContentResponses = t.inlinedResponses, delete t.inlinedResponses), e;
}
function Fl(e, t) {
  const n = t;
  if (!e.isVertexAI()) {
    if (/batches\/[^/]+$/.test(n)) return n.split("/").pop();
    throw new Error(`Invalid batch job name: ${n}.`);
  }
  if (/^projects\/[^/]+\/locations\/[^/]+\/batchPredictionJobs\/[^/]+$/.test(n)) return n.split("/").pop();
  if (/^\d+$/.test(n)) return n;
  throw new Error(`Invalid batch job name: ${n}.`);
}
function wP(e) {
  const t = e;
  return t === "BATCH_STATE_UNSPECIFIED" ? "JOB_STATE_UNSPECIFIED" : t === "BATCH_STATE_PENDING" ? "JOB_STATE_PENDING" : t === "BATCH_STATE_RUNNING" ? "JOB_STATE_RUNNING" : t === "BATCH_STATE_SUCCEEDED" ? "JOB_STATE_SUCCEEDED" : t === "BATCH_STATE_FAILED" ? "JOB_STATE_FAILED" : t === "BATCH_STATE_CANCELLED" ? "JOB_STATE_CANCELLED" : t === "BATCH_STATE_EXPIRED" ? "JOB_STATE_EXPIRED" : t;
}
function T4(e) {
  return e.includes("gemini") && e !== "gemini-embedding-001" || e.includes("maas");
}
function E4(e) {
  const t = {}, n = c(e, ["apiKey"]);
  if (n != null && f(t, ["apiKey"], n), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return t;
}
function C4(e) {
  const t = {}, n = c(e, ["responsesFile"]);
  n != null && f(t, ["fileName"], n);
  const r = c(e, ["inlinedResponses", "inlinedResponses"]);
  if (r != null) {
    let o = r;
    Array.isArray(o) && (o = o.map((i) => oV(i))), f(t, ["inlinedResponses"], o);
  }
  const s = c(e, ["inlinedEmbedContentResponses", "inlinedResponses"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((i) => i)), f(t, ["inlinedEmbedContentResponses"], o);
  }
  return t;
}
function A4(e) {
  const t = {}, n = c(e, ["predictionsFormat"]);
  n != null && f(t, ["format"], n);
  const r = c(e, ["gcsDestination", "outputUriPrefix"]);
  r != null && f(t, ["gcsUri"], r);
  const s = c(e, ["bigqueryDestination", "outputUri"]);
  return s != null && f(t, ["bigqueryUri"], s), t;
}
function I4(e) {
  const t = {}, n = c(e, ["format"]);
  n != null && f(t, ["predictionsFormat"], n);
  const r = c(e, ["gcsUri"]);
  r != null && f(t, ["gcsDestination", "outputUriPrefix"], r);
  const s = c(e, ["bigqueryUri"]);
  if (s != null && f(t, ["bigqueryDestination", "outputUri"], s), c(e, ["fileName"]) !== void 0) throw new Error("fileName parameter is not supported in Vertex AI.");
  if (c(e, ["inlinedResponses"]) !== void 0) throw new Error("inlinedResponses parameter is not supported in Vertex AI.");
  if (c(e, ["inlinedEmbedContentResponses"]) !== void 0) throw new Error("inlinedEmbedContentResponses parameter is not supported in Vertex AI.");
  return t;
}
function sh(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && f(t, ["name"], n);
  const r = c(e, ["metadata", "displayName"]);
  r != null && f(t, ["displayName"], r);
  const s = c(e, ["metadata", "state"]);
  s != null && f(t, ["state"], wP(s));
  const o = c(e, ["metadata", "createTime"]);
  o != null && f(t, ["createTime"], o);
  const i = c(e, ["metadata", "endTime"]);
  i != null && f(t, ["endTime"], i);
  const a = c(e, ["metadata", "updateTime"]);
  a != null && f(t, ["updateTime"], a);
  const l = c(e, ["metadata", "model"]);
  l != null && f(t, ["model"], l);
  const u = c(e, ["metadata", "output"]);
  return u != null && f(t, ["dest"], C4(bP(u))), t;
}
function Yy(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && f(t, ["name"], n);
  const r = c(e, ["displayName"]);
  r != null && f(t, ["displayName"], r);
  const s = c(e, ["state"]);
  s != null && f(t, ["state"], wP(s));
  const o = c(e, ["error"]);
  o != null && f(t, ["error"], o);
  const i = c(e, ["createTime"]);
  i != null && f(t, ["createTime"], i);
  const a = c(e, ["startTime"]);
  a != null && f(t, ["startTime"], a);
  const l = c(e, ["endTime"]);
  l != null && f(t, ["endTime"], l);
  const u = c(e, ["updateTime"]);
  u != null && f(t, ["updateTime"], u);
  const d = c(e, ["model"]);
  d != null && f(t, ["model"], d);
  const h = c(e, ["inputConfig"]);
  h != null && f(t, ["src"], x4(h));
  const p = c(e, ["outputConfig"]);
  p != null && f(t, ["dest"], A4(bP(p)));
  const y = c(e, ["completionStats"]);
  return y != null && f(t, ["completionStats"], y), t;
}
function x4(e) {
  const t = {}, n = c(e, ["instancesFormat"]);
  n != null && f(t, ["format"], n);
  const r = c(e, ["gcsSource", "uris"]);
  r != null && f(t, ["gcsUri"], r);
  const s = c(e, ["bigquerySource", "inputUri"]);
  return s != null && f(t, ["bigqueryUri"], s), t;
}
function k4(e, t) {
  const n = {};
  if (c(t, ["format"]) !== void 0) throw new Error("format parameter is not supported in Gemini API.");
  if (c(t, ["gcsUri"]) !== void 0) throw new Error("gcsUri parameter is not supported in Gemini API.");
  if (c(t, ["bigqueryUri"]) !== void 0) throw new Error("bigqueryUri parameter is not supported in Gemini API.");
  const r = c(t, ["fileName"]);
  r != null && f(n, ["fileName"], r);
  const s = c(t, ["inlinedRequests"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((i) => sV(e, i))), f(n, ["requests", "requests"], o);
  }
  return n;
}
function M4(e) {
  const t = {}, n = c(e, ["format"]);
  n != null && f(t, ["instancesFormat"], n);
  const r = c(e, ["gcsUri"]);
  r != null && f(t, ["gcsSource", "uris"], r);
  const s = c(e, ["bigqueryUri"]);
  if (s != null && f(t, ["bigquerySource", "inputUri"], s), c(e, ["fileName"]) !== void 0) throw new Error("fileName parameter is not supported in Vertex AI.");
  if (c(e, ["inlinedRequests"]) !== void 0) throw new Error("inlinedRequests parameter is not supported in Vertex AI.");
  return t;
}
function P4(e) {
  const t = {}, n = c(e, ["data"]);
  if (n != null && f(t, ["data"], n), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["mimeType"]);
  return r != null && f(t, ["mimeType"], r), t;
}
function R4(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && f(n, ["_url", "name"], Fl(e, r)), n;
}
function N4(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && f(n, ["_url", "name"], Fl(e, r)), n;
}
function $4(e) {
  const t = {}, n = c(e, ["content"]);
  n != null && f(t, ["content"], n);
  const r = c(e, ["citationMetadata"]);
  r != null && f(t, ["citationMetadata"], D4(r));
  const s = c(e, ["tokenCount"]);
  s != null && f(t, ["tokenCount"], s);
  const o = c(e, ["finishReason"]);
  o != null && f(t, ["finishReason"], o);
  const i = c(e, ["groundingMetadata"]);
  i != null && f(t, ["groundingMetadata"], i);
  const a = c(e, ["avgLogprobs"]);
  a != null && f(t, ["avgLogprobs"], a);
  const l = c(e, ["index"]);
  l != null && f(t, ["index"], l);
  const u = c(e, ["logprobsResult"]);
  u != null && f(t, ["logprobsResult"], u);
  const d = c(e, ["safetyRatings"]);
  if (d != null) {
    let p = d;
    Array.isArray(p) && (p = p.map((y) => y)), f(t, ["safetyRatings"], p);
  }
  const h = c(e, ["urlContextMetadata"]);
  return h != null && f(t, ["urlContextMetadata"], h), t;
}
function D4(e) {
  const t = {}, n = c(e, ["citationSources"]);
  if (n != null) {
    let r = n;
    Array.isArray(r) && (r = r.map((s) => s)), f(t, ["citations"], r);
  }
  return t;
}
function SP(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((o) => fV(o))), f(t, ["parts"], s);
  }
  const r = c(e, ["role"]);
  return r != null && f(t, ["role"], r), t;
}
function L4(e, t) {
  const n = {}, r = c(e, ["displayName"]);
  if (t !== void 0 && r != null && f(t, ["batch", "displayName"], r), c(e, ["dest"]) !== void 0) throw new Error("dest parameter is not supported in Gemini API.");
  const s = c(e, ["webhookConfig"]);
  return t !== void 0 && s != null && f(t, ["batch", "webhookConfig"], s), n;
}
function O4(e, t) {
  const n = {}, r = c(e, ["displayName"]);
  t !== void 0 && r != null && f(t, ["displayName"], r);
  const s = c(e, ["dest"]);
  if (t !== void 0 && s != null && f(t, ["outputConfig"], I4(S4(s))), c(e, ["webhookConfig"]) !== void 0) throw new Error("webhookConfig parameter is not supported in Vertex AI.");
  return n;
}
function vC(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && f(n, ["_url", "model"], Rt(e, r));
  const s = c(t, ["src"]);
  s != null && f(n, ["batch", "inputConfig"], k4(e, _P(e, s)));
  const o = c(t, ["config"]);
  return o != null && L4(o, n), n;
}
function F4(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && f(n, ["model"], Rt(e, r));
  const s = c(t, ["src"]);
  s != null && f(n, ["inputConfig"], M4(_P(e, s)));
  const o = c(t, ["config"]);
  return o != null && O4(o, n), n;
}
function B4(e, t) {
  const n = {}, r = c(e, ["displayName"]);
  return t !== void 0 && r != null && f(t, ["batch", "displayName"], r), n;
}
function U4(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && f(n, ["_url", "model"], Rt(e, r));
  const s = c(t, ["src"]);
  s != null && f(n, ["batch", "inputConfig"], z4(e, s));
  const o = c(t, ["config"]);
  return o != null && B4(o, n), n;
}
function q4(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && f(n, ["_url", "name"], Fl(e, r)), n;
}
function H4(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && f(n, ["_url", "name"], Fl(e, r)), n;
}
function G4(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["name"]);
  r != null && f(t, ["name"], r);
  const s = c(e, ["done"]);
  s != null && f(t, ["done"], s);
  const o = c(e, ["error"]);
  return o != null && f(t, ["error"], o), t;
}
function W4(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["name"]);
  r != null && f(t, ["name"], r);
  const s = c(e, ["done"]);
  s != null && f(t, ["done"], s);
  const o = c(e, ["error"]);
  return o != null && f(t, ["error"], o), t;
}
function V4(e, t) {
  const n = {}, r = c(t, ["contents"]);
  if (r != null) {
    let o = hw(e, r);
    Array.isArray(o) && (o = o.map((i) => i)), f(n, [
      "requests[]",
      "request",
      "content"
    ], o);
  }
  const s = c(t, ["config"]);
  return s != null && (f(n, ["_self"], K4(s, n)), OW(n, { "requests[].*": "requests[].request.*" })), n;
}
function K4(e, t) {
  const n = {}, r = c(e, ["taskType"]);
  t !== void 0 && r != null && f(t, ["requests[]", "taskType"], r);
  const s = c(e, ["title"]);
  t !== void 0 && s != null && f(t, ["requests[]", "title"], s);
  const o = c(e, ["outputDimensionality"]);
  if (t !== void 0 && o != null && f(t, ["requests[]", "outputDimensionality"], o), c(e, ["mimeType"]) !== void 0) throw new Error("mimeType parameter is not supported in Gemini API.");
  if (c(e, ["autoTruncate"]) !== void 0) throw new Error("autoTruncate parameter is not supported in Gemini API.");
  if (c(e, ["documentOcr"]) !== void 0) throw new Error("documentOcr parameter is not supported in Gemini API.");
  if (c(e, ["audioTrackExtraction"]) !== void 0) throw new Error("audioTrackExtraction parameter is not supported in Gemini API.");
  return n;
}
function z4(e, t) {
  const n = {}, r = c(t, ["fileName"]);
  r != null && f(n, ["file_name"], r);
  const s = c(t, ["inlinedRequests"]);
  return s != null && f(n, ["requests"], V4(e, s)), n;
}
function J4(e) {
  const t = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const n = c(e, ["fileUri"]);
  n != null && f(t, ["fileUri"], n);
  const r = c(e, ["mimeType"]);
  return r != null && f(t, ["mimeType"], r), t;
}
function Y4(e) {
  const t = {}, n = c(e, ["id"]);
  n != null && f(t, ["id"], n);
  const r = c(e, ["args"]);
  r != null && f(t, ["args"], r);
  const s = c(e, ["name"]);
  if (s != null && f(t, ["name"], s), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return t;
}
function X4(e) {
  const t = {}, n = c(e, ["allowedFunctionNames"]);
  n != null && f(t, ["allowedFunctionNames"], n);
  const r = c(e, ["mode"]);
  if (r != null && f(t, ["mode"], r), c(e, ["streamFunctionCallArguments"]) !== void 0) throw new Error("streamFunctionCallArguments parameter is not supported in Gemini API.");
  return t;
}
function Q4(e, t, n) {
  const r = {}, s = c(t, ["systemInstruction"]);
  n !== void 0 && s != null && f(n, ["systemInstruction"], SP(bn(s)));
  const o = c(t, ["temperature"]);
  o != null && f(r, ["temperature"], o);
  const i = c(t, ["topP"]);
  i != null && f(r, ["topP"], i);
  const a = c(t, ["topK"]);
  a != null && f(r, ["topK"], a);
  const l = c(t, ["candidateCount"]);
  l != null && f(r, ["candidateCount"], l);
  const u = c(t, ["maxOutputTokens"]);
  u != null && f(r, ["maxOutputTokens"], u);
  const d = c(t, ["stopSequences"]);
  d != null && f(r, ["stopSequences"], d);
  const h = c(t, ["responseLogprobs"]);
  h != null && f(r, ["responseLogprobs"], h);
  const p = c(t, ["logprobs"]);
  p != null && f(r, ["logprobs"], p);
  const y = c(t, ["presencePenalty"]);
  y != null && f(r, ["presencePenalty"], y);
  const w = c(t, ["frequencyPenalty"]);
  w != null && f(r, ["frequencyPenalty"], w);
  const E = c(t, ["seed"]);
  E != null && f(r, ["seed"], E);
  const C = c(t, ["responseMimeType"]);
  C != null && f(r, ["responseMimeType"], C);
  const M = c(t, ["responseSchema"]);
  M != null && f(r, ["responseSchema"], pw(M));
  const k = c(t, ["responseJsonSchema"]);
  if (k != null && f(r, ["responseJsonSchema"], k), c(t, ["routingConfig"]) !== void 0) throw new Error("routingConfig parameter is not supported in Gemini API.");
  if (c(t, ["modelSelectionConfig"]) !== void 0) throw new Error("modelSelectionConfig parameter is not supported in Gemini API.");
  const R = c(t, ["safetySettings"]);
  if (n !== void 0 && R != null) {
    let z = R;
    Array.isArray(z) && (z = z.map((V) => hV(V))), f(n, ["safetySettings"], z);
  }
  const D = c(t, ["tools"]);
  if (n !== void 0 && D != null) {
    let z = Ol(D);
    Array.isArray(z) && (z = z.map((V) => mV(Ll(V)))), f(n, ["tools"], z);
  }
  const m = c(t, ["toolConfig"]);
  if (n !== void 0 && m != null && f(n, ["toolConfig"], pV(m)), c(t, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  const b = c(t, ["cachedContent"]);
  n !== void 0 && b != null && f(n, ["cachedContent"], _o(e, b));
  const v = c(t, ["responseModalities"]);
  v != null && f(r, ["responseModalities"], v);
  const T = c(t, ["mediaResolution"]);
  T != null && f(r, ["mediaResolution"], T);
  const I = c(t, ["speechConfig"]);
  if (I != null && f(r, ["speechConfig"], mw(I)), c(t, ["audioTimestamp"]) !== void 0) throw new Error("audioTimestamp parameter is not supported in Gemini API.");
  const x = c(t, ["thinkingConfig"]);
  x != null && f(r, ["thinkingConfig"], x);
  const P = c(t, ["imageConfig"]);
  P != null && f(r, ["imageConfig"], rV(P));
  const L = c(t, ["enableEnhancedCivicAnswers"]);
  if (L != null && f(r, ["enableEnhancedCivicAnswers"], L), c(t, ["modelArmorConfig"]) !== void 0) throw new Error("modelArmorConfig parameter is not supported in Gemini API.");
  const G = c(t, ["serviceTier"]);
  return n !== void 0 && G != null && f(n, ["serviceTier"], G), r;
}
function Z4(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["candidates"]);
  if (r != null) {
    let u = r;
    Array.isArray(u) && (u = u.map((d) => $4(d))), f(t, ["candidates"], u);
  }
  const s = c(e, ["modelVersion"]);
  s != null && f(t, ["modelVersion"], s);
  const o = c(e, ["promptFeedback"]);
  o != null && f(t, ["promptFeedback"], o);
  const i = c(e, ["responseId"]);
  i != null && f(t, ["responseId"], i);
  const a = c(e, ["usageMetadata"]);
  a != null && f(t, ["usageMetadata"], a);
  const l = c(e, ["modelStatus"]);
  return l != null && f(t, ["modelStatus"], l), t;
}
function j4(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && f(n, ["_url", "name"], Fl(e, r)), n;
}
function eV(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && f(n, ["_url", "name"], Fl(e, r)), n;
}
function tV(e) {
  const t = {}, n = c(e, ["authConfig"]);
  n != null && f(t, ["authConfig"], E4(n));
  const r = c(e, ["enableWidget"]);
  return r != null && f(t, ["enableWidget"], r), t;
}
function nV(e) {
  const t = {}, n = c(e, ["searchTypes"]);
  if (n != null && f(t, ["searchTypes"], n), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const r = c(e, ["timeRangeFilter"]);
  return r != null && f(t, ["timeRangeFilter"], r), t;
}
function rV(e) {
  const t = {}, n = c(e, ["aspectRatio"]);
  n != null && f(t, ["aspectRatio"], n);
  const r = c(e, ["imageSize"]);
  if (r != null && f(t, ["imageSize"], r), c(e, ["personGeneration"]) !== void 0) throw new Error("personGeneration parameter is not supported in Gemini API.");
  if (c(e, ["prominentPeople"]) !== void 0) throw new Error("prominentPeople parameter is not supported in Gemini API.");
  if (c(e, ["outputMimeType"]) !== void 0) throw new Error("outputMimeType parameter is not supported in Gemini API.");
  if (c(e, ["outputCompressionQuality"]) !== void 0) throw new Error("outputCompressionQuality parameter is not supported in Gemini API.");
  if (c(e, ["imageOutputOptions"]) !== void 0) throw new Error("imageOutputOptions parameter is not supported in Gemini API.");
  return t;
}
function sV(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && f(n, ["request", "model"], Rt(e, r));
  const s = c(t, ["contents"]);
  if (s != null) {
    let a = rr(s);
    Array.isArray(a) && (a = a.map((l) => SP(l))), f(n, ["request", "contents"], a);
  }
  const o = c(t, ["metadata"]);
  o != null && f(n, ["metadata"], o);
  const i = c(t, ["config"]);
  return i != null && f(n, ["request", "generationConfig"], Q4(e, i, c(n, ["request"], {}))), n;
}
function oV(e) {
  const t = {}, n = c(e, ["response"]);
  n != null && f(t, ["response"], Z4(n));
  const r = c(e, ["metadata"]);
  r != null && f(t, ["metadata"], r);
  const s = c(e, ["error"]);
  return s != null && f(t, ["error"], s), t;
}
function iV(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && f(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  if (t !== void 0 && s != null && f(t, ["_query", "pageToken"], s), c(e, ["filter"]) !== void 0) throw new Error("filter parameter is not supported in Gemini API.");
  return n;
}
function aV(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && f(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  t !== void 0 && s != null && f(t, ["_query", "pageToken"], s);
  const o = c(e, ["filter"]);
  return t !== void 0 && o != null && f(t, ["_query", "filter"], o), n;
}
function lV(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && iV(n, t), t;
}
function uV(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && aV(n, t), t;
}
function cV(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && f(t, ["nextPageToken"], r);
  const s = c(e, ["operations"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((i) => sh(i))), f(t, ["batchJobs"], o);
  }
  return t;
}
function dV(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && f(t, ["nextPageToken"], r);
  const s = c(e, ["batchPredictionJobs"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((i) => Yy(i))), f(t, ["batchJobs"], o);
  }
  return t;
}
function fV(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && f(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && f(t, ["codeExecutionResult"], r);
  const s = c(e, ["executableCode"]);
  s != null && f(t, ["executableCode"], s);
  const o = c(e, ["fileData"]);
  o != null && f(t, ["fileData"], J4(o));
  const i = c(e, ["functionCall"]);
  i != null && f(t, ["functionCall"], Y4(i));
  const a = c(e, ["functionResponse"]);
  a != null && f(t, ["functionResponse"], a);
  const l = c(e, ["inlineData"]);
  l != null && f(t, ["inlineData"], P4(l));
  const u = c(e, ["text"]);
  u != null && f(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && f(t, ["thought"], d);
  const h = c(e, ["thoughtSignature"]);
  h != null && f(t, ["thoughtSignature"], h);
  const p = c(e, ["videoMetadata"]);
  p != null && f(t, ["videoMetadata"], p);
  const y = c(e, ["toolCall"]);
  y != null && f(t, ["toolCall"], y);
  const w = c(e, ["toolResponse"]);
  w != null && f(t, ["toolResponse"], w);
  const E = c(e, ["partMetadata"]);
  return E != null && f(t, ["partMetadata"], E), t;
}
function hV(e) {
  const t = {}, n = c(e, ["category"]);
  if (n != null && f(t, ["category"], n), c(e, ["method"]) !== void 0) throw new Error("method parameter is not supported in Gemini API.");
  const r = c(e, ["threshold"]);
  return r != null && f(t, ["threshold"], r), t;
}
function pV(e) {
  const t = {}, n = c(e, ["retrievalConfig"]);
  n != null && f(t, ["retrievalConfig"], n);
  const r = c(e, ["functionCallingConfig"]);
  r != null && f(t, ["functionCallingConfig"], X4(r));
  const s = c(e, ["includeServerSideToolInvocations"]);
  return s != null && f(t, ["includeServerSideToolInvocations"], s), t;
}
function mV(e) {
  const t = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const n = c(e, ["computerUse"]);
  n != null && f(t, ["computerUse"], n);
  const r = c(e, ["fileSearch"]);
  r != null && f(t, ["fileSearch"], r);
  const s = c(e, ["googleSearch"]);
  s != null && f(t, ["googleSearch"], nV(s));
  const o = c(e, ["googleMaps"]);
  o != null && f(t, ["googleMaps"], tV(o));
  const i = c(e, ["codeExecution"]);
  if (i != null && f(t, ["codeExecution"], i), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const a = c(e, ["functionDeclarations"]);
  if (a != null) {
    let h = a;
    Array.isArray(h) && (h = h.map((p) => p)), f(t, ["functionDeclarations"], h);
  }
  const l = c(e, ["googleSearchRetrieval"]);
  if (l != null && f(t, ["googleSearchRetrieval"], l), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const u = c(e, ["urlContext"]);
  u != null && f(t, ["urlContext"], u);
  const d = c(e, ["mcpServers"]);
  if (d != null) {
    let h = d;
    Array.isArray(h) && (h = h.map((p) => p)), f(t, ["mcpServers"], h);
  }
  return t;
}
var go;
(function(e) {
  e.PAGED_ITEM_BATCH_JOBS = "batchJobs", e.PAGED_ITEM_MODELS = "models", e.PAGED_ITEM_TUNING_JOBS = "tuningJobs", e.PAGED_ITEM_FILES = "files", e.PAGED_ITEM_CACHED_CONTENTS = "cachedContents", e.PAGED_ITEM_FILE_SEARCH_STORES = "fileSearchStores", e.PAGED_ITEM_DOCUMENTS = "documents";
})(go || (go = {}));
var ua = class {
  constructor(e, t, n, r) {
    this.pageInternal = [], this.paramsInternal = {}, this.requestInternal = t, this.init(e, n, r);
  }
  init(e, t, n) {
    var r, s;
    this.nameInternal = e, this.pageInternal = t[this.nameInternal] || [], this.sdkHttpResponseInternal = t?.sdkHttpResponse, this.idxInternal = 0;
    let o = { config: {} };
    !n || Object.keys(n).length === 0 ? o = { config: {} } : typeof n == "object" ? o = Object.assign({}, n) : o = n, o.config && (o.config.pageToken = t.nextPageToken), this.paramsInternal = o, this.pageInternalSize = (s = (r = o.config) === null || r === void 0 ? void 0 : r.pageSize) !== null && s !== void 0 ? s : this.pageInternal.length;
  }
  initNextPage(e) {
    this.init(this.nameInternal, e, this.paramsInternal);
  }
  get page() {
    return this.pageInternal;
  }
  get name() {
    return this.nameInternal;
  }
  get pageSize() {
    return this.pageInternalSize;
  }
  get sdkHttpResponse() {
    return this.sdkHttpResponseInternal;
  }
  get params() {
    return this.paramsInternal;
  }
  get pageLength() {
    return this.pageInternal.length;
  }
  getItem(e) {
    return this.pageInternal[e];
  }
  [Symbol.asyncIterator]() {
    return {
      next: async () => {
        if (this.idxInternal >= this.pageLength) if (this.hasNextPage()) await this.nextPage();
        else return {
          value: void 0,
          done: !0
        };
        const e = this.getItem(this.idxInternal);
        return this.idxInternal += 1, {
          value: e,
          done: !1
        };
      },
      return: async () => ({
        value: void 0,
        done: !0
      })
    };
  }
  async nextPage() {
    if (!this.hasNextPage()) throw new Error("No more pages to fetch.");
    const e = await this.requestInternal(this.params);
    return this.initNextPage(e), this.page;
  }
  hasNextPage() {
    var e;
    return ((e = this.params.config) === null || e === void 0 ? void 0 : e.pageToken) !== void 0;
  }
}, gV = class extends yo {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t = {}) => new ua(go.PAGED_ITEM_BATCH_JOBS, (n) => this.listInternal(n), await this.listInternal(t), t), this.create = async (t) => (this.apiClient.isVertexAI() && (t.config = this.formatDestination(t.src, t.config)), this.createInternal(t)), this.createEmbeddings = async (t) => {
      if (console.warn("batches.createEmbeddings() is experimental and may change without notice."), this.apiClient.isVertexAI()) throw new Error("Vertex AI does not support batches.createEmbeddings.");
      return this.createEmbeddingsInternal(t);
    };
  }
  createInlinedGenerateContentRequest(e) {
    const t = vC(this.apiClient, e), n = t._url, r = Ue("{model}:batchGenerateContent", n), s = t.batch.inputConfig.requests, o = s.requests, i = [];
    for (const a of o) {
      const l = Object.assign({}, a);
      if (l.systemInstruction) {
        const u = l.systemInstruction;
        delete l.systemInstruction;
        const d = l.request;
        d.systemInstruction = u, l.request = d;
      }
      i.push(l);
    }
    return s.requests = i, delete t.config, delete t._url, delete t._query, {
      path: r,
      body: t
    };
  }
  getGcsUri(e) {
    if (typeof e == "string") return e.startsWith("gs://") ? e : void 0;
    if (!Array.isArray(e) && e.gcsUri && e.gcsUri.length > 0) return e.gcsUri[0];
  }
  getBigqueryUri(e) {
    if (typeof e == "string") return e.startsWith("bq://") ? e : void 0;
    if (!Array.isArray(e)) return e.bigqueryUri;
  }
  formatDestination(e, t) {
    const n = t ? Object.assign({}, t) : {}, r = Date.now().toString();
    if (n.displayName || (n.displayName = `genaiBatchJob_${r}`), n.dest === void 0) {
      const s = this.getGcsUri(e), o = this.getBigqueryUri(e);
      if (s) s.endsWith(".jsonl") ? n.dest = `${s.slice(0, -6)}/dest` : n.dest = `${s}_dest_${r}`;
      else if (o) n.dest = `${o}_dest_${r}`;
      else throw new Error("Unsupported source for Vertex AI: No GCS or BigQuery URI found.");
    }
    return n;
  }
  async createInternal(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = F4(this.apiClient, e);
      return i = Ue("batchPredictionJobs", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), o.then((u) => Yy(u));
    } else {
      const l = vC(this.apiClient, e);
      return i = Ue("{model}:batchGenerateContent", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), o.then((u) => sh(u));
    }
  }
  async createEmbeddingsInternal(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = U4(this.apiClient, e);
      return s = Ue("{model}:asyncBatchEmbedContent", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r.then((a) => sh(a));
    }
  }
  async get(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = eV(this.apiClient, e);
      return i = Ue("batchPredictionJobs/{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), o.then((u) => Yy(u));
    } else {
      const l = j4(this.apiClient, e);
      return i = Ue("batches/{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), o.then((u) => sh(u));
    }
  }
  async cancel(e) {
    var t, n, r, s;
    let o = "", i = {};
    if (this.apiClient.isVertexAI()) {
      const a = N4(this.apiClient, e);
      o = Ue("batchPredictionJobs/{name}:cancel", a._url), i = a._query, delete a._url, delete a._query, await this.apiClient.request({
        path: o,
        queryParams: i,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      });
    } else {
      const a = R4(this.apiClient, e);
      o = Ue("batches/{name}:cancel", a._url), i = a._query, delete a._url, delete a._query, await this.apiClient.request({
        path: o,
        queryParams: i,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      });
    }
  }
  async listInternal(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = uV(e);
      return i = Ue("batchPredictionJobs", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = dV(u), h = new hC();
        return Object.assign(h, d), h;
      });
    } else {
      const l = lV(e);
      return i = Ue("batches", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = cV(u), h = new hC();
        return Object.assign(h, d), h;
      });
    }
  }
  async delete(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = H4(this.apiClient, e);
      return i = Ue("batchPredictionJobs/{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => W4(u));
    } else {
      const l = q4(this.apiClient, e);
      return i = Ue("batches/{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "DELETE",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => G4(u));
    }
  }
};
function vV(e) {
  const t = {}, n = c(e, ["apiKey"]);
  if (n != null && f(t, ["apiKey"], n), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return t;
}
function yV(e) {
  const t = {}, n = c(e, ["data"]);
  if (n != null && f(t, ["data"], n), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["mimeType"]);
  return r != null && f(t, ["mimeType"], r), t;
}
function yC(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((o) => qV(o))), f(t, ["parts"], s);
  }
  const r = c(e, ["role"]);
  return r != null && f(t, ["role"], r), t;
}
function _C(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((o) => HV(o))), f(t, ["parts"], s);
  }
  const r = c(e, ["role"]);
  return r != null && f(t, ["role"], r), t;
}
function _V(e, t) {
  const n = {}, r = c(e, ["ttl"]);
  t !== void 0 && r != null && f(t, ["ttl"], r);
  const s = c(e, ["expireTime"]);
  t !== void 0 && s != null && f(t, ["expireTime"], s);
  const o = c(e, ["displayName"]);
  t !== void 0 && o != null && f(t, ["displayName"], o);
  const i = c(e, ["contents"]);
  if (t !== void 0 && i != null) {
    let d = rr(i);
    Array.isArray(d) && (d = d.map((h) => yC(h))), f(t, ["contents"], d);
  }
  const a = c(e, ["systemInstruction"]);
  t !== void 0 && a != null && f(t, ["systemInstruction"], yC(bn(a)));
  const l = c(e, ["tools"]);
  if (t !== void 0 && l != null) {
    let d = l;
    Array.isArray(d) && (d = d.map((h) => VV(h))), f(t, ["tools"], d);
  }
  const u = c(e, ["toolConfig"]);
  if (t !== void 0 && u != null && f(t, ["toolConfig"], GV(u)), c(e, ["kmsKeyName"]) !== void 0) throw new Error("kmsKeyName parameter is not supported in Gemini API.");
  return n;
}
function bV(e, t) {
  const n = {}, r = c(e, ["ttl"]);
  t !== void 0 && r != null && f(t, ["ttl"], r);
  const s = c(e, ["expireTime"]);
  t !== void 0 && s != null && f(t, ["expireTime"], s);
  const o = c(e, ["displayName"]);
  t !== void 0 && o != null && f(t, ["displayName"], o);
  const i = c(e, ["contents"]);
  if (t !== void 0 && i != null) {
    let h = rr(i);
    Array.isArray(h) && (h = h.map((p) => _C(p))), f(t, ["contents"], h);
  }
  const a = c(e, ["systemInstruction"]);
  t !== void 0 && a != null && f(t, ["systemInstruction"], _C(bn(a)));
  const l = c(e, ["tools"]);
  if (t !== void 0 && l != null) {
    let h = l;
    Array.isArray(h) && (h = h.map((p) => KV(p))), f(t, ["tools"], h);
  }
  const u = c(e, ["toolConfig"]);
  t !== void 0 && u != null && f(t, ["toolConfig"], WV(u));
  const d = c(e, ["kmsKeyName"]);
  return t !== void 0 && d != null && f(t, ["encryption_spec", "kmsKeyName"], d), n;
}
function wV(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && f(n, ["model"], cP(e, r));
  const s = c(t, ["config"]);
  return s != null && _V(s, n), n;
}
function SV(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && f(n, ["model"], cP(e, r));
  const s = c(t, ["config"]);
  return s != null && bV(s, n), n;
}
function TV(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && f(n, ["_url", "name"], _o(e, r)), n;
}
function EV(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && f(n, ["_url", "name"], _o(e, r)), n;
}
function CV(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && f(t, ["sdkHttpResponse"], n), t;
}
function AV(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && f(t, ["sdkHttpResponse"], n), t;
}
function IV(e) {
  const t = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const n = c(e, ["fileUri"]);
  n != null && f(t, ["fileUri"], n);
  const r = c(e, ["mimeType"]);
  return r != null && f(t, ["mimeType"], r), t;
}
function xV(e) {
  const t = {}, n = c(e, ["id"]);
  n != null && f(t, ["id"], n);
  const r = c(e, ["args"]);
  r != null && f(t, ["args"], r);
  const s = c(e, ["name"]);
  if (s != null && f(t, ["name"], s), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return t;
}
function kV(e) {
  const t = {}, n = c(e, ["allowedFunctionNames"]);
  n != null && f(t, ["allowedFunctionNames"], n);
  const r = c(e, ["mode"]);
  if (r != null && f(t, ["mode"], r), c(e, ["streamFunctionCallArguments"]) !== void 0) throw new Error("streamFunctionCallArguments parameter is not supported in Gemini API.");
  return t;
}
function MV(e) {
  const t = {}, n = c(e, ["description"]);
  n != null && f(t, ["description"], n);
  const r = c(e, ["name"]);
  r != null && f(t, ["name"], r);
  const s = c(e, ["parameters"]);
  s != null && f(t, ["parameters"], s);
  const o = c(e, ["parametersJsonSchema"]);
  o != null && f(t, ["parametersJsonSchema"], o);
  const i = c(e, ["response"]);
  i != null && f(t, ["response"], i);
  const a = c(e, ["responseJsonSchema"]);
  if (a != null && f(t, ["responseJsonSchema"], a), c(e, ["behavior"]) !== void 0) throw new Error("behavior parameter is not supported in Vertex AI.");
  return t;
}
function PV(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && f(n, ["_url", "name"], _o(e, r)), n;
}
function RV(e, t) {
  const n = {}, r = c(t, ["name"]);
  return r != null && f(n, ["_url", "name"], _o(e, r)), n;
}
function NV(e) {
  const t = {}, n = c(e, ["authConfig"]);
  n != null && f(t, ["authConfig"], vV(n));
  const r = c(e, ["enableWidget"]);
  return r != null && f(t, ["enableWidget"], r), t;
}
function $V(e) {
  const t = {}, n = c(e, ["searchTypes"]);
  if (n != null && f(t, ["searchTypes"], n), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const r = c(e, ["timeRangeFilter"]);
  return r != null && f(t, ["timeRangeFilter"], r), t;
}
function DV(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && f(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  return t !== void 0 && s != null && f(t, ["_query", "pageToken"], s), n;
}
function LV(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && f(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  return t !== void 0 && s != null && f(t, ["_query", "pageToken"], s), n;
}
function OV(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && DV(n, t), t;
}
function FV(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && LV(n, t), t;
}
function BV(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && f(t, ["nextPageToken"], r);
  const s = c(e, ["cachedContents"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((i) => i)), f(t, ["cachedContents"], o);
  }
  return t;
}
function UV(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && f(t, ["nextPageToken"], r);
  const s = c(e, ["cachedContents"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((i) => i)), f(t, ["cachedContents"], o);
  }
  return t;
}
function qV(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && f(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && f(t, ["codeExecutionResult"], r);
  const s = c(e, ["executableCode"]);
  s != null && f(t, ["executableCode"], s);
  const o = c(e, ["fileData"]);
  o != null && f(t, ["fileData"], IV(o));
  const i = c(e, ["functionCall"]);
  i != null && f(t, ["functionCall"], xV(i));
  const a = c(e, ["functionResponse"]);
  a != null && f(t, ["functionResponse"], a);
  const l = c(e, ["inlineData"]);
  l != null && f(t, ["inlineData"], yV(l));
  const u = c(e, ["text"]);
  u != null && f(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && f(t, ["thought"], d);
  const h = c(e, ["thoughtSignature"]);
  h != null && f(t, ["thoughtSignature"], h);
  const p = c(e, ["videoMetadata"]);
  p != null && f(t, ["videoMetadata"], p);
  const y = c(e, ["toolCall"]);
  y != null && f(t, ["toolCall"], y);
  const w = c(e, ["toolResponse"]);
  w != null && f(t, ["toolResponse"], w);
  const E = c(e, ["partMetadata"]);
  return E != null && f(t, ["partMetadata"], E), t;
}
function HV(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && f(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && f(t, ["codeExecutionResult"], r);
  const s = c(e, ["executableCode"]);
  s != null && f(t, ["executableCode"], s);
  const o = c(e, ["fileData"]);
  o != null && f(t, ["fileData"], o);
  const i = c(e, ["functionCall"]);
  i != null && f(t, ["functionCall"], i);
  const a = c(e, ["functionResponse"]);
  a != null && f(t, ["functionResponse"], a);
  const l = c(e, ["inlineData"]);
  l != null && f(t, ["inlineData"], l);
  const u = c(e, ["text"]);
  u != null && f(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && f(t, ["thought"], d);
  const h = c(e, ["thoughtSignature"]);
  h != null && f(t, ["thoughtSignature"], h);
  const p = c(e, ["videoMetadata"]);
  if (p != null && f(t, ["videoMetadata"], p), c(e, ["toolCall"]) !== void 0) throw new Error("toolCall parameter is not supported in Vertex AI.");
  if (c(e, ["toolResponse"]) !== void 0) throw new Error("toolResponse parameter is not supported in Vertex AI.");
  if (c(e, ["partMetadata"]) !== void 0) throw new Error("partMetadata parameter is not supported in Vertex AI.");
  return t;
}
function GV(e) {
  const t = {}, n = c(e, ["retrievalConfig"]);
  n != null && f(t, ["retrievalConfig"], n);
  const r = c(e, ["functionCallingConfig"]);
  r != null && f(t, ["functionCallingConfig"], kV(r));
  const s = c(e, ["includeServerSideToolInvocations"]);
  return s != null && f(t, ["includeServerSideToolInvocations"], s), t;
}
function WV(e) {
  const t = {}, n = c(e, ["retrievalConfig"]);
  n != null && f(t, ["retrievalConfig"], n);
  const r = c(e, ["functionCallingConfig"]);
  if (r != null && f(t, ["functionCallingConfig"], r), c(e, ["includeServerSideToolInvocations"]) !== void 0) throw new Error("includeServerSideToolInvocations parameter is not supported in Vertex AI.");
  return t;
}
function VV(e) {
  const t = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const n = c(e, ["computerUse"]);
  n != null && f(t, ["computerUse"], n);
  const r = c(e, ["fileSearch"]);
  r != null && f(t, ["fileSearch"], r);
  const s = c(e, ["googleSearch"]);
  s != null && f(t, ["googleSearch"], $V(s));
  const o = c(e, ["googleMaps"]);
  o != null && f(t, ["googleMaps"], NV(o));
  const i = c(e, ["codeExecution"]);
  if (i != null && f(t, ["codeExecution"], i), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const a = c(e, ["functionDeclarations"]);
  if (a != null) {
    let h = a;
    Array.isArray(h) && (h = h.map((p) => p)), f(t, ["functionDeclarations"], h);
  }
  const l = c(e, ["googleSearchRetrieval"]);
  if (l != null && f(t, ["googleSearchRetrieval"], l), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const u = c(e, ["urlContext"]);
  u != null && f(t, ["urlContext"], u);
  const d = c(e, ["mcpServers"]);
  if (d != null) {
    let h = d;
    Array.isArray(h) && (h = h.map((p) => p)), f(t, ["mcpServers"], h);
  }
  return t;
}
function KV(e) {
  const t = {}, n = c(e, ["retrieval"]);
  n != null && f(t, ["retrieval"], n);
  const r = c(e, ["computerUse"]);
  if (r != null && f(t, ["computerUse"], r), c(e, ["fileSearch"]) !== void 0) throw new Error("fileSearch parameter is not supported in Vertex AI.");
  const s = c(e, ["googleSearch"]);
  s != null && f(t, ["googleSearch"], s);
  const o = c(e, ["googleMaps"]);
  o != null && f(t, ["googleMaps"], o);
  const i = c(e, ["codeExecution"]);
  i != null && f(t, ["codeExecution"], i);
  const a = c(e, ["enterpriseWebSearch"]);
  a != null && f(t, ["enterpriseWebSearch"], a);
  const l = c(e, ["functionDeclarations"]);
  if (l != null) {
    let p = l;
    Array.isArray(p) && (p = p.map((y) => MV(y))), f(t, ["functionDeclarations"], p);
  }
  const u = c(e, ["googleSearchRetrieval"]);
  u != null && f(t, ["googleSearchRetrieval"], u);
  const d = c(e, ["parallelAiSearch"]);
  d != null && f(t, ["parallelAiSearch"], d);
  const h = c(e, ["urlContext"]);
  if (h != null && f(t, ["urlContext"], h), c(e, ["mcpServers"]) !== void 0) throw new Error("mcpServers parameter is not supported in Vertex AI.");
  return t;
}
function zV(e, t) {
  const n = {}, r = c(e, ["ttl"]);
  t !== void 0 && r != null && f(t, ["ttl"], r);
  const s = c(e, ["expireTime"]);
  return t !== void 0 && s != null && f(t, ["expireTime"], s), n;
}
function JV(e, t) {
  const n = {}, r = c(e, ["ttl"]);
  t !== void 0 && r != null && f(t, ["ttl"], r);
  const s = c(e, ["expireTime"]);
  return t !== void 0 && s != null && f(t, ["expireTime"], s), n;
}
function YV(e, t) {
  const n = {}, r = c(t, ["name"]);
  r != null && f(n, ["_url", "name"], _o(e, r));
  const s = c(t, ["config"]);
  return s != null && zV(s, n), n;
}
function XV(e, t) {
  const n = {}, r = c(t, ["name"]);
  r != null && f(n, ["_url", "name"], _o(e, r));
  const s = c(t, ["config"]);
  return s != null && JV(s, n), n;
}
var QV = class extends yo {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t = {}) => new ua(go.PAGED_ITEM_CACHED_CONTENTS, (n) => this.listInternal(n), await this.listInternal(t), t);
  }
  async create(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = SV(this.apiClient, e);
      return i = Ue("cachedContents", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), o.then((u) => u);
    } else {
      const l = wV(this.apiClient, e);
      return i = Ue("cachedContents", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), o.then((u) => u);
    }
  }
  async get(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = RV(this.apiClient, e);
      return i = Ue("{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), o.then((u) => u);
    } else {
      const l = PV(this.apiClient, e);
      return i = Ue("{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), o.then((u) => u);
    }
  }
  async delete(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = EV(this.apiClient, e);
      return i = Ue("{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = AV(u), h = new dC();
        return Object.assign(h, d), h;
      });
    } else {
      const l = TV(this.apiClient, e);
      return i = Ue("{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "DELETE",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = CV(u), h = new dC();
        return Object.assign(h, d), h;
      });
    }
  }
  async update(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = XV(this.apiClient, e);
      return i = Ue("{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "PATCH",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), o.then((u) => u);
    } else {
      const l = YV(this.apiClient, e);
      return i = Ue("{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "PATCH",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), o.then((u) => u);
    }
  }
  async listInternal(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = FV(e);
      return i = Ue("cachedContents", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = UV(u), h = new fC();
        return Object.assign(h, d), h;
      });
    } else {
      const l = OV(e);
      return i = Ue("cachedContents", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = BV(u), h = new fC();
        return Object.assign(h, d), h;
      });
    }
  }
};
function Go(e, t) {
  var n = {};
  for (var r in e) Object.prototype.hasOwnProperty.call(e, r) && t.indexOf(r) < 0 && (n[r] = e[r]);
  if (e != null && typeof Object.getOwnPropertySymbols == "function")
    for (var s = 0, r = Object.getOwnPropertySymbols(e); s < r.length; s++) t.indexOf(r[s]) < 0 && Object.prototype.propertyIsEnumerable.call(e, r[s]) && (n[r[s]] = e[r[s]]);
  return n;
}
function bC(e) {
  var t = typeof Symbol == "function" && Symbol.iterator, n = t && e[t], r = 0;
  if (n) return n.call(e);
  if (e && typeof e.length == "number") return { next: function() {
    return e && r >= e.length && (e = void 0), {
      value: e && e[r++],
      done: !e
    };
  } };
  throw new TypeError(t ? "Object is not iterable." : "Symbol.iterator is not defined.");
}
function bt(e) {
  return this instanceof bt ? (this.v = e, this) : new bt(e);
}
function ls(e, t, n) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var r = n.apply(e, t || []), s, o = [];
  return s = Object.create((typeof AsyncIterator == "function" ? AsyncIterator : Object).prototype), a("next"), a("throw"), a("return", i), s[Symbol.asyncIterator] = function() {
    return this;
  }, s;
  function i(y) {
    return function(w) {
      return Promise.resolve(w).then(y, h);
    };
  }
  function a(y, w) {
    r[y] && (s[y] = function(E) {
      return new Promise(function(C, M) {
        o.push([
          y,
          E,
          C,
          M
        ]) > 1 || l(y, E);
      });
    }, w && (s[y] = w(s[y])));
  }
  function l(y, w) {
    try {
      u(r[y](w));
    } catch (E) {
      p(o[0][3], E);
    }
  }
  function u(y) {
    y.value instanceof bt ? Promise.resolve(y.value.v).then(d, h) : p(o[0][2], y);
  }
  function d(y) {
    l("next", y);
  }
  function h(y) {
    l("throw", y);
  }
  function p(y, w) {
    y(w), o.shift(), o.length && l(o[0][0], o[0][1]);
  }
}
function us(e) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var t = e[Symbol.asyncIterator], n;
  return t ? t.call(e) : (e = typeof bC == "function" ? bC(e) : e[Symbol.iterator](), n = {}, r("next"), r("throw"), r("return"), n[Symbol.asyncIterator] = function() {
    return this;
  }, n);
  function r(o) {
    n[o] = e[o] && function(i) {
      return new Promise(function(a, l) {
        i = e[o](i), s(a, l, i.done, i.value);
      });
    };
  }
  function s(o, i, a, l) {
    Promise.resolve(l).then(function(u) {
      o({
        value: u,
        done: a
      });
    }, i);
  }
}
function ZV(e) {
  var t;
  if (e.candidates == null || e.candidates.length === 0) return !1;
  const n = (t = e.candidates[0]) === null || t === void 0 ? void 0 : t.content;
  return n === void 0 ? !1 : TP(n);
}
function TP(e) {
  if (e.parts === void 0 || e.parts.length === 0) return !1;
  for (const t of e.parts) if (t === void 0 || Object.keys(t).length === 0) return !1;
  return !0;
}
function jV(e) {
  if (e.length !== 0) {
    for (const t of e) if (t.role !== "user" && t.role !== "model") throw new Error(`Role must be user or model, but got ${t.role}.`);
  }
}
function wC(e) {
  if (e === void 0 || e.length === 0) return [];
  const t = [], n = e.length;
  let r = 0;
  for (; r < n; ) if (e[r].role === "user")
    t.push(e[r]), r++;
  else {
    const s = [];
    let o = !0;
    for (; r < n && e[r].role === "model"; )
      s.push(e[r]), o && !TP(e[r]) && (o = !1), r++;
    o ? t.push(...s) : t.pop();
  }
  return t;
}
var eK = class {
  constructor(e, t) {
    this.modelsModule = e, this.apiClient = t;
  }
  create(e) {
    return new tK(this.apiClient, this.modelsModule, e.model, e.config, structuredClone(e.history));
  }
}, tK = class {
  constructor(e, t, n, r = {}, s = []) {
    this.apiClient = e, this.modelsModule = t, this.model = n, this.config = r, this.history = s, this.sendPromise = Promise.resolve(), jV(s);
  }
  async sendMessage(e) {
    var t;
    await this.sendPromise;
    const n = bn(e.message), r = this.modelsModule.generateContent({
      model: this.model,
      contents: this.getHistory(!0).concat(n),
      config: (t = e.config) !== null && t !== void 0 ? t : this.config
    });
    return this.sendPromise = (async () => {
      var s, o, i;
      const a = await r, l = (o = (s = a.candidates) === null || s === void 0 ? void 0 : s[0]) === null || o === void 0 ? void 0 : o.content, u = a.automaticFunctionCallingHistory, d = this.getHistory(!0).length;
      let h = [];
      u != null && (h = (i = u.slice(d)) !== null && i !== void 0 ? i : []);
      const p = l ? [l] : [];
      this.recordHistory(n, p, h);
    })(), await this.sendPromise.catch(() => {
      this.sendPromise = Promise.resolve();
    }), r;
  }
  async sendMessageStream(e) {
    var t;
    await this.sendPromise;
    const n = bn(e.message), r = this.modelsModule.generateContentStream({
      model: this.model,
      contents: this.getHistory(!0).concat(n),
      config: (t = e.config) !== null && t !== void 0 ? t : this.config
    });
    this.sendPromise = r.then(() => {
    }).catch(() => {
    });
    const s = await r;
    return this.processStreamResponse(s, n);
  }
  getHistory(e = !1) {
    const t = e ? wC(this.history) : this.history;
    return structuredClone(t);
  }
  processStreamResponse(e, t) {
    return ls(this, arguments, function* () {
      var r, s, o, i, a, l;
      const u = [];
      try {
        for (var d = !0, h = us(e), p; p = yield bt(h.next()), r = p.done, !r; d = !0) {
          i = p.value, d = !1;
          const y = i;
          if (ZV(y)) {
            const w = (l = (a = y.candidates) === null || a === void 0 ? void 0 : a[0]) === null || l === void 0 ? void 0 : l.content;
            w !== void 0 && u.push(w);
          }
          yield yield bt(y);
        }
      } catch (y) {
        s = { error: y };
      } finally {
        try {
          !d && !r && (o = h.return) && (yield bt(o.call(h)));
        } finally {
          if (s) throw s.error;
        }
      }
      this.recordHistory(t, u);
    });
  }
  recordHistory(e, t, n) {
    let r = [];
    t.length > 0 && t.every((s) => s.role !== void 0) ? r = t : r.push({
      role: "model",
      parts: []
    }), n && n.length > 0 ? this.history.push(...wC(n)) : this.history.push(e), this.history.push(...r);
  }
}, EP = class CP extends Error {
  constructor(t) {
    super(t.message), this.name = "ApiError", this.status = t.status, Object.setPrototypeOf(this, CP.prototype);
  }
};
function nK(e) {
  const t = {}, n = c(e, ["file"]);
  return n != null && f(t, ["file"], n), t;
}
function rK(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && f(t, ["sdkHttpResponse"], n), t;
}
function sK(e) {
  const t = {}, n = c(e, ["name"]);
  return n != null && f(t, ["_url", "file"], gP(n)), t;
}
function oK(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && f(t, ["sdkHttpResponse"], n), t;
}
function iK(e) {
  const t = {}, n = c(e, ["name"]);
  return n != null && f(t, ["_url", "file"], gP(n)), t;
}
function aK(e) {
  const t = {}, n = c(e, ["uris"]);
  return n != null && f(t, ["uris"], n), t;
}
function lK(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && f(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  return t !== void 0 && s != null && f(t, ["_query", "pageToken"], s), n;
}
function uK(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && lK(n, t), t;
}
function cK(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && f(t, ["nextPageToken"], r);
  const s = c(e, ["files"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((i) => i)), f(t, ["files"], o);
  }
  return t;
}
function dK(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["files"]);
  if (r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((o) => o)), f(t, ["files"], s);
  }
  return t;
}
var fK = class extends yo {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t = {}) => new ua(go.PAGED_ITEM_FILES, (n) => this.listInternal(n), await this.listInternal(t), t);
  }
  async upload(e) {
    if (this.apiClient.isVertexAI()) throw new Error("Vertex AI does not support uploading files. You can share files through a GCS bucket.");
    return this.apiClient.uploadFile(e.file, e.config).then((t) => t);
  }
  async download(e) {
    await this.apiClient.downloadFile(e);
  }
  async registerFiles(e) {
    throw new Error("registerFiles is only supported in Node.js environments.");
  }
  async _registerFiles(e) {
    return this.registerFilesInternal(e);
  }
  async listInternal(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = uK(e);
      return s = Ue("files", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: a.headers }, u;
      })), r.then((a) => {
        const l = cK(a), u = new a4();
        return Object.assign(u, l), u;
      });
    }
  }
  async createInternal(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = nK(e);
      return s = Ue("upload/v1beta/files", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r.then((a) => {
        const l = rK(a), u = new l4();
        return Object.assign(u, l), u;
      });
    }
  }
  async get(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = iK(e);
      return s = Ue("files/{file}", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r.then((a) => a);
    }
  }
  async delete(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = sK(e);
      return s = Ue("files/{file}", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: a.headers }, u;
      })), r.then((a) => {
        const l = oK(a), u = new u4();
        return Object.assign(u, l), u;
      });
    }
  }
  async registerFilesInternal(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = aK(e);
      return s = Ue("files:register", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r.then((a) => {
        const l = dK(a), u = new c4();
        return Object.assign(u, l), u;
      });
    }
  }
};
function SC(e) {
  const t = {};
  if (c(e, ["languageCodes"]) !== void 0) throw new Error("languageCodes parameter is not supported in Gemini API.");
  return t;
}
function hK(e) {
  const t = {}, n = c(e, ["apiKey"]);
  if (n != null && f(t, ["apiKey"], n), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return t;
}
function oh(e) {
  const t = {}, n = c(e, ["data"]);
  if (n != null && f(t, ["data"], n), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["mimeType"]);
  return r != null && f(t, ["mimeType"], r), t;
}
function pK(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((o) => PK(o))), f(t, ["parts"], s);
  }
  const r = c(e, ["role"]);
  return r != null && f(t, ["role"], r), t;
}
function mK(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((o) => RK(o))), f(t, ["parts"], s);
  }
  const r = c(e, ["role"]);
  return r != null && f(t, ["role"], r), t;
}
function gK(e) {
  const t = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const n = c(e, ["fileUri"]);
  n != null && f(t, ["fileUri"], n);
  const r = c(e, ["mimeType"]);
  return r != null && f(t, ["mimeType"], r), t;
}
function vK(e) {
  const t = {}, n = c(e, ["id"]);
  n != null && f(t, ["id"], n);
  const r = c(e, ["args"]);
  r != null && f(t, ["args"], r);
  const s = c(e, ["name"]);
  if (s != null && f(t, ["name"], s), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return t;
}
function yK(e) {
  const t = {}, n = c(e, ["description"]);
  n != null && f(t, ["description"], n);
  const r = c(e, ["name"]);
  r != null && f(t, ["name"], r);
  const s = c(e, ["parameters"]);
  s != null && f(t, ["parameters"], s);
  const o = c(e, ["parametersJsonSchema"]);
  o != null && f(t, ["parametersJsonSchema"], o);
  const i = c(e, ["response"]);
  i != null && f(t, ["response"], i);
  const a = c(e, ["responseJsonSchema"]);
  if (a != null && f(t, ["responseJsonSchema"], a), c(e, ["behavior"]) !== void 0) throw new Error("behavior parameter is not supported in Vertex AI.");
  return t;
}
function _K(e) {
  const t = {}, n = c(e, ["modelSelectionConfig"]);
  n != null && f(t, ["modelConfig"], n);
  const r = c(e, ["responseJsonSchema"]);
  r != null && f(t, ["responseJsonSchema"], r);
  const s = c(e, ["audioTimestamp"]);
  s != null && f(t, ["audioTimestamp"], s);
  const o = c(e, ["candidateCount"]);
  o != null && f(t, ["candidateCount"], o);
  const i = c(e, ["enableAffectiveDialog"]);
  i != null && f(t, ["enableAffectiveDialog"], i);
  const a = c(e, ["frequencyPenalty"]);
  a != null && f(t, ["frequencyPenalty"], a);
  const l = c(e, ["logprobs"]);
  l != null && f(t, ["logprobs"], l);
  const u = c(e, ["maxOutputTokens"]);
  u != null && f(t, ["maxOutputTokens"], u);
  const d = c(e, ["mediaResolution"]);
  d != null && f(t, ["mediaResolution"], d);
  const h = c(e, ["presencePenalty"]);
  h != null && f(t, ["presencePenalty"], h);
  const p = c(e, ["responseLogprobs"]);
  p != null && f(t, ["responseLogprobs"], p);
  const y = c(e, ["responseMimeType"]);
  y != null && f(t, ["responseMimeType"], y);
  const w = c(e, ["responseModalities"]);
  w != null && f(t, ["responseModalities"], w);
  const E = c(e, ["responseSchema"]);
  E != null && f(t, ["responseSchema"], E);
  const C = c(e, ["routingConfig"]);
  C != null && f(t, ["routingConfig"], C);
  const M = c(e, ["seed"]);
  M != null && f(t, ["seed"], M);
  const k = c(e, ["speechConfig"]);
  k != null && f(t, ["speechConfig"], k);
  const R = c(e, ["stopSequences"]);
  R != null && f(t, ["stopSequences"], R);
  const D = c(e, ["temperature"]);
  D != null && f(t, ["temperature"], D);
  const m = c(e, ["thinkingConfig"]);
  m != null && f(t, ["thinkingConfig"], m);
  const b = c(e, ["topK"]);
  b != null && f(t, ["topK"], b);
  const v = c(e, ["topP"]);
  if (v != null && f(t, ["topP"], v), c(e, ["enableEnhancedCivicAnswers"]) !== void 0) throw new Error("enableEnhancedCivicAnswers parameter is not supported in Vertex AI.");
  return t;
}
function bK(e) {
  const t = {}, n = c(e, ["authConfig"]);
  n != null && f(t, ["authConfig"], hK(n));
  const r = c(e, ["enableWidget"]);
  return r != null && f(t, ["enableWidget"], r), t;
}
function wK(e) {
  const t = {}, n = c(e, ["searchTypes"]);
  if (n != null && f(t, ["searchTypes"], n), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const r = c(e, ["timeRangeFilter"]);
  return r != null && f(t, ["timeRangeFilter"], r), t;
}
function SK(e, t) {
  const n = {}, r = c(e, ["generationConfig"]);
  t !== void 0 && r != null && f(t, ["setup", "generationConfig"], r);
  const s = c(e, ["responseModalities"]);
  t !== void 0 && s != null && f(t, [
    "setup",
    "generationConfig",
    "responseModalities"
  ], s);
  const o = c(e, ["temperature"]);
  t !== void 0 && o != null && f(t, [
    "setup",
    "generationConfig",
    "temperature"
  ], o);
  const i = c(e, ["topP"]);
  t !== void 0 && i != null && f(t, [
    "setup",
    "generationConfig",
    "topP"
  ], i);
  const a = c(e, ["topK"]);
  t !== void 0 && a != null && f(t, [
    "setup",
    "generationConfig",
    "topK"
  ], a);
  const l = c(e, ["maxOutputTokens"]);
  t !== void 0 && l != null && f(t, [
    "setup",
    "generationConfig",
    "maxOutputTokens"
  ], l);
  const u = c(e, ["mediaResolution"]);
  t !== void 0 && u != null && f(t, [
    "setup",
    "generationConfig",
    "mediaResolution"
  ], u);
  const d = c(e, ["seed"]);
  t !== void 0 && d != null && f(t, [
    "setup",
    "generationConfig",
    "seed"
  ], d);
  const h = c(e, ["speechConfig"]);
  t !== void 0 && h != null && f(t, [
    "setup",
    "generationConfig",
    "speechConfig"
  ], gw(h));
  const p = c(e, ["thinkingConfig"]);
  t !== void 0 && p != null && f(t, [
    "setup",
    "generationConfig",
    "thinkingConfig"
  ], p);
  const y = c(e, ["enableAffectiveDialog"]);
  t !== void 0 && y != null && f(t, [
    "setup",
    "generationConfig",
    "enableAffectiveDialog"
  ], y);
  const w = c(e, ["systemInstruction"]);
  t !== void 0 && w != null && f(t, ["setup", "systemInstruction"], pK(bn(w)));
  const E = c(e, ["tools"]);
  if (t !== void 0 && E != null) {
    let T = Ol(E);
    Array.isArray(T) && (T = T.map((I) => DK(Ll(I)))), f(t, ["setup", "tools"], T);
  }
  const C = c(e, ["sessionResumption"]);
  t !== void 0 && C != null && f(t, ["setup", "sessionResumption"], $K(C));
  const M = c(e, ["inputAudioTranscription"]);
  t !== void 0 && M != null && f(t, ["setup", "inputAudioTranscription"], SC(M));
  const k = c(e, ["outputAudioTranscription"]);
  t !== void 0 && k != null && f(t, ["setup", "outputAudioTranscription"], SC(k));
  const R = c(e, ["realtimeInputConfig"]);
  t !== void 0 && R != null && f(t, ["setup", "realtimeInputConfig"], R);
  const D = c(e, ["contextWindowCompression"]);
  t !== void 0 && D != null && f(t, ["setup", "contextWindowCompression"], D);
  const m = c(e, ["proactivity"]);
  if (t !== void 0 && m != null && f(t, ["setup", "proactivity"], m), c(e, ["explicitVadSignal"]) !== void 0) throw new Error("explicitVadSignal parameter is not supported in Gemini API.");
  const b = c(e, ["avatarConfig"]);
  t !== void 0 && b != null && f(t, ["setup", "avatarConfig"], b);
  const v = c(e, ["safetySettings"]);
  if (t !== void 0 && v != null) {
    let T = v;
    Array.isArray(T) && (T = T.map((I) => NK(I))), f(t, ["setup", "safetySettings"], T);
  }
  return n;
}
function TK(e, t) {
  const n = {}, r = c(e, ["generationConfig"]);
  t !== void 0 && r != null && f(t, ["setup", "generationConfig"], _K(r));
  const s = c(e, ["responseModalities"]);
  t !== void 0 && s != null && f(t, [
    "setup",
    "generationConfig",
    "responseModalities"
  ], s);
  const o = c(e, ["temperature"]);
  t !== void 0 && o != null && f(t, [
    "setup",
    "generationConfig",
    "temperature"
  ], o);
  const i = c(e, ["topP"]);
  t !== void 0 && i != null && f(t, [
    "setup",
    "generationConfig",
    "topP"
  ], i);
  const a = c(e, ["topK"]);
  t !== void 0 && a != null && f(t, [
    "setup",
    "generationConfig",
    "topK"
  ], a);
  const l = c(e, ["maxOutputTokens"]);
  t !== void 0 && l != null && f(t, [
    "setup",
    "generationConfig",
    "maxOutputTokens"
  ], l);
  const u = c(e, ["mediaResolution"]);
  t !== void 0 && u != null && f(t, [
    "setup",
    "generationConfig",
    "mediaResolution"
  ], u);
  const d = c(e, ["seed"]);
  t !== void 0 && d != null && f(t, [
    "setup",
    "generationConfig",
    "seed"
  ], d);
  const h = c(e, ["speechConfig"]);
  t !== void 0 && h != null && f(t, [
    "setup",
    "generationConfig",
    "speechConfig"
  ], gw(h));
  const p = c(e, ["thinkingConfig"]);
  t !== void 0 && p != null && f(t, [
    "setup",
    "generationConfig",
    "thinkingConfig"
  ], p);
  const y = c(e, ["enableAffectiveDialog"]);
  t !== void 0 && y != null && f(t, [
    "setup",
    "generationConfig",
    "enableAffectiveDialog"
  ], y);
  const w = c(e, ["systemInstruction"]);
  t !== void 0 && w != null && f(t, ["setup", "systemInstruction"], mK(bn(w)));
  const E = c(e, ["tools"]);
  if (t !== void 0 && E != null) {
    let I = Ol(E);
    Array.isArray(I) && (I = I.map((x) => LK(Ll(x)))), f(t, ["setup", "tools"], I);
  }
  const C = c(e, ["sessionResumption"]);
  t !== void 0 && C != null && f(t, ["setup", "sessionResumption"], C);
  const M = c(e, ["inputAudioTranscription"]);
  t !== void 0 && M != null && f(t, ["setup", "inputAudioTranscription"], M);
  const k = c(e, ["outputAudioTranscription"]);
  t !== void 0 && k != null && f(t, ["setup", "outputAudioTranscription"], k);
  const R = c(e, ["realtimeInputConfig"]);
  t !== void 0 && R != null && f(t, ["setup", "realtimeInputConfig"], R);
  const D = c(e, ["contextWindowCompression"]);
  t !== void 0 && D != null && f(t, ["setup", "contextWindowCompression"], D);
  const m = c(e, ["proactivity"]);
  t !== void 0 && m != null && f(t, ["setup", "proactivity"], m);
  const b = c(e, ["explicitVadSignal"]);
  t !== void 0 && b != null && f(t, ["setup", "explicitVadSignal"], b);
  const v = c(e, ["avatarConfig"]);
  t !== void 0 && v != null && f(t, ["setup", "avatarConfig"], v);
  const T = c(e, ["safetySettings"]);
  if (t !== void 0 && T != null) {
    let I = T;
    Array.isArray(I) && (I = I.map((x) => x)), f(t, ["setup", "safetySettings"], I);
  }
  return n;
}
function EK(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && f(n, ["setup", "model"], Rt(e, r));
  const s = c(t, ["config"]);
  return s != null && f(n, ["config"], SK(s, n)), n;
}
function CK(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && f(n, ["setup", "model"], Rt(e, r));
  const s = c(t, ["config"]);
  return s != null && f(n, ["config"], TK(s, n)), n;
}
function AK(e) {
  const t = {}, n = c(e, ["musicGenerationConfig"]);
  return n != null && f(t, ["musicGenerationConfig"], n), t;
}
function IK(e) {
  const t = {}, n = c(e, ["weightedPrompts"]);
  if (n != null) {
    let r = n;
    Array.isArray(r) && (r = r.map((s) => s)), f(t, ["weightedPrompts"], r);
  }
  return t;
}
function xK(e) {
  const t = {}, n = c(e, ["media"]);
  if (n != null) {
    let u = dP(n);
    Array.isArray(u) && (u = u.map((d) => oh(d))), f(t, ["mediaChunks"], u);
  }
  const r = c(e, ["audio"]);
  r != null && f(t, ["audio"], oh(hP(r)));
  const s = c(e, ["audioStreamEnd"]);
  s != null && f(t, ["audioStreamEnd"], s);
  const o = c(e, ["video"]);
  o != null && f(t, ["video"], oh(fP(o)));
  const i = c(e, ["text"]);
  i != null && f(t, ["text"], i);
  const a = c(e, ["activityStart"]);
  a != null && f(t, ["activityStart"], a);
  const l = c(e, ["activityEnd"]);
  return l != null && f(t, ["activityEnd"], l), t;
}
function kK(e) {
  const t = {}, n = c(e, ["media"]);
  if (n != null) {
    let u = dP(n);
    Array.isArray(u) && (u = u.map((d) => d)), f(t, ["mediaChunks"], u);
  }
  const r = c(e, ["audio"]);
  r != null && f(t, ["audio"], hP(r));
  const s = c(e, ["audioStreamEnd"]);
  s != null && f(t, ["audioStreamEnd"], s);
  const o = c(e, ["video"]);
  o != null && f(t, ["video"], fP(o));
  const i = c(e, ["text"]);
  i != null && f(t, ["text"], i);
  const a = c(e, ["activityStart"]);
  a != null && f(t, ["activityStart"], a);
  const l = c(e, ["activityEnd"]);
  return l != null && f(t, ["activityEnd"], l), t;
}
function MK(e) {
  const t = {}, n = c(e, ["setupComplete"]);
  n != null && f(t, ["setupComplete"], n);
  const r = c(e, ["serverContent"]);
  r != null && f(t, ["serverContent"], r);
  const s = c(e, ["toolCall"]);
  s != null && f(t, ["toolCall"], s);
  const o = c(e, ["toolCallCancellation"]);
  o != null && f(t, ["toolCallCancellation"], o);
  const i = c(e, ["usageMetadata"]);
  i != null && f(t, ["usageMetadata"], OK(i));
  const a = c(e, ["goAway"]);
  a != null && f(t, ["goAway"], a);
  const l = c(e, ["sessionResumptionUpdate"]);
  l != null && f(t, ["sessionResumptionUpdate"], l);
  const u = c(e, ["voiceActivityDetectionSignal"]);
  u != null && f(t, ["voiceActivityDetectionSignal"], u);
  const d = c(e, ["voiceActivity"]);
  return d != null && f(t, ["voiceActivity"], FK(d)), t;
}
function PK(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && f(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && f(t, ["codeExecutionResult"], r);
  const s = c(e, ["executableCode"]);
  s != null && f(t, ["executableCode"], s);
  const o = c(e, ["fileData"]);
  o != null && f(t, ["fileData"], gK(o));
  const i = c(e, ["functionCall"]);
  i != null && f(t, ["functionCall"], vK(i));
  const a = c(e, ["functionResponse"]);
  a != null && f(t, ["functionResponse"], a);
  const l = c(e, ["inlineData"]);
  l != null && f(t, ["inlineData"], oh(l));
  const u = c(e, ["text"]);
  u != null && f(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && f(t, ["thought"], d);
  const h = c(e, ["thoughtSignature"]);
  h != null && f(t, ["thoughtSignature"], h);
  const p = c(e, ["videoMetadata"]);
  p != null && f(t, ["videoMetadata"], p);
  const y = c(e, ["toolCall"]);
  y != null && f(t, ["toolCall"], y);
  const w = c(e, ["toolResponse"]);
  w != null && f(t, ["toolResponse"], w);
  const E = c(e, ["partMetadata"]);
  return E != null && f(t, ["partMetadata"], E), t;
}
function RK(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && f(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && f(t, ["codeExecutionResult"], r);
  const s = c(e, ["executableCode"]);
  s != null && f(t, ["executableCode"], s);
  const o = c(e, ["fileData"]);
  o != null && f(t, ["fileData"], o);
  const i = c(e, ["functionCall"]);
  i != null && f(t, ["functionCall"], i);
  const a = c(e, ["functionResponse"]);
  a != null && f(t, ["functionResponse"], a);
  const l = c(e, ["inlineData"]);
  l != null && f(t, ["inlineData"], l);
  const u = c(e, ["text"]);
  u != null && f(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && f(t, ["thought"], d);
  const h = c(e, ["thoughtSignature"]);
  h != null && f(t, ["thoughtSignature"], h);
  const p = c(e, ["videoMetadata"]);
  if (p != null && f(t, ["videoMetadata"], p), c(e, ["toolCall"]) !== void 0) throw new Error("toolCall parameter is not supported in Vertex AI.");
  if (c(e, ["toolResponse"]) !== void 0) throw new Error("toolResponse parameter is not supported in Vertex AI.");
  if (c(e, ["partMetadata"]) !== void 0) throw new Error("partMetadata parameter is not supported in Vertex AI.");
  return t;
}
function NK(e) {
  const t = {}, n = c(e, ["category"]);
  if (n != null && f(t, ["category"], n), c(e, ["method"]) !== void 0) throw new Error("method parameter is not supported in Gemini API.");
  const r = c(e, ["threshold"]);
  return r != null && f(t, ["threshold"], r), t;
}
function $K(e) {
  const t = {}, n = c(e, ["handle"]);
  if (n != null && f(t, ["handle"], n), c(e, ["transparent"]) !== void 0) throw new Error("transparent parameter is not supported in Gemini API.");
  return t;
}
function DK(e) {
  const t = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const n = c(e, ["computerUse"]);
  n != null && f(t, ["computerUse"], n);
  const r = c(e, ["fileSearch"]);
  r != null && f(t, ["fileSearch"], r);
  const s = c(e, ["googleSearch"]);
  s != null && f(t, ["googleSearch"], wK(s));
  const o = c(e, ["googleMaps"]);
  o != null && f(t, ["googleMaps"], bK(o));
  const i = c(e, ["codeExecution"]);
  if (i != null && f(t, ["codeExecution"], i), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const a = c(e, ["functionDeclarations"]);
  if (a != null) {
    let h = a;
    Array.isArray(h) && (h = h.map((p) => p)), f(t, ["functionDeclarations"], h);
  }
  const l = c(e, ["googleSearchRetrieval"]);
  if (l != null && f(t, ["googleSearchRetrieval"], l), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const u = c(e, ["urlContext"]);
  u != null && f(t, ["urlContext"], u);
  const d = c(e, ["mcpServers"]);
  if (d != null) {
    let h = d;
    Array.isArray(h) && (h = h.map((p) => p)), f(t, ["mcpServers"], h);
  }
  return t;
}
function LK(e) {
  const t = {}, n = c(e, ["retrieval"]);
  n != null && f(t, ["retrieval"], n);
  const r = c(e, ["computerUse"]);
  if (r != null && f(t, ["computerUse"], r), c(e, ["fileSearch"]) !== void 0) throw new Error("fileSearch parameter is not supported in Vertex AI.");
  const s = c(e, ["googleSearch"]);
  s != null && f(t, ["googleSearch"], s);
  const o = c(e, ["googleMaps"]);
  o != null && f(t, ["googleMaps"], o);
  const i = c(e, ["codeExecution"]);
  i != null && f(t, ["codeExecution"], i);
  const a = c(e, ["enterpriseWebSearch"]);
  a != null && f(t, ["enterpriseWebSearch"], a);
  const l = c(e, ["functionDeclarations"]);
  if (l != null) {
    let p = l;
    Array.isArray(p) && (p = p.map((y) => yK(y))), f(t, ["functionDeclarations"], p);
  }
  const u = c(e, ["googleSearchRetrieval"]);
  u != null && f(t, ["googleSearchRetrieval"], u);
  const d = c(e, ["parallelAiSearch"]);
  d != null && f(t, ["parallelAiSearch"], d);
  const h = c(e, ["urlContext"]);
  if (h != null && f(t, ["urlContext"], h), c(e, ["mcpServers"]) !== void 0) throw new Error("mcpServers parameter is not supported in Vertex AI.");
  return t;
}
function OK(e) {
  const t = {}, n = c(e, ["promptTokenCount"]);
  n != null && f(t, ["promptTokenCount"], n);
  const r = c(e, ["cachedContentTokenCount"]);
  r != null && f(t, ["cachedContentTokenCount"], r);
  const s = c(e, ["candidatesTokenCount"]);
  s != null && f(t, ["responseTokenCount"], s);
  const o = c(e, ["toolUsePromptTokenCount"]);
  o != null && f(t, ["toolUsePromptTokenCount"], o);
  const i = c(e, ["thoughtsTokenCount"]);
  i != null && f(t, ["thoughtsTokenCount"], i);
  const a = c(e, ["totalTokenCount"]);
  a != null && f(t, ["totalTokenCount"], a);
  const l = c(e, ["promptTokensDetails"]);
  if (l != null) {
    let y = l;
    Array.isArray(y) && (y = y.map((w) => w)), f(t, ["promptTokensDetails"], y);
  }
  const u = c(e, ["cacheTokensDetails"]);
  if (u != null) {
    let y = u;
    Array.isArray(y) && (y = y.map((w) => w)), f(t, ["cacheTokensDetails"], y);
  }
  const d = c(e, ["candidatesTokensDetails"]);
  if (d != null) {
    let y = d;
    Array.isArray(y) && (y = y.map((w) => w)), f(t, ["responseTokensDetails"], y);
  }
  const h = c(e, ["toolUsePromptTokensDetails"]);
  if (h != null) {
    let y = h;
    Array.isArray(y) && (y = y.map((w) => w)), f(t, ["toolUsePromptTokensDetails"], y);
  }
  const p = c(e, ["trafficType"]);
  return p != null && f(t, ["trafficType"], p), t;
}
function FK(e) {
  const t = {}, n = c(e, ["type"]);
  return n != null && f(t, ["voiceActivityType"], n), t;
}
function BK(e, t) {
  const n = {}, r = c(e, ["apiKey"]);
  if (r != null && f(n, ["apiKey"], r), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return n;
}
function UK(e, t) {
  const n = {}, r = c(e, ["data"]);
  if (r != null && f(n, ["data"], r), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const s = c(e, ["mimeType"]);
  return s != null && f(n, ["mimeType"], s), n;
}
function qK(e, t) {
  const n = {}, r = c(e, ["content"]);
  r != null && f(n, ["content"], r);
  const s = c(e, ["citationMetadata"]);
  s != null && f(n, ["citationMetadata"], HK(s));
  const o = c(e, ["tokenCount"]);
  o != null && f(n, ["tokenCount"], o);
  const i = c(e, ["finishReason"]);
  i != null && f(n, ["finishReason"], i);
  const a = c(e, ["groundingMetadata"]);
  a != null && f(n, ["groundingMetadata"], a);
  const l = c(e, ["avgLogprobs"]);
  l != null && f(n, ["avgLogprobs"], l);
  const u = c(e, ["index"]);
  u != null && f(n, ["index"], u);
  const d = c(e, ["logprobsResult"]);
  d != null && f(n, ["logprobsResult"], d);
  const h = c(e, ["safetyRatings"]);
  if (h != null) {
    let y = h;
    Array.isArray(y) && (y = y.map((w) => w)), f(n, ["safetyRatings"], y);
  }
  const p = c(e, ["urlContextMetadata"]);
  return p != null && f(n, ["urlContextMetadata"], p), n;
}
function HK(e, t) {
  const n = {}, r = c(e, ["citationSources"]);
  if (r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((o) => o)), f(n, ["citations"], s);
  }
  return n;
}
function GK(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["contents"]);
  if (o != null) {
    let i = rr(o);
    Array.isArray(i) && (i = i.map((a) => Bl(a))), f(r, ["contents"], i);
  }
  return r;
}
function WK(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["tokensInfo"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((i) => i)), f(n, ["tokensInfo"], o);
  }
  return n;
}
function VK(e, t) {
  const n = {}, r = c(e, ["values"]);
  r != null && f(n, ["values"], r);
  const s = c(e, ["statistics"]);
  return s != null && f(n, ["statistics"], KK(s)), n;
}
function KK(e, t) {
  const n = {}, r = c(e, ["truncated"]);
  r != null && f(n, ["truncated"], r);
  const s = c(e, ["token_count"]);
  return s != null && f(n, ["tokenCount"], s), n;
}
function dd(e, t) {
  const n = {}, r = c(e, ["parts"]);
  if (r != null) {
    let o = r;
    Array.isArray(o) && (o = o.map((i) => tz(i))), f(n, ["parts"], o);
  }
  const s = c(e, ["role"]);
  return s != null && f(n, ["role"], s), n;
}
function Bl(e, t) {
  const n = {}, r = c(e, ["parts"]);
  if (r != null) {
    let o = r;
    Array.isArray(o) && (o = o.map((i) => nz(i))), f(n, ["parts"], o);
  }
  const s = c(e, ["role"]);
  return s != null && f(n, ["role"], s), n;
}
function zK(e, t) {
  const n = {}, r = c(e, ["controlType"]);
  r != null && f(n, ["controlType"], r);
  const s = c(e, ["enableControlImageComputation"]);
  return s != null && f(n, ["computeControl"], s), n;
}
function JK(e, t) {
  const n = {};
  if (c(e, ["systemInstruction"]) !== void 0) throw new Error("systemInstruction parameter is not supported in Gemini API.");
  if (c(e, ["tools"]) !== void 0) throw new Error("tools parameter is not supported in Gemini API.");
  if (c(e, ["generationConfig"]) !== void 0) throw new Error("generationConfig parameter is not supported in Gemini API.");
  return n;
}
function YK(e, t, n) {
  const r = {}, s = c(e, ["systemInstruction"]);
  t !== void 0 && s != null && f(t, ["systemInstruction"], Bl(bn(s)));
  const o = c(e, ["tools"]);
  if (t !== void 0 && o != null) {
    let a = o;
    Array.isArray(a) && (a = a.map((l) => kP(l))), f(t, ["tools"], a);
  }
  const i = c(e, ["generationConfig"]);
  return t !== void 0 && i != null && f(t, ["generationConfig"], U3(i)), r;
}
function XK(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["contents"]);
  if (o != null) {
    let a = rr(o);
    Array.isArray(a) && (a = a.map((l) => dd(l))), f(r, ["contents"], a);
  }
  const i = c(t, ["config"]);
  return i != null && JK(i), r;
}
function QK(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["contents"]);
  if (o != null) {
    let a = rr(o);
    Array.isArray(a) && (a = a.map((l) => Bl(l))), f(r, ["contents"], a);
  }
  const i = c(t, ["config"]);
  return i != null && YK(i, r), r;
}
function ZK(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["totalTokens"]);
  s != null && f(n, ["totalTokens"], s);
  const o = c(e, ["cachedContentTokenCount"]);
  return o != null && f(n, ["cachedContentTokenCount"], o), n;
}
function jK(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["totalTokens"]);
  return s != null && f(n, ["totalTokens"], s), n;
}
function e3(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  return s != null && f(r, ["_url", "name"], Rt(e, s)), r;
}
function t3(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  return s != null && f(r, ["_url", "name"], Rt(e, s)), r;
}
function n3(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  return r != null && f(n, ["sdkHttpResponse"], r), n;
}
function r3(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  return r != null && f(n, ["sdkHttpResponse"], r), n;
}
function s3(e, t, n) {
  const r = {}, s = c(e, ["outputGcsUri"]);
  t !== void 0 && s != null && f(t, ["parameters", "storageUri"], s);
  const o = c(e, ["negativePrompt"]);
  t !== void 0 && o != null && f(t, ["parameters", "negativePrompt"], o);
  const i = c(e, ["numberOfImages"]);
  t !== void 0 && i != null && f(t, ["parameters", "sampleCount"], i);
  const a = c(e, ["aspectRatio"]);
  t !== void 0 && a != null && f(t, ["parameters", "aspectRatio"], a);
  const l = c(e, ["guidanceScale"]);
  t !== void 0 && l != null && f(t, ["parameters", "guidanceScale"], l);
  const u = c(e, ["seed"]);
  t !== void 0 && u != null && f(t, ["parameters", "seed"], u);
  const d = c(e, ["safetyFilterLevel"]);
  t !== void 0 && d != null && f(t, ["parameters", "safetySetting"], d);
  const h = c(e, ["personGeneration"]);
  t !== void 0 && h != null && f(t, ["parameters", "personGeneration"], h);
  const p = c(e, ["includeSafetyAttributes"]);
  t !== void 0 && p != null && f(t, ["parameters", "includeSafetyAttributes"], p);
  const y = c(e, ["includeRaiReason"]);
  t !== void 0 && y != null && f(t, ["parameters", "includeRaiReason"], y);
  const w = c(e, ["language"]);
  t !== void 0 && w != null && f(t, ["parameters", "language"], w);
  const E = c(e, ["outputMimeType"]);
  t !== void 0 && E != null && f(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], E);
  const C = c(e, ["outputCompressionQuality"]);
  t !== void 0 && C != null && f(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], C);
  const M = c(e, ["addWatermark"]);
  t !== void 0 && M != null && f(t, ["parameters", "addWatermark"], M);
  const k = c(e, ["labels"]);
  t !== void 0 && k != null && f(t, ["labels"], k);
  const R = c(e, ["editMode"]);
  t !== void 0 && R != null && f(t, ["parameters", "editMode"], R);
  const D = c(e, ["baseSteps"]);
  return t !== void 0 && D != null && f(t, [
    "parameters",
    "editConfig",
    "baseSteps"
  ], D), r;
}
function o3(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["prompt"]);
  o != null && f(r, ["instances[0]", "prompt"], o);
  const i = c(t, ["referenceImages"]);
  if (i != null) {
    let l = i;
    Array.isArray(l) && (l = l.map((u) => lz(u))), f(r, ["instances[0]", "referenceImages"], l);
  }
  const a = c(t, ["config"]);
  return a != null && s3(a, r), r;
}
function i3(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["predictions"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((i) => em(i))), f(n, ["generatedImages"], o);
  }
  return n;
}
function a3(e, t, n) {
  const r = {}, s = c(e, ["taskType"]);
  t !== void 0 && s != null && f(t, ["requests[]", "taskType"], s);
  const o = c(e, ["title"]);
  t !== void 0 && o != null && f(t, ["requests[]", "title"], o);
  const i = c(e, ["outputDimensionality"]);
  if (t !== void 0 && i != null && f(t, ["requests[]", "outputDimensionality"], i), c(e, ["mimeType"]) !== void 0) throw new Error("mimeType parameter is not supported in Gemini API.");
  if (c(e, ["autoTruncate"]) !== void 0) throw new Error("autoTruncate parameter is not supported in Gemini API.");
  if (c(e, ["documentOcr"]) !== void 0) throw new Error("documentOcr parameter is not supported in Gemini API.");
  if (c(e, ["audioTrackExtraction"]) !== void 0) throw new Error("audioTrackExtraction parameter is not supported in Gemini API.");
  return r;
}
function l3(e, t, n) {
  const r = {};
  let s = c(n, ["embeddingApiType"]);
  if (s === void 0 && (s = "PREDICT"), s === "PREDICT") {
    const h = c(e, ["taskType"]);
    t !== void 0 && h != null && f(t, ["instances[]", "task_type"], h);
  } else if (s === "EMBED_CONTENT") {
    const h = c(e, ["taskType"]);
    t !== void 0 && h != null && f(t, ["embedContentConfig", "taskType"], h);
  }
  let o = c(n, ["embeddingApiType"]);
  if (o === void 0 && (o = "PREDICT"), o === "PREDICT") {
    const h = c(e, ["title"]);
    t !== void 0 && h != null && f(t, ["instances[]", "title"], h);
  } else if (o === "EMBED_CONTENT") {
    const h = c(e, ["title"]);
    t !== void 0 && h != null && f(t, ["embedContentConfig", "title"], h);
  }
  let i = c(n, ["embeddingApiType"]);
  if (i === void 0 && (i = "PREDICT"), i === "PREDICT") {
    const h = c(e, ["outputDimensionality"]);
    t !== void 0 && h != null && f(t, ["parameters", "outputDimensionality"], h);
  } else if (i === "EMBED_CONTENT") {
    const h = c(e, ["outputDimensionality"]);
    t !== void 0 && h != null && f(t, ["embedContentConfig", "outputDimensionality"], h);
  }
  let a = c(n, ["embeddingApiType"]);
  if (a === void 0 && (a = "PREDICT"), a === "PREDICT") {
    const h = c(e, ["mimeType"]);
    t !== void 0 && h != null && f(t, ["instances[]", "mimeType"], h);
  }
  let l = c(n, ["embeddingApiType"]);
  if (l === void 0 && (l = "PREDICT"), l === "PREDICT") {
    const h = c(e, ["autoTruncate"]);
    t !== void 0 && h != null && f(t, ["parameters", "autoTruncate"], h);
  } else if (l === "EMBED_CONTENT") {
    const h = c(e, ["autoTruncate"]);
    t !== void 0 && h != null && f(t, ["embedContentConfig", "autoTruncate"], h);
  }
  let u = c(n, ["embeddingApiType"]);
  if (u === void 0 && (u = "PREDICT"), u === "EMBED_CONTENT") {
    const h = c(e, ["documentOcr"]);
    t !== void 0 && h != null && f(t, ["embedContentConfig", "documentOcr"], h);
  }
  let d = c(n, ["embeddingApiType"]);
  if (d === void 0 && (d = "PREDICT"), d === "EMBED_CONTENT") {
    const h = c(e, ["audioTrackExtraction"]);
    t !== void 0 && h != null && f(t, ["embedContentConfig", "audioTrackExtraction"], h);
  }
  return r;
}
function u3(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["contents"]);
  if (o != null) {
    let u = hw(e, o);
    Array.isArray(u) && (u = u.map((d) => d)), f(r, ["requests[]", "content"], u);
  }
  const i = c(t, ["content"]);
  i != null && dd(bn(i));
  const a = c(t, ["config"]);
  a != null && a3(a, r);
  const l = c(t, ["model"]);
  return l !== void 0 && f(r, ["requests[]", "model"], Rt(e, l)), r;
}
function c3(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  let o = c(n, ["embeddingApiType"]);
  if (o === void 0 && (o = "PREDICT"), o === "PREDICT") {
    const l = c(t, ["contents"]);
    if (l != null) {
      let u = hw(e, l);
      Array.isArray(u) && (u = u.map((d) => d)), f(r, ["instances[]", "content"], u);
    }
  }
  let i = c(n, ["embeddingApiType"]);
  if (i === void 0 && (i = "PREDICT"), i === "EMBED_CONTENT") {
    const l = c(t, ["content"]);
    l != null && f(r, ["content"], Bl(bn(l)));
  }
  const a = c(t, ["config"]);
  return a != null && l3(a, r, n), r;
}
function d3(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["embeddings"]);
  if (s != null) {
    let i = s;
    Array.isArray(i) && (i = i.map((a) => a)), f(n, ["embeddings"], i);
  }
  const o = c(e, ["metadata"]);
  return o != null && f(n, ["metadata"], o), n;
}
function f3(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["predictions[]", "embeddings"]);
  if (s != null) {
    let i = s;
    Array.isArray(i) && (i = i.map((a) => VK(a))), f(n, ["embeddings"], i);
  }
  const o = c(e, ["metadata"]);
  if (o != null && f(n, ["metadata"], o), t && c(t, ["embeddingApiType"]) === "EMBED_CONTENT") {
    const i = c(e, ["embedding"]), a = c(e, ["usageMetadata"]), l = c(e, ["truncated"]);
    if (i) {
      const u = {};
      a && a.promptTokenCount && (u.tokenCount = a.promptTokenCount), l && (u.truncated = l), i.statistics = u, f(n, ["embeddings"], [i]);
    }
  }
  return n;
}
function h3(e, t) {
  const n = {}, r = c(e, ["endpoint"]);
  r != null && f(n, ["name"], r);
  const s = c(e, ["deployedModelId"]);
  return s != null && f(n, ["deployedModelId"], s), n;
}
function p3(e, t) {
  const n = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["fileUri"]);
  r != null && f(n, ["fileUri"], r);
  const s = c(e, ["mimeType"]);
  return s != null && f(n, ["mimeType"], s), n;
}
function m3(e, t) {
  const n = {}, r = c(e, ["id"]);
  r != null && f(n, ["id"], r);
  const s = c(e, ["args"]);
  s != null && f(n, ["args"], s);
  const o = c(e, ["name"]);
  if (o != null && f(n, ["name"], o), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return n;
}
function g3(e, t) {
  const n = {}, r = c(e, ["allowedFunctionNames"]);
  r != null && f(n, ["allowedFunctionNames"], r);
  const s = c(e, ["mode"]);
  if (s != null && f(n, ["mode"], s), c(e, ["streamFunctionCallArguments"]) !== void 0) throw new Error("streamFunctionCallArguments parameter is not supported in Gemini API.");
  return n;
}
function v3(e, t) {
  const n = {}, r = c(e, ["description"]);
  r != null && f(n, ["description"], r);
  const s = c(e, ["name"]);
  s != null && f(n, ["name"], s);
  const o = c(e, ["parameters"]);
  o != null && f(n, ["parameters"], o);
  const i = c(e, ["parametersJsonSchema"]);
  i != null && f(n, ["parametersJsonSchema"], i);
  const a = c(e, ["response"]);
  a != null && f(n, ["response"], a);
  const l = c(e, ["responseJsonSchema"]);
  if (l != null && f(n, ["responseJsonSchema"], l), c(e, ["behavior"]) !== void 0) throw new Error("behavior parameter is not supported in Vertex AI.");
  return n;
}
function y3(e, t, n, r) {
  const s = {}, o = c(t, ["systemInstruction"]);
  n !== void 0 && o != null && f(n, ["systemInstruction"], dd(bn(o)));
  const i = c(t, ["temperature"]);
  i != null && f(s, ["temperature"], i);
  const a = c(t, ["topP"]);
  a != null && f(s, ["topP"], a);
  const l = c(t, ["topK"]);
  l != null && f(s, ["topK"], l);
  const u = c(t, ["candidateCount"]);
  u != null && f(s, ["candidateCount"], u);
  const d = c(t, ["maxOutputTokens"]);
  d != null && f(s, ["maxOutputTokens"], d);
  const h = c(t, ["stopSequences"]);
  h != null && f(s, ["stopSequences"], h);
  const p = c(t, ["responseLogprobs"]);
  p != null && f(s, ["responseLogprobs"], p);
  const y = c(t, ["logprobs"]);
  y != null && f(s, ["logprobs"], y);
  const w = c(t, ["presencePenalty"]);
  w != null && f(s, ["presencePenalty"], w);
  const E = c(t, ["frequencyPenalty"]);
  E != null && f(s, ["frequencyPenalty"], E);
  const C = c(t, ["seed"]);
  C != null && f(s, ["seed"], C);
  const M = c(t, ["responseMimeType"]);
  M != null && f(s, ["responseMimeType"], M);
  const k = c(t, ["responseSchema"]);
  k != null && f(s, ["responseSchema"], pw(k));
  const R = c(t, ["responseJsonSchema"]);
  if (R != null && f(s, ["responseJsonSchema"], R), c(t, ["routingConfig"]) !== void 0) throw new Error("routingConfig parameter is not supported in Gemini API.");
  if (c(t, ["modelSelectionConfig"]) !== void 0) throw new Error("modelSelectionConfig parameter is not supported in Gemini API.");
  const D = c(t, ["safetySettings"]);
  if (n !== void 0 && D != null) {
    let V = D;
    Array.isArray(V) && (V = V.map((Z) => uz(Z))), f(n, ["safetySettings"], V);
  }
  const m = c(t, ["tools"]);
  if (n !== void 0 && m != null) {
    let V = Ol(m);
    Array.isArray(V) && (V = V.map((Z) => vz(Ll(Z)))), f(n, ["tools"], V);
  }
  const b = c(t, ["toolConfig"]);
  if (n !== void 0 && b != null && f(n, ["toolConfig"], mz(b)), c(t, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  const v = c(t, ["cachedContent"]);
  n !== void 0 && v != null && f(n, ["cachedContent"], _o(e, v));
  const T = c(t, ["responseModalities"]);
  T != null && f(s, ["responseModalities"], T);
  const I = c(t, ["mediaResolution"]);
  I != null && f(s, ["mediaResolution"], I);
  const x = c(t, ["speechConfig"]);
  if (x != null && f(s, ["speechConfig"], mw(x)), c(t, ["audioTimestamp"]) !== void 0) throw new Error("audioTimestamp parameter is not supported in Gemini API.");
  const P = c(t, ["thinkingConfig"]);
  P != null && f(s, ["thinkingConfig"], P);
  const L = c(t, ["imageConfig"]);
  L != null && f(s, ["imageConfig"], V3(L));
  const G = c(t, ["enableEnhancedCivicAnswers"]);
  if (G != null && f(s, ["enableEnhancedCivicAnswers"], G), c(t, ["modelArmorConfig"]) !== void 0) throw new Error("modelArmorConfig parameter is not supported in Gemini API.");
  const z = c(t, ["serviceTier"]);
  return n !== void 0 && z != null && f(n, ["serviceTier"], z), s;
}
function _3(e, t, n, r) {
  const s = {}, o = c(t, ["systemInstruction"]);
  n !== void 0 && o != null && f(n, ["systemInstruction"], Bl(bn(o)));
  const i = c(t, ["temperature"]);
  i != null && f(s, ["temperature"], i);
  const a = c(t, ["topP"]);
  a != null && f(s, ["topP"], a);
  const l = c(t, ["topK"]);
  l != null && f(s, ["topK"], l);
  const u = c(t, ["candidateCount"]);
  u != null && f(s, ["candidateCount"], u);
  const d = c(t, ["maxOutputTokens"]);
  d != null && f(s, ["maxOutputTokens"], d);
  const h = c(t, ["stopSequences"]);
  h != null && f(s, ["stopSequences"], h);
  const p = c(t, ["responseLogprobs"]);
  p != null && f(s, ["responseLogprobs"], p);
  const y = c(t, ["logprobs"]);
  y != null && f(s, ["logprobs"], y);
  const w = c(t, ["presencePenalty"]);
  w != null && f(s, ["presencePenalty"], w);
  const E = c(t, ["frequencyPenalty"]);
  E != null && f(s, ["frequencyPenalty"], E);
  const C = c(t, ["seed"]);
  C != null && f(s, ["seed"], C);
  const M = c(t, ["responseMimeType"]);
  M != null && f(s, ["responseMimeType"], M);
  const k = c(t, ["responseSchema"]);
  k != null && f(s, ["responseSchema"], pw(k));
  const R = c(t, ["responseJsonSchema"]);
  R != null && f(s, ["responseJsonSchema"], R);
  const D = c(t, ["routingConfig"]);
  D != null && f(s, ["routingConfig"], D);
  const m = c(t, ["modelSelectionConfig"]);
  m != null && f(s, ["modelConfig"], m);
  const b = c(t, ["safetySettings"]);
  if (n !== void 0 && b != null) {
    let X = b;
    Array.isArray(X) && (X = X.map((j) => j)), f(n, ["safetySettings"], X);
  }
  const v = c(t, ["tools"]);
  if (n !== void 0 && v != null) {
    let X = Ol(v);
    Array.isArray(X) && (X = X.map((j) => kP(Ll(j)))), f(n, ["tools"], X);
  }
  const T = c(t, ["toolConfig"]);
  n !== void 0 && T != null && f(n, ["toolConfig"], gz(T));
  const I = c(t, ["labels"]);
  n !== void 0 && I != null && f(n, ["labels"], I);
  const x = c(t, ["cachedContent"]);
  n !== void 0 && x != null && f(n, ["cachedContent"], _o(e, x));
  const P = c(t, ["responseModalities"]);
  P != null && f(s, ["responseModalities"], P);
  const L = c(t, ["mediaResolution"]);
  L != null && f(s, ["mediaResolution"], L);
  const G = c(t, ["speechConfig"]);
  G != null && f(s, ["speechConfig"], mw(G));
  const z = c(t, ["audioTimestamp"]);
  z != null && f(s, ["audioTimestamp"], z);
  const V = c(t, ["thinkingConfig"]);
  V != null && f(s, ["thinkingConfig"], V);
  const Z = c(t, ["imageConfig"]);
  if (Z != null && f(s, ["imageConfig"], K3(Z)), c(t, ["enableEnhancedCivicAnswers"]) !== void 0) throw new Error("enableEnhancedCivicAnswers parameter is not supported in Vertex AI.");
  const O = c(t, ["modelArmorConfig"]);
  n !== void 0 && O != null && f(n, ["modelArmorConfig"], O);
  const Y = c(t, ["serviceTier"]);
  return n !== void 0 && Y != null && f(n, ["serviceTier"], Y), s;
}
function TC(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["contents"]);
  if (o != null) {
    let a = rr(o);
    Array.isArray(a) && (a = a.map((l) => dd(l))), f(r, ["contents"], a);
  }
  const i = c(t, ["config"]);
  return i != null && f(r, ["generationConfig"], y3(e, i, r)), r;
}
function EC(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["contents"]);
  if (o != null) {
    let a = rr(o);
    Array.isArray(a) && (a = a.map((l) => Bl(l))), f(r, ["contents"], a);
  }
  const i = c(t, ["config"]);
  return i != null && f(r, ["generationConfig"], _3(e, i, r)), r;
}
function CC(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["candidates"]);
  if (s != null) {
    let d = s;
    Array.isArray(d) && (d = d.map((h) => qK(h))), f(n, ["candidates"], d);
  }
  const o = c(e, ["modelVersion"]);
  o != null && f(n, ["modelVersion"], o);
  const i = c(e, ["promptFeedback"]);
  i != null && f(n, ["promptFeedback"], i);
  const a = c(e, ["responseId"]);
  a != null && f(n, ["responseId"], a);
  const l = c(e, ["usageMetadata"]);
  l != null && f(n, ["usageMetadata"], l);
  const u = c(e, ["modelStatus"]);
  return u != null && f(n, ["modelStatus"], u), n;
}
function AC(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["candidates"]);
  if (s != null) {
    let d = s;
    Array.isArray(d) && (d = d.map((h) => h)), f(n, ["candidates"], d);
  }
  const o = c(e, ["createTime"]);
  o != null && f(n, ["createTime"], o);
  const i = c(e, ["modelVersion"]);
  i != null && f(n, ["modelVersion"], i);
  const a = c(e, ["promptFeedback"]);
  a != null && f(n, ["promptFeedback"], a);
  const l = c(e, ["responseId"]);
  l != null && f(n, ["responseId"], l);
  const u = c(e, ["usageMetadata"]);
  return u != null && f(n, ["usageMetadata"], u), n;
}
function b3(e, t, n) {
  const r = {};
  if (c(e, ["outputGcsUri"]) !== void 0) throw new Error("outputGcsUri parameter is not supported in Gemini API.");
  if (c(e, ["negativePrompt"]) !== void 0) throw new Error("negativePrompt parameter is not supported in Gemini API.");
  const s = c(e, ["numberOfImages"]);
  t !== void 0 && s != null && f(t, ["parameters", "sampleCount"], s);
  const o = c(e, ["aspectRatio"]);
  t !== void 0 && o != null && f(t, ["parameters", "aspectRatio"], o);
  const i = c(e, ["guidanceScale"]);
  if (t !== void 0 && i != null && f(t, ["parameters", "guidanceScale"], i), c(e, ["seed"]) !== void 0) throw new Error("seed parameter is not supported in Gemini API.");
  const a = c(e, ["safetyFilterLevel"]);
  t !== void 0 && a != null && f(t, ["parameters", "safetySetting"], a);
  const l = c(e, ["personGeneration"]);
  t !== void 0 && l != null && f(t, ["parameters", "personGeneration"], l);
  const u = c(e, ["includeSafetyAttributes"]);
  t !== void 0 && u != null && f(t, ["parameters", "includeSafetyAttributes"], u);
  const d = c(e, ["includeRaiReason"]);
  t !== void 0 && d != null && f(t, ["parameters", "includeRaiReason"], d);
  const h = c(e, ["language"]);
  t !== void 0 && h != null && f(t, ["parameters", "language"], h);
  const p = c(e, ["outputMimeType"]);
  t !== void 0 && p != null && f(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], p);
  const y = c(e, ["outputCompressionQuality"]);
  if (t !== void 0 && y != null && f(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], y), c(e, ["addWatermark"]) !== void 0) throw new Error("addWatermark parameter is not supported in Gemini API.");
  if (c(e, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  const w = c(e, ["imageSize"]);
  if (t !== void 0 && w != null && f(t, ["parameters", "sampleImageSize"], w), c(e, ["enhancePrompt"]) !== void 0) throw new Error("enhancePrompt parameter is not supported in Gemini API.");
  return r;
}
function w3(e, t, n) {
  const r = {}, s = c(e, ["outputGcsUri"]);
  t !== void 0 && s != null && f(t, ["parameters", "storageUri"], s);
  const o = c(e, ["negativePrompt"]);
  t !== void 0 && o != null && f(t, ["parameters", "negativePrompt"], o);
  const i = c(e, ["numberOfImages"]);
  t !== void 0 && i != null && f(t, ["parameters", "sampleCount"], i);
  const a = c(e, ["aspectRatio"]);
  t !== void 0 && a != null && f(t, ["parameters", "aspectRatio"], a);
  const l = c(e, ["guidanceScale"]);
  t !== void 0 && l != null && f(t, ["parameters", "guidanceScale"], l);
  const u = c(e, ["seed"]);
  t !== void 0 && u != null && f(t, ["parameters", "seed"], u);
  const d = c(e, ["safetyFilterLevel"]);
  t !== void 0 && d != null && f(t, ["parameters", "safetySetting"], d);
  const h = c(e, ["personGeneration"]);
  t !== void 0 && h != null && f(t, ["parameters", "personGeneration"], h);
  const p = c(e, ["includeSafetyAttributes"]);
  t !== void 0 && p != null && f(t, ["parameters", "includeSafetyAttributes"], p);
  const y = c(e, ["includeRaiReason"]);
  t !== void 0 && y != null && f(t, ["parameters", "includeRaiReason"], y);
  const w = c(e, ["language"]);
  t !== void 0 && w != null && f(t, ["parameters", "language"], w);
  const E = c(e, ["outputMimeType"]);
  t !== void 0 && E != null && f(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], E);
  const C = c(e, ["outputCompressionQuality"]);
  t !== void 0 && C != null && f(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], C);
  const M = c(e, ["addWatermark"]);
  t !== void 0 && M != null && f(t, ["parameters", "addWatermark"], M);
  const k = c(e, ["labels"]);
  t !== void 0 && k != null && f(t, ["labels"], k);
  const R = c(e, ["imageSize"]);
  t !== void 0 && R != null && f(t, ["parameters", "sampleImageSize"], R);
  const D = c(e, ["enhancePrompt"]);
  return t !== void 0 && D != null && f(t, ["parameters", "enhancePrompt"], D), r;
}
function S3(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["prompt"]);
  o != null && f(r, ["instances[0]", "prompt"], o);
  const i = c(t, ["config"]);
  return i != null && b3(i, r), r;
}
function T3(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["prompt"]);
  o != null && f(r, ["instances[0]", "prompt"], o);
  const i = c(t, ["config"]);
  return i != null && w3(i, r), r;
}
function E3(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["predictions"]);
  if (s != null) {
    let i = s;
    Array.isArray(i) && (i = i.map((a) => L3(a))), f(n, ["generatedImages"], i);
  }
  const o = c(e, ["positivePromptSafetyAttributes"]);
  return o != null && f(n, ["positivePromptSafetyAttributes"], IP(o)), n;
}
function C3(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["predictions"]);
  if (s != null) {
    let i = s;
    Array.isArray(i) && (i = i.map((a) => em(a))), f(n, ["generatedImages"], i);
  }
  const o = c(e, ["positivePromptSafetyAttributes"]);
  return o != null && f(n, ["positivePromptSafetyAttributes"], xP(o)), n;
}
function A3(e, t, n) {
  const r = {}, s = c(e, ["numberOfVideos"]);
  if (t !== void 0 && s != null && f(t, ["parameters", "sampleCount"], s), c(e, ["outputGcsUri"]) !== void 0) throw new Error("outputGcsUri parameter is not supported in Gemini API.");
  if (c(e, ["fps"]) !== void 0) throw new Error("fps parameter is not supported in Gemini API.");
  const o = c(e, ["durationSeconds"]);
  if (t !== void 0 && o != null && f(t, ["parameters", "durationSeconds"], o), c(e, ["seed"]) !== void 0) throw new Error("seed parameter is not supported in Gemini API.");
  const i = c(e, ["aspectRatio"]);
  t !== void 0 && i != null && f(t, ["parameters", "aspectRatio"], i);
  const a = c(e, ["resolution"]);
  t !== void 0 && a != null && f(t, ["parameters", "resolution"], a);
  const l = c(e, ["personGeneration"]);
  if (t !== void 0 && l != null && f(t, ["parameters", "personGeneration"], l), c(e, ["pubsubTopic"]) !== void 0) throw new Error("pubsubTopic parameter is not supported in Gemini API.");
  const u = c(e, ["negativePrompt"]);
  t !== void 0 && u != null && f(t, ["parameters", "negativePrompt"], u);
  const d = c(e, ["enhancePrompt"]);
  if (t !== void 0 && d != null && f(t, ["parameters", "enhancePrompt"], d), c(e, ["generateAudio"]) !== void 0) throw new Error("generateAudio parameter is not supported in Gemini API.");
  const h = c(e, ["lastFrame"]);
  t !== void 0 && h != null && f(t, ["instances[0]", "lastFrame"], tm(h));
  const p = c(e, ["referenceImages"]);
  if (t !== void 0 && p != null) {
    let w = p;
    Array.isArray(w) && (w = w.map((E) => Mz(E))), f(t, ["instances[0]", "referenceImages"], w);
  }
  if (c(e, ["mask"]) !== void 0) throw new Error("mask parameter is not supported in Gemini API.");
  if (c(e, ["compressionQuality"]) !== void 0) throw new Error("compressionQuality parameter is not supported in Gemini API.");
  if (c(e, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  const y = c(e, ["webhookConfig"]);
  return t !== void 0 && y != null && f(t, ["webhookConfig"], y), r;
}
function I3(e, t, n) {
  const r = {}, s = c(e, ["numberOfVideos"]);
  t !== void 0 && s != null && f(t, ["parameters", "sampleCount"], s);
  const o = c(e, ["outputGcsUri"]);
  t !== void 0 && o != null && f(t, ["parameters", "storageUri"], o);
  const i = c(e, ["fps"]);
  t !== void 0 && i != null && f(t, ["parameters", "fps"], i);
  const a = c(e, ["durationSeconds"]);
  t !== void 0 && a != null && f(t, ["parameters", "durationSeconds"], a);
  const l = c(e, ["seed"]);
  t !== void 0 && l != null && f(t, ["parameters", "seed"], l);
  const u = c(e, ["aspectRatio"]);
  t !== void 0 && u != null && f(t, ["parameters", "aspectRatio"], u);
  const d = c(e, ["resolution"]);
  t !== void 0 && d != null && f(t, ["parameters", "resolution"], d);
  const h = c(e, ["personGeneration"]);
  t !== void 0 && h != null && f(t, ["parameters", "personGeneration"], h);
  const p = c(e, ["pubsubTopic"]);
  t !== void 0 && p != null && f(t, ["parameters", "pubsubTopic"], p);
  const y = c(e, ["negativePrompt"]);
  t !== void 0 && y != null && f(t, ["parameters", "negativePrompt"], y);
  const w = c(e, ["enhancePrompt"]);
  t !== void 0 && w != null && f(t, ["parameters", "enhancePrompt"], w);
  const E = c(e, ["generateAudio"]);
  t !== void 0 && E != null && f(t, ["parameters", "generateAudio"], E);
  const C = c(e, ["lastFrame"]);
  t !== void 0 && C != null && f(t, ["instances[0]", "lastFrame"], hs(C));
  const M = c(e, ["referenceImages"]);
  if (t !== void 0 && M != null) {
    let m = M;
    Array.isArray(m) && (m = m.map((b) => Pz(b))), f(t, ["instances[0]", "referenceImages"], m);
  }
  const k = c(e, ["mask"]);
  t !== void 0 && k != null && f(t, ["instances[0]", "mask"], kz(k));
  const R = c(e, ["compressionQuality"]);
  t !== void 0 && R != null && f(t, ["parameters", "compressionQuality"], R);
  const D = c(e, ["labels"]);
  if (t !== void 0 && D != null && f(t, ["labels"], D), c(e, ["webhookConfig"]) !== void 0) throw new Error("webhookConfig parameter is not supported in Vertex AI.");
  return r;
}
function x3(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && f(n, ["name"], r);
  const s = c(e, ["metadata"]);
  s != null && f(n, ["metadata"], s);
  const o = c(e, ["done"]);
  o != null && f(n, ["done"], o);
  const i = c(e, ["error"]);
  i != null && f(n, ["error"], i);
  const a = c(e, ["response", "generateVideoResponse"]);
  return a != null && f(n, ["response"], R3(a)), n;
}
function k3(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && f(n, ["name"], r);
  const s = c(e, ["metadata"]);
  s != null && f(n, ["metadata"], s);
  const o = c(e, ["done"]);
  o != null && f(n, ["done"], o);
  const i = c(e, ["error"]);
  i != null && f(n, ["error"], i);
  const a = c(e, ["response"]);
  return a != null && f(n, ["response"], N3(a)), n;
}
function M3(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["prompt"]);
  o != null && f(r, ["instances[0]", "prompt"], o);
  const i = c(t, ["image"]);
  i != null && f(r, ["instances[0]", "image"], tm(i));
  const a = c(t, ["video"]);
  a != null && f(r, ["instances[0]", "video"], MP(a));
  const l = c(t, ["source"]);
  l != null && $3(l, r);
  const u = c(t, ["config"]);
  return u != null && A3(u, r), r;
}
function P3(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["prompt"]);
  o != null && f(r, ["instances[0]", "prompt"], o);
  const i = c(t, ["image"]);
  i != null && f(r, ["instances[0]", "image"], hs(i));
  const a = c(t, ["video"]);
  a != null && f(r, ["instances[0]", "video"], PP(a));
  const l = c(t, ["source"]);
  l != null && D3(l, r);
  const u = c(t, ["config"]);
  return u != null && I3(u, r), r;
}
function R3(e, t) {
  const n = {}, r = c(e, ["generatedSamples"]);
  if (r != null) {
    let i = r;
    Array.isArray(i) && (i = i.map((a) => F3(a))), f(n, ["generatedVideos"], i);
  }
  const s = c(e, ["raiMediaFilteredCount"]);
  s != null && f(n, ["raiMediaFilteredCount"], s);
  const o = c(e, ["raiMediaFilteredReasons"]);
  return o != null && f(n, ["raiMediaFilteredReasons"], o), n;
}
function N3(e, t) {
  const n = {}, r = c(e, ["videos"]);
  if (r != null) {
    let i = r;
    Array.isArray(i) && (i = i.map((a) => B3(a))), f(n, ["generatedVideos"], i);
  }
  const s = c(e, ["raiMediaFilteredCount"]);
  s != null && f(n, ["raiMediaFilteredCount"], s);
  const o = c(e, ["raiMediaFilteredReasons"]);
  return o != null && f(n, ["raiMediaFilteredReasons"], o), n;
}
function $3(e, t, n) {
  const r = {}, s = c(e, ["prompt"]);
  t !== void 0 && s != null && f(t, ["instances[0]", "prompt"], s);
  const o = c(e, ["image"]);
  t !== void 0 && o != null && f(t, ["instances[0]", "image"], tm(o));
  const i = c(e, ["video"]);
  return t !== void 0 && i != null && f(t, ["instances[0]", "video"], MP(i)), r;
}
function D3(e, t, n) {
  const r = {}, s = c(e, ["prompt"]);
  t !== void 0 && s != null && f(t, ["instances[0]", "prompt"], s);
  const o = c(e, ["image"]);
  t !== void 0 && o != null && f(t, ["instances[0]", "image"], hs(o));
  const i = c(e, ["video"]);
  return t !== void 0 && i != null && f(t, ["instances[0]", "video"], PP(i)), r;
}
function L3(e, t) {
  const n = {}, r = c(e, ["_self"]);
  r != null && f(n, ["image"], z3(r));
  const s = c(e, ["raiFilteredReason"]);
  s != null && f(n, ["raiFilteredReason"], s);
  const o = c(e, ["_self"]);
  return o != null && f(n, ["safetyAttributes"], IP(o)), n;
}
function em(e, t) {
  const n = {}, r = c(e, ["_self"]);
  r != null && f(n, ["image"], AP(r));
  const s = c(e, ["raiFilteredReason"]);
  s != null && f(n, ["raiFilteredReason"], s);
  const o = c(e, ["_self"]);
  o != null && f(n, ["safetyAttributes"], xP(o));
  const i = c(e, ["prompt"]);
  return i != null && f(n, ["enhancedPrompt"], i), n;
}
function O3(e, t) {
  const n = {}, r = c(e, ["_self"]);
  r != null && f(n, ["mask"], AP(r));
  const s = c(e, ["labels"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((i) => i)), f(n, ["labels"], o);
  }
  return n;
}
function F3(e, t) {
  const n = {}, r = c(e, ["video"]);
  return r != null && f(n, ["video"], Iz(r)), n;
}
function B3(e, t) {
  const n = {}, r = c(e, ["_self"]);
  return r != null && f(n, ["video"], xz(r)), n;
}
function U3(e, t) {
  const n = {}, r = c(e, ["modelSelectionConfig"]);
  r != null && f(n, ["modelConfig"], r);
  const s = c(e, ["responseJsonSchema"]);
  s != null && f(n, ["responseJsonSchema"], s);
  const o = c(e, ["audioTimestamp"]);
  o != null && f(n, ["audioTimestamp"], o);
  const i = c(e, ["candidateCount"]);
  i != null && f(n, ["candidateCount"], i);
  const a = c(e, ["enableAffectiveDialog"]);
  a != null && f(n, ["enableAffectiveDialog"], a);
  const l = c(e, ["frequencyPenalty"]);
  l != null && f(n, ["frequencyPenalty"], l);
  const u = c(e, ["logprobs"]);
  u != null && f(n, ["logprobs"], u);
  const d = c(e, ["maxOutputTokens"]);
  d != null && f(n, ["maxOutputTokens"], d);
  const h = c(e, ["mediaResolution"]);
  h != null && f(n, ["mediaResolution"], h);
  const p = c(e, ["presencePenalty"]);
  p != null && f(n, ["presencePenalty"], p);
  const y = c(e, ["responseLogprobs"]);
  y != null && f(n, ["responseLogprobs"], y);
  const w = c(e, ["responseMimeType"]);
  w != null && f(n, ["responseMimeType"], w);
  const E = c(e, ["responseModalities"]);
  E != null && f(n, ["responseModalities"], E);
  const C = c(e, ["responseSchema"]);
  C != null && f(n, ["responseSchema"], C);
  const M = c(e, ["routingConfig"]);
  M != null && f(n, ["routingConfig"], M);
  const k = c(e, ["seed"]);
  k != null && f(n, ["seed"], k);
  const R = c(e, ["speechConfig"]);
  R != null && f(n, ["speechConfig"], R);
  const D = c(e, ["stopSequences"]);
  D != null && f(n, ["stopSequences"], D);
  const m = c(e, ["temperature"]);
  m != null && f(n, ["temperature"], m);
  const b = c(e, ["thinkingConfig"]);
  b != null && f(n, ["thinkingConfig"], b);
  const v = c(e, ["topK"]);
  v != null && f(n, ["topK"], v);
  const T = c(e, ["topP"]);
  if (T != null && f(n, ["topP"], T), c(e, ["enableEnhancedCivicAnswers"]) !== void 0) throw new Error("enableEnhancedCivicAnswers parameter is not supported in Vertex AI.");
  return n;
}
function q3(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  return s != null && f(r, ["_url", "name"], Rt(e, s)), r;
}
function H3(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  return s != null && f(r, ["_url", "name"], Rt(e, s)), r;
}
function G3(e, t) {
  const n = {}, r = c(e, ["authConfig"]);
  r != null && f(n, ["authConfig"], BK(r));
  const s = c(e, ["enableWidget"]);
  return s != null && f(n, ["enableWidget"], s), n;
}
function W3(e, t) {
  const n = {}, r = c(e, ["searchTypes"]);
  if (r != null && f(n, ["searchTypes"], r), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const s = c(e, ["timeRangeFilter"]);
  return s != null && f(n, ["timeRangeFilter"], s), n;
}
function V3(e, t) {
  const n = {}, r = c(e, ["aspectRatio"]);
  r != null && f(n, ["aspectRatio"], r);
  const s = c(e, ["imageSize"]);
  if (s != null && f(n, ["imageSize"], s), c(e, ["personGeneration"]) !== void 0) throw new Error("personGeneration parameter is not supported in Gemini API.");
  if (c(e, ["prominentPeople"]) !== void 0) throw new Error("prominentPeople parameter is not supported in Gemini API.");
  if (c(e, ["outputMimeType"]) !== void 0) throw new Error("outputMimeType parameter is not supported in Gemini API.");
  if (c(e, ["outputCompressionQuality"]) !== void 0) throw new Error("outputCompressionQuality parameter is not supported in Gemini API.");
  if (c(e, ["imageOutputOptions"]) !== void 0) throw new Error("imageOutputOptions parameter is not supported in Gemini API.");
  return n;
}
function K3(e, t) {
  const n = {}, r = c(e, ["aspectRatio"]);
  r != null && f(n, ["aspectRatio"], r);
  const s = c(e, ["imageSize"]);
  s != null && f(n, ["imageSize"], s);
  const o = c(e, ["personGeneration"]);
  o != null && f(n, ["personGeneration"], o);
  const i = c(e, ["prominentPeople"]);
  i != null && f(n, ["prominentPeople"], i);
  const a = c(e, ["outputMimeType"]);
  a != null && f(n, ["imageOutputOptions", "mimeType"], a);
  const l = c(e, ["outputCompressionQuality"]);
  l != null && f(n, ["imageOutputOptions", "compressionQuality"], l);
  const u = c(e, ["imageOutputOptions"]);
  return u != null && f(n, ["imageOutputOptions"], u), n;
}
function z3(e, t) {
  const n = {}, r = c(e, ["bytesBase64Encoded"]);
  r != null && f(n, ["imageBytes"], ii(r));
  const s = c(e, ["mimeType"]);
  return s != null && f(n, ["mimeType"], s), n;
}
function AP(e, t) {
  const n = {}, r = c(e, ["gcsUri"]);
  r != null && f(n, ["gcsUri"], r);
  const s = c(e, ["bytesBase64Encoded"]);
  s != null && f(n, ["imageBytes"], ii(s));
  const o = c(e, ["mimeType"]);
  return o != null && f(n, ["mimeType"], o), n;
}
function tm(e, t) {
  const n = {};
  if (c(e, ["gcsUri"]) !== void 0) throw new Error("gcsUri parameter is not supported in Gemini API.");
  const r = c(e, ["imageBytes"]);
  r != null && f(n, ["bytesBase64Encoded"], ii(r));
  const s = c(e, ["mimeType"]);
  return s != null && f(n, ["mimeType"], s), n;
}
function hs(e, t) {
  const n = {}, r = c(e, ["gcsUri"]);
  r != null && f(n, ["gcsUri"], r);
  const s = c(e, ["imageBytes"]);
  s != null && f(n, ["bytesBase64Encoded"], ii(s));
  const o = c(e, ["mimeType"]);
  return o != null && f(n, ["mimeType"], o), n;
}
function J3(e, t, n, r) {
  const s = {}, o = c(t, ["pageSize"]);
  n !== void 0 && o != null && f(n, ["_query", "pageSize"], o);
  const i = c(t, ["pageToken"]);
  n !== void 0 && i != null && f(n, ["_query", "pageToken"], i);
  const a = c(t, ["filter"]);
  n !== void 0 && a != null && f(n, ["_query", "filter"], a);
  const l = c(t, ["queryBase"]);
  return n !== void 0 && l != null && f(n, ["_url", "models_url"], vP(e, l)), s;
}
function Y3(e, t, n, r) {
  const s = {}, o = c(t, ["pageSize"]);
  n !== void 0 && o != null && f(n, ["_query", "pageSize"], o);
  const i = c(t, ["pageToken"]);
  n !== void 0 && i != null && f(n, ["_query", "pageToken"], i);
  const a = c(t, ["filter"]);
  n !== void 0 && a != null && f(n, ["_query", "filter"], a);
  const l = c(t, ["queryBase"]);
  return n !== void 0 && l != null && f(n, ["_url", "models_url"], vP(e, l)), s;
}
function X3(e, t, n) {
  const r = {}, s = c(t, ["config"]);
  return s != null && J3(e, s, r), r;
}
function Q3(e, t, n) {
  const r = {}, s = c(t, ["config"]);
  return s != null && Y3(e, s, r), r;
}
function Z3(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["nextPageToken"]);
  s != null && f(n, ["nextPageToken"], s);
  const o = c(e, ["_self"]);
  if (o != null) {
    let i = yP(o);
    Array.isArray(i) && (i = i.map((a) => Xy(a))), f(n, ["models"], i);
  }
  return n;
}
function j3(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["nextPageToken"]);
  s != null && f(n, ["nextPageToken"], s);
  const o = c(e, ["_self"]);
  if (o != null) {
    let i = yP(o);
    Array.isArray(i) && (i = i.map((a) => Qy(a))), f(n, ["models"], i);
  }
  return n;
}
function ez(e, t) {
  const n = {}, r = c(e, ["maskMode"]);
  r != null && f(n, ["maskMode"], r);
  const s = c(e, ["segmentationClasses"]);
  s != null && f(n, ["maskClasses"], s);
  const o = c(e, ["maskDilation"]);
  return o != null && f(n, ["dilation"], o), n;
}
function Xy(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && f(n, ["name"], r);
  const s = c(e, ["displayName"]);
  s != null && f(n, ["displayName"], s);
  const o = c(e, ["description"]);
  o != null && f(n, ["description"], o);
  const i = c(e, ["version"]);
  i != null && f(n, ["version"], i);
  const a = c(e, ["_self"]);
  a != null && f(n, ["tunedModelInfo"], yz(a));
  const l = c(e, ["inputTokenLimit"]);
  l != null && f(n, ["inputTokenLimit"], l);
  const u = c(e, ["outputTokenLimit"]);
  u != null && f(n, ["outputTokenLimit"], u);
  const d = c(e, ["supportedGenerationMethods"]);
  d != null && f(n, ["supportedActions"], d);
  const h = c(e, ["temperature"]);
  h != null && f(n, ["temperature"], h);
  const p = c(e, ["maxTemperature"]);
  p != null && f(n, ["maxTemperature"], p);
  const y = c(e, ["topP"]);
  y != null && f(n, ["topP"], y);
  const w = c(e, ["topK"]);
  w != null && f(n, ["topK"], w);
  const E = c(e, ["thinking"]);
  return E != null && f(n, ["thinking"], E), n;
}
function Qy(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && f(n, ["name"], r);
  const s = c(e, ["displayName"]);
  s != null && f(n, ["displayName"], s);
  const o = c(e, ["description"]);
  o != null && f(n, ["description"], o);
  const i = c(e, ["versionId"]);
  i != null && f(n, ["version"], i);
  const a = c(e, ["deployedModels"]);
  if (a != null) {
    let p = a;
    Array.isArray(p) && (p = p.map((y) => h3(y))), f(n, ["endpoints"], p);
  }
  const l = c(e, ["labels"]);
  l != null && f(n, ["labels"], l);
  const u = c(e, ["_self"]);
  u != null && f(n, ["tunedModelInfo"], _z(u));
  const d = c(e, ["defaultCheckpointId"]);
  d != null && f(n, ["defaultCheckpointId"], d);
  const h = c(e, ["checkpoints"]);
  if (h != null) {
    let p = h;
    Array.isArray(p) && (p = p.map((y) => y)), f(n, ["checkpoints"], p);
  }
  return n;
}
function tz(e, t) {
  const n = {}, r = c(e, ["mediaResolution"]);
  r != null && f(n, ["mediaResolution"], r);
  const s = c(e, ["codeExecutionResult"]);
  s != null && f(n, ["codeExecutionResult"], s);
  const o = c(e, ["executableCode"]);
  o != null && f(n, ["executableCode"], o);
  const i = c(e, ["fileData"]);
  i != null && f(n, ["fileData"], p3(i));
  const a = c(e, ["functionCall"]);
  a != null && f(n, ["functionCall"], m3(a));
  const l = c(e, ["functionResponse"]);
  l != null && f(n, ["functionResponse"], l);
  const u = c(e, ["inlineData"]);
  u != null && f(n, ["inlineData"], UK(u));
  const d = c(e, ["text"]);
  d != null && f(n, ["text"], d);
  const h = c(e, ["thought"]);
  h != null && f(n, ["thought"], h);
  const p = c(e, ["thoughtSignature"]);
  p != null && f(n, ["thoughtSignature"], p);
  const y = c(e, ["videoMetadata"]);
  y != null && f(n, ["videoMetadata"], y);
  const w = c(e, ["toolCall"]);
  w != null && f(n, ["toolCall"], w);
  const E = c(e, ["toolResponse"]);
  E != null && f(n, ["toolResponse"], E);
  const C = c(e, ["partMetadata"]);
  return C != null && f(n, ["partMetadata"], C), n;
}
function nz(e, t) {
  const n = {}, r = c(e, ["mediaResolution"]);
  r != null && f(n, ["mediaResolution"], r);
  const s = c(e, ["codeExecutionResult"]);
  s != null && f(n, ["codeExecutionResult"], s);
  const o = c(e, ["executableCode"]);
  o != null && f(n, ["executableCode"], o);
  const i = c(e, ["fileData"]);
  i != null && f(n, ["fileData"], i);
  const a = c(e, ["functionCall"]);
  a != null && f(n, ["functionCall"], a);
  const l = c(e, ["functionResponse"]);
  l != null && f(n, ["functionResponse"], l);
  const u = c(e, ["inlineData"]);
  u != null && f(n, ["inlineData"], u);
  const d = c(e, ["text"]);
  d != null && f(n, ["text"], d);
  const h = c(e, ["thought"]);
  h != null && f(n, ["thought"], h);
  const p = c(e, ["thoughtSignature"]);
  p != null && f(n, ["thoughtSignature"], p);
  const y = c(e, ["videoMetadata"]);
  if (y != null && f(n, ["videoMetadata"], y), c(e, ["toolCall"]) !== void 0) throw new Error("toolCall parameter is not supported in Vertex AI.");
  if (c(e, ["toolResponse"]) !== void 0) throw new Error("toolResponse parameter is not supported in Vertex AI.");
  if (c(e, ["partMetadata"]) !== void 0) throw new Error("partMetadata parameter is not supported in Vertex AI.");
  return n;
}
function rz(e, t) {
  const n = {}, r = c(e, ["productImage"]);
  return r != null && f(n, ["image"], hs(r)), n;
}
function sz(e, t, n) {
  const r = {}, s = c(e, ["numberOfImages"]);
  t !== void 0 && s != null && f(t, ["parameters", "sampleCount"], s);
  const o = c(e, ["baseSteps"]);
  t !== void 0 && o != null && f(t, ["parameters", "baseSteps"], o);
  const i = c(e, ["outputGcsUri"]);
  t !== void 0 && i != null && f(t, ["parameters", "storageUri"], i);
  const a = c(e, ["seed"]);
  t !== void 0 && a != null && f(t, ["parameters", "seed"], a);
  const l = c(e, ["safetyFilterLevel"]);
  t !== void 0 && l != null && f(t, ["parameters", "safetySetting"], l);
  const u = c(e, ["personGeneration"]);
  t !== void 0 && u != null && f(t, ["parameters", "personGeneration"], u);
  const d = c(e, ["addWatermark"]);
  t !== void 0 && d != null && f(t, ["parameters", "addWatermark"], d);
  const h = c(e, ["outputMimeType"]);
  t !== void 0 && h != null && f(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], h);
  const p = c(e, ["outputCompressionQuality"]);
  t !== void 0 && p != null && f(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], p);
  const y = c(e, ["enhancePrompt"]);
  t !== void 0 && y != null && f(t, ["parameters", "enhancePrompt"], y);
  const w = c(e, ["labels"]);
  return t !== void 0 && w != null && f(t, ["labels"], w), r;
}
function oz(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["source"]);
  o != null && az(o, r);
  const i = c(t, ["config"]);
  return i != null && sz(i, r), r;
}
function iz(e, t) {
  const n = {}, r = c(e, ["predictions"]);
  if (r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((o) => em(o))), f(n, ["generatedImages"], s);
  }
  return n;
}
function az(e, t, n) {
  const r = {}, s = c(e, ["prompt"]);
  t !== void 0 && s != null && f(t, ["instances[0]", "prompt"], s);
  const o = c(e, ["personImage"]);
  t !== void 0 && o != null && f(t, [
    "instances[0]",
    "personImage",
    "image"
  ], hs(o));
  const i = c(e, ["productImages"]);
  if (t !== void 0 && i != null) {
    let a = i;
    Array.isArray(a) && (a = a.map((l) => rz(l))), f(t, ["instances[0]", "productImages"], a);
  }
  return r;
}
function lz(e, t) {
  const n = {}, r = c(e, ["referenceImage"]);
  r != null && f(n, ["referenceImage"], hs(r));
  const s = c(e, ["referenceId"]);
  s != null && f(n, ["referenceId"], s);
  const o = c(e, ["referenceType"]);
  o != null && f(n, ["referenceType"], o);
  const i = c(e, ["maskImageConfig"]);
  i != null && f(n, ["maskImageConfig"], ez(i));
  const a = c(e, ["controlImageConfig"]);
  a != null && f(n, ["controlImageConfig"], zK(a));
  const l = c(e, ["styleImageConfig"]);
  l != null && f(n, ["styleImageConfig"], l);
  const u = c(e, ["subjectImageConfig"]);
  return u != null && f(n, ["subjectImageConfig"], u), n;
}
function IP(e, t) {
  const n = {}, r = c(e, ["safetyAttributes", "categories"]);
  r != null && f(n, ["categories"], r);
  const s = c(e, ["safetyAttributes", "scores"]);
  s != null && f(n, ["scores"], s);
  const o = c(e, ["contentType"]);
  return o != null && f(n, ["contentType"], o), n;
}
function xP(e, t) {
  const n = {}, r = c(e, ["safetyAttributes", "categories"]);
  r != null && f(n, ["categories"], r);
  const s = c(e, ["safetyAttributes", "scores"]);
  s != null && f(n, ["scores"], s);
  const o = c(e, ["contentType"]);
  return o != null && f(n, ["contentType"], o), n;
}
function uz(e, t) {
  const n = {}, r = c(e, ["category"]);
  if (r != null && f(n, ["category"], r), c(e, ["method"]) !== void 0) throw new Error("method parameter is not supported in Gemini API.");
  const s = c(e, ["threshold"]);
  return s != null && f(n, ["threshold"], s), n;
}
function cz(e, t) {
  const n = {}, r = c(e, ["image"]);
  return r != null && f(n, ["image"], hs(r)), n;
}
function dz(e, t, n) {
  const r = {}, s = c(e, ["mode"]);
  t !== void 0 && s != null && f(t, ["parameters", "mode"], s);
  const o = c(e, ["maxPredictions"]);
  t !== void 0 && o != null && f(t, ["parameters", "maxPredictions"], o);
  const i = c(e, ["confidenceThreshold"]);
  t !== void 0 && i != null && f(t, ["parameters", "confidenceThreshold"], i);
  const a = c(e, ["maskDilation"]);
  t !== void 0 && a != null && f(t, ["parameters", "maskDilation"], a);
  const l = c(e, ["binaryColorThreshold"]);
  t !== void 0 && l != null && f(t, ["parameters", "binaryColorThreshold"], l);
  const u = c(e, ["labels"]);
  return t !== void 0 && u != null && f(t, ["labels"], u), r;
}
function fz(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["source"]);
  o != null && pz(o, r);
  const i = c(t, ["config"]);
  return i != null && dz(i, r), r;
}
function hz(e, t) {
  const n = {}, r = c(e, ["predictions"]);
  if (r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((o) => O3(o))), f(n, ["generatedMasks"], s);
  }
  return n;
}
function pz(e, t, n) {
  const r = {}, s = c(e, ["prompt"]);
  t !== void 0 && s != null && f(t, ["instances[0]", "prompt"], s);
  const o = c(e, ["image"]);
  t !== void 0 && o != null && f(t, ["instances[0]", "image"], hs(o));
  const i = c(e, ["scribbleImage"]);
  return t !== void 0 && i != null && f(t, ["instances[0]", "scribble"], cz(i)), r;
}
function mz(e, t) {
  const n = {}, r = c(e, ["retrievalConfig"]);
  r != null && f(n, ["retrievalConfig"], r);
  const s = c(e, ["functionCallingConfig"]);
  s != null && f(n, ["functionCallingConfig"], g3(s));
  const o = c(e, ["includeServerSideToolInvocations"]);
  return o != null && f(n, ["includeServerSideToolInvocations"], o), n;
}
function gz(e, t) {
  const n = {}, r = c(e, ["retrievalConfig"]);
  r != null && f(n, ["retrievalConfig"], r);
  const s = c(e, ["functionCallingConfig"]);
  if (s != null && f(n, ["functionCallingConfig"], s), c(e, ["includeServerSideToolInvocations"]) !== void 0) throw new Error("includeServerSideToolInvocations parameter is not supported in Vertex AI.");
  return n;
}
function vz(e, t) {
  const n = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const r = c(e, ["computerUse"]);
  r != null && f(n, ["computerUse"], r);
  const s = c(e, ["fileSearch"]);
  s != null && f(n, ["fileSearch"], s);
  const o = c(e, ["googleSearch"]);
  o != null && f(n, ["googleSearch"], W3(o));
  const i = c(e, ["googleMaps"]);
  i != null && f(n, ["googleMaps"], G3(i));
  const a = c(e, ["codeExecution"]);
  if (a != null && f(n, ["codeExecution"], a), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const l = c(e, ["functionDeclarations"]);
  if (l != null) {
    let p = l;
    Array.isArray(p) && (p = p.map((y) => y)), f(n, ["functionDeclarations"], p);
  }
  const u = c(e, ["googleSearchRetrieval"]);
  if (u != null && f(n, ["googleSearchRetrieval"], u), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const d = c(e, ["urlContext"]);
  d != null && f(n, ["urlContext"], d);
  const h = c(e, ["mcpServers"]);
  if (h != null) {
    let p = h;
    Array.isArray(p) && (p = p.map((y) => y)), f(n, ["mcpServers"], p);
  }
  return n;
}
function kP(e, t) {
  const n = {}, r = c(e, ["retrieval"]);
  r != null && f(n, ["retrieval"], r);
  const s = c(e, ["computerUse"]);
  if (s != null && f(n, ["computerUse"], s), c(e, ["fileSearch"]) !== void 0) throw new Error("fileSearch parameter is not supported in Vertex AI.");
  const o = c(e, ["googleSearch"]);
  o != null && f(n, ["googleSearch"], o);
  const i = c(e, ["googleMaps"]);
  i != null && f(n, ["googleMaps"], i);
  const a = c(e, ["codeExecution"]);
  a != null && f(n, ["codeExecution"], a);
  const l = c(e, ["enterpriseWebSearch"]);
  l != null && f(n, ["enterpriseWebSearch"], l);
  const u = c(e, ["functionDeclarations"]);
  if (u != null) {
    let y = u;
    Array.isArray(y) && (y = y.map((w) => v3(w))), f(n, ["functionDeclarations"], y);
  }
  const d = c(e, ["googleSearchRetrieval"]);
  d != null && f(n, ["googleSearchRetrieval"], d);
  const h = c(e, ["parallelAiSearch"]);
  h != null && f(n, ["parallelAiSearch"], h);
  const p = c(e, ["urlContext"]);
  if (p != null && f(n, ["urlContext"], p), c(e, ["mcpServers"]) !== void 0) throw new Error("mcpServers parameter is not supported in Vertex AI.");
  return n;
}
function yz(e, t) {
  const n = {}, r = c(e, ["baseModel"]);
  r != null && f(n, ["baseModel"], r);
  const s = c(e, ["createTime"]);
  s != null && f(n, ["createTime"], s);
  const o = c(e, ["updateTime"]);
  return o != null && f(n, ["updateTime"], o), n;
}
function _z(e, t) {
  const n = {}, r = c(e, ["labels", "google-vertex-llm-tuning-base-model-id"]);
  r != null && f(n, ["baseModel"], r);
  const s = c(e, ["createTime"]);
  s != null && f(n, ["createTime"], s);
  const o = c(e, ["updateTime"]);
  return o != null && f(n, ["updateTime"], o), n;
}
function bz(e, t, n) {
  const r = {}, s = c(e, ["displayName"]);
  t !== void 0 && s != null && f(t, ["displayName"], s);
  const o = c(e, ["description"]);
  t !== void 0 && o != null && f(t, ["description"], o);
  const i = c(e, ["defaultCheckpointId"]);
  return t !== void 0 && i != null && f(t, ["defaultCheckpointId"], i), r;
}
function wz(e, t, n) {
  const r = {}, s = c(e, ["displayName"]);
  t !== void 0 && s != null && f(t, ["displayName"], s);
  const o = c(e, ["description"]);
  t !== void 0 && o != null && f(t, ["description"], o);
  const i = c(e, ["defaultCheckpointId"]);
  return t !== void 0 && i != null && f(t, ["defaultCheckpointId"], i), r;
}
function Sz(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "name"], Rt(e, s));
  const o = c(t, ["config"]);
  return o != null && bz(o, r), r;
}
function Tz(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["config"]);
  return o != null && wz(o, r), r;
}
function Ez(e, t, n) {
  const r = {}, s = c(e, ["outputGcsUri"]);
  t !== void 0 && s != null && f(t, ["parameters", "storageUri"], s);
  const o = c(e, ["safetyFilterLevel"]);
  t !== void 0 && o != null && f(t, ["parameters", "safetySetting"], o);
  const i = c(e, ["personGeneration"]);
  t !== void 0 && i != null && f(t, ["parameters", "personGeneration"], i);
  const a = c(e, ["includeRaiReason"]);
  t !== void 0 && a != null && f(t, ["parameters", "includeRaiReason"], a);
  const l = c(e, ["outputMimeType"]);
  t !== void 0 && l != null && f(t, [
    "parameters",
    "outputOptions",
    "mimeType"
  ], l);
  const u = c(e, ["outputCompressionQuality"]);
  t !== void 0 && u != null && f(t, [
    "parameters",
    "outputOptions",
    "compressionQuality"
  ], u);
  const d = c(e, ["enhanceInputImage"]);
  t !== void 0 && d != null && f(t, [
    "parameters",
    "upscaleConfig",
    "enhanceInputImage"
  ], d);
  const h = c(e, ["imagePreservationFactor"]);
  t !== void 0 && h != null && f(t, [
    "parameters",
    "upscaleConfig",
    "imagePreservationFactor"
  ], h);
  const p = c(e, ["labels"]);
  t !== void 0 && p != null && f(t, ["labels"], p);
  const y = c(e, ["numberOfImages"]);
  t !== void 0 && y != null && f(t, ["parameters", "sampleCount"], y);
  const w = c(e, ["mode"]);
  return t !== void 0 && w != null && f(t, ["parameters", "mode"], w), r;
}
function Cz(e, t, n) {
  const r = {}, s = c(t, ["model"]);
  s != null && f(r, ["_url", "model"], Rt(e, s));
  const o = c(t, ["image"]);
  o != null && f(r, ["instances[0]", "image"], hs(o));
  const i = c(t, ["upscaleFactor"]);
  i != null && f(r, [
    "parameters",
    "upscaleConfig",
    "upscaleFactor"
  ], i);
  const a = c(t, ["config"]);
  return a != null && Ez(a, r), r;
}
function Az(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["predictions"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((i) => em(i))), f(n, ["generatedImages"], o);
  }
  return n;
}
function Iz(e, t) {
  const n = {}, r = c(e, ["uri"]);
  r != null && f(n, ["uri"], r);
  const s = c(e, ["encodedVideo"]);
  s != null && f(n, ["videoBytes"], ii(s));
  const o = c(e, ["encoding"]);
  return o != null && f(n, ["mimeType"], o), n;
}
function xz(e, t) {
  const n = {}, r = c(e, ["gcsUri"]);
  r != null && f(n, ["uri"], r);
  const s = c(e, ["bytesBase64Encoded"]);
  s != null && f(n, ["videoBytes"], ii(s));
  const o = c(e, ["mimeType"]);
  return o != null && f(n, ["mimeType"], o), n;
}
function kz(e, t) {
  const n = {}, r = c(e, ["image"]);
  r != null && f(n, ["_self"], hs(r));
  const s = c(e, ["maskMode"]);
  return s != null && f(n, ["maskMode"], s), n;
}
function Mz(e, t) {
  const n = {}, r = c(e, ["image"]);
  r != null && f(n, ["image"], tm(r));
  const s = c(e, ["referenceType"]);
  return s != null && f(n, ["referenceType"], s), n;
}
function Pz(e, t) {
  const n = {}, r = c(e, ["image"]);
  r != null && f(n, ["image"], hs(r));
  const s = c(e, ["referenceType"]);
  return s != null && f(n, ["referenceType"], s), n;
}
function MP(e, t) {
  const n = {}, r = c(e, ["uri"]);
  r != null && f(n, ["uri"], r);
  const s = c(e, ["videoBytes"]);
  s != null && f(n, ["encodedVideo"], ii(s));
  const o = c(e, ["mimeType"]);
  return o != null && f(n, ["encoding"], o), n;
}
function PP(e, t) {
  const n = {}, r = c(e, ["uri"]);
  r != null && f(n, ["gcsUri"], r);
  const s = c(e, ["videoBytes"]);
  s != null && f(n, ["bytesBase64Encoded"], ii(s));
  const o = c(e, ["mimeType"]);
  return o != null && f(n, ["mimeType"], o), n;
}
function Rz(e, t) {
  const n = {}, r = c(e, ["displayName"]);
  return t !== void 0 && r != null && f(t, ["displayName"], r), n;
}
function Nz(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && Rz(n, t), t;
}
function $z(e, t) {
  const n = {}, r = c(e, ["force"]);
  return t !== void 0 && r != null && f(t, ["_query", "force"], r), n;
}
function Dz(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && f(t, ["_url", "name"], n);
  const r = c(e, ["config"]);
  return r != null && $z(r, t), t;
}
function Lz(e) {
  const t = {}, n = c(e, ["name"]);
  return n != null && f(t, ["_url", "name"], n), t;
}
function Oz(e, t) {
  const n = {}, r = c(e, ["customMetadata"]);
  if (t !== void 0 && r != null) {
    let o = r;
    Array.isArray(o) && (o = o.map((i) => i)), f(t, ["customMetadata"], o);
  }
  const s = c(e, ["chunkingConfig"]);
  return t !== void 0 && s != null && f(t, ["chunkingConfig"], s), n;
}
function Fz(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && f(t, ["name"], n);
  const r = c(e, ["metadata"]);
  r != null && f(t, ["metadata"], r);
  const s = c(e, ["done"]);
  s != null && f(t, ["done"], s);
  const o = c(e, ["error"]);
  o != null && f(t, ["error"], o);
  const i = c(e, ["response"]);
  return i != null && f(t, ["response"], Uz(i)), t;
}
function Bz(e) {
  const t = {}, n = c(e, ["fileSearchStoreName"]);
  n != null && f(t, ["_url", "file_search_store_name"], n);
  const r = c(e, ["fileName"]);
  r != null && f(t, ["fileName"], r);
  const s = c(e, ["config"]);
  return s != null && Oz(s, t), t;
}
function Uz(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["parent"]);
  r != null && f(t, ["parent"], r);
  const s = c(e, ["documentName"]);
  return s != null && f(t, ["documentName"], s), t;
}
function qz(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && f(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  return t !== void 0 && s != null && f(t, ["_query", "pageToken"], s), n;
}
function Hz(e) {
  const t = {}, n = c(e, ["config"]);
  return n != null && qz(n, t), t;
}
function Gz(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && f(t, ["nextPageToken"], r);
  const s = c(e, ["fileSearchStores"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((i) => i)), f(t, ["fileSearchStores"], o);
  }
  return t;
}
function RP(e, t) {
  const n = {}, r = c(e, ["mimeType"]);
  t !== void 0 && r != null && f(t, ["mimeType"], r);
  const s = c(e, ["displayName"]);
  t !== void 0 && s != null && f(t, ["displayName"], s);
  const o = c(e, ["customMetadata"]);
  if (t !== void 0 && o != null) {
    let a = o;
    Array.isArray(a) && (a = a.map((l) => l)), f(t, ["customMetadata"], a);
  }
  const i = c(e, ["chunkingConfig"]);
  return t !== void 0 && i != null && f(t, ["chunkingConfig"], i), n;
}
function Wz(e) {
  const t = {}, n = c(e, ["fileSearchStoreName"]);
  n != null && f(t, ["_url", "file_search_store_name"], n);
  const r = c(e, ["config"]);
  return r != null && RP(r, t), t;
}
function Vz(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  return n != null && f(t, ["sdkHttpResponse"], n), t;
}
var Kz = "Content-Type", zz = "X-Server-Timeout", Jz = "User-Agent", Zy = "x-goog-api-client", Yz = "google-genai-sdk/1.50.1", Xz = "v1beta1", Qz = "v1beta", Zz = /* @__PURE__ */ new Set(["us", "eu"]), jz = 5, eJ = [
  408,
  429,
  500,
  502,
  503,
  504
], tJ = class {
  constructor(e) {
    var t, n, r;
    this.clientOptions = Object.assign({}, e), this.customBaseUrl = (t = e.httpOptions) === null || t === void 0 ? void 0 : t.baseUrl, this.clientOptions.vertexai && (this.clientOptions.project && this.clientOptions.location ? this.clientOptions.apiKey = void 0 : this.clientOptions.apiKey && (this.clientOptions.project = void 0, this.clientOptions.location = void 0));
    const s = {};
    if (this.clientOptions.vertexai) {
      if (!this.clientOptions.location && !this.clientOptions.apiKey && !this.customBaseUrl && (this.clientOptions.location = "global"), !(this.clientOptions.project && this.clientOptions.location || this.clientOptions.apiKey) && !this.customBaseUrl) throw new Error("Authentication is not set up. Please provide either a project and location, or an API key, or a custom base URL.");
      const o = e.project && e.location || !!e.apiKey;
      this.customBaseUrl && !o ? (s.baseUrl = this.customBaseUrl, this.clientOptions.project = void 0, this.clientOptions.location = void 0) : this.clientOptions.apiKey || this.clientOptions.location === "global" ? s.baseUrl = "https://aiplatform.googleapis.com/" : this.clientOptions.project && this.clientOptions.location && Zz.has(this.clientOptions.location) ? s.baseUrl = `https://aiplatform.${this.clientOptions.location}.rep.googleapis.com/` : this.clientOptions.project && this.clientOptions.location && (s.baseUrl = `https://${this.clientOptions.location}-aiplatform.googleapis.com/`), s.apiVersion = (n = this.clientOptions.apiVersion) !== null && n !== void 0 ? n : Xz;
    } else
      this.clientOptions.apiKey || console.warn("API key should be set when using the Gemini API."), s.apiVersion = (r = this.clientOptions.apiVersion) !== null && r !== void 0 ? r : Qz, s.baseUrl = "https://generativelanguage.googleapis.com/";
    s.headers = this.getDefaultHeaders(), this.clientOptions.httpOptions = s, e.httpOptions && (this.clientOptions.httpOptions = this.patchHttpOptions(s, e.httpOptions));
  }
  isVertexAI() {
    var e;
    return (e = this.clientOptions.vertexai) !== null && e !== void 0 ? e : !1;
  }
  getProject() {
    return this.clientOptions.project;
  }
  getLocation() {
    return this.clientOptions.location;
  }
  getCustomBaseUrl() {
    return this.customBaseUrl;
  }
  async getAuthHeaders() {
    const e = new Headers();
    return await this.clientOptions.auth.addAuthHeaders(e), e;
  }
  getApiVersion() {
    if (this.clientOptions.httpOptions && this.clientOptions.httpOptions.apiVersion !== void 0) return this.clientOptions.httpOptions.apiVersion;
    throw new Error("API version is not set.");
  }
  getBaseUrl() {
    if (this.clientOptions.httpOptions && this.clientOptions.httpOptions.baseUrl !== void 0) return this.clientOptions.httpOptions.baseUrl;
    throw new Error("Base URL is not set.");
  }
  getRequestUrl() {
    return this.getRequestUrlInternal(this.clientOptions.httpOptions);
  }
  getHeaders() {
    if (this.clientOptions.httpOptions && this.clientOptions.httpOptions.headers !== void 0) return this.clientOptions.httpOptions.headers;
    throw new Error("Headers are not set.");
  }
  getRequestUrlInternal(e) {
    if (!e || e.baseUrl === void 0 || e.apiVersion === void 0) throw new Error("HTTP options are not correctly set.");
    const t = [e.baseUrl.endsWith("/") ? e.baseUrl.slice(0, -1) : e.baseUrl];
    return e.apiVersion && e.apiVersion !== "" && t.push(e.apiVersion), t.join("/");
  }
  getBaseResourcePath() {
    return `projects/${this.clientOptions.project}/locations/${this.clientOptions.location}`;
  }
  getApiKey() {
    return this.clientOptions.apiKey;
  }
  getWebsocketBaseUrl() {
    const e = this.getBaseUrl(), t = new URL(e);
    return t.protocol = t.protocol == "http:" ? "ws" : "wss", t.toString();
  }
  setBaseUrl(e) {
    if (this.clientOptions.httpOptions) this.clientOptions.httpOptions.baseUrl = e;
    else throw new Error("HTTP options are not correctly set.");
  }
  constructUrl(e, t, n) {
    const r = [this.getRequestUrlInternal(t)];
    return n && r.push(this.getBaseResourcePath()), e !== "" && r.push(e), new URL(`${r.join("/")}`);
  }
  shouldPrependVertexProjectPath(e, t) {
    return !(t.baseUrl && t.baseUrlResourceScope === Ky.COLLECTION || this.clientOptions.apiKey || !this.clientOptions.vertexai || e.path.startsWith("projects/") || e.httpMethod === "GET" && e.path.startsWith("publishers/google/models"));
  }
  async request(e) {
    let t = this.clientOptions.httpOptions;
    e.httpOptions && (t = this.patchHttpOptions(this.clientOptions.httpOptions, e.httpOptions));
    const n = this.shouldPrependVertexProjectPath(e, t), r = this.constructUrl(e.path, t, n);
    if (e.queryParams) for (const [o, i] of Object.entries(e.queryParams)) r.searchParams.append(o, String(i));
    let s = {};
    if (e.httpMethod === "GET") {
      if (e.body && e.body !== "{}") throw new Error("Request body should be empty for GET request, but got non empty request body");
    } else s.body = e.body;
    return s = await this.includeExtraHttpOptionsToRequestInit(s, t, r.toString(), e.abortSignal), this.unaryApiCall(r, s, e.httpMethod);
  }
  patchHttpOptions(e, t) {
    const n = JSON.parse(JSON.stringify(e));
    for (const [r, s] of Object.entries(t)) typeof s == "object" ? n[r] = Object.assign(Object.assign({}, n[r]), s) : s !== void 0 && (n[r] = s);
    return n;
  }
  async requestStream(e) {
    let t = this.clientOptions.httpOptions;
    e.httpOptions && (t = this.patchHttpOptions(this.clientOptions.httpOptions, e.httpOptions));
    const n = this.shouldPrependVertexProjectPath(e, t), r = this.constructUrl(e.path, t, n);
    (!r.searchParams.has("alt") || r.searchParams.get("alt") !== "sse") && r.searchParams.set("alt", "sse");
    let s = {};
    return s.body = e.body, s = await this.includeExtraHttpOptionsToRequestInit(s, t, r.toString(), e.abortSignal), this.streamApiCall(r, s, e.httpMethod);
  }
  async includeExtraHttpOptionsToRequestInit(e, t, n, r) {
    if (t && t.timeout || r) {
      const s = new AbortController(), o = s.signal;
      if (t.timeout && t?.timeout > 0) {
        const i = setTimeout(() => s.abort(), t.timeout);
        i && typeof i.unref == "function" && i.unref();
      }
      r && r.addEventListener("abort", () => {
        s.abort();
      }), e.signal = o;
    }
    return t && t.extraBody !== null && nJ(e, t.extraBody), e.headers = await this.getHeadersInternal(t, n), e;
  }
  async unaryApiCall(e, t, n) {
    return this.apiCall(e.toString(), Object.assign(Object.assign({}, t), { method: n })).then(async (r) => (await IC(r), new zy(r))).catch((r) => {
      throw r instanceof Error ? r : new Error(JSON.stringify(r));
    });
  }
  async streamApiCall(e, t, n) {
    return this.apiCall(e.toString(), Object.assign(Object.assign({}, t), { method: n })).then(async (r) => (await IC(r), this.processStreamResponse(r))).catch((r) => {
      throw r instanceof Error ? r : new Error(JSON.stringify(r));
    });
  }
  processStreamResponse(e) {
    return ls(this, arguments, function* () {
      var n;
      const r = (n = e?.body) === null || n === void 0 ? void 0 : n.getReader(), s = new TextDecoder("utf-8");
      if (!r) throw new Error("Response body is empty");
      try {
        let o = "";
        const i = "data:", a = [
          `

`,
          "\r\r",
          `\r
\r
`
        ];
        for (; ; ) {
          const { done: l, value: u } = yield bt(r.read());
          if (l) {
            if (o.trim().length > 0) throw new Error("Incomplete JSON segment at the end");
            break;
          }
          const d = s.decode(u, { stream: !0 });
          try {
            const y = JSON.parse(d);
            if ("error" in y) {
              const w = JSON.parse(JSON.stringify(y.error)), E = w.status, C = w.code, M = `got status: ${E}. ${JSON.stringify(y)}`;
              if (C >= 400 && C < 600) throw new EP({
                message: M,
                status: C
              });
            }
          } catch (y) {
            if (y.name === "ApiError") throw y;
          }
          o += d;
          let h = -1, p = 0;
          for (; ; ) {
            h = -1, p = 0;
            for (const E of a) {
              const C = o.indexOf(E);
              C !== -1 && (h === -1 || C < h) && (h = C, p = E.length);
            }
            if (h === -1) break;
            const y = o.substring(0, h);
            o = o.substring(h + p);
            const w = y.trim();
            if (w.startsWith(i)) {
              const E = w.substring(5).trim();
              try {
                yield yield bt(new zy(new Response(E, {
                  headers: e?.headers,
                  status: e?.status,
                  statusText: e?.statusText
                })));
              } catch (C) {
                throw new Error(`exception parsing stream chunk ${E}. ${C}`);
              }
            }
          }
        }
      } finally {
        r.releaseLock();
      }
    });
  }
  async apiCall(e, t) {
    var n;
    if (!this.clientOptions.httpOptions || !this.clientOptions.httpOptions.retryOptions) return fetch(e, t);
    const r = this.clientOptions.httpOptions.retryOptions, s = async () => {
      const o = await fetch(e, t);
      if (o.ok) return o;
      throw eJ.includes(o.status) ? new Error(`Retryable HTTP Error: ${o.statusText}`) : new jT.AbortError(`Non-retryable exception ${o.statusText} sending request`);
    };
    return (0, jT.default)(s, { retries: ((n = r.attempts) !== null && n !== void 0 ? n : jz) - 1 });
  }
  getDefaultHeaders() {
    const e = {}, t = Yz + " " + this.clientOptions.userAgentExtra;
    return e[Jz] = t, e[Zy] = t, e[Kz] = "application/json", e;
  }
  async getHeadersInternal(e, t) {
    const n = new Headers();
    if (e && e.headers) {
      for (const [r, s] of Object.entries(e.headers)) n.append(r, s);
      e.timeout && e.timeout > 0 && n.append(zz, String(Math.ceil(e.timeout / 1e3)));
    }
    return await this.clientOptions.auth.addAuthHeaders(n, t), n;
  }
  getFileName(e) {
    var t;
    let n = "";
    return typeof e == "string" && (n = e.replace(/[/\\]+$/, ""), n = (t = n.split(/[/\\]/).pop()) !== null && t !== void 0 ? t : ""), n;
  }
  async uploadFile(e, t) {
    var n;
    const r = {};
    t != null && (r.mimeType = t.mimeType, r.name = t.name, r.displayName = t.displayName), r.name && !r.name.startsWith("files/") && (r.name = `files/${r.name}`);
    const s = this.clientOptions.uploader, o = await s.stat(e);
    r.sizeBytes = String(o.size);
    const i = (n = t?.mimeType) !== null && n !== void 0 ? n : o.type;
    if (i === void 0 || i === "") throw new Error("Can not determine mimeType. Please provide mimeType in the config.");
    r.mimeType = i;
    const a = { file: r }, l = this.getFileName(e), u = Ue("upload/v1beta/files", a._url), d = await this.fetchUploadUrl(u, r.sizeBytes, r.mimeType, l, a, t?.httpOptions);
    return s.upload(e, d, this);
  }
  async uploadFileToFileSearchStore(e, t, n) {
    var r;
    const s = this.clientOptions.uploader, o = await s.stat(t), i = String(o.size), a = (r = n?.mimeType) !== null && r !== void 0 ? r : o.type;
    if (a === void 0 || a === "") throw new Error("Can not determine mimeType. Please provide mimeType in the config.");
    const l = `upload/v1beta/${e}:uploadToFileSearchStore`, u = this.getFileName(t), d = {};
    n != null && RP(n, d);
    const h = await this.fetchUploadUrl(l, i, a, u, d, n?.httpOptions);
    return s.uploadToFileSearchStore(t, h, this);
  }
  async downloadFile(e) {
    await this.clientOptions.downloader.download(e, this);
  }
  async fetchUploadUrl(e, t, n, r, s, o) {
    var i;
    let a = {};
    o ? a = o : a = {
      apiVersion: "",
      headers: Object.assign({
        "Content-Type": "application/json",
        "X-Goog-Upload-Protocol": "resumable",
        "X-Goog-Upload-Command": "start",
        "X-Goog-Upload-Header-Content-Length": `${t}`,
        "X-Goog-Upload-Header-Content-Type": `${n}`
      }, r ? { "X-Goog-Upload-File-Name": r } : {})
    };
    const l = await this.request({
      path: e,
      body: JSON.stringify(s),
      httpMethod: "POST",
      httpOptions: a
    });
    if (!l || !l?.headers) throw new Error("Server did not return an HttpResponse or the returned HttpResponse did not have headers.");
    const u = (i = l?.headers) === null || i === void 0 ? void 0 : i["x-goog-upload-url"];
    if (u === void 0) throw new Error("Failed to get upload url. Server did not return the x-google-upload-url in the headers");
    return u;
  }
};
async function IC(e) {
  var t;
  if (e === void 0) throw new Error("response is undefined");
  if (!e.ok) {
    const n = e.status;
    let r;
    !((t = e.headers.get("content-type")) === null || t === void 0) && t.includes("application/json") ? r = await e.json() : r = { error: {
      message: await e.text(),
      code: e.status,
      status: e.statusText
    } };
    const s = JSON.stringify(r);
    throw n >= 400 && n < 600 ? new EP({
      message: s,
      status: n
    }) : new Error(s);
  }
}
function nJ(e, t) {
  if (!t || Object.keys(t).length === 0) return;
  if (e.body instanceof Blob) {
    console.warn("includeExtraBodyToRequestInit: extraBody provided but current request body is a Blob. extraBody will be ignored as merging is not supported for Blob bodies.");
    return;
  }
  let n = {};
  if (typeof e.body == "string" && e.body.length > 0) try {
    const o = JSON.parse(e.body);
    if (typeof o == "object" && o !== null && !Array.isArray(o)) n = o;
    else {
      console.warn("includeExtraBodyToRequestInit: Original request body is valid JSON but not a non-array object. Skip applying extraBody to the request body.");
      return;
    }
  } catch {
    console.warn("includeExtraBodyToRequestInit: Original request body is not valid JSON. Skip applying extraBody to the request body.");
    return;
  }
  function r(o, i) {
    const a = Object.assign({}, o);
    for (const l in i) if (Object.prototype.hasOwnProperty.call(i, l)) {
      const u = i[l], d = a[l];
      u && typeof u == "object" && !Array.isArray(u) && d && typeof d == "object" && !Array.isArray(d) ? a[l] = r(d, u) : (d && u && typeof d != typeof u && console.warn(`includeExtraBodyToRequestInit:deepMerge: Type mismatch for key "${l}". Original type: ${typeof d}, New type: ${typeof u}. Overwriting.`), a[l] = u);
    }
    return a;
  }
  const s = r(n, t);
  e.body = JSON.stringify(s);
}
var rJ = "mcp_used/unknown", sJ = !1;
function NP(e) {
  for (const t of e)
    if (oJ(t) || typeof t == "object" && "inputSchema" in t) return !0;
  return sJ;
}
function $P(e) {
  var t;
  e[Zy] = (((t = e[Zy]) !== null && t !== void 0 ? t : "") + ` ${rJ}`).trimStart();
}
function oJ(e) {
  return e !== null && typeof e == "object" && e instanceof aJ;
}
function iJ(e) {
  return ls(this, arguments, function* (n, r = 100) {
    let s, o = 0;
    for (; o < r; ) {
      const i = yield bt(n.listTools({ cursor: s }));
      for (const a of i.tools)
        yield yield bt(a), o++;
      if (!i.nextCursor) break;
      s = i.nextCursor;
    }
  });
}
var aJ = class DP {
  constructor(t = [], n) {
    this.mcpTools = [], this.functionNameToMcpClient = {}, this.mcpClients = t, this.config = n;
  }
  static create(t, n) {
    return new DP(t, n);
  }
  async initialize() {
    var t, n, r, s;
    if (this.mcpTools.length > 0) return;
    const o = {}, i = [];
    for (const d of this.mcpClients) try {
      for (var a = !0, l = (n = void 0, us(iJ(d))), u; u = await l.next(), t = u.done, !t; a = !0) {
        s = u.value, a = !1;
        const h = s;
        i.push(h);
        const p = h.name;
        if (o[p]) throw new Error(`Duplicate function name ${p} found in MCP tools. Please ensure function names are unique.`);
        o[p] = d;
      }
    } catch (h) {
      n = { error: h };
    } finally {
      try {
        !a && !t && (r = l.return) && await r.call(l);
      } finally {
        if (n) throw n.error;
      }
    }
    this.mcpTools = i, this.functionNameToMcpClient = o;
  }
  async tool() {
    return await this.initialize(), w4(this.mcpTools, this.config);
  }
  async callTool(t) {
    await this.initialize();
    const n = [];
    for (const r of t) if (r.name in this.functionNameToMcpClient) {
      const s = this.functionNameToMcpClient[r.name];
      let o;
      this.config.timeout && (o = { timeout: this.config.timeout });
      const i = await s.callTool({
        name: r.name,
        arguments: r.args
      }, void 0, o);
      n.push({ functionResponse: {
        name: r.name,
        response: i.isError ? { error: i } : i
      } });
    }
    return n;
  }
};
async function lJ(e, t, n) {
  const r = new f4();
  let s;
  n.data instanceof Blob ? s = JSON.parse(await n.data.text()) : s = JSON.parse(n.data), Object.assign(r, s), t(r);
}
var uJ = class {
  constructor(e, t, n) {
    this.apiClient = e, this.auth = t, this.webSocketFactory = n;
  }
  async connect(e) {
    var t, n;
    if (this.apiClient.isVertexAI()) throw new Error("Live music is not supported for Vertex AI.");
    console.warn("Live music generation is experimental and may change in future versions.");
    const r = this.apiClient.getWebsocketBaseUrl(), s = this.apiClient.getApiVersion(), o = fJ(this.apiClient.getDefaultHeaders()), i = `${r}/ws/google.ai.generativelanguage.${s}.GenerativeService.BidiGenerateMusic?key=${this.apiClient.getApiKey()}`;
    let a = () => {
    };
    const l = new Promise((E) => {
      a = E;
    }), u = e.callbacks, d = function() {
      a({});
    }, h = this.apiClient, p = {
      onopen: d,
      onmessage: (E) => {
        lJ(h, u.onmessage, E);
      },
      onerror: (t = u?.onerror) !== null && t !== void 0 ? t : function(E) {
      },
      onclose: (n = u?.onclose) !== null && n !== void 0 ? n : function(E) {
      }
    }, y = this.webSocketFactory.create(i, dJ(o), p);
    y.connect(), await l;
    const w = { setup: { model: Rt(this.apiClient, e.model) } };
    return y.send(JSON.stringify(w)), new cJ(y, this.apiClient);
  }
}, cJ = class {
  constructor(e, t) {
    this.conn = e, this.apiClient = t;
  }
  async setWeightedPrompts(e) {
    if (!e.weightedPrompts || Object.keys(e.weightedPrompts).length === 0) throw new Error("Weighted prompts must be set and contain at least one entry.");
    const t = IK(e);
    this.conn.send(JSON.stringify({ clientContent: t }));
  }
  async setMusicGenerationConfig(e) {
    e.musicGenerationConfig || (e.musicGenerationConfig = {});
    const t = AK(e);
    this.conn.send(JSON.stringify(t));
  }
  sendPlaybackControl(e) {
    const t = { playbackControl: e };
    this.conn.send(JSON.stringify(t));
  }
  play() {
    this.sendPlaybackControl(Za.PLAY);
  }
  pause() {
    this.sendPlaybackControl(Za.PAUSE);
  }
  stop() {
    this.sendPlaybackControl(Za.STOP);
  }
  resetContext() {
    this.sendPlaybackControl(Za.RESET_CONTEXT);
  }
  close() {
    this.conn.close();
  }
};
function dJ(e) {
  const t = {};
  return e.forEach((n, r) => {
    t[r] = n;
  }), t;
}
function fJ(e) {
  const t = new Headers();
  for (const [n, r] of Object.entries(e)) t.append(n, r);
  return t;
}
var hJ = "FunctionResponse request must have an `id` field from the response of a ToolCall.FunctionalCalls in Google AI.";
async function pJ(e, t, n) {
  const r = new d4();
  let s;
  n.data instanceof Blob ? s = await n.data.text() : n.data instanceof ArrayBuffer ? s = new TextDecoder().decode(n.data) : s = n.data;
  const o = JSON.parse(s);
  if (e.isVertexAI()) {
    const i = MK(o);
    Object.assign(r, i);
  } else Object.assign(r, o);
  t(r);
}
var mJ = class {
  constructor(e, t, n) {
    this.apiClient = e, this.auth = t, this.webSocketFactory = n, this.music = new uJ(this.apiClient, this.auth, this.webSocketFactory);
  }
  async connect(e) {
    var t, n, r, s, o, i;
    if (e.config && e.config.httpOptions) throw new Error("The Live module does not support httpOptions at request-level in LiveConnectConfig yet. Please use the client-level httpOptions configuration instead.");
    const a = this.apiClient.getWebsocketBaseUrl(), l = this.apiClient.getApiVersion();
    let u;
    const d = this.apiClient.getHeaders();
    e.config && e.config.tools && NP(e.config.tools) && $P(d);
    const h = _J(d);
    if (this.apiClient.isVertexAI()) {
      const T = this.apiClient.getProject(), I = this.apiClient.getLocation(), x = this.apiClient.getApiKey(), P = !!T && !!I || !!x;
      this.apiClient.getCustomBaseUrl() && !P ? u = a : (u = `${a}/ws/google.cloud.aiplatform.${l}.LlmBidiService/BidiGenerateContent`, await this.auth.addAuthHeaders(h, u));
    } else {
      const T = this.apiClient.getApiKey();
      let I = "BidiGenerateContent", x = "key";
      T?.startsWith("auth_tokens/") && (console.warn("Warning: Ephemeral token support is experimental and may change in future versions."), l !== "v1alpha" && console.warn("Warning: The SDK's ephemeral token support is in v1alpha only. Please use const ai = new GoogleGenAI({apiKey: token.name, httpOptions: { apiVersion: 'v1alpha' }}); before session connection."), I = "BidiGenerateContentConstrained", x = "access_token"), u = `${a}/ws/google.ai.generativelanguage.${l}.GenerativeService.${I}?${x}=${T}`;
    }
    let p = () => {
    };
    const y = new Promise((T) => {
      p = T;
    }), w = e.callbacks, E = function() {
      var T;
      (T = w?.onopen) === null || T === void 0 || T.call(w), p({});
    }, C = this.apiClient, M = {
      onopen: E,
      onmessage: (T) => {
        pJ(C, w.onmessage, T);
      },
      onerror: (t = w?.onerror) !== null && t !== void 0 ? t : function(T) {
      },
      onclose: (n = w?.onclose) !== null && n !== void 0 ? n : function(T) {
      }
    }, k = this.webSocketFactory.create(u, yJ(h), M);
    k.connect(), await y;
    let R = Rt(this.apiClient, e.model);
    if (this.apiClient.isVertexAI() && R.startsWith("publishers/")) {
      const T = this.apiClient.getProject(), I = this.apiClient.getLocation();
      T && I && (R = `projects/${T}/locations/${I}/` + R);
    }
    let D = {};
    this.apiClient.isVertexAI() && ((r = e.config) === null || r === void 0 ? void 0 : r.responseModalities) === void 0 && (e.config === void 0 ? e.config = { responseModalities: [ip.AUDIO] } : e.config.responseModalities = [ip.AUDIO]), !((s = e.config) === null || s === void 0) && s.generationConfig && console.warn("Setting `LiveConnectConfig.generation_config` is deprecated, please set the fields on `LiveConnectConfig` directly. This will become an error in a future version (not before Q3 2025).");
    const m = (i = (o = e.config) === null || o === void 0 ? void 0 : o.tools) !== null && i !== void 0 ? i : [], b = [];
    for (const T of m) if (this.isCallableTool(T)) {
      const I = T;
      b.push(await I.tool());
    } else b.push(T);
    b.length > 0 && (e.config.tools = b);
    const v = {
      model: R,
      config: e.config,
      callbacks: e.callbacks
    };
    return this.apiClient.isVertexAI() ? D = CK(this.apiClient, v) : D = EK(this.apiClient, v), delete D.config, k.send(JSON.stringify(D)), new vJ(k, this.apiClient);
  }
  isCallableTool(e) {
    return "callTool" in e && typeof e.callTool == "function";
  }
}, gJ = { turnComplete: !0 }, vJ = class {
  constructor(e, t) {
    this.conn = e, this.apiClient = t;
  }
  tLiveClientContent(e, t) {
    if (t.turns !== null && t.turns !== void 0) {
      let n = [];
      try {
        n = rr(t.turns), e.isVertexAI() || (n = n.map((r) => dd(r)));
      } catch {
        throw new Error(`Failed to parse client content "turns", type: '${typeof t.turns}'`);
      }
      return { clientContent: {
        turns: n,
        turnComplete: t.turnComplete
      } };
    }
    return { clientContent: { turnComplete: t.turnComplete } };
  }
  tLiveClienttToolResponse(e, t) {
    let n = [];
    if (t.functionResponses == null) throw new Error("functionResponses is required.");
    if (Array.isArray(t.functionResponses) ? n = t.functionResponses : n = [t.functionResponses], n.length === 0) throw new Error("functionResponses is required.");
    for (const r of n) {
      if (typeof r != "object" || r === null || !("name" in r) || !("response" in r)) throw new Error(`Could not parse function response, type '${typeof r}'.`);
      if (!e.isVertexAI() && !("id" in r)) throw new Error(hJ);
    }
    return { toolResponse: { functionResponses: n } };
  }
  sendClientContent(e) {
    e = Object.assign(Object.assign({}, gJ), e);
    const t = this.tLiveClientContent(this.apiClient, e);
    this.conn.send(JSON.stringify(t));
  }
  sendRealtimeInput(e) {
    let t = {};
    this.apiClient.isVertexAI() ? t = { realtimeInput: kK(e) } : t = { realtimeInput: xK(e) }, this.conn.send(JSON.stringify(t));
  }
  sendToolResponse(e) {
    if (e.functionResponses == null) throw new Error("Tool response parameters are required.");
    const t = this.tLiveClienttToolResponse(this.apiClient, e);
    this.conn.send(JSON.stringify(t));
  }
  close() {
    this.conn.close();
  }
};
function yJ(e) {
  const t = {};
  return e.forEach((n, r) => {
    t[r] = n;
  }), t;
}
function _J(e) {
  const t = new Headers();
  for (const [n, r] of Object.entries(e)) t.append(n, r);
  return t;
}
var xC = 10;
function kC(e) {
  var t, n, r;
  if (!((t = e?.automaticFunctionCalling) === null || t === void 0) && t.disable) return !0;
  let s = !1;
  for (const i of (n = e?.tools) !== null && n !== void 0 ? n : []) if (pl(i)) {
    s = !0;
    break;
  }
  if (!s) return !0;
  const o = (r = e?.automaticFunctionCalling) === null || r === void 0 ? void 0 : r.maximumRemoteCalls;
  return o && (o < 0 || !Number.isInteger(o)) || o == 0 ? (console.warn("Invalid maximumRemoteCalls value provided for automatic function calling. Disabled automatic function calling. Please provide a valid integer value greater than 0. maximumRemoteCalls provided:", o), !0) : !1;
}
function pl(e) {
  return "callTool" in e && typeof e.callTool == "function";
}
function bJ(e) {
  var t, n, r;
  return (r = (n = (t = e.config) === null || t === void 0 ? void 0 : t.tools) === null || n === void 0 ? void 0 : n.some((s) => pl(s))) !== null && r !== void 0 ? r : !1;
}
function MC(e) {
  var t;
  const n = [];
  return !((t = e?.config) === null || t === void 0) && t.tools && e.config.tools.forEach((r, s) => {
    if (pl(r)) return;
    const o = r;
    o.functionDeclarations && o.functionDeclarations.length > 0 && n.push(s);
  }), n;
}
function PC(e) {
  var t;
  return !(!((t = e?.automaticFunctionCalling) === null || t === void 0) && t.ignoreCallHistory);
}
var wJ = class extends yo {
  constructor(e) {
    super(), this.apiClient = e, this.embedContent = async (t) => {
      if (!this.apiClient.isVertexAI())
        return t.model.includes("gemini-embedding-2") && (t.contents = rr(t.contents)), await this.embedContentInternal(t);
      if (t.model.includes("gemini") && t.model !== "gemini-embedding-001" || t.model.includes("maas")) {
        const n = rr(t.contents);
        if (n.length > 1) throw new Error("The embedContent API for this model only supports one content at a time.");
        const r = Object.assign(Object.assign({}, t), {
          content: n[0],
          embeddingApiType: ap.EMBED_CONTENT
        });
        return await this.embedContentInternal(r);
      } else {
        const n = Object.assign(Object.assign({}, t), { embeddingApiType: ap.PREDICT });
        return await this.embedContentInternal(n);
      }
    }, this.generateContent = async (t) => {
      var n, r, s, o, i;
      const a = await this.processParamsMaybeAddMcpUsage(t);
      if (this.maybeMoveToResponseJsonSchem(t), !bJ(t) || kC(t.config)) return await this.generateContentInternal(a);
      const l = MC(t);
      if (l.length > 0) {
        const w = l.map((E) => `tools[${E}]`).join(", ");
        throw new Error(`Automatic function calling with CallableTools (or MCP objects) and basic FunctionDeclarations is not yet supported. Incompatible tools found at ${w}.`);
      }
      let u, d;
      const h = rr(a.contents), p = (s = (r = (n = a.config) === null || n === void 0 ? void 0 : n.automaticFunctionCalling) === null || r === void 0 ? void 0 : r.maximumRemoteCalls) !== null && s !== void 0 ? s : xC;
      let y = 0;
      for (; y < p && (u = await this.generateContentInternal(a), !(!u.functionCalls || u.functionCalls.length === 0)); ) {
        const w = u.candidates[0].content, E = [];
        for (const C of (i = (o = t.config) === null || o === void 0 ? void 0 : o.tools) !== null && i !== void 0 ? i : []) if (pl(C)) {
          const M = await C.callTool(u.functionCalls);
          E.push(...M);
        }
        y++, d = {
          role: "user",
          parts: E
        }, a.contents = rr(a.contents), a.contents.push(w), a.contents.push(d), PC(a.config) && (h.push(w), h.push(d));
      }
      return PC(a.config) && (u.automaticFunctionCallingHistory = h), u;
    }, this.generateContentStream = async (t) => {
      var n, r, s, o, i;
      if (this.maybeMoveToResponseJsonSchem(t), kC(t.config)) {
        const d = await this.processParamsMaybeAddMcpUsage(t);
        return await this.generateContentStreamInternal(d);
      }
      const a = MC(t);
      if (a.length > 0) {
        const d = a.map((h) => `tools[${h}]`).join(", ");
        throw new Error(`Incompatible tools found at ${d}. Automatic function calling with CallableTools (or MCP objects) and basic FunctionDeclarations" is not yet supported.`);
      }
      const l = (s = (r = (n = t?.config) === null || n === void 0 ? void 0 : n.toolConfig) === null || r === void 0 ? void 0 : r.functionCallingConfig) === null || s === void 0 ? void 0 : s.streamFunctionCallArguments, u = (i = (o = t?.config) === null || o === void 0 ? void 0 : o.automaticFunctionCalling) === null || i === void 0 ? void 0 : i.disable;
      if (l && !u) throw new Error("Running in streaming mode with 'streamFunctionCallArguments' enabled, this feature is not compatible with automatic function calling (AFC). Please set 'config.automaticFunctionCalling.disable' to true to disable AFC or leave 'config.toolConfig.functionCallingConfig.streamFunctionCallArguments' to be undefined or set to false to disable streaming function call arguments feature.");
      return await this.processAfcStream(t);
    }, this.generateImages = async (t) => await this.generateImagesInternal(t).then((n) => {
      var r;
      let s;
      const o = [];
      if (n?.generatedImages) for (const a of n.generatedImages) a && a?.safetyAttributes && ((r = a?.safetyAttributes) === null || r === void 0 ? void 0 : r.contentType) === "Positive Prompt" ? s = a?.safetyAttributes : o.push(a);
      let i;
      return s ? i = {
        generatedImages: o,
        positivePromptSafetyAttributes: s,
        sdkHttpResponse: n.sdkHttpResponse
      } : i = {
        generatedImages: o,
        sdkHttpResponse: n.sdkHttpResponse
      }, i;
    }), this.list = async (t) => {
      var n;
      const r = { config: Object.assign(Object.assign({}, { queryBase: !0 }), t?.config) };
      if (this.apiClient.isVertexAI() && !r.config.queryBase) {
        if (!((n = r.config) === null || n === void 0) && n.filter) throw new Error("Filtering tuned models list for Vertex AI is not currently supported");
        r.config.filter = "labels.tune-type:*";
      }
      return new ua(go.PAGED_ITEM_MODELS, (s) => this.listInternal(s), await this.listInternal(r), r);
    }, this.editImage = async (t) => {
      const n = {
        model: t.model,
        prompt: t.prompt,
        referenceImages: [],
        config: t.config
      };
      return t.referenceImages && t.referenceImages && (n.referenceImages = t.referenceImages.map((r) => r.toReferenceImageAPI())), await this.editImageInternal(n);
    }, this.upscaleImage = async (t) => {
      let n = {
        numberOfImages: 1,
        mode: "upscale"
      };
      t.config && (n = Object.assign(Object.assign({}, n), t.config));
      const r = {
        model: t.model,
        image: t.image,
        upscaleFactor: t.upscaleFactor,
        config: n
      };
      return await this.upscaleImageInternal(r);
    }, this.generateVideos = async (t) => {
      var n, r, s, o, i, a;
      if ((t.prompt || t.image || t.video) && t.source) throw new Error("Source and prompt/image/video are mutually exclusive. Please only use source.");
      return this.apiClient.isVertexAI() || (!((n = t.video) === null || n === void 0) && n.uri && (!((r = t.video) === null || r === void 0) && r.videoBytes) ? t.video = {
        uri: t.video.uri,
        mimeType: t.video.mimeType
      } : !((o = (s = t.source) === null || s === void 0 ? void 0 : s.video) === null || o === void 0) && o.uri && (!((a = (i = t.source) === null || i === void 0 ? void 0 : i.video) === null || a === void 0) && a.videoBytes) && (t.source.video = {
        uri: t.source.video.uri,
        mimeType: t.source.video.mimeType
      })), await this.generateVideosInternal(t);
    };
  }
  maybeMoveToResponseJsonSchem(e) {
    e.config && e.config.responseSchema && (e.config.responseJsonSchema || Object.keys(e.config.responseSchema).includes("$schema") && (e.config.responseJsonSchema = e.config.responseSchema, delete e.config.responseSchema));
  }
  async processParamsMaybeAddMcpUsage(e) {
    var t, n, r;
    const s = (t = e.config) === null || t === void 0 ? void 0 : t.tools;
    if (!s) return e;
    const o = await Promise.all(s.map(async (a) => pl(a) ? await a.tool() : a)), i = {
      model: e.model,
      contents: e.contents,
      config: Object.assign(Object.assign({}, e.config), { tools: o })
    };
    if (i.config.tools = o, e.config && e.config.tools && NP(e.config.tools)) {
      const a = (r = (n = e.config.httpOptions) === null || n === void 0 ? void 0 : n.headers) !== null && r !== void 0 ? r : {};
      let l = Object.assign({}, a);
      Object.keys(l).length === 0 && (l = this.apiClient.getDefaultHeaders()), $P(l), i.config.httpOptions = Object.assign(Object.assign({}, e.config.httpOptions), { headers: l });
    }
    return i;
  }
  async initAfcToolsMap(e) {
    var t, n, r;
    const s = /* @__PURE__ */ new Map();
    for (const o of (n = (t = e.config) === null || t === void 0 ? void 0 : t.tools) !== null && n !== void 0 ? n : []) if (pl(o)) {
      const i = o, a = await i.tool();
      for (const l of (r = a.functionDeclarations) !== null && r !== void 0 ? r : []) {
        if (!l.name) throw new Error("Function declaration name is required.");
        if (s.has(l.name)) throw new Error(`Duplicate tool declaration name: ${l.name}`);
        s.set(l.name, i);
      }
    }
    return s;
  }
  async processAfcStream(e) {
    var t, n, r;
    const s = (r = (n = (t = e.config) === null || t === void 0 ? void 0 : t.automaticFunctionCalling) === null || n === void 0 ? void 0 : n.maximumRemoteCalls) !== null && r !== void 0 ? r : xC;
    let o = !1, i = 0;
    const a = await this.initAfcToolsMap(e);
    return (function(l, u, d) {
      return ls(this, arguments, function* () {
        for (var h, p, y, w, E, C; i < s; ) {
          o && (i++, o = !1);
          const D = yield bt(l.processParamsMaybeAddMcpUsage(d)), m = yield bt(l.generateContentStreamInternal(D)), b = [], v = [];
          try {
            for (var M = !0, k = (p = void 0, us(m)), R; R = yield bt(k.next()), h = R.done, !h; M = !0) {
              w = R.value, M = !1;
              const T = w;
              if (yield yield bt(T), T.candidates && (!((E = T.candidates[0]) === null || E === void 0) && E.content)) {
                v.push(T.candidates[0].content);
                for (const I of (C = T.candidates[0].content.parts) !== null && C !== void 0 ? C : []) if (i < s && I.functionCall) {
                  if (!I.functionCall.name) throw new Error("Function call name was not returned by the model.");
                  if (u.has(I.functionCall.name)) {
                    const x = yield bt(u.get(I.functionCall.name).callTool([I.functionCall]));
                    b.push(...x);
                  } else
                    throw new Error(`Automatic function calling was requested, but not all the tools the model used implement the CallableTool interface. Available tools: ${u.keys()}, mising tool: ${I.functionCall.name}`);
                }
              }
            }
          } catch (T) {
            p = { error: T };
          } finally {
            try {
              !M && !h && (y = k.return) && (yield bt(y.call(k)));
            } finally {
              if (p) throw p.error;
            }
          }
          if (b.length > 0) {
            o = !0;
            const T = new Mu();
            T.candidates = [{ content: {
              role: "user",
              parts: b
            } }], yield yield bt(T);
            const I = [];
            I.push(...v), I.push({
              role: "user",
              parts: b
            }), d.contents = rr(d.contents).concat(I);
          } else break;
        }
      });
    })(this, a, e);
  }
  async generateContentInternal(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = EC(this.apiClient, e);
      return i = Ue("{model}:generateContent", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = AC(u), h = new Mu();
        return Object.assign(h, d), h;
      });
    } else {
      const l = TC(this.apiClient, e);
      return i = Ue("{model}:generateContent", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = CC(u), h = new Mu();
        return Object.assign(h, d), h;
      });
    }
  }
  async generateContentStreamInternal(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = EC(this.apiClient, e);
      return i = Ue("{model}:streamGenerateContent?alt=sse", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.requestStream({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }), o.then(function(u) {
        return ls(this, arguments, function* () {
          var d, h, p, y;
          try {
            for (var w = !0, E = us(u), C; C = yield bt(E.next()), d = C.done, !d; w = !0) {
              y = C.value, w = !1;
              const M = y, k = AC(yield bt(M.json()), e);
              k.sdkHttpResponse = { headers: M.headers };
              const R = new Mu();
              Object.assign(R, k), yield yield bt(R);
            }
          } catch (M) {
            h = { error: M };
          } finally {
            try {
              !w && !d && (p = E.return) && (yield bt(p.call(E)));
            } finally {
              if (h) throw h.error;
            }
          }
        });
      });
    } else {
      const l = TC(this.apiClient, e);
      return i = Ue("{model}:streamGenerateContent?alt=sse", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.requestStream({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }), o.then(function(u) {
        return ls(this, arguments, function* () {
          var d, h, p, y;
          try {
            for (var w = !0, E = us(u), C; C = yield bt(E.next()), d = C.done, !d; w = !0) {
              y = C.value, w = !1;
              const M = y, k = CC(yield bt(M.json()), e);
              k.sdkHttpResponse = { headers: M.headers };
              const R = new Mu();
              Object.assign(R, k), yield yield bt(R);
            }
          } catch (M) {
            h = { error: M };
          } finally {
            try {
              !w && !d && (p = E.return) && (yield bt(p.call(E)));
            } finally {
              if (h) throw h.error;
            }
          }
        });
      });
    }
  }
  async embedContentInternal(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = c3(this.apiClient, e, e);
      return i = Ue(T4(e.model) ? "{model}:embedContent" : "{model}:predict", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = f3(u, e), h = new rC();
        return Object.assign(h, d), h;
      });
    } else {
      const l = u3(this.apiClient, e);
      return i = Ue("{model}:batchEmbedContents", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = d3(u), h = new rC();
        return Object.assign(h, d), h;
      });
    }
  }
  async generateImagesInternal(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = T3(this.apiClient, e);
      return i = Ue("{model}:predict", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = C3(u), h = new sC();
        return Object.assign(h, d), h;
      });
    } else {
      const l = S3(this.apiClient, e);
      return i = Ue("{model}:predict", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = E3(u), h = new sC();
        return Object.assign(h, d), h;
      });
    }
  }
  async editImageInternal(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) {
      const i = o3(this.apiClient, e);
      return s = Ue("{model}:predict", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: a.headers }, u;
      })), r.then((a) => {
        const l = i3(a), u = new ZW();
        return Object.assign(u, l), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async upscaleImageInternal(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) {
      const i = Cz(this.apiClient, e);
      return s = Ue("{model}:predict", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: a.headers }, u;
      })), r.then((a) => {
        const l = Az(a), u = new jW();
        return Object.assign(u, l), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async recontextImage(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) {
      const i = oz(this.apiClient, e);
      return s = Ue("{model}:predict", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r.then((a) => {
        const l = iz(a), u = new e4();
        return Object.assign(u, l), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async segmentImage(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) {
      const i = fz(this.apiClient, e);
      return s = Ue("{model}:predict", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r.then((a) => {
        const l = hz(a), u = new t4();
        return Object.assign(u, l), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async get(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = H3(this.apiClient, e);
      return i = Ue("{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), o.then((u) => Qy(u));
    } else {
      const l = q3(this.apiClient, e);
      return i = Ue("{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), o.then((u) => Xy(u));
    }
  }
  async listInternal(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = Q3(this.apiClient, e);
      return i = Ue("{models_url}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = j3(u), h = new oC();
        return Object.assign(h, d), h;
      });
    } else {
      const l = X3(this.apiClient, e);
      return i = Ue("{models_url}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = Z3(u), h = new oC();
        return Object.assign(h, d), h;
      });
    }
  }
  async update(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = Tz(this.apiClient, e);
      return i = Ue("{model}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "PATCH",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), o.then((u) => Qy(u));
    } else {
      const l = Sz(this.apiClient, e);
      return i = Ue("{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "PATCH",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), o.then((u) => Xy(u));
    }
  }
  async delete(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = t3(this.apiClient, e);
      return i = Ue("{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = r3(u), h = new iC();
        return Object.assign(h, d), h;
      });
    } else {
      const l = e3(this.apiClient, e);
      return i = Ue("{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "DELETE",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = n3(u), h = new iC();
        return Object.assign(h, d), h;
      });
    }
  }
  async countTokens(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = QK(this.apiClient, e);
      return i = Ue("{model}:countTokens", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = jK(u), h = new aC();
        return Object.assign(h, d), h;
      });
    } else {
      const l = XK(this.apiClient, e);
      return i = Ue("{model}:countTokens", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = ZK(u), h = new aC();
        return Object.assign(h, d), h;
      });
    }
  }
  async computeTokens(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) {
      const i = GK(this.apiClient, e);
      return s = Ue("{model}:computeTokens", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: a.headers }, u;
      })), r.then((a) => {
        const l = WK(a), u = new n4();
        return Object.assign(u, l), u;
      });
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async generateVideosInternal(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = P3(this.apiClient, e);
      return i = Ue("{model}:predictLongRunning", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), o.then((u) => {
        const d = k3(u), h = new lC();
        return Object.assign(h, d), h;
      });
    } else {
      const l = M3(this.apiClient, e);
      return i = Ue("{model}:predictLongRunning", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), o.then((u) => {
        const d = x3(u), h = new lC();
        return Object.assign(h, d), h;
      });
    }
  }
}, SJ = class extends yo {
  constructor(e) {
    super(), this.apiClient = e;
  }
  async getVideosOperation(e) {
    const t = e.operation, n = e.config;
    if (t.name === void 0 || t.name === "") throw new Error("Operation name is required.");
    if (this.apiClient.isVertexAI()) {
      const r = t.name.split("/operations/")[0];
      let s;
      n && "httpOptions" in n && (s = n.httpOptions);
      const o = await this.fetchPredictVideosOperationInternal({
        operationName: t.name,
        resourceName: r,
        config: { httpOptions: s }
      });
      return t._fromAPIResponse({
        apiResponse: o,
        _isVertexAI: !0
      });
    } else {
      const r = await this.getVideosOperationInternal({
        operationName: t.name,
        config: n
      });
      return t._fromAPIResponse({
        apiResponse: r,
        _isVertexAI: !1
      });
    }
  }
  async get(e) {
    const t = e.operation, n = e.config;
    if (t.name === void 0 || t.name === "") throw new Error("Operation name is required.");
    if (this.apiClient.isVertexAI()) {
      const r = t.name.split("/operations/")[0];
      let s;
      n && "httpOptions" in n && (s = n.httpOptions);
      const o = await this.fetchPredictVideosOperationInternal({
        operationName: t.name,
        resourceName: r,
        config: { httpOptions: s }
      });
      return t._fromAPIResponse({
        apiResponse: o,
        _isVertexAI: !0
      });
    } else {
      const r = await this.getVideosOperationInternal({
        operationName: t.name,
        config: n
      });
      return t._fromAPIResponse({
        apiResponse: r,
        _isVertexAI: !1
      });
    }
  }
  async getVideosOperationInternal(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = KW(e);
      return i = Ue("{operationName}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json()), o;
    } else {
      const l = VW(e);
      return i = Ue("{operationName}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json()), o;
    }
  }
  async fetchPredictVideosOperationInternal(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) {
      const i = FW(e);
      return s = Ue("{resourceName}:fetchPredictOperation", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r;
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
};
function RC(e) {
  const t = {};
  if (c(e, ["languageCodes"]) !== void 0) throw new Error("languageCodes parameter is not supported in Gemini API.");
  return t;
}
function TJ(e) {
  const t = {}, n = c(e, ["apiKey"]);
  if (n != null && f(t, ["apiKey"], n), c(e, ["apiKeyConfig"]) !== void 0) throw new Error("apiKeyConfig parameter is not supported in Gemini API.");
  if (c(e, ["authType"]) !== void 0) throw new Error("authType parameter is not supported in Gemini API.");
  if (c(e, ["googleServiceAccountConfig"]) !== void 0) throw new Error("googleServiceAccountConfig parameter is not supported in Gemini API.");
  if (c(e, ["httpBasicAuthConfig"]) !== void 0) throw new Error("httpBasicAuthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oauthConfig"]) !== void 0) throw new Error("oauthConfig parameter is not supported in Gemini API.");
  if (c(e, ["oidcConfig"]) !== void 0) throw new Error("oidcConfig parameter is not supported in Gemini API.");
  return t;
}
function EJ(e) {
  const t = {}, n = c(e, ["data"]);
  if (n != null && f(t, ["data"], n), c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const r = c(e, ["mimeType"]);
  return r != null && f(t, ["mimeType"], r), t;
}
function CJ(e) {
  const t = {}, n = c(e, ["parts"]);
  if (n != null) {
    let s = n;
    Array.isArray(s) && (s = s.map((o) => $J(o))), f(t, ["parts"], s);
  }
  const r = c(e, ["role"]);
  return r != null && f(t, ["role"], r), t;
}
function AJ(e, t, n) {
  const r = {}, s = c(t, ["expireTime"]);
  n !== void 0 && s != null && f(n, ["expireTime"], s);
  const o = c(t, ["newSessionExpireTime"]);
  n !== void 0 && o != null && f(n, ["newSessionExpireTime"], o);
  const i = c(t, ["uses"]);
  n !== void 0 && i != null && f(n, ["uses"], i);
  const a = c(t, ["liveConnectConstraints"]);
  n !== void 0 && a != null && f(n, ["bidiGenerateContentSetup"], NJ(e, a));
  const l = c(t, ["lockAdditionalFields"]);
  return n !== void 0 && l != null && f(n, ["fieldMask"], l), r;
}
function IJ(e, t) {
  const n = {}, r = c(t, ["config"]);
  return r != null && f(n, ["config"], AJ(e, r, n)), n;
}
function xJ(e) {
  const t = {};
  if (c(e, ["displayName"]) !== void 0) throw new Error("displayName parameter is not supported in Gemini API.");
  const n = c(e, ["fileUri"]);
  n != null && f(t, ["fileUri"], n);
  const r = c(e, ["mimeType"]);
  return r != null && f(t, ["mimeType"], r), t;
}
function kJ(e) {
  const t = {}, n = c(e, ["id"]);
  n != null && f(t, ["id"], n);
  const r = c(e, ["args"]);
  r != null && f(t, ["args"], r);
  const s = c(e, ["name"]);
  if (s != null && f(t, ["name"], s), c(e, ["partialArgs"]) !== void 0) throw new Error("partialArgs parameter is not supported in Gemini API.");
  if (c(e, ["willContinue"]) !== void 0) throw new Error("willContinue parameter is not supported in Gemini API.");
  return t;
}
function MJ(e) {
  const t = {}, n = c(e, ["authConfig"]);
  n != null && f(t, ["authConfig"], TJ(n));
  const r = c(e, ["enableWidget"]);
  return r != null && f(t, ["enableWidget"], r), t;
}
function PJ(e) {
  const t = {}, n = c(e, ["searchTypes"]);
  if (n != null && f(t, ["searchTypes"], n), c(e, ["blockingConfidence"]) !== void 0) throw new Error("blockingConfidence parameter is not supported in Gemini API.");
  if (c(e, ["excludeDomains"]) !== void 0) throw new Error("excludeDomains parameter is not supported in Gemini API.");
  const r = c(e, ["timeRangeFilter"]);
  return r != null && f(t, ["timeRangeFilter"], r), t;
}
function RJ(e, t) {
  const n = {}, r = c(e, ["generationConfig"]);
  t !== void 0 && r != null && f(t, ["setup", "generationConfig"], r);
  const s = c(e, ["responseModalities"]);
  t !== void 0 && s != null && f(t, [
    "setup",
    "generationConfig",
    "responseModalities"
  ], s);
  const o = c(e, ["temperature"]);
  t !== void 0 && o != null && f(t, [
    "setup",
    "generationConfig",
    "temperature"
  ], o);
  const i = c(e, ["topP"]);
  t !== void 0 && i != null && f(t, [
    "setup",
    "generationConfig",
    "topP"
  ], i);
  const a = c(e, ["topK"]);
  t !== void 0 && a != null && f(t, [
    "setup",
    "generationConfig",
    "topK"
  ], a);
  const l = c(e, ["maxOutputTokens"]);
  t !== void 0 && l != null && f(t, [
    "setup",
    "generationConfig",
    "maxOutputTokens"
  ], l);
  const u = c(e, ["mediaResolution"]);
  t !== void 0 && u != null && f(t, [
    "setup",
    "generationConfig",
    "mediaResolution"
  ], u);
  const d = c(e, ["seed"]);
  t !== void 0 && d != null && f(t, [
    "setup",
    "generationConfig",
    "seed"
  ], d);
  const h = c(e, ["speechConfig"]);
  t !== void 0 && h != null && f(t, [
    "setup",
    "generationConfig",
    "speechConfig"
  ], gw(h));
  const p = c(e, ["thinkingConfig"]);
  t !== void 0 && p != null && f(t, [
    "setup",
    "generationConfig",
    "thinkingConfig"
  ], p);
  const y = c(e, ["enableAffectiveDialog"]);
  t !== void 0 && y != null && f(t, [
    "setup",
    "generationConfig",
    "enableAffectiveDialog"
  ], y);
  const w = c(e, ["systemInstruction"]);
  t !== void 0 && w != null && f(t, ["setup", "systemInstruction"], CJ(bn(w)));
  const E = c(e, ["tools"]);
  if (t !== void 0 && E != null) {
    let T = Ol(E);
    Array.isArray(T) && (T = T.map((I) => OJ(Ll(I)))), f(t, ["setup", "tools"], T);
  }
  const C = c(e, ["sessionResumption"]);
  t !== void 0 && C != null && f(t, ["setup", "sessionResumption"], LJ(C));
  const M = c(e, ["inputAudioTranscription"]);
  t !== void 0 && M != null && f(t, ["setup", "inputAudioTranscription"], RC(M));
  const k = c(e, ["outputAudioTranscription"]);
  t !== void 0 && k != null && f(t, ["setup", "outputAudioTranscription"], RC(k));
  const R = c(e, ["realtimeInputConfig"]);
  t !== void 0 && R != null && f(t, ["setup", "realtimeInputConfig"], R);
  const D = c(e, ["contextWindowCompression"]);
  t !== void 0 && D != null && f(t, ["setup", "contextWindowCompression"], D);
  const m = c(e, ["proactivity"]);
  if (t !== void 0 && m != null && f(t, ["setup", "proactivity"], m), c(e, ["explicitVadSignal"]) !== void 0) throw new Error("explicitVadSignal parameter is not supported in Gemini API.");
  const b = c(e, ["avatarConfig"]);
  t !== void 0 && b != null && f(t, ["setup", "avatarConfig"], b);
  const v = c(e, ["safetySettings"]);
  if (t !== void 0 && v != null) {
    let T = v;
    Array.isArray(T) && (T = T.map((I) => DJ(I))), f(t, ["setup", "safetySettings"], T);
  }
  return n;
}
function NJ(e, t) {
  const n = {}, r = c(t, ["model"]);
  r != null && f(n, ["setup", "model"], Rt(e, r));
  const s = c(t, ["config"]);
  return s != null && f(n, ["config"], RJ(s, n)), n;
}
function $J(e) {
  const t = {}, n = c(e, ["mediaResolution"]);
  n != null && f(t, ["mediaResolution"], n);
  const r = c(e, ["codeExecutionResult"]);
  r != null && f(t, ["codeExecutionResult"], r);
  const s = c(e, ["executableCode"]);
  s != null && f(t, ["executableCode"], s);
  const o = c(e, ["fileData"]);
  o != null && f(t, ["fileData"], xJ(o));
  const i = c(e, ["functionCall"]);
  i != null && f(t, ["functionCall"], kJ(i));
  const a = c(e, ["functionResponse"]);
  a != null && f(t, ["functionResponse"], a);
  const l = c(e, ["inlineData"]);
  l != null && f(t, ["inlineData"], EJ(l));
  const u = c(e, ["text"]);
  u != null && f(t, ["text"], u);
  const d = c(e, ["thought"]);
  d != null && f(t, ["thought"], d);
  const h = c(e, ["thoughtSignature"]);
  h != null && f(t, ["thoughtSignature"], h);
  const p = c(e, ["videoMetadata"]);
  p != null && f(t, ["videoMetadata"], p);
  const y = c(e, ["toolCall"]);
  y != null && f(t, ["toolCall"], y);
  const w = c(e, ["toolResponse"]);
  w != null && f(t, ["toolResponse"], w);
  const E = c(e, ["partMetadata"]);
  return E != null && f(t, ["partMetadata"], E), t;
}
function DJ(e) {
  const t = {}, n = c(e, ["category"]);
  if (n != null && f(t, ["category"], n), c(e, ["method"]) !== void 0) throw new Error("method parameter is not supported in Gemini API.");
  const r = c(e, ["threshold"]);
  return r != null && f(t, ["threshold"], r), t;
}
function LJ(e) {
  const t = {}, n = c(e, ["handle"]);
  if (n != null && f(t, ["handle"], n), c(e, ["transparent"]) !== void 0) throw new Error("transparent parameter is not supported in Gemini API.");
  return t;
}
function OJ(e) {
  const t = {};
  if (c(e, ["retrieval"]) !== void 0) throw new Error("retrieval parameter is not supported in Gemini API.");
  const n = c(e, ["computerUse"]);
  n != null && f(t, ["computerUse"], n);
  const r = c(e, ["fileSearch"]);
  r != null && f(t, ["fileSearch"], r);
  const s = c(e, ["googleSearch"]);
  s != null && f(t, ["googleSearch"], PJ(s));
  const o = c(e, ["googleMaps"]);
  o != null && f(t, ["googleMaps"], MJ(o));
  const i = c(e, ["codeExecution"]);
  if (i != null && f(t, ["codeExecution"], i), c(e, ["enterpriseWebSearch"]) !== void 0) throw new Error("enterpriseWebSearch parameter is not supported in Gemini API.");
  const a = c(e, ["functionDeclarations"]);
  if (a != null) {
    let h = a;
    Array.isArray(h) && (h = h.map((p) => p)), f(t, ["functionDeclarations"], h);
  }
  const l = c(e, ["googleSearchRetrieval"]);
  if (l != null && f(t, ["googleSearchRetrieval"], l), c(e, ["parallelAiSearch"]) !== void 0) throw new Error("parallelAiSearch parameter is not supported in Gemini API.");
  const u = c(e, ["urlContext"]);
  u != null && f(t, ["urlContext"], u);
  const d = c(e, ["mcpServers"]);
  if (d != null) {
    let h = d;
    Array.isArray(h) && (h = h.map((p) => p)), f(t, ["mcpServers"], h);
  }
  return t;
}
function FJ(e) {
  const t = [];
  for (const n in e) if (Object.prototype.hasOwnProperty.call(e, n)) {
    const r = e[n];
    if (typeof r == "object" && r != null && Object.keys(r).length > 0) {
      const s = Object.keys(r).map((o) => `${n}.${o}`);
      t.push(...s);
    } else t.push(n);
  }
  return t.join(",");
}
function BJ(e, t) {
  let n = null;
  const r = e.bidiGenerateContentSetup;
  if (typeof r == "object" && r !== null && "setup" in r) {
    const o = r.setup;
    typeof o == "object" && o !== null ? (e.bidiGenerateContentSetup = o, n = o) : delete e.bidiGenerateContentSetup;
  } else r !== void 0 && delete e.bidiGenerateContentSetup;
  const s = e.fieldMask;
  if (n) {
    const o = FJ(n);
    if (Array.isArray(t?.lockAdditionalFields) && t?.lockAdditionalFields.length === 0) o ? e.fieldMask = o : delete e.fieldMask;
    else if (t?.lockAdditionalFields && t.lockAdditionalFields.length > 0 && s !== null && Array.isArray(s) && s.length > 0) {
      const i = [
        "temperature",
        "topK",
        "topP",
        "maxOutputTokens",
        "responseModalities",
        "seed",
        "speechConfig"
      ];
      let a = [];
      s.length > 0 && (a = s.map((u) => i.includes(u) ? `generationConfig.${u}` : u));
      const l = [];
      o && l.push(o), a.length > 0 && l.push(...a), l.length > 0 ? e.fieldMask = l.join(",") : delete e.fieldMask;
    } else delete e.fieldMask;
  } else s !== null && Array.isArray(s) && s.length > 0 ? e.fieldMask = s.join(",") : delete e.fieldMask;
  return e;
}
var UJ = class extends yo {
  constructor(e) {
    super(), this.apiClient = e;
  }
  async create(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("The client.tokens.create method is only supported by the Gemini Developer API.");
    {
      const i = IJ(this.apiClient, e);
      s = Ue("auth_tokens", i._url), o = i._query, delete i.config, delete i._url, delete i._query;
      const a = BJ(i, e.config);
      return r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(a),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((l) => l.json()), r.then((l) => l);
    }
  }
};
function qJ(e, t) {
  const n = {}, r = c(e, ["force"]);
  return t !== void 0 && r != null && f(t, ["_query", "force"], r), n;
}
function HJ(e) {
  const t = {}, n = c(e, ["name"]);
  n != null && f(t, ["_url", "name"], n);
  const r = c(e, ["config"]);
  return r != null && qJ(r, t), t;
}
function GJ(e) {
  const t = {}, n = c(e, ["name"]);
  return n != null && f(t, ["_url", "name"], n), t;
}
function WJ(e, t) {
  const n = {}, r = c(e, ["pageSize"]);
  t !== void 0 && r != null && f(t, ["_query", "pageSize"], r);
  const s = c(e, ["pageToken"]);
  return t !== void 0 && s != null && f(t, ["_query", "pageToken"], s), n;
}
function VJ(e) {
  const t = {}, n = c(e, ["parent"]);
  n != null && f(t, ["_url", "parent"], n);
  const r = c(e, ["config"]);
  return r != null && WJ(r, t), t;
}
function KJ(e) {
  const t = {}, n = c(e, ["sdkHttpResponse"]);
  n != null && f(t, ["sdkHttpResponse"], n);
  const r = c(e, ["nextPageToken"]);
  r != null && f(t, ["nextPageToken"], r);
  const s = c(e, ["documents"]);
  if (s != null) {
    let o = s;
    Array.isArray(o) && (o = o.map((i) => i)), f(t, ["documents"], o);
  }
  return t;
}
var zJ = class extends yo {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t) => new ua(go.PAGED_ITEM_DOCUMENTS, (n) => this.listInternal({
      parent: t.parent,
      config: n.config
    }), await this.listInternal(t), t);
  }
  async get(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = GJ(e);
      return s = Ue("{name}", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r.then((a) => a);
    }
  }
  async delete(e) {
    var t, n;
    let r = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = HJ(e);
      r = Ue("{name}", o._url), s = o._query, delete o._url, delete o._query, await this.apiClient.request({
        path: r,
        queryParams: s,
        body: JSON.stringify(o),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      });
    }
  }
  async listInternal(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = VJ(e);
      return s = Ue("{parent}/documents", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r.then((a) => {
        const l = KJ(a), u = new r4();
        return Object.assign(u, l), u;
      });
    }
  }
}, JJ = class extends yo {
  constructor(e, t = new zJ(e)) {
    super(), this.apiClient = e, this.documents = t, this.list = async (n = {}) => new ua(go.PAGED_ITEM_FILE_SEARCH_STORES, (r) => this.listInternal(r), await this.listInternal(n), n);
  }
  async uploadToFileSearchStore(e) {
    if (this.apiClient.isVertexAI()) throw new Error("Vertex AI does not support uploading files to a file search store.");
    return this.apiClient.uploadFileToFileSearchStore(e.fileSearchStoreName, e.file, e.config);
  }
  async create(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = Nz(e);
      return s = Ue("fileSearchStores", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r.then((a) => a);
    }
  }
  async get(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = Lz(e);
      return s = Ue("{name}", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r.then((a) => a);
    }
  }
  async delete(e) {
    var t, n;
    let r = "", s = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const o = Dz(e);
      r = Ue("{name}", o._url), s = o._query, delete o._url, delete o._query, await this.apiClient.request({
        path: r,
        queryParams: s,
        body: JSON.stringify(o),
        httpMethod: "DELETE",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      });
    }
  }
  async listInternal(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = Hz(e);
      return s = Ue("fileSearchStores", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r.then((a) => {
        const l = Gz(a), u = new s4();
        return Object.assign(u, l), u;
      });
    }
  }
  async uploadToFileSearchStoreInternal(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = Wz(e);
      return s = Ue("upload/v1beta/{file_search_store_name}:uploadToFileSearchStore", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r.then((a) => {
        const l = Vz(a), u = new o4();
        return Object.assign(u, l), u;
      });
    }
  }
  async importFile(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = Bz(e);
      return s = Ue("{file_search_store_name}:importFile", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json()), r.then((a) => {
        const l = Fz(a), u = new i4();
        return Object.assign(u, l), u;
      });
    }
  }
}, LP = function() {
  const { crypto: e } = globalThis;
  if (e?.randomUUID)
    return LP = e.randomUUID.bind(e), e.randomUUID();
  const t = new Uint8Array(1), n = e ? () => e.getRandomValues(t)[0] : () => Math.random() * 255 & 255;
  return "10000000-1000-4000-8000-100000000000".replace(/[018]/g, (r) => (+r ^ n() & 15 >> +r / 4).toString(16));
}, YJ = () => LP();
function jy(e) {
  return typeof e == "object" && e !== null && ("name" in e && e.name === "AbortError" || "message" in e && String(e.message).includes("FetchRequestCanceledException"));
}
var e_ = (e) => {
  if (e instanceof Error) return e;
  if (typeof e == "object" && e !== null) {
    try {
      if (Object.prototype.toString.call(e) === "[object Error]") {
        const t = new Error(e.message, e.cause ? { cause: e.cause } : {});
        return e.stack && (t.stack = e.stack), e.cause && !t.cause && (t.cause = e.cause), e.name && (t.name = e.name), t;
      }
    } catch {
    }
    try {
      return new Error(JSON.stringify(e));
    } catch {
    }
  }
  return new Error(e);
}, Yr = class extends Error {
}, Xr = class t_ extends Yr {
  constructor(t, n, r, s) {
    super(`${t_.makeMessage(t, n, r)}`), this.status = t, this.headers = s, this.error = n;
  }
  static makeMessage(t, n, r) {
    const s = n?.message ? typeof n.message == "string" ? n.message : JSON.stringify(n.message) : n ? JSON.stringify(n) : r;
    return t && s ? `${t} ${s}` : t ? `${t} status code (no body)` : s || "(no status code or body)";
  }
  static generate(t, n, r, s) {
    if (!t || !s) return new nm({
      message: r,
      cause: e_(n)
    });
    const o = n;
    return t === 400 ? new FP(t, o, r, s) : t === 401 ? new BP(t, o, r, s) : t === 403 ? new UP(t, o, r, s) : t === 404 ? new qP(t, o, r, s) : t === 409 ? new HP(t, o, r, s) : t === 422 ? new GP(t, o, r, s) : t === 429 ? new WP(t, o, r, s) : t >= 500 ? new VP(t, o, r, s) : new t_(t, o, r, s);
  }
}, n_ = class extends Xr {
  constructor({ message: e } = {}) {
    super(void 0, void 0, e || "Request was aborted.", void 0);
  }
}, nm = class extends Xr {
  constructor({ message: e, cause: t }) {
    super(void 0, void 0, e || "Connection error.", void 0), t && (this.cause = t);
  }
}, OP = class extends nm {
  constructor({ message: e } = {}) {
    super({ message: e ?? "Request timed out." });
  }
}, FP = class extends Xr {
}, BP = class extends Xr {
}, UP = class extends Xr {
}, qP = class extends Xr {
}, HP = class extends Xr {
}, GP = class extends Xr {
}, WP = class extends Xr {
}, VP = class extends Xr {
}, XJ = /^[a-z][a-z0-9+.-]*:/i, QJ = (e) => XJ.test(e), r_ = (e) => (r_ = Array.isArray, r_(e)), NC = r_;
function $C(e) {
  if (!e) return !0;
  for (const t in e) return !1;
  return !0;
}
function ZJ(e, t) {
  return Object.prototype.hasOwnProperty.call(e, t);
}
var jJ = (e, t) => {
  if (typeof t != "number" || !Number.isInteger(t)) throw new Yr(`${e} must be an integer`);
  if (t < 0) throw new Yr(`${e} must be a positive integer`);
  return t;
}, e5 = (e) => {
  try {
    return JSON.parse(e);
  } catch {
    return;
  }
}, t5 = (e) => new Promise((t) => setTimeout(t, e));
function n5() {
  if (typeof fetch < "u") return fetch;
  throw new Error("`fetch` is not defined as a global; Either pass `fetch` to the client, `new GeminiNextGenAPIClient({ fetch })` or polyfill the global, `globalThis.fetch = fetch`");
}
function KP(...e) {
  const t = globalThis.ReadableStream;
  if (typeof t > "u") throw new Error("`ReadableStream` is not defined as a global; You will need to polyfill it, `globalThis.ReadableStream = ReadableStream`");
  return new t(...e);
}
function r5(e) {
  let t = Symbol.asyncIterator in e ? e[Symbol.asyncIterator]() : e[Symbol.iterator]();
  return KP({
    start() {
    },
    async pull(n) {
      const { done: r, value: s } = await t.next();
      r ? n.close() : n.enqueue(s);
    },
    async cancel() {
      var n;
      await ((n = t.return) === null || n === void 0 ? void 0 : n.call(t));
    }
  });
}
function zP(e) {
  if (e[Symbol.asyncIterator]) return e;
  const t = e.getReader();
  return {
    async next() {
      try {
        const n = await t.read();
        return n?.done && t.releaseLock(), n;
      } catch (n) {
        throw t.releaseLock(), n;
      }
    },
    async return() {
      const n = t.cancel();
      return t.releaseLock(), await n, {
        done: !0,
        value: void 0
      };
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
async function s5(e) {
  var t, n;
  if (e === null || typeof e != "object") return;
  if (e[Symbol.asyncIterator]) {
    await ((n = (t = e[Symbol.asyncIterator]()).return) === null || n === void 0 ? void 0 : n.call(t));
    return;
  }
  const r = e.getReader(), s = r.cancel();
  r.releaseLock(), await s;
}
var o5 = ({ headers: e, body: t }) => ({
  bodyHeaders: { "content-type": "application/json" },
  body: JSON.stringify(t)
});
function i5(e) {
  return Object.entries(e).filter(([t, n]) => typeof n < "u").map(([t, n]) => {
    if (typeof n == "string" || typeof n == "number" || typeof n == "boolean") return `${encodeURIComponent(t)}=${encodeURIComponent(n)}`;
    if (n === null) return `${encodeURIComponent(t)}=`;
    throw new Yr(`Cannot stringify type ${typeof n}; Expected string, number, boolean, or null. If you need to pass nested query parameters, you can manually encode them, e.g. { query: { 'foo[key1]': value1, 'foo[key2]': value2 } }, and please open a GitHub issue requesting better support for your use case.`);
  }).join("&");
}
var a5 = "0.0.1", JP = () => {
  var e;
  if (typeof File > "u") {
    const { process: t } = globalThis, n = typeof ((e = t?.versions) === null || e === void 0 ? void 0 : e.node) == "string" && parseInt(t.versions.node.split(".")) < 20;
    throw new Error("`File` is not defined as a global, which is required for file uploads." + (n ? " Update to Node 20 LTS or newer, or set `globalThis.File` to `import('node:buffer').File`." : ""));
  }
};
function lv(e, t, n) {
  return JP(), new File(e, t ?? "unknown_file", n);
}
function l5(e) {
  return (typeof e == "object" && e !== null && ("name" in e && e.name && String(e.name) || "url" in e && e.url && String(e.url) || "filename" in e && e.filename && String(e.filename) || "path" in e && e.path && String(e.path)) || "").split(/[\\/]/).pop() || void 0;
}
var u5 = (e) => e != null && typeof e == "object" && typeof e[Symbol.asyncIterator] == "function", YP = (e) => e != null && typeof e == "object" && typeof e.size == "number" && typeof e.type == "string" && typeof e.text == "function" && typeof e.slice == "function" && typeof e.arrayBuffer == "function", c5 = (e) => e != null && typeof e == "object" && typeof e.name == "string" && typeof e.lastModified == "number" && YP(e), d5 = (e) => e != null && typeof e == "object" && typeof e.url == "string" && typeof e.blob == "function";
async function f5(e, t, n) {
  if (JP(), e = await e, c5(e))
    return e instanceof File ? e : lv([await e.arrayBuffer()], e.name);
  if (d5(e)) {
    const s = await e.blob();
    return t || (t = new URL(e.url).pathname.split(/[\\/]/).pop()), lv(await s_(s), t, n);
  }
  const r = await s_(e);
  if (t || (t = l5(e)), !n?.type) {
    const s = r.find((o) => typeof o == "object" && "type" in o && o.type);
    typeof s == "string" && (n = Object.assign(Object.assign({}, n), { type: s }));
  }
  return lv(r, t, n);
}
async function s_(e) {
  var t, n, r, s, o;
  let i = [];
  if (typeof e == "string" || ArrayBuffer.isView(e) || e instanceof ArrayBuffer) i.push(e);
  else if (YP(e)) i.push(e instanceof Blob ? e : await e.arrayBuffer());
  else if (u5(e)) try {
    for (var a = !0, l = us(e), u; u = await l.next(), t = u.done, !t; a = !0) {
      s = u.value, a = !1;
      const d = s;
      i.push(...await s_(d));
    }
  } catch (d) {
    n = { error: d };
  } finally {
    try {
      !a && !t && (r = l.return) && await r.call(l);
    } finally {
      if (n) throw n.error;
    }
  }
  else {
    const d = (o = e?.constructor) === null || o === void 0 ? void 0 : o.name;
    throw new Error(`Unexpected data type: ${typeof e}${d ? `; constructor: ${d}` : ""}${h5(e)}`);
  }
  return i;
}
function h5(e) {
  return typeof e != "object" || e === null ? "" : `; props: [${Object.getOwnPropertyNames(e).map((t) => `"${t}"`).join(", ")}]`;
}
var vw = class {
  constructor(e) {
    this._client = e;
  }
};
vw._key = [];
function XP(e) {
  return e.replace(/[^A-Za-z0-9\-._~!$&'()*+,;=:@]+/g, encodeURIComponent);
}
var DC = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.create(null)), p5 = (e = XP) => (function(n, ...r) {
  if (n.length === 1) return n[0];
  let s = !1;
  const o = [], i = n.reduce((d, h, p) => {
    var y, w, E;
    /[?#]/.test(h) && (s = !0);
    const C = r[p];
    let M = (s ? encodeURIComponent : e)("" + C);
    return p !== r.length && (C == null || typeof C == "object" && C.toString === ((E = Object.getPrototypeOf((w = Object.getPrototypeOf((y = C.hasOwnProperty) !== null && y !== void 0 ? y : DC)) !== null && w !== void 0 ? w : DC)) === null || E === void 0 ? void 0 : E.toString)) && (M = C + "", o.push({
      start: d.length + h.length,
      length: M.length,
      error: `Value of type ${Object.prototype.toString.call(C).slice(8, -1)} is not a valid path parameter`
    })), d + h + (p === r.length ? "" : M);
  }, ""), a = i.split(/[?#]/, 1)[0], l = /(^|\/)(?:\.|%2e){1,2}(?=\/|$)/gi;
  let u;
  for (; (u = l.exec(a)) !== null; ) {
    const d = u[0].startsWith("/"), h = d ? 1 : 0, p = d ? u[0].slice(1) : u[0];
    o.push({
      start: u.index + h,
      length: p.length,
      error: `Value "${p}" can't be safely passed as a path parameter`
    });
  }
  if (o.sort((d, h) => d.start - h.start), o.length > 0) {
    let d = 0;
    const h = o.reduce((p, y) => {
      const w = " ".repeat(y.start - d), E = "^".repeat(y.length);
      return d = y.start + y.length, p + w + E;
    }, "");
    throw new Yr(`Path parameters result in path with invalid segments:
${o.map((p) => p.error).join(`
`)}
${i}
${h}`);
  }
  return i;
}), ns = /* @__PURE__ */ p5(XP), QP = class extends vw {
  create(e, t) {
    var n;
    const { api_version: r = this._client.apiVersion } = e, s = Go(e, ["api_version"]);
    if ("model" in s && "agent_config" in s) throw new Yr("Invalid request: specified `model` and `agent_config`. If specifying `model`, use `generation_config`.");
    if ("agent" in s && "generation_config" in s) throw new Yr("Invalid request: specified `agent` and `generation_config`. If specifying `agent`, use `agent_config`.");
    return this._client.post(ns`/${r}/interactions`, Object.assign(Object.assign({ body: s }, t), { stream: (n = e.stream) !== null && n !== void 0 ? n : !1 }));
  }
  delete(e, t = {}, n) {
    const { api_version: r = this._client.apiVersion } = t ?? {};
    return this._client.delete(ns`/${r}/interactions/${e}`, n);
  }
  cancel(e, t = {}, n) {
    const { api_version: r = this._client.apiVersion } = t ?? {};
    return this._client.post(ns`/${r}/interactions/${e}/cancel`, n);
  }
  get(e, t = {}, n) {
    var r;
    const s = t ?? {}, { api_version: o = this._client.apiVersion } = s, i = Go(s, ["api_version"]);
    return this._client.get(ns`/${o}/interactions/${e}`, Object.assign(Object.assign({ query: i }, n), { stream: (r = t?.stream) !== null && r !== void 0 ? r : !1 }));
  }
};
QP._key = Object.freeze(["interactions"]);
var ZP = class extends QP {
}, jP = class extends vw {
  create(e, t) {
    const { api_version: n = this._client.apiVersion, webhook_id: r } = e, s = Go(e, ["api_version", "webhook_id"]);
    return this._client.post(ns`/${n}/webhooks`, Object.assign({
      query: { webhook_id: r },
      body: s
    }, t));
  }
  update(e, t, n) {
    const { api_version: r = this._client.apiVersion, update_mask: s } = t, o = Go(t, ["api_version", "update_mask"]);
    return this._client.patch(ns`/${r}/webhooks/${e}`, Object.assign({
      query: { update_mask: s },
      body: o
    }, n));
  }
  list(e = {}, t) {
    const n = e ?? {}, { api_version: r = this._client.apiVersion } = n, s = Go(n, ["api_version"]);
    return this._client.get(ns`/${r}/webhooks`, Object.assign({ query: s }, t));
  }
  delete(e, t = {}, n) {
    const { api_version: r = this._client.apiVersion } = t ?? {};
    return this._client.delete(ns`/${r}/webhooks/${e}`, n);
  }
  get(e, t = {}, n) {
    const { api_version: r = this._client.apiVersion } = t ?? {};
    return this._client.get(ns`/${r}/webhooks/${e}`, n);
  }
  ping(e, t = void 0, n) {
    const { api_version: r = this._client.apiVersion, body: s } = t ?? {};
    return this._client.post(ns`/${r}/webhooks/${e}:ping`, Object.assign({ body: s }, n));
  }
  rotateSigningSecret(e, t = {}, n) {
    const r = t ?? {}, { api_version: s = this._client.apiVersion } = r, o = Go(r, ["api_version"]);
    return this._client.post(ns`/${s}/webhooks/${e}:rotateSigningSecret`, Object.assign({ body: o }, n));
  }
};
jP._key = Object.freeze(["webhooks"]);
var eR = class extends jP {
};
function m5(e) {
  let t = 0;
  for (const s of e) t += s.length;
  const n = new Uint8Array(t);
  let r = 0;
  for (const s of e)
    n.set(s, r), r += s.length;
  return n;
}
var xf;
function yw(e) {
  let t;
  return (xf ?? (t = new globalThis.TextEncoder(), xf = t.encode.bind(t)))(e);
}
var kf;
function LC(e) {
  let t;
  return (kf ?? (t = new globalThis.TextDecoder(), kf = t.decode.bind(t)))(e);
}
var rm = class {
  constructor() {
    this.buffer = new Uint8Array(), this.carriageReturnIndex = null, this.searchIndex = 0;
  }
  decode(e) {
    var t;
    if (e == null) return [];
    const n = e instanceof ArrayBuffer ? new Uint8Array(e) : typeof e == "string" ? yw(e) : e;
    this.buffer = m5([this.buffer, n]);
    const r = [];
    let s;
    for (; (s = g5(this.buffer, (t = this.carriageReturnIndex) !== null && t !== void 0 ? t : this.searchIndex)) != null; ) {
      if (s.carriage && this.carriageReturnIndex == null) {
        this.carriageReturnIndex = s.index;
        continue;
      }
      if (this.carriageReturnIndex != null && (s.index !== this.carriageReturnIndex + 1 || s.carriage)) {
        r.push(LC(this.buffer.subarray(0, this.carriageReturnIndex - 1))), this.buffer = this.buffer.subarray(this.carriageReturnIndex), this.carriageReturnIndex = null, this.searchIndex = 0;
        continue;
      }
      const o = this.carriageReturnIndex !== null ? s.preceding - 1 : s.preceding, i = LC(this.buffer.subarray(0, o));
      r.push(i), this.buffer = this.buffer.subarray(s.index), this.carriageReturnIndex = null, this.searchIndex = 0;
    }
    return this.searchIndex = Math.max(0, this.buffer.length - 1), r;
  }
  flush() {
    return this.buffer.length ? this.decode(`
`) : [];
  }
};
rm.NEWLINE_CHARS = /* @__PURE__ */ new Set([`
`, "\r"]);
rm.NEWLINE_REGEXP = /\r\n|[\n\r]/g;
function g5(e, t) {
  const s = t ?? 0, o = e.indexOf(10, s), i = e.indexOf(13, s);
  if (o === -1 && i === -1) return null;
  let a;
  return o !== -1 && i !== -1 ? a = Math.min(o, i) : a = o !== -1 ? o : i, e[a] === 10 ? {
    preceding: a,
    index: a + 1,
    carriage: !1
  } : {
    preceding: a,
    index: a + 1,
    carriage: !0
  };
}
var up = {
  off: 0,
  error: 200,
  warn: 300,
  info: 400,
  debug: 500
}, OC = (e, t, n) => {
  if (e) {
    if (ZJ(up, e)) return e;
    Wn(n).warn(`${t} was set to ${JSON.stringify(e)}, expected one of ${JSON.stringify(Object.keys(up))}`);
  }
};
function Ku() {
}
function Mf(e, t, n) {
  return !t || up[e] > up[n] ? Ku : t[e].bind(t);
}
var v5 = {
  error: Ku,
  warn: Ku,
  info: Ku,
  debug: Ku
}, FC = /* @__PURE__ */ new WeakMap();
function Wn(e) {
  var t;
  const n = e.logger, r = (t = e.logLevel) !== null && t !== void 0 ? t : "off";
  if (!n) return v5;
  const s = FC.get(n);
  if (s && s[0] === r) return s[1];
  const o = {
    error: Mf("error", n, r),
    warn: Mf("warn", n, r),
    info: Mf("info", n, r),
    debug: Mf("debug", n, r)
  };
  return FC.set(n, [r, o]), o;
}
var Ri = (e) => (e.options && (e.options = Object.assign({}, e.options), delete e.options.headers), e.headers && (e.headers = Object.fromEntries((e.headers instanceof Headers ? [...e.headers] : Object.entries(e.headers)).map(([t, n]) => [t, t.toLowerCase() === "x-goog-api-key" || t.toLowerCase() === "authorization" || t.toLowerCase() === "cookie" || t.toLowerCase() === "set-cookie" ? "***" : n]))), "retryOfRequestLogID" in e && (e.retryOfRequestLogID && (e.retryOf = e.retryOfRequestLogID), delete e.retryOfRequestLogID), e), y5 = class zu {
  constructor(t, n, r) {
    this.iterator = t, this.controller = n, this.client = r;
  }
  static fromSSEResponse(t, n, r) {
    let s = !1;
    const o = r ? Wn(r) : console;
    function i() {
      return ls(this, arguments, function* () {
        var l, u, d, h;
        if (s) throw new Yr("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
        s = !0;
        let p = !1;
        try {
          try {
            for (var y = !0, w = us(_5(t, n)), E; E = yield bt(w.next()), l = E.done, !l; y = !0) {
              h = E.value, y = !1;
              const C = h;
              if (!p)
                if (C.data.startsWith("[DONE]")) {
                  p = !0;
                  continue;
                } else try {
                  yield yield bt(JSON.parse(C.data));
                } catch (M) {
                  throw o.error("Could not parse message into JSON:", C.data), o.error("From chunk:", C.raw), M;
                }
            }
          } catch (C) {
            u = { error: C };
          } finally {
            try {
              !y && !l && (d = w.return) && (yield bt(d.call(w)));
            } finally {
              if (u) throw u.error;
            }
          }
          p = !0;
        } catch (C) {
          if (jy(C)) return yield bt(void 0);
          throw C;
        } finally {
          p || n.abort();
        }
      });
    }
    return new zu(i, n, r);
  }
  static fromReadableStream(t, n, r) {
    let s = !1;
    function o() {
      return ls(this, arguments, function* () {
        var l, u, d, h;
        const p = new rm(), y = zP(t);
        try {
          for (var w = !0, E = us(y), C; C = yield bt(E.next()), l = C.done, !l; w = !0) {
            h = C.value, w = !1;
            const M = h;
            for (const k of p.decode(M)) yield yield bt(k);
          }
        } catch (M) {
          u = { error: M };
        } finally {
          try {
            !w && !l && (d = E.return) && (yield bt(d.call(E)));
          } finally {
            if (u) throw u.error;
          }
        }
        for (const M of p.flush()) yield yield bt(M);
      });
    }
    function i() {
      return ls(this, arguments, function* () {
        var l, u, d, h;
        if (s) throw new Yr("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
        s = !0;
        let p = !1;
        try {
          try {
            for (var y = !0, w = us(o()), E; E = yield bt(w.next()), l = E.done, !l; y = !0) {
              h = E.value, y = !1;
              const C = h;
              p || C && (yield yield bt(JSON.parse(C)));
            }
          } catch (C) {
            u = { error: C };
          } finally {
            try {
              !y && !l && (d = w.return) && (yield bt(d.call(w)));
            } finally {
              if (u) throw u.error;
            }
          }
          p = !0;
        } catch (C) {
          if (jy(C)) return yield bt(void 0);
          throw C;
        } finally {
          p || n.abort();
        }
      });
    }
    return new zu(i, n, r);
  }
  [Symbol.asyncIterator]() {
    return this.iterator();
  }
  tee() {
    const t = [], n = [], r = this.iterator(), s = (o) => ({ next: () => {
      if (o.length === 0) {
        const i = r.next();
        t.push(i), n.push(i);
      }
      return o.shift();
    } });
    return [new zu(() => s(t), this.controller, this.client), new zu(() => s(n), this.controller, this.client)];
  }
  toReadableStream() {
    const t = this;
    let n;
    return KP({
      async start() {
        n = t[Symbol.asyncIterator]();
      },
      async pull(r) {
        try {
          const { value: s, done: o } = await n.next();
          if (o) return r.close();
          const i = yw(JSON.stringify(s) + `
`);
          r.enqueue(i);
        } catch (s) {
          r.error(s);
        }
      },
      async cancel() {
        var r;
        await ((r = n.return) === null || r === void 0 ? void 0 : r.call(n));
      }
    });
  }
};
function _5(e, t) {
  return ls(this, arguments, function* () {
    var r, s, o, i;
    if (!e.body)
      throw t.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new Yr("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new Yr("Attempted to iterate over a response with no body");
    const a = new w5(), l = new rm(), u = zP(e.body);
    try {
      for (var d = !0, h = us(b5(u)), p; p = yield bt(h.next()), r = p.done, !r; d = !0) {
        i = p.value, d = !1;
        const y = i;
        for (const w of l.decode(y)) {
          const E = a.decode(w);
          E && (yield yield bt(E));
        }
      }
    } catch (y) {
      s = { error: y };
    } finally {
      try {
        !d && !r && (o = h.return) && (yield bt(o.call(h)));
      } finally {
        if (s) throw s.error;
      }
    }
    for (const y of l.flush()) {
      const w = a.decode(y);
      w && (yield yield bt(w));
    }
  });
}
function b5(e) {
  return ls(this, arguments, function* () {
    var n, r, s, o;
    try {
      for (var i = !0, a = us(e), l; l = yield bt(a.next()), n = l.done, !n; i = !0) {
        o = l.value, i = !1;
        const u = o;
        u != null && (yield yield bt(u instanceof ArrayBuffer ? new Uint8Array(u) : typeof u == "string" ? yw(u) : u));
      }
    } catch (u) {
      r = { error: u };
    } finally {
      try {
        !i && !n && (s = a.return) && (yield bt(s.call(a)));
      } finally {
        if (r) throw r.error;
      }
    }
  });
}
var w5 = class {
  constructor() {
    this.event = null, this.data = [], this.chunks = [];
  }
  decode(e) {
    if (e.endsWith("\r") && (e = e.substring(0, e.length - 1)), !e) {
      if (!this.event && !this.data.length) return null;
      const s = {
        event: this.event,
        data: this.data.join(`
`),
        raw: this.chunks
      };
      return this.event = null, this.data = [], this.chunks = [], s;
    }
    if (this.chunks.push(e), e.startsWith(":")) return null;
    let [t, n, r] = S5(e, ":");
    return r.startsWith(" ") && (r = r.substring(1)), t === "event" ? this.event = r : t === "data" && this.data.push(r), null;
  }
};
function S5(e, t) {
  const n = e.indexOf(t);
  return n !== -1 ? [
    e.substring(0, n),
    t,
    e.substring(n + t.length)
  ] : [
    e,
    "",
    ""
  ];
}
async function T5(e, t) {
  const { response: n, requestLogID: r, retryOfRequestLogID: s, startTime: o } = t, i = await (async () => {
    var a;
    if (t.options.stream)
      return Wn(e).debug("response", n.status, n.url, n.headers, n.body), t.options.__streamClass ? t.options.__streamClass.fromSSEResponse(n, t.controller, e) : y5.fromSSEResponse(n, t.controller, e);
    if (n.status === 204) return null;
    if (t.options.__binaryResponse) return n;
    const l = n.headers.get("content-type"), u = (a = l?.split(";")[0]) === null || a === void 0 ? void 0 : a.trim();
    return u?.includes("application/json") || u?.endsWith("+json") ? n.headers.get("content-length") === "0" ? void 0 : await n.json() : await n.text();
  })();
  return Wn(e).debug(`[${r}] response parsed`, Ri({
    retryOfRequestLogID: s,
    url: n.url,
    status: n.status,
    body: i,
    durationMs: Date.now() - o
  })), i;
}
var E5 = class tR extends Promise {
  constructor(t, n, r = T5) {
    super((s) => {
      s(null);
    }), this.responsePromise = n, this.parseResponse = r, this.client = t;
  }
  _thenUnwrap(t) {
    return new tR(this.client, this.responsePromise, async (n, r) => t(await this.parseResponse(n, r), r));
  }
  asResponse() {
    return this.responsePromise.then((t) => t.response);
  }
  async withResponse() {
    const [t, n] = await Promise.all([this.parse(), this.asResponse()]);
    return {
      data: t,
      response: n
    };
  }
  parse() {
    return this.parsedPromise || (this.parsedPromise = this.responsePromise.then((t) => this.parseResponse(this.client, t))), this.parsedPromise;
  }
  then(t, n) {
    return this.parse().then(t, n);
  }
  catch(t) {
    return this.parse().catch(t);
  }
  finally(t) {
    return this.parse().finally(t);
  }
}, nR = /* @__PURE__ */ Symbol("brand.privateNullableHeaders");
function* C5(e) {
  if (!e) return;
  if (nR in e) {
    const { values: r, nulls: s } = e;
    yield* r.entries();
    for (const o of s) yield [o, null];
    return;
  }
  let t = !1, n;
  e instanceof Headers ? n = e.entries() : NC(e) ? n = e : (t = !0, n = Object.entries(e ?? {}));
  for (let r of n) {
    const s = r[0];
    if (typeof s != "string") throw new TypeError("expected header name to be a string");
    const o = NC(r[1]) ? r[1] : [r[1]];
    let i = !1;
    for (const a of o)
      a !== void 0 && (t && !i && (i = !0, yield [s, null]), yield [s, a]);
  }
}
var Pu = (e) => {
  const t = new Headers(), n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const s = /* @__PURE__ */ new Set();
    for (const [o, i] of C5(r)) {
      const a = o.toLowerCase();
      s.has(a) || (t.delete(o), s.add(a)), i === null ? (t.delete(o), n.add(a)) : (t.append(o, i), n.delete(a));
    }
  }
  return {
    [nR]: !0,
    values: t,
    nulls: n
  };
}, uv = (e) => {
  var t, n, r, s, o;
  if (typeof globalThis.process < "u") return ((n = (t = globalThis.process.env) === null || t === void 0 ? void 0 : t[e]) === null || n === void 0 ? void 0 : n.trim()) || void 0;
  if (typeof globalThis.Deno < "u") return ((o = (s = (r = globalThis.Deno.env) === null || r === void 0 ? void 0 : r.get) === null || s === void 0 ? void 0 : s.call(r, e)) === null || o === void 0 ? void 0 : o.trim()) || void 0;
}, rR, sR = class oR {
  constructor(t) {
    var n, r, s, o, i, a, l, { baseURL: u = uv("GEMINI_NEXT_GEN_API_BASE_URL"), apiKey: d = (n = uv("GEMINI_API_KEY")) !== null && n !== void 0 ? n : null, apiVersion: h = "v1beta" } = t, p = Go(t, [
      "baseURL",
      "apiKey",
      "apiVersion"
    ]);
    const y = Object.assign(Object.assign({
      apiKey: d,
      apiVersion: h
    }, p), { baseURL: u || "https://generativelanguage.googleapis.com" });
    this.baseURL = y.baseURL, this.timeout = (r = y.timeout) !== null && r !== void 0 ? r : oR.DEFAULT_TIMEOUT, this.logger = (s = y.logger) !== null && s !== void 0 ? s : console;
    const w = "warn";
    this.logLevel = w, this.logLevel = (i = (o = OC(y.logLevel, "ClientOptions.logLevel", this)) !== null && o !== void 0 ? o : OC(uv("GEMINI_NEXT_GEN_API_LOG"), "process.env['GEMINI_NEXT_GEN_API_LOG']", this)) !== null && i !== void 0 ? i : w, this.fetchOptions = y.fetchOptions, this.maxRetries = (a = y.maxRetries) !== null && a !== void 0 ? a : 2, this.fetch = (l = y.fetch) !== null && l !== void 0 ? l : n5(), this.encoder = o5, this._options = y, this.apiKey = d, this.apiVersion = h, this.clientAdapter = y.clientAdapter;
  }
  withOptions(t) {
    return new this.constructor(Object.assign(Object.assign(Object.assign({}, this._options), {
      baseURL: this.baseURL,
      maxRetries: this.maxRetries,
      timeout: this.timeout,
      logger: this.logger,
      logLevel: this.logLevel,
      fetch: this.fetch,
      fetchOptions: this.fetchOptions,
      apiKey: this.apiKey,
      apiVersion: this.apiVersion
    }), t));
  }
  baseURLOverridden() {
    return this.baseURL !== "https://generativelanguage.googleapis.com";
  }
  defaultQuery() {
    return this._options.defaultQuery;
  }
  validateHeaders({ values: t, nulls: n }) {
    if (!(t.has("authorization") || t.has("x-goog-api-key")) && !(this.apiKey && t.get("x-goog-api-key")) && !n.has("x-goog-api-key"))
      throw new Error('Could not resolve authentication method. Expected the apiKey to be set. Or for the "x-goog-api-key" headers to be explicitly omitted');
  }
  async authHeaders(t) {
    const n = Pu([t.headers]);
    if (!(n.values.has("authorization") || n.values.has("x-goog-api-key"))) {
      if (this.apiKey) return Pu([{ "x-goog-api-key": this.apiKey }]);
      if (this.clientAdapter && this.clientAdapter.isVertexAI()) return Pu([await this.clientAdapter.getAuthHeaders()]);
    }
  }
  stringifyQuery(t) {
    return i5(t);
  }
  getUserAgent() {
    return `${this.constructor.name}/JS ${a5}`;
  }
  defaultIdempotencyKey() {
    return `stainless-node-retry-${YJ()}`;
  }
  makeStatusError(t, n, r, s) {
    return Xr.generate(t, n, r, s);
  }
  buildURL(t, n, r) {
    const s = !this.baseURLOverridden() && r || this.baseURL, o = QJ(t) ? new URL(t) : new URL(s + (s.endsWith("/") && t.startsWith("/") ? t.slice(1) : t)), i = this.defaultQuery(), a = Object.fromEntries(o.searchParams);
    return (!$C(i) || !$C(a)) && (n = Object.assign(Object.assign(Object.assign({}, a), i), n)), typeof n == "object" && n && !Array.isArray(n) && (o.search = this.stringifyQuery(n)), o.toString();
  }
  async prepareOptions(t) {
    if (this.clientAdapter && this.clientAdapter.isVertexAI() && !t.path.startsWith(`/${this.apiVersion}/projects/`)) {
      const n = t.path.slice(this.apiVersion.length + 1);
      t.path = `/${this.apiVersion}/projects/${this.clientAdapter.getProject()}/locations/${this.clientAdapter.getLocation()}${n}`;
    }
  }
  async prepareRequest(t, { url: n, options: r }) {
  }
  get(t, n) {
    return this.methodRequest("get", t, n);
  }
  post(t, n) {
    return this.methodRequest("post", t, n);
  }
  patch(t, n) {
    return this.methodRequest("patch", t, n);
  }
  put(t, n) {
    return this.methodRequest("put", t, n);
  }
  delete(t, n) {
    return this.methodRequest("delete", t, n);
  }
  methodRequest(t, n, r) {
    return this.request(Promise.resolve(r).then((s) => Object.assign({
      method: t,
      path: n
    }, s)));
  }
  request(t, n = null) {
    return new E5(this, this.makeRequest(t, n, void 0));
  }
  async makeRequest(t, n, r) {
    var s, o, i;
    const a = await t, l = (s = a.maxRetries) !== null && s !== void 0 ? s : this.maxRetries;
    n == null && (n = l), await this.prepareOptions(a);
    const { req: u, url: d, timeout: h } = await this.buildRequest(a, { retryCount: l - n });
    await this.prepareRequest(u, {
      url: d,
      options: a
    });
    const p = "log_" + (Math.random() * (1 << 24) | 0).toString(16).padStart(6, "0"), y = r === void 0 ? "" : `, retryOf: ${r}`, w = Date.now();
    if (Wn(this).debug(`[${p}] sending request`, Ri({
      retryOfRequestLogID: r,
      method: a.method,
      url: d,
      options: a,
      headers: u.headers
    })), !((o = a.signal) === null || o === void 0) && o.aborted) throw new n_();
    const E = new AbortController(), C = await this.fetchWithTimeout(d, u, h, E).catch(e_), M = Date.now();
    if (C instanceof globalThis.Error) {
      const R = `retrying, ${n} attempts remaining`;
      if (!((i = a.signal) === null || i === void 0) && i.aborted) throw new n_();
      const D = jy(C) || /timed? ?out/i.test(String(C) + ("cause" in C ? String(C.cause) : ""));
      if (n)
        return Wn(this).info(`[${p}] connection ${D ? "timed out" : "failed"} - ${R}`), Wn(this).debug(`[${p}] connection ${D ? "timed out" : "failed"} (${R})`, Ri({
          retryOfRequestLogID: r,
          url: d,
          durationMs: M - w,
          message: C.message
        })), this.retryRequest(a, n, r ?? p);
      throw Wn(this).info(`[${p}] connection ${D ? "timed out" : "failed"} - error; no more retries left`), Wn(this).debug(`[${p}] connection ${D ? "timed out" : "failed"} (error; no more retries left)`, Ri({
        retryOfRequestLogID: r,
        url: d,
        durationMs: M - w,
        message: C.message
      })), D ? new OP() : new nm({ cause: C });
    }
    const k = `[${p}${y}] ${u.method} ${d} ${C.ok ? "succeeded" : "failed"} with status ${C.status} in ${M - w}ms`;
    if (!C.ok) {
      const R = await this.shouldRetry(C);
      if (n && R) {
        const T = `retrying, ${n} attempts remaining`;
        return await s5(C.body), Wn(this).info(`${k} - ${T}`), Wn(this).debug(`[${p}] response error (${T})`, Ri({
          retryOfRequestLogID: r,
          url: C.url,
          status: C.status,
          headers: C.headers,
          durationMs: M - w
        })), this.retryRequest(a, n, r ?? p, C.headers);
      }
      const D = R ? "error; no more retries left" : "error; not retryable";
      Wn(this).info(`${k} - ${D}`);
      const m = await C.text().catch((T) => e_(T).message), b = e5(m), v = b ? void 0 : m;
      throw Wn(this).debug(`[${p}] response error (${D})`, Ri({
        retryOfRequestLogID: r,
        url: C.url,
        status: C.status,
        headers: C.headers,
        message: v,
        durationMs: Date.now() - w
      })), this.makeStatusError(C.status, b, v, C.headers);
    }
    return Wn(this).info(k), Wn(this).debug(`[${p}] response start`, Ri({
      retryOfRequestLogID: r,
      url: C.url,
      status: C.status,
      headers: C.headers,
      durationMs: M - w
    })), {
      response: C,
      options: a,
      controller: E,
      requestLogID: p,
      retryOfRequestLogID: r,
      startTime: w
    };
  }
  async fetchWithTimeout(t, n, r, s) {
    const o = n || {}, { signal: i, method: a } = o, l = Go(o, ["signal", "method"]), u = this._makeAbort(s);
    i && i.addEventListener("abort", u, { once: !0 });
    const d = setTimeout(u, r), h = globalThis.ReadableStream && l.body instanceof globalThis.ReadableStream || typeof l.body == "object" && l.body !== null && Symbol.asyncIterator in l.body, p = Object.assign(Object.assign(Object.assign({ signal: s.signal }, h ? { duplex: "half" } : {}), { method: "GET" }), l);
    a && (p.method = a.toUpperCase());
    try {
      return await this.fetch.call(void 0, t, p);
    } finally {
      clearTimeout(d);
    }
  }
  async shouldRetry(t) {
    const n = t.headers.get("x-should-retry");
    return n === "true" ? !0 : n === "false" ? !1 : t.status === 408 || t.status === 409 || t.status === 429 || t.status >= 500;
  }
  async retryRequest(t, n, r, s) {
    var o;
    let i;
    const a = s?.get("retry-after-ms");
    if (a) {
      const u = parseFloat(a);
      Number.isNaN(u) || (i = u);
    }
    const l = s?.get("retry-after");
    if (l && !i) {
      const u = parseFloat(l);
      Number.isNaN(u) ? i = Date.parse(l) - Date.now() : i = u * 1e3;
    }
    if (i === void 0) {
      const u = (o = t.maxRetries) !== null && o !== void 0 ? o : this.maxRetries;
      i = this.calculateDefaultRetryTimeoutMillis(n, u);
    }
    return await t5(i), this.makeRequest(t, n - 1, r);
  }
  calculateDefaultRetryTimeoutMillis(t, n) {
    const o = n - t;
    return Math.min(0.5 * Math.pow(2, o), 8) * (1 - Math.random() * 0.25) * 1e3;
  }
  async buildRequest(t, { retryCount: n = 0 } = {}) {
    var r, s, o;
    const i = Object.assign({}, t), { method: a, path: l, query: u, defaultBaseURL: d } = i, h = this.buildURL(l, u, d);
    "timeout" in i && jJ("timeout", i.timeout), i.timeout = (r = i.timeout) !== null && r !== void 0 ? r : this.timeout;
    const { bodyHeaders: p, body: y } = this.buildBody({ options: i }), w = await this.buildHeaders({
      options: t,
      method: a,
      bodyHeaders: p,
      retryCount: n
    });
    return {
      req: Object.assign(Object.assign(Object.assign(Object.assign(Object.assign({
        method: a,
        headers: w
      }, i.signal && { signal: i.signal }), globalThis.ReadableStream && y instanceof globalThis.ReadableStream && { duplex: "half" }), y && { body: y }), (s = this.fetchOptions) !== null && s !== void 0 ? s : {}), (o = i.fetchOptions) !== null && o !== void 0 ? o : {}),
      url: h,
      timeout: i.timeout
    };
  }
  async buildHeaders({ options: t, method: n, bodyHeaders: r, retryCount: s }) {
    let o = {};
    this.idempotencyHeader && n !== "get" && (t.idempotencyKey || (t.idempotencyKey = this.defaultIdempotencyKey()), o[this.idempotencyHeader] = t.idempotencyKey);
    const i = await this.authHeaders(t);
    let a = Pu([
      o,
      {
        Accept: "application/json",
        "User-Agent": this.getUserAgent()
      },
      this._options.defaultHeaders,
      r,
      t.headers,
      i
    ]);
    return this.validateHeaders(a), a.values;
  }
  _makeAbort(t) {
    return () => t.abort();
  }
  buildBody({ options: { body: t, headers: n } }) {
    if (!t) return {
      bodyHeaders: void 0,
      body: void 0
    };
    const r = Pu([n]);
    return ArrayBuffer.isView(t) || t instanceof ArrayBuffer || t instanceof DataView || typeof t == "string" && r.values.has("content-type") || globalThis.Blob && t instanceof globalThis.Blob || t instanceof FormData || t instanceof URLSearchParams || globalThis.ReadableStream && t instanceof globalThis.ReadableStream ? {
      bodyHeaders: void 0,
      body: t
    } : typeof t == "object" && (Symbol.asyncIterator in t || Symbol.iterator in t && "next" in t && typeof t.next == "function") ? {
      bodyHeaders: void 0,
      body: r5(t)
    } : typeof t == "object" && r.values.get("content-type") === "application/x-www-form-urlencoded" ? {
      bodyHeaders: { "content-type": "application/x-www-form-urlencoded" },
      body: this.stringifyQuery(t)
    } : this.encoder({
      body: t,
      headers: r
    });
  }
};
sR.DEFAULT_TIMEOUT = 6e4;
var pn = class extends sR {
  constructor() {
    super(...arguments), this.interactions = new ZP(this), this.webhooks = new eR(this);
  }
};
rR = pn;
pn.GeminiNextGenAPIClient = rR;
pn.GeminiNextGenAPIClientError = Yr;
pn.APIError = Xr;
pn.APIConnectionError = nm;
pn.APIConnectionTimeoutError = OP;
pn.APIUserAbortError = n_;
pn.NotFoundError = qP;
pn.ConflictError = HP;
pn.RateLimitError = WP;
pn.BadRequestError = FP;
pn.AuthenticationError = BP;
pn.InternalServerError = VP;
pn.PermissionDeniedError = UP;
pn.UnprocessableEntityError = GP;
pn.toFile = f5;
pn.Interactions = ZP;
pn.Webhooks = eR;
function A5(e, t) {
  const n = {}, r = c(e, ["name"]);
  return r != null && f(n, ["_url", "name"], r), n;
}
function I5(e, t) {
  const n = {}, r = c(e, ["name"]);
  return r != null && f(n, ["_url", "name"], r), n;
}
function x5(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  return r != null && f(n, ["sdkHttpResponse"], r), n;
}
function k5(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  return r != null && f(n, ["sdkHttpResponse"], r), n;
}
function M5(e, t, n) {
  const r = {};
  if (c(e, ["validationDataset"]) !== void 0) throw new Error("validationDataset parameter is not supported in Gemini API.");
  const s = c(e, ["tunedModelDisplayName"]);
  if (t !== void 0 && s != null && f(t, ["displayName"], s), c(e, ["description"]) !== void 0) throw new Error("description parameter is not supported in Gemini API.");
  const o = c(e, ["epochCount"]);
  t !== void 0 && o != null && f(t, [
    "tuningTask",
    "hyperparameters",
    "epochCount"
  ], o);
  const i = c(e, ["learningRateMultiplier"]);
  if (i != null && f(r, [
    "tuningTask",
    "hyperparameters",
    "learningRateMultiplier"
  ], i), c(e, ["exportLastCheckpointOnly"]) !== void 0) throw new Error("exportLastCheckpointOnly parameter is not supported in Gemini API.");
  if (c(e, ["preTunedModelCheckpointId"]) !== void 0) throw new Error("preTunedModelCheckpointId parameter is not supported in Gemini API.");
  if (c(e, ["adapterSize"]) !== void 0) throw new Error("adapterSize parameter is not supported in Gemini API.");
  if (c(e, ["tuningMode"]) !== void 0) throw new Error("tuningMode parameter is not supported in Gemini API.");
  if (c(e, ["customBaseModel"]) !== void 0) throw new Error("customBaseModel parameter is not supported in Gemini API.");
  const a = c(e, ["batchSize"]);
  t !== void 0 && a != null && f(t, [
    "tuningTask",
    "hyperparameters",
    "batchSize"
  ], a);
  const l = c(e, ["learningRate"]);
  if (t !== void 0 && l != null && f(t, [
    "tuningTask",
    "hyperparameters",
    "learningRate"
  ], l), c(e, ["labels"]) !== void 0) throw new Error("labels parameter is not supported in Gemini API.");
  if (c(e, ["beta"]) !== void 0) throw new Error("beta parameter is not supported in Gemini API.");
  if (c(e, ["baseTeacherModel"]) !== void 0) throw new Error("baseTeacherModel parameter is not supported in Gemini API.");
  if (c(e, ["tunedTeacherModelSource"]) !== void 0) throw new Error("tunedTeacherModelSource parameter is not supported in Gemini API.");
  if (c(e, ["sftLossWeightMultiplier"]) !== void 0) throw new Error("sftLossWeightMultiplier parameter is not supported in Gemini API.");
  if (c(e, ["outputUri"]) !== void 0) throw new Error("outputUri parameter is not supported in Gemini API.");
  if (c(e, ["encryptionSpec"]) !== void 0) throw new Error("encryptionSpec parameter is not supported in Gemini API.");
  return r;
}
function P5(e, t, n) {
  const r = {};
  let s = c(n, ["config", "method"]);
  if (s === void 0 && (s = "SUPERVISED_FINE_TUNING"), s === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["validationDataset"]);
    t !== void 0 && b != null && f(t, ["supervisedTuningSpec"], cv(b));
  } else if (s === "PREFERENCE_TUNING") {
    const b = c(e, ["validationDataset"]);
    t !== void 0 && b != null && f(t, ["preferenceOptimizationSpec"], cv(b));
  } else if (s === "DISTILLATION") {
    const b = c(e, ["validationDataset"]);
    t !== void 0 && b != null && f(t, ["distillationSpec"], cv(b));
  }
  const o = c(e, ["tunedModelDisplayName"]);
  t !== void 0 && o != null && f(t, ["tunedModelDisplayName"], o);
  const i = c(e, ["description"]);
  t !== void 0 && i != null && f(t, ["description"], i);
  let a = c(n, ["config", "method"]);
  if (a === void 0 && (a = "SUPERVISED_FINE_TUNING"), a === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["epochCount"]);
    t !== void 0 && b != null && f(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "epochCount"
    ], b);
  } else if (a === "PREFERENCE_TUNING") {
    const b = c(e, ["epochCount"]);
    t !== void 0 && b != null && f(t, [
      "preferenceOptimizationSpec",
      "hyperParameters",
      "epochCount"
    ], b);
  } else if (a === "DISTILLATION") {
    const b = c(e, ["epochCount"]);
    t !== void 0 && b != null && f(t, [
      "distillationSpec",
      "hyperParameters",
      "epochCount"
    ], b);
  }
  let l = c(n, ["config", "method"]);
  if (l === void 0 && (l = "SUPERVISED_FINE_TUNING"), l === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["learningRateMultiplier"]);
    t !== void 0 && b != null && f(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "learningRateMultiplier"
    ], b);
  } else if (l === "PREFERENCE_TUNING") {
    const b = c(e, ["learningRateMultiplier"]);
    t !== void 0 && b != null && f(t, [
      "preferenceOptimizationSpec",
      "hyperParameters",
      "learningRateMultiplier"
    ], b);
  } else if (l === "DISTILLATION") {
    const b = c(e, ["learningRateMultiplier"]);
    t !== void 0 && b != null && f(t, [
      "distillationSpec",
      "hyperParameters",
      "learningRateMultiplier"
    ], b);
  }
  let u = c(n, ["config", "method"]);
  if (u === void 0 && (u = "SUPERVISED_FINE_TUNING"), u === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["exportLastCheckpointOnly"]);
    t !== void 0 && b != null && f(t, ["supervisedTuningSpec", "exportLastCheckpointOnly"], b);
  } else if (u === "PREFERENCE_TUNING") {
    const b = c(e, ["exportLastCheckpointOnly"]);
    t !== void 0 && b != null && f(t, ["preferenceOptimizationSpec", "exportLastCheckpointOnly"], b);
  } else if (u === "DISTILLATION") {
    const b = c(e, ["exportLastCheckpointOnly"]);
    t !== void 0 && b != null && f(t, ["distillationSpec", "exportLastCheckpointOnly"], b);
  }
  let d = c(n, ["config", "method"]);
  if (d === void 0 && (d = "SUPERVISED_FINE_TUNING"), d === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["adapterSize"]);
    t !== void 0 && b != null && f(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "adapterSize"
    ], b);
  } else if (d === "PREFERENCE_TUNING") {
    const b = c(e, ["adapterSize"]);
    t !== void 0 && b != null && f(t, [
      "preferenceOptimizationSpec",
      "hyperParameters",
      "adapterSize"
    ], b);
  } else if (d === "DISTILLATION") {
    const b = c(e, ["adapterSize"]);
    t !== void 0 && b != null && f(t, [
      "distillationSpec",
      "hyperParameters",
      "adapterSize"
    ], b);
  }
  let h = c(n, ["config", "method"]);
  if (h === void 0 && (h = "SUPERVISED_FINE_TUNING"), h === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["tuningMode"]);
    t !== void 0 && b != null && f(t, ["supervisedTuningSpec", "tuningMode"], b);
  } else if (h === "DISTILLATION") {
    const b = c(e, ["tuningMode"]);
    t !== void 0 && b != null && f(t, ["distillationSpec", "tuningMode"], b);
  }
  const p = c(e, ["customBaseModel"]);
  t !== void 0 && p != null && f(t, ["customBaseModel"], p);
  let y = c(n, ["config", "method"]);
  if (y === void 0 && (y = "SUPERVISED_FINE_TUNING"), y === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["batchSize"]);
    t !== void 0 && b != null && f(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "batchSize"
    ], b);
  } else if (y === "DISTILLATION") {
    const b = c(e, ["batchSize"]);
    t !== void 0 && b != null && f(t, [
      "distillationSpec",
      "hyperParameters",
      "batchSize"
    ], b);
  }
  let w = c(n, ["config", "method"]);
  if (w === void 0 && (w = "SUPERVISED_FINE_TUNING"), w === "SUPERVISED_FINE_TUNING") {
    const b = c(e, ["learningRate"]);
    t !== void 0 && b != null && f(t, [
      "supervisedTuningSpec",
      "hyperParameters",
      "learningRate"
    ], b);
  } else if (w === "DISTILLATION") {
    const b = c(e, ["learningRate"]);
    t !== void 0 && b != null && f(t, [
      "distillationSpec",
      "hyperParameters",
      "learningRate"
    ], b);
  }
  const E = c(e, ["labels"]);
  t !== void 0 && E != null && f(t, ["labels"], E);
  const C = c(e, ["beta"]);
  t !== void 0 && C != null && f(t, [
    "preferenceOptimizationSpec",
    "hyperParameters",
    "beta"
  ], C);
  const M = c(e, ["baseTeacherModel"]);
  t !== void 0 && M != null && f(t, ["distillationSpec", "baseTeacherModel"], M);
  const k = c(e, ["tunedTeacherModelSource"]);
  t !== void 0 && k != null && f(t, ["distillationSpec", "tunedTeacherModelSource"], k);
  const R = c(e, ["sftLossWeightMultiplier"]);
  t !== void 0 && R != null && f(t, [
    "distillationSpec",
    "hyperParameters",
    "sftLossWeightMultiplier"
  ], R);
  const D = c(e, ["outputUri"]);
  t !== void 0 && D != null && f(t, ["outputUri"], D);
  const m = c(e, ["encryptionSpec"]);
  return t !== void 0 && m != null && f(t, ["encryptionSpec"], m), r;
}
function R5(e, t) {
  const n = {}, r = c(e, ["baseModel"]);
  r != null && f(n, ["baseModel"], r);
  const s = c(e, ["preTunedModel"]);
  s != null && f(n, ["preTunedModel"], s);
  const o = c(e, ["trainingDataset"]);
  o != null && G5(o);
  const i = c(e, ["config"]);
  return i != null && M5(i, n), n;
}
function N5(e, t) {
  const n = {}, r = c(e, ["baseModel"]);
  r != null && f(n, ["baseModel"], r);
  const s = c(e, ["preTunedModel"]);
  s != null && f(n, ["preTunedModel"], s);
  const o = c(e, ["trainingDataset"]);
  o != null && W5(o, n, t);
  const i = c(e, ["config"]);
  return i != null && P5(i, n, t), n;
}
function $5(e, t) {
  const n = {}, r = c(e, ["name"]);
  return r != null && f(n, ["_url", "name"], r), n;
}
function D5(e, t) {
  const n = {}, r = c(e, ["name"]);
  return r != null && f(n, ["_url", "name"], r), n;
}
function L5(e, t, n) {
  const r = {}, s = c(e, ["pageSize"]);
  t !== void 0 && s != null && f(t, ["_query", "pageSize"], s);
  const o = c(e, ["pageToken"]);
  t !== void 0 && o != null && f(t, ["_query", "pageToken"], o);
  const i = c(e, ["filter"]);
  return t !== void 0 && i != null && f(t, ["_query", "filter"], i), r;
}
function O5(e, t, n) {
  const r = {}, s = c(e, ["pageSize"]);
  t !== void 0 && s != null && f(t, ["_query", "pageSize"], s);
  const o = c(e, ["pageToken"]);
  t !== void 0 && o != null && f(t, ["_query", "pageToken"], o);
  const i = c(e, ["filter"]);
  return t !== void 0 && i != null && f(t, ["_query", "filter"], i), r;
}
function F5(e, t) {
  const n = {}, r = c(e, ["config"]);
  return r != null && L5(r, n), n;
}
function B5(e, t) {
  const n = {}, r = c(e, ["config"]);
  return r != null && O5(r, n), n;
}
function U5(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["nextPageToken"]);
  s != null && f(n, ["nextPageToken"], s);
  const o = c(e, ["tunedModels"]);
  if (o != null) {
    let i = o;
    Array.isArray(i) && (i = i.map((a) => iR(a))), f(n, ["tuningJobs"], i);
  }
  return n;
}
function q5(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["nextPageToken"]);
  s != null && f(n, ["nextPageToken"], s);
  const o = c(e, ["tuningJobs"]);
  if (o != null) {
    let i = o;
    Array.isArray(i) && (i = i.map((a) => o_(a))), f(n, ["tuningJobs"], i);
  }
  return n;
}
function H5(e, t) {
  const n = {}, r = c(e, ["name"]);
  r != null && f(n, ["model"], r);
  const s = c(e, ["name"]);
  return s != null && f(n, ["endpoint"], s), n;
}
function G5(e, t) {
  const n = {};
  if (c(e, ["gcsUri"]) !== void 0) throw new Error("gcsUri parameter is not supported in Gemini API.");
  if (c(e, ["vertexDatasetResource"]) !== void 0) throw new Error("vertexDatasetResource parameter is not supported in Gemini API.");
  const r = c(e, ["examples"]);
  if (r != null) {
    let s = r;
    Array.isArray(s) && (s = s.map((o) => o)), f(n, ["examples", "examples"], s);
  }
  return n;
}
function W5(e, t, n) {
  const r = {};
  let s = c(n, ["config", "method"]);
  if (s === void 0 && (s = "SUPERVISED_FINE_TUNING"), s === "SUPERVISED_FINE_TUNING") {
    const i = c(e, ["gcsUri"]);
    t !== void 0 && i != null && f(t, ["supervisedTuningSpec", "trainingDatasetUri"], i);
  } else if (s === "PREFERENCE_TUNING") {
    const i = c(e, ["gcsUri"]);
    t !== void 0 && i != null && f(t, ["preferenceOptimizationSpec", "trainingDatasetUri"], i);
  } else if (s === "DISTILLATION") {
    const i = c(e, ["gcsUri"]);
    t !== void 0 && i != null && f(t, ["distillationSpec", "promptDatasetUri"], i);
  }
  let o = c(n, ["config", "method"]);
  if (o === void 0 && (o = "SUPERVISED_FINE_TUNING"), o === "SUPERVISED_FINE_TUNING") {
    const i = c(e, ["vertexDatasetResource"]);
    t !== void 0 && i != null && f(t, ["supervisedTuningSpec", "trainingDatasetUri"], i);
  } else if (o === "PREFERENCE_TUNING") {
    const i = c(e, ["vertexDatasetResource"]);
    t !== void 0 && i != null && f(t, ["preferenceOptimizationSpec", "trainingDatasetUri"], i);
  } else if (o === "DISTILLATION") {
    const i = c(e, ["vertexDatasetResource"]);
    t !== void 0 && i != null && f(t, ["distillationSpec", "promptDatasetUri"], i);
  }
  if (c(e, ["examples"]) !== void 0) throw new Error("examples parameter is not supported in Vertex AI.");
  return r;
}
function iR(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["name"]);
  s != null && f(n, ["name"], s);
  const o = c(e, ["state"]);
  o != null && f(n, ["state"], mP(o));
  const i = c(e, ["createTime"]);
  i != null && f(n, ["createTime"], i);
  const a = c(e, ["tuningTask", "startTime"]);
  a != null && f(n, ["startTime"], a);
  const l = c(e, ["tuningTask", "completeTime"]);
  l != null && f(n, ["endTime"], l);
  const u = c(e, ["updateTime"]);
  u != null && f(n, ["updateTime"], u);
  const d = c(e, ["description"]);
  d != null && f(n, ["description"], d);
  const h = c(e, ["baseModel"]);
  h != null && f(n, ["baseModel"], h);
  const p = c(e, ["_self"]);
  return p != null && f(n, ["tunedModel"], H5(p)), n;
}
function o_(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["name"]);
  s != null && f(n, ["name"], s);
  const o = c(e, ["state"]);
  o != null && f(n, ["state"], mP(o));
  const i = c(e, ["createTime"]);
  i != null && f(n, ["createTime"], i);
  const a = c(e, ["startTime"]);
  a != null && f(n, ["startTime"], a);
  const l = c(e, ["endTime"]);
  l != null && f(n, ["endTime"], l);
  const u = c(e, ["updateTime"]);
  u != null && f(n, ["updateTime"], u);
  const d = c(e, ["error"]);
  d != null && f(n, ["error"], d);
  const h = c(e, ["description"]);
  h != null && f(n, ["description"], h);
  const p = c(e, ["baseModel"]);
  p != null && f(n, ["baseModel"], p);
  const y = c(e, ["tunedModel"]);
  y != null && f(n, ["tunedModel"], y);
  const w = c(e, ["preTunedModel"]);
  w != null && f(n, ["preTunedModel"], w);
  const E = c(e, ["supervisedTuningSpec"]);
  E != null && f(n, ["supervisedTuningSpec"], E);
  const C = c(e, ["preferenceOptimizationSpec"]);
  C != null && f(n, ["preferenceOptimizationSpec"], C);
  const M = c(e, ["distillationSpec"]);
  M != null && f(n, ["distillationSpec"], M);
  const k = c(e, ["tuningDataStats"]);
  k != null && f(n, ["tuningDataStats"], k);
  const R = c(e, ["encryptionSpec"]);
  R != null && f(n, ["encryptionSpec"], R);
  const D = c(e, ["partnerModelTuningSpec"]);
  D != null && f(n, ["partnerModelTuningSpec"], D);
  const m = c(e, ["customBaseModel"]);
  m != null && f(n, ["customBaseModel"], m);
  const b = c(e, ["evaluateDatasetRuns"]);
  if (b != null) {
    let Y = b;
    Array.isArray(Y) && (Y = Y.map((X) => X)), f(n, ["evaluateDatasetRuns"], Y);
  }
  const v = c(e, ["experiment"]);
  v != null && f(n, ["experiment"], v);
  const T = c(e, ["fullFineTuningSpec"]);
  T != null && f(n, ["fullFineTuningSpec"], T);
  const I = c(e, ["labels"]);
  I != null && f(n, ["labels"], I);
  const x = c(e, ["outputUri"]);
  x != null && f(n, ["outputUri"], x);
  const P = c(e, ["pipelineJob"]);
  P != null && f(n, ["pipelineJob"], P);
  const L = c(e, ["serviceAccount"]);
  L != null && f(n, ["serviceAccount"], L);
  const G = c(e, ["tunedModelDisplayName"]);
  G != null && f(n, ["tunedModelDisplayName"], G);
  const z = c(e, ["tuningJobState"]);
  z != null && f(n, ["tuningJobState"], z);
  const V = c(e, ["veoTuningSpec"]);
  V != null && f(n, ["veoTuningSpec"], V);
  const Z = c(e, ["distillationSamplingSpec"]);
  Z != null && f(n, ["distillationSamplingSpec"], Z);
  const O = c(e, ["tuningJobMetadata"]);
  return O != null && f(n, ["tuningJobMetadata"], O), n;
}
function V5(e, t) {
  const n = {}, r = c(e, ["sdkHttpResponse"]);
  r != null && f(n, ["sdkHttpResponse"], r);
  const s = c(e, ["name"]);
  s != null && f(n, ["name"], s);
  const o = c(e, ["metadata"]);
  o != null && f(n, ["metadata"], o);
  const i = c(e, ["done"]);
  i != null && f(n, ["done"], i);
  const a = c(e, ["error"]);
  return a != null && f(n, ["error"], a), n;
}
function cv(e, t) {
  const n = {}, r = c(e, ["gcsUri"]);
  r != null && f(n, ["validationDatasetUri"], r);
  const s = c(e, ["vertexDatasetResource"]);
  return s != null && f(n, ["validationDatasetUri"], s), n;
}
var K5 = class extends yo {
  constructor(e) {
    super(), this.apiClient = e, this.list = async (t = {}) => new ua(go.PAGED_ITEM_TUNING_JOBS, (n) => this.listInternal(n), await this.listInternal(t), t), this.get = async (t) => await this.getInternal(t), this.tune = async (t) => {
      var n;
      if (this.apiClient.isVertexAI()) if (t.baseModel.startsWith("projects/")) {
        const r = { tunedModelName: t.baseModel };
        !((n = t.config) === null || n === void 0) && n.preTunedModelCheckpointId && (r.checkpointId = t.config.preTunedModelCheckpointId);
        const s = Object.assign(Object.assign({}, t), { preTunedModel: r });
        return s.baseModel = void 0, await this.tuneInternal(s);
      } else {
        const r = Object.assign({}, t);
        return await this.tuneInternal(r);
      }
      else {
        const r = Object.assign({}, t), s = await this.tuneMldevInternal(r);
        let o = "";
        return s.metadata !== void 0 && s.metadata.tunedModel !== void 0 ? o = s.metadata.tunedModel : s.name !== void 0 && s.name.includes("/operations/") && (o = s.name.split("/operations/")[0]), {
          name: o,
          state: Vy.JOB_STATE_QUEUED
        };
      }
    };
  }
  async getInternal(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = D5(e);
      return i = Ue("{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => o_(u));
    } else {
      const l = $5(e);
      return i = Ue("{name}", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => iR(u));
    }
  }
  async listInternal(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = B5(e);
      return i = Ue("tuningJobs", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = q5(u), h = new uC();
        return Object.assign(h, d), h;
      });
    } else {
      const l = F5(e);
      return i = Ue("tunedModels", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "GET",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = U5(u), h = new uC();
        return Object.assign(h, d), h;
      });
    }
  }
  async cancel(e) {
    var t, n, r, s;
    let o, i = "", a = {};
    if (this.apiClient.isVertexAI()) {
      const l = I5(e);
      return i = Ue("{name}:cancel", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = k5(u), h = new cC();
        return Object.assign(h, d), h;
      });
    } else {
      const l = A5(e);
      return i = Ue("{name}:cancel", l._url), a = l._query, delete l._url, delete l._query, o = this.apiClient.request({
        path: i,
        queryParams: a,
        body: JSON.stringify(l),
        httpMethod: "POST",
        httpOptions: (r = e.config) === null || r === void 0 ? void 0 : r.httpOptions,
        abortSignal: (s = e.config) === null || s === void 0 ? void 0 : s.abortSignal
      }).then((u) => u.json().then((d) => {
        const h = d;
        return h.sdkHttpResponse = { headers: u.headers }, h;
      })), o.then((u) => {
        const d = x5(u), h = new cC();
        return Object.assign(h, d), h;
      });
    }
  }
  async tuneInternal(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) {
      const i = N5(e, e);
      return s = Ue("tuningJobs", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: a.headers }, u;
      })), r.then((a) => o_(a));
    } else throw new Error("This method is only supported by the Vertex AI.");
  }
  async tuneMldevInternal(e) {
    var t, n;
    let r, s = "", o = {};
    if (this.apiClient.isVertexAI()) throw new Error("This method is only supported by the Gemini Developer API.");
    {
      const i = R5(e);
      return s = Ue("tunedModels", i._url), o = i._query, delete i._url, delete i._query, r = this.apiClient.request({
        path: s,
        queryParams: o,
        body: JSON.stringify(i),
        httpMethod: "POST",
        httpOptions: (t = e.config) === null || t === void 0 ? void 0 : t.httpOptions,
        abortSignal: (n = e.config) === null || n === void 0 ? void 0 : n.abortSignal
      }).then((a) => a.json().then((l) => {
        const u = l;
        return u.sdkHttpResponse = { headers: a.headers }, u;
      })), r.then((a) => V5(a));
    }
  }
}, z5 = class {
  async download(e, t) {
    throw new Error("Download to file is not supported in the browser, please use a browser compliant download like an <a> tag.");
  }
}, J5 = 1024 * 1024 * 8, Y5 = 3, X5 = 1e3, Q5 = 2, cp = "x-goog-upload-status";
async function Z5(e, t, n, r) {
  var s;
  const o = await aR(e, t, n, r), i = await o?.json();
  if (((s = o?.headers) === null || s === void 0 ? void 0 : s[cp]) !== "final") throw new Error("Failed to upload file: Upload status is not finalized.");
  return i.file;
}
async function j5(e, t, n, r) {
  var s;
  const o = await aR(e, t, n, r), i = await o?.json();
  if (((s = o?.headers) === null || s === void 0 ? void 0 : s[cp]) !== "final") throw new Error("Failed to upload file: Upload status is not finalized.");
  const a = iP(i), l = new h4();
  return Object.assign(l, a), l;
}
async function aR(e, t, n, r) {
  var s, o, i;
  let a = t;
  const l = r?.baseUrl || ((s = n.clientOptions.httpOptions) === null || s === void 0 ? void 0 : s.baseUrl);
  if (l) {
    const y = new URL(l), w = new URL(t);
    w.protocol = y.protocol, w.host = y.host, w.port = y.port, a = w.toString();
  }
  let u = 0, d = 0, h = new zy(new Response()), p = "upload";
  for (u = e.size; d < u; ) {
    const y = Math.min(J5, u - d), w = e.slice(d, d + y);
    d + y >= u && (p += ", finalize");
    let E = 0, C = X5;
    for (; E < Y5; ) {
      const M = Object.assign(Object.assign({}, r?.headers || {}), {
        "X-Goog-Upload-Command": p,
        "X-Goog-Upload-Offset": String(d),
        "Content-Length": String(y)
      });
      if (h = await n.request({
        path: "",
        body: w,
        httpMethod: "POST",
        httpOptions: Object.assign(Object.assign({}, r), {
          apiVersion: "",
          baseUrl: a,
          headers: M
        })
      }), !((o = h?.headers) === null || o === void 0) && o[cp]) break;
      E++, await t6(C), C = C * Q5;
    }
    if (d += y, ((i = h?.headers) === null || i === void 0 ? void 0 : i[cp]) !== "active") break;
    if (u <= d) throw new Error("All content has been uploaded, but the upload status is not finalized.");
  }
  return h;
}
async function e6(e) {
  return {
    size: e.size,
    type: e.type
  };
}
function t6(e) {
  return new Promise((t) => setTimeout(t, e));
}
var n6 = class {
  async upload(e, t, n, r) {
    if (typeof e == "string") throw new Error("File path is not supported in browser uploader.");
    return await Z5(e, t, n, r);
  }
  async uploadToFileSearchStore(e, t, n, r) {
    if (typeof e == "string") throw new Error("File path is not supported in browser uploader.");
    return await j5(e, t, n, r);
  }
  async stat(e) {
    if (typeof e == "string") throw new Error("File path is not supported in browser uploader.");
    return await e6(e);
  }
}, r6 = class {
  create(e, t, n) {
    return new s6(e, t, n);
  }
}, s6 = class {
  constructor(e, t, n) {
    this.url = e, this.headers = t, this.callbacks = n;
  }
  connect() {
    this.ws = new WebSocket(this.url), this.ws.onopen = this.callbacks.onopen, this.ws.onerror = this.callbacks.onerror, this.ws.onclose = this.callbacks.onclose, this.ws.onmessage = this.callbacks.onmessage;
  }
  send(e) {
    if (this.ws === void 0) throw new Error("WebSocket is not connected");
    this.ws.send(e);
  }
  close() {
    if (this.ws === void 0) throw new Error("WebSocket is not connected");
    this.ws.close();
  }
}, BC = "x-goog-api-key", o6 = class {
  constructor(e) {
    this.apiKey = e;
  }
  async addAuthHeaders(e, t) {
    if (e.get(BC) === null) {
      if (this.apiKey.startsWith("auth_tokens/")) throw new Error("Ephemeral tokens are only supported by the live API.");
      if (!this.apiKey) throw new Error("API key is missing. Please provide a valid API key.");
      e.append(BC, this.apiKey);
    }
  }
}, i6 = "gl-node/", a6 = class {
  getNextGenClient() {
    var e;
    const t = this.httpOptions;
    if (this._nextGenClient === void 0) {
      const n = this.httpOptions;
      this._nextGenClient = new pn({
        baseURL: this.apiClient.getBaseUrl(),
        apiKey: this.apiKey,
        apiVersion: this.apiClient.getApiVersion(),
        clientAdapter: this.apiClient,
        defaultHeaders: this.apiClient.getDefaultHeaders(),
        timeout: n?.timeout,
        maxRetries: (e = n?.retryOptions) === null || e === void 0 ? void 0 : e.attempts
      });
    }
    return t?.extraBody && console.warn("GoogleGenAI.interactions: Client level httpOptions.extraBody is not supported by the interactions client and will be ignored."), this._nextGenClient;
  }
  get interactions() {
    return this._interactions !== void 0 ? this._interactions : (console.warn("GoogleGenAI.interactions: Interactions usage is experimental and may change in future versions."), this._interactions = this.getNextGenClient().interactions, this._interactions);
  }
  get webhooks() {
    return this._webhooks !== void 0 ? this._webhooks : (this._webhooks = this.getNextGenClient().webhooks, this._webhooks);
  }
  constructor(e) {
    var t;
    if (e.apiKey == null) throw new Error("An API Key must be set when running in a browser");
    if (e.project || e.location) throw new Error("Vertex AI project based authentication is not supported on browser runtimes. Please do not provide a project or location.");
    this.vertexai = (t = e.vertexai) !== null && t !== void 0 ? t : !1, this.apiKey = e.apiKey;
    const n = LW(e.httpOptions, e.vertexai, void 0, void 0);
    n && (e.httpOptions ? e.httpOptions.baseUrl = n : e.httpOptions = { baseUrl: n }), this.apiVersion = e.apiVersion, this.httpOptions = e.httpOptions;
    const r = new o6(this.apiKey);
    this.apiClient = new tJ({
      auth: r,
      apiVersion: this.apiVersion,
      apiKey: this.apiKey,
      vertexai: this.vertexai,
      httpOptions: this.httpOptions,
      userAgentExtra: i6 + "web",
      uploader: new n6(),
      downloader: new z5()
    }), this.models = new wJ(this.apiClient), this.live = new mJ(this.apiClient, r, new r6()), this.batches = new gV(this.apiClient), this.chats = new eK(this.models, this.apiClient), this.caches = new QV(this.apiClient), this.files = new fK(this.apiClient), this.operations = new SJ(this.apiClient), this.authTokens = new UJ(this.apiClient), this.tunings = new K5(this.apiClient), this.fileSearchStores = new JJ(this.apiClient);
  }
};
function UC(e) {
  try {
    return JSON.parse(e || "{}");
  } catch {
    return {};
  }
}
function gc(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function Wi(e) {
  return { text: String(e || "") };
}
function l6(e = "") {
  const t = String(e || "").match(/^data:([^;,]+);base64,(.+)$/);
  return t ? { inlineData: {
    mimeType: t[1],
    data: t[2]
  } } : null;
}
function u6(e) {
  if (typeof e == "string") return [Wi(e)];
  if (!Array.isArray(e)) return [Wi("")];
  const t = e.map((n) => !n || typeof n != "object" ? null : n.type === "text" ? Wi(n.text || "") : n.type === "image_url" && n.image_url?.url ? l6(n.image_url.url) : null).filter(Boolean);
  return t.length ? t : [Wi("")];
}
function qC() {
  return {
    role: "user",
    parts: [Wi("")]
  };
}
function fd(e, t = "model") {
  if (!e?.parts?.length) return null;
  const n = gc(e);
  return n ? (n.role || (n.role = t), n) : null;
}
function c6(e) {
  return !!e?.parts?.some((t) => typeof t?.thoughtSignature == "string" && t.thoughtSignature);
}
function d6(e) {
  return !!e?.parts?.some((t) => t?.functionCall?.name);
}
function dv(e, t) {
  return e?.functionCall?.name ? [
    String(e.functionCall.id || ""),
    String(e.functionCall.name || ""),
    JSON.stringify(e.functionCall.args || {}),
    String(t)
  ].join("\0") : "";
}
function f6(e = [], t = "") {
  const n = e.map((l) => fd(l, "model")).filter(Boolean);
  if (!n.length) return null;
  const r = [...n].reverse().find((l) => c6(l)) || null, s = [...n].reverse().find((l) => d6(l)) || null, o = gc(r || s || n[n.length - 1]);
  if (!o?.parts?.length) return n[n.length - 1];
  if (s) {
    const l = /* @__PURE__ */ new Map();
    n.forEach((d) => {
      d.parts.forEach((h, p) => {
        const y = dv(h, p);
        if (!y) return;
        const w = l.get(y);
        (!w || h.thoughtSignature || !w.thoughtSignature) && l.set(y, gc(h));
      });
    });
    const u = /* @__PURE__ */ new Set();
    o.parts = o.parts.map((d, h) => {
      const p = dv(d, h);
      return p ? (u.add(p), l.get(p) || d) : d;
    }), s.parts.forEach((d, h) => {
      const p = dv(d, h);
      !p || u.has(p) || (o.parts.push(l.get(p) || gc(d)), u.add(p));
    });
  }
  const i = String(t || ""), a = o.parts.filter((l) => !(typeof l?.text == "string" && !l?.thought));
  return o.parts = i ? [{ text: i }, ...a] : a, o.parts.length ? o : n[n.length - 1];
}
function HC(e) {
  const t = e?.candidates?.[0]?.content?.parts || [], n = t.filter((r) => !r?.thought && typeof r?.text == "string" && r.text).map((r) => r.text).join(`
`);
  return n || t.length ? n : typeof e?.text == "string" && e.text ? e.text : "";
}
function GC(e) {
  const t = Array.isArray(e?.functionCalls) ? e.functionCalls : [], n = (e?.candidates?.[0]?.content?.parts || []).map((r) => r?.functionCall || r).filter((r) => r && r.name);
  return (t.length ? t : n).map((r, s) => ({
    id: r.id || `google-tool-${s + 1}`,
    name: r.name || "",
    arguments: JSON.stringify(r.args || {})
  })).filter((r) => r.name);
}
function h6(e = [], t = []) {
  const n = Array.isArray(e) ? [...e] : [];
  return (Array.isArray(t) ? t : []).forEach((r) => {
    if (!r?.name) return;
    const s = [
      String(r.id || ""),
      String(r.name || ""),
      String(r.arguments || "")
    ].join("\0");
    n.some((o) => [
      String(o.id || ""),
      String(o.name || ""),
      String(o.arguments || "")
    ].join("\0") === s) || n.push(r);
  }), n;
}
function p6(e = []) {
  return {
    role: "user",
    parts: e.filter((t) => t && t.name).map((t) => ({ functionResponse: {
      name: t.name,
      response: t.response || {}
    } }))
  };
}
function m6(e) {
  switch (e) {
    case "high":
      return mc.HIGH;
    case "medium":
      return mc.MEDIUM;
    default:
      return mc.LOW;
  }
}
function WC(e) {
  return (e?.candidates?.[0]?.content?.parts || []).filter((t) => t?.thought && typeof t.text == "string" && t.text.trim()).map((t, n) => ({
    label: `思考块 ${n + 1}`,
    text: t.text.trim()
  }));
}
function g6(e) {
  const t = [String(e.systemPrompt || "").trim(), ...(e.messages || []).filter((n) => n.role === "system").map((n) => String(n.content || "").trim())].filter(Boolean);
  if (t.length)
    return [...new Set(t)].join(`

`);
}
function v6(e) {
  const t = e?.providerPayload?.googleContent;
  return fd(t, "model");
}
function y6(e) {
  const t = e?.providerPayload?.googleContents;
  if (!Array.isArray(t) || !t.length) {
    const n = v6(e);
    return n ? [n] : [];
  }
  return t.map((n) => fd(n, "model")).filter(Boolean);
}
function _w(e = []) {
  const t = (Array.isArray(e) ? e : []).map((n) => fd(n, "model")).filter(Boolean);
  if (t.length)
    return {
      googleContent: t[t.length - 1],
      googleContents: t
    };
}
function _6(e) {
  const t = e?.candidates?.[0]?.content;
  return _w(t ? [t] : []);
}
function b6(e) {
  return _w(e ? [e] : []);
}
function lR(e) {
  try {
    if (typeof e?.getHistory == "function") return e.getHistory(!1);
  } catch {
    return [];
  }
  return Array.isArray(e?.history) ? gc(e.history) || [] : [];
}
function w6(e, t = 0) {
  return lR(e).slice(Math.max(0, t)).filter((n) => n?.role === "model").map((n) => fd(n, "model")).filter(Boolean);
}
function S6(e) {
  const t = /* @__PURE__ */ new Map(), n = [], r = (e || []).filter((o) => o.role === "user" || o.role === "assistant" || o.role === "tool");
  r.forEach((o) => {
    (o.tool_calls || []).forEach((i) => {
      i.id && i.function?.name && t.set(i.id, i.function.name);
    });
  });
  for (let o = 0; o < r.length; o += 1) {
    const i = r[o];
    if (i.role === "tool") {
      const a = [];
      let l = o;
      for (; l < r.length && r[l].role === "tool"; ) {
        const u = r[l];
        a.push({ functionResponse: {
          name: String(u.toolName || u.tool_name || "").trim() || t.get(u.tool_call_id || "") || "tool_result",
          response: UC(u.content)
        } }), l += 1;
      }
      n.push({
        role: "user",
        parts: a
      }), o = l - 1;
      continue;
    }
    if (i.role === "assistant") {
      const a = y6(i);
      if (a.length) {
        n.push(...a);
        continue;
      }
    }
    if (i.role === "assistant" && Array.isArray(i.tool_calls) && i.tool_calls.length) {
      n.push({
        role: "model",
        parts: [...i.content ? [Wi(i.content)] : [], ...i.tool_calls.map((a) => ({ functionCall: {
          name: a.function.name,
          args: UC(a.function.arguments)
        } }))]
      });
      continue;
    }
    n.push({
      role: i.role === "assistant" ? "model" : "user",
      parts: u6(i.content)
    });
  }
  if (!n.length) return {
    history: [],
    latestMessage: qC().parts
  };
  const s = n[n.length - 1];
  return s.role === "user" && s.parts?.length ? {
    history: n.slice(0, -1),
    latestMessage: s.parts
  } : {
    history: n,
    latestMessage: qC().parts
  };
}
function T6(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function VC(e, t) {
  const n = String(t || ""), r = String(e || "");
  return n ? !r || n.startsWith(r) ? n : r.endsWith(n) ? r : `${r}${n}` : r;
}
var E6 = class {
  constructor(e) {
    this.config = e, this.supportsSessionToolLoop = !0, this.activeChat = null, this.client = new a6({
      apiKey: e.apiKey,
      httpOptions: {
        baseUrl: String(e.baseUrl || "https://generativelanguage.googleapis.com/v1beta").replace(/\/$/, ""),
        timeout: Number(e.timeoutMs) || 900 * 1e3
      }
    });
  }
  buildChatPayload(e) {
    const t = S6(e.messages), n = Array.isArray(e.tools) ? e.tools : [], r = g6(e), s = {
      ...r ? { systemInstruction: r } : {},
      temperature: e.temperature,
      ...e.maxTokens ? { maxOutputTokens: e.maxTokens } : {}
    };
    return e.reasoning?.enabled && (s.thinkingConfig = {
      includeThoughts: !0,
      thinkingLevel: m6(e.reasoning.effort)
    }), n.length && (s.tools = [{ functionDeclarations: n.map((o) => ({
      name: o.function.name,
      description: o.function.description,
      parameters: o.function.parameters
    })) }]), n.length && e.toolChoice && e.toolChoice !== "auto" && e.toolChoice !== "none" && (s.toolConfig = { functionCallingConfig: { mode: Wy.ANY } }), {
      createPayload: {
        model: this.config.model,
        history: t.history,
        config: s
      },
      sendPayload: { message: t.latestMessage }
    };
  }
  inspectRequest(e, t = {}) {
    const n = t.payload || this.buildChatPayload(e), r = String(this.config.baseUrl || "https://generativelanguage.googleapis.com/v1beta").replace(/\/$/, "");
    return Wc({
      provider: "google",
      model: this.config.model,
      transport: "google-genai-sdk",
      url: `${r}/models/${encodeURIComponent(this.config.model || "")}:generateContent`,
      headers: {
        "Content-Type": "application/json",
        "x-goog-api-key": this.config.apiKey || ""
      },
      body: {
        chatCreate: n.createPayload,
        sendMessage: n.sendPayload,
        stream: typeof e.onStreamProgress == "function"
      },
      sdk: typeof e.onStreamProgress == "function" ? "client.chats.create(...).sendMessageStream" : "client.chats.create(...).sendMessage"
    });
  }
  inspectSendRequest(e, t) {
    const n = String(this.config.baseUrl || "https://generativelanguage.googleapis.com/v1beta").replace(/\/$/, "");
    return Wc({
      provider: "google",
      model: this.config.model,
      transport: "google-genai-sdk",
      url: `${n}/models/${encodeURIComponent(this.config.model || "")}:generateContent`,
      headers: {
        "Content-Type": "application/json",
        "x-goog-api-key": this.config.apiKey || ""
      },
      body: {
        sendMessage: e,
        stream: typeof t.onStreamProgress == "function"
      },
      sdk: typeof t.onStreamProgress == "function" ? "activeChat.sendMessageStream" : "activeChat.sendMessage"
    });
  }
  createChat(e) {
    const t = this.buildChatPayload(e);
    return {
      chat: this.client.chats.create(t.createPayload),
      sendPayload: t.sendPayload,
      requestInspection: this.inspectRequest(e, { payload: t })
    };
  }
  async sendThroughChat(e, t, n) {
    let r, s, o, i = [], a = null;
    const l = { ...t }, u = typeof n.onStreamProgress == "function", d = lR(e).length;
    if (u) {
      const w = await e.sendMessageStream(l), E = /* @__PURE__ */ new Map();
      let C = "", M = [], k = null;
      const R = [];
      for await (const D of w) {
        k = D;
        const m = D?.candidates?.[0]?.content;
        m?.parts?.length && R.push(m), WC(D).forEach((v, T) => {
          const I = `${v.label}:${T}`;
          E.set(I, VC(E.get(I) || "", v.text));
        }), M = (D.functionCalls || []).map((v, T) => ({
          id: v.id || `google-tool-${T + 1}`,
          name: v.name || "",
          arguments: JSON.stringify(v.args || {})
        })).filter((v) => v.name), i = h6(i, M.length ? M : GC(D));
        const b = HC(D);
        C = VC(C, b), T6(n, {
          text: C,
          thoughts: Array.from(E.values()).filter(Boolean).map((v, T) => ({
            label: `思考块 ${T + 1}`,
            text: v
          })),
          ...M.length ? {
            toolCalls: M,
            toolCallDraft: !0
          } : {}
        });
      }
      r = k || { functionCalls: M }, a = f6(R, C) || r?.candidates?.[0]?.content || null, s = Array.from(E.values()).filter(Boolean).map((D, m) => ({
        label: `思考块 ${m + 1}`,
        text: D
      })), o = C;
    } else
      r = await e.sendMessage(l), s = WC(r), o = HC(r);
    const h = GC(r), p = h.length ? h : i, y = w6(e, d);
    return {
      text: o,
      toolCalls: p,
      thoughts: s,
      finishReason: r.candidates?.[0]?.finishReason || "STOP",
      model: r.modelVersion || this.config.model,
      provider: "google",
      providerPayload: _w(y) || b6(a) || _6(r)
    };
  }
  async chat(e) {
    if (Array.isArray(e.toolResponses) && e.toolResponses.length) {
      if (!this.activeChat) throw new Error("google_chat_session_missing");
      const r = { message: p6(e.toolResponses) };
      return {
        ...await this.sendThroughChat(this.activeChat, r, e),
        requestInspection: this.inspectSendRequest(r, e)
      };
    }
    const t = String(e.finalAnswerReminderText || "").trim();
    if (t) {
      if (!this.activeChat) throw new Error("google_chat_session_missing");
      const r = { message: [Wi(t)] };
      return {
        ...await this.sendThroughChat(this.activeChat, r, e),
        requestInspection: this.inspectSendRequest(r, e)
      };
    }
    const n = this.createChat(e);
    return this.activeChat = n.chat, {
      ...await this.sendThroughChat(this.activeChat, n.sendPayload, e),
      requestInspection: n.requestInspection
    };
  }
};
function ht(e, t, n, r, s) {
  if (r === "m") throw new TypeError("Private method is not writable");
  if (r === "a" && !s) throw new TypeError("Private accessor was defined without a setter");
  if (typeof t == "function" ? e !== t || !s : !t.has(e)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return r === "a" ? s.call(e, n) : s ? s.value = n : t.set(e, n), n;
}
function de(e, t, n, r) {
  if (n === "a" && !r) throw new TypeError("Private accessor was defined without a getter");
  if (typeof t == "function" ? e !== t || !r : !t.has(e)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return n === "m" ? r : n === "a" ? r.call(e) : r ? r.value : t.get(e);
}
var uR = function() {
  const { crypto: e } = globalThis;
  if (e?.randomUUID)
    return uR = e.randomUUID.bind(e), e.randomUUID();
  const t = new Uint8Array(1), n = e ? () => e.getRandomValues(t)[0] : () => Math.random() * 255 & 255;
  return "10000000-1000-4000-8000-100000000000".replace(/[018]/g, (r) => (+r ^ n() & 15 >> +r / 4).toString(16));
};
function i_(e) {
  return typeof e == "object" && e !== null && ("name" in e && e.name === "AbortError" || "message" in e && String(e.message).includes("FetchRequestCanceledException"));
}
var a_ = (e) => {
  if (e instanceof Error) return e;
  if (typeof e == "object" && e !== null) {
    try {
      if (Object.prototype.toString.call(e) === "[object Error]") {
        const t = new Error(e.message, e.cause ? { cause: e.cause } : {});
        return e.stack && (t.stack = e.stack), e.cause && !t.cause && (t.cause = e.cause), e.name && (t.name = e.name), t;
      }
    } catch {
    }
    try {
      return new Error(JSON.stringify(e));
    } catch {
    }
  }
  return new Error(e);
}, st = class extends Error {
}, Ln = class l_ extends st {
  constructor(t, n, r, s) {
    super(`${l_.makeMessage(t, n, r)}`), this.status = t, this.headers = s, this.requestID = s?.get("x-request-id"), this.error = n;
    const o = n;
    this.code = o?.code, this.param = o?.param, this.type = o?.type;
  }
  static makeMessage(t, n, r) {
    const s = n?.message ? typeof n.message == "string" ? n.message : JSON.stringify(n.message) : n ? JSON.stringify(n) : r;
    return t && s ? `${t} ${s}` : t ? `${t} status code (no body)` : s || "(no status code or body)";
  }
  static generate(t, n, r, s) {
    if (!t || !s) return new sm({
      message: r,
      cause: a_(n)
    });
    const o = n?.error;
    return t === 400 ? new cR(t, o, r, s) : t === 401 ? new dR(t, o, r, s) : t === 403 ? new fR(t, o, r, s) : t === 404 ? new hR(t, o, r, s) : t === 409 ? new pR(t, o, r, s) : t === 422 ? new mR(t, o, r, s) : t === 429 ? new gR(t, o, r, s) : t >= 500 ? new vR(t, o, r, s) : new l_(t, o, r, s);
  }
}, Kr = class extends Ln {
  constructor({ message: e } = {}) {
    super(void 0, void 0, e || "Request was aborted.", void 0);
  }
}, sm = class extends Ln {
  constructor({ message: e, cause: t }) {
    super(void 0, void 0, e || "Connection error.", void 0), t && (this.cause = t);
  }
}, bw = class extends sm {
  constructor({ message: e } = {}) {
    super({ message: e ?? "Request timed out." });
  }
}, cR = class extends Ln {
}, dR = class extends Ln {
}, fR = class extends Ln {
}, hR = class extends Ln {
}, pR = class extends Ln {
}, mR = class extends Ln {
}, gR = class extends Ln {
}, vR = class extends Ln {
}, yR = class extends st {
  constructor() {
    super("Could not parse response content as the length limit was reached");
  }
}, _R = class extends st {
  constructor() {
    super("Could not parse response content as the request was rejected by the content filter");
  }
}, Ju = class extends Error {
  constructor(e) {
    super(e);
  }
}, bR = class extends Ln {
  constructor(e, t, n) {
    let r = "OAuth2 authentication error", s;
    if (t && typeof t == "object") {
      const o = t;
      s = o.error;
      const i = o.error_description;
      i && typeof i == "string" ? r = i : s && (r = s);
    }
    super(e, t, r, n), this.error_code = s;
  }
}, C6 = class extends st {
  constructor(e, t, n) {
    super(e), this.provider = t, this.cause = n;
  }
}, A6 = /^[a-z][a-z0-9+.-]*:/i, I6 = (e) => A6.test(e), tr = (e) => (tr = Array.isArray, tr(e)), KC = tr;
function wR(e) {
  return typeof e != "object" ? {} : e ?? {};
}
function zC(e) {
  if (!e) return !0;
  for (const t in e) return !1;
  return !0;
}
function x6(e, t) {
  return Object.prototype.hasOwnProperty.call(e, t);
}
function fv(e) {
  return e != null && typeof e == "object" && !Array.isArray(e);
}
var k6 = (e, t) => {
  if (typeof t != "number" || !Number.isInteger(t)) throw new st(`${e} must be an integer`);
  if (t < 0) throw new st(`${e} must be a positive integer`);
  return t;
}, M6 = (e) => {
  try {
    return JSON.parse(e);
  } catch {
    return;
  }
}, hd = (e) => new Promise((t) => setTimeout(t, e)), Ka = "6.34.0", P6 = () => typeof window < "u" && typeof window.document < "u" && typeof navigator < "u";
function R6() {
  return typeof Deno < "u" && Deno.build != null ? "deno" : typeof EdgeRuntime < "u" ? "edge" : Object.prototype.toString.call(typeof globalThis.process < "u" ? globalThis.process : 0) === "[object process]" ? "node" : "unknown";
}
var N6 = () => {
  const e = R6();
  if (e === "deno") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Ka,
    "X-Stainless-OS": YC(Deno.build.os),
    "X-Stainless-Arch": JC(Deno.build.arch),
    "X-Stainless-Runtime": "deno",
    "X-Stainless-Runtime-Version": typeof Deno.version == "string" ? Deno.version : Deno.version?.deno ?? "unknown"
  };
  if (typeof EdgeRuntime < "u") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Ka,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": `other:${EdgeRuntime}`,
    "X-Stainless-Runtime": "edge",
    "X-Stainless-Runtime-Version": globalThis.process.version
  };
  if (e === "node") return {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Ka,
    "X-Stainless-OS": YC(globalThis.process.platform ?? "unknown"),
    "X-Stainless-Arch": JC(globalThis.process.arch ?? "unknown"),
    "X-Stainless-Runtime": "node",
    "X-Stainless-Runtime-Version": globalThis.process.version ?? "unknown"
  };
  const t = $6();
  return t ? {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Ka,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": "unknown",
    "X-Stainless-Runtime": `browser:${t.browser}`,
    "X-Stainless-Runtime-Version": t.version
  } : {
    "X-Stainless-Lang": "js",
    "X-Stainless-Package-Version": Ka,
    "X-Stainless-OS": "Unknown",
    "X-Stainless-Arch": "unknown",
    "X-Stainless-Runtime": "unknown",
    "X-Stainless-Runtime-Version": "unknown"
  };
};
function $6() {
  if (typeof navigator > "u" || !navigator) return null;
  for (const { key: e, pattern: t } of [
    {
      key: "edge",
      pattern: /Edge(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "ie",
      pattern: /MSIE(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "ie",
      pattern: /Trident(?:.*rv\:(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "chrome",
      pattern: /Chrome(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "firefox",
      pattern: /Firefox(?:\W+(\d+)\.(\d+)(?:\.(\d+))?)?/
    },
    {
      key: "safari",
      pattern: /(?:Version\W+(\d+)\.(\d+)(?:\.(\d+))?)?(?:\W+Mobile\S*)?\W+Safari/
    }
  ]) {
    const n = t.exec(navigator.userAgent);
    if (n) return {
      browser: e,
      version: `${n[1] || 0}.${n[2] || 0}.${n[3] || 0}`
    };
  }
  return null;
}
var JC = (e) => e === "x32" ? "x32" : e === "x86_64" || e === "x64" ? "x64" : e === "arm" ? "arm" : e === "aarch64" || e === "arm64" ? "arm64" : e ? `other:${e}` : "unknown", YC = (e) => (e = e.toLowerCase(), e.includes("ios") ? "iOS" : e === "android" ? "Android" : e === "darwin" ? "MacOS" : e === "win32" ? "Windows" : e === "freebsd" ? "FreeBSD" : e === "openbsd" ? "OpenBSD" : e === "linux" ? "Linux" : e ? `Other:${e}` : "Unknown"), XC, D6 = () => XC ?? (XC = N6());
function SR() {
  if (typeof fetch < "u") return fetch;
  throw new Error("`fetch` is not defined as a global; Either pass `fetch` to the client, `new OpenAI({ fetch })` or polyfill the global, `globalThis.fetch = fetch`");
}
function TR(...e) {
  const t = globalThis.ReadableStream;
  if (typeof t > "u") throw new Error("`ReadableStream` is not defined as a global; You will need to polyfill it, `globalThis.ReadableStream = ReadableStream`");
  return new t(...e);
}
function ER(e) {
  let t = Symbol.asyncIterator in e ? e[Symbol.asyncIterator]() : e[Symbol.iterator]();
  return TR({
    start() {
    },
    async pull(n) {
      const { done: r, value: s } = await t.next();
      r ? n.close() : n.enqueue(s);
    },
    async cancel() {
      await t.return?.();
    }
  });
}
function CR(e) {
  if (e[Symbol.asyncIterator]) return e;
  const t = e.getReader();
  return {
    async next() {
      try {
        const n = await t.read();
        return n?.done && t.releaseLock(), n;
      } catch (n) {
        throw t.releaseLock(), n;
      }
    },
    async return() {
      const n = t.cancel();
      return t.releaseLock(), await n, {
        done: !0,
        value: void 0
      };
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
async function QC(e) {
  if (e === null || typeof e != "object") return;
  if (e[Symbol.asyncIterator]) {
    await e[Symbol.asyncIterator]().return?.();
    return;
  }
  const t = e.getReader(), n = t.cancel();
  t.releaseLock(), await n;
}
var L6 = ({ headers: e, body: t }) => ({
  bodyHeaders: { "content-type": "application/json" },
  body: JSON.stringify(t)
}), AR = "RFC3986", IR = (e) => String(e), ZC = {
  RFC1738: (e) => String(e).replace(/%20/g, "+"),
  RFC3986: IR
};
var u_ = (e, t) => (u_ = Object.hasOwn ?? Function.prototype.call.bind(Object.prototype.hasOwnProperty), u_(e, t)), bs = /* @__PURE__ */ (() => {
  const e = [];
  for (let t = 0; t < 256; ++t) e.push("%" + ((t < 16 ? "0" : "") + t.toString(16)).toUpperCase());
  return e;
})(), hv = 1024, O6 = (e, t, n, r, s) => {
  if (e.length === 0) return e;
  let o = e;
  if (typeof e == "symbol" ? o = Symbol.prototype.toString.call(e) : typeof e != "string" && (o = String(e)), n === "iso-8859-1") return escape(o).replace(/%u[0-9a-f]{4}/gi, function(a) {
    return "%26%23" + parseInt(a.slice(2), 16) + "%3B";
  });
  let i = "";
  for (let a = 0; a < o.length; a += hv) {
    const l = o.length >= hv ? o.slice(a, a + hv) : o, u = [];
    for (let d = 0; d < l.length; ++d) {
      let h = l.charCodeAt(d);
      if (h === 45 || h === 46 || h === 95 || h === 126 || h >= 48 && h <= 57 || h >= 65 && h <= 90 || h >= 97 && h <= 122 || s === "RFC1738" && (h === 40 || h === 41)) {
        u[u.length] = l.charAt(d);
        continue;
      }
      if (h < 128) {
        u[u.length] = bs[h];
        continue;
      }
      if (h < 2048) {
        u[u.length] = bs[192 | h >> 6] + bs[128 | h & 63];
        continue;
      }
      if (h < 55296 || h >= 57344) {
        u[u.length] = bs[224 | h >> 12] + bs[128 | h >> 6 & 63] + bs[128 | h & 63];
        continue;
      }
      d += 1, h = 65536 + ((h & 1023) << 10 | l.charCodeAt(d) & 1023), u[u.length] = bs[240 | h >> 18] + bs[128 | h >> 12 & 63] + bs[128 | h >> 6 & 63] + bs[128 | h & 63];
    }
    i += u.join("");
  }
  return i;
};
function F6(e) {
  return !e || typeof e != "object" ? !1 : !!(e.constructor && e.constructor.isBuffer && e.constructor.isBuffer(e));
}
function jC(e, t) {
  if (tr(e)) {
    const n = [];
    for (let r = 0; r < e.length; r += 1) n.push(t(e[r]));
    return n;
  }
  return t(e);
}
var xR = {
  brackets(e) {
    return String(e) + "[]";
  },
  comma: "comma",
  indices(e, t) {
    return String(e) + "[" + t + "]";
  },
  repeat(e) {
    return String(e);
  }
}, kR = function(e, t) {
  Array.prototype.push.apply(e, tr(t) ? t : [t]);
}, eA, dn = {
  addQueryPrefix: !1,
  allowDots: !1,
  allowEmptyArrays: !1,
  arrayFormat: "indices",
  charset: "utf-8",
  charsetSentinel: !1,
  delimiter: "&",
  encode: !0,
  encodeDotInKeys: !1,
  encoder: O6,
  encodeValuesOnly: !1,
  format: AR,
  formatter: IR,
  indices: !1,
  serializeDate(e) {
    return (eA ?? (eA = Function.prototype.call.bind(Date.prototype.toISOString)))(e);
  },
  skipNulls: !1,
  strictNullHandling: !1
};
function B6(e) {
  return typeof e == "string" || typeof e == "number" || typeof e == "boolean" || typeof e == "symbol" || typeof e == "bigint";
}
var pv = {};
function MR(e, t, n, r, s, o, i, a, l, u, d, h, p, y, w, E, C, M) {
  let k = e, R = M, D = 0, m = !1;
  for (; (R = R.get(pv)) !== void 0 && !m; ) {
    const x = R.get(e);
    if (D += 1, typeof x < "u") {
      if (x === D) throw new RangeError("Cyclic object value");
      m = !0;
    }
    typeof R.get(pv) > "u" && (D = 0);
  }
  if (typeof u == "function" ? k = u(t, k) : k instanceof Date ? k = p?.(k) : n === "comma" && tr(k) && (k = jC(k, function(x) {
    return x instanceof Date ? p?.(x) : x;
  })), k === null) {
    if (o) return l && !E ? l(t, dn.encoder, C, "key", y) : t;
    k = "";
  }
  if (B6(k) || F6(k)) {
    if (l) {
      const x = E ? t : l(t, dn.encoder, C, "key", y);
      return [w?.(x) + "=" + w?.(l(k, dn.encoder, C, "value", y))];
    }
    return [w?.(t) + "=" + w?.(String(k))];
  }
  const b = [];
  if (typeof k > "u") return b;
  let v;
  if (n === "comma" && tr(k))
    E && l && (k = jC(k, l)), v = [{ value: k.length > 0 ? k.join(",") || null : void 0 }];
  else if (tr(u)) v = u;
  else {
    const x = Object.keys(k);
    v = d ? x.sort(d) : x;
  }
  const T = a ? String(t).replace(/\./g, "%2E") : String(t), I = r && tr(k) && k.length === 1 ? T + "[]" : T;
  if (s && tr(k) && k.length === 0) return I + "[]";
  for (let x = 0; x < v.length; ++x) {
    const P = v[x], L = typeof P == "object" && typeof P.value < "u" ? P.value : k[P];
    if (i && L === null) continue;
    const G = h && a ? P.replace(/\./g, "%2E") : P, z = tr(k) ? typeof n == "function" ? n(I, G) : I : I + (h ? "." + G : "[" + G + "]");
    M.set(e, D);
    const V = /* @__PURE__ */ new WeakMap();
    V.set(pv, M), kR(b, MR(L, z, n, r, s, o, i, a, n === "comma" && E && tr(k) ? null : l, u, d, h, p, y, w, E, C, V));
  }
  return b;
}
function U6(e = dn) {
  if (typeof e.allowEmptyArrays < "u" && typeof e.allowEmptyArrays != "boolean") throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
  if (typeof e.encodeDotInKeys < "u" && typeof e.encodeDotInKeys != "boolean") throw new TypeError("`encodeDotInKeys` option can only be `true` or `false`, when provided");
  if (e.encoder !== null && typeof e.encoder < "u" && typeof e.encoder != "function") throw new TypeError("Encoder has to be a function.");
  const t = e.charset || dn.charset;
  if (typeof e.charset < "u" && e.charset !== "utf-8" && e.charset !== "iso-8859-1") throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
  let n = AR;
  if (typeof e.format < "u") {
    if (!u_(ZC, e.format)) throw new TypeError("Unknown format option provided.");
    n = e.format;
  }
  const r = ZC[n];
  let s = dn.filter;
  (typeof e.filter == "function" || tr(e.filter)) && (s = e.filter);
  let o;
  if (e.arrayFormat && e.arrayFormat in xR ? o = e.arrayFormat : "indices" in e ? o = e.indices ? "indices" : "repeat" : o = dn.arrayFormat, "commaRoundTrip" in e && typeof e.commaRoundTrip != "boolean") throw new TypeError("`commaRoundTrip` must be a boolean, or absent");
  const i = typeof e.allowDots > "u" ? e.encodeDotInKeys ? !0 : dn.allowDots : !!e.allowDots;
  return {
    addQueryPrefix: typeof e.addQueryPrefix == "boolean" ? e.addQueryPrefix : dn.addQueryPrefix,
    allowDots: i,
    allowEmptyArrays: typeof e.allowEmptyArrays == "boolean" ? !!e.allowEmptyArrays : dn.allowEmptyArrays,
    arrayFormat: o,
    charset: t,
    charsetSentinel: typeof e.charsetSentinel == "boolean" ? e.charsetSentinel : dn.charsetSentinel,
    commaRoundTrip: !!e.commaRoundTrip,
    delimiter: typeof e.delimiter > "u" ? dn.delimiter : e.delimiter,
    encode: typeof e.encode == "boolean" ? e.encode : dn.encode,
    encodeDotInKeys: typeof e.encodeDotInKeys == "boolean" ? e.encodeDotInKeys : dn.encodeDotInKeys,
    encoder: typeof e.encoder == "function" ? e.encoder : dn.encoder,
    encodeValuesOnly: typeof e.encodeValuesOnly == "boolean" ? e.encodeValuesOnly : dn.encodeValuesOnly,
    filter: s,
    format: n,
    formatter: r,
    serializeDate: typeof e.serializeDate == "function" ? e.serializeDate : dn.serializeDate,
    skipNulls: typeof e.skipNulls == "boolean" ? e.skipNulls : dn.skipNulls,
    sort: typeof e.sort == "function" ? e.sort : null,
    strictNullHandling: typeof e.strictNullHandling == "boolean" ? e.strictNullHandling : dn.strictNullHandling
  };
}
function q6(e, t = {}) {
  let n = e;
  const r = U6(t);
  let s, o;
  typeof r.filter == "function" ? (o = r.filter, n = o("", n)) : tr(r.filter) && (o = r.filter, s = o);
  const i = [];
  if (typeof n != "object" || n === null) return "";
  const a = xR[r.arrayFormat], l = a === "comma" && r.commaRoundTrip;
  s || (s = Object.keys(n)), r.sort && s.sort(r.sort);
  const u = /* @__PURE__ */ new WeakMap();
  for (let p = 0; p < s.length; ++p) {
    const y = s[p];
    r.skipNulls && n[y] === null || kR(i, MR(n[y], y, a, l, r.allowEmptyArrays, r.strictNullHandling, r.skipNulls, r.encodeDotInKeys, r.encode ? r.encoder : null, r.filter, r.sort, r.allowDots, r.serializeDate, r.format, r.formatter, r.encodeValuesOnly, r.charset, u));
  }
  const d = i.join(r.delimiter);
  let h = r.addQueryPrefix === !0 ? "?" : "";
  return r.charsetSentinel && (r.charset === "iso-8859-1" ? h += "utf8=%26%2310003%3B&" : h += "utf8=%E2%9C%93&"), d.length > 0 ? h + d : "";
}
function H6(e) {
  return q6(e, { arrayFormat: "brackets" });
}
function G6(e) {
  let t = 0;
  for (const s of e) t += s.length;
  const n = new Uint8Array(t);
  let r = 0;
  for (const s of e)
    n.set(s, r), r += s.length;
  return n;
}
var tA;
function ww(e) {
  let t;
  return (tA ?? (t = new globalThis.TextEncoder(), tA = t.encode.bind(t)))(e);
}
var nA;
function rA(e) {
  let t;
  return (nA ?? (t = new globalThis.TextDecoder(), nA = t.decode.bind(t)))(e);
}
var wr, Sr, om = class {
  constructor() {
    wr.set(this, void 0), Sr.set(this, void 0), ht(this, wr, new Uint8Array(), "f"), ht(this, Sr, null, "f");
  }
  decode(e) {
    if (e == null) return [];
    const t = e instanceof ArrayBuffer ? new Uint8Array(e) : typeof e == "string" ? ww(e) : e;
    ht(this, wr, G6([de(this, wr, "f"), t]), "f");
    const n = [];
    let r;
    for (; (r = W6(de(this, wr, "f"), de(this, Sr, "f"))) != null; ) {
      if (r.carriage && de(this, Sr, "f") == null) {
        ht(this, Sr, r.index, "f");
        continue;
      }
      if (de(this, Sr, "f") != null && (r.index !== de(this, Sr, "f") + 1 || r.carriage)) {
        n.push(rA(de(this, wr, "f").subarray(0, de(this, Sr, "f") - 1))), ht(this, wr, de(this, wr, "f").subarray(de(this, Sr, "f")), "f"), ht(this, Sr, null, "f");
        continue;
      }
      const s = de(this, Sr, "f") !== null ? r.preceding - 1 : r.preceding, o = rA(de(this, wr, "f").subarray(0, s));
      n.push(o), ht(this, wr, de(this, wr, "f").subarray(r.index), "f"), ht(this, Sr, null, "f");
    }
    return n;
  }
  flush() {
    return de(this, wr, "f").length ? this.decode(`
`) : [];
  }
};
wr = /* @__PURE__ */ new WeakMap(), Sr = /* @__PURE__ */ new WeakMap();
om.NEWLINE_CHARS = /* @__PURE__ */ new Set([`
`, "\r"]);
om.NEWLINE_REGEXP = /\r\n|[\n\r]/g;
function W6(e, t) {
  for (let s = t ?? 0; s < e.length; s++) {
    if (e[s] === 10) return {
      preceding: s,
      index: s + 1,
      carriage: !1
    };
    if (e[s] === 13) return {
      preceding: s,
      index: s + 1,
      carriage: !0
    };
  }
  return null;
}
function V6(e) {
  for (let r = 0; r < e.length - 1; r++) {
    if (e[r] === 10 && e[r + 1] === 10 || e[r] === 13 && e[r + 1] === 13) return r + 2;
    if (e[r] === 13 && e[r + 1] === 10 && r + 3 < e.length && e[r + 2] === 13 && e[r + 3] === 10) return r + 4;
  }
  return -1;
}
var dp = {
  off: 0,
  error: 200,
  warn: 300,
  info: 400,
  debug: 500
}, sA = (e, t, n) => {
  if (e) {
    if (x6(dp, e)) return e;
    Cn(n).warn(`${t} was set to ${JSON.stringify(e)}, expected one of ${JSON.stringify(Object.keys(dp))}`);
  }
};
function Yu() {
}
function Pf(e, t, n) {
  return !t || dp[e] > dp[n] ? Yu : t[e].bind(t);
}
var K6 = {
  error: Yu,
  warn: Yu,
  info: Yu,
  debug: Yu
}, oA = /* @__PURE__ */ new WeakMap();
function Cn(e) {
  const t = e.logger, n = e.logLevel ?? "off";
  if (!t) return K6;
  const r = oA.get(t);
  if (r && r[0] === n) return r[1];
  const s = {
    error: Pf("error", t, n),
    warn: Pf("warn", t, n),
    info: Pf("info", t, n),
    debug: Pf("debug", t, n)
  };
  return oA.set(t, [n, s]), s;
}
var Ni = (e) => (e.options && (e.options = { ...e.options }, delete e.options.headers), e.headers && (e.headers = Object.fromEntries((e.headers instanceof Headers ? [...e.headers] : Object.entries(e.headers)).map(([t, n]) => [t, t.toLowerCase() === "authorization" || t.toLowerCase() === "cookie" || t.toLowerCase() === "set-cookie" ? "***" : n]))), "retryOfRequestLogID" in e && (e.retryOfRequestLogID && (e.retryOf = e.retryOfRequestLogID), delete e.retryOfRequestLogID), e), Ru, Vc = class Xu {
  constructor(t, n, r) {
    this.iterator = t, Ru.set(this, void 0), this.controller = n, ht(this, Ru, r, "f");
  }
  static fromSSEResponse(t, n, r, s) {
    let o = !1;
    const i = r ? Cn(r) : console;
    async function* a() {
      if (o) throw new st("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
      o = !0;
      let l = !1;
      try {
        for await (const u of z6(t, n))
          if (!l) {
            if (u.data.startsWith("[DONE]")) {
              l = !0;
              continue;
            }
            if (u.event === null || !u.event.startsWith("thread.")) {
              let d;
              try {
                d = JSON.parse(u.data);
              } catch (h) {
                throw i.error("Could not parse message into JSON:", u.data), i.error("From chunk:", u.raw), h;
              }
              if (d && d.error) throw new Ln(void 0, d.error, void 0, t.headers);
              yield s ? {
                event: u.event,
                data: d
              } : d;
            } else {
              let d;
              try {
                d = JSON.parse(u.data);
              } catch (h) {
                throw console.error("Could not parse message into JSON:", u.data), console.error("From chunk:", u.raw), h;
              }
              if (u.event == "error") throw new Ln(void 0, d.error, d.message, void 0);
              yield {
                event: u.event,
                data: d
              };
            }
          }
        l = !0;
      } catch (u) {
        if (i_(u)) return;
        throw u;
      } finally {
        l || n.abort();
      }
    }
    return new Xu(a, n, r);
  }
  static fromReadableStream(t, n, r) {
    let s = !1;
    async function* o() {
      const a = new om(), l = CR(t);
      for await (const u of l) for (const d of a.decode(u)) yield d;
      for (const u of a.flush()) yield u;
    }
    async function* i() {
      if (s) throw new st("Cannot iterate over a consumed stream, use `.tee()` to split the stream.");
      s = !0;
      let a = !1;
      try {
        for await (const l of o())
          a || l && (yield JSON.parse(l));
        a = !0;
      } catch (l) {
        if (i_(l)) return;
        throw l;
      } finally {
        a || n.abort();
      }
    }
    return new Xu(i, n, r);
  }
  [(Ru = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
    return this.iterator();
  }
  tee() {
    const t = [], n = [], r = this.iterator(), s = (o) => ({ next: () => {
      if (o.length === 0) {
        const i = r.next();
        t.push(i), n.push(i);
      }
      return o.shift();
    } });
    return [new Xu(() => s(t), this.controller, de(this, Ru, "f")), new Xu(() => s(n), this.controller, de(this, Ru, "f"))];
  }
  toReadableStream() {
    const t = this;
    let n;
    return TR({
      async start() {
        n = t[Symbol.asyncIterator]();
      },
      async pull(r) {
        try {
          const { value: s, done: o } = await n.next();
          if (o) return r.close();
          const i = ww(JSON.stringify(s) + `
`);
          r.enqueue(i);
        } catch (s) {
          r.error(s);
        }
      },
      async cancel() {
        await n.return?.();
      }
    });
  }
};
async function* z6(e, t) {
  if (!e.body)
    throw t.abort(), typeof globalThis.navigator < "u" && globalThis.navigator.product === "ReactNative" ? new st("The default react-native fetch implementation does not support streaming. Please use expo/fetch: https://docs.expo.dev/versions/latest/sdk/expo/#expofetch-api") : new st("Attempted to iterate over a response with no body");
  const n = new Y6(), r = new om(), s = CR(e.body);
  for await (const o of J6(s)) for (const i of r.decode(o)) {
    const a = n.decode(i);
    a && (yield a);
  }
  for (const o of r.flush()) {
    const i = n.decode(o);
    i && (yield i);
  }
}
async function* J6(e) {
  let t = new Uint8Array();
  for await (const n of e) {
    if (n == null) continue;
    const r = n instanceof ArrayBuffer ? new Uint8Array(n) : typeof n == "string" ? ww(n) : n;
    let s = new Uint8Array(t.length + r.length);
    s.set(t), s.set(r, t.length), t = s;
    let o;
    for (; (o = V6(t)) !== -1; )
      yield t.slice(0, o), t = t.slice(o);
  }
  t.length > 0 && (yield t);
}
var Y6 = class {
  constructor() {
    this.event = null, this.data = [], this.chunks = [];
  }
  decode(e) {
    if (e.endsWith("\r") && (e = e.substring(0, e.length - 1)), !e) {
      if (!this.event && !this.data.length) return null;
      const s = {
        event: this.event,
        data: this.data.join(`
`),
        raw: this.chunks
      };
      return this.event = null, this.data = [], this.chunks = [], s;
    }
    if (this.chunks.push(e), e.startsWith(":")) return null;
    let [t, n, r] = X6(e, ":");
    return r.startsWith(" ") && (r = r.substring(1)), t === "event" ? this.event = r : t === "data" && this.data.push(r), null;
  }
};
function X6(e, t) {
  const n = e.indexOf(t);
  return n !== -1 ? [
    e.substring(0, n),
    t,
    e.substring(n + t.length)
  ] : [
    e,
    "",
    ""
  ];
}
async function PR(e, t) {
  const { response: n, requestLogID: r, retryOfRequestLogID: s, startTime: o } = t, i = await (async () => {
    if (t.options.stream)
      return Cn(e).debug("response", n.status, n.url, n.headers, n.body), t.options.__streamClass ? t.options.__streamClass.fromSSEResponse(n, t.controller, e, t.options.__synthesizeEventData) : Vc.fromSSEResponse(n, t.controller, e, t.options.__synthesizeEventData);
    if (n.status === 204) return null;
    if (t.options.__binaryResponse) return n;
    const a = n.headers.get("content-type")?.split(";")[0]?.trim();
    return a?.includes("application/json") || a?.endsWith("+json") ? n.headers.get("content-length") === "0" ? void 0 : RR(await n.json(), n) : await n.text();
  })();
  return Cn(e).debug(`[${r}] response parsed`, Ni({
    retryOfRequestLogID: s,
    url: n.url,
    status: n.status,
    body: i,
    durationMs: Date.now() - o
  })), i;
}
function RR(e, t) {
  return !e || typeof e != "object" || Array.isArray(e) ? e : Object.defineProperty(e, "_request_id", {
    value: t.headers.get("x-request-id"),
    enumerable: !1
  });
}
var Qu, NR = class $R extends Promise {
  constructor(t, n, r = PR) {
    super((s) => {
      s(null);
    }), this.responsePromise = n, this.parseResponse = r, Qu.set(this, void 0), ht(this, Qu, t, "f");
  }
  _thenUnwrap(t) {
    return new $R(de(this, Qu, "f"), this.responsePromise, async (n, r) => RR(t(await this.parseResponse(n, r), r), r.response));
  }
  asResponse() {
    return this.responsePromise.then((t) => t.response);
  }
  async withResponse() {
    const [t, n] = await Promise.all([this.parse(), this.asResponse()]);
    return {
      data: t,
      response: n,
      request_id: n.headers.get("x-request-id")
    };
  }
  parse() {
    return this.parsedPromise || (this.parsedPromise = this.responsePromise.then((t) => this.parseResponse(de(this, Qu, "f"), t))), this.parsedPromise;
  }
  then(t, n) {
    return this.parse().then(t, n);
  }
  catch(t) {
    return this.parse().catch(t);
  }
  finally(t) {
    return this.parse().finally(t);
  }
};
Qu = /* @__PURE__ */ new WeakMap();
var Rf, Sw = class {
  constructor(e, t, n, r) {
    Rf.set(this, void 0), ht(this, Rf, e, "f"), this.options = r, this.response = t, this.body = n;
  }
  hasNextPage() {
    return this.getPaginatedItems().length ? this.nextPageRequestOptions() != null : !1;
  }
  async getNextPage() {
    const e = this.nextPageRequestOptions();
    if (!e) throw new st("No next page expected; please check `.hasNextPage()` before calling `.getNextPage()`.");
    return await de(this, Rf, "f").requestAPIList(this.constructor, e);
  }
  async *iterPages() {
    let e = this;
    for (yield e; e.hasNextPage(); )
      e = await e.getNextPage(), yield e;
  }
  async *[(Rf = /* @__PURE__ */ new WeakMap(), Symbol.asyncIterator)]() {
    for await (const e of this.iterPages()) for (const t of e.getPaginatedItems()) yield t;
  }
}, Q6 = class extends NR {
  constructor(e, t, n) {
    super(e, t, async (r, s) => new n(r, s.response, await PR(r, s), s.options));
  }
  async *[Symbol.asyncIterator]() {
    const e = await this;
    for await (const t of e) yield t;
  }
}, im = class extends Sw {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.object = n.object;
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  nextPageRequestOptions() {
    return null;
  }
}, nn = class extends Sw {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.has_more = n.has_more || !1;
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  hasNextPage() {
    return this.has_more === !1 ? !1 : super.hasNextPage();
  }
  nextPageRequestOptions() {
    const e = this.getPaginatedItems(), t = e[e.length - 1]?.id;
    return t ? {
      ...this.options,
      query: {
        ...wR(this.options.query),
        after: t
      }
    } : null;
  }
}, Kc = class extends Sw {
  constructor(e, t, n, r) {
    super(e, t, n, r), this.data = n.data || [], this.has_more = n.has_more || !1, this.last_id = n.last_id || "";
  }
  getPaginatedItems() {
    return this.data ?? [];
  }
  hasNextPage() {
    return this.has_more === !1 ? !1 : super.hasNextPage();
  }
  nextPageRequestOptions() {
    const e = this.last_id;
    return e ? {
      ...this.options,
      query: {
        ...wR(this.options.query),
        after: e
      }
    } : null;
  }
}, Z6 = {
  jwt: "urn:ietf:params:oauth:token-type:jwt",
  id: "urn:ietf:params:oauth:token-type:id_token"
}, j6 = "urn:ietf:params:oauth:grant-type:token-exchange", e8 = class {
  constructor(e, t) {
    this.cachedToken = null, this.refreshPromise = null, this.tokenExchangeUrl = "https://auth.openai.com/oauth/token", this.config = e, this.fetch = t ?? SR();
  }
  async getToken() {
    if (!this.cachedToken || this.isTokenExpired(this.cachedToken)) {
      if (this.refreshPromise) return await this.refreshPromise;
      this.refreshPromise = this.refreshToken();
      try {
        return await this.refreshPromise;
      } finally {
        this.refreshPromise = null;
      }
    }
    return this.needsRefresh(this.cachedToken) && !this.refreshPromise && (this.refreshPromise = this.refreshToken().finally(() => {
      this.refreshPromise = null;
    })), this.cachedToken.token;
  }
  async refreshToken() {
    const e = await this.config.provider.getToken(), t = await this.fetch(this.tokenExchangeUrl, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        grant_type: j6,
        client_id: this.config.clientId,
        subject_token: e,
        subject_token_type: Z6[this.config.provider.tokenType],
        identity_provider_id: this.config.identityProviderId,
        service_account_id: this.config.serviceAccountId
      })
    });
    if (!t.ok) {
      const o = await t.text();
      let i;
      try {
        i = JSON.parse(o);
      } catch {
      }
      throw t.status === 400 || t.status === 401 || t.status === 403 ? new bR(t.status, i, t.headers) : Ln.generate(t.status, i, `Token exchange failed with status ${t.status}`, t.headers);
    }
    const n = await t.json(), r = n.expires_in || 3600, s = Date.now() + r * 1e3;
    return this.cachedToken = {
      token: n.access_token,
      expiresAt: s
    }, n.access_token;
  }
  isTokenExpired(e) {
    return Date.now() >= e.expiresAt;
  }
  needsRefresh(e) {
    const t = (this.config.refreshBufferSeconds ?? 1200) * 1e3;
    return Date.now() >= e.expiresAt - t;
  }
  invalidateToken() {
    this.cachedToken = null, this.refreshPromise = null;
  }
}, DR = () => {
  if (typeof File > "u") {
    const { process: e } = globalThis, t = typeof e?.versions?.node == "string" && parseInt(e.versions.node.split(".")) < 20;
    throw new Error("`File` is not defined as a global, which is required for file uploads." + (t ? " Update to Node 20 LTS or newer, or set `globalThis.File` to `import('node:buffer').File`." : ""));
  }
};
function vc(e, t, n) {
  return DR(), new File(e, t ?? "unknown_file", n);
}
function ih(e) {
  return (typeof e == "object" && e !== null && ("name" in e && e.name && String(e.name) || "url" in e && e.url && String(e.url) || "filename" in e && e.filename && String(e.filename) || "path" in e && e.path && String(e.path)) || "").split(/[\\/]/).pop() || void 0;
}
var Tw = (e) => e != null && typeof e == "object" && typeof e[Symbol.asyncIterator] == "function", am = async (e, t) => c_(e.body) ? {
  ...e,
  body: await LR(e.body, t)
} : e, Ps = async (e, t) => ({
  ...e,
  body: await LR(e.body, t)
}), iA = /* @__PURE__ */ new WeakMap();
function t8(e) {
  const t = typeof e == "function" ? e : e.fetch, n = iA.get(t);
  if (n) return n;
  const r = (async () => {
    try {
      const s = "Response" in t ? t.Response : (await t("data:,")).constructor, o = new FormData();
      return o.toString() !== await new s(o).text();
    } catch {
      return !0;
    }
  })();
  return iA.set(t, r), r;
}
var LR = async (e, t) => {
  if (!await t8(t)) throw new TypeError("The provided fetch function does not support file uploads with the current global FormData class.");
  const n = new FormData();
  return await Promise.all(Object.entries(e || {}).map(([r, s]) => d_(n, r, s))), n;
}, OR = (e) => e instanceof Blob && "name" in e, n8 = (e) => typeof e == "object" && e !== null && (e instanceof Response || Tw(e) || OR(e)), c_ = (e) => {
  if (n8(e)) return !0;
  if (Array.isArray(e)) return e.some(c_);
  if (e && typeof e == "object") {
    for (const t in e) if (c_(e[t])) return !0;
  }
  return !1;
}, d_ = async (e, t, n) => {
  if (n !== void 0) {
    if (n == null) throw new TypeError(`Received null for "${t}"; to pass null in FormData, you must use the string 'null'`);
    if (typeof n == "string" || typeof n == "number" || typeof n == "boolean") e.append(t, String(n));
    else if (n instanceof Response) e.append(t, vc([await n.blob()], ih(n)));
    else if (Tw(n)) e.append(t, vc([await new Response(ER(n)).blob()], ih(n)));
    else if (OR(n)) e.append(t, n, ih(n));
    else if (Array.isArray(n)) await Promise.all(n.map((r) => d_(e, t + "[]", r)));
    else if (typeof n == "object") await Promise.all(Object.entries(n).map(([r, s]) => d_(e, `${t}[${r}]`, s)));
    else throw new TypeError(`Invalid value given to form, expected a string, number, boolean, object, Array, File or Blob but got ${n} instead`);
  }
}, FR = (e) => e != null && typeof e == "object" && typeof e.size == "number" && typeof e.type == "string" && typeof e.text == "function" && typeof e.slice == "function" && typeof e.arrayBuffer == "function", r8 = (e) => e != null && typeof e == "object" && typeof e.name == "string" && typeof e.lastModified == "number" && FR(e), s8 = (e) => e != null && typeof e == "object" && typeof e.url == "string" && typeof e.blob == "function";
async function o8(e, t, n) {
  if (DR(), e = await e, r8(e))
    return e instanceof File ? e : vc([await e.arrayBuffer()], e.name);
  if (s8(e)) {
    const s = await e.blob();
    return t || (t = new URL(e.url).pathname.split(/[\\/]/).pop()), vc(await f_(s), t, n);
  }
  const r = await f_(e);
  if (t || (t = ih(e)), !n?.type) {
    const s = r.find((o) => typeof o == "object" && "type" in o && o.type);
    typeof s == "string" && (n = {
      ...n,
      type: s
    });
  }
  return vc(r, t, n);
}
async function f_(e) {
  let t = [];
  if (typeof e == "string" || ArrayBuffer.isView(e) || e instanceof ArrayBuffer) t.push(e);
  else if (FR(e)) t.push(e instanceof Blob ? e : await e.arrayBuffer());
  else if (Tw(e)) for await (const n of e) t.push(...await f_(n));
  else {
    const n = e?.constructor?.name;
    throw new Error(`Unexpected data type: ${typeof e}${n ? `; constructor: ${n}` : ""}${i8(e)}`);
  }
  return t;
}
function i8(e) {
  return typeof e != "object" || e === null ? "" : `; props: [${Object.getOwnPropertyNames(e).map((t) => `"${t}"`).join(", ")}]`;
}
var et = class {
  constructor(e) {
    this._client = e;
  }
};
function BR(e) {
  return e.replace(/[^A-Za-z0-9\-._~!$&'()*+,;=:@]+/g, encodeURIComponent);
}
var aA = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.create(null)), a8 = (e = BR) => function(n, ...r) {
  if (n.length === 1) return n[0];
  let s = !1;
  const o = [], i = n.reduce((d, h, p) => {
    /[?#]/.test(h) && (s = !0);
    const y = r[p];
    let w = (s ? encodeURIComponent : e)("" + y);
    return p !== r.length && (y == null || typeof y == "object" && y.toString === Object.getPrototypeOf(Object.getPrototypeOf(y.hasOwnProperty ?? aA) ?? aA)?.toString) && (w = y + "", o.push({
      start: d.length + h.length,
      length: w.length,
      error: `Value of type ${Object.prototype.toString.call(y).slice(8, -1)} is not a valid path parameter`
    })), d + h + (p === r.length ? "" : w);
  }, ""), a = i.split(/[?#]/, 1)[0], l = /(?<=^|\/)(?:\.|%2e){1,2}(?=\/|$)/gi;
  let u;
  for (; (u = l.exec(a)) !== null; ) o.push({
    start: u.index,
    length: u[0].length,
    error: `Value "${u[0]}" can't be safely passed as a path parameter`
  });
  if (o.sort((d, h) => d.start - h.start), o.length > 0) {
    let d = 0;
    const h = o.reduce((p, y) => {
      const w = " ".repeat(y.start - d), E = "^".repeat(y.length);
      return d = y.start + y.length, p + w + E;
    }, "");
    throw new st(`Path parameters result in path with invalid segments:
${o.map((p) => p.error).join(`
`)}
${i}
${h}`);
  }
  return i;
}, Ae = /* @__PURE__ */ a8(BR), UR = class extends et {
  list(e, t = {}, n) {
    return this._client.getAPIList(Ae`/chat/completions/${e}/messages`, nn, {
      query: t,
      ...n
    });
  }
};
function fp(e) {
  return e !== void 0 && "function" in e && e.function !== void 0;
}
function Ew(e) {
  return e?.$brand === "auto-parseable-response-format";
}
function pd(e) {
  return e?.$brand === "auto-parseable-tool";
}
function l8(e, t) {
  return !t || !qR(t) ? {
    ...e,
    choices: e.choices.map((n) => (HR(n.message.tool_calls), {
      ...n,
      message: {
        ...n.message,
        parsed: null,
        ...n.message.tool_calls ? { tool_calls: n.message.tool_calls } : void 0
      }
    }))
  } : Cw(e, t);
}
function Cw(e, t) {
  const n = e.choices.map((r) => {
    if (r.finish_reason === "length") throw new yR();
    if (r.finish_reason === "content_filter") throw new _R();
    return HR(r.message.tool_calls), {
      ...r,
      message: {
        ...r.message,
        ...r.message.tool_calls ? { tool_calls: r.message.tool_calls?.map((s) => c8(t, s)) ?? void 0 } : void 0,
        parsed: r.message.content && !r.message.refusal ? u8(t, r.message.content) : null
      }
    };
  });
  return {
    ...e,
    choices: n
  };
}
function u8(e, t) {
  return e.response_format?.type !== "json_schema" ? null : e.response_format?.type === "json_schema" ? "$parseRaw" in e.response_format ? e.response_format.$parseRaw(t) : JSON.parse(t) : null;
}
function c8(e, t) {
  const n = e.tools?.find((r) => fp(r) && r.function?.name === t.function.name);
  return {
    ...t,
    function: {
      ...t.function,
      parsed_arguments: pd(n) ? n.$parseRaw(t.function.arguments) : n?.function.strict ? JSON.parse(t.function.arguments) : null
    }
  };
}
function d8(e, t) {
  if (!e || !("tools" in e) || !e.tools) return !1;
  const n = e.tools?.find((r) => fp(r) && r.function?.name === t.function.name);
  return fp(n) && (pd(n) || n?.function.strict || !1);
}
function qR(e) {
  return Ew(e.response_format) ? !0 : e.tools?.some((t) => pd(t) || t.type === "function" && t.function.strict === !0) ?? !1;
}
function HR(e) {
  for (const t of e || []) if (t.type !== "function") throw new st(`Currently only \`function\` tool calls are supported; Received \`${t.type}\``);
}
function f8(e) {
  for (const t of e ?? []) {
    if (t.type !== "function") throw new st(`Currently only \`function\` tool types support auto-parsing; Received \`${t.type}\``);
    if (t.function.strict !== !0) throw new st(`The \`${t.function.name}\` tool is not marked with \`strict: true\`. Only strict function tools can be auto-parsed`);
  }
}
var hp = (e) => e?.role === "assistant", GR = (e) => e?.role === "tool", h_, ah, lh, Zu, ju, uh, ec, zs, tc, pp, mp, za, WR, Aw = class {
  constructor() {
    h_.add(this), this.controller = new AbortController(), ah.set(this, void 0), lh.set(this, () => {
    }), Zu.set(this, () => {
    }), ju.set(this, void 0), uh.set(this, () => {
    }), ec.set(this, () => {
    }), zs.set(this, {}), tc.set(this, !1), pp.set(this, !1), mp.set(this, !1), za.set(this, !1), ht(this, ah, new Promise((e, t) => {
      ht(this, lh, e, "f"), ht(this, Zu, t, "f");
    }), "f"), ht(this, ju, new Promise((e, t) => {
      ht(this, uh, e, "f"), ht(this, ec, t, "f");
    }), "f"), de(this, ah, "f").catch(() => {
    }), de(this, ju, "f").catch(() => {
    });
  }
  _run(e) {
    setTimeout(() => {
      e().then(() => {
        this._emitFinal(), this._emit("end");
      }, de(this, h_, "m", WR).bind(this));
    }, 0);
  }
  _connected() {
    this.ended || (de(this, lh, "f").call(this), this._emit("connect"));
  }
  get ended() {
    return de(this, tc, "f");
  }
  get errored() {
    return de(this, pp, "f");
  }
  get aborted() {
    return de(this, mp, "f");
  }
  abort() {
    this.controller.abort();
  }
  on(e, t) {
    return (de(this, zs, "f")[e] || (de(this, zs, "f")[e] = [])).push({ listener: t }), this;
  }
  off(e, t) {
    const n = de(this, zs, "f")[e];
    if (!n) return this;
    const r = n.findIndex((s) => s.listener === t);
    return r >= 0 && n.splice(r, 1), this;
  }
  once(e, t) {
    return (de(this, zs, "f")[e] || (de(this, zs, "f")[e] = [])).push({
      listener: t,
      once: !0
    }), this;
  }
  emitted(e) {
    return new Promise((t, n) => {
      ht(this, za, !0, "f"), e !== "error" && this.once("error", n), this.once(e, t);
    });
  }
  async done() {
    ht(this, za, !0, "f"), await de(this, ju, "f");
  }
  _emit(e, ...t) {
    if (de(this, tc, "f")) return;
    e === "end" && (ht(this, tc, !0, "f"), de(this, uh, "f").call(this));
    const n = de(this, zs, "f")[e];
    if (n && (de(this, zs, "f")[e] = n.filter((r) => !r.once), n.forEach(({ listener: r }) => r(...t))), e === "abort") {
      const r = t[0];
      !de(this, za, "f") && !n?.length && Promise.reject(r), de(this, Zu, "f").call(this, r), de(this, ec, "f").call(this, r), this._emit("end");
      return;
    }
    if (e === "error") {
      const r = t[0];
      !de(this, za, "f") && !n?.length && Promise.reject(r), de(this, Zu, "f").call(this, r), de(this, ec, "f").call(this, r), this._emit("end");
    }
  }
  _emitFinal() {
  }
};
ah = /* @__PURE__ */ new WeakMap(), lh = /* @__PURE__ */ new WeakMap(), Zu = /* @__PURE__ */ new WeakMap(), ju = /* @__PURE__ */ new WeakMap(), uh = /* @__PURE__ */ new WeakMap(), ec = /* @__PURE__ */ new WeakMap(), zs = /* @__PURE__ */ new WeakMap(), tc = /* @__PURE__ */ new WeakMap(), pp = /* @__PURE__ */ new WeakMap(), mp = /* @__PURE__ */ new WeakMap(), za = /* @__PURE__ */ new WeakMap(), h_ = /* @__PURE__ */ new WeakSet(), WR = function(t) {
  if (ht(this, pp, !0, "f"), t instanceof Error && t.name === "AbortError" && (t = new Kr()), t instanceof Kr)
    return ht(this, mp, !0, "f"), this._emit("abort", t);
  if (t instanceof st) return this._emit("error", t);
  if (t instanceof Error) {
    const n = new st(t.message);
    return n.cause = t, this._emit("error", n);
  }
  return this._emit("error", new st(String(t)));
};
function h8(e) {
  return typeof e.parse == "function";
}
var Hn, p_, gp, m_, g_, v_, VR, KR, p8 = 10, zR = class extends Aw {
  constructor() {
    super(...arguments), Hn.add(this), this._chatCompletions = [], this.messages = [];
  }
  _addChatCompletion(e) {
    this._chatCompletions.push(e), this._emit("chatCompletion", e);
    const t = e.choices[0]?.message;
    return t && this._addMessage(t), e;
  }
  _addMessage(e, t = !0) {
    if ("content" in e || (e.content = null), this.messages.push(e), t) {
      if (this._emit("message", e), GR(e) && e.content) this._emit("functionToolCallResult", e.content);
      else if (hp(e) && e.tool_calls)
        for (const n of e.tool_calls) n.type === "function" && this._emit("functionToolCall", n.function);
    }
  }
  async finalChatCompletion() {
    await this.done();
    const e = this._chatCompletions[this._chatCompletions.length - 1];
    if (!e) throw new st("stream ended without producing a ChatCompletion");
    return e;
  }
  async finalContent() {
    return await this.done(), de(this, Hn, "m", p_).call(this);
  }
  async finalMessage() {
    return await this.done(), de(this, Hn, "m", gp).call(this);
  }
  async finalFunctionToolCall() {
    return await this.done(), de(this, Hn, "m", m_).call(this);
  }
  async finalFunctionToolCallResult() {
    return await this.done(), de(this, Hn, "m", g_).call(this);
  }
  async totalUsage() {
    return await this.done(), de(this, Hn, "m", v_).call(this);
  }
  allChatCompletions() {
    return [...this._chatCompletions];
  }
  _emitFinal() {
    const e = this._chatCompletions[this._chatCompletions.length - 1];
    e && this._emit("finalChatCompletion", e);
    const t = de(this, Hn, "m", gp).call(this);
    t && this._emit("finalMessage", t);
    const n = de(this, Hn, "m", p_).call(this);
    n && this._emit("finalContent", n);
    const r = de(this, Hn, "m", m_).call(this);
    r && this._emit("finalFunctionToolCall", r);
    const s = de(this, Hn, "m", g_).call(this);
    s != null && this._emit("finalFunctionToolCallResult", s), this._chatCompletions.some((o) => o.usage) && this._emit("totalUsage", de(this, Hn, "m", v_).call(this));
  }
  async _createChatCompletion(e, t, n) {
    const r = n?.signal;
    r && (r.aborted && this.controller.abort(), r.addEventListener("abort", () => this.controller.abort())), de(this, Hn, "m", VR).call(this, t);
    const s = await e.chat.completions.create({
      ...t,
      stream: !1
    }, {
      ...n,
      signal: this.controller.signal
    });
    return this._connected(), this._addChatCompletion(Cw(s, t));
  }
  async _runChatCompletion(e, t, n) {
    for (const r of t.messages) this._addMessage(r, !1);
    return await this._createChatCompletion(e, t, n);
  }
  async _runTools(e, t, n) {
    const r = "tool", { tool_choice: s = "auto", stream: o, ...i } = t, a = typeof s != "string" && s.type === "function" && s?.function?.name, { maxChatCompletions: l = p8 } = n || {}, u = t.tools.map((p) => {
      if (pd(p)) {
        if (!p.$callback) throw new st("Tool given to `.runTools()` that does not have an associated function");
        return {
          type: "function",
          function: {
            function: p.$callback,
            name: p.function.name,
            description: p.function.description || "",
            parameters: p.function.parameters,
            parse: p.$parseRaw,
            strict: !0
          }
        };
      }
      return p;
    }), d = {};
    for (const p of u) p.type === "function" && (d[p.function.name || p.function.function.name] = p.function);
    const h = "tools" in t ? u.map((p) => p.type === "function" ? {
      type: "function",
      function: {
        name: p.function.name || p.function.function.name,
        parameters: p.function.parameters,
        description: p.function.description,
        strict: p.function.strict
      }
    } : p) : void 0;
    for (const p of t.messages) this._addMessage(p, !1);
    for (let p = 0; p < l; ++p) {
      const y = (await this._createChatCompletion(e, {
        ...i,
        tool_choice: s,
        tools: h,
        messages: [...this.messages]
      }, n)).choices[0]?.message;
      if (!y) throw new st("missing message in ChatCompletion response");
      if (!y.tool_calls?.length) return;
      for (const w of y.tool_calls) {
        if (w.type !== "function") continue;
        const E = w.id, { name: C, arguments: M } = w.function, k = d[C];
        if (k) {
          if (a && a !== C) {
            const b = `Invalid tool_call: ${JSON.stringify(C)}. ${JSON.stringify(a)} requested. Please try again`;
            this._addMessage({
              role: r,
              tool_call_id: E,
              content: b
            });
            continue;
          }
        } else {
          const b = `Invalid tool_call: ${JSON.stringify(C)}. Available options are: ${Object.keys(d).map((v) => JSON.stringify(v)).join(", ")}. Please try again`;
          this._addMessage({
            role: r,
            tool_call_id: E,
            content: b
          });
          continue;
        }
        let R;
        try {
          R = h8(k) ? await k.parse(M) : M;
        } catch (b) {
          const v = b instanceof Error ? b.message : String(b);
          this._addMessage({
            role: r,
            tool_call_id: E,
            content: v
          });
          continue;
        }
        const D = await k.function(R, this), m = de(this, Hn, "m", KR).call(this, D);
        if (this._addMessage({
          role: r,
          tool_call_id: E,
          content: m
        }), a) return;
      }
    }
  }
};
Hn = /* @__PURE__ */ new WeakSet(), p_ = function() {
  return de(this, Hn, "m", gp).call(this).content ?? null;
}, gp = function() {
  let t = this.messages.length;
  for (; t-- > 0; ) {
    const n = this.messages[t];
    if (hp(n)) return {
      ...n,
      content: n.content ?? null,
      refusal: n.refusal ?? null
    };
  }
  throw new st("stream ended without producing a ChatCompletionMessage with role=assistant");
}, m_ = function() {
  for (let t = this.messages.length - 1; t >= 0; t--) {
    const n = this.messages[t];
    if (hp(n) && n?.tool_calls?.length) return n.tool_calls.filter((r) => r.type === "function").at(-1)?.function;
  }
}, g_ = function() {
  for (let t = this.messages.length - 1; t >= 0; t--) {
    const n = this.messages[t];
    if (GR(n) && n.content != null && typeof n.content == "string" && this.messages.some((r) => r.role === "assistant" && r.tool_calls?.some((s) => s.type === "function" && s.id === n.tool_call_id))) return n.content;
  }
}, v_ = function() {
  const t = {
    completion_tokens: 0,
    prompt_tokens: 0,
    total_tokens: 0
  };
  for (const { usage: n } of this._chatCompletions) n && (t.completion_tokens += n.completion_tokens, t.prompt_tokens += n.prompt_tokens, t.total_tokens += n.total_tokens);
  return t;
}, VR = function(t) {
  if (t.n != null && t.n > 1) throw new st("ChatCompletion convenience helpers only support n=1 at this time. To use n>1, please use chat.completions.create() directly.");
}, KR = function(t) {
  return typeof t == "string" ? t : t === void 0 ? "undefined" : JSON.stringify(t);
};
var m8 = class JR extends zR {
  static runTools(t, n, r) {
    const s = new JR(), o = {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "runTools"
      }
    };
    return s._run(() => s._runTools(t, n, o)), s;
  }
  _addMessage(t, n = !0) {
    super._addMessage(t, n), hp(t) && t.content && this._emit("content", t.content);
  }
}, g8 = 1, YR = 2, XR = 4, QR = 8, v8 = 16, y8 = 32, _8 = 64, ZR = 128, jR = 256, b8 = ZR | jR, w8 = 496, lA = YR | 497, uA = XR | QR, vn = {
  STR: g8,
  NUM: YR,
  ARR: XR,
  OBJ: QR,
  NULL: v8,
  BOOL: y8,
  NAN: _8,
  INFINITY: ZR,
  MINUS_INFINITY: jR,
  INF: b8,
  SPECIAL: w8,
  ATOM: lA,
  COLLECTION: uA,
  ALL: lA | uA
}, S8 = class extends Error {
}, T8 = class extends Error {
};
function E8(e, t = vn.ALL) {
  if (typeof e != "string") throw new TypeError(`expecting str, got ${typeof e}`);
  if (!e.trim()) throw new Error(`${e} is empty`);
  return C8(e.trim(), t);
}
var C8 = (e, t) => {
  const n = e.length;
  let r = 0;
  const s = (p) => {
    throw new S8(`${p} at position ${r}`);
  }, o = (p) => {
    throw new T8(`${p} at position ${r}`);
  }, i = () => (h(), r >= n && s("Unexpected end of input"), e[r] === '"' ? a() : e[r] === "{" ? l() : e[r] === "[" ? u() : e.substring(r, r + 4) === "null" || vn.NULL & t && n - r < 4 && "null".startsWith(e.substring(r)) ? (r += 4, null) : e.substring(r, r + 4) === "true" || vn.BOOL & t && n - r < 4 && "true".startsWith(e.substring(r)) ? (r += 4, !0) : e.substring(r, r + 5) === "false" || vn.BOOL & t && n - r < 5 && "false".startsWith(e.substring(r)) ? (r += 5, !1) : e.substring(r, r + 8) === "Infinity" || vn.INFINITY & t && n - r < 8 && "Infinity".startsWith(e.substring(r)) ? (r += 8, 1 / 0) : e.substring(r, r + 9) === "-Infinity" || vn.MINUS_INFINITY & t && 1 < n - r && n - r < 9 && "-Infinity".startsWith(e.substring(r)) ? (r += 9, -1 / 0) : e.substring(r, r + 3) === "NaN" || vn.NAN & t && n - r < 3 && "NaN".startsWith(e.substring(r)) ? (r += 3, NaN) : d()), a = () => {
    const p = r;
    let y = !1;
    for (r++; r < n && (e[r] !== '"' || y && e[r - 1] === "\\"); )
      y = e[r] === "\\" ? !y : !1, r++;
    if (e.charAt(r) == '"') try {
      return JSON.parse(e.substring(p, ++r - Number(y)));
    } catch (w) {
      o(String(w));
    }
    else if (vn.STR & t) try {
      return JSON.parse(e.substring(p, r - Number(y)) + '"');
    } catch {
      return JSON.parse(e.substring(p, e.lastIndexOf("\\")) + '"');
    }
    s("Unterminated string literal");
  }, l = () => {
    r++, h();
    const p = {};
    try {
      for (; e[r] !== "}"; ) {
        if (h(), r >= n && vn.OBJ & t) return p;
        const y = a();
        h(), r++;
        try {
          const w = i();
          Object.defineProperty(p, y, {
            value: w,
            writable: !0,
            enumerable: !0,
            configurable: !0
          });
        } catch (w) {
          if (vn.OBJ & t) return p;
          throw w;
        }
        h(), e[r] === "," && r++;
      }
    } catch {
      if (vn.OBJ & t) return p;
      s("Expected '}' at end of object");
    }
    return r++, p;
  }, u = () => {
    r++;
    const p = [];
    try {
      for (; e[r] !== "]"; )
        p.push(i()), h(), e[r] === "," && r++;
    } catch {
      if (vn.ARR & t) return p;
      s("Expected ']' at end of array");
    }
    return r++, p;
  }, d = () => {
    if (r === 0) {
      e === "-" && vn.NUM & t && s("Not sure what '-' is");
      try {
        return JSON.parse(e);
      } catch (y) {
        if (vn.NUM & t) try {
          return e[e.length - 1] === "." ? JSON.parse(e.substring(0, e.lastIndexOf("."))) : JSON.parse(e.substring(0, e.lastIndexOf("e")));
        } catch {
        }
        o(String(y));
      }
    }
    const p = r;
    for (e[r] === "-" && r++; e[r] && !",]}".includes(e[r]); ) r++;
    r == n && !(vn.NUM & t) && s("Unterminated number literal");
    try {
      return JSON.parse(e.substring(p, r));
    } catch {
      e.substring(p, r) === "-" && vn.NUM & t && s("Not sure what '-' is");
      try {
        return JSON.parse(e.substring(p, e.lastIndexOf("e")));
      } catch (w) {
        o(String(w));
      }
    }
  }, h = () => {
    for (; r < n && " \n\r\t".includes(e[r]); ) r++;
  };
  return i();
}, cA = (e) => E8(e, vn.ALL ^ vn.NUM), un, Ks, $a, Mo, mv, Nf, gv, vv, yv, $f, _v, dA, e1 = class y_ extends zR {
  constructor(t) {
    super(), un.add(this), Ks.set(this, void 0), $a.set(this, void 0), Mo.set(this, void 0), ht(this, Ks, t, "f"), ht(this, $a, [], "f");
  }
  get currentChatCompletionSnapshot() {
    return de(this, Mo, "f");
  }
  static fromReadableStream(t) {
    const n = new y_(null);
    return n._run(() => n._fromReadableStream(t)), n;
  }
  static createChatCompletion(t, n, r) {
    const s = new y_(n);
    return s._run(() => s._runChatCompletion(t, {
      ...n,
      stream: !0
    }, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), s;
  }
  async _createChatCompletion(t, n, r) {
    super._createChatCompletion;
    const s = r?.signal;
    s && (s.aborted && this.controller.abort(), s.addEventListener("abort", () => this.controller.abort())), de(this, un, "m", mv).call(this);
    const o = await t.chat.completions.create({
      ...n,
      stream: !0
    }, {
      ...r,
      signal: this.controller.signal
    });
    this._connected();
    for await (const i of o) de(this, un, "m", gv).call(this, i);
    if (o.controller.signal?.aborted) throw new Kr();
    return this._addChatCompletion(de(this, un, "m", $f).call(this));
  }
  async _fromReadableStream(t, n) {
    const r = n?.signal;
    r && (r.aborted && this.controller.abort(), r.addEventListener("abort", () => this.controller.abort())), de(this, un, "m", mv).call(this), this._connected();
    const s = Vc.fromReadableStream(t, this.controller);
    let o;
    for await (const i of s)
      o && o !== i.id && this._addChatCompletion(de(this, un, "m", $f).call(this)), de(this, un, "m", gv).call(this, i), o = i.id;
    if (s.controller.signal?.aborted) throw new Kr();
    return this._addChatCompletion(de(this, un, "m", $f).call(this));
  }
  [(Ks = /* @__PURE__ */ new WeakMap(), $a = /* @__PURE__ */ new WeakMap(), Mo = /* @__PURE__ */ new WeakMap(), un = /* @__PURE__ */ new WeakSet(), mv = function() {
    this.ended || ht(this, Mo, void 0, "f");
  }, Nf = function(n) {
    let r = de(this, $a, "f")[n.index];
    return r || (r = {
      content_done: !1,
      refusal_done: !1,
      logprobs_content_done: !1,
      logprobs_refusal_done: !1,
      done_tool_calls: /* @__PURE__ */ new Set(),
      current_tool_call_index: null
    }, de(this, $a, "f")[n.index] = r, r);
  }, gv = function(n) {
    if (this.ended) return;
    const r = de(this, un, "m", dA).call(this, n);
    this._emit("chunk", n, r);
    for (const s of n.choices) {
      const o = r.choices[s.index];
      s.delta.content != null && o.message?.role === "assistant" && o.message?.content && (this._emit("content", s.delta.content, o.message.content), this._emit("content.delta", {
        delta: s.delta.content,
        snapshot: o.message.content,
        parsed: o.message.parsed
      })), s.delta.refusal != null && o.message?.role === "assistant" && o.message?.refusal && this._emit("refusal.delta", {
        delta: s.delta.refusal,
        snapshot: o.message.refusal
      }), s.logprobs?.content != null && o.message?.role === "assistant" && this._emit("logprobs.content.delta", {
        content: s.logprobs?.content,
        snapshot: o.logprobs?.content ?? []
      }), s.logprobs?.refusal != null && o.message?.role === "assistant" && this._emit("logprobs.refusal.delta", {
        refusal: s.logprobs?.refusal,
        snapshot: o.logprobs?.refusal ?? []
      });
      const i = de(this, un, "m", Nf).call(this, o);
      o.finish_reason && (de(this, un, "m", yv).call(this, o), i.current_tool_call_index != null && de(this, un, "m", vv).call(this, o, i.current_tool_call_index));
      for (const a of s.delta.tool_calls ?? [])
        i.current_tool_call_index !== a.index && (de(this, un, "m", yv).call(this, o), i.current_tool_call_index != null && de(this, un, "m", vv).call(this, o, i.current_tool_call_index)), i.current_tool_call_index = a.index;
      for (const a of s.delta.tool_calls ?? []) {
        const l = o.message.tool_calls?.[a.index];
        l?.type && (l?.type === "function" ? this._emit("tool_calls.function.arguments.delta", {
          name: l.function?.name,
          index: a.index,
          arguments: l.function.arguments,
          parsed_arguments: l.function.parsed_arguments,
          arguments_delta: a.function?.arguments ?? ""
        }) : (l?.type, void 0));
      }
    }
  }, vv = function(n, r) {
    if (de(this, un, "m", Nf).call(this, n).done_tool_calls.has(r)) return;
    const s = n.message.tool_calls?.[r];
    if (!s) throw new Error("no tool call snapshot");
    if (!s.type) throw new Error("tool call snapshot missing `type`");
    if (s.type === "function") {
      const o = de(this, Ks, "f")?.tools?.find((i) => fp(i) && i.function.name === s.function.name);
      this._emit("tool_calls.function.arguments.done", {
        name: s.function.name,
        index: r,
        arguments: s.function.arguments,
        parsed_arguments: pd(o) ? o.$parseRaw(s.function.arguments) : o?.function.strict ? JSON.parse(s.function.arguments) : null
      });
    } else s.type;
  }, yv = function(n) {
    const r = de(this, un, "m", Nf).call(this, n);
    if (n.message.content && !r.content_done) {
      r.content_done = !0;
      const s = de(this, un, "m", _v).call(this);
      this._emit("content.done", {
        content: n.message.content,
        parsed: s ? s.$parseRaw(n.message.content) : null
      });
    }
    n.message.refusal && !r.refusal_done && (r.refusal_done = !0, this._emit("refusal.done", { refusal: n.message.refusal })), n.logprobs?.content && !r.logprobs_content_done && (r.logprobs_content_done = !0, this._emit("logprobs.content.done", { content: n.logprobs.content })), n.logprobs?.refusal && !r.logprobs_refusal_done && (r.logprobs_refusal_done = !0, this._emit("logprobs.refusal.done", { refusal: n.logprobs.refusal }));
  }, $f = function() {
    if (this.ended) throw new st("stream has ended, this shouldn't happen");
    const n = de(this, Mo, "f");
    if (!n) throw new st("request ended without sending any chunks");
    return ht(this, Mo, void 0, "f"), ht(this, $a, [], "f"), A8(n, de(this, Ks, "f"));
  }, _v = function() {
    const n = de(this, Ks, "f")?.response_format;
    return Ew(n) ? n : null;
  }, dA = function(n) {
    var r, s, o, i;
    let a = de(this, Mo, "f");
    const { choices: l, ...u } = n;
    a ? Object.assign(a, u) : a = ht(this, Mo, {
      ...u,
      choices: []
    }, "f");
    for (const { delta: d, finish_reason: h, index: p, logprobs: y = null, ...w } of n.choices) {
      let E = a.choices[p];
      if (E || (E = a.choices[p] = {
        finish_reason: h,
        index: p,
        message: {},
        logprobs: y,
        ...w
      }), y) if (!E.logprobs) E.logprobs = Object.assign({}, y);
      else {
        const { content: b, refusal: v, ...T } = y;
        Object.assign(E.logprobs, T), b && ((r = E.logprobs).content ?? (r.content = []), E.logprobs.content.push(...b)), v && ((s = E.logprobs).refusal ?? (s.refusal = []), E.logprobs.refusal.push(...v));
      }
      if (h && (E.finish_reason = h, de(this, Ks, "f") && qR(de(this, Ks, "f")))) {
        if (h === "length") throw new yR();
        if (h === "content_filter") throw new _R();
      }
      if (Object.assign(E, w), !d) continue;
      const { content: C, refusal: M, function_call: k, role: R, tool_calls: D, ...m } = d;
      if (Object.assign(E.message, m), M && (E.message.refusal = (E.message.refusal || "") + M), R && (E.message.role = R), k && (E.message.function_call ? (k.name && (E.message.function_call.name = k.name), k.arguments && ((o = E.message.function_call).arguments ?? (o.arguments = ""), E.message.function_call.arguments += k.arguments)) : E.message.function_call = k), C && (E.message.content = (E.message.content || "") + C, !E.message.refusal && de(this, un, "m", _v).call(this) && (E.message.parsed = cA(E.message.content))), D) {
        E.message.tool_calls || (E.message.tool_calls = []);
        for (const { index: b, id: v, type: T, function: I, ...x } of D) {
          const P = (i = E.message.tool_calls)[b] ?? (i[b] = {});
          Object.assign(P, x), v && (P.id = v), T && (P.type = T), I && (P.function ?? (P.function = {
            name: I.name ?? "",
            arguments: ""
          })), I?.name && (P.function.name = I.name), I?.arguments && (P.function.arguments += I.arguments, d8(de(this, Ks, "f"), P) && (P.function.parsed_arguments = cA(P.function.arguments)));
        }
      }
    }
    return a;
  }, Symbol.asyncIterator)]() {
    const t = [], n = [];
    let r = !1;
    return this.on("chunk", (s) => {
      const o = n.shift();
      o ? o.resolve(s) : t.push(s);
    }), this.on("end", () => {
      r = !0;
      for (const s of n) s.resolve(void 0);
      n.length = 0;
    }), this.on("abort", (s) => {
      r = !0;
      for (const o of n) o.reject(s);
      n.length = 0;
    }), this.on("error", (s) => {
      r = !0;
      for (const o of n) o.reject(s);
      n.length = 0;
    }), {
      next: async () => t.length ? {
        value: t.shift(),
        done: !1
      } : r ? {
        value: void 0,
        done: !0
      } : new Promise((s, o) => n.push({
        resolve: s,
        reject: o
      })).then((s) => s ? {
        value: s,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  toReadableStream() {
    return new Vc(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
  }
};
function A8(e, t) {
  const { id: n, choices: r, created: s, model: o, system_fingerprint: i, ...a } = e;
  return l8({
    ...a,
    id: n,
    choices: r.map(({ message: l, finish_reason: u, index: d, logprobs: h, ...p }) => {
      if (!u) throw new st(`missing finish_reason for choice ${d}`);
      const { content: y = null, function_call: w, tool_calls: E, ...C } = l, M = l.role;
      if (!M) throw new st(`missing role for choice ${d}`);
      if (w) {
        const { arguments: k, name: R } = w;
        if (k == null) throw new st(`missing function_call.arguments for choice ${d}`);
        if (!R) throw new st(`missing function_call.name for choice ${d}`);
        return {
          ...p,
          message: {
            content: y,
            function_call: {
              arguments: k,
              name: R
            },
            role: M,
            refusal: l.refusal ?? null
          },
          finish_reason: u,
          index: d,
          logprobs: h
        };
      }
      return E ? {
        ...p,
        index: d,
        finish_reason: u,
        logprobs: h,
        message: {
          ...C,
          role: M,
          content: y,
          refusal: l.refusal ?? null,
          tool_calls: E.map((k, R) => {
            const { function: D, type: m, id: b, ...v } = k, { arguments: T, name: I, ...x } = D || {};
            if (b == null) throw new st(`missing choices[${d}].tool_calls[${R}].id
${Df(e)}`);
            if (m == null) throw new st(`missing choices[${d}].tool_calls[${R}].type
${Df(e)}`);
            if (I == null) throw new st(`missing choices[${d}].tool_calls[${R}].function.name
${Df(e)}`);
            if (T == null) throw new st(`missing choices[${d}].tool_calls[${R}].function.arguments
${Df(e)}`);
            return {
              ...v,
              id: b,
              type: m,
              function: {
                ...x,
                name: I,
                arguments: T
              }
            };
          })
        }
      } : {
        ...p,
        message: {
          ...C,
          content: y,
          role: M,
          refusal: l.refusal ?? null
        },
        finish_reason: u,
        index: d,
        logprobs: h
      };
    }),
    created: s,
    model: o,
    object: "chat.completion",
    ...i ? { system_fingerprint: i } : {}
  }, t);
}
function Df(e) {
  return JSON.stringify(e);
}
var I8 = class __ extends e1 {
  static fromReadableStream(t) {
    const n = new __(null);
    return n._run(() => n._fromReadableStream(t)), n;
  }
  static runTools(t, n, r) {
    const s = new __(n), o = {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "runTools"
      }
    };
    return s._run(() => s._runTools(t, n, o)), s;
  }
}, Iw = class extends et {
  constructor() {
    super(...arguments), this.messages = new UR(this._client);
  }
  create(e, t) {
    return this._client.post("/chat/completions", {
      body: e,
      ...t,
      stream: e.stream ?? !1
    });
  }
  retrieve(e, t) {
    return this._client.get(Ae`/chat/completions/${e}`, t);
  }
  update(e, t, n) {
    return this._client.post(Ae`/chat/completions/${e}`, {
      body: t,
      ...n
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/chat/completions", nn, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(Ae`/chat/completions/${e}`, t);
  }
  parse(e, t) {
    return f8(e.tools), this._client.chat.completions.create(e, {
      ...t,
      headers: {
        ...t?.headers,
        "X-Stainless-Helper-Method": "chat.completions.parse"
      }
    })._thenUnwrap((n) => Cw(n, e));
  }
  runTools(e, t) {
    return e.stream ? I8.runTools(this._client, e, t) : m8.runTools(this._client, e, t);
  }
  stream(e, t) {
    return e1.createChatCompletion(this._client, e, t);
  }
};
Iw.Messages = UR;
var xw = class extends et {
  constructor() {
    super(...arguments), this.completions = new Iw(this._client);
  }
};
xw.Completions = Iw;
var t1 = /* @__PURE__ */ Symbol("brand.privateNullableHeaders");
function* x8(e) {
  if (!e) return;
  if (t1 in e) {
    const { values: r, nulls: s } = e;
    yield* r.entries();
    for (const o of s) yield [o, null];
    return;
  }
  let t = !1, n;
  e instanceof Headers ? n = e.entries() : KC(e) ? n = e : (t = !0, n = Object.entries(e ?? {}));
  for (let r of n) {
    const s = r[0];
    if (typeof s != "string") throw new TypeError("expected header name to be a string");
    const o = KC(r[1]) ? r[1] : [r[1]];
    let i = !1;
    for (const a of o)
      a !== void 0 && (t && !i && (i = !0, yield [s, null]), yield [s, a]);
  }
}
var Xe = (e) => {
  const t = new Headers(), n = /* @__PURE__ */ new Set();
  for (const r of e) {
    const s = /* @__PURE__ */ new Set();
    for (const [o, i] of x8(r)) {
      const a = o.toLowerCase();
      s.has(a) || (t.delete(o), s.add(a)), i === null ? (t.delete(o), n.add(a)) : (t.append(o, i), n.delete(a));
    }
  }
  return {
    [t1]: !0,
    values: t,
    nulls: n
  };
}, n1 = class extends et {
  create(e, t) {
    return this._client.post("/audio/speech", {
      body: e,
      ...t,
      headers: Xe([{ Accept: "application/octet-stream" }, t?.headers]),
      __binaryResponse: !0
    });
  }
}, r1 = class extends et {
  create(e, t) {
    return this._client.post("/audio/transcriptions", Ps({
      body: e,
      ...t,
      stream: e.stream ?? !1,
      __metadata: { model: e.model }
    }, this._client));
  }
}, s1 = class extends et {
  create(e, t) {
    return this._client.post("/audio/translations", Ps({
      body: e,
      ...t,
      __metadata: { model: e.model }
    }, this._client));
  }
}, md = class extends et {
  constructor() {
    super(...arguments), this.transcriptions = new r1(this._client), this.translations = new s1(this._client), this.speech = new n1(this._client);
  }
};
md.Transcriptions = r1;
md.Translations = s1;
md.Speech = n1;
var o1 = class extends et {
  create(e, t) {
    return this._client.post("/batches", {
      body: e,
      ...t
    });
  }
  retrieve(e, t) {
    return this._client.get(Ae`/batches/${e}`, t);
  }
  list(e = {}, t) {
    return this._client.getAPIList("/batches", nn, {
      query: e,
      ...t
    });
  }
  cancel(e, t) {
    return this._client.post(Ae`/batches/${e}/cancel`, t);
  }
}, i1 = class extends et {
  create(e, t) {
    return this._client.post("/assistants", {
      body: e,
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  retrieve(e, t) {
    return this._client.get(Ae`/assistants/${e}`, {
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  update(e, t, n) {
    return this._client.post(Ae`/assistants/${e}`, {
      body: t,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/assistants", nn, {
      query: e,
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  delete(e, t) {
    return this._client.delete(Ae`/assistants/${e}`, {
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
}, a1 = class extends et {
  create(e, t) {
    return this._client.post("/realtime/sessions", {
      body: e,
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
}, l1 = class extends et {
  create(e, t) {
    return this._client.post("/realtime/transcription_sessions", {
      body: e,
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
}, lm = class extends et {
  constructor() {
    super(...arguments), this.sessions = new a1(this._client), this.transcriptionSessions = new l1(this._client);
  }
};
lm.Sessions = a1;
lm.TranscriptionSessions = l1;
var u1 = class extends et {
  create(e, t) {
    return this._client.post("/chatkit/sessions", {
      body: e,
      ...t,
      headers: Xe([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers])
    });
  }
  cancel(e, t) {
    return this._client.post(Ae`/chatkit/sessions/${e}/cancel`, {
      ...t,
      headers: Xe([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers])
    });
  }
}, c1 = class extends et {
  retrieve(e, t) {
    return this._client.get(Ae`/chatkit/threads/${e}`, {
      ...t,
      headers: Xe([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers])
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/chatkit/threads", Kc, {
      query: e,
      ...t,
      headers: Xe([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers])
    });
  }
  delete(e, t) {
    return this._client.delete(Ae`/chatkit/threads/${e}`, {
      ...t,
      headers: Xe([{ "OpenAI-Beta": "chatkit_beta=v1" }, t?.headers])
    });
  }
  listItems(e, t = {}, n) {
    return this._client.getAPIList(Ae`/chatkit/threads/${e}/items`, Kc, {
      query: t,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "chatkit_beta=v1" }, n?.headers])
    });
  }
}, um = class extends et {
  constructor() {
    super(...arguments), this.sessions = new u1(this._client), this.threads = new c1(this._client);
  }
};
um.Sessions = u1;
um.Threads = c1;
var d1 = class extends et {
  create(e, t, n) {
    return this._client.post(Ae`/threads/${e}/messages`, {
      body: t,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  retrieve(e, t, n) {
    const { thread_id: r } = t;
    return this._client.get(Ae`/threads/${r}/messages/${e}`, {
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  update(e, t, n) {
    const { thread_id: r, ...s } = t;
    return this._client.post(Ae`/threads/${r}/messages/${e}`, {
      body: s,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Ae`/threads/${e}/messages`, nn, {
      query: t,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { thread_id: r } = t;
    return this._client.delete(Ae`/threads/${r}/messages/${e}`, {
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
}, f1 = class extends et {
  retrieve(e, t, n) {
    const { thread_id: r, run_id: s, ...o } = t;
    return this._client.get(Ae`/threads/${r}/runs/${s}/steps/${e}`, {
      query: o,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  list(e, t, n) {
    const { thread_id: r, ...s } = t;
    return this._client.getAPIList(Ae`/threads/${r}/runs/${e}/steps`, nn, {
      query: s,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
}, k8 = (e) => {
  if (typeof Buffer < "u") {
    const t = Buffer.from(e, "base64");
    return Array.from(new Float32Array(t.buffer, t.byteOffset, t.length / Float32Array.BYTES_PER_ELEMENT));
  } else {
    const t = atob(e), n = t.length, r = new Uint8Array(n);
    for (let s = 0; s < n; s++) r[s] = t.charCodeAt(s);
    return Array.from(new Float32Array(r.buffer));
  }
}, Da = (e) => {
  if (typeof globalThis.process < "u") return globalThis.process.env?.[e]?.trim() ?? void 0;
  if (typeof globalThis.Deno < "u") return globalThis.Deno.env?.get?.(e)?.trim();
}, An, Vi, b_, Es, ch, ts, Ki, ja, Ui, vp, Tr, dh, fh, yc, nc, rc, fA, hA, pA, mA, gA, vA, yA, _c = class extends Aw {
  constructor() {
    super(...arguments), An.add(this), b_.set(this, []), Es.set(this, {}), ch.set(this, {}), ts.set(this, void 0), Ki.set(this, void 0), ja.set(this, void 0), Ui.set(this, void 0), vp.set(this, void 0), Tr.set(this, void 0), dh.set(this, void 0), fh.set(this, void 0), yc.set(this, void 0);
  }
  [(b_ = /* @__PURE__ */ new WeakMap(), Es = /* @__PURE__ */ new WeakMap(), ch = /* @__PURE__ */ new WeakMap(), ts = /* @__PURE__ */ new WeakMap(), Ki = /* @__PURE__ */ new WeakMap(), ja = /* @__PURE__ */ new WeakMap(), Ui = /* @__PURE__ */ new WeakMap(), vp = /* @__PURE__ */ new WeakMap(), Tr = /* @__PURE__ */ new WeakMap(), dh = /* @__PURE__ */ new WeakMap(), fh = /* @__PURE__ */ new WeakMap(), yc = /* @__PURE__ */ new WeakMap(), An = /* @__PURE__ */ new WeakSet(), Symbol.asyncIterator)]() {
    const e = [], t = [];
    let n = !1;
    return this.on("event", (r) => {
      const s = t.shift();
      s ? s.resolve(r) : e.push(r);
    }), this.on("end", () => {
      n = !0;
      for (const r of t) r.resolve(void 0);
      t.length = 0;
    }), this.on("abort", (r) => {
      n = !0;
      for (const s of t) s.reject(r);
      t.length = 0;
    }), this.on("error", (r) => {
      n = !0;
      for (const s of t) s.reject(r);
      t.length = 0;
    }), {
      next: async () => e.length ? {
        value: e.shift(),
        done: !1
      } : n ? {
        value: void 0,
        done: !0
      } : new Promise((r, s) => t.push({
        resolve: r,
        reject: s
      })).then((r) => r ? {
        value: r,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  static fromReadableStream(e) {
    const t = new Vi();
    return t._run(() => t._fromReadableStream(e)), t;
  }
  async _fromReadableStream(e, t) {
    const n = t?.signal;
    n && (n.aborted && this.controller.abort(), n.addEventListener("abort", () => this.controller.abort())), this._connected();
    const r = Vc.fromReadableStream(e, this.controller);
    for await (const s of r) de(this, An, "m", nc).call(this, s);
    if (r.controller.signal?.aborted) throw new Kr();
    return this._addRun(de(this, An, "m", rc).call(this));
  }
  toReadableStream() {
    return new Vc(this[Symbol.asyncIterator].bind(this), this.controller).toReadableStream();
  }
  static createToolAssistantStream(e, t, n, r) {
    const s = new Vi();
    return s._run(() => s._runToolAssistantStream(e, t, n, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), s;
  }
  async _createToolAssistantStream(e, t, n, r) {
    const s = r?.signal;
    s && (s.aborted && this.controller.abort(), s.addEventListener("abort", () => this.controller.abort()));
    const o = {
      ...n,
      stream: !0
    }, i = await e.submitToolOutputs(t, o, {
      ...r,
      signal: this.controller.signal
    });
    this._connected();
    for await (const a of i) de(this, An, "m", nc).call(this, a);
    if (i.controller.signal?.aborted) throw new Kr();
    return this._addRun(de(this, An, "m", rc).call(this));
  }
  static createThreadAssistantStream(e, t, n) {
    const r = new Vi();
    return r._run(() => r._threadAssistantStream(e, t, {
      ...n,
      headers: {
        ...n?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), r;
  }
  static createAssistantStream(e, t, n, r) {
    const s = new Vi();
    return s._run(() => s._runAssistantStream(e, t, n, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), s;
  }
  currentEvent() {
    return de(this, dh, "f");
  }
  currentRun() {
    return de(this, fh, "f");
  }
  currentMessageSnapshot() {
    return de(this, ts, "f");
  }
  currentRunStepSnapshot() {
    return de(this, yc, "f");
  }
  async finalRunSteps() {
    return await this.done(), Object.values(de(this, Es, "f"));
  }
  async finalMessages() {
    return await this.done(), Object.values(de(this, ch, "f"));
  }
  async finalRun() {
    if (await this.done(), !de(this, Ki, "f")) throw Error("Final run was not received.");
    return de(this, Ki, "f");
  }
  async _createThreadAssistantStream(e, t, n) {
    const r = n?.signal;
    r && (r.aborted && this.controller.abort(), r.addEventListener("abort", () => this.controller.abort()));
    const s = {
      ...t,
      stream: !0
    }, o = await e.createAndRun(s, {
      ...n,
      signal: this.controller.signal
    });
    this._connected();
    for await (const i of o) de(this, An, "m", nc).call(this, i);
    if (o.controller.signal?.aborted) throw new Kr();
    return this._addRun(de(this, An, "m", rc).call(this));
  }
  async _createAssistantStream(e, t, n, r) {
    const s = r?.signal;
    s && (s.aborted && this.controller.abort(), s.addEventListener("abort", () => this.controller.abort()));
    const o = {
      ...n,
      stream: !0
    }, i = await e.create(t, o, {
      ...r,
      signal: this.controller.signal
    });
    this._connected();
    for await (const a of i) de(this, An, "m", nc).call(this, a);
    if (i.controller.signal?.aborted) throw new Kr();
    return this._addRun(de(this, An, "m", rc).call(this));
  }
  static accumulateDelta(e, t) {
    for (const [n, r] of Object.entries(t)) {
      if (!e.hasOwnProperty(n)) {
        e[n] = r;
        continue;
      }
      let s = e[n];
      if (s == null) {
        e[n] = r;
        continue;
      }
      if (n === "index" || n === "type") {
        e[n] = r;
        continue;
      }
      if (typeof s == "string" && typeof r == "string") s += r;
      else if (typeof s == "number" && typeof r == "number") s += r;
      else if (fv(s) && fv(r)) s = this.accumulateDelta(s, r);
      else if (Array.isArray(s) && Array.isArray(r)) {
        if (s.every((o) => typeof o == "string" || typeof o == "number")) {
          s.push(...r);
          continue;
        }
        for (const o of r) {
          if (!fv(o)) throw new Error(`Expected array delta entry to be an object but got: ${o}`);
          const i = o.index;
          if (i == null)
            throw console.error(o), new Error("Expected array delta entry to have an `index` property");
          if (typeof i != "number") throw new Error(`Expected array delta entry \`index\` property to be a number but got ${i}`);
          const a = s[i];
          a == null ? s.push(o) : s[i] = this.accumulateDelta(a, o);
        }
        continue;
      } else throw Error(`Unhandled record type: ${n}, deltaValue: ${r}, accValue: ${s}`);
      e[n] = s;
    }
    return e;
  }
  _addRun(e) {
    return e;
  }
  async _threadAssistantStream(e, t, n) {
    return await this._createThreadAssistantStream(t, e, n);
  }
  async _runAssistantStream(e, t, n, r) {
    return await this._createAssistantStream(t, e, n, r);
  }
  async _runToolAssistantStream(e, t, n, r) {
    return await this._createToolAssistantStream(t, e, n, r);
  }
};
Vi = _c, nc = function(t) {
  if (!this.ended)
    switch (ht(this, dh, t, "f"), de(this, An, "m", pA).call(this, t), t.event) {
      case "thread.created":
        break;
      case "thread.run.created":
      case "thread.run.queued":
      case "thread.run.in_progress":
      case "thread.run.requires_action":
      case "thread.run.completed":
      case "thread.run.incomplete":
      case "thread.run.failed":
      case "thread.run.cancelling":
      case "thread.run.cancelled":
      case "thread.run.expired":
        de(this, An, "m", yA).call(this, t);
        break;
      case "thread.run.step.created":
      case "thread.run.step.in_progress":
      case "thread.run.step.delta":
      case "thread.run.step.completed":
      case "thread.run.step.failed":
      case "thread.run.step.cancelled":
      case "thread.run.step.expired":
        de(this, An, "m", hA).call(this, t);
        break;
      case "thread.message.created":
      case "thread.message.in_progress":
      case "thread.message.delta":
      case "thread.message.completed":
      case "thread.message.incomplete":
        de(this, An, "m", fA).call(this, t);
        break;
      case "error":
        throw new Error("Encountered an error event in event processing - errors should be processed earlier");
      default:
    }
}, rc = function() {
  if (this.ended) throw new st("stream has ended, this shouldn't happen");
  if (!de(this, Ki, "f")) throw Error("Final run has not been received");
  return de(this, Ki, "f");
}, fA = function(t) {
  const [n, r] = de(this, An, "m", gA).call(this, t, de(this, ts, "f"));
  ht(this, ts, n, "f"), de(this, ch, "f")[n.id] = n;
  for (const s of r) {
    const o = n.content[s.index];
    o?.type == "text" && this._emit("textCreated", o.text);
  }
  switch (t.event) {
    case "thread.message.created":
      this._emit("messageCreated", t.data);
      break;
    case "thread.message.in_progress":
      break;
    case "thread.message.delta":
      if (this._emit("messageDelta", t.data.delta, n), t.data.delta.content) for (const s of t.data.delta.content) {
        if (s.type == "text" && s.text) {
          let o = s.text, i = n.content[s.index];
          if (i && i.type == "text") this._emit("textDelta", o, i.text);
          else throw Error("The snapshot associated with this text delta is not text or missing");
        }
        if (s.index != de(this, ja, "f")) {
          if (de(this, Ui, "f")) switch (de(this, Ui, "f").type) {
            case "text":
              this._emit("textDone", de(this, Ui, "f").text, de(this, ts, "f"));
              break;
            case "image_file":
              this._emit("imageFileDone", de(this, Ui, "f").image_file, de(this, ts, "f"));
              break;
          }
          ht(this, ja, s.index, "f");
        }
        ht(this, Ui, n.content[s.index], "f");
      }
      break;
    case "thread.message.completed":
    case "thread.message.incomplete":
      if (de(this, ja, "f") !== void 0) {
        const s = t.data.content[de(this, ja, "f")];
        if (s) switch (s.type) {
          case "image_file":
            this._emit("imageFileDone", s.image_file, de(this, ts, "f"));
            break;
          case "text":
            this._emit("textDone", s.text, de(this, ts, "f"));
            break;
        }
      }
      de(this, ts, "f") && this._emit("messageDone", t.data), ht(this, ts, void 0, "f");
  }
}, hA = function(t) {
  const n = de(this, An, "m", mA).call(this, t);
  switch (ht(this, yc, n, "f"), t.event) {
    case "thread.run.step.created":
      this._emit("runStepCreated", t.data);
      break;
    case "thread.run.step.delta":
      const r = t.data.delta;
      if (r.step_details && r.step_details.type == "tool_calls" && r.step_details.tool_calls && n.step_details.type == "tool_calls") for (const s of r.step_details.tool_calls) s.index == de(this, vp, "f") ? this._emit("toolCallDelta", s, n.step_details.tool_calls[s.index]) : (de(this, Tr, "f") && this._emit("toolCallDone", de(this, Tr, "f")), ht(this, vp, s.index, "f"), ht(this, Tr, n.step_details.tool_calls[s.index], "f"), de(this, Tr, "f") && this._emit("toolCallCreated", de(this, Tr, "f")));
      this._emit("runStepDelta", t.data.delta, n);
      break;
    case "thread.run.step.completed":
    case "thread.run.step.failed":
    case "thread.run.step.cancelled":
    case "thread.run.step.expired":
      ht(this, yc, void 0, "f"), t.data.step_details.type == "tool_calls" && de(this, Tr, "f") && (this._emit("toolCallDone", de(this, Tr, "f")), ht(this, Tr, void 0, "f")), this._emit("runStepDone", t.data, n);
      break;
    case "thread.run.step.in_progress":
      break;
  }
}, pA = function(t) {
  de(this, b_, "f").push(t), this._emit("event", t);
}, mA = function(t) {
  switch (t.event) {
    case "thread.run.step.created":
      return de(this, Es, "f")[t.data.id] = t.data, t.data;
    case "thread.run.step.delta":
      let n = de(this, Es, "f")[t.data.id];
      if (!n) throw Error("Received a RunStepDelta before creation of a snapshot");
      let r = t.data;
      if (r.delta) {
        const s = Vi.accumulateDelta(n, r.delta);
        de(this, Es, "f")[t.data.id] = s;
      }
      return de(this, Es, "f")[t.data.id];
    case "thread.run.step.completed":
    case "thread.run.step.failed":
    case "thread.run.step.cancelled":
    case "thread.run.step.expired":
    case "thread.run.step.in_progress":
      de(this, Es, "f")[t.data.id] = t.data;
      break;
  }
  if (de(this, Es, "f")[t.data.id]) return de(this, Es, "f")[t.data.id];
  throw new Error("No snapshot available");
}, gA = function(t, n) {
  let r = [];
  switch (t.event) {
    case "thread.message.created":
      return [t.data, r];
    case "thread.message.delta":
      if (!n) throw Error("Received a delta with no existing snapshot (there should be one from message creation)");
      let s = t.data;
      if (s.delta.content) for (const o of s.delta.content) if (o.index in n.content) {
        let i = n.content[o.index];
        n.content[o.index] = de(this, An, "m", vA).call(this, o, i);
      } else
        n.content[o.index] = o, r.push(o);
      return [n, r];
    case "thread.message.in_progress":
    case "thread.message.completed":
    case "thread.message.incomplete":
      if (n) return [n, r];
      throw Error("Received thread message event with no existing snapshot");
  }
  throw Error("Tried to accumulate a non-message event");
}, vA = function(t, n) {
  return Vi.accumulateDelta(n, t);
}, yA = function(t) {
  switch (ht(this, fh, t.data, "f"), t.event) {
    case "thread.run.created":
      break;
    case "thread.run.queued":
      break;
    case "thread.run.in_progress":
      break;
    case "thread.run.requires_action":
    case "thread.run.cancelled":
    case "thread.run.failed":
    case "thread.run.completed":
    case "thread.run.expired":
    case "thread.run.incomplete":
      ht(this, Ki, t.data, "f"), de(this, Tr, "f") && (this._emit("toolCallDone", de(this, Tr, "f")), ht(this, Tr, void 0, "f"));
      break;
    case "thread.run.cancelling":
      break;
  }
};
var kw = class extends et {
  constructor() {
    super(...arguments), this.steps = new f1(this._client);
  }
  create(e, t, n) {
    const { include: r, ...s } = t;
    return this._client.post(Ae`/threads/${e}/runs`, {
      query: { include: r },
      body: s,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      stream: t.stream ?? !1,
      __synthesizeEventData: !0
    });
  }
  retrieve(e, t, n) {
    const { thread_id: r } = t;
    return this._client.get(Ae`/threads/${r}/runs/${e}`, {
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  update(e, t, n) {
    const { thread_id: r, ...s } = t;
    return this._client.post(Ae`/threads/${r}/runs/${e}`, {
      body: s,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Ae`/threads/${e}/runs`, nn, {
      query: t,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  cancel(e, t, n) {
    const { thread_id: r } = t;
    return this._client.post(Ae`/threads/${r}/runs/${e}/cancel`, {
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  async createAndPoll(e, t, n) {
    const r = await this.create(e, t, n);
    return await this.poll(r.id, { thread_id: e }, n);
  }
  createAndStream(e, t, n) {
    return _c.createAssistantStream(e, this._client.beta.threads.runs, t, n);
  }
  async poll(e, t, n) {
    const r = Xe([n?.headers, {
      "X-Stainless-Poll-Helper": "true",
      "X-Stainless-Custom-Poll-Interval": n?.pollIntervalMs?.toString() ?? void 0
    }]);
    for (; ; ) {
      const { data: s, response: o } = await this.retrieve(e, t, {
        ...n,
        headers: {
          ...n?.headers,
          ...r
        }
      }).withResponse();
      switch (s.status) {
        case "queued":
        case "in_progress":
        case "cancelling":
          let i = 5e3;
          if (n?.pollIntervalMs) i = n.pollIntervalMs;
          else {
            const a = o.headers.get("openai-poll-after-ms");
            if (a) {
              const l = parseInt(a);
              isNaN(l) || (i = l);
            }
          }
          await hd(i);
          break;
        case "requires_action":
        case "incomplete":
        case "cancelled":
        case "completed":
        case "failed":
        case "expired":
          return s;
      }
    }
  }
  stream(e, t, n) {
    return _c.createAssistantStream(e, this._client.beta.threads.runs, t, n);
  }
  submitToolOutputs(e, t, n) {
    const { thread_id: r, ...s } = t;
    return this._client.post(Ae`/threads/${r}/runs/${e}/submit_tool_outputs`, {
      body: s,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers]),
      stream: t.stream ?? !1,
      __synthesizeEventData: !0
    });
  }
  async submitToolOutputsAndPoll(e, t, n) {
    const r = await this.submitToolOutputs(e, t, n);
    return await this.poll(r.id, t, n);
  }
  submitToolOutputsStream(e, t, n) {
    return _c.createToolAssistantStream(e, this._client.beta.threads.runs, t, n);
  }
};
kw.Steps = f1;
var cm = class extends et {
  constructor() {
    super(...arguments), this.runs = new kw(this._client), this.messages = new d1(this._client);
  }
  create(e = {}, t) {
    return this._client.post("/threads", {
      body: e,
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  retrieve(e, t) {
    return this._client.get(Ae`/threads/${e}`, {
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  update(e, t, n) {
    return this._client.post(Ae`/threads/${e}`, {
      body: t,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  delete(e, t) {
    return this._client.delete(Ae`/threads/${e}`, {
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  createAndRun(e, t) {
    return this._client.post("/threads/runs", {
      body: e,
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers]),
      stream: e.stream ?? !1,
      __synthesizeEventData: !0
    });
  }
  async createAndRunPoll(e, t) {
    const n = await this.createAndRun(e, t);
    return await this.runs.poll(n.id, { thread_id: n.thread_id }, t);
  }
  createAndRunStream(e, t) {
    return _c.createThreadAssistantStream(e, this._client.beta.threads, t);
  }
};
cm.Runs = kw;
cm.Messages = d1;
var Ul = class extends et {
  constructor() {
    super(...arguments), this.realtime = new lm(this._client), this.chatkit = new um(this._client), this.assistants = new i1(this._client), this.threads = new cm(this._client);
  }
};
Ul.Realtime = lm;
Ul.ChatKit = um;
Ul.Assistants = i1;
Ul.Threads = cm;
var h1 = class extends et {
  create(e, t) {
    return this._client.post("/completions", {
      body: e,
      ...t,
      stream: e.stream ?? !1
    });
  }
}, p1 = class extends et {
  retrieve(e, t, n) {
    const { container_id: r } = t;
    return this._client.get(Ae`/containers/${r}/files/${e}/content`, {
      ...n,
      headers: Xe([{ Accept: "application/binary" }, n?.headers]),
      __binaryResponse: !0
    });
  }
}, Mw = class extends et {
  constructor() {
    super(...arguments), this.content = new p1(this._client);
  }
  create(e, t, n) {
    return this._client.post(Ae`/containers/${e}/files`, am({
      body: t,
      ...n
    }, this._client));
  }
  retrieve(e, t, n) {
    const { container_id: r } = t;
    return this._client.get(Ae`/containers/${r}/files/${e}`, n);
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Ae`/containers/${e}/files`, nn, {
      query: t,
      ...n
    });
  }
  delete(e, t, n) {
    const { container_id: r } = t;
    return this._client.delete(Ae`/containers/${r}/files/${e}`, {
      ...n,
      headers: Xe([{ Accept: "*/*" }, n?.headers])
    });
  }
};
Mw.Content = p1;
var Pw = class extends et {
  constructor() {
    super(...arguments), this.files = new Mw(this._client);
  }
  create(e, t) {
    return this._client.post("/containers", {
      body: e,
      ...t
    });
  }
  retrieve(e, t) {
    return this._client.get(Ae`/containers/${e}`, t);
  }
  list(e = {}, t) {
    return this._client.getAPIList("/containers", nn, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(Ae`/containers/${e}`, {
      ...t,
      headers: Xe([{ Accept: "*/*" }, t?.headers])
    });
  }
};
Pw.Files = Mw;
var m1 = class extends et {
  create(e, t, n) {
    const { include: r, ...s } = t;
    return this._client.post(Ae`/conversations/${e}/items`, {
      query: { include: r },
      body: s,
      ...n
    });
  }
  retrieve(e, t, n) {
    const { conversation_id: r, ...s } = t;
    return this._client.get(Ae`/conversations/${r}/items/${e}`, {
      query: s,
      ...n
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Ae`/conversations/${e}/items`, Kc, {
      query: t,
      ...n
    });
  }
  delete(e, t, n) {
    const { conversation_id: r } = t;
    return this._client.delete(Ae`/conversations/${r}/items/${e}`, n);
  }
}, Rw = class extends et {
  constructor() {
    super(...arguments), this.items = new m1(this._client);
  }
  create(e = {}, t) {
    return this._client.post("/conversations", {
      body: e,
      ...t
    });
  }
  retrieve(e, t) {
    return this._client.get(Ae`/conversations/${e}`, t);
  }
  update(e, t, n) {
    return this._client.post(Ae`/conversations/${e}`, {
      body: t,
      ...n
    });
  }
  delete(e, t) {
    return this._client.delete(Ae`/conversations/${e}`, t);
  }
};
Rw.Items = m1;
var g1 = class extends et {
  create(e, t) {
    const n = !!e.encoding_format;
    let r = n ? e.encoding_format : "base64";
    n && Cn(this._client).debug("embeddings/user defined encoding_format:", e.encoding_format);
    const s = this._client.post("/embeddings", {
      body: {
        ...e,
        encoding_format: r
      },
      ...t
    });
    return n ? s : (Cn(this._client).debug("embeddings/decoding base64 embeddings from base64"), s._thenUnwrap((o) => (o && o.data && o.data.forEach((i) => {
      const a = i.embedding;
      i.embedding = k8(a);
    }), o)));
  }
}, v1 = class extends et {
  retrieve(e, t, n) {
    const { eval_id: r, run_id: s } = t;
    return this._client.get(Ae`/evals/${r}/runs/${s}/output_items/${e}`, n);
  }
  list(e, t, n) {
    const { eval_id: r, ...s } = t;
    return this._client.getAPIList(Ae`/evals/${r}/runs/${e}/output_items`, nn, {
      query: s,
      ...n
    });
  }
}, Nw = class extends et {
  constructor() {
    super(...arguments), this.outputItems = new v1(this._client);
  }
  create(e, t, n) {
    return this._client.post(Ae`/evals/${e}/runs`, {
      body: t,
      ...n
    });
  }
  retrieve(e, t, n) {
    const { eval_id: r } = t;
    return this._client.get(Ae`/evals/${r}/runs/${e}`, n);
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Ae`/evals/${e}/runs`, nn, {
      query: t,
      ...n
    });
  }
  delete(e, t, n) {
    const { eval_id: r } = t;
    return this._client.delete(Ae`/evals/${r}/runs/${e}`, n);
  }
  cancel(e, t, n) {
    const { eval_id: r } = t;
    return this._client.post(Ae`/evals/${r}/runs/${e}`, n);
  }
};
Nw.OutputItems = v1;
var $w = class extends et {
  constructor() {
    super(...arguments), this.runs = new Nw(this._client);
  }
  create(e, t) {
    return this._client.post("/evals", {
      body: e,
      ...t
    });
  }
  retrieve(e, t) {
    return this._client.get(Ae`/evals/${e}`, t);
  }
  update(e, t, n) {
    return this._client.post(Ae`/evals/${e}`, {
      body: t,
      ...n
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/evals", nn, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(Ae`/evals/${e}`, t);
  }
};
$w.Runs = Nw;
var y1 = class extends et {
  create(e, t) {
    return this._client.post("/files", Ps({
      body: e,
      ...t
    }, this._client));
  }
  retrieve(e, t) {
    return this._client.get(Ae`/files/${e}`, t);
  }
  list(e = {}, t) {
    return this._client.getAPIList("/files", nn, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(Ae`/files/${e}`, t);
  }
  content(e, t) {
    return this._client.get(Ae`/files/${e}/content`, {
      ...t,
      headers: Xe([{ Accept: "application/binary" }, t?.headers]),
      __binaryResponse: !0
    });
  }
  async waitForProcessing(e, { pollInterval: t = 5e3, maxWait: n = 1800 * 1e3 } = {}) {
    const r = /* @__PURE__ */ new Set([
      "processed",
      "error",
      "deleted"
    ]), s = Date.now();
    let o = await this.retrieve(e);
    for (; !o.status || !r.has(o.status); )
      if (await hd(t), o = await this.retrieve(e), Date.now() - s > n) throw new bw({ message: `Giving up on waiting for file ${e} to finish processing after ${n} milliseconds.` });
    return o;
  }
}, _1 = class extends et {
}, b1 = class extends et {
  run(e, t) {
    return this._client.post("/fine_tuning/alpha/graders/run", {
      body: e,
      ...t
    });
  }
  validate(e, t) {
    return this._client.post("/fine_tuning/alpha/graders/validate", {
      body: e,
      ...t
    });
  }
}, Dw = class extends et {
  constructor() {
    super(...arguments), this.graders = new b1(this._client);
  }
};
Dw.Graders = b1;
var w1 = class extends et {
  create(e, t, n) {
    return this._client.getAPIList(Ae`/fine_tuning/checkpoints/${e}/permissions`, im, {
      body: t,
      method: "post",
      ...n
    });
  }
  retrieve(e, t = {}, n) {
    return this._client.get(Ae`/fine_tuning/checkpoints/${e}/permissions`, {
      query: t,
      ...n
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Ae`/fine_tuning/checkpoints/${e}/permissions`, Kc, {
      query: t,
      ...n
    });
  }
  delete(e, t, n) {
    const { fine_tuned_model_checkpoint: r } = t;
    return this._client.delete(Ae`/fine_tuning/checkpoints/${r}/permissions/${e}`, n);
  }
}, Lw = class extends et {
  constructor() {
    super(...arguments), this.permissions = new w1(this._client);
  }
};
Lw.Permissions = w1;
var S1 = class extends et {
  list(e, t = {}, n) {
    return this._client.getAPIList(Ae`/fine_tuning/jobs/${e}/checkpoints`, nn, {
      query: t,
      ...n
    });
  }
}, Ow = class extends et {
  constructor() {
    super(...arguments), this.checkpoints = new S1(this._client);
  }
  create(e, t) {
    return this._client.post("/fine_tuning/jobs", {
      body: e,
      ...t
    });
  }
  retrieve(e, t) {
    return this._client.get(Ae`/fine_tuning/jobs/${e}`, t);
  }
  list(e = {}, t) {
    return this._client.getAPIList("/fine_tuning/jobs", nn, {
      query: e,
      ...t
    });
  }
  cancel(e, t) {
    return this._client.post(Ae`/fine_tuning/jobs/${e}/cancel`, t);
  }
  listEvents(e, t = {}, n) {
    return this._client.getAPIList(Ae`/fine_tuning/jobs/${e}/events`, nn, {
      query: t,
      ...n
    });
  }
  pause(e, t) {
    return this._client.post(Ae`/fine_tuning/jobs/${e}/pause`, t);
  }
  resume(e, t) {
    return this._client.post(Ae`/fine_tuning/jobs/${e}/resume`, t);
  }
};
Ow.Checkpoints = S1;
var ql = class extends et {
  constructor() {
    super(...arguments), this.methods = new _1(this._client), this.jobs = new Ow(this._client), this.checkpoints = new Lw(this._client), this.alpha = new Dw(this._client);
  }
};
ql.Methods = _1;
ql.Jobs = Ow;
ql.Checkpoints = Lw;
ql.Alpha = Dw;
var T1 = class extends et {
}, Fw = class extends et {
  constructor() {
    super(...arguments), this.graderModels = new T1(this._client);
  }
};
Fw.GraderModels = T1;
var E1 = class extends et {
  createVariation(e, t) {
    return this._client.post("/images/variations", Ps({
      body: e,
      ...t
    }, this._client));
  }
  edit(e, t) {
    return this._client.post("/images/edits", Ps({
      body: e,
      ...t,
      stream: e.stream ?? !1
    }, this._client));
  }
  generate(e, t) {
    return this._client.post("/images/generations", {
      body: e,
      ...t,
      stream: e.stream ?? !1
    });
  }
}, C1 = class extends et {
  retrieve(e, t) {
    return this._client.get(Ae`/models/${e}`, t);
  }
  list(e) {
    return this._client.getAPIList("/models", im, e);
  }
  delete(e, t) {
    return this._client.delete(Ae`/models/${e}`, t);
  }
}, A1 = class extends et {
  create(e, t) {
    return this._client.post("/moderations", {
      body: e,
      ...t
    });
  }
}, I1 = class extends et {
  accept(e, t, n) {
    return this._client.post(Ae`/realtime/calls/${e}/accept`, {
      body: t,
      ...n,
      headers: Xe([{ Accept: "*/*" }, n?.headers])
    });
  }
  hangup(e, t) {
    return this._client.post(Ae`/realtime/calls/${e}/hangup`, {
      ...t,
      headers: Xe([{ Accept: "*/*" }, t?.headers])
    });
  }
  refer(e, t, n) {
    return this._client.post(Ae`/realtime/calls/${e}/refer`, {
      body: t,
      ...n,
      headers: Xe([{ Accept: "*/*" }, n?.headers])
    });
  }
  reject(e, t = {}, n) {
    return this._client.post(Ae`/realtime/calls/${e}/reject`, {
      body: t,
      ...n,
      headers: Xe([{ Accept: "*/*" }, n?.headers])
    });
  }
}, x1 = class extends et {
  create(e, t) {
    return this._client.post("/realtime/client_secrets", {
      body: e,
      ...t
    });
  }
}, dm = class extends et {
  constructor() {
    super(...arguments), this.clientSecrets = new x1(this._client), this.calls = new I1(this._client);
  }
};
dm.ClientSecrets = x1;
dm.Calls = I1;
function M8(e, t) {
  return !t || !R8(t) ? {
    ...e,
    output_parsed: null,
    output: e.output.map((n) => n.type === "function_call" ? {
      ...n,
      parsed_arguments: null
    } : n.type === "message" ? {
      ...n,
      content: n.content.map((r) => ({
        ...r,
        parsed: null
      }))
    } : n)
  } : k1(e, t);
}
function k1(e, t) {
  const n = e.output.map((s) => {
    if (s.type === "function_call") return {
      ...s,
      parsed_arguments: D8(t, s)
    };
    if (s.type === "message") {
      const o = s.content.map((i) => i.type === "output_text" ? {
        ...i,
        parsed: P8(t, i.text)
      } : i);
      return {
        ...s,
        content: o
      };
    }
    return s;
  }), r = Object.assign({}, e, { output: n });
  return Object.getOwnPropertyDescriptor(e, "output_text") || w_(r), Object.defineProperty(r, "output_parsed", {
    enumerable: !0,
    get() {
      for (const s of r.output)
        if (s.type === "message") {
          for (const o of s.content) if (o.type === "output_text" && o.parsed !== null) return o.parsed;
        }
      return null;
    }
  }), r;
}
function P8(e, t) {
  return e.text?.format?.type !== "json_schema" ? null : "$parseRaw" in e.text?.format ? (e.text?.format).$parseRaw(t) : JSON.parse(t);
}
function R8(e) {
  return !!Ew(e.text?.format);
}
function N8(e) {
  return e?.$brand === "auto-parseable-tool";
}
function $8(e, t) {
  return e.find((n) => n.type === "function" && n.name === t);
}
function D8(e, t) {
  const n = $8(e.tools ?? [], t.name);
  return {
    ...t,
    ...t,
    parsed_arguments: N8(n) ? n.$parseRaw(t.arguments) : n?.strict ? JSON.parse(t.arguments) : null
  };
}
function w_(e) {
  const t = [];
  for (const n of e.output)
    if (n.type === "message")
      for (const r of n.content) r.type === "output_text" && t.push(r.text);
  e.output_text = t.join("");
}
var La, Lf, Po, Of, _A, bA, wA, SA, L8 = class M1 extends Aw {
  constructor(t) {
    super(), La.add(this), Lf.set(this, void 0), Po.set(this, void 0), Of.set(this, void 0), ht(this, Lf, t, "f");
  }
  static createResponse(t, n, r) {
    const s = new M1(n);
    return s._run(() => s._createOrRetrieveResponse(t, n, {
      ...r,
      headers: {
        ...r?.headers,
        "X-Stainless-Helper-Method": "stream"
      }
    })), s;
  }
  async _createOrRetrieveResponse(t, n, r) {
    const s = r?.signal;
    s && (s.aborted && this.controller.abort(), s.addEventListener("abort", () => this.controller.abort())), de(this, La, "m", _A).call(this);
    let o, i = null;
    "response_id" in n ? (o = await t.responses.retrieve(n.response_id, { stream: !0 }, {
      ...r,
      signal: this.controller.signal,
      stream: !0
    }), i = n.starting_after ?? null) : o = await t.responses.create({
      ...n,
      stream: !0
    }, {
      ...r,
      signal: this.controller.signal
    }), this._connected();
    for await (const a of o) de(this, La, "m", bA).call(this, a, i);
    if (o.controller.signal?.aborted) throw new Kr();
    return de(this, La, "m", wA).call(this);
  }
  [(Lf = /* @__PURE__ */ new WeakMap(), Po = /* @__PURE__ */ new WeakMap(), Of = /* @__PURE__ */ new WeakMap(), La = /* @__PURE__ */ new WeakSet(), _A = function() {
    this.ended || ht(this, Po, void 0, "f");
  }, bA = function(n, r) {
    if (this.ended) return;
    const s = (i, a) => {
      (r == null || a.sequence_number > r) && this._emit(i, a);
    }, o = de(this, La, "m", SA).call(this, n);
    switch (s("event", n), n.type) {
      case "response.output_text.delta": {
        const i = o.output[n.output_index];
        if (!i) throw new st(`missing output at index ${n.output_index}`);
        if (i.type === "message") {
          const a = i.content[n.content_index];
          if (!a) throw new st(`missing content at index ${n.content_index}`);
          if (a.type !== "output_text") throw new st(`expected content to be 'output_text', got ${a.type}`);
          s("response.output_text.delta", {
            ...n,
            snapshot: a.text
          });
        }
        break;
      }
      case "response.function_call_arguments.delta": {
        const i = o.output[n.output_index];
        if (!i) throw new st(`missing output at index ${n.output_index}`);
        i.type === "function_call" && s("response.function_call_arguments.delta", {
          ...n,
          snapshot: i.arguments
        });
        break;
      }
      default:
        s(n.type, n);
        break;
    }
  }, wA = function() {
    if (this.ended) throw new st("stream has ended, this shouldn't happen");
    const n = de(this, Po, "f");
    if (!n) throw new st("request ended without sending any events");
    ht(this, Po, void 0, "f");
    const r = O8(n, de(this, Lf, "f"));
    return ht(this, Of, r, "f"), r;
  }, SA = function(n) {
    let r = de(this, Po, "f");
    if (!r) {
      if (n.type !== "response.created") throw new st(`When snapshot hasn't been set yet, expected 'response.created' event, got ${n.type}`);
      return r = ht(this, Po, n.response, "f"), r;
    }
    switch (n.type) {
      case "response.output_item.added":
        r.output.push(n.item);
        break;
      case "response.content_part.added": {
        const s = r.output[n.output_index];
        if (!s) throw new st(`missing output at index ${n.output_index}`);
        const o = s.type, i = n.part;
        o === "message" && i.type !== "reasoning_text" ? s.content.push(i) : o === "reasoning" && i.type === "reasoning_text" && (s.content || (s.content = []), s.content.push(i));
        break;
      }
      case "response.output_text.delta": {
        const s = r.output[n.output_index];
        if (!s) throw new st(`missing output at index ${n.output_index}`);
        if (s.type === "message") {
          const o = s.content[n.content_index];
          if (!o) throw new st(`missing content at index ${n.content_index}`);
          if (o.type !== "output_text") throw new st(`expected content to be 'output_text', got ${o.type}`);
          o.text += n.delta;
        }
        break;
      }
      case "response.function_call_arguments.delta": {
        const s = r.output[n.output_index];
        if (!s) throw new st(`missing output at index ${n.output_index}`);
        s.type === "function_call" && (s.arguments += n.delta);
        break;
      }
      case "response.reasoning_text.delta": {
        const s = r.output[n.output_index];
        if (!s) throw new st(`missing output at index ${n.output_index}`);
        if (s.type === "reasoning") {
          const o = s.content?.[n.content_index];
          if (!o) throw new st(`missing content at index ${n.content_index}`);
          if (o.type !== "reasoning_text") throw new st(`expected content to be 'reasoning_text', got ${o.type}`);
          o.text += n.delta;
        }
        break;
      }
      case "response.completed":
        ht(this, Po, n.response, "f");
        break;
    }
    return r;
  }, Symbol.asyncIterator)]() {
    const t = [], n = [];
    let r = !1;
    return this.on("event", (s) => {
      const o = n.shift();
      o ? o.resolve(s) : t.push(s);
    }), this.on("end", () => {
      r = !0;
      for (const s of n) s.resolve(void 0);
      n.length = 0;
    }), this.on("abort", (s) => {
      r = !0;
      for (const o of n) o.reject(s);
      n.length = 0;
    }), this.on("error", (s) => {
      r = !0;
      for (const o of n) o.reject(s);
      n.length = 0;
    }), {
      next: async () => t.length ? {
        value: t.shift(),
        done: !1
      } : r ? {
        value: void 0,
        done: !0
      } : new Promise((s, o) => n.push({
        resolve: s,
        reject: o
      })).then((s) => s ? {
        value: s,
        done: !1
      } : {
        value: void 0,
        done: !0
      }),
      return: async () => (this.abort(), {
        value: void 0,
        done: !0
      })
    };
  }
  async finalResponse() {
    await this.done();
    const t = de(this, Of, "f");
    if (!t) throw new st("stream ended without producing a ChatCompletion");
    return t;
  }
};
function O8(e, t) {
  return M8(e, t);
}
var P1 = class extends et {
  list(e, t = {}, n) {
    return this._client.getAPIList(Ae`/responses/${e}/input_items`, nn, {
      query: t,
      ...n
    });
  }
}, R1 = class extends et {
  count(e = {}, t) {
    return this._client.post("/responses/input_tokens", {
      body: e,
      ...t
    });
  }
}, fm = class extends et {
  constructor() {
    super(...arguments), this.inputItems = new P1(this._client), this.inputTokens = new R1(this._client);
  }
  create(e, t) {
    return this._client.post("/responses", {
      body: e,
      ...t,
      stream: e.stream ?? !1
    })._thenUnwrap((n) => ("object" in n && n.object === "response" && w_(n), n));
  }
  retrieve(e, t = {}, n) {
    return this._client.get(Ae`/responses/${e}`, {
      query: t,
      ...n,
      stream: t?.stream ?? !1
    })._thenUnwrap((r) => ("object" in r && r.object === "response" && w_(r), r));
  }
  delete(e, t) {
    return this._client.delete(Ae`/responses/${e}`, {
      ...t,
      headers: Xe([{ Accept: "*/*" }, t?.headers])
    });
  }
  parse(e, t) {
    return this._client.responses.create(e, t)._thenUnwrap((n) => k1(n, e));
  }
  stream(e, t) {
    return L8.createResponse(this._client, e, t);
  }
  cancel(e, t) {
    return this._client.post(Ae`/responses/${e}/cancel`, t);
  }
  compact(e, t) {
    return this._client.post("/responses/compact", {
      body: e,
      ...t
    });
  }
};
fm.InputItems = P1;
fm.InputTokens = R1;
var N1 = class extends et {
  retrieve(e, t) {
    return this._client.get(Ae`/skills/${e}/content`, {
      ...t,
      headers: Xe([{ Accept: "application/binary" }, t?.headers]),
      __binaryResponse: !0
    });
  }
}, $1 = class extends et {
  retrieve(e, t, n) {
    const { skill_id: r } = t;
    return this._client.get(Ae`/skills/${r}/versions/${e}/content`, {
      ...n,
      headers: Xe([{ Accept: "application/binary" }, n?.headers]),
      __binaryResponse: !0
    });
  }
}, Bw = class extends et {
  constructor() {
    super(...arguments), this.content = new $1(this._client);
  }
  create(e, t = {}, n) {
    return this._client.post(Ae`/skills/${e}/versions`, am({
      body: t,
      ...n
    }, this._client));
  }
  retrieve(e, t, n) {
    const { skill_id: r } = t;
    return this._client.get(Ae`/skills/${r}/versions/${e}`, n);
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Ae`/skills/${e}/versions`, nn, {
      query: t,
      ...n
    });
  }
  delete(e, t, n) {
    const { skill_id: r } = t;
    return this._client.delete(Ae`/skills/${r}/versions/${e}`, n);
  }
};
Bw.Content = $1;
var hm = class extends et {
  constructor() {
    super(...arguments), this.content = new N1(this._client), this.versions = new Bw(this._client);
  }
  create(e = {}, t) {
    return this._client.post("/skills", am({
      body: e,
      ...t
    }, this._client));
  }
  retrieve(e, t) {
    return this._client.get(Ae`/skills/${e}`, t);
  }
  update(e, t, n) {
    return this._client.post(Ae`/skills/${e}`, {
      body: t,
      ...n
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/skills", nn, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(Ae`/skills/${e}`, t);
  }
};
hm.Content = N1;
hm.Versions = Bw;
var D1 = class extends et {
  create(e, t, n) {
    return this._client.post(Ae`/uploads/${e}/parts`, Ps({
      body: t,
      ...n
    }, this._client));
  }
}, Uw = class extends et {
  constructor() {
    super(...arguments), this.parts = new D1(this._client);
  }
  create(e, t) {
    return this._client.post("/uploads", {
      body: e,
      ...t
    });
  }
  cancel(e, t) {
    return this._client.post(Ae`/uploads/${e}/cancel`, t);
  }
  complete(e, t, n) {
    return this._client.post(Ae`/uploads/${e}/complete`, {
      body: t,
      ...n
    });
  }
};
Uw.Parts = D1;
var F8 = async (e) => {
  const t = await Promise.allSettled(e), n = t.filter((s) => s.status === "rejected");
  if (n.length) {
    for (const s of n) console.error(s.reason);
    throw new Error(`${n.length} promise(s) failed - see the above errors`);
  }
  const r = [];
  for (const s of t) s.status === "fulfilled" && r.push(s.value);
  return r;
}, L1 = class extends et {
  create(e, t, n) {
    return this._client.post(Ae`/vector_stores/${e}/file_batches`, {
      body: t,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  retrieve(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.get(Ae`/vector_stores/${r}/file_batches/${e}`, {
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  cancel(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.post(Ae`/vector_stores/${r}/file_batches/${e}/cancel`, {
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  async createAndPoll(e, t, n) {
    const r = await this.create(e, t);
    return await this.poll(e, r.id, n);
  }
  listFiles(e, t, n) {
    const { vector_store_id: r, ...s } = t;
    return this._client.getAPIList(Ae`/vector_stores/${r}/file_batches/${e}/files`, nn, {
      query: s,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  async poll(e, t, n) {
    const r = Xe([n?.headers, {
      "X-Stainless-Poll-Helper": "true",
      "X-Stainless-Custom-Poll-Interval": n?.pollIntervalMs?.toString() ?? void 0
    }]);
    for (; ; ) {
      const { data: s, response: o } = await this.retrieve(t, { vector_store_id: e }, {
        ...n,
        headers: r
      }).withResponse();
      switch (s.status) {
        case "in_progress":
          let i = 5e3;
          if (n?.pollIntervalMs) i = n.pollIntervalMs;
          else {
            const a = o.headers.get("openai-poll-after-ms");
            if (a) {
              const l = parseInt(a);
              isNaN(l) || (i = l);
            }
          }
          await hd(i);
          break;
        case "failed":
        case "cancelled":
        case "completed":
          return s;
      }
    }
  }
  async uploadAndPoll(e, { files: t, fileIds: n = [] }, r) {
    if (t == null || t.length == 0) throw new Error("No `files` provided to process. If you've already uploaded files you should use `.createAndPoll()` instead");
    const s = r?.maxConcurrency ?? 5, o = Math.min(s, t.length), i = this._client, a = t.values(), l = [...n];
    async function u(d) {
      for (let h of d) {
        const p = await i.files.create({
          file: h,
          purpose: "assistants"
        }, r);
        l.push(p.id);
      }
    }
    return await F8(Array(o).fill(a).map(u)), await this.createAndPoll(e, { file_ids: l });
  }
}, O1 = class extends et {
  create(e, t, n) {
    return this._client.post(Ae`/vector_stores/${e}/files`, {
      body: t,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  retrieve(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.get(Ae`/vector_stores/${r}/files/${e}`, {
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  update(e, t, n) {
    const { vector_store_id: r, ...s } = t;
    return this._client.post(Ae`/vector_stores/${r}/files/${e}`, {
      body: s,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  list(e, t = {}, n) {
    return this._client.getAPIList(Ae`/vector_stores/${e}/files`, nn, {
      query: t,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  delete(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.delete(Ae`/vector_stores/${r}/files/${e}`, {
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  async createAndPoll(e, t, n) {
    const r = await this.create(e, t, n);
    return await this.poll(e, r.id, n);
  }
  async poll(e, t, n) {
    const r = Xe([n?.headers, {
      "X-Stainless-Poll-Helper": "true",
      "X-Stainless-Custom-Poll-Interval": n?.pollIntervalMs?.toString() ?? void 0
    }]);
    for (; ; ) {
      const s = await this.retrieve(t, { vector_store_id: e }, {
        ...n,
        headers: r
      }).withResponse(), o = s.data;
      switch (o.status) {
        case "in_progress":
          let i = 5e3;
          if (n?.pollIntervalMs) i = n.pollIntervalMs;
          else {
            const a = s.response.headers.get("openai-poll-after-ms");
            if (a) {
              const l = parseInt(a);
              isNaN(l) || (i = l);
            }
          }
          await hd(i);
          break;
        case "failed":
        case "completed":
          return o;
      }
    }
  }
  async upload(e, t, n) {
    const r = await this._client.files.create({
      file: t,
      purpose: "assistants"
    }, n);
    return this.create(e, { file_id: r.id }, n);
  }
  async uploadAndPoll(e, t, n) {
    const r = await this.upload(e, t, n);
    return await this.poll(e, r.id, n);
  }
  content(e, t, n) {
    const { vector_store_id: r } = t;
    return this._client.getAPIList(Ae`/vector_stores/${r}/files/${e}/content`, im, {
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
}, pm = class extends et {
  constructor() {
    super(...arguments), this.files = new O1(this._client), this.fileBatches = new L1(this._client);
  }
  create(e, t) {
    return this._client.post("/vector_stores", {
      body: e,
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  retrieve(e, t) {
    return this._client.get(Ae`/vector_stores/${e}`, {
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  update(e, t, n) {
    return this._client.post(Ae`/vector_stores/${e}`, {
      body: t,
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
  list(e = {}, t) {
    return this._client.getAPIList("/vector_stores", nn, {
      query: e,
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  delete(e, t) {
    return this._client.delete(Ae`/vector_stores/${e}`, {
      ...t,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, t?.headers])
    });
  }
  search(e, t, n) {
    return this._client.getAPIList(Ae`/vector_stores/${e}/search`, im, {
      body: t,
      method: "post",
      ...n,
      headers: Xe([{ "OpenAI-Beta": "assistants=v2" }, n?.headers])
    });
  }
};
pm.Files = O1;
pm.FileBatches = L1;
var F1 = class extends et {
  create(e, t) {
    return this._client.post("/videos", Ps({
      body: e,
      ...t
    }, this._client));
  }
  retrieve(e, t) {
    return this._client.get(Ae`/videos/${e}`, t);
  }
  list(e = {}, t) {
    return this._client.getAPIList("/videos", Kc, {
      query: e,
      ...t
    });
  }
  delete(e, t) {
    return this._client.delete(Ae`/videos/${e}`, t);
  }
  createCharacter(e, t) {
    return this._client.post("/videos/characters", Ps({
      body: e,
      ...t
    }, this._client));
  }
  downloadContent(e, t = {}, n) {
    return this._client.get(Ae`/videos/${e}/content`, {
      query: t,
      ...n,
      headers: Xe([{ Accept: "application/binary" }, n?.headers]),
      __binaryResponse: !0
    });
  }
  edit(e, t) {
    return this._client.post("/videos/edits", Ps({
      body: e,
      ...t
    }, this._client));
  }
  extend(e, t) {
    return this._client.post("/videos/extensions", Ps({
      body: e,
      ...t
    }, this._client));
  }
  getCharacter(e, t) {
    return this._client.get(Ae`/videos/characters/${e}`, t);
  }
  remix(e, t, n) {
    return this._client.post(Ae`/videos/${e}/remix`, am({
      body: t,
      ...n
    }, this._client));
  }
}, Ja, B1, hh, U1 = class extends et {
  constructor() {
    super(...arguments), Ja.add(this);
  }
  async unwrap(e, t, n = this._client.webhookSecret, r = 300) {
    return await this.verifySignature(e, t, n, r), JSON.parse(e);
  }
  async verifySignature(e, t, n = this._client.webhookSecret, r = 300) {
    if (typeof crypto > "u" || typeof crypto.subtle.importKey != "function" || typeof crypto.subtle.verify != "function") throw new Error("Webhook signature verification is only supported when the `crypto` global is defined");
    de(this, Ja, "m", B1).call(this, n);
    const s = Xe([t]).values, o = de(this, Ja, "m", hh).call(this, s, "webhook-signature"), i = de(this, Ja, "m", hh).call(this, s, "webhook-timestamp"), a = de(this, Ja, "m", hh).call(this, s, "webhook-id"), l = parseInt(i, 10);
    if (isNaN(l)) throw new Ju("Invalid webhook timestamp format");
    const u = Math.floor(Date.now() / 1e3);
    if (u - l > r) throw new Ju("Webhook timestamp is too old");
    if (l > u + r) throw new Ju("Webhook timestamp is too new");
    const d = o.split(" ").map((w) => w.startsWith("v1,") ? w.substring(3) : w), h = n.startsWith("whsec_") ? Buffer.from(n.replace("whsec_", ""), "base64") : Buffer.from(n, "utf-8"), p = a ? `${a}.${i}.${e}` : `${i}.${e}`, y = await crypto.subtle.importKey("raw", h, {
      name: "HMAC",
      hash: "SHA-256"
    }, !1, ["verify"]);
    for (const w of d) try {
      const E = Buffer.from(w, "base64");
      if (await crypto.subtle.verify("HMAC", y, E, new TextEncoder().encode(p))) return;
    } catch {
      continue;
    }
    throw new Ju("The given webhook signature does not match the expected signature");
  }
};
Ja = /* @__PURE__ */ new WeakSet(), B1 = function(t) {
  if (typeof t != "string" || t.length === 0) throw new Error("The webhook secret must either be set using the env var, OPENAI_WEBHOOK_SECRET, on the client class, OpenAI({ webhookSecret: '123' }), or passed to this function");
}, hh = function(t, n) {
  if (!t) throw new Error("Headers are required");
  const r = t.get(n);
  if (r == null) throw new Error(`Missing required header: ${n}`);
  return r;
};
var S_, qw, ph, q1, bv = "workload-identity-auth", Tt = class {
  constructor({ baseURL: e = Da("OPENAI_BASE_URL"), apiKey: t = Da("OPENAI_API_KEY"), organization: n = Da("OPENAI_ORG_ID") ?? null, project: r = Da("OPENAI_PROJECT_ID") ?? null, webhookSecret: s = Da("OPENAI_WEBHOOK_SECRET") ?? null, workloadIdentity: o, ...i } = {}) {
    if (S_.add(this), ph.set(this, void 0), this.completions = new h1(this), this.chat = new xw(this), this.embeddings = new g1(this), this.files = new y1(this), this.images = new E1(this), this.audio = new md(this), this.moderations = new A1(this), this.models = new C1(this), this.fineTuning = new ql(this), this.graders = new Fw(this), this.vectorStores = new pm(this), this.webhooks = new U1(this), this.beta = new Ul(this), this.batches = new o1(this), this.uploads = new Uw(this), this.responses = new fm(this), this.realtime = new dm(this), this.conversations = new Rw(this), this.evals = new $w(this), this.containers = new Pw(this), this.skills = new hm(this), this.videos = new F1(this), o) {
      if (t && t !== bv) throw new st("The `apiKey` and `workloadIdentity` arguments are mutually exclusive; only one can be passed at a time.");
      t = bv;
    } else if (t === void 0) throw new st("Missing credentials. Please pass an `apiKey`, `workloadIdentity`, or set the `OPENAI_API_KEY` environment variable.");
    const a = {
      apiKey: t,
      organization: n,
      project: r,
      webhookSecret: s,
      workloadIdentity: o,
      ...i,
      baseURL: e || "https://api.openai.com/v1"
    };
    if (!a.dangerouslyAllowBrowser && P6()) throw new st(`It looks like you're running in a browser-like environment.

This is disabled by default, as it risks exposing your secret API credentials to attackers.
If you understand the risks and have appropriate mitigations in place,
you can set the \`dangerouslyAllowBrowser\` option to \`true\`, e.g.,

new OpenAI({ apiKey, dangerouslyAllowBrowser: true });

https://help.openai.com/en/articles/5112595-best-practices-for-api-key-safety
`);
    this.baseURL = a.baseURL, this.timeout = a.timeout ?? qw.DEFAULT_TIMEOUT, this.logger = a.logger ?? console;
    const l = "warn";
    this.logLevel = l, this.logLevel = sA(a.logLevel, "ClientOptions.logLevel", this) ?? sA(Da("OPENAI_LOG"), "process.env['OPENAI_LOG']", this) ?? l, this.fetchOptions = a.fetchOptions, this.maxRetries = a.maxRetries ?? 2, this.fetch = a.fetch ?? SR(), ht(this, ph, L6, "f"), this._options = a, o && (this._workloadIdentityAuth = new e8(o, this.fetch)), this.apiKey = typeof t == "string" ? t : "Missing Key", this.organization = n, this.project = r, this.webhookSecret = s;
  }
  withOptions(e) {
    return new this.constructor({
      ...this._options,
      baseURL: this.baseURL,
      maxRetries: this.maxRetries,
      timeout: this.timeout,
      logger: this.logger,
      logLevel: this.logLevel,
      fetch: this.fetch,
      fetchOptions: this.fetchOptions,
      apiKey: this.apiKey,
      workloadIdentity: this._options.workloadIdentity,
      organization: this.organization,
      project: this.project,
      webhookSecret: this.webhookSecret,
      ...e
    });
  }
  defaultQuery() {
    return this._options.defaultQuery;
  }
  validateHeaders({ values: e, nulls: t }) {
  }
  async authHeaders(e) {
    return Xe([{ Authorization: `Bearer ${this.apiKey}` }]);
  }
  stringifyQuery(e) {
    return H6(e);
  }
  getUserAgent() {
    return `${this.constructor.name}/JS ${Ka}`;
  }
  defaultIdempotencyKey() {
    return `stainless-node-retry-${uR()}`;
  }
  makeStatusError(e, t, n, r) {
    return Ln.generate(e, t, n, r);
  }
  async _callApiKey() {
    const e = this._options.apiKey;
    if (typeof e != "function") return !1;
    let t;
    try {
      t = await e();
    } catch (n) {
      throw n instanceof st ? n : new st(`Failed to get token from 'apiKey' function: ${n.message}`, { cause: n });
    }
    if (typeof t != "string" || !t) throw new st(`Expected 'apiKey' function argument to return a string but it returned ${t}`);
    return this.apiKey = t, !0;
  }
  buildURL(e, t, n) {
    const r = !de(this, S_, "m", q1).call(this) && n || this.baseURL, s = I6(e) ? new URL(e) : new URL(r + (r.endsWith("/") && e.startsWith("/") ? e.slice(1) : e)), o = this.defaultQuery(), i = Object.fromEntries(s.searchParams);
    return (!zC(o) || !zC(i)) && (t = {
      ...i,
      ...o,
      ...t
    }), typeof t == "object" && t && !Array.isArray(t) && (s.search = this.stringifyQuery(t)), s.toString();
  }
  async prepareOptions(e) {
    await this._callApiKey();
  }
  async prepareRequest(e, { url: t, options: n }) {
  }
  get(e, t) {
    return this.methodRequest("get", e, t);
  }
  post(e, t) {
    return this.methodRequest("post", e, t);
  }
  patch(e, t) {
    return this.methodRequest("patch", e, t);
  }
  put(e, t) {
    return this.methodRequest("put", e, t);
  }
  delete(e, t) {
    return this.methodRequest("delete", e, t);
  }
  methodRequest(e, t, n) {
    return this.request(Promise.resolve(n).then((r) => ({
      method: e,
      path: t,
      ...r
    })));
  }
  request(e, t = null) {
    return new NR(this, this.makeRequest(e, t, void 0));
  }
  async makeRequest(e, t, n) {
    const r = await e, s = r.maxRetries ?? this.maxRetries;
    t == null && (t = s), await this.prepareOptions(r);
    const { req: o, url: i, timeout: a } = await this.buildRequest(r, { retryCount: s - t });
    await this.prepareRequest(o, {
      url: i,
      options: r
    });
    const l = "log_" + (Math.random() * (1 << 24) | 0).toString(16).padStart(6, "0"), u = n === void 0 ? "" : `, retryOf: ${n}`, d = Date.now();
    if (Cn(this).debug(`[${l}] sending request`, Ni({
      retryOfRequestLogID: n,
      method: r.method,
      url: i,
      options: r,
      headers: o.headers
    })), r.signal?.aborted) throw new Kr();
    const h = new AbortController(), p = await this.fetchWithAuth(i, o, a, h).catch(a_), y = Date.now();
    if (p instanceof globalThis.Error) {
      const E = `retrying, ${t} attempts remaining`;
      if (r.signal?.aborted) throw new Kr();
      const C = i_(p) || /timed? ?out/i.test(String(p) + ("cause" in p ? String(p.cause) : ""));
      if (t)
        return Cn(this).info(`[${l}] connection ${C ? "timed out" : "failed"} - ${E}`), Cn(this).debug(`[${l}] connection ${C ? "timed out" : "failed"} (${E})`, Ni({
          retryOfRequestLogID: n,
          url: i,
          durationMs: y - d,
          message: p.message
        })), this.retryRequest(r, t, n ?? l);
      throw Cn(this).info(`[${l}] connection ${C ? "timed out" : "failed"} - error; no more retries left`), Cn(this).debug(`[${l}] connection ${C ? "timed out" : "failed"} (error; no more retries left)`, Ni({
        retryOfRequestLogID: n,
        url: i,
        durationMs: y - d,
        message: p.message
      })), p instanceof bR || p instanceof C6 ? p : C ? new bw() : new sm({ cause: p });
    }
    const w = `[${l}${u}${[...p.headers.entries()].filter(([E]) => E === "x-request-id").map(([E, C]) => ", " + E + ": " + JSON.stringify(C)).join("")}] ${o.method} ${i} ${p.ok ? "succeeded" : "failed"} with status ${p.status} in ${y - d}ms`;
    if (!p.ok) {
      if (p.status === 401 && this._workloadIdentityAuth && !r.__metadata?.hasStreamingBody && !r.__metadata?.workloadIdentityTokenRefreshed)
        return await QC(p.body), this._workloadIdentityAuth.invalidateToken(), this.makeRequest({
          ...r,
          __metadata: {
            ...r.__metadata,
            workloadIdentityTokenRefreshed: !0
          }
        }, t, n ?? l);
      const E = await this.shouldRetry(p);
      if (t && E) {
        const D = `retrying, ${t} attempts remaining`;
        return await QC(p.body), Cn(this).info(`${w} - ${D}`), Cn(this).debug(`[${l}] response error (${D})`, Ni({
          retryOfRequestLogID: n,
          url: p.url,
          status: p.status,
          headers: p.headers,
          durationMs: y - d
        })), this.retryRequest(r, t, n ?? l, p.headers);
      }
      const C = E ? "error; no more retries left" : "error; not retryable";
      Cn(this).info(`${w} - ${C}`);
      const M = await p.text().catch((D) => a_(D).message), k = M6(M), R = k ? void 0 : M;
      throw Cn(this).debug(`[${l}] response error (${C})`, Ni({
        retryOfRequestLogID: n,
        url: p.url,
        status: p.status,
        headers: p.headers,
        message: R,
        durationMs: Date.now() - d
      })), this.makeStatusError(p.status, k, R, p.headers);
    }
    return Cn(this).info(w), Cn(this).debug(`[${l}] response start`, Ni({
      retryOfRequestLogID: n,
      url: p.url,
      status: p.status,
      headers: p.headers,
      durationMs: y - d
    })), {
      response: p,
      options: r,
      controller: h,
      requestLogID: l,
      retryOfRequestLogID: n,
      startTime: d
    };
  }
  getAPIList(e, t, n) {
    return this.requestAPIList(t, n && "then" in n ? n.then((r) => ({
      method: "get",
      path: e,
      ...r
    })) : {
      method: "get",
      path: e,
      ...n
    });
  }
  requestAPIList(e, t) {
    const n = this.makeRequest(t, null, void 0);
    return new Q6(this, n, e);
  }
  async fetchWithAuth(e, t, n, r) {
    if (this._workloadIdentityAuth) {
      const s = t.headers, o = s.get("Authorization");
      if (!o || o === `Bearer ${bv}`) {
        const i = await this._workloadIdentityAuth.getToken();
        s.set("Authorization", `Bearer ${i}`);
      }
    }
    return await this.fetchWithTimeout(e, t, n, r);
  }
  async fetchWithTimeout(e, t, n, r) {
    const { signal: s, method: o, ...i } = t || {}, a = this._makeAbort(r);
    s && s.addEventListener("abort", a, { once: !0 });
    const l = setTimeout(a, n), u = globalThis.ReadableStream && i.body instanceof globalThis.ReadableStream || typeof i.body == "object" && i.body !== null && Symbol.asyncIterator in i.body, d = {
      signal: r.signal,
      ...u ? { duplex: "half" } : {},
      method: "GET",
      ...i
    };
    o && (d.method = o.toUpperCase());
    try {
      return await this.fetch.call(void 0, e, d);
    } finally {
      clearTimeout(l);
    }
  }
  async shouldRetry(e) {
    const t = e.headers.get("x-should-retry");
    return t === "true" ? !0 : t === "false" ? !1 : e.status === 408 || e.status === 409 || e.status === 429 || e.status >= 500;
  }
  async retryRequest(e, t, n, r) {
    let s;
    const o = r?.get("retry-after-ms");
    if (o) {
      const a = parseFloat(o);
      Number.isNaN(a) || (s = a);
    }
    const i = r?.get("retry-after");
    if (i && !s) {
      const a = parseFloat(i);
      Number.isNaN(a) ? s = Date.parse(i) - Date.now() : s = a * 1e3;
    }
    if (s === void 0) {
      const a = e.maxRetries ?? this.maxRetries;
      s = this.calculateDefaultRetryTimeoutMillis(t, a);
    }
    return await hd(s), this.makeRequest(e, t - 1, n);
  }
  calculateDefaultRetryTimeoutMillis(e, t) {
    const s = t - e;
    return Math.min(0.5 * Math.pow(2, s), 8) * (1 - Math.random() * 0.25) * 1e3;
  }
  async buildRequest(e, { retryCount: t = 0 } = {}) {
    const n = { ...e }, { method: r, path: s, query: o, defaultBaseURL: i } = n, a = this.buildURL(s, o, i);
    "timeout" in n && k6("timeout", n.timeout), n.timeout = n.timeout ?? this.timeout;
    const { bodyHeaders: l, body: u, isStreamingBody: d } = this.buildBody({ options: n });
    return d && (e.__metadata = {
      ...e.__metadata,
      hasStreamingBody: !0
    }), {
      req: {
        method: r,
        headers: await this.buildHeaders({
          options: e,
          method: r,
          bodyHeaders: l,
          retryCount: t
        }),
        ...n.signal && { signal: n.signal },
        ...globalThis.ReadableStream && u instanceof globalThis.ReadableStream && { duplex: "half" },
        ...u && { body: u },
        ...this.fetchOptions ?? {},
        ...n.fetchOptions ?? {}
      },
      url: a,
      timeout: n.timeout
    };
  }
  async buildHeaders({ options: e, method: t, bodyHeaders: n, retryCount: r }) {
    let s = {};
    this.idempotencyHeader && t !== "get" && (e.idempotencyKey || (e.idempotencyKey = this.defaultIdempotencyKey()), s[this.idempotencyHeader] = e.idempotencyKey);
    const o = Xe([
      s,
      {
        Accept: "application/json",
        "User-Agent": this.getUserAgent(),
        "X-Stainless-Retry-Count": String(r),
        ...e.timeout ? { "X-Stainless-Timeout": String(Math.trunc(e.timeout / 1e3)) } : {},
        ...D6(),
        "OpenAI-Organization": this.organization,
        "OpenAI-Project": this.project
      },
      await this.authHeaders(e),
      this._options.defaultHeaders,
      n,
      e.headers
    ]);
    return this.validateHeaders(o), o.values;
  }
  _makeAbort(e) {
    return () => e.abort();
  }
  buildBody({ options: { body: e, headers: t } }) {
    if (!e) return {
      bodyHeaders: void 0,
      body: void 0,
      isStreamingBody: !1
    };
    const n = Xe([t]), r = typeof globalThis.ReadableStream < "u" && e instanceof globalThis.ReadableStream, s = !r && (typeof e == "string" || e instanceof ArrayBuffer || ArrayBuffer.isView(e) || typeof globalThis.Blob < "u" && e instanceof globalThis.Blob || e instanceof URLSearchParams || e instanceof FormData);
    return ArrayBuffer.isView(e) || e instanceof ArrayBuffer || e instanceof DataView || typeof e == "string" && n.values.has("content-type") || globalThis.Blob && e instanceof globalThis.Blob || e instanceof FormData || e instanceof URLSearchParams || r ? {
      bodyHeaders: void 0,
      body: e,
      isStreamingBody: !s
    } : typeof e == "object" && (Symbol.asyncIterator in e || Symbol.iterator in e && "next" in e && typeof e.next == "function") ? {
      bodyHeaders: void 0,
      body: ER(e),
      isStreamingBody: !0
    } : typeof e == "object" && n.values.get("content-type") === "application/x-www-form-urlencoded" ? {
      bodyHeaders: { "content-type": "application/x-www-form-urlencoded" },
      body: this.stringifyQuery(e),
      isStreamingBody: !1
    } : {
      ...de(this, ph, "f").call(this, {
        body: e,
        headers: n
      }),
      isStreamingBody: !1
    };
  }
};
qw = Tt, ph = /* @__PURE__ */ new WeakMap(), S_ = /* @__PURE__ */ new WeakSet(), q1 = function() {
  return this.baseURL !== "https://api.openai.com/v1";
};
Tt.OpenAI = qw;
Tt.DEFAULT_TIMEOUT = 6e5;
Tt.OpenAIError = st;
Tt.APIError = Ln;
Tt.APIConnectionError = sm;
Tt.APIConnectionTimeoutError = bw;
Tt.APIUserAbortError = Kr;
Tt.NotFoundError = hR;
Tt.ConflictError = pR;
Tt.RateLimitError = gR;
Tt.BadRequestError = cR;
Tt.AuthenticationError = dR;
Tt.InternalServerError = vR;
Tt.PermissionDeniedError = fR;
Tt.UnprocessableEntityError = mR;
Tt.InvalidWebhookSignatureError = Ju;
Tt.toFile = o8;
Tt.Completions = h1;
Tt.Chat = xw;
Tt.Embeddings = g1;
Tt.Files = y1;
Tt.Images = E1;
Tt.Audio = md;
Tt.Moderations = A1;
Tt.Models = C1;
Tt.FineTuning = ql;
Tt.Graders = Fw;
Tt.VectorStores = pm;
Tt.Webhooks = U1;
Tt.Beta = Ul;
Tt.Batches = o1;
Tt.Uploads = Uw;
Tt.Responses = fm;
Tt.Realtime = dm;
Tt.Conversations = Rw;
Tt.Evals = $w;
Tt.Containers = Pw;
Tt.Skills = hm;
Tt.Videos = F1;
function TA(e = "", t = 0) {
  let n = 0;
  for (let r = t - 1; r >= 0 && e[r] === "\\"; r -= 1) n += 1;
  return n % 2 === 1;
}
function B8(e = "") {
  return /^[0-9a-fA-F]{4}$/.test(e);
}
function U8(e = "") {
  return /^[dD][89a-bA-B][0-9a-fA-F]{2}$/.test(e);
}
function q8(e = "") {
  return /^[dD][c-fC-F][0-9a-fA-F]{2}$/.test(e);
}
function H8(e = "") {
  const t = String(e ?? "");
  let n = "", r = 0;
  for (; r < t.length; ) {
    const s = t.slice(r, r + 2), o = t.slice(r + 2, r + 6);
    if (s !== "\\u" || TA(t, r) || !B8(o)) {
      n += t[r] || "", r += 1;
      continue;
    }
    const i = r + 6, a = t.slice(i + 2, i + 6);
    if (U8(o) && t.slice(i, i + 2) === "\\u" && !TA(t, i) && q8(a)) {
      const l = Number.parseInt(o, 16), u = Number.parseInt(a, 16), d = 65536 + (l - 55296 << 10) + (u - 56320);
      n += String.fromCodePoint(d), r += 12;
      continue;
    }
    n += String.fromCharCode(Number.parseInt(o, 16)), r += 6;
  }
  return n;
}
function G8(e = "") {
  let t = String(e ?? "").trim();
  return t.endsWith(",") && (t = t.slice(0, -1).trimEnd()), t.startsWith('\\"') && (t = t.slice(2)), t.endsWith('\\"') && (t = t.slice(0, -2)), t.startsWith('"') && (t = t.slice(1)), t.endsWith('"') && (t = t.slice(0, -1)), H8(t.replace(/\r\n/g, `
`).replace(/\\r/g, "\r").replace(/\\n/g, `
`).replace(/\\t/g, "	").replace(/\\"/g, '"')).replace(/\\\\/g, "\\");
}
function W8(e = "") {
  return String(e || "").replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function Hw(e = "", t = "", n = 0) {
  const r = new RegExp(`(^|[^A-Za-z0-9_])(?:\\\\?")?${W8(t)}(?:\\\\?")?\\s*:`, "i"), s = String(e || "").slice(Math.max(0, n)).match(r);
  if (!s || s.index === void 0) return null;
  const o = s[1]?.length || 0;
  return {
    key: t,
    index: Math.max(0, n) + s.index + o,
    end: Math.max(0, n) + s.index + s[0].length
  };
}
function V8(e = "", t = [], n = 0) {
  return t.map((r) => Hw(e, r, n)).filter(Boolean).sort((r, s) => r.index - s.index)[0] || null;
}
function Ms(e = "", t = "", n = []) {
  const r = String(e || ""), s = Hw(r, t);
  if (!s) return;
  let o = s.end;
  for (; /\s/.test(r[o] || ""); ) o += 1;
  r[o] === '"' && (o += 1);
  const i = V8(r, n.filter((u) => u !== t), o);
  let a = i ? i.index : r.length;
  if (i) {
    const u = r.lastIndexOf(",", i.index);
    u >= o && (a = u);
  }
  let l = r.slice(o, a).trim();
  return i || (l = l.replace(/\}\s*$/, "").trimEnd()), G8(l);
}
function No(e = "") {
  const t = String(e ?? "").trim();
  return /^-?\d+(?:\.\d+)?$/.test(t) ? Number(t) : /^true$/i.test(t) ? !0 : /^false$/i.test(t) ? !1 : /^null$/i.test(t) ? null : t;
}
var mh = {
  Read: [
    "filePath",
    "path",
    "scope",
    "fromLine",
    "toLine",
    "tail",
    "outputMode",
    "contentFormat"
  ],
  Write: [
    "filePath",
    "path",
    "content"
  ],
  Edit: [
    "filePath",
    "path",
    "edits"
  ],
  Delete: ["filePath", "path"],
  Move: [
    "fromPath",
    "toPath",
    "filePath",
    "path"
  ],
  RenameBook: ["title", "name"],
  ImportMaterial: [
    "title",
    "content",
    "source"
  ],
  Glob: [
    "pattern",
    "path",
    "scope"
  ],
  Grep: [
    "pattern",
    "query",
    "path",
    "scope",
    "include",
    "outputMode",
    "limit",
    "offset",
    "contextLines",
    "useRegex"
  ],
  MemoryRead: [
    "filePath",
    "path",
    "offset",
    "limit",
    "tail"
  ],
  MemoryWrite: [
    "filePath",
    "path",
    "content"
  ],
  MemoryEdit: [
    "filePath",
    "path",
    "edits"
  ],
  MemoryGrep: [
    "pattern",
    "query",
    "filePath",
    "path",
    "scope",
    "outputMode",
    "limit",
    "offset",
    "contextLines",
    "regex",
    "useRegex"
  ],
  ChatHistory: [
    "mode",
    "limit",
    "offset",
    "startOrder",
    "endOrder",
    "pattern",
    "query",
    "regex",
    "useRegex",
    "full"
  ],
  WebSearch: ["query", "maxResults"],
  DelegateRun: ["task"],
  PlanCreate: [
    "title",
    "details",
    "priority",
    "owner",
    "blockedBy"
  ],
  PlanUpdate: [
    "id",
    "status",
    "details",
    "priority",
    "owner",
    "blockedBy"
  ],
  PlanList: ["status"],
  apply_patch: ["patchText"]
}, K8 = [
  "filePath",
  "path",
  "fromPath",
  "toPath",
  "content",
  "edits",
  "patchText",
  "query",
  "task",
  "title",
  "details",
  "pattern",
  "scope",
  "include",
  "status",
  "priority",
  "owner",
  "blockedBy",
  "fromLine",
  "toLine",
  "tail",
  "maxResults",
  "outputMode",
  "contentFormat",
  "limit",
  "offset",
  "contextLines",
  "useRegex",
  "regex",
  "mode",
  "startOrder",
  "endOrder",
  "full"
];
function EA(e = "", t = [], n = []) {
  for (const r of t) {
    const s = Ms(e, r, n);
    if (s !== void 0) return s;
  }
}
function z8(e = "", t = "") {
  if (t === "Write") {
    const n = {}, r = EA(e, ["filePath", "path"], ["content"]), s = Ms(e, "content", []);
    return r !== void 0 && (n.filePath = No(r)), s !== void 0 && (n.content = No(s)), Object.keys(n).length ? n : null;
  }
  if (t === "Edit") {
    const n = {}, r = EA(e, ["filePath", "path"], ["edits"]), s = Ms(e, "edits", []);
    return r !== void 0 && (n.filePath = No(r)), s !== void 0 && (n.edits = No(s)), Object.keys(n).length ? n : null;
  }
  if (t === "Grep") {
    const n = mh.Grep, r = {};
    return n.forEach((s, o) => {
      const i = Ms(e, s, n.slice(o + 1));
      i !== void 0 && (r[s] = No(i));
    }), r.pattern === void 0 && r.query !== void 0 && (r.pattern = r.query), r.path === void 0 && r.scope !== void 0 && (r.path = r.scope), Object.keys(r).length ? r : null;
  }
  if (t === "MemoryGrep") {
    const n = mh.MemoryGrep, r = {};
    return n.forEach((s) => {
      const o = Ms(e, s, n.filter((i) => i !== s));
      o !== void 0 && (r[s] = No(o));
    }), r.pattern === void 0 && r.query !== void 0 && (r.pattern = r.query), r.path === void 0 && r.scope !== void 0 && (r.path = r.scope), r.regex === void 0 && r.useRegex !== void 0 && (r.regex = r.useRegex), Object.keys(r).length ? r : null;
  }
  if (t === "ChatHistory") {
    const n = mh.ChatHistory, r = {};
    return n.forEach((s) => {
      const o = Ms(e, s, n.filter((i) => i !== s));
      o !== void 0 && (r[s] = No(o));
    }), r.pattern === void 0 && r.query !== void 0 && (r.pattern = r.query), r.regex === void 0 && r.useRegex !== void 0 && (r.regex = r.useRegex), Object.keys(r).length ? r : null;
  }
  return null;
}
function J8(e = "", t = "") {
  const n = String(e || "").trim();
  if (!n) return null;
  try {
    const i = JSON.parse(n);
    if (i && typeof i == "object" && !Array.isArray(i)) return i;
  } catch {
  }
  const r = z8(n, t);
  if (r) return r;
  const s = mh[t] || K8, o = {};
  return s.forEach((i, a) => {
    const l = Ms(n, i, s.slice(a + 1));
    l !== void 0 && (o[i] = No(l));
  }), Object.keys(o).length ? o : null;
}
function H1(e = "", t = "") {
  const n = J8(e, t);
  return n ? JSON.stringify(n) : "";
}
function G1(e) {
  try {
    return JSON.parse(e || "{}");
  } catch {
    return {};
  }
}
function rs(e, t, n) {
  const r = String(n || "").trim();
  r && e.push({
    label: t,
    text: r
  });
}
function sr(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function On(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function W1(e) {
  if (typeof e == "string") return e;
  if (e == null) return "{}";
  try {
    return JSON.stringify(e);
  } catch {
    return "{}";
  }
}
function V1(e, t = "") {
  if (e && typeof e == "object" && !Array.isArray(e)) return JSON.stringify(e);
  const n = typeof e == "string" ? e : W1(e);
  return H1(n, t) || JSON.stringify(G1(n));
}
function Y8(e = "") {
  const t = String(e || ""), n = Hw(t, "arguments");
  if (!n) return "";
  let r = n.end;
  for (; /\s/.test(t[r] || ""); ) r += 1;
  const s = t[r] || "";
  return s === "{" ? t.slice(r).replace(/\}\s*$/, "").trimEnd() : s === '"' ? t.slice(r + 1).replace(/"\s*\}\s*$/, "").trimEnd() : t.slice(r).replace(/\}\s*$/, "").trimEnd();
}
function X8(e = "", t = 0) {
  const n = String(e || "").trim(), r = Ms(n, "name", ["id", "arguments"]) || Ms(n, "toolName", ["id", "arguments"]) || "", s = Ms(n, "id", [
    "name",
    "toolName",
    "arguments"
  ]) || `tool-call-${t + 1}`, o = Y8(n);
  return !r || !o ? null : {
    id: s,
    name: r,
    arguments: V1(o, r)
  };
}
function Q8(e, t = 0, n = "openai-tool") {
  if (!On(e)) return null;
  const r = On(e.function) ? e.function : null, s = String(r?.name || "").trim();
  if (!s) return null;
  const o = sr(e) || {};
  return delete o.index, o.id = String(o.id || `${n}-${t + 1}`), o.type = "function", o.function = {
    ...sr(r) || {},
    name: s,
    arguments: W1(r.arguments)
  }, o;
}
function oa(e = [], t = "openai-tool") {
  return (Array.isArray(e) ? e : []).map((n, r) => Q8(n, r, t)).filter(Boolean);
}
function Gw(e) {
  if (!On(e)) return null;
  const t = sr(e) || {};
  if (typeof t.content == "string" && /<tool_call\b/i.test(t.content) && (t.content = Hi(qi(t.content).cleaned)), Array.isArray(t.tool_calls)) {
    const n = oa(t.tool_calls);
    n.length ? t.tool_calls = n : delete t.tool_calls;
  }
  return t;
}
function Bo(e = [], t = "openai-tool") {
  return oa(e, t).map((n, r) => ({
    id: n.id || `${t}-${Date.now()}-${r + 1}`,
    name: n.function.name,
    arguments: n.function.arguments
  }));
}
function K1(e) {
  return typeof e == "string" ? e : Array.isArray(e) ? e.map((t) => t ? typeof t == "string" ? t : t.text || t.content || "" : "").filter(Boolean).join(`
`) : "";
}
function qi(e = "") {
  const t = [];
  return {
    cleaned: String(e || "").replace(/<think>([\s\S]*?)<\/think>/gi, (n, r) => (rs(t, "思考块", r), "")).trim(),
    thoughts: t
  };
}
function Hi(e = "") {
  const t = String(e || ""), n = t.search(/<tool_call\b/i);
  return n < 0 ? t.trim() : t.slice(0, n).trim();
}
function T_(e = "") {
  const t = String(e || "");
  return /<tool_call\b/i.test(t) ? [{
    id: "tagged-json-draft",
    name: t.match(/["']?name["']?\s*:\s*["']([^"']+)/i)?.[1] || "工具调用",
    arguments: "{}",
    draft: !0
  }] : [];
}
function $i(e, t, n) {
  if (t) {
    if (typeof t == "string") {
      rs(e, n, t);
      return;
    }
    if (Array.isArray(t)) {
      t.forEach((r) => $i(e, r, n));
      return;
    }
    typeof t == "object" && (typeof t.text == "string" && rs(e, n, t.text), typeof t.content == "string" && rs(e, n, t.content), typeof t.reasoning_content == "string" && rs(e, n, t.reasoning_content), typeof t.thinking == "string" && rs(e, n, t.thinking), Array.isArray(t.summary) && t.summary.forEach((r) => {
      if (typeof r == "string") {
        rs(e, "推理摘要", r);
        return;
      }
      r && typeof r == "object" && rs(e, "推理摘要", r.text || r.content || "");
    }));
  }
}
function Uo(e = {}, t = {}) {
  const n = [];
  return $i(n, e.reasoning_content, "推理文本"), $i(n, e.reasoning, "推理文本"), $i(n, e.reasoning_text, "推理文本"), $i(n, e.thinking, "思考块"), $i(n, t.reasoning_content, "推理文本"), $i(n, t.reasoning, "推理文本"), Array.isArray(e.content) && e.content.forEach((r) => {
    if (!(!r || typeof r != "object")) {
      if (r.type === "reasoning_text") {
        rs(n, "推理文本", r.text);
        return;
      }
      if (r.type === "summary_text") {
        rs(n, "推理摘要", r.text);
        return;
      }
      (r.type === "thinking" || r.type === "reasoning" || r.type === "reasoning_content") && rs(n, "思考块", r.text || r.content || r.reasoning || "");
    }
  }), n;
}
function bc(e = "") {
  const t = [/<tool_call>\s*([\s\S]*?)\s*<\/tool_call>/g], n = [];
  return t.forEach((r) => {
    [...e.matchAll(r)].forEach((s, o) => {
      try {
        const i = JSON.parse(s[1]);
        n.push({
          id: i.id || `tool-call-${o + 1}`,
          name: String(i.name || ""),
          arguments: V1(i.arguments, i.name)
        });
      } catch {
        const i = X8(s[1], o);
        i && n.push(i);
      }
    });
  }), n.filter((r) => r.name);
}
function mm(e) {
  const t = e?.providerPayload?.openaiCompatibleMessage;
  return !t || typeof t != "object" || Array.isArray(t) ? null : Gw(t);
}
function Z8(e = []) {
  for (let t = e.length - 1; t >= 0; t -= 1) if (e[t]?.role === "user") return t;
  return -1;
}
function j8(e) {
  if (oa(e?.tool_calls).length > 0) return !0;
  const t = mm(e);
  return Array.isArray(t?.tool_calls) && t.tool_calls.length > 0;
}
function eY(e = {}) {
  const t = oa(e?.tool_calls);
  if (t.length) return t;
  const n = oa(mm(e)?.tool_calls);
  return n.length ? n : [];
}
function tY(e = {}) {
  return oa(e?.tool_calls).length > 0;
}
function nY(e, t, n) {
  return e?.role !== "assistant" || t <= n ? !1 : j8(e);
}
function rY(e = "") {
  return /deepseek/i.test(String(e || ""));
}
function CA(e, t = "") {
  return !On(e) || !rY(t) || !Array.isArray(e.tool_calls) || !e.tool_calls.length || Object.prototype.hasOwnProperty.call(e, "reasoning_content") ? e : {
    ...e,
    reasoning_content: ""
  };
}
var AA = /* @__PURE__ */ new Set([
  "content",
  "refusal",
  "arguments",
  "reasoning_content",
  "reasoning_text",
  "thinking",
  "text"
]);
function sY(e = [], t = []) {
  const n = Array.isArray(e) ? e.map((r) => sr(r) || {}) : [];
  return (Array.isArray(t) ? t : []).forEach((r, s) => {
    const o = sr(r) || {}, i = Number.isInteger(Number(r?.index)) ? Number(r.index) : s, a = n[i];
    n[i] = On(a) ? ia(a, o, "tool_call") : o;
  }), n.filter((r) => r !== void 0);
}
function ia(e, t, n = "") {
  if (t === void 0) return e;
  if (e === void 0) return sr(t);
  if (t === null && AA.has(String(n || ""))) return e;
  if (n === "tool_calls" && Array.isArray(e) && Array.isArray(t)) return sY(e, t);
  if (typeof e == "string" && typeof t == "string")
    return AA.has(String(n || "")) ? e === t ? e : t.startsWith(e) ? t : e.startsWith(t) ? e : `${e}${t}` : e === t ? e : sr(t);
  if (Array.isArray(e) && Array.isArray(t)) return e.concat(sr(t) || []);
  if (On(e) && On(t)) {
    const r = { ...e };
    return Object.entries(t).forEach(([s, o]) => {
      r[s] = ia(r[s], o, s);
    }), r;
  }
  return sr(t);
}
function yp(e = {}, t = {}) {
  const n = On(e) ? sr(e) || {} : {}, r = On(t) ? sr(t) || {} : {};
  return delete r.message, delete r.finish_reason, delete r.index, delete r.logprobs, delete r.delta, Object.entries(r).forEach(([s, o]) => {
    n[s] = ia(n[s], o, s);
  }), n.role || (n.role = "assistant"), Gw(n) || { role: "assistant" };
}
function wc(e, t = {}) {
  const n = Gw(yp(e, t));
  if (!(!n || typeof n != "object" || Array.isArray(n)))
    return { openaiCompatibleMessage: n };
}
function oY(e = {}, t = {}) {
  return On(e) ? On(t) ? ia(sr(e) || {}, t, "") : sr(e) : sr(t);
}
function E_(e, t = "") {
  const n = Array.isArray(e.messages) ? e.messages : [], r = Z8(n), s = n.map((i, a) => {
    const l = oa(i?.tool_calls);
    if (nY(i, a, r)) {
      const d = mm(i);
      if (tY(d)) return CA({
        ...d,
        ...l.length ? { tool_calls: l } : {}
      }, t);
    }
    const u = {
      role: i.role,
      content: i.content
    };
    return i.role === "tool" && i.tool_call_id && (u.tool_call_id = i.tool_call_id), i.role === "assistant" && l.length && (u.tool_calls = l), CA(u, t);
  }), o = String(e.systemPrompt || "").trim();
  return o && s[0]?.role !== "system" && s.unshift({
    role: "system",
    content: o
  }), s;
}
function IA(e) {
  const t = (e.tools || []).map((n) => [`- ${n.function.name}: ${n.function.description || ""}`.trim(), `  参数 JSON Schema: ${JSON.stringify(n.function.parameters || {})}`].join(`
`)).join(`
`);
  return [
    e.systemPrompt || "",
    "如果你需要调用工具，不要使用原生 tool calling 字段。",
    "用 <tool_call> 和 </tool_call> 明确 JSON 范围，请严格输出如下边界标记和包裹的 JSON，不要改写边界标记：",
    '<tool_call>{"name":"工具名","arguments":{...}}</tool_call>',
    "如果需要多个工具调用，可以连续输出多段 <tool_call> ... </tool_call>。",
    "在输出第一个 <tool_call> 之前，可根据任务复杂度决定是否需要先说明：简单查询可直接输出 <tool_call>；复杂任务可先简要说明你准备查什么或怎么查。",
    "一旦开始输出第一个 <tool_call>，就不要再继续输出面向用户的正文、解释、总结或补充；把本轮需要的 tool_call 连续输出完就结束。",
    t ? `可用工具:
${t}` : ""
  ].filter(Boolean).join(`

`);
}
function C_(e) {
  const t = /* @__PURE__ */ new Map(), n = [];
  return (Array.isArray(e.messages) ? e.messages : []).forEach((r) => {
    if (r.role === "assistant") {
      const s = eY(r);
      if (s.length) {
        const o = mm(r), i = typeof o?.content == "string" ? o.content : String(r.content || ""), a = s.map((l, u) => {
          const d = l.function?.name || "", h = l.id || `tool-call-${u + 1}`;
          return d && t.set(h, d), `<tool_call>${JSON.stringify({
            id: h,
            name: d,
            arguments: G1(l.function?.arguments || "{}")
          })}</tool_call>`;
        }).join(`
`);
        n.push({
          role: "assistant",
          content: [i, a].filter(Boolean).join(`

`)
        });
        return;
      }
    }
    if (r.role === "tool") {
      const s = String(r.toolName || r.tool_name || "").trim() || t.get(r.tool_call_id || "") || "unknown_tool";
      r.tool_call_id && t.delete(r.tool_call_id);
      const o = String(r.content || "");
      n.push({
        role: "user",
        content: [
          "<tool_result>",
          "这是系统工具执行结果，不是用户新发言。",
          `name: ${s}`,
          "content:",
          o,
          "</tool_result>"
        ].join(`
`)
      });
      return;
    }
    n.push({
      role: r.role,
      content: r.content
    });
  }), !n.length || n[0].role !== "system" ? n.unshift({
    role: "system",
    content: IA(e)
  }) : n[0] = {
    ...n[0],
    content: IA({
      ...e,
      systemPrompt: n[0].content || e.systemPrompt
    })
  }, n;
}
function xA(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function kA(e, t, n) {
  !e || !t || n === void 0 || (e[t] = ia(e[t], n, t));
}
function iY(e, t = []) {
  !Array.isArray(t) || !t.length || (Array.isArray(e.tool_calls) || (e.tool_calls = []), t.forEach((n) => {
    const r = Number(n?.index ?? 0), s = { ...e.tool_calls[r] || {} };
    Object.entries(n || {}).forEach(([o, i]) => {
      if (o !== "index" && !(o === "function" && i == null)) {
        if (o === "function" && On(i)) {
          s.function = On(s.function) ? { ...s.function } : {}, Object.entries(i).forEach(([a, l]) => {
            s.function[a] = ia(s.function[a], l, a);
          });
          return;
        }
        s[o] = ia(s[o], i, o);
      }
    }), e.tool_calls[r] = s;
  }));
}
function A_(e, t = {}) {
  if (!e || !t || typeof t != "object") return;
  Object.entries(t).forEach(([r, s]) => {
    r === "delta" || r === "finish_reason" || r === "index" || r === "logprobs" || kA(e, r, s);
  });
  const n = On(t.delta) ? t.delta : {};
  Object.entries(n).forEach(([r, s]) => {
    if (r === "tool_calls") {
      iY(e, s);
      return;
    }
    kA(e, r, s);
  });
}
function I_(e, t = {}) {
  if (!e || !On(t)) return;
  const n = Number(t.index ?? 0), r = e.toolCalls[n] || {
    id: "",
    type: "function",
    function: {
      name: "",
      arguments: ""
    }
  }, s = On(t.function) ? t.function : {};
  e.toolCalls[n] = {
    ...r,
    id: t.id || r.id,
    type: t.type || r.type,
    function: {
      name: s.name || r.function?.name || "",
      arguments: `${r.function?.arguments || ""}${s.arguments || ""}`
    }
  };
}
async function aY(e, t) {
  const n = e.body?.getReader?.();
  if (!n) throw new Error("openai_compatible_stream_missing_body");
  const r = new TextDecoder();
  let s = "";
  const o = /\r?\n\r?\n/;
  for (; ; ) {
    const { done: a, value: l } = await n.read();
    if (a) break;
    for (s += r.decode(l, { stream: !0 }); ; ) {
      const u = s.match(o);
      if (!u || typeof u.index != "number") break;
      const d = u.index, h = s.slice(0, d);
      s = s.slice(d + u[0].length);
      const p = h.split(/\r?\n/).filter((y) => y.startsWith("data:")).map((y) => y.slice(5).trimStart()).join(`
`).trim();
      !p || p === "[DONE]" || t(JSON.parse(p));
    }
  }
  const i = s.trim();
  if (i && i !== "[DONE]") {
    const a = i.split(/\r?\n/).filter((l) => l.startsWith("data:")).map((l) => l.slice(5).trimStart()).join(`
`).trim();
    a && a !== "[DONE]" && t(JSON.parse(a));
  }
}
var lY = class {
  constructor(e) {
    this.config = e, this.client = new Tt({
      apiKey: e.apiKey,
      baseURL: String(e.baseUrl || "https://api.openai.com/v1").replace(/\/$/, ""),
      timeout: Number(e.timeoutMs) || 900 * 1e3,
      maxRetries: 0,
      dangerouslyAllowBrowser: !0
    });
  }
  buildRequestBody(e) {
    const t = (this.config.toolMode || "native") === "tagged-json" && Array.isArray(e.tools) && e.tools.length > 0, n = !t && Array.isArray(e.tools) && e.tools.length ? e.tools : null, r = {
      model: this.config.model,
      messages: t ? C_(e) : E_(e, this.config.model),
      ...n ? {
        tools: n,
        tool_choice: e.toolChoice || "auto"
      } : {},
      ...e.maxTokens ? { max_tokens: e.maxTokens } : {}
    };
    return !e.reasoning?.enabled && typeof e.temperature == "number" && (r.temperature = e.temperature), e.reasoning?.enabled && (r.reasoning_effort = e.reasoning.effort), r;
  }
  inspectRequest(e, t = {}) {
    const n = typeof e.onStreamProgress == "function", r = {
      ...t.body || this.buildRequestBody(e),
      ...n ? { stream: !0 } : {}
    }, s = String(this.config.baseUrl || "https://api.openai.com/v1").replace(/\/$/, "");
    return Wc({
      provider: "openai-compatible",
      model: this.config.model,
      transport: "openai-compatible",
      url: `${s}/chat/completions`,
      headers: {
        "Content-Type": "application/json",
        Authorization: this.config.apiKey ? `Bearer ${this.config.apiKey}` : ""
      },
      body: r,
      sdk: n ? "client.chat.completions.create(..., { stream: true })" : "client.chat.completions.create"
    });
  }
  async streamNativeChatCompletions(e, t) {
    const n = `${String(this.config.baseUrl || "https://api.openai.com/v1").replace(/\/$/, "")}/chat/completions`, r = await fetch(n, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${this.config.apiKey}`
      },
      body: JSON.stringify({
        ...t,
        stream: !0
      }),
      signal: e.signal
    });
    if (!r.ok) {
      const w = await r.text().catch(() => "");
      throw new Error(w || `openai_compatible_stream_http_${r.status}`);
    }
    const s = {
      content: "",
      toolCalls: []
    }, o = { role: "assistant" };
    let i = "stop", a = this.config.model;
    await aY(r, (w) => {
      a = w?.model || a;
      const E = w?.choices?.[0], C = E?.delta || {};
      A_(o, E), E?.finish_reason && (i = E.finish_reason), typeof C.content == "string" && (s.content += C.content), Array.isArray(C.tool_calls) && C.tool_calls.forEach((D) => {
        I_(s, D);
      });
      const M = qi(s.content), k = s.toolCalls.filter((D) => D?.function?.name), R = k.length ? Bo(s.toolCalls) : T_(M.cleaned);
      xA(e, {
        text: k.length ? M.cleaned : Hi(M.cleaned),
        thoughts: Uo(o, E).concat(M.thoughts),
        ...R.length ? { toolCalls: R } : {},
        ...!k.length && R.length ? { toolCallDraft: !0 } : {}
      });
    });
    const l = wc(o), u = Bo(s.toolCalls), d = qi(s.content), h = Uo(o, {});
    d.thoughts.forEach((w) => h.push(w));
    const p = u.length ? [] : bc(d.cleaned), y = [...u, ...p];
    return {
      text: u.length ? d.cleaned : Hi(d.cleaned),
      toolCalls: y,
      thoughts: h,
      finishReason: i,
      model: a,
      provider: "openai-compatible",
      providerPayload: l
    };
  }
  async chat(e) {
    const t = (this.config.toolMode || "native") === "tagged-json" && Array.isArray(e.tools) && e.tools.length > 0, n = typeof e.onStreamProgress == "function", r = this.buildRequestBody(e), s = this.inspectRequest(e, { body: r });
    if (n) {
      if (!t) return {
        ...await this.streamNativeChatCompletions(e, r),
        requestInspection: s
      };
      const E = await this.client.chat.completions.create({
        ...r,
        stream: !0
      }, { signal: e.signal }), C = {
        content: "",
        toolCalls: []
      }, M = { role: "assistant" };
      let k = "stop", R = this.config.model, D;
      for await (const L of E) {
        R = L.model || R;
        const G = L.choices?.[0], z = G?.delta || {};
        A_(M, G), G?.finish_reason && (k = G.finish_reason), typeof z.content == "string" && (C.content += z.content), Array.isArray(z.tool_calls) && z.tool_calls.forEach((Y) => {
          I_(C, Y);
        });
        const V = qi(C.content), Z = C.toolCalls.filter((Y) => Y?.function?.name), O = Z.length ? Bo(C.toolCalls) : T_(V.cleaned);
        xA(e, {
          text: Z.length ? V.cleaned : Hi(V.cleaned),
          thoughts: Uo(M, G).concat(V.thoughts),
          ...O.length ? { toolCalls: O } : {},
          ...!Z.length && O.length ? { toolCallDraft: !0 } : {}
        });
      }
      const m = (typeof E.finalChatCompletion == "function" ? await E.finalChatCompletion() : null)?.choices?.[0] || null, b = oY(M, yp(m?.message || M, m || {}));
      D = wc(b);
      const v = Bo(C.toolCalls), T = qi(C.content), I = Uo(b, m || {});
      T.thoughts.forEach((L) => I.push(L));
      const x = v.length ? [] : bc(T.cleaned), P = [...v, ...x];
      return {
        text: v.length ? T.cleaned : Hi(T.cleaned),
        toolCalls: P,
        thoughts: I,
        finishReason: k,
        model: R,
        provider: "openai-compatible",
        providerPayload: D,
        requestInspection: s
      };
    }
    const o = await this.client.chat.completions.create(r, { signal: e.signal }), i = o.choices?.[0] || {}, a = i.message || {}, l = Uo(a, i), u = Bo(a.tool_calls || []), d = qi(K1(a.content));
    d.thoughts.forEach((E) => l.push(E));
    const h = u.length ? [] : bc(d.cleaned), p = [...u, ...h], y = u.length ? d.cleaned : Hi(d.cleaned), w = yp(a, i);
    return {
      text: y,
      toolCalls: p,
      thoughts: l,
      finishReason: i.finish_reason || "stop",
      model: o.model || this.config.model,
      provider: "openai-compatible",
      providerPayload: wc(w),
      requestInspection: s
    };
  }
};
function z1(e, t) {
  return {
    type: "message",
    role: e,
    content: uY(t)
  };
}
function _p(e) {
  return {
    role: "assistant",
    content: typeof e == "string" ? e : ""
  };
}
function uY(e) {
  if (typeof e == "string") return [{
    type: "input_text",
    text: e
  }];
  if (!Array.isArray(e)) return [{
    type: "input_text",
    text: ""
  }];
  const t = e.map((n) => !n || typeof n != "object" ? null : n.type === "image_url" && n.image_url?.url ? {
    type: "input_image",
    image_url: n.image_url.url
  } : n.type === "text" ? {
    type: "input_text",
    text: n.text || ""
  } : null).filter(Boolean);
  return t.length ? t : [{
    type: "input_text",
    text: ""
  }];
}
function bp(e, t, n) {
  const r = String(n || "").trim();
  r && e.push({
    label: t,
    text: r
  });
}
function MA(e, t = [], n = {}) {
  (t || []).forEach((r) => {
    if (!(!r || typeof r != "object")) {
      if (r.type === "reasoning_text") {
        bp(e, n.reasoning || "推理文本", r.text);
        return;
      }
      r.type === "summary_text" && bp(e, n.summary || "推理摘要", r.text);
    }
  });
}
function cY(e = []) {
  const t = [];
  return (e || []).forEach((n) => {
    !n || typeof n != "object" || n.type === "reasoning" && (MA(t, n.content, {
      reasoning: "推理文本",
      summary: "推理摘要"
    }), MA(t, n.summary, {
      reasoning: "推理文本",
      summary: "推理摘要"
    }));
  }), t;
}
function dY(e) {
  const t = [String(e.systemPrompt || "").trim(), ...(e.messages || []).filter((n) => n.role === "system").map((n) => String(n.content || "").trim())].filter(Boolean);
  return t.length ? [...new Set(t)].join(`

`) : "";
}
function fY(e) {
  const t = e?.choices?.[0]?.message?.content;
  if (typeof t == "string" && t.trim()) return t.trim();
  if (typeof e?.output_text == "string" && e.output_text.trim()) return e.output_text.trim();
  const n = [];
  return (Array.isArray(e?.output) ? e.output : []).forEach((r) => {
    if (!(!r || typeof r != "object")) {
      if (r.type === "message" && Array.isArray(r.content)) {
        r.content.forEach((s) => {
          if (!(!s || typeof s != "object")) {
            if (s.type === "output_text" && typeof s.text == "string" && s.text.trim()) {
              n.push(s.text.trim());
              return;
            }
            s.type === "refusal" && typeof s.refusal == "string" && s.refusal.trim() && n.push(s.refusal.trim());
          }
        });
        return;
      }
      typeof r.text == "string" && r.text.trim() && n.push(r.text.trim());
    }
  }), n.join(`
`).trim();
}
function hY(e) {
  const t = e?.choices?.[0], n = t?.message?.content, r = String(t?.finish_reason || "");
  if (typeof n != "string" || !n.trim()) return null;
  const s = n.toLowerCase();
  return !s.includes("proxy error") || !s.includes("/responses") && !r.toLowerCase().includes("proxy error") ? null : n.trim();
}
function pY(e) {
  const t = [];
  for (const n of e.messages || [])
    if (n.role !== "system") {
      if (n.role === "tool") {
        t.push({
          type: "function_call_output",
          call_id: n.tool_call_id || "missing_tool_call_id",
          output: n.content
        });
        continue;
      }
      if (n.role === "assistant" && Array.isArray(n.tool_calls) && n.tool_calls.length) {
        n.content?.trim() && t.push(_p(n.content)), n.tool_calls.forEach((r, s) => {
          t.push({
            type: "function_call",
            call_id: r.id || `function_call_${s + 1}`,
            name: r.function?.name || "",
            arguments: r.function?.arguments || "{}",
            status: "completed"
          });
        });
        continue;
      }
      if (n.role === "assistant") {
        t.push(_p(n.content || ""));
        continue;
      }
      t.push(n.role === "user" ? z1(n.role, n.content || "") : {
        role: n.role,
        content: typeof n.content == "string" ? n.content : ""
      });
    }
  return t;
}
function mY(e) {
  const t = [];
  for (const n of e.messages || []) {
    if (n.role === "system") {
      t.push({
        role: "system",
        content: typeof n.content == "string" ? n.content : ""
      });
      continue;
    }
    if (n.role === "tool") {
      t.push({
        type: "function_call_output",
        call_id: n.tool_call_id || "missing_tool_call_id",
        output: n.content
      });
      continue;
    }
    if (n.role === "assistant" && Array.isArray(n.tool_calls) && n.tool_calls.length) {
      n.content?.trim() && t.push(_p(n.content)), n.tool_calls.forEach((r, s) => {
        t.push({
          type: "function_call",
          call_id: r.id || `function_call_${s + 1}`,
          name: r.function?.name || "",
          arguments: r.function?.arguments || "{}",
          status: "completed"
        });
      });
      continue;
    }
    if (n.role === "assistant") {
      t.push(_p(n.content || ""));
      continue;
    }
    t.push(n.role === "user" ? z1(n.role, n.content || "") : {
      role: n.role,
      content: typeof n.content == "string" ? n.content : ""
    });
  }
  return t;
}
function gY(e) {
  try {
    return new URL(String(e || "https://api.openai.com/v1")).hostname === "api.openai.com";
  } catch {
    return !1;
  }
}
function vY(e) {
  const t = String(e?.message || e || "").toLowerCase();
  return t.includes("instructions") || t.includes("unsupported") || t.includes("unknown parameter") || t.includes("invalid input");
}
function yY(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {}
  });
}
function wv(e, t) {
  const [n = "0", r = "0"] = String(e || "").split(":"), [s = "0", o = "0"] = String(t || "").split(":");
  return Number(n) - Number(s) || Number(r) - Number(o);
}
var _Y = class {
  constructor(e) {
    this.config = e, this.client = new Tt({
      apiKey: e.apiKey,
      baseURL: String(e.baseUrl || "https://api.openai.com/v1").replace(/\/$/, ""),
      timeout: Number(e.timeoutMs) || 900 * 1e3,
      maxRetries: 0,
      dangerouslyAllowBrowser: !0
    });
  }
  buildRequestBody(e, t = !1) {
    const n = {
      model: this.config.model,
      instructions: t ? void 0 : dY(e) || void 0,
      input: t ? mY(e) : pY(e),
      ...Array.isArray(e.tools) && e.tools.length ? {
        tools: e.tools.map((r) => ({
          type: "function",
          name: r.function.name,
          description: r.function.description,
          parameters: r.function.parameters
        })),
        tool_choice: e.toolChoice || "auto"
      } : {},
      ...e.maxTokens ? { max_output_tokens: e.maxTokens } : {}
    };
    return !e.reasoning?.enabled && typeof e.temperature == "number" && (n.temperature = e.temperature), e.reasoning?.enabled && (n.reasoning = {
      effort: e.reasoning.effort,
      summary: "detailed"
    }), n;
  }
  inspectRequest(e, t = {}) {
    const n = typeof e.onStreamProgress == "function", r = t.legacySystemInInput === !0, s = String(this.config.baseUrl || "https://api.openai.com/v1").replace(/\/$/, "");
    return Wc({
      provider: "openai-responses",
      model: this.config.model,
      transport: "openai-responses",
      url: `${s}/responses`,
      headers: {
        "Content-Type": "application/json",
        Authorization: this.config.apiKey ? `Bearer ${this.config.apiKey}` : ""
      },
      body: t.body || this.buildRequestBody(e, r),
      sdk: n ? "client.responses.stream" : "client.responses.create"
    });
  }
  async chat(e) {
    let t = this.inspectRequest(e);
    const n = (l) => {
      const u = hY(l);
      if (u) {
        const h = new Error(u);
        throw h.name = "ProxyEndpointError", h.rawDisplay = u, h;
      }
      const d = Array.isArray(l.output) ? l.output : [];
      return {
        output: d,
        thoughts: cY(d),
        toolCalls: d.filter((h) => h.type === "function_call" && h.name).map((h, p) => ({
          id: h.call_id || `response-tool-${p + 1}`,
          name: h.name || "",
          arguments: h.arguments || "{}"
        })),
        text: fY(l)
      };
    }, r = async (l = !1) => {
      const u = this.buildRequestBody(e, l);
      return t = this.inspectRequest(e, {
        body: u,
        legacySystemInInput: l
      }), await this.client.responses.create(u, { signal: e.signal });
    }, s = async (l = !1) => {
      const u = this.buildRequestBody(e, l);
      t = this.inspectRequest(e, {
        body: u,
        legacySystemInInput: l
      });
      const d = this.client.responses.stream(u, { signal: e.signal }), h = /* @__PURE__ */ new Map(), p = /* @__PURE__ */ new Map(), y = /* @__PURE__ */ new Map(), w = () => {
        const E = [];
        Array.from(p.entries()).sort(([C], [M]) => wv(C, M)).forEach(([, C]) => bp(E, "推理文本", C)), Array.from(y.entries()).sort(([C], [M]) => wv(C, M)).forEach(([, C]) => bp(E, "推理摘要", C)), yY(e, {
          text: Array.from(h.entries()).sort(([C], [M]) => wv(C, M)).map(([, C]) => C).join(`
`).trim(),
          thoughts: E
        });
      };
      return d.on("response.output_text.delta", (E) => {
        const C = `${E.output_index}:${E.content_index}`;
        h.set(C, `${h.get(C) || ""}${E.delta}`), w();
      }), d.on("response.reasoning_text.delta", (E) => {
        const C = `${E.output_index}:${E.content_index}`;
        p.set(C, `${p.get(C) || ""}${E.delta}`), w();
      }), d.on("response.reasoning_summary_text.delta", (E) => {
        const C = `${E.output_index}:${E.summary_index}`;
        y.set(C, `${y.get(C) || ""}${E.delta}`), w();
      }), await d.finalResponse();
    }, o = !gY(this.config.baseUrl);
    let i, a;
    try {
      i = typeof e.onStreamProgress == "function" ? await s(!1) : await r(!1), a = n(i), o && !a.text && !a.toolCalls.length && (i = typeof e.onStreamProgress == "function" ? await s(!0) : await r(!0), a = n(i));
    } catch (l) {
      if (!o || !vY(l)) throw l;
      i = typeof e.onStreamProgress == "function" ? await s(!0) : await r(!0), a = n(i);
    }
    return {
      text: a.text,
      toolCalls: a.toolCalls,
      thoughts: a.thoughts,
      finishReason: i.incomplete_details?.reason || i.status || "stop",
      model: i.model || this.config.model,
      provider: "openai-responses",
      requestInspection: t
    };
  }
};
function aa(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function J1(e = "") {
  try {
    return {
      ok: !0,
      input: JSON.parse(String(e || ""))
    };
  } catch (t) {
    return {
      ok: !1,
      input: {},
      raw: String(e || ""),
      error: t instanceof Error ? t.message : String(t || "invalid_tool_input_json")
    };
  }
}
function bY(e = []) {
  return (Array.isArray(e) ? e : []).map((t) => {
    const n = String(t?.function?.name || "").trim();
    if (!n) return null;
    const r = J1(t.function.arguments || "{}");
    return {
      type: "tool_use",
      id: String(t.id || n),
      name: n,
      input: r.input,
      ...r.ok ? {} : {
        invalidInputJson: r.raw,
        inputParseError: r.error
      }
    };
  }).filter(Boolean);
}
function wY(e = []) {
  const t = Array.isArray(e) ? aa(e) : null;
  return Array.isArray(t) && t.length ? t : null;
}
function SY(e = {}) {
  const t = Array.isArray(e.messages) ? e.messages : [], n = [];
  return t.forEach((r) => {
    if (!r || typeof r != "object") return;
    const s = aa(r) || {}, o = wY(s?.providerPayload?.anthropicContent), i = bY(s.tool_calls);
    delete s.providerPayload, s.role === "assistant" && o && i.length ? (delete s.tool_calls, s.content = o.filter((a) => a?.type !== "tool_use").concat(i)) : s.role === "assistant" && o && (delete s.tool_calls, s.content = o), n.push(s);
  }), n;
}
function TY(e = []) {
  return (Array.isArray(e) ? e : []).map((t) => {
    if (!t || typeof t != "object") return null;
    if (t.type === "text") return {
      type: "text",
      text: String(t.text || "")
    };
    if (t.type === "tool_use" && t.name) {
      if (t.inputJson !== void 0) {
        const r = J1(t.inputJson);
        return {
          type: "tool_use",
          id: String(t.id || t.name),
          name: String(t.name),
          input: r.input,
          ...r.ok ? {} : {
            invalidInputJson: r.raw,
            inputParseError: r.error
          }
        };
      }
      const n = aa(t.input);
      return n !== void 0 ? {
        type: "tool_use",
        id: String(t.id || t.name),
        name: String(t.name),
        input: n
      } : {
        type: "tool_use",
        id: String(t.id || t.name),
        name: String(t.name),
        input: {}
      };
    }
    return t.type === "thinking" ? {
      type: "thinking",
      thinking: String(t.thinking || t.text || "")
    } : t.type === "redacted_thinking" ? {
      type: "redacted_thinking",
      data: String(t.data || "")
    } : aa(t) || null;
  }).filter(Boolean);
}
function EY(e = []) {
  return e.map((t) => !t || typeof t != "object" ? null : t.type === "tool_use" && t.name ? {
    type: "tool_use",
    id: t.id,
    name: t.name,
    input: aa(t.input) || {}
  } : aa(t) || null).filter(Boolean);
}
function CY(e = []) {
  const t = Array.isArray(e) ? e : [], n = t.filter((o) => o?.type === "text").map((o) => o.text || "").join(`
`), r = t.filter((o) => o?.type === "thinking" || o?.type === "redacted_thinking").map((o) => ({
    label: o.type === "thinking" ? "思考块" : "已脱敏思考块",
    text: o.type === "thinking" ? o.thinking || "" : o.data || ""
  })).filter((o) => o.text), s = t.filter((o) => o?.type === "tool_use" && o.name).map((o, i) => ({
    id: o.id || `st-claude-tool-${i + 1}`,
    name: o.name,
    arguments: o.inputJson !== void 0 ? o.inputJson : JSON.stringify(o.input || {})
  }));
  return {
    text: n,
    thoughts: r,
    ...s.length ? {
      toolCalls: s,
      toolCallDraft: !0
    } : {}
  };
}
function Y1(e = [], t = {}) {
  const n = TY(e), r = n.filter((s) => s.type === "tool_use" && s.name).map((s, o) => ({
    id: s.id || `st-claude-tool-${o + 1}`,
    name: s.name,
    arguments: s.invalidInputJson !== void 0 ? s.invalidInputJson : JSON.stringify(s.input || {})
  }));
  return {
    text: n.filter((s) => s.type === "text").map((s) => s.text || "").join(`
`),
    toolCalls: r,
    thoughts: n.filter((s) => s.type === "thinking" || s.type === "redacted_thinking").map((s) => ({
      label: s.type === "thinking" ? "思考块" : "已脱敏思考块",
      text: s.type === "thinking" ? s.thinking || "" : s.data || ""
    })).filter((s) => s.text),
    finishReason: t.finishReason || "stop",
    model: t.model || "",
    provider: "sillytavern-claude",
    providerPayload: n.length ? { anthropicContent: EY(n) } : void 0
  };
}
function AY(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function IY(e, t = {}) {
  const n = [];
  let r = "stop", s = t.model || "";
  const o = (a, l = {}) => {
    const u = Number.isInteger(Number(a)) ? Number(a) : n.length;
    return n[u] ? n[u] = {
      ...n[u],
      ...l
    } : n[u] = { ...l }, n[u];
  }, i = () => {
    const a = CY(n);
    AY(e, {
      text: a.text,
      thoughts: a.thoughts,
      ...Array.isArray(a.toolCalls) ? { toolCalls: a.toolCalls } : {},
      ...a.toolCallDraft ? { toolCallDraft: !0 } : {}
    });
  };
  return {
    accept(a = {}) {
      if (a?.message?.model && (s = a.message.model), a.type === "content_block_start") {
        o(a.index, aa(a.content_block) || {}), i();
        return;
      }
      if (a.type === "content_block_delta") {
        const l = o(a.index), u = a.delta || {};
        u.type === "text_delta" ? (l.type = l.type || "text", l.text = `${l.text || ""}${u.text || ""}`) : u.type === "input_json_delta" ? (l.type = l.type || "tool_use", l.inputJson = `${l.inputJson || ""}${u.partial_json || ""}`) : u.type === "thinking_delta" ? (l.type = l.type || "thinking", l.thinking = `${l.thinking || ""}${u.thinking || ""}`) : u.type === "signature_delta" && (l.signature = `${l.signature || ""}${u.signature || ""}`), i();
        return;
      }
      a.type === "message_delta" && (r = a.delta?.stop_reason || r);
    },
    result() {
      return Y1(n, {
        finishReason: r,
        model: s
      });
    }
  };
}
var xY = class {
  constructor(e) {
    this.config = e;
  }
  buildMessages(e) {
    return SY(e);
  }
  buildPayload(e) {
    const t = typeof e.onStreamProgress == "function", n = this.buildMessages(e);
    return R2(this.config, e, n, t);
  }
  async inspectRequest(e, t = {}) {
    const n = await td(t.payload || this.buildPayload(e), typeof e.onStreamProgress == "function");
    return this.buildRequestInspection(n);
  }
  buildRequestInspection(e) {
    return {
      provider: "sillytavern-claude",
      model: this.config.model,
      transport: "sillytavern-chat-completions",
      request: Cl(e)
    };
  }
  async chat(e) {
    const t = typeof e.onStreamProgress == "function", n = this.buildPayload(e);
    let r = null;
    const s = (o) => {
      r = this.buildRequestInspection(o);
    };
    try {
      if (t) {
        const i = IY(e, this.config);
        return await fb(n, (a) => {
          i.accept(a);
        }, {
          signal: e.signal,
          onRequest: s
        }), {
          ...i.result(),
          requestInspection: r
        };
      }
      const o = await db(n, {
        signal: e.signal,
        onRequest: s
      });
      return {
        ...Y1(Array.isArray(o?.content) ? o.content : [{
          type: "text",
          text: o?.choices?.[0]?.message?.content || ""
        }], {
          finishReason: o?.stop_reason || o?.choices?.[0]?.finish_reason || "stop",
          model: o?.model || this.config.model
        }),
        requestInspection: r
      };
    } catch (o) {
      throw r && o && typeof o == "object" && (o.requestInspection = r), o;
    }
  }
};
function Ww(e) {
  if (e !== void 0)
    try {
      return JSON.parse(JSON.stringify(e));
    } catch {
      return;
    }
}
function Al(e) {
  if (typeof e == "string") return {
    role: "model",
    parts: e ? [{ text: e }] : []
  };
  if (!e || typeof e != "object") return {
    role: "model",
    parts: []
  };
  const t = Ww(e) || {};
  return t.role = t.role || "model", t.parts = Array.isArray(t.parts) ? t.parts : [], t;
}
function kY(e) {
  const t = Array.isArray(e?.providerPayload?.googleContents) ? e.providerPayload.googleContents : [];
  if (t.length) return t.map((s) => Al(s)).filter((s) => Array.isArray(s.parts) && s.parts.length);
  const n = e?.providerPayload?.googleContent, r = Al(n);
  return r.parts.length ? [r] : [];
}
function MY(e = {}) {
  const t = String(e?.mimeType || "").trim(), n = String(e?.data || "").trim();
  if (!t || !n) return null;
  const r = `data:${t};base64,${n}`;
  return t.startsWith("image/") ? {
    type: "image_url",
    image_url: { url: r }
  } : t.startsWith("video/") ? {
    type: "video_url",
    video_url: { url: r }
  } : t.startsWith("audio/") ? {
    type: "audio_url",
    audio_url: { url: r }
  } : null;
}
function PY(e = {}, t = 0) {
  const n = Al(e);
  if (!n.parts.length) return null;
  const r = {
    role: n.role === "user" ? "user" : "assistant",
    content: []
  }, s = n.parts.find((i) => !i?.thought && typeof i?.text == "string" && typeof i?.thoughtSignature == "string" && i.thoughtSignature)?.thoughtSignature || "", o = [];
  return n.parts.forEach((i) => {
    if (!i || typeof i != "object") return;
    if (!i.thought && typeof i.text == "string" && i.text) {
      r.content.push({
        type: "text",
        text: i.text
      });
      return;
    }
    if (i.functionCall?.name) {
      o.push({
        id: String(i.functionCall.id || `st-google-tool-${t + 1}-${o.length + 1}`),
        type: "function",
        function: {
          name: String(i.functionCall.name || ""),
          arguments: JSON.stringify(i.functionCall.args || {})
        },
        ...typeof i.thoughtSignature == "string" && i.thoughtSignature ? { signature: i.thoughtSignature } : {}
      });
      return;
    }
    const a = MY(i.inlineData);
    a && r.content.push(a);
  }), o.length && r.content.push({
    type: "tool_calls",
    tool_calls: o
  }), s && r.content.some((i) => i?.type === "text") && (r.signature = s), r.content.length ? r : null;
}
function RY(e = {}) {
  const t = Array.isArray(e.messages) ? e.messages : [], n = [];
  return t.forEach((r) => {
    if (!r || typeof r != "object") return;
    const s = kY(r);
    if (r.role === "assistant" && s.length) {
      s.forEach((i, a) => {
        const l = PY(i, a);
        l && n.push(l);
      });
      return;
    }
    const o = Ww(r) || {};
    delete o.providerPayload, n.push(o);
  }), n;
}
function X1(e = {}) {
  return Al(e?.responseContent || e?.candidates?.[0]?.content || "");
}
function Q1(e = {}) {
  return (e.parts || []).filter((t) => !t?.thought && typeof t?.text == "string" && t.text).map((t) => t.text).join(`
`);
}
function Z1(e = {}) {
  return (e.parts || []).filter((t) => t?.thought && typeof t.text == "string" && t.text.trim()).map((t, n) => ({
    label: `思考块 ${n + 1}`,
    text: t.text.trim()
  }));
}
function j1(e = {}) {
  return (e.parts || []).map((t) => t?.functionCall || null).filter((t) => t?.name).map((t, n) => ({
    id: t.id || `st-google-tool-${n + 1}`,
    name: t.name,
    arguments: JSON.stringify(t.args || {})
  }));
}
function NY(e, t) {
  const n = String(t || ""), r = String(e || "");
  return n ? !r || n.startsWith(r) ? n : r.endsWith(n) ? r : `${r}${n}` : r;
}
function $Y(e = [], t = []) {
  const n = Array.isArray(e) ? [...e] : [];
  return t.forEach((r) => {
    const s = [
      r.id || "",
      r.name || "",
      r.arguments || ""
    ].join("\0");
    n.some((o) => [
      o.id || "",
      o.name || "",
      o.arguments || ""
    ].join("\0") === s) || n.push(r);
  }), n;
}
function eN(e) {
  const t = Al(e);
  return t.parts.length ? {
    googleContent: t,
    googleContents: [t]
  } : void 0;
}
function DY(e = {}, t = {}) {
  const n = X1(e), r = e?.choices?.[0]?.message?.content || "";
  return {
    text: Q1(n) || r,
    toolCalls: j1(n),
    thoughts: Z1(n),
    finishReason: e?.candidates?.[0]?.finishReason || e?.choices?.[0]?.finish_reason || t.finishReason || "STOP",
    model: e?.model || e?.modelVersion || t.model || "",
    provider: "sillytavern-google",
    providerPayload: eN(n)
  };
}
function LY(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function OY(e, t = {}) {
  let n = "", r = [], s = [], o = "STOP", i = t.model || "";
  const a = [];
  return {
    accept(l = {}) {
      i = l.model || l.modelVersion || i, o = l?.candidates?.[0]?.finishReason || o;
      const u = X1(l);
      u.parts.length && a.push(...Ww(u.parts) || []), n = NY(n, Q1(u)), r = $Y(r, j1(u));
      const d = Z1(u);
      d.length && (s = d), LY(e, {
        text: n,
        thoughts: s,
        ...r.length ? {
          toolCalls: r,
          toolCallDraft: !0
        } : {}
      });
    },
    result() {
      const l = Al({
        role: "model",
        parts: a.length ? a : n ? [{ text: n }] : []
      });
      return {
        text: n,
        toolCalls: r,
        thoughts: s,
        finishReason: o,
        model: i,
        provider: "sillytavern-google",
        providerPayload: eN(l)
      };
    }
  };
}
var FY = class {
  constructor(e) {
    this.config = e;
  }
  buildMessages(e) {
    return RY(e);
  }
  buildPayload(e) {
    const t = typeof e.onStreamProgress == "function", n = this.buildMessages(e);
    return N2(this.config, e, n, t);
  }
  async inspectRequest(e, t = {}) {
    const n = await td(t.payload || this.buildPayload(e), typeof e.onStreamProgress == "function");
    return this.buildRequestInspection(n);
  }
  buildRequestInspection(e) {
    return {
      provider: "sillytavern-google",
      model: this.config.model,
      transport: "sillytavern-chat-completions",
      request: Cl(e)
    };
  }
  async chat(e) {
    const t = typeof e.onStreamProgress == "function", n = this.buildPayload(e);
    let r = null;
    const s = (o) => {
      r = this.buildRequestInspection(o);
    };
    try {
      if (t) {
        const o = OY(e, this.config);
        return await fb(n, (i) => {
          o.accept(i);
        }, {
          signal: e.signal,
          onRequest: s
        }), {
          ...o.result(),
          requestInspection: r
        };
      }
      return {
        ...DY(await db(n, {
          signal: e.signal,
          onRequest: s
        }), { model: this.config.model }),
        requestInspection: r
      };
    } catch (o) {
      throw r && o && typeof o == "object" && (o.requestInspection = r), o;
    }
  }
};
function BY(e, t) {
  typeof e.onStreamProgress == "function" && e.onStreamProgress({
    ...typeof t.text == "string" ? { text: t.text } : {},
    ...Array.isArray(t.thoughts) ? { thoughts: t.thoughts } : {},
    ...Array.isArray(t.toolCalls) ? { toolCalls: t.toolCalls } : {},
    ...t.toolCallDraft ? { toolCallDraft: !0 } : {}
  });
}
function Sv(e, t = []) {
  const n = qi(e);
  return {
    thinkTagged: n,
    cleanedText: t.length ? n.cleaned : Hi(n.cleaned)
  };
}
function UY(e) {
  const t = String(e?.message || e || "");
  return /Cannot read properties of null \(reading ['"]function['"]\)/i.test(t) || /reading ['"]function['"]/i.test(t) || /badresponsestatuscode/i.test(t);
}
var qY = class {
  constructor(e) {
    this.config = e;
  }
  buildMessages(e) {
    return (this.config.toolMode || "native") === "tagged-json" && Array.isArray(e.tools) && e.tools.length > 0 ? C_(e) : E_(e, this.config.model);
  }
  buildPayload(e, t = !1) {
    const n = t ? C_(e) : E_(e, this.config.model);
    return P2(this.config, t ? {
      ...e,
      tools: void 0,
      toolChoice: void 0
    } : e, n, typeof e.onStreamProgress == "function");
  }
  async inspectRequest(e, t = {}) {
    const n = await td(t.payload || this.buildPayload(e, !!t.taggedMode), typeof e.onStreamProgress == "function");
    return this.buildRequestInspection(n);
  }
  buildRequestInspection(e) {
    return {
      provider: "sillytavern-openai-compatible",
      model: this.config.model,
      transport: "sillytavern-chat-completions",
      request: Cl(e)
    };
  }
  async streamChat(e, t, n = {}) {
    const r = {
      content: "",
      toolCalls: []
    }, s = { role: "assistant" };
    let o = "stop", i = this.config.model;
    await fb(t, (p) => {
      i = p?.model || i;
      const y = p?.choices?.[0] || {}, w = y.delta || {};
      A_(s, y), y.finish_reason && (o = y.finish_reason), typeof w.content == "string" && (r.content += w.content), Array.isArray(w.tool_calls) && w.tool_calls.forEach((R) => {
        I_(r, R);
      });
      const E = r.toolCalls.filter((R) => R?.function?.name), { thinkTagged: C, cleanedText: M } = Sv(r.content, E), k = E.length ? Bo(r.toolCalls, "st-openai-tool") : T_(C.cleaned);
      BY(e, {
        text: M,
        thoughts: Uo(s, y).concat(C.thoughts),
        ...k.length ? { toolCalls: k } : {},
        ...!E.length && k.length ? { toolCallDraft: !0 } : {}
      });
    }, {
      signal: e.signal,
      onRequest: n.onRequest
    });
    const a = Bo(r.toolCalls, "st-openai-tool"), { thinkTagged: l, cleanedText: u } = Sv(r.content, a), d = Uo(s, {});
    l.thoughts.forEach((p) => d.push(p));
    const h = a.length ? [] : bc(l.cleaned);
    return {
      text: u,
      toolCalls: [...a, ...h],
      thoughts: d,
      finishReason: o,
      model: i,
      provider: "sillytavern-openai-compatible",
      providerPayload: wc(s)
    };
  }
  async nonStreamingChat(e, t, n = {}) {
    const r = await db(t, {
      signal: e.signal,
      onRequest: n.onRequest
    }), s = r.choices?.[0] || {}, o = s.message || {}, i = Uo(o, s), a = Bo(o.tool_calls || [], "st-openai-tool"), { thinkTagged: l, cleanedText: u } = Sv(K1(o.content), a);
    l.thoughts.forEach((p) => i.push(p));
    const d = a.length ? [] : bc(l.cleaned), h = yp(o, s);
    return {
      text: u,
      toolCalls: [...a, ...d],
      thoughts: i,
      finishReason: s.finish_reason || "stop",
      model: r.model || this.config.model,
      provider: "sillytavern-openai-compatible",
      providerPayload: wc(h)
    };
  }
  async chat(e) {
    const t = (this.config.toolMode || "native") === "tagged-json" && Array.isArray(e.tools) && e.tools.length > 0, n = Array.isArray(e.tools) && e.tools.length > 0, r = async (o) => {
      let i = null;
      const a = (l) => {
        i = this.buildRequestInspection(l);
      };
      try {
        return {
          ...typeof e.onStreamProgress == "function" ? await this.streamChat(e, o, { onRequest: a }) : await this.nonStreamingChat(e, o, { onRequest: a }),
          requestInspection: i
        };
      } catch (l) {
        throw i && l && typeof l == "object" && (l.requestInspection = i), l;
      }
    }, s = this.buildPayload(e, t);
    try {
      return await r(s);
    } catch (o) {
      if (t || !n || !UY(o)) throw o;
    }
    return typeof e.onToolProtocolFallback == "function" && e.onToolProtocolFallback({
      provider: "sillytavern-openai-compatible",
      fromToolMode: "native",
      toToolMode: "tagged-json",
      reason: "malformed_native_tool_host_error"
    }), await r(this.buildPayload(e, !0));
  }
}, HY = "https://api.tavily.com";
function x_(e = "") {
  return String(e || "").trim();
}
function xr(e = "") {
  return String(e || "").trim().replace(/\/+$/, "") || "https://api.tavily.com";
}
var tN = "openai-compatible", Vw = "默认", nN = "default", GY = "deny", WY = Object.freeze([{
  value: "default",
  label: "默认权限"
}, {
  value: "full",
  label: "完全权限"
}]), VY = Object.freeze([{
  value: "deny",
  label: "禁止"
}, {
  value: "allow",
  label: "允许"
}]), k_ = {
  "openai-responses": {
    baseUrl: "https://api.openai.com/v1",
    model: "gpt-4.1-mini",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  },
  "openai-compatible": {
    baseUrl: "https://api.openai.com/v1",
    model: "gpt-4o-mini",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0,
    toolMode: "native"
  },
  "sillytavern-openai-compatible": {
    baseUrl: "",
    model: "gpt-4o-mini",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0,
    toolMode: "native"
  },
  "sillytavern-claude": {
    baseUrl: "",
    model: "claude-sonnet-4-0",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  },
  "sillytavern-google": {
    baseUrl: "",
    model: "gemini-2.5-pro",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  },
  anthropic: {
    baseUrl: "https://api.anthropic.com",
    model: "claude-sonnet-4-0",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  },
  google: {
    baseUrl: "https://generativelanguage.googleapis.com/v1beta",
    model: "gemini-2.5-pro",
    apiKey: "",
    temperature: 0.2,
    sendTemperature: !0
  }
};
function el() {
  return JSON.parse(JSON.stringify(k_));
}
function kr() {
  return {
    provider: tN,
    modelConfigs: el(),
    permissionMode: nN
  };
}
function KY(e = kr()) {
  const t = e && typeof e == "object" ? e : kr();
  return {
    provider: Kw(t.provider),
    modelConfigs: Ar(t.modelConfigs || {})
  };
}
function tl(e) {
  return e === "full" ? "full" : nN;
}
function Js(e) {
  return e === "allow" ? "allow" : GY;
}
function In(e) {
  return String(e || "").trim() || "默认";
}
function Ar(e = {}) {
  const t = el();
  return Object.keys(k_).forEach((n) => {
    t[n] = {
      ...k_[n],
      ...e && typeof e[n] == "object" ? e[n] : {}
    };
  }), t;
}
function Kw(e) {
  return typeof e == "string" && e.trim() ? e : tN;
}
function zw(e = {}, t) {
  return e && typeof e.presets == "object" && e.presets ? e.presets : e?.modelConfigs ? { [t]: {
    provider: e.provider || "openai-compatible",
    modelConfigs: e.modelConfigs,
    permissionMode: e.permissionMode
  } } : {};
}
function zY(e = {}, t) {
  const n = {}, r = zw(e, t);
  return Object.entries(r).forEach(([s, o]) => {
    if (!o || typeof o != "object") return;
    const i = In(s);
    n[i] = {
      provider: Kw(o.provider),
      modelConfigs: Ar(o.modelConfigs || {}),
      permissionMode: tl(o.permissionMode)
    };
  }), Object.keys(n).length || (n[Vw] = kr()), n;
}
function JY(e, t) {
  const n = In(t);
  return e[n] ? n : Object.keys(e)[0];
}
function YY(e, t, n) {
  const r = In(t || n);
  return e[r] ? r : e[n] ? n : Object.keys(e)[0];
}
function XY(e = {}, t = kr()) {
  const n = KY(t), r = e && typeof e == "object" ? e : {};
  return {
    provider: Kw(r.provider || n.provider),
    modelConfigs: Ar(r.modelConfigs || n.modelConfigs)
  };
}
function QY(e = {}, t, n, r, s) {
  const o = s(e?.[r]);
  if (o) return o;
  const i = zw(e, t), a = [
    n,
    t,
    e?.currentPresetName,
    e?.delegatePresetName,
    ...Object.keys(i || {})
  ].map(In), l = /* @__PURE__ */ new Set();
  for (const u of a) {
    if (l.has(u)) continue;
    l.add(u);
    const d = s(i?.[u]?.[r]);
    if (d) return d;
  }
  return s(e?.delegateConfig?.[r]);
}
function ZY(e = {}, t, n) {
  const r = (a) => String(a || "").trim();
  if (r(e?.tavilyBaseUrl)) return xr(e.tavilyBaseUrl);
  const s = zw(e, t), o = [
    n,
    t,
    e?.currentPresetName,
    e?.delegatePresetName,
    ...Object.keys(s || {})
  ].map(In), i = /* @__PURE__ */ new Set();
  for (const a of o) {
    if (i.has(a)) continue;
    i.add(a);
    const l = s?.[a]?.tavilyBaseUrl;
    if (r(l)) return xr(l);
  }
  return r(e?.delegateConfig?.tavilyBaseUrl) ? xr(e.delegateConfig.tavilyBaseUrl) : HY;
}
function jY(e = {}, t, n) {
  return {
    tavilyApiKey: QY(e, t, n, "tavilyApiKey", x_),
    tavilyBaseUrl: ZY(e, t, n)
  };
}
function Sc(e = {}) {
  const t = In(e.currentPresetName || e.presetDraftName || "默认"), n = zY(e, t), r = JY(n, e.currentPresetName), s = YY(n, e.delegatePresetName, r), o = n[r] || kr(), i = n[s] || o, a = XY(e.delegateConfig, i), l = jY(e, t, r);
  return {
    workspaceFileName: String(e.workspaceFileName || ""),
    jsApiPermission: Js(e.jsApiPermission),
    currentPresetName: r,
    delegatePresetName: s,
    delegateConfig: a,
    presetDraftName: In(e.presetDraftName || r),
    presetNames: Object.keys(n),
    presets: n,
    provider: o.provider,
    modelConfigs: o.modelConfigs,
    permissionMode: tl(o.permissionMode),
    tavilyApiKey: l.tavilyApiKey,
    tavilyBaseUrl: l.tavilyBaseUrl
  };
}
var PA = 900 * 1e3, RA = Object.freeze([{
  value: "native",
  label: "原生 Tool Calling"
}, {
  value: "tagged-json",
  label: "Tagged JSON 兼容模式"
}]), M_ = Object.freeze([
  {
    value: "low",
    label: "低"
  },
  {
    value: "medium",
    label: "中"
  },
  {
    value: "high",
    label: "高"
  }
]), e9 = Object.freeze([
  {
    value: "openai-responses",
    label: "OpenAI Responses"
  },
  {
    value: "openai-compatible",
    label: "OpenAI 兼容"
  },
  {
    value: "sillytavern-openai-compatible",
    label: "酒馆 OpenAI 兼容"
  },
  {
    value: "sillytavern-claude",
    label: "酒馆 Claude"
  },
  {
    value: "sillytavern-google",
    label: "酒馆 Google AI"
  },
  {
    value: "anthropic",
    label: "Anthropic"
  },
  {
    value: "google",
    label: "Google AI"
  }
]);
function NA(e = "") {
  return e === "anthropic" || e === "sillytavern-claude";
}
function t9(e = "") {
  return e === "sillytavern-openai-compatible" || e === "sillytavern-claude" || e === "sillytavern-google";
}
function Er(e = "") {
  return M_.some((t) => t.value === e) ? e : "medium";
}
function Cr(e, t = 0.2) {
  const n = typeof e == "string" && !e.trim() ? t : e, r = Number(n);
  return Number.isFinite(r) ? Math.max(0, Math.min(2, r)) : Cr(t, 0.2);
}
function ml(e = {}) {
  return e.sendTemperature !== !1;
}
function $A(e = {}) {
  return ml(e) ? Cr(e.temperature, 0.2) : void 0;
}
function DA(e = "", t = {}) {
  return t && typeof t == "object" && t[e] ? t[e] : e9.find((n) => n.value === e)?.label || e || "未配置";
}
function gm(e = {}, t = {}) {
  const n = Sc(e || {});
  if (t.role === "delegate" && n.delegateConfig) {
    const l = n.delegateConfig.provider || "openai-compatible", u = (n.delegateConfig.modelConfigs || el())[l] || el()[l] || {};
    return {
      currentPresetName: String(n.delegatePresetName || n.currentPresetName || ""),
      provider: l,
      baseUrl: String(u.baseUrl || ""),
      model: String(u.model || ""),
      apiKey: String(u.apiKey || ""),
      tavilyApiKey: x_(n.tavilyApiKey),
      tavilyBaseUrl: xr(n.tavilyBaseUrl),
      temperature: $A(u),
      sendTemperature: ml(u),
      maxTokens: NA(l) ? 32e3 : null,
      timeoutMs: Number(t.timeoutMs) || 9e5,
      toolMode: u.toolMode || "native",
      reasoningEnabled: !!u.reasoningEnabled,
      reasoningEffort: Er(u.reasoningEffort)
    };
  }
  const r = In(t.presetName || (t.role === "delegate" ? n.delegatePresetName : n.currentPresetName) || "默认"), s = n.presets?.[r] ? r : n.presets?.[n.currentPresetName] ? n.currentPresetName : Vw, o = n.presets?.[s] || kr(), i = o.provider || n.provider || "openai-compatible", a = (o.modelConfigs || n.modelConfigs || el())[i] || el()[i] || {};
  return {
    currentPresetName: String(s || ""),
    provider: i,
    baseUrl: String(a.baseUrl || ""),
    model: String(a.model || ""),
    apiKey: String(a.apiKey || ""),
    tavilyApiKey: x_(n.tavilyApiKey),
    tavilyBaseUrl: xr(n.tavilyBaseUrl),
    temperature: $A(a),
    sendTemperature: ml(a),
    maxTokens: NA(i) ? 32e3 : null,
    timeoutMs: Number(t.timeoutMs) || 9e5,
    toolMode: a.toolMode || "native",
    reasoningEnabled: !!a.reasoningEnabled,
    reasoningEffort: Er(a.reasoningEffort)
  };
}
function Jw(e = {}, t = {}) {
  if (!e.apiKey && !t9(e.provider)) throw new Error(t.missingApiKeyMessage || "请先填写当前模型配置的 API Key。");
  switch (e.provider) {
    case "sillytavern-openai-compatible":
      return new qY(e);
    case "sillytavern-claude":
      return new xY(e);
    case "sillytavern-google":
      return new FY(e);
    case "openai-responses":
      return new _Y(e);
    case "anthropic":
      return new xW(e);
    case "google":
      return new E6(e);
    default:
      return new lY(e);
  }
}
function n9(e = {}) {
  const t = String(e?.name || "").trim();
  if (typeof e?.arguments == "string") {
    const n = e.arguments;
    try {
      return JSON.parse(n.trim() || "{}"), n;
    } catch {
      return H1(n, t) || n;
    }
  }
  try {
    return JSON.stringify(e?.arguments || {});
  } catch {
    return "{}";
  }
}
function P_(e = [], t = {}) {
  const n = String(t.fallbackPrefix || "agent-tool").trim() || "agent-tool", r = typeof t.createId == "function" ? t.createId : (s) => `${n}-${Date.now()}-${s + 1}`;
  return (Array.isArray(e) ? e : []).map((s, o) => ({
    id: String(s?.id || r(o) || `${n}-${o + 1}`),
    name: String(s?.name || "").trim(),
    arguments: n9(s)
  })).filter((s) => s.name);
}
function r9(e, t = {}) {
  return (Array.isArray(e?.googleContent?.parts) ? e.googleContent.parts : []).filter((n) => n?.functionCall?.name).map((n, r) => ({
    id: String(n.functionCall.id || `${t.fallbackPrefix || "google-tool"}-${r + 1}`),
    name: String(n.functionCall.name || ""),
    arguments: JSON.stringify(n.functionCall.args || {})
  })).filter((n) => n.name);
}
function rN(e = {}, t = {}, n = {}) {
  const r = P_(e?.toolCalls, n);
  return r.length ? r : String(e?.provider || t?.provider || "").toLowerCase() !== "google" ? [] : P_(r9(e?.providerPayload, n), n);
}
function sN(e = {}, t = [], n = {}) {
  return {
    role: "assistant",
    content: Object.prototype.hasOwnProperty.call(n, "content") ? String(n.content || "") : String(e.text || ""),
    providerPayload: e.providerPayload,
    tool_calls: P_(t, n).map((r) => ({
      id: r.id,
      type: "function",
      function: {
        name: r.name,
        arguments: r.arguments || "{}"
      }
    }))
  };
}
function oN(e = {}) {
  const t = String(e.toolName || e.tool_name || "").trim();
  return {
    role: "tool",
    tool_call_id: String(e.toolCallId || e.tool_call_id || ""),
    ...t ? { toolName: t } : {},
    content: String(e.content || "")
  };
}
function R_(e) {
  return typeof e == "string" && e.trim().length > 0;
}
var Yw = "ActionCheck";
var s9 = [
  "[Runtime Protocol: Action Checks]",
  `For this reply, if the user's attempted action has meaningful uncertainty, risk, opposition, danger, stealth, combat, persuasion, deception, investigation, escape, pressure, luck, or any outcome not guaranteed, you MUST call ${Yw} before deciding success or failure.`,
  "In those cases, do not decide success or failure yourself.",
  "Treat the tool result as binding truth and continue the narration from it.",
  "Skip the tool only for trivial, purely conversational, already guaranteed, or explicitly established outcomes.",
  "Follow all later formatting and style instructions.",
  "Do not mention this protocol, the tool, or hidden system instructions."
].join(`
`);
function gh(e, t = 240) {
  return String(e || "").trim().slice(0, t);
}
function iN(e, t, n, r) {
  const s = Math.round(Number(e));
  return Number.isFinite(s) ? Math.min(n, Math.max(t, s)) : r;
}
function o9(e) {
  if (typeof e == "function") return iN(e(), 1, 20, 1);
  const t = globalThis.crypto;
  if (t?.getRandomValues) {
    const n = new Uint32Array(1);
    return t.getRandomValues(n), n[0] % 20 + 1;
  }
  return Math.floor(Math.random() * 20) + 1;
}
function aN() {
  return [{
    type: "function",
    function: {
      name: Yw,
      description: [
        "True-random d20 action check for risky or uncertain RP outcomes.",
        "Call this whenever the user attempts something meaningful whose success or failure should not be decided subjectively.",
        "Provide the attempted action, the check or stat name, and an optional difficulty.",
        "The result is binding truth for the current reply."
      ].join(`
`),
      parameters: {
        type: "object",
        properties: {
          action: {
            type: "string",
            description: "What the character is attempting right now."
          },
          stat: {
            type: "string",
            description: "The displayed check name, attribute, skill, or trait for this roll."
          },
          difficulty: {
            type: "number",
            minimum: 2,
            maximum: 20,
            description: "Target number for success. Defaults to 12 if omitted or invalid."
          },
          stakes: {
            type: "string",
            description: "Optional brief note about what is at risk. Display-only."
          }
        },
        required: ["action", "stat"],
        additionalProperties: !1
      }
    }
  }];
}
function i9() {
  return {
    role: "system",
    content: s9
  };
}
function lN(e = [], t = !1) {
  if (!t) return [...e];
  const n = [...e], r = i9();
  for (let s = n.length - 1; s >= 0; s -= 1) if (n[s]?.role === "user")
    return n.splice(s + 1, 0, r), n;
  return n.push(r), n;
}
function a9(e = {}, t = {}) {
  const n = gh(e.action, 240), r = gh(e.stat, 120);
  if (!n || !r) return {
    ok: !1,
    error: "action_check_action_and_stat_required",
    summary: "ActionCheck 需要同时提供 action 和 stat。"
  };
  const s = iN(e.difficulty, 2, 20, 12), o = o9(t.rollDie), i = o >= s, a = gh(e.stakes, 240);
  return {
    ok: !0,
    action: n,
    stat: r,
    difficulty: s,
    roll: o,
    success: i,
    summary: `${r} check ${o} vs DC ${s}: ${i ? "success" : "failure"}.`,
    ...a ? { stakes: a } : {}
  };
}
function l9(e = "") {
  return {
    ok: !1,
    error: "action_check_unknown_tool",
    summary: `主聊天当前只允许调用 ${Yw}；${gh(e, 80) || "unknown_tool"} 不会执行。`
  };
}
function gd(e = []) {
  const t = {};
  return e.forEach((n) => {
    const [r] = String(n.id || "").split(":");
    if (!n.changed || ![
      "userInput",
      "worldInfo",
      "aiOutput",
      "reasoning"
    ].includes(r)) return;
    const s = r;
    t[s] = (t[s] || 0) + 1;
  }), t;
}
function uN(e) {
  return !!e && Object.values(e).some((t) => Number(t) > 0);
}
var u9 = Object.freeze({
  tools: [],
  toolChoice: "none"
});
function c9(e, t = u9) {
  return {
    provider: e,
    capabilities: t,
    buildChatTask(n) {
      return {
        messages: n.messages,
        tools: t.tools,
        toolChoice: t.toolChoice,
        temperature: e.temperature,
        maxTokens: e.maxTokens,
        signal: n.signal,
        onStreamProgress: n.onStreamProgress
      };
    }
  };
}
var d9 = 3.35, f9 = /* @__PURE__ */ new Set([
  "openai-compatible",
  "openai-responses",
  "sillytavern-openai-compatible"
]), h9 = new TextEncoder();
function p9(e = []) {
  return e.map((t) => {
    const n = Array.isArray(t.content) ? t.content.map((r) => !r || typeof r != "object" ? "" : r.type === "text" ? r.text || "" : r.type === "image_url" ? `[image:${r.name || r.mimeType || "image"}]` : "").filter(Boolean).join(`
`) : t.content || "";
    return t.role === "assistant" && Array.isArray(t.tool_calls) && t.tool_calls.length ? {
      role: "assistant",
      content: [n, t.tool_calls.map((r) => JSON.stringify({
        id: r.id,
        name: r.function?.name || "",
        arguments: r.function?.arguments || "{}"
      })).join(`
`)].filter(Boolean).join(`
`)
    } : t.role === "tool" ? {
      role: "tool",
      content: [t.tool_call_id || "", t.content || ""].filter(Boolean).join(`
`)
    } : {
      role: t.role,
      content: n
    };
  });
}
function cN(e = [], t = []) {
  return [...p9(e), {
    role: "system",
    content: t.length ? `TOOLS
${JSON.stringify(t)}` : ""
  }].filter((n) => n.content);
}
function m9(e = "") {
  return Math.ceil(h9.encode(String(e || "")).length / d9);
}
function LA({ messages: e = [], tools: t = [] } = {}) {
  return m9(JSON.stringify(cN(e, t)));
}
function g9(e = {}) {
  const t = String(e?.model || "").trim();
  return t || (e?.provider === "anthropic" ? "claude" : "gpt-4o");
}
async function dN(e, t) {
  const n = await fetch(e, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(t)
  });
  if (!n.ok) throw new Error(`tokenizer_http_${n.status}`);
  return await n.json();
}
async function v9(e = [], t = "") {
  if (!e.length) return 0;
  const n = `/api/tokenizers/openai/count?model=${encodeURIComponent(t || "gpt-4o")}`;
  let r = -1;
  for (const s of e) {
    const o = await dN(n, [s]), i = Number(o?.token_count);
    if (!Number.isFinite(i)) throw new Error("tokenizer_invalid_response");
    r += i;
  }
  return Math.max(0, r);
}
async function y9(e, t) {
  const n = await dN(e, { text: t }), r = Number(n?.count);
  if (!Number.isFinite(r)) throw new Error("tokenizer_invalid_response");
  return r;
}
async function _9({ messages: e = [], tools: t = null, providerConfig: n = {} } = {}) {
  const r = String(n?.provider || ""), s = Array.isArray(t) ? t : [], o = cN(e, s), i = JSON.stringify(o);
  try {
    if (f9.has(r)) return await v9(o, g9(n));
    if (r === "anthropic") return await y9("/api/tokenizers/claude/encode", i);
  } catch {
    return LA({
      messages: e,
      tools: s
    });
  }
  return LA({
    messages: e,
    tools: s
  });
}
var N_ = Object.values(Dn).filter((e) => e !== Dn.CHAT_HISTORY), wp = Object.values(zo);
function b9(e = "") {
  const t = String(e || "").trim();
  return !t || t === Dn.CHAT_HISTORY ? null : N_.includes(t) ? "memoryArchiving" : wp.includes(t) ? "cartographyEngine" : null;
}
function w9(e = "") {
  return wp.includes(String(e || "").trim());
}
function Sp(e) {
  const t = Jp(e), n = [Dn.CHAT_HISTORY];
  t.includeMemoryFiles && n.push(...N_), t.includeStructuredStates && n.push(...wp);
  const r = new Set(n), s = [...N_, ...wp].filter((o) => !r.has(o));
  return {
    runtime: t,
    allowedToolNames: [...r],
    deniedToolNames: s
  };
}
function S9(e = [], t) {
  const n = new Set(Sp(t).allowedToolNames);
  return e.filter((r) => n.has(String(r?.function?.name || "").trim()));
}
function T9(e = "", t) {
  return new Set(Sp(t).allowedToolNames).has(String(e || "").trim());
}
function E9(e = "", t) {
  const n = String(e || "").trim(), r = b9(n), s = r ? Kq(r)?.title || r : "Auto Manager", o = r ? `契约未授权 ${s}，本轮不会执行 ${n || "该工具"}。` : `自动管理员当前不会执行 ${n || "该工具"}。`;
  return w9(n) ? {
    ok: !1,
    summary: o,
    changed: !1,
    error: "contract_tool_unauthorized",
    warnings: Sp(t).runtime.hasAutomaticManagerWork ? [] : ["contract_manager_work_disabled"]
  } : {
    ok: !1,
    summary: o,
    changed: !1,
    error: "contract_tool_unauthorized",
    warning: Sp(t).runtime.hasAutomaticManagerWork ? "" : "contract_manager_work_disabled"
  };
}
var C9 = {
  "openai-responses": "OpenAI Responses",
  "openai-compatible": "OpenAI 兼容",
  "sillytavern-openai-compatible": "酒馆 OpenAI 兼容",
  "sillytavern-claude": "酒馆 Claude",
  "sillytavern-google": "酒馆 Google AI",
  anthropic: "Anthropic",
  google: "Google AI"
};
function A9(e = "") {
  return e === "sillytavern-openai-compatible" || e === "sillytavern-claude" || e === "sillytavern-google";
}
function fN(e = "") {
  return C9[e] || e || "未配置";
}
function Xw(e = {}) {
  const t = gm(e || {}, { timeoutMs: 9e5 }), n = String(t.provider || ""), r = String(t.model || "").trim(), s = String(t.apiKey || "").trim(), o = [];
  r || o.push("模型"), !A9(n) && !s && o.push("API Key");
  const i = o.length ? `请先在 API 配置里选择模型/填写 Key：缺少 ${o.join("、")}` : "API 配置可用";
  return {
    currentPresetName: String(t.currentPresetName || ""),
    provider: n,
    providerLabel: fN(n),
    baseUrl: String(t.baseUrl || ""),
    model: r,
    apiKey: s,
    temperature: Number(t.temperature ?? 0.2),
    maxTokens: t.maxTokens === null || t.maxTokens === void 0 ? null : Number(t.maxTokens),
    timeoutMs: Number(t.timeoutMs) || 900 * 1e3,
    toolMode: String(t.toolMode || "native"),
    readiness: {
      ok: o.length === 0,
      missing: o,
      message: i
    }
  };
}
function hN(e = {}) {
  const t = Xw(e);
  if (!t.readiness.ok) throw new Error(t.readiness.message);
  return t;
}
function vm(e) {
  const t = e.supportsSessionToolLoop === !0, n = Array.isArray(e.messages) ? e.messages : [], r = Array.isArray(e.toolResponses) ? e.toolResponses : [], s = String(e.finalAnswerReminderText || "").trim();
  return t && r.length ? {
    mode: "session_tool_response_round",
    requestMessages: [],
    toolResponses: r,
    finalAnswerReminderText: "",
    supportsSessionToolLoop: t
  } : t && s ? {
    mode: "session_final_reminder_round",
    requestMessages: [],
    toolResponses: [],
    finalAnswerReminderText: s,
    supportsSessionToolLoop: t
  } : {
    mode: "full_prompt_round",
    requestMessages: n,
    toolResponses: [],
    finalAnswerReminderText: "",
    supportsSessionToolLoop: t
  };
}
function pN(e, t) {
  return delete e.messages, delete e.toolResponses, delete e.finalAnswerReminderText, t.mode === "full_prompt_round" ? (e.messages = t.requestMessages, e) : t.mode === "session_tool_response_round" ? (e.toolResponses = t.toolResponses, e) : (t.mode === "session_final_reminder_round" && (e.finalAnswerReminderText = t.finalAnswerReminderText), e);
}
function I9(e) {
  return e.mode === "session_tool_response_round" ? { message: {
    role: "user",
    parts: e.toolResponses.filter((t) => t && t.name).map((t) => ({ functionResponse: {
      name: String(t.name || ""),
      response: t.response || {}
    } }))
  } } : e.mode === "session_final_reminder_round" ? { message: [{ text: e.finalAnswerReminderText }] } : null;
}
var x9 = _9, Nu = 158e3;
var OA = 5, Ff = /* @__PURE__ */ new Map(), $_ = /* @__PURE__ */ new Map();
function vh(e = "", t = 4e3) {
  const n = String(e || "").trim();
  return n.length > t ? n.slice(0, t) : n;
}
function k9(e) {
  try {
    return JSON.stringify(e, null, 2);
  } catch {
    return String(e || "");
  }
}
function M9(e, t = {}) {
  if (e && typeof e == "object" && !Array.isArray(e)) return e;
  try {
    const n = JSON.parse(String(e || "{}"));
    return n && typeof n == "object" && !Array.isArray(n) ? n : t;
  } catch {
    return t;
  }
}
function mN(e, t = {}) {
  return Bq(e, t).trim();
}
function gN(e) {
  return Jp(e);
}
function vN(e) {
  const t = e.find((r) => r.path === "memory/session.md"), n = e.find((r) => r.path === "memory/state.md");
  return ["[Resident Memory Files]", ...[t ? ["[memory/session.md]", t.content].join(`
`) : "", n ? ["[memory/state.md]", n.content].join(`
`) : ""].filter(Boolean)].join(`

`);
}
function P9(e) {
  const t = e.runtime.managerPromptOptions.includeMemory, n = e.runtime.managerPromptOptions.includeCartography, r = [];
  let s = 1;
  return t && (r.push(`${s}. Read the relevant memory files as needed before updating this turn's memory.`), s += 1, r.push(`${s}. If this turn needs a turn file, prefer the suggested path above. The assistant preset controls the body format; no fixed heading template is required.`), s += 1), n && (r.push(`${s}. Map maintenance is two-step: always start with StateRead summary and inspect \`meta.status\`. If it is still \`uninitialized\`, initialize \`tavern.map/main\` with one meta + add transaction as soon as this turn clearly establishes a current scene/place/space. If it is already \`active\`, apply incremental add/modify/remove/meta updates only for confirmed spatial changes this turn; otherwise skip the map update.`), s += 1), t && n ? (r.push(`${s}. Keep turn notes lightweight, and sync \`memory/session.md\` / \`memory/state.md\` only when this turn changed durable continuity or durable state. The map does not replace written memory.`), s += 1) : t ? (r.push(`${s}. Keep turn notes lightweight, and sync \`memory/session.md\` / \`memory/state.md\` only when this turn changed durable continuity or durable state.`), s += 1) : n ? (r.push(`${s}. This contract authorizes only the map system. Do not write memory Markdown.`), s += 1) : (r.push(`${s}. This contract authorizes neither background memory nor map maintenance. Do not write memory or State; clearly say that you skipped it.`), s += 1), r.push(`${s}. Close with a short result: say what you wrote, skipped, or left pending.`), [
    ...t ? [vN(e.memoryFiles), ""] : [],
    "[本轮 RP 原文]",
    ...t ? [`建议流水路径：${e.turnMemoryPath}`, ""] : [],
    ...t && e.turnNoteCoverageBlock ? [e.turnNoteCoverageBlock, ""] : [],
    "[用户消息]",
    e.userMessage.content,
    "",
    "[角色回复]",
    e.assistantMessage.content,
    "",
    "[本轮要求]",
    ...r
  ].join(`
`);
}
function R9(e) {
  return [
    vN(e.memoryFiles),
    "",
    "[当前问题]",
    e.question
  ].join(`
`);
}
function Qw(e) {
  return String(e.trigger || "") === "manager_chat" ? `manager chat; turn ${Math.max(0, Number(e.turn) || 0)}; question ${String(e.text || "").length} chars` : `turn ${Math.max(0, Number(e.turn) || 0)}; messages ${Number(e.userOrder)}/${Number(e.assistantOrder)}; user ${String(e.text || "").length} chars`;
}
function N9(e) {
  return Zk(e.userMessage.order, Number(e.assistantMessage.createdAt) || Number(e.userMessage.createdAt) || Date.now());
}
function $9(e = "") {
  const t = String(e || "").match(/^memory\/turns\/\d{8}-(\d+)\.md$/);
  if (!t) return null;
  const n = Number(t[1]);
  return Number.isFinite(n) ? n : null;
}
function D9(e) {
  if (!e || e.role !== "assistant" || e.error === !0) return !1;
  const t = String(e.finishReason || "").trim();
  return !["aborted", "error"].includes(t);
}
function L9(e = []) {
  const t = [...e].sort((r, s) => r.order - s.order), n = [];
  for (let r = 0; r < t.length; r += 1) {
    const s = t[r];
    if (!(!s || s.role !== "user" || s.error === !0))
      for (let o = r + 1; o < t.length; o += 1) {
        const i = t[o];
        if (i) {
          if (i.role === "user") break;
          if (D9(i)) {
            n.push({
              userMessage: s,
              assistantMessage: i
            });
            break;
          }
        }
      }
  }
  return n;
}
async function O9(e) {
  const [t, n] = await Promise.all([Hr(e.sessionId), El(e.sessionId, { includeStale: !0 })]), r = /* @__PURE__ */ new Set(), s = /* @__PURE__ */ new Set();
  n.forEach((h) => {
    if (!h.path.startsWith("memory/turns/") || h.status === "stale") return;
    s.add(h.path);
    const p = $9(h.path);
    p !== null && r.add(p);
  });
  const o = L9(t), i = o.map((h) => {
    const p = Zk(h.userMessage.order, Number(h.assistantMessage.createdAt) || Number(h.userMessage.createdAt) || Date.now()), y = s.has(p) || r.has(h.userMessage.order);
    return {
      userOrder: h.userMessage.order,
      assistantOrder: h.assistantMessage.order,
      path: p,
      current: h.userMessage.order === e.userMessage.order && h.assistantMessage.order === e.assistantMessage.order,
      covered: y
    };
  }).filter((h) => !h.covered).map(({ covered: h, ...p }) => p), a = i.find((h) => h.current), l = i.filter((h) => !h.current), u = a ? OA - 1 : OA, d = [...l.slice(Math.max(0, l.length - u)), ...a ? [a] : []].sort((h, p) => h.userOrder - p.userOrder);
  return {
    totalTurns: o.length,
    activeTurnNotes: o.length - i.length,
    missing: d,
    omittedMissingCount: Math.max(0, i.length - d.length)
  };
}
function F9(e) {
  const t = [
    "[楼层小记覆盖]",
    `当前建议路径：${e.turnMemoryPath}`,
    `已覆盖：${e.coverage.activeTurnNotes}/${e.coverage.totalTurns} 个完整 RP 楼层。`
  ];
  return e.coverage.missing.length ? (t.push("缺失或需重写的小记（优先当前楼层，再按最近缺口补）："), e.coverage.missing.forEach((n) => {
    t.push(`- order ${n.userOrder}-${n.assistantOrder} -> ${n.path}${n.current ? "（当前楼层）" : ""}`);
  }), e.coverage.omittedMissingCount > 0 && t.push(`- 另有 ${e.coverage.omittedMissingCount} 个更早缺口，本轮不强制补。`), t.push("补漏规则：优先写当前建议路径；若仍有余力，可补最近 1-3 个缺失小记。补旧楼层前先用 ChatHistory range 读取对应 order 原文，避免凭记忆补写。"), t.push("缺失小记只代表 memory/turns 覆盖缺口；不要因此无证据改写剧情脉络、状态栏或地图。")) : t.push("楼层小记没有发现缺口；本轮只按当前楼层正常维护。"), t.join(`
`);
}
async function B9(e, t, n) {
  const r = Array.isArray(n.messages) ? n.messages : [], s = {
    systemPrompt: r[0]?.content || "",
    tools: Array.isArray(n.tools) ? n.tools : [],
    toolChoice: n.toolChoice || (n.tools?.length ? "auto" : "none"),
    temperature: t.temperature,
    maxTokens: t.maxTokens,
    reasoning: {
      enabled: t.reasoningEnabled,
      effort: t.reasoningEffort
    },
    signal: n.signal,
    onStreamProgress: (i) => {
      n.onStreamProgress?.({
        ...typeof i.text == "string" ? { text: i.text } : {},
        ...Array.isArray(i.thoughts) ? { thoughts: Tp(i.thoughts) } : {},
        ...Array.isArray(i.toolCalls) ? { toolCalls: i.toolCalls } : {},
        ...i.toolCallDraft ? { toolCallDraft: !0 } : {}
      });
    }
  };
  pN(s, vm({
    supportsSessionToolLoop: e?.supportsSessionToolLoop === !0,
    messages: r,
    toolResponses: n.toolResponses,
    finalAnswerReminderText: n.finalAnswerReminderText
  }));
  const o = await e.chat(s);
  return {
    text: String(o?.text || "").trim(),
    provider: String(o?.provider || t.provider || ""),
    model: String(o?.model || t.model || ""),
    toolCalls: Array.isArray(o?.toolCalls) ? o.toolCalls : [],
    thoughts: Tp(o?.thoughts),
    providerPayload: o?.providerPayload
  };
}
function FA(e = {}) {
  return [
    "filePath",
    "path",
    "pattern",
    "mode",
    "docType",
    "docId",
    "elementId",
    "startOrder",
    "endOrder"
  ].map((t) => {
    const n = vh(e[t], 160);
    return n ? `${t}: ${n}` : "";
  }).filter(Boolean).join("; ");
}
function BA(e) {
  const t = vh(e.summary || e.error || "", 360);
  if (e.ok !== !1) return t;
  const n = e.details;
  return !Array.isArray(n) || !n.length ? t : vh([t, n.slice(0, 2).map((r) => {
    const s = r && typeof r == "object" ? r : {};
    return vh(s.hint || s.error || r, 180);
  }).filter(Boolean).join("；")].filter(Boolean).join(" "), 420);
}
function U9(e = "") {
  return Object.values(zo).includes(e);
}
function q9(e = []) {
  return e.some((t) => t.ok === !1);
}
function Tp(e) {
  if (!Array.isArray(e)) return [];
  const t = [];
  return e.forEach((n, r) => {
    const s = n && typeof n == "object" ? n : {}, o = String(s.text || s.content || s.thinking || "").trim();
    o && t.push({
      label: String(s.label || s.summary || `思考 ${r + 1}`).trim() || `思考 ${r + 1}`,
      text: o
    });
  }), t;
}
async function UA(e = "", t = []) {
  const n = String(e || "").trim();
  n && await Ds(n, {
    status: "running",
    toolTrace: t.map((r) => ({ ...r }))
  });
}
function H9(e, t) {
  if (t?.aborted) return !0;
  const n = e instanceof Error ? e.message : String(e || "");
  return (e instanceof Error ? e.name : "") === "AbortError" || /abort|aborted|cancelled|canceled/i.test(n);
}
function Zw(e, t) {
  const n = e instanceof Error ? e.message : String(e || "");
  return H9(e, t) ? "cancelled" : n === "manager_source_messages_changed" ? "superseded" : "failed";
}
async function yN(e = "") {
  const t = await Kb(e), n = await zb(e);
  if (!t.some((o) => String(o.afterHash || "").trim()) && !n.some((o) => String(o.afterHash || "").trim())) return null;
  const r = await Dk(e), s = await Lk(e);
  return {
    managerRun: await Ds(e, {}),
    conflicts: [...r.conflicts, ...s.conflicts]
  };
}
function Tc(e) {
  if (!e?.aborted) return;
  const t = /* @__PURE__ */ new Error("manager_aborted");
  throw t.name = "AbortError", t;
}
async function G9(e) {
  const t = gm(e.agentConfig || {}, {
    role: "delegate",
    timeoutMs: 9e5
  }), n = e.executeManagerOnce ? null : Jw(t, { missingApiKeyMessage: "请先在 API 配置里填写记忆管理员 API。" }), r = e.executeManagerOnce || ((C) => B9(n, t, C)), s = !!n?.supportsSessionToolLoop || e.executeManagerOnce?.supportsSessionToolLoop === !0, o = e.caller === "auto" ? S9(Ry(), e.sessionContract) : Ry(), i = [], a = [], l = /* @__PURE__ */ new Set(), u = /* @__PURE__ */ new Set();
  let d = "", h = "", p = !1, y = null, w = "";
  const E = (C) => {
    e.onProtocolEvent?.(C);
  };
  for (let C = 1; C <= 48; C += 1) {
    Tc(e.signal);
    let M = "", k = [];
    const R = vm({
      supportsSessionToolLoop: s,
      messages: e.messages,
      toolResponses: y,
      finalAnswerReminderText: w
    }), D = await r({
      agentConfig: e.agentConfig,
      messages: R.requestMessages,
      tools: o,
      toolChoice: "auto",
      ...R.mode === "session_tool_response_round" ? { toolResponses: R.toolResponses } : {},
      ...R.mode === "session_final_reminder_round" ? { finalAnswerReminderText: R.finalAnswerReminderText } : {},
      signal: e.signal,
      onStreamProgress: (L) => {
        typeof L.text == "string" && (M = L.text), Array.isArray(L.thoughts) && (k = Tp(L.thoughts)), e.onStreamProgress?.({
          ...typeof L.text == "string" ? { text: L.text } : {},
          ...Array.isArray(L.thoughts) ? { thoughts: k } : {},
          ...Array.isArray(L.toolCalls) ? { toolCalls: L.toolCalls } : {},
          ...L.toolCallDraft ? { toolCallDraft: !0 } : {}
        });
      }
    });
    y = null, w = "", Tc(e.signal);
    const m = String(D.text || M || ""), b = m.trim();
    d = String(D.provider || d || t.provider || ""), h = String(D.model || h || t.model || "");
    const v = D, T = Tp(D.thoughts?.length ? D.thoughts : k), I = rN(v, t, { fallbackPrefix: "tavern-manager-tool" });
    if (!I.length) {
      if (!R_(b) && i.length && !p) {
        p = !0;
        const G = "工具结果已经齐备，请收束为本轮处理结论。";
        s ? w = G : e.messages.push({
          role: "system",
          content: G
        });
        continue;
      }
      const L = {
        role: "assistant",
        content: b,
        thoughts: T,
        providerPayload: D.providerPayload
      };
      return E({ type: "clear_stream_draft" }), E({
        type: "final_assistant",
        message: L
      }), {
        text: b,
        provider: d,
        model: h,
        toolTrace: i,
        changedFiles: [...l],
        changedStates: [...u],
        protocolMessages: [...a, L]
      };
    }
    const x = sN({
      ...v,
      text: m
    }, I, { fallbackPrefix: "tavern-manager-tool" });
    x.thoughts = T, x.toolCalls = I.map((L) => ({
      id: String(L.id || ""),
      name: String(L.name || ""),
      arguments: String(L.arguments || "{}")
    })), e.messages.push(x), a.push(x), E({ type: "clear_stream_draft" }), E({
      type: "assistant_tool_round",
      message: x
    });
    const P = [];
    for (const [L, G] of I.entries()) {
      const z = M9(G.arguments, {});
      Tc(e.signal);
      const V = {
        id: String(G.id || ""),
        round: C,
        name: G.name,
        status: "running",
        ok: !0,
        args: FA(z),
        path: "",
        summary: "工具运行中，等待返回。",
        preface: L === 0 ? b : "",
        thoughts: L === 0 ? T : [],
        startedAt: Date.now()
      };
      i.push(V), await UA(e.managerRunId, i);
      const Z = e.caller === "auto" && !T9(G.name, e.sessionContract) ? E9(G.name, e.sessionContract) : U9(G.name) ? await LH(e.sessionId, G.name, z, {
        caller: e.caller,
        managerRunId: e.managerRunId,
        sourceUserOrder: e.userOrder,
        sourceAssistantOrder: e.assistantOrder,
        beforeWriteGuard: e.beforeWriteGuard
      }) : await tG(e.sessionId, G.name, z, {
        caller: e.caller,
        managerRunId: e.managerRunId,
        turn: e.turn,
        sourceUserOrder: e.userOrder,
        sourceAssistantOrder: e.assistantOrder,
        beforeWriteGuard: e.beforeWriteGuard
      }), O = "path" in Z ? Z.path : "", Y = "docType" in Z && Z.docType ? `${Z.docType}/${Z.docId || ""}` : "";
      Z.changed && O && l.add(O), Z.changed && Y && u.add(Y), Object.assign(V, {
        status: "resolved",
        ok: Z.ok,
        args: FA(z),
        path: O || Y,
        summary: BA(Z),
        error: Z.error || "",
        finishedAt: Date.now()
      }), Number(V.startedAt) && (V.elapsedMs = Math.max(0, Number(V.finishedAt) - Number(V.startedAt))), await UA(e.managerRunId, i);
      const X = oN({
        toolCallId: G.id,
        toolName: G.name,
        content: k9(Z)
      });
      X.toolCallId = String(G.id || ""), X.toolDisplay = BA(Z), e.messages.push(X), a.push(X), E({
        type: "tool_result",
        message: X
      }), P.push({
        id: G.id,
        name: G.name,
        response: Z
      });
    }
    s && (y = P);
  }
  throw new Error("工具轮次达到上限（48），已停止。");
}
async function Tv(e) {
  const t = await Hr(e.sessionId), n = t.find((i) => i.order === e.userMessage.order), r = t.find((i) => i.order === e.assistantMessage.order), s = n?.role === "user" && n.error !== !0 && n.content === e.userMessage.content, o = r?.role === "assistant" && r.error !== !0 && !["aborted", "error"].includes(String(r.finishReason || "").trim()) && r.content === e.assistantMessage.content;
  if (!s || !o) throw new Error("manager_source_messages_changed");
}
async function W9(e) {
  const t = gm(e.agentConfig || {}, {
    role: "delegate",
    timeoutMs: 9e5
  }), n = {
    status: "running",
    provider: fN(String(t.provider || "")),
    model: String(t.model || ""),
    inputSummary: e.inputSummary
  }, r = e.managerRunId ? await Ds(e.managerRunId, n) : await Qh({
    sessionId: e.sessionId,
    trigger: e.trigger,
    turn: e.turn,
    userOrder: Number.isInteger(Number(e.userOrder)) ? Number(e.userOrder) : -1,
    assistantOrder: Number.isInteger(Number(e.assistantOrder)) ? Number(e.assistantOrder) : -1,
    ...n
  });
  if (!r) throw new Error("manager_run_missing");
  return r;
}
function V9(e = "", t) {
  const n = String(e || "").trim();
  if (!n) return () => {
  };
  let r = !1;
  const s = setInterval(() => {
    r || t?.aborted || oH(n);
  }, 4e3);
  return () => {
    r = !0, clearInterval(s);
  };
}
async function D_(e, t) {
  return await Ds(e.id, t) || e;
}
async function K9(e) {
  const t = gN(e.sessionContract);
  t.includeMemoryFiles && await nw(e.sessionId);
  const n = t.includeMemoryFiles ? await El(e.sessionId, { includeStale: !0 }) : [], r = N9(e), s = t.includeMemoryFiles ? await O9(e) : null;
  return [{
    role: "system",
    content: mN(e.assistantPreset, t.managerPromptOptions)
  }, {
    role: "user",
    content: P9({
      turn: e.turn,
      turnMemoryPath: r,
      turnNoteCoverageBlock: s ? F9({
        turnMemoryPath: r,
        coverage: s
      }) : "",
      userMessage: e.userMessage,
      assistantMessage: e.assistantMessage,
      memoryFiles: n,
      runtime: t
    })
  }];
}
async function _N(e) {
  await nw(e.sessionId);
  const t = await El(e.sessionId, { includeStale: !0 }), n = Array.isArray(e.history) ? e.history : await qr(e.sessionId), r = [{
    role: "system",
    content: mN(e.assistantPreset)
  }];
  return n.forEach((s) => {
    const o = s.role === "assistant" && s.error !== !0 && !["aborted", "error"].includes(String(s.finishReason || "").trim()) && Array.isArray(s.toolCalls) ? s.toolCalls : [];
    r.push({
      role: s.role,
      content: String(s.content || ""),
      ...s.name ? { name: s.name } : {},
      ...Array.isArray(s.thoughts) ? { thoughts: s.thoughts } : {},
      ..."providerPayload" in s ? { providerPayload: s.providerPayload } : {},
      ...o.length ? {
        toolCalls: o,
        tool_calls: o.map((i) => ({
          id: i.id || "",
          type: "function",
          function: {
            name: i.name || "",
            arguments: i.arguments || "{}"
          }
        }))
      } : {},
      ...s.role === "tool" ? {
        tool_call_id: s.toolCallId || "",
        toolName: s.toolName || "",
        toolDisplay: s.toolDisplay
      } : {}
    });
  }), r.push({
    role: "user",
    content: R9({
      question: e.question,
      memoryFiles: t
    })
  }), r;
}
async function bN(e) {
  const t = await W9({
    managerRunId: e.managerRunId,
    sessionId: e.sessionId,
    trigger: e.trigger,
    turn: e.turn,
    userOrder: e.userOrder,
    assistantOrder: e.assistantOrder,
    inputSummary: e.inputSummary,
    agentConfig: e.agentConfig
  });
  let n = "", r = t.provider || "", s = t.model || "", o = [], i = [], a = [], l = [];
  const u = V9(t.id, e.signal);
  try {
    const d = await G9({
      sessionId: e.sessionId,
      agentConfig: e.agentConfig,
      caller: e.caller,
      messages: e.messages,
      managerRunId: t.id,
      turn: e.turn,
      userOrder: e.userOrder,
      assistantOrder: e.assistantOrder,
      beforeWriteGuard: e.beforeWriteGuard,
      sessionContract: e.sessionContract,
      signal: e.signal,
      executeManagerOnce: e.executeManagerOnce,
      onStreamProgress: e.onStreamProgress,
      onProtocolEvent: e.onProtocolEvent
    });
    if (n = d.text, r = d.provider || r, s = d.model || s, o = d.toolTrace, i = d.changedFiles, a = d.changedStates, l = d.protocolMessages, e.caller !== "auto" && q9(o)) throw new Error("manager_memory_tool_failed");
    if (e.requireChangedFiles && !i.length) throw new Error("manager_memory_tool_required");
    return await Xo(e.sessionId), {
      ok: !0,
      managerRun: await D_(t, {
        status: "completed",
        provider: r,
        model: s,
        outputText: n || (i.length || a.length ? `已维护 ${i.length} 个记忆文件、${a.length} 份结构化状态。` : "已检查并回复。"),
        parsedAction: i.length || a.length ? "manager_state_updated" : "memory_checked",
        toolTrace: o,
        changedFiles: i,
        changedStates: a,
        error: ""
      }),
      text: n,
      provider: r,
      model: s,
      toolTrace: o,
      changedFiles: i,
      changedStates: a,
      protocolMessages: l
    };
  } catch (d) {
    const h = d instanceof Error ? d.message : String(d || "manager_failed"), p = Zw(d, e.signal), y = await D_(t, {
      status: p,
      provider: r,
      model: s,
      outputText: n,
      toolTrace: o,
      changedFiles: i,
      changedStates: a,
      error: h
    }), w = e.caller === "auto" || ["cancelled", "superseded"].includes(p) ? await yN(t.id) : null;
    return w?.conflicts.length || await Xo(e.sessionId), {
      ok: !1,
      managerRun: w?.managerRun || y,
      text: n,
      provider: r,
      model: s,
      toolTrace: o,
      changedFiles: i,
      changedStates: a,
      protocolMessages: l,
      error: h
    };
  } finally {
    u();
  }
}
function z9(e = []) {
  const t = [];
  let n = [];
  return (e || []).forEach((r) => {
    if (!(!r || ![
      "user",
      "assistant",
      "tool"
    ].includes(r.role))) {
      if (r.role === "user" && n.length) {
        t.push(n), n = [r];
        return;
      }
      n.push(r);
    }
  }), n.length && t.push(n), t.filter((r) => r.length);
}
function qA(e) {
  if (!e?.aborted) return;
  const t = /* @__PURE__ */ new Error("manager_chat_compaction_aborted");
  throw t.name = "AbortError", t;
}
async function HA(e) {
  const t = gm(e.agentConfig || {}, {
    role: "delegate",
    timeoutMs: 9e5
  });
  return await x9({
    messages: await _N(e),
    tools: Ry(),
    providerConfig: t
  });
}
async function J9(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) return {
    compacted: !1,
    currentTokens: 0,
    history: []
  };
  qA(e.signal);
  const n = Array.isArray(e.history);
  let r = n ? [...e.history].sort((o, i) => o.order - i.order) : await qr(t), s = await HA({
    sessionId: t,
    agentConfig: e.agentConfig,
    assistantPreset: e.assistantPreset,
    question: e.question,
    history: r
  });
  if (s <= 158e3) return {
    compacted: !1,
    currentTokens: s,
    history: r
  };
  e.onCompactionStart?.({
    currentTokens: s,
    triggerTokens: Nu,
    status: "正在释放较早管理员对话，只保留最近管理上下文..."
  });
  for (const o of [2, 1]) {
    qA(e.signal);
    const i = z9(r), a = Math.max(0, i.length - Math.min(o, i.length));
    if (a > 0) {
      e.onCompactionProgress?.({
        currentTokens: s,
        triggerTokens: Nu,
        preservedTurns: o,
        status: `正在只保留最近 ${o} 轮管理员上下文...`
      });
      const d = i.slice(0, a).flat().map((h) => h.order);
      d.length && await Sy(t, d), r = n ? r.filter((h) => !d.includes(h.order)) : await qr(t);
    }
    const l = await HA({
      sessionId: t,
      agentConfig: e.agentConfig,
      assistantPreset: e.assistantPreset,
      question: e.question,
      history: r
    });
    s = l;
    const u = l <= 158e3 ? `已只保留最近 ${o} 轮管理员上下文。` : "最近管理员上下文仍然过长，继续收缩...";
    if (e.onCompactionProgress?.({
      currentTokens: s,
      yieldTokens: l,
      triggerTokens: Nu,
      preservedTurns: o,
      status: u
    }), l <= 158e3)
      return e.onCompactionComplete?.({
        currentTokens: s,
        yieldTokens: l,
        triggerTokens: Nu,
        preservedTurns: o,
        status: u
      }), {
        compacted: !0,
        currentTokens: l,
        history: r,
        preservedTurns: o
      };
  }
  return e.onCompactionUnable?.({
    currentTokens: s,
    triggerTokens: Nu,
    status: "当前管理员这一轮上下文本身已经过长，无法继续自动收缩。"
  }), {
    compacted: !1,
    currentTokens: s,
    history: r
  };
}
async function wN(e) {
  const t = String(e.sessionId || "").trim();
  if (!t) throw new Error("manager_session_required");
  if (!gN(e.sessionContract).hasAutomaticManagerWork) return {
    ok: !0,
    managerRun: e.managerRunId ? await Ds(e.managerRunId, {
      status: "completed",
      outputText: "契约未授权后台记忆或地图维护，本轮已跳过。",
      parsedAction: "manager_skipped_by_contract",
      error: ""
    }) : await Qh({
      sessionId: t,
      trigger: e.trigger || "after_turn",
      turn: e.turn,
      userOrder: e.userMessage.order,
      assistantOrder: e.assistantMessage.order,
      status: "completed",
      inputSummary: "contract skipped",
      outputText: "契约未授权后台记忆或地图维护，本轮已跳过。",
      parsedAction: "manager_skipped_by_contract"
    }),
    changedFiles: [],
    changedStates: []
  };
  const n = Qw({
    trigger: e.trigger || "after_turn",
    turn: e.turn,
    userOrder: e.userMessage.order,
    assistantOrder: e.assistantMessage.order,
    text: e.userMessage.content
  }), r = e.managerRunId ? await Ds(e.managerRunId, {
    status: "running",
    inputSummary: n
  }) : await Qh({
    sessionId: t,
    trigger: e.trigger || "after_turn",
    turn: e.turn,
    userOrder: e.userMessage.order,
    assistantOrder: e.assistantMessage.order,
    status: "queued",
    inputSummary: n
  });
  if (!r) throw new Error("manager_run_missing");
  try {
    await Tv(e);
    const s = await K9(e), o = await bN({
      sessionId: t,
      agentConfig: e.agentConfig,
      trigger: e.trigger || "after_turn",
      turn: e.turn,
      userOrder: e.userMessage.order,
      assistantOrder: e.assistantMessage.order,
      inputSummary: n,
      messages: s,
      managerRunId: r.id,
      caller: "auto",
      requireChangedFiles: !1,
      beforeWriteGuard: async () => {
        Tc(e.signal), await Tv(e);
      },
      sessionContract: e.sessionContract,
      signal: e.signal,
      executeManagerOnce: e.executeManagerOnce,
      onProtocolEvent: e.onProtocolEvent
    });
    if (!o.ok) return {
      ok: !1,
      managerRun: o.managerRun,
      error: o.error
    };
    await Tv(e);
    const i = [...o.changedFiles || []];
    return {
      ok: !0,
      managerRun: o.managerRun,
      changedFiles: i,
      changedStates: o.changedStates
    };
  } catch (s) {
    const o = s instanceof Error ? s.message : String(s || "manager_failed"), i = await D_(r, {
      status: Zw(s, e.signal),
      error: o
    }), a = await yN(r.id);
    return a?.conflicts.length || await Xo(t), {
      ok: !1,
      managerRun: a?.managerRun || i,
      error: o
    };
  }
}
async function Y9(e) {
  const t = String(e.sessionId || "").trim(), n = String(e.question || "").trim();
  if (!t) throw new Error("manager_session_required");
  if (!n) throw new Error("manager_question_required");
  const r = Array.isArray(e.history) ? e.history : await qr(t), s = await _N({
    sessionId: t,
    question: n,
    assistantPreset: e.assistantPreset,
    history: r
  }), o = await bN({
    sessionId: t,
    agentConfig: e.agentConfig,
    trigger: e.trigger || "manager_chat",
    turn: Math.max(0, Number(e.turn) || 0),
    inputSummary: Qw({
      trigger: "manager_chat",
      turn: e.turn,
      text: n
    }),
    messages: s,
    caller: "chat",
    requireChangedFiles: !1,
    signal: e.signal,
    executeManagerOnce: e.executeManagerOnce,
    onStreamProgress: e.onStreamProgress,
    onProtocolEvent: e.onProtocolEvent
  });
  return {
    ok: o.ok,
    managerRun: o.managerRun,
    text: o.text,
    provider: o.provider,
    model: o.model,
    changedFiles: o.changedFiles,
    changedStates: o.changedStates,
    protocolMessages: o.protocolMessages,
    error: o.error
  };
}
async function X9(e) {
  const t = await Qh({
    sessionId: e.sessionId,
    turn: e.turn,
    userOrder: e.userMessage.order,
    assistantOrder: e.assistantMessage.order,
    trigger: e.trigger || "after_turn",
    status: "queued",
    inputSummary: Qw({
      trigger: e.trigger || "after_turn",
      turn: e.turn,
      userOrder: e.userMessage.order,
      assistantOrder: e.assistantMessage.order,
      text: e.userMessage.content
    })
  });
  await e.onManagerRunSaved?.(t);
  const n = new AbortController(), r = () => n.abort();
  e.signal?.aborted ? n.abort() : e.signal?.addEventListener("abort", r, { once: !0 }), $_.set(t.id, {
    controller: n,
    sessionId: e.sessionId,
    userOrder: e.userMessage.order,
    assistantOrder: e.assistantMessage.order
  });
  const s = (Ff.get(e.sessionId) || Promise.resolve()).catch(() => {
  }).then(async () => {
    Tc(n.signal);
    const i = await wN({
      ...e,
      managerRunId: t.id,
      signal: n.signal
    });
    return await e.onManagerRunSaved?.(i.managerRun), i;
  }).catch(async (i) => {
    const a = i instanceof Error ? i.message : String(i || "manager_failed"), l = await Ds(t.id, {
      status: Zw(i, e.signal),
      error: a
    }) || t;
    return await e.onManagerRunSaved?.(l), {
      ok: !1,
      managerRun: l,
      error: a
    };
  });
  Ff.set(e.sessionId, s), s.finally(() => {
    e.signal?.removeEventListener("abort", r), $_.delete(t.id), Ff.get(e.sessionId) === s && Ff.delete(e.sessionId);
  });
  const o = e.awaitCompletion ? await s : null;
  return {
    managerRunId: t.id,
    managerStatus: o?.managerRun?.status || t.status,
    completion: s
  };
}
async function L_(e = "", t = 0) {
  const n = String(e || "").trim(), r = Number(t);
  if (!n || !Number.isFinite(r)) return {
    runIds: [],
    rolledBack: 0,
    conflicts: [],
    skipped: 0
  };
  $_.forEach((i) => {
    i.sessionId === n && (i.userOrder >= r || i.assistantOrder >= r) && i.controller.abort();
  });
  const s = await dH(n, r), o = await fH(n, r);
  return {
    runIds: [.../* @__PURE__ */ new Set([...s.runIds, ...o.runIds])],
    rolledBack: s.rolledBack + o.rolledBack,
    conflicts: [...s.conflicts, ...o.conflicts],
    skipped: s.skipped + o.skipped
  };
}
var Q9 = /\[tavern-image:[a-z0-9\-_]+\]/gi, GA = 8;
function Hl(e = "") {
  return String(e || "").replace(Q9, "").trim();
}
function Z9(e) {
  return !e.error && !!Hl(e.content);
}
function j9(e = "") {
  return !!Hl(e);
}
function jw(e = {}) {
  const t = [...e.messages || []].sort((a, l) => a.order - l.order), n = t.filter(Z9), r = j9(e.currentUserMessage || "") ? 1 : 0;
  let s = Math.max(0, Math.floor(Number(e.contextWindowStartOrder) || 0));
  if (!n.length || n.length < 5) s = 0;
  else if (s > 0) {
    let a = n.filter((l) => l.order >= s);
    !n.some((l) => l.order === s) && a.length && (s = a[0].order, a = n.filter((l) => l.order >= s)), a.length < 5 && (s = n[Math.max(0, n.length - 10)]?.order || 0);
  }
  let o = s > 0 ? n.filter((a) => a.order >= s) : n;
  if (o.length + r > 20) {
    const a = Math.max(0, 10 - r);
    s = a > 0 && o.slice(-a)[0]?.order || 0, o = s > 0 ? n.filter((l) => l.order >= s) : n;
  }
  const i = s > 0 ? t.filter((a) => a.order >= s) : t;
  return {
    contextWindowStartOrder: s,
    historyMessages: i,
    usableHistoryCount: n.length,
    windowHistoryCount: o.length,
    currentUserCount: r
  };
}
function e7(e = []) {
  return e.filter((t) => t.role === "user").slice(-2).some((t) => d2(t.runtimeEvents));
}
function t7(e) {
  return Number.isFinite(e) && e < 0.1;
}
function n7(e) {
  if (!e.runtime.includeRandomEncounters) return null;
  const t = ab(e.reusedUserMessage?.runtimeEvents);
  return t || (e.reusedUserMessage && e.rerollRuntimeEvents !== !0 || e7(e.reusedUserMessage ? e.historyMessages : e.sessionMessages) ? null : t7(e.randomEncounterRoll ? e.randomEncounterRoll() : Math.random()) ? u2() : null);
}
function SN(e) {
  return e.includeActionChecks ? {
    tools: aN(),
    toolChoice: "auto"
  } : {
    tools: [],
    toolChoice: "none"
  };
}
function r7(e) {
  const t = "error" in e ? e.error : "";
  return String(e.summary || t || "").trim();
}
function s7(e, t) {
  if (t?.aborted) return !0;
  if (!e || typeof e != "object") return !1;
  const n = e, r = String(n.name || ""), s = String(n.code || ""), o = String(n.message || "");
  return r === "AbortError" || s === "ABORT_ERR" || /abort|aborted|cancelled|canceled/i.test(o);
}
function o7(e, t) {
  if (t instanceof Error && /^\[xb-tavern:[^\]]+\]/.test(t.message)) return t;
  const n = t instanceof Error ? t.message : String(t || "unknown_error"), r = /* @__PURE__ */ new Error(`[xb-tavern:${e}] ${n}`);
  return r.name = t instanceof Error ? t.name : "Error", t instanceof Error && t.stack && (r.stack = `${r.name}: ${r.message}
Caused by: ${t.stack}`), r;
}
async function xn(e, t) {
  try {
    return await t();
  } catch (n) {
    throw o7(e, n);
  }
}
async function $u(e) {
  if (e)
    try {
      await e();
    } catch (t) {
      console.warn("[小白酒馆] run callback failed", t);
    }
}
async function Ev(e, t, n = {}) {
  return n.replace ? await Ik(e, t) : await Ak(e, t);
}
function O_(e) {
  const t = String(e?.character?.name || "").trim();
  return !!t && !/^(sillytavern\s+system|system)\b/i.test(t);
}
function TN(e, t = {}) {
  return O_(t) ? t || {} : O_(e?.contextSnapshot) ? e?.contextSnapshot || {} : t || e?.contextSnapshot || {};
}
function F_(e = {}) {
  if (!O_(e))
    throw new Error("当前没有可用角色，请先选择角色或刷新当前会话。");
}
function vd(e = {}) {
  return e.chatPreset || e.preset || {};
}
function EN(e) {
  return Wr(e?.contract);
}
function i7(e, t) {
  const n = e && typeof e == "object" && !Array.isArray(e) ? e : {};
  return nr({
    ...n,
    contract: zp(n.contract) ? Xh(t, n.contract) : Xh(t, void 0)
  });
}
function CN(e, t) {
  if (!e) return e;
  if (!t.includeMemoryFiles && !t.includeStructuredStates) return {};
  const n = {};
  return t.includeMemoryFiles && Array.isArray(e.memoryFiles) && (n.memoryFiles = e.memoryFiles), t.includeStructuredStates && Array.isArray(e.structuredStates) && (n.structuredStates = e.structuredStates), n;
}
function eo(e, t) {
  t && [
    "userInput",
    "worldInfo",
    "aiOutput",
    "reasoning"
  ].forEach((n) => {
    const r = Number(t[n]) || 0;
    r > 0 && (e[n] = (e[n] || 0) + r);
  });
}
function a7(e = []) {
  return e.map((t) => ({
    id: t.id,
    text: t.text,
    changed: !1
  }));
}
async function yd(e, t = []) {
  if (!t.length) return [];
  if (!e) return a7(t);
  const n = await e(t), r = new Map((Array.isArray(n.items) ? n.items : []).map((s) => [s.id, s]));
  return t.map((s) => r.get(s.id) || {
    id: s.id,
    text: s.text,
    changed: !1
  });
}
async function yh(e) {
  const [t] = await yd(e.applyRegex, [{
    id: `${e.placement}:${e.id}`,
    text: e.text,
    placement: e.placement,
    options: e.options
  }]);
  return {
    text: t?.text ?? e.text,
    summary: gd(t ? [t] : [])
  };
}
async function l7(e) {
  const t = Array.isArray(e.thoughts) ? e.thoughts : [], n = t.map((o, i) => ({
    id: `reasoning:${i}`,
    text: String(o.text || ""),
    placement: "reasoning"
  })).filter((o) => o.text);
  if (!n.length) return { thoughts: e.thoughts };
  const r = await yd(e.applyRegex, n), s = new Map(r.map((o) => [o.id, o]));
  return {
    thoughts: t.map((o, i) => ({
      ...o,
      text: s.get(`reasoning:${i}`)?.text ?? o.text
    })),
    summary: gd(r)
  };
}
async function AN(e) {
  const t = Array.isArray(e.messages) ? e.messages : [], n = t.reduce((o, i, a) => {
    const l = t.length - a - 1;
    if (["user", "assistant"].includes(i.role) && String(i.content || "")) {
      const u = i.role === "user" ? "userInput" : "aiOutput";
      o.push({
        id: `${u}:prompt:${a}`,
        text: String(i.content || ""),
        placement: u,
        options: {
          isPrompt: !0,
          depth: l
        }
      });
    }
    return i.role === "assistant" && Array.isArray(i.thoughts) && i.thoughts.forEach((u, d) => {
      const h = String(u?.text || "");
      h && o.push({
        id: `reasoning:prompt:${a}:${d}`,
        text: h,
        placement: "reasoning",
        options: {
          isPrompt: !0,
          depth: l
        }
      });
    }), o;
  }, []);
  if (!n.length) return { messages: t };
  const r = await yd(e.applyRegex, n), s = new Map(r.map((o) => [o.id, o]));
  return {
    messages: t.map((o, i) => {
      const a = o.role === "user" ? "userInput" : o.role === "assistant" ? "aiOutput" : "", l = a ? s.get(`${a}:prompt:${i}`) : null, u = Array.isArray(o.thoughts) ? o.thoughts.map((d, h) => ({
        ...d,
        text: s.get(`reasoning:prompt:${i}:${h}`)?.text ?? d.text
      })) : o.thoughts;
      return {
        ...o,
        ...l ? { content: l.text } : {},
        ...u ? { thoughts: u } : {}
      };
    }),
    summary: gd(r)
  };
}
function u7(e = []) {
  return e.map((t) => ({
    id: t.id,
    text: t.text,
    changed: !1
  }));
}
async function eS(e, t = []) {
  if (!t.length) return [];
  if (!e) return u7(t);
  const n = await e(t), r = new Map((Array.isArray(n.items) ? n.items : []).map((s) => [s.id, s]));
  return t.map((s) => r.get(s.id) || {
    id: s.id,
    text: s.text,
    changed: !1
  });
}
function tS(e = {}) {
  const t = {}, n = String(e.user?.name || "").trim(), r = String(e.character?.name || "").trim();
  return n && (t.name1Override = n), r && (t.name2Override = r), t;
}
async function IN(e) {
  const [t] = await eS(e.applySubstituteParams, [{
    id: e.id,
    text: e.text,
    options: e.options
  }]);
  return t?.text ?? e.text;
}
function c7(e) {
  return Array.isArray(e) ? e.map((t) => String(t || "")) : e == null ? [] : [String(e)];
}
function d7(e) {
  return !!e && typeof e == "object" && !Array.isArray(e);
}
function xN(e) {
  return Array.isArray(e) ? e.filter(d7) : [];
}
async function WA(e) {
  const t = xN(e.entries), n = [], r = [];
  t.forEach((a, l) => {
    const u = String(a.content || "");
    if (u) {
      const d = `${e.scope}:${l}:content`;
      r.push({
        entryIndex: l,
        field: "content",
        id: d
      }), n.push({
        id: d,
        text: u,
        options: e.options
      });
    }
    [
      "key",
      "keysecondary",
      "secondary_keys"
    ].forEach((d) => {
      c7(a[d]).forEach((h, p) => {
        if (!h) return;
        const y = `${e.scope}:${l}:${d}:${p}`;
        r.push({
          entryIndex: l,
          field: d,
          valueIndex: p,
          id: y
        }), n.push({
          id: y,
          text: h,
          options: e.options
        });
      });
    });
  });
  const s = await eS(e.applySubstituteParams, n), o = new Map(s.map((a) => [a.id, a.text])), i = t.map((a) => ({ ...a }));
  return r.forEach((a) => {
    const l = o.get(a.id);
    if (l === void 0) return;
    const u = i[a.entryIndex];
    if (!u) return;
    if (a.field === "content") {
      u.content = l;
      return;
    }
    const d = u[a.field];
    if (Array.isArray(d)) {
      const h = [...d];
      h[a.valueIndex || 0] = l, u[a.field] = h;
    } else u[a.field] = l;
  }), i;
}
async function nS(e) {
  if (!e.applySubstituteParams) return e.context;
  const t = {
    ...e.context,
    worldEntries: Array.isArray(e.context.worldEntries) ? await WA({
      applySubstituteParams: e.applySubstituteParams,
      entries: e.context.worldEntries,
      options: e.options,
      scope: "worldEntries"
    }) : e.context.worldEntries
  };
  return Array.isArray(e.context.worldBooks) && (t.worldBooks = await Promise.all(xN(e.context.worldBooks).map(async (n, r) => ({
    ...n,
    entries: await WA({
      applySubstituteParams: e.applySubstituteParams,
      entries: n.entries,
      options: e.options,
      scope: `worldBooks:${r}`
    })
  })))), t;
}
async function Ep(e) {
  const t = Array.isArray(e.messages) ? e.messages : [], n = [];
  t.forEach((o, i) => {
    const a = String(o.content || "");
    a && n.push({
      id: `message:${i}`,
      text: a,
      options: e.options
    }), o.role === "assistant" && Array.isArray(o.thoughts) && o.thoughts.forEach((l, u) => {
      const d = String(l?.text || "");
      d && n.push({
        id: `thought:${i}:${u}`,
        text: d,
        options: e.options
      });
    });
  });
  const r = await eS(e.applySubstituteParams, n), s = new Map(r.map((o) => [o.id, o.text]));
  return t.map((o, i) => {
    const a = Array.isArray(o.thoughts) ? o.thoughts.map((l, u) => ({
      ...l,
      text: s.get(`thought:${i}:${u}`) ?? l.text
    })) : o.thoughts;
    return {
      ...o,
      content: s.get(`message:${i}`) ?? o.content,
      ...a ? { thoughts: a } : {}
    };
  });
}
function Ec(e) {
  const t = e && typeof e == "object" && !Array.isArray(e) ? e : {}, n = (r) => {
    if (!r || typeof r != "object" || Array.isArray(r)) return {};
    const s = {};
    return Object.entries(r).forEach(([o, i]) => {
      if (!o || !i || typeof i != "object" || Array.isArray(i)) return;
      const a = i, l = {}, u = Number(a.hash), d = Number(a.start), h = Number(a.end);
      Number.isFinite(u) && (l.hash = u), Number.isFinite(d) && (l.start = d), Number.isFinite(h) && (l.end = h), a.protected === !0 && (l.protected = !0), Object.keys(l).length && (s[o] = l);
    }), s;
  };
  return {
    sticky: n(t.sticky),
    cooldown: n(t.cooldown)
  };
}
async function rS(e) {
  const t = Ec(e.timedState);
  if (!e.getNativeWorldInfoRuntime) return {
    context: e.context,
    timedState: t
  };
  const n = await e.getNativeWorldInfoRuntime({
    context: e.context,
    currentUserMessage: e.currentUserMessage,
    trigger: e.trigger,
    timedState: t
  });
  return {
    context: {
      ...e.context,
      nativeWorldInfo: n
    },
    timedState: Ec(n?.timedState)
  };
}
function kN(e, t = !1) {
  return !t || String(e.worldSourceType || "").trim() === "embedded";
}
function VA(e = {}, t, n = !1) {
  const r = e.worldEntryStates || {}, s = t.meta.worldEntryStateUpdates || {};
  return n ? Gb(r, s) : s;
}
function Il(e = {}, t = [], n = {}) {
  const r = Xw(e), s = n.requestInspection || null, o = String(n.chatPreset?.name || "").trim(), i = JSON.stringify(t, null, 2), a = s || {
    provider: String(n.provider || r.provider || ""),
    model: String(n.model || r.model || ""),
    transport: "unavailable",
    request: n.requestTask || { messages: t }
  };
  return {
    presetName: o || r.currentPresetName,
    chatPresetName: o,
    apiPresetName: r.currentPresetName,
    provider: String(n.provider || r.provider || ""),
    providerLabel: r.providerLabel,
    model: String(n.model || r.model || ""),
    toolMode: r.toolMode,
    messageCount: t.length,
    messageChars: t.reduce((l, u) => l + String(u.content || "").length, 0),
    rawMessagesJson: i,
    rawRequestJson: JSON.stringify(a, null, 2),
    requestKind: n.requestKind || "actual",
    capturedAt: Date.now(),
    ...uN(n.regexApplications) ? { regexApplications: n.regexApplications } : {},
    ...s ? { requestInspection: s } : {}
  };
}
async function sS(e) {
  const t = e.providerConfig || hN(e.agentConfig), n = c9(t, {
    tools: Array.isArray(e.tools) ? e.tools : [],
    toolChoice: e.toolChoice || (Array.isArray(e.tools) && e.tools.length ? "auto" : "none")
  }), r = e.adapter || Jw(t, { missingApiKeyMessage: "请先在 API 配置里选择模型/填写 Key。" }), s = n.buildChatTask({
    messages: e.messages,
    signal: e.signal,
    onStreamProgress: e.onStreamProgress
  }), o = vm({
    supportsSessionToolLoop: r.supportsSessionToolLoop === !0,
    messages: e.messages,
    toolResponses: e.toolResponses,
    finalAnswerReminderText: e.finalAnswerReminderText
  });
  pN(s, o);
  const i = I9(o), a = i && typeof r.inspectSendRequest == "function" ? await r.inspectSendRequest(i, s) : typeof r.inspectRequest == "function" ? await r.inspectRequest(s) : null;
  return {
    task: s,
    adapter: r,
    providerConfig: t,
    requestSnapshot: Il(e.agentConfig, o.requestMessages, {
      provider: String(a?.provider || t.provider || ""),
      model: String(a?.model || t.model || ""),
      requestInspection: a,
      requestKind: e.requestKind || "actual",
      chatPreset: e.chatPreset,
      regexApplications: e.regexApplications,
      requestTask: s
    }),
    snapshotMessages: o.requestMessages
  };
}
function _d(e = []) {
  return e.filter((t) => !t.error).map((t) => ({
    role: [
      "system",
      "user",
      "assistant",
      "tool"
    ].includes(t.role) ? t.role : "assistant",
    content: Hl(t.content),
    ...t.name ? { name: t.name } : {},
    ...Array.isArray(t.thoughts) && t.thoughts.length ? { thoughts: t.thoughts } : {}
  })).filter((t) => String(t.content || "").trim());
}
function MN(e = [], t = -1) {
  if (t < 0) return null;
  for (let n = t + 1; n < e.length; n += 1) {
    const r = e[n];
    if (r.role === "user") break;
    if (r.role === "assistant" && !r.error && String(r.content || "").trim()) return r;
  }
  return null;
}
function f7(e) {
  const t = vd(e), n = [...e.messages || []].sort((h, p) => h.order - p.order), r = e.contextSnapshot || {}, s = [];
  let o = 0, i, a, l = "", u = "";
  n.forEach((h, p) => {
    const y = Hl(h.content);
    if (h.role !== "user" || h.error || !y) {
      s.push(h);
      return;
    }
    const w = MN(n, p);
    if (w) {
      const E = qx({
        context: {
          ...r,
          history: _d(s)
        },
        chatPreset: t,
        currentUserMessage: y,
        historyMode: e.historyMode || "raw",
        turn: o,
        entryStates: {},
        diagnostics: e.diagnostics || {}
      });
      o += 1, i = E.buildSnapshot, a = w.requestSnapshot || h.requestSnapshot, l = String(w.provider || ""), u = String(w.model || "");
    }
    s.push(h);
  });
  const d = n[n.length - 1];
  return {
    turn: o,
    worldEntryStates: {},
    nativeWorldInfoTimedState: {
      sticky: {},
      cooldown: {}
    },
    lastBuildSnapshot: i,
    lastRequestSnapshot: a,
    lastProvider: l,
    lastModel: u,
    lastError: d?.error ? String(d.content || "") : ""
  };
}
async function PN(e) {
  if (!e.applySubstituteParams && !e.getNativeWorldInfoRuntime) return f7(e);
  const t = vd(e), n = [...e.messages || []].sort((w, E) => w.order - E.order), r = e.contextSnapshot || {}, s = tS(r), o = [];
  let i = 0, a = {}, l = {
    sticky: {},
    cooldown: {}
  }, u, d, h = "", p = "";
  for (let w = 0; w < n.length; w += 1) {
    const E = n[w], C = Hl(E.content);
    if (E.role !== "user" || E.error || !C) {
      o.push(E);
      continue;
    }
    const M = MN(n, w);
    if (M) {
      const k = String(M.buildSnapshot?.nativeWorldInfo?.trigger || E.buildSnapshot?.nativeWorldInfo?.trigger || "normal"), R = {
        ...r,
        worldSettings: {
          ...r.worldSettings || {},
          trigger: k
        },
        history: _d(o)
      }, D = await rS({
        getNativeWorldInfoRuntime: e.getNativeWorldInfoRuntime,
        context: R,
        currentUserMessage: C,
        trigger: k,
        timedState: l
      });
      l = D.timedState;
      const m = await yb({
        context: await nS({
          applySubstituteParams: e.applySubstituteParams,
          context: D.context,
          options: s
        }),
        chatPreset: t,
        currentUserMessage: C,
        historyMode: e.historyMode || "raw",
        turn: i,
        entryStates: a,
        diagnostics: e.diagnostics || {}
      });
      a = Gb(a, m.buildResult.meta.worldEntryStateUpdates), i += 1, u = m.buildSnapshot, d = M.requestSnapshot || E.requestSnapshot, h = String(M.provider || ""), p = String(M.model || "");
    }
    o.push(E);
  }
  const y = n[n.length - 1];
  return {
    turn: i,
    worldEntryStates: a,
    nativeWorldInfoTimedState: l,
    lastBuildSnapshot: u,
    lastRequestSnapshot: d,
    lastProvider: h,
    lastModel: p,
    lastError: y?.error ? String(y.content || "") : ""
  };
}
async function h7(e, t) {
  const n = await po(e.sessionId || "");
  if (n) return n;
  const r = vd(e), s = e.contextSnapshot || {}, o = s.character || {}, i = nr(e.runtimeState || {});
  return await Ck({
    title: String(o.name || "未选择角色"),
    characterId: String(o.id || ""),
    characterName: String(o.name || "未选择角色"),
    contextSnapshot: s,
    buildSnapshot: t,
    chatPresetId: String(r.id || ""),
    chatPresetName: String(r.name || ""),
    presetId: String(r.id || ""),
    presetName: String(r.name || ""),
    state: {
      turn: 0,
      contract: i.contract,
      worldEntryStates: {},
      nativeWorldInfoTimedState: {
        sticky: {},
        cooldown: {}
      }
    }
  });
}
async function p7(e, t, n) {
  const r = await xn("provider_request_inspection", () => sS({
    agentConfig: n.agentConfig,
    messages: n.messages,
    chatPreset: n.chatPreset,
    regexApplications: n.regexApplications,
    tools: n.tools,
    toolChoice: n.toolChoice,
    toolResponses: n.toolResponses,
    finalAnswerReminderText: n.finalAnswerReminderText,
    signal: n.signal,
    onStreamProgress: n.onStreamProgress,
    requestKind: "actual",
    providerConfig: t,
    adapter: e
  }));
  let s;
  try {
    s = await xn("provider_chat", () => r.adapter.chat(r.task));
  } catch (d) {
    const h = d?.requestInspection;
    throw h && d && typeof d == "object" && (d.requestSnapshot = Il(n.agentConfig, r.snapshotMessages, {
      provider: String(h.provider || r.providerConfig.provider || ""),
      model: String(h.model || r.providerConfig.model || ""),
      requestInspection: h,
      requestKind: "actual",
      chatPreset: n.chatPreset,
      regexApplications: n.regexApplications,
      requestTask: r.task
    })), d;
  }
  const o = s?.requestInspection || r.requestSnapshot.requestInspection || null, i = String(s?.text || ""), a = String(s?.provider || o?.provider || r.providerConfig.provider || ""), l = String(s?.model || o?.model || r.providerConfig.model || ""), u = rN(s || {}, r.providerConfig, { fallbackPrefix: "tavern-rp-tool" });
  return {
    text: i,
    thoughts: s?.thoughts,
    model: l,
    provider: a,
    finishReason: s?.finishReason,
    providerPayload: s?.providerPayload,
    toolCalls: u,
    requestSnapshot: Il(n.agentConfig, r.snapshotMessages, {
      provider: a,
      model: l,
      requestInspection: o,
      requestKind: "actual",
      chatPreset: n.chatPreset,
      regexApplications: n.regexApplications,
      requestTask: r.task
    })
  };
}
async function m7(e) {
  const t = vd(e), n = e.sessionId ? await po(e.sessionId) : null, r = n ? await Hr(n.id) : [], s = TN(n, e.contextSnapshot);
  F_(s);
  const o = nr(n?.state || e.runtimeState || {}), i = Jp(EN(o)), a = SN(i), l = await xn("simulate_user_input_regex", () => yh({
    applyRegex: e.applyRegex,
    placement: "userInput",
    id: "simulate",
    text: e.currentUserMessage
  })), u = {};
  eo(u, l.summary);
  const d = tS(s), h = await xn("simulate_user_input_substitute", () => IN({
    applySubstituteParams: e.applySubstituteParams,
    id: "userInput:simulate",
    text: l.text,
    options: d
  })), p = n ? jw({
    messages: r,
    contextWindowStartOrder: o.contextWindowStartOrder,
    currentUserMessage: h
  }) : null, y = {
    ...s,
    worldSettings: {
      ...s.worldSettings || {},
      trigger: String(e.generationTrigger || "normal")
    },
    history: n ? _d(p?.historyMessages || []) : e.contextSnapshot.history || []
  }, w = await xn("simulate_native_worldbook_runtime", () => rS({
    getNativeWorldInfoRuntime: e.getNativeWorldInfoRuntime,
    context: y,
    currentUserMessage: h,
    trigger: String(e.generationTrigger || "normal"),
    timedState: o.nativeWorldInfoTimedState
  })), E = await xn("simulate_world_entry_substitute", () => nS({
    applySubstituteParams: e.applySubstituteParams,
    context: w.context,
    options: d
  })), C = await xn("simulate_memory_query", () => uM(E, h)), M = CN(n && (i.includeMemoryFiles || i.includeStructuredStates) ? await xn("simulate_memory_retrieval", () => cM({
    sessionId: n.id,
    queryText: C,
    ignoredTerms: rw(E),
    includeMemoryFiles: i.includeMemoryFiles,
    includeStructuredStates: i.includeStructuredStates
  })) : void 0, i), k = await xn("simulate_brain_build", () => yb({
    context: E,
    chatPreset: t,
    currentUserMessage: h,
    historyMode: e.historyMode || "raw",
    turn: o.turn,
    entryStates: o.worldEntryStates,
    memoryContext: M,
    diagnostics: e.diagnostics || {},
    regexApplications: u,
    transformConversationMessages: async (b) => {
      const v = lN(b, i.includeActionChecks), T = await Ep({
        applySubstituteParams: e.applySubstituteParams,
        messages: v,
        options: d
      }), I = await AN({
        applyRegex: e.applyRegex,
        messages: T
      });
      return eo(u, I.summary), I.messages;
    },
    transformFinalMessages: async (b) => Ep({
      applySubstituteParams: e.applySubstituteParams,
      messages: b,
      options: d
    }),
    transformWorldEntries: async (b) => {
      const v = !!E.nativeWorldInfo, T = b.filter((P) => kN(P, v)), I = await yd(e.applyRegex, T.map((P) => ({
        id: `worldInfo:${P.activationKey}`,
        text: P.content,
        placement: "worldInfo",
        options: {
          isMarkdown: !1,
          isPrompt: !0,
          depth: P.position === dt.atDepth ? P.depth : null
        }
      })));
      eo(u, gd(I));
      const x = new Map(I.map((P) => [P.id, P]));
      return b.map((P) => {
        const L = x.get(`worldInfo:${P.activationKey}`)?.text ?? P.content;
        return {
          ...P,
          content: L,
          contentChars: L.length
        };
      });
    }
  })), R = await xn("simulate_request_inspection", () => sS({
    agentConfig: e.agentConfig,
    messages: k.buildResult.messages,
    chatPreset: t,
    tools: a.tools,
    toolChoice: a.toolChoice,
    onStreamProgress: () => {
    },
    requestKind: "simulated",
    regexApplications: u
  })), D = R.requestSnapshot.provider, m = R.requestSnapshot.model;
  return {
    buildResult: k.buildResult,
    buildSnapshot: k.buildSnapshot,
    requestSnapshot: R.requestSnapshot,
    provider: D,
    model: m
  };
}
function g7(e, t = {}) {
  if (e && typeof e == "object" && !Array.isArray(e)) return e;
  try {
    const n = JSON.parse(String(e || "{}"));
    return n && typeof n == "object" && !Array.isArray(n) ? n : t;
  } catch {
    return t;
  }
}
function v7(e) {
  try {
    return JSON.stringify(e || {});
  } catch {
    return "{}";
  }
}
async function y7(e) {
  const t = aN(), n = [...e.messages], r = [], s = _7(e.agentConfig, e.executeRunOnce);
  let o = "", i, a = "", l = "", u = "", d, h = Il(e.agentConfig, e.messages, {
    chatPreset: e.chatPreset,
    regexApplications: e.regexApplications
  }), p, y = "", w = !1, E = !1;
  for (let C = 1; C <= GA + 2; C += 1) {
    const M = vm({
      supportsSessionToolLoop: s,
      messages: n,
      toolResponses: p,
      finalAnswerReminderText: y
    }), k = await e.executeRunOnce({
      agentConfig: e.agentConfig,
      messages: M.requestMessages,
      chatPreset: e.chatPreset,
      regexApplications: e.regexApplications,
      tools: t,
      toolChoice: "auto",
      ...M.mode === "session_tool_response_round" ? { toolResponses: M.toolResponses } : {},
      ...M.mode === "session_final_reminder_round" ? { finalAnswerReminderText: M.finalAnswerReminderText } : {},
      signal: e.signal,
      onStreamProgress: (T) => {
        if (e.onStreamProgress) {
          if (typeof T?.text != "string") {
            e.onStreamProgress(T);
            return;
          }
          e.onStreamProgress({
            ...T,
            text: o + T.text
          });
        }
      }
    });
    p = void 0, y = "", a = String(k.provider || a || ""), l = String(k.model || l || ""), u = String(k.finishReason || u || ""), d = k.providerPayload, h = k.requestSnapshot || h;
    const R = String(k.text || ""), D = Array.isArray(k.toolCalls) ? k.toolCalls : [];
    if (!D.length) {
      if (!R_(R) && w && !E) {
        E = !0;
        const T = "All required action checks are complete. Do not call more tools. Finish the assistant reply now.";
        s ? y = T : n.push({
          role: "system",
          content: T
        });
        continue;
      }
      if (!R_(R) && w) throw new Error("模型在检定后没有给出有效结论。");
      return o += R, i = k.thoughts, {
        ...k,
        text: o,
        thoughts: i,
        provider: a,
        model: l,
        finishReason: u,
        providerPayload: d,
        requestSnapshot: h,
        runtimeEvents: r
      };
    }
    if (C > GA) throw new Error("action_check_tool_round_limit_exceeded");
    w = !0, o += R;
    const m = o.length, b = sN({
      text: R,
      providerPayload: k.providerPayload
    }, D, { fallbackPrefix: "tavern-rp-tool" });
    b.thoughts = k.thoughts, n.push(b);
    const v = [];
    D.forEach((T) => {
      const I = g7(T.arguments, {}), x = T.name === "ActionCheck" ? a9(I, { rollDie: e.actionCheckRoll }) : l9(T.name);
      x.ok && (r.push(c2({
        action: x.action,
        stat: x.stat,
        difficulty: x.difficulty,
        roll: x.roll,
        success: x.success,
        insertAfterChars: m,
        toolCallId: String(T.id || ""),
        summary: r7(x),
        stakes: x.stakes
      })), e.onStreamProgress?.({
        text: o,
        liveActionCheckEvents: r.map((P) => ({ ...P }))
      })), v.push({
        id: String(T.id || ""),
        name: String(T.name || ""),
        response: x
      }), n.push(oN({
        toolCallId: String(T.id || ""),
        toolName: String(T.name || ""),
        content: v7(x)
      }));
    }), s && (p = v);
  }
  throw new Error("action_check_tool_round_limit_exceeded");
}
function _7(e, t) {
  return t?.supportsSessionToolLoop === !0;
}
async function b7(e) {
  const t = vd(e);
  e.sessionId || F_(e.contextSnapshot || {});
  const n = await h7(e);
  let r = await Hr(n.id);
  const s = TN(n, e.contextSnapshot);
  F_(s);
  const o = Number(e.reuseUserMessageOrder), i = Number.isInteger(o) && o >= 0 && r.find((re) => re.order === o && re.role === "user" && !re.error) || null;
  if (i) {
    const re = await L_(n.id, i.order + 1);
    await Ey(n.id, i.order), re.conflicts.length || await Xo(n.id), await kk(n.id, r.filter((pe) => pe.order > i.order).map((pe) => pe.order)), r = await Hr(n.id);
  }
  const a = i ? r.filter((re) => re.order < i.order) : r, l = nr(n.state || e.runtimeState || {}), u = i ? await PN({
    messages: a,
    contextSnapshot: s,
    chatPreset: e.chatPreset,
    historyMode: e.historyMode || "raw",
    diagnostics: e.diagnostics,
    applySubstituteParams: e.applySubstituteParams,
    getNativeWorldInfoRuntime: e.getNativeWorldInfoRuntime
  }) : null, d = i ? i7({
    ...u,
    contextWindowStartOrder: l.contextWindowStartOrder
  }, l.contract) : l, h = EN(d), p = Jp(h), y = SN(p), w = !!i, E = Hl(i?.content || e.currentUserMessage), C = {}, M = i ? {
    text: E,
    summary: void 0
  } : await xn("turn_user_input_regex", () => yh({
    applyRegex: e.applyRegex,
    placement: "userInput",
    id: "turn",
    text: E
  }));
  eo(C, M.summary);
  const k = tS(s), R = i ? M.text : await xn("turn_user_input_substitute", () => IN({
    applySubstituteParams: e.applySubstituteParams,
    id: "userInput:turn",
    text: M.text,
    options: k
  })), D = jw({
    messages: a,
    contextWindowStartOrder: d.contextWindowStartOrder,
    currentUserMessage: R
  }), m = n7({
    runtime: p,
    sessionMessages: r,
    historyMessages: a,
    reusedUserMessage: i,
    rerollRuntimeEvents: e.rerollRuntimeEvents,
    randomEncounterRoll: e.randomEncounterRoll
  }), b = String(e.generationTrigger || (i ? "regenerate" : "normal")), v = {
    ...s,
    worldSettings: {
      ...s.worldSettings || {},
      trigger: b
    },
    history: _d(D.historyMessages)
  }, T = await xn("turn_native_worldbook_runtime", () => rS({
    getNativeWorldInfoRuntime: e.getNativeWorldInfoRuntime,
    context: v,
    currentUserMessage: R,
    trigger: b,
    timedState: d.nativeWorldInfoTimedState
  })), I = await xn("turn_world_entry_substitute", () => nS({
    applySubstituteParams: e.applySubstituteParams,
    context: T.context,
    options: k
  })), x = await xn("turn_memory_query", () => uM(I, R)), P = CN(p.includeMemoryFiles || p.includeStructuredStates ? await xn("turn_memory_retrieval", () => cM({
    sessionId: n.id,
    queryText: x,
    ignoredTerms: rw(I),
    includeMemoryFiles: p.includeMemoryFiles,
    includeStructuredStates: p.includeStructuredStates
  })) : void 0, p), { buildResult: L, buildSnapshot: G } = await xn("turn_brain_build", () => yb({
    context: I,
    chatPreset: t,
    currentUserMessage: R,
    historyMode: e.historyMode || "raw",
    turn: d.turn,
    entryStates: d.worldEntryStates,
    memoryContext: P,
    diagnostics: e.diagnostics || {},
    regexApplications: C,
    transformConversationMessages: async (re) => {
      const pe = m2(lN(re, p.includeActionChecks), m), B = await Ep({
        applySubstituteParams: e.applySubstituteParams,
        messages: pe,
        options: k
      }), N = await AN({
        applyRegex: e.applyRegex,
        messages: B
      });
      return eo(C, N.summary), N.messages;
    },
    transformFinalMessages: async (re) => Ep({
      applySubstituteParams: e.applySubstituteParams,
      messages: re,
      options: k
    }),
    transformWorldEntries: async (re) => {
      const pe = !!I.nativeWorldInfo, B = re.filter((le) => kN(le, pe)), N = await yd(e.applyRegex, B.map((le) => ({
        id: `worldInfo:${le.activationKey}`,
        text: le.content,
        placement: "worldInfo",
        options: {
          isMarkdown: !1,
          isPrompt: !0,
          depth: le.position === dt.atDepth ? le.depth : null
        }
      })));
      eo(C, gd(N));
      const H = new Map(N.map((le) => [le.id, le]));
      return re.map((le) => {
        const Ie = H.get(`worldInfo:${le.activationKey}`)?.text ?? le.content;
        return {
          ...le,
          content: Ie,
          contentChars: Ie.length
        };
      });
    }
  })), z = await xk(n.id, {
    contextSnapshot: s,
    buildSnapshot: G,
    chatPresetId: String(t.id || n.chatPresetId || n.presetId || ""),
    chatPresetName: String(t.name || n.chatPresetName || n.presetName || ""),
    presetId: String(t.id || n.presetId || ""),
    presetName: String(t.name || n.presetName || "")
  }) || n;
  let V = "";
  const Z = (re) => {
    typeof re.text == "string" && (V = re.text), e.onStreamProgress?.(re);
  };
  let O = Il(e.agentConfig, L.messages, {
    chatPreset: t,
    regexApplications: C
  });
  try {
    O = (await sS({
      agentConfig: e.agentConfig,
      messages: L.messages,
      chatPreset: t,
      tools: y.tools,
      toolChoice: y.toolChoice,
      onStreamProgress: Z,
      requestKind: "actual",
      regexApplications: C
    })).requestSnapshot;
  } catch {
    O = Il(e.agentConfig, L.messages, {
      requestKind: "fallback",
      chatPreset: t,
      regexApplications: C,
      requestTask: {
        messages: L.messages,
        tools: y.tools,
        toolChoice: y.toolChoice
      }
    });
  }
  const Y = String(t.id || z.chatPresetId || z.presetId || ""), X = String(t.name || z.chatPresetName || z.presetName || "");
  let j = i;
  j ? !ab(j.runtimeEvents) && m && (j = await wy(z.id, j.order, { runtimeEvents: [m] }) || j) : j = await Uu(z.id, {
    role: "user",
    content: R,
    contextSnapshot: s,
    buildSnapshot: G,
    chatPresetId: Y,
    chatPresetName: X,
    presetId: Y,
    presetName: X,
    requestSnapshot: O,
    runtimeEvents: m ? [m] : []
  }), await $u(() => e.onUserMessageSaved?.(z.id, j));
  try {
    const re = e.executeRunOnce || w7(e.agentConfig), pe = p.includeActionChecks ? await y7({
      agentConfig: e.agentConfig,
      messages: L.messages,
      chatPreset: t,
      regexApplications: C,
      signal: e.signal,
      onStreamProgress: Z,
      executeRunOnce: re,
      actionCheckRoll: e.actionCheckRoll
    }) : await re({
      agentConfig: e.agentConfig,
      messages: L.messages,
      chatPreset: t,
      regexApplications: C,
      signal: e.signal,
      onStreamProgress: Z
    }), B = p.includeActionChecks ? Wo(pe.runtimeEvents) : [], N = Kv(pe.text, B), H = await yh({
      applyRegex: e.applyRegex,
      placement: "aiOutput",
      id: "assistant",
      text: N.text
    }), le = await l7({
      applyRegex: e.applyRegex,
      thoughts: pe.thoughts
    });
    eo(C, H.summary), eo(C, le.summary);
    const Ie = Tx(H.text, B, N.boundaries);
    (Ie.text !== pe.text || le.thoughts !== pe.thoughts) && e.onStreamProgress?.({
      text: Ie.text,
      thoughts: le.thoughts,
      liveActionCheckEvents: Ie.events.map((ue) => ({ ...ue }))
    });
    const De = Ie.events, Be = {
      ...pe.requestSnapshot,
      regexApplications: C
    }, it = await Uu(z.id, {
      role: "assistant",
      content: Ie.text,
      thoughts: le.thoughts,
      providerPayload: pe.providerPayload,
      contextSnapshot: s,
      buildSnapshot: G,
      chatPresetId: Y,
      chatPresetName: X,
      presetId: Y,
      presetName: X,
      requestSnapshot: Be,
      provider: pe.provider || "",
      model: pe.model || "",
      finishReason: pe.finishReason || "",
      runtimeEvents: De
    });
    await $u(() => e.onAssistantMessageSaved?.(z.id, it));
    const Qe = Number(d.turn || 0) + 1;
    await Ev(z.id, {
      turn: Qe,
      contextWindowStartOrder: D.contextWindowStartOrder,
      worldEntryStates: VA(d, L, w),
      nativeWorldInfoTimedState: I.nativeWorldInfo ? T.timedState : Ec(d.nativeWorldInfoTimedState),
      lastBuildSnapshot: G,
      lastRequestSnapshot: Be,
      lastProvider: pe.provider || "",
      lastModel: pe.model || ""
    }, { replace: w });
    let je = "", W = "";
    const ee = String(pe.finishReason || "").trim();
    if (e.runManager === !0 && !it.error && !["aborted", "error"].includes(ee) && p.hasAutomaticManagerWork) {
      const ue = await X9({
        sessionId: z.id,
        agentConfig: e.agentConfig,
        userMessage: j,
        assistantMessage: it,
        turn: Qe,
        assistantPreset: e.assistantPreset,
        sessionContract: h,
        awaitCompletion: e.awaitManager === !0,
        executeManagerOnce: e.executeManagerOnce,
        onManagerRunSaved: async (Se) => {
          await $u(() => e.onManagerRunSaved?.(z.id, Se.id));
        }
      });
      je = ue.managerRunId, W = ue.managerStatus;
    }
    return {
      sessionId: z.id,
      userMessage: j,
      assistantMessage: it,
      buildResult: L,
      buildSnapshot: G,
      requestSnapshot: Be,
      provider: pe.provider || "",
      model: pe.model || "",
      finishReason: pe.finishReason,
      previewMatchesRequest: L.meta.rawMessagesJson === Be.rawMessagesJson,
      nextTurn: Qe,
      managerRunId: je,
      managerStatus: W
    };
  } catch (re) {
    const pe = re?.requestSnapshot;
    pe && (O = {
      ...pe,
      regexApplications: C
    });
    const B = s7(re, e.signal), N = String(V || "").trim();
    if (B && N) {
      const Ie = await yh({
        applyRegex: e.applyRegex,
        placement: "aiOutput",
        id: "assistant-partial",
        text: N
      });
      eo(C, Ie.summary), uN(C) && (O = {
        ...O,
        regexApplications: C
      }), Ie.text !== N && e.onStreamProgress?.({ text: Ie.text });
      const De = await Uu(z.id, {
        role: "assistant",
        content: Ie.text,
        error: !1,
        contextSnapshot: s,
        buildSnapshot: G,
        chatPresetId: Y,
        chatPresetName: X,
        presetId: Y,
        presetName: X,
        requestSnapshot: O,
        provider: O.provider,
        model: O.model,
        finishReason: "aborted"
      });
      await $u(() => e.onAssistantMessageSaved?.(z.id, De));
      const Be = Number(d.turn || 0) + 1;
      return await Ev(z.id, {
        turn: Be,
        contextWindowStartOrder: D.contextWindowStartOrder,
        worldEntryStates: VA(d, L, w),
        nativeWorldInfoTimedState: I.nativeWorldInfo ? T.timedState : Ec(d.nativeWorldInfoTimedState),
        lastBuildSnapshot: G,
        lastRequestSnapshot: O,
        lastProvider: O.provider,
        lastModel: O.model
      }, { replace: w }), {
        sessionId: z.id,
        userMessage: j,
        assistantMessage: De,
        buildResult: L,
        buildSnapshot: G,
        requestSnapshot: O,
        provider: O.provider,
        model: O.model,
        finishReason: "aborted",
        previewMatchesRequest: L.meta.rawMessagesJson === O.rawMessagesJson,
        nextTurn: Be,
        managerRunId: "",
        managerStatus: ""
      };
    }
    const H = re instanceof Error ? re.message : String(re || "run_failed"), le = await Uu(z.id, {
      role: "assistant",
      content: B ? "已停止生成。" : H,
      error: !0,
      contextSnapshot: s,
      buildSnapshot: G,
      chatPresetId: Y,
      chatPresetName: X,
      presetId: Y,
      presetName: X,
      requestSnapshot: O,
      provider: O.provider,
      model: O.model,
      finishReason: B ? "aborted" : "error"
    });
    return await $u(() => e.onAssistantMessageSaved?.(z.id, le)), await Ev(z.id, {
      turn: Number(d.turn || 0),
      contextWindowStartOrder: D.contextWindowStartOrder,
      worldEntryStates: w ? d.worldEntryStates || {} : {},
      nativeWorldInfoTimedState: Ec(d.nativeWorldInfoTimedState),
      lastBuildSnapshot: G,
      lastRequestSnapshot: O,
      lastProvider: O.provider,
      lastModel: O.model,
      lastError: B ? "已停止生成。" : H
    }, { replace: w }), {
      sessionId: z.id,
      userMessage: j,
      errorMessage: le,
      buildResult: L,
      buildSnapshot: G,
      requestSnapshot: O,
      provider: O.provider,
      model: O.model,
      finishReason: B ? "aborted" : "error",
      previewMatchesRequest: L.meta.rawMessagesJson === O.rawMessagesJson,
      nextTurn: Number(d.turn || 0),
      error: B ? "已停止生成。" : H
    };
  }
}
function w7(e) {
  const t = hN(e), n = Jw(t, { missingApiKeyMessage: "请先在 API 配置里选择模型/填写 Key。" }), r = ((s) => p7(n, t, s));
  return r.supportsSessionToolLoop = n.supportsSessionToolLoop === !0, r;
}
function S7(e) {
  return !Array.isArray(e) || !e.length ? [] : e.map((t) => {
    const n = t && typeof t == "object" ? t : {}, r = n.function && typeof n.function == "object" ? n.function : {}, s = typeof n.id == "string" ? n.id : typeof n.tool_call_id == "string" ? n.tool_call_id : void 0, o = typeof n.name == "string" ? n.name : typeof r.name == "string" ? r.name : void 0, i = n.arguments ?? r.arguments, a = typeof i == "string" ? i : i === void 0 ? void 0 : JSON.stringify(i);
    return {
      ...s ? { id: s } : {},
      ...o ? { name: o } : {},
      ...a ? { arguments: a } : {}
    };
  }).filter((t) => !!(t.id || t.name || t.arguments));
}
function _h(e = "", t = 180) {
  const n = String(e || "").trim();
  return n.length > t ? `${n.slice(0, t)}...` : n;
}
function T7(e) {
  return `manager:${e.sessionId}:${e.order}`;
}
function KA(e) {
  if (!e || typeof e != "object") return !1;
  const t = e;
  return t.error === !0 || ["aborted", "error"].includes(String(t.finishReason || "").trim()) ? !1 : Array.isArray(t.toolCalls) && t.toolCalls.length > 0 || Array.isArray(t.tool_calls) && t.tool_calls.length > 0;
}
function E7(e) {
  const t = e && typeof e == "object" ? e : {};
  return (Array.isArray(t.toolCalls) && t.toolCalls.length ? t.toolCalls : Array.isArray(t.tool_calls) ? t.tool_calls : []).map((n, r) => {
    const s = n && typeof n == "object" ? n : {}, o = s.function && typeof s.function == "object" ? s.function : {}, i = String(s.id || s.tool_call_id || `tool-call-${r + 1}`), a = String(s.name || o.name || "").trim() || "工具", l = s.arguments ?? o.arguments ?? {};
    return {
      id: i,
      name: a,
      argumentsText: _h(typeof l == "string" ? l : JSON.stringify(l || {}), 320)
    };
  }).filter((n) => n.name);
}
function RN(e) {
  if (!e || typeof e != "string") return {};
  try {
    const t = JSON.parse(e);
    return t && typeof t == "object" && !Array.isArray(t) ? t : {};
  } catch {
    return {};
  }
}
function C7(e) {
  if (!e) return "等待工具返回。";
  const t = String(e.toolDisplay || "").trim();
  if (t) return _h(t, 360);
  const n = RN(e.content), r = String(n.summary || n.message || n.error || "").trim();
  if (r) return _h(r, 360);
  const s = String(n.path || n.filePath || n.docId || "").trim(), o = n.changed === !0 ? "已变更" : n.changed === !1 ? "无变化" : "", i = [
    n.ok === !1 ? "失败" : n.ok === !0 ? "成功" : "",
    o,
    s
  ].filter(Boolean).join(" · ");
  return i || _h(String(e.content || "").trim(), 360) || "工具已返回。";
}
function A7(e, t) {
  return {
    assistantMessage: e,
    toolMessages: t,
    calls: E7(e).map((n) => {
      const r = t.find((o) => String(o.toolCallId || o.tool_call_id || "") === n.id), s = RN(r?.content);
      return {
        id: n.id,
        name: r?.toolName || n.name,
        argumentsText: n.argumentsText,
        resultText: C7(r),
        ok: s.ok !== !1 && !r?.error,
        toolMessage: r
      };
    })
  };
}
function I7(e) {
  const t = e[0], n = e[e.length - 1] || t;
  return {
    kind: "tool-turn",
    key: `tool-turn:${t.assistantMessage.sessionId}:${t.assistantMessage.order}:${n.assistantMessage.order}`,
    anchorKey: `tool:${t.assistantMessage.sessionId}:${t.assistantMessage.order}`,
    rounds: e,
    assistantMessage: t.assistantMessage,
    toolMessages: e.flatMap((r) => r.toolMessages),
    calls: e.flatMap((r) => r.calls)
  };
}
function zA(e) {
  const t = [...e].sort((r, s) => r.order - s.order), n = [];
  for (let r = 0; r < t.length; r += 1) {
    const s = t[r];
    if (!(!s || ![
      "user",
      "assistant",
      "tool"
    ].includes(s.role))) {
      if (s.role === "assistant" && KA(s)) {
        const o = [];
        let i = r;
        for (; i < t.length && t[i]?.role === "assistant" && KA(t[i]); ) {
          const a = t[i], l = [];
          for (i += 1; i < t.length && t[i]?.role === "tool"; )
            l.push(t[i]), i += 1;
          o.push(A7(a, l));
        }
        n.push(I7(o)), r = i - 1;
        continue;
      }
      s.role !== "tool" && n.push({
        kind: "message",
        key: T7(s),
        anchorKey: `msg:${s.sessionId}:${s.order}`,
        message: s
      });
    }
  }
  return n;
}
function x7(e) {
  const t = e.calls.filter((s) => !s.ok).length, n = e.calls.filter((s) => !s.toolMessage).length, r = e.rounds.length > 1 ? `${e.rounds.length} 轮 · ` : "";
  return n ? `${r}工具调用 ${e.calls.length} 次 · ${n} 个等待返回` : t ? `${r}工具调用 ${e.calls.length} 次 · ${t} 次失败` : `${r}工具调用 ${e.calls.length} 次 · 全部成功`;
}
function k7(e) {
  const t = e.calls.map((r) => r.name).filter(Boolean).slice(0, 3).join("、"), n = e.calls.length > 3 ? ` 等 ${e.calls.length} 个` : "";
  return t ? `${t}${n}` : "等待工具返回";
}
function M7() {
  let e = [], t = !1;
  return {
    update(n = {}) {
      const r = Array.isArray(n.toolCalls) && n.toolCalls.length ? S7(n.toolCalls) : null, s = typeof n.text == "string" && n.text.trim().length > 0;
      return r ? (e = r, t = !0) : s && t && n.toolCallDraft !== !0 && (e = [], t = !1), {
        content: typeof n.text == "string" && n.text.trim() ? n.text : e.length ? "正在准备工具调用..." : "正在思考...",
        toolCalls: e
      };
    },
    reset() {
      e = [], t = !1;
    }
  };
}
var P7 = ["title", "aria-label"], R7 = {
  key: 0,
  class: "xb-theme-icon",
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  "stroke-width": "1.75",
  "stroke-linecap": "round",
  "stroke-linejoin": "round",
  "aria-hidden": "true"
}, N7 = {
  key: 1,
  class: "xb-theme-glyph",
  "aria-hidden": "true"
}, $7 = /* @__PURE__ */ jt({
  __name: "TavernCornerActions",
  props: {
    dark: { type: Boolean },
    includeHome: { type: Boolean },
    includeExit: { type: Boolean },
    compact: { type: Boolean }
  },
  emits: [
    "home",
    "toggleTheme",
    "exit"
  ],
  setup(e, { emit: t }) {
    const n = t;
    return (r, s) => (U(), q("div", { class: Fe(["home-corner-actions", { "page-corner-actions": e.includeHome || e.compact }]) }, [
      e.includeHome ? (U(), q("button", {
        key: 0,
        type: "button",
        class: "home-icon-button page-home-button",
        title: "首页",
        "aria-label": "首页",
        onClick: s[0] || (s[0] = (o) => n("home"))
      }, [...s[3] || (s[3] = [_("svg", {
        class: "xb-home-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-width": "1.8",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [
        _("path", { d: "M3 11.5 12 4l9 7.5" }),
        _("path", { d: "M5.5 10.5V20h13v-9.5" }),
        _("path", { d: "M9.5 20v-5.5h5V20" })
      ], -1)])])) : ve("", !0),
      _("button", {
        type: "button",
        class: "home-icon-button home-theme-button",
        title: e.dark ? "切换到白天" : "切换到夜间",
        "aria-label": e.dark ? "切换到白天" : "切换到夜间",
        onClick: s[1] || (s[1] = (o) => n("toggleTheme"))
      }, [e.dark ? (U(), q("svg", R7, [...s[4] || (s[4] = [kl('<circle cx="12" cy="12" r="4"></circle><path d="M12 2v2"></path><path d="M12 20v2"></path><path d="m4.93 4.93 1.41 1.41"></path><path d="m17.66 17.66 1.41 1.41"></path><path d="M2 12h2"></path><path d="M20 12h2"></path><path d="m6.34 17.66-1.41 1.41"></path><path d="m19.07 4.93-1.41 1.41"></path>', 9)])])) : (U(), q("span", N7, "☾"))], 8, P7),
      e.includeExit ? (U(), q("button", {
        key: 1,
        type: "button",
        class: "home-icon-button home-exit-button",
        title: "退出",
        "aria-label": "退出",
        onClick: s[2] || (s[2] = (o) => n("exit"))
      }, [...s[5] || (s[5] = [_("svg", {
        class: "xb-exit-icon",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-width": "1.9",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [
        _("path", { d: "M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4" }),
        _("path", { d: "M16 17l5-5-5-5" }),
        _("path", { d: "M21 12H9" })
      ], -1)])])) : ve("", !0)
    ], 2));
  }
}), bd = $7, D7 = { class: "xb-page tavern-about-page" }, L7 = /* @__PURE__ */ jt({
  __name: "TavernAboutPage",
  props: { dark: { type: Boolean } },
  emits: ["toggle-theme", "back"],
  setup(e) {
    return (t, n) => (U(), q("section", D7, [
      Ft(bd, {
        compact: "",
        dark: e.dark,
        onToggleTheme: n[0] || (n[0] = (r) => t.$emit("toggle-theme"))
      }, null, 8, ["dark"]),
      _("button", {
        type: "button",
        class: "about-return-inline",
        onClick: n[1] || (n[1] = (r) => t.$emit("back"))
      }, " RETURN TO INDEX "),
      n[2] || (n[2] = kl('<section class="about-title-block"><h1 class="about-title"> 小白酒馆 </h1><div class="about-sub"> A CLEANER WAY TO ROLEPLAY </div></section><section class="manifesto-body"><p class="intro-dropcap"> 当你与角色聊到数十万字时，故事不该变得卡顿，记忆也不该变成负担。小白酒馆彻底重构了渲染底层，剥离了原生框架庞大的 DOM 堆积。但流畅只是基石，我们真正带来的，是双线并行的 RP 新架构。 </p><p>在这里，角色扮演不再是一场对抗 AI 遗忘的马拉松，因为你多了一位后台统筹者。</p><div class="feature-block"><h3>I. 幕后代理与双面板</h3><p>舞台前，你与角色保持绝对沉浸的互动；舞台后，一个专注的 Agent 正在替你处理苦差事。它实时旁观对话，你可以随时翻转面板进入助手聊天，直接找它梳理当前线索、讨论伏笔设定，而不必出戏去干扰剧情里的角色。</p></div><div class="feature-block"><h3>II. 自动演进的可视化世界</h3><p>代理不仅替你陪聊，更在后台自动提炼剧情脉络、归档阶段事件。在这套活的结构化数据之上，系统直接支撑起了可视化的空间地图与任务系统。你去过的地点、触发的事件都会在台面下自动生长。未来还会演化出更多可能。</p></div><div class="feature-block"><h3>III. 依然是你的酒馆</h3><p>这套新基建依然扎根在你熟悉的土壤上。角色卡、世界书、聊天预设、正则，全都无缝直连酒馆本体。你可以在这里直接调取和修改旧设定，并在请求日志中，清晰地审视代理发出的每一条真实 log。</p></div><div class="feature-block quiet"><p>抛下那些沉重拥挤的参数窗口。选一张卡片，在这个干净的台面前，把心思还给故事本身。</p></div></section>', 2))
    ]));
  }
}), O7 = L7;
function ym() {
  const e = /* @__PURE__ */ se(/* @__PURE__ */ new Map());
  function t(i, a = !1) {
    return e.value.get(i) ?? a;
  }
  function n(i, a) {
    const l = new Map(e.value);
    l.set(i, a), e.value = l;
  }
  function r(i, a) {
    n(i, !!a.currentTarget?.open);
  }
  function s(i, a = !1) {
    n(i, !t(i, a));
  }
  function o() {
    e.value = /* @__PURE__ */ new Map();
  }
  return vl(o), {
    isOpen: t,
    reset: o,
    setOpen: n,
    setOpenFromEvent: r,
    toggleOpen: s
  };
}
var F7 = { class: "xb-page character-select-page" }, B7 = { class: "character-desk" }, U7 = { class: "character-select-head" }, q7 = { class: "head-left" }, H7 = {
  key: 0,
  class: "character-select-error"
}, G7 = { class: "character-index-panel" }, W7 = { class: "index-panel-toolbar" }, V7 = { class: "os-search-bar" }, K7 = ["value"], z7 = { class: "index-meta" }, J7 = [
  "data-character-card-id",
  "disabled",
  "onClick",
  "onDblclick"
], Y7 = { class: "character-card-avatar" }, X7 = ["src", "onError"], Q7 = { key: 1 }, Z7 = { class: "character-card-body" }, j7 = { class: "character-card-title" }, eX = { class: "character-card-desc" }, tX = { class: "card-status-tags" }, nX = {
  key: 0,
  class: "tag active"
}, rX = {
  key: 1,
  class: "tag loading"
}, sX = {
  key: 2,
  class: "tag loading"
}, oX = {
  key: 0,
  class: "empty-note"
}, iX = {
  key: 0,
  class: "character-preview-panel dossier-view"
}, aX = { class: "dossier-header" }, lX = { class: "dossier-identity" }, uX = { class: "sys-mono" }, cX = { class: "dossier-title-row" }, dX = { class: "dossier-title-actions" }, fX = ["disabled", "title"], hX = ["disabled"], pX = { class: "dossier-summary" }, mX = { class: "dossier-details" }, gX = { class: "data-group" }, vX = { class: "data-row" }, yX = { key: 0 }, _X = {
  key: 0,
  class: "greeting-current-card"
}, bX = { class: "greeting-current-head" }, wX = { key: 0 }, SX = { class: "data-block greeting-current-text" }, TX = ["open"], EX = {
  key: 0,
  class: "greeting-choice-list"
}, CX = ["disabled", "onClick"], AX = { class: "greeting-choice-name" }, IX = { class: "greeting-choice-text" }, xX = {
  key: 1,
  class: "data-block"
}, kX = {
  key: 2,
  class: "data-block"
}, MX = { class: "character-data-list" }, PX = ["open"], RX = {
  key: 0,
  class: "data-block"
}, NX = ["open"], $X = {
  key: 0,
  class: "data-block"
}, DX = ["open"], LX = {
  key: 0,
  class: "data-block"
}, OX = ["open"], FX = {
  key: 0,
  class: "data-block"
}, BX = ["open"], UX = {
  key: 0,
  class: "data-block"
}, qX = ["open"], HX = {
  key: 0,
  class: "data-block"
}, GX = {
  key: 1,
  class: "character-preview-panel dossier-empty"
}, WX = {
  key: 2,
  class: "character-empty"
}, VX = { class: "empty-content" }, KX = /* @__PURE__ */ jt({
  __name: "TavernCharacterSelectPage",
  props: {
    dark: { type: Boolean },
    pendingError: {},
    characters: {},
    visibleCharacters: {},
    filteredCount: {},
    liveCharacterId: {},
    selectedCharacter: {},
    selectedGreetingIndex: {},
    pendingPreviewCharacterId: {},
    pendingCharacterSessionId: {},
    characterWorldbookState: {},
    characterWorldbookBusy: { type: Boolean },
    searchText: {},
    hiddenCount: {},
    batchSize: {},
    avatarAvailable: { type: Function },
    shortText: { type: Function }
  },
  emits: [
    "toggle-theme",
    "back",
    "refresh",
    "update:searchText",
    "select",
    "enter-selected",
    "open-character-worldbook",
    "enter-character",
    "select-greeting",
    "load-more",
    "keydown",
    "avatar-error"
  ],
  setup(e, { expose: t, emit: n }) {
    const r = e, s = n, o = /* @__PURE__ */ se(null), i = ym(), a = ie(() => {
      const M = r.selectedCharacter;
      return M ? [String(M.firstMessage || "").trim(), ...M.alternateGreetings || []].filter(Boolean) : [];
    }), l = ie(() => {
      if (!a.value.length) return "";
      const M = Math.min(Math.max(0, Number(r.selectedGreetingIndex) || 0), a.value.length - 1);
      return a.value[M] || "";
    }), u = ie(() => a.value.length > 1), d = ie(() => !!r.selectedCharacter && String(r.pendingPreviewCharacterId || "") === String(r.selectedCharacter.id || "")), h = ie(() => {
      const M = r.characterWorldbookState;
      return M ? M.boundWorldbookName && M.boundExists ? `打开 ${M.boundWorldbookName}` : M.hasEmbeddedBook ? `导入 ${M.embeddedBookName}` : "选择一本世界书绑定" : "读取角色世界书";
    }), p = ie(() => r.characterWorldbookState?.boundExists === !0);
    function y() {
      Jt(() => {
        const M = o.value, k = String(r.selectedCharacter?.id || "").trim();
        !M || !k || Array.from(M.querySelectorAll("[data-character-card-id]")).find((R) => R.dataset.characterCardId === k)?.scrollIntoView?.({
          block: "nearest",
          behavior: "smooth"
        });
      });
    }
    function w(M) {
      s("update:searchText", M.target.value);
    }
    function E(M) {
      return `character-data:${r.selectedCharacter?.id || "none"}:${M}`;
    }
    function C(M) {
      return M === 0 ? "主开场白" : `备用 ${M}`;
    }
    return yt(() => r.selectedCharacter?.id, () => {
      i.reset(), y();
    }), t({ scrollSelectedIntoView: y }), (M, k) => (U(), q("section", F7, [Ft(bd, {
      compact: "",
      dark: e.dark,
      onToggleTheme: k[0] || (k[0] = (R) => M.$emit("toggle-theme"))
    }, null, 8, ["dark"]), _("div", B7, [
      _("header", U7, [
        _("div", q7, [_("button", {
          type: "button",
          class: "os-sys-button",
          onClick: k[1] || (k[1] = (R) => M.$emit("back"))
        }, [...k[14] || (k[14] = [_("span", { class: "icon" }, "↵", -1), er(" 首页 ", -1)])])]),
        k[15] || (k[15] = _("div", { class: "character-title-copy" }, [_("p", { class: "eyebrow" }, " SYSTEM // DESK "), _("h2", null, "角色档案库")], -1)),
        k[16] || (k[16] = _("div", {
          class: "head-balance",
          "aria-hidden": "true"
        }, null, -1))
      ]),
      e.pendingError ? (U(), q("div", H7, " [SYS_ERROR] " + Q(e.pendingError), 1)) : ve("", !0),
      e.characters.length ? (U(), q("section", {
        key: 1,
        class: Fe(["character-archive", { "has-character-selection": !!e.selectedCharacter }])
      }, [_("aside", G7, [
        _("div", W7, [_("label", V7, [k[17] || (k[17] = _("span", { class: "icon" }, "⌕", -1)), _("input", {
          value: e.searchText,
          type: "search",
          placeholder: "检索角色特征...",
          onInput: w
        }, null, 40, K7)]), _("div", z7, Q(e.visibleCharacters.length) + " / " + Q(e.filteredCount) + " ENTRIES ", 1)]),
        _("div", {
          ref_key: "listRef",
          ref: o,
          class: "character-card-grid",
          tabindex: "0",
          "aria-label": "角色卡列表",
          onKeydown: k[3] || (k[3] = (R) => M.$emit("keydown", R))
        }, [(U(!0), q(Ge, null, ut(e.visibleCharacters, (R) => (U(), q("button", {
          key: R.id,
          type: "button",
          class: Fe(["character-card-option", {
            current: R.id === e.liveCharacterId,
            selected: R.id === e.selectedCharacter?.id,
            pending: R.id === e.pendingCharacterSessionId
          }]),
          "data-character-card-id": R.id,
          disabled: !!e.pendingCharacterSessionId,
          onClick: (D) => M.$emit("select", R.id),
          onDblclick: (D) => M.$emit("enter-character", R.id)
        }, [
          k[18] || (k[18] = _("div", { class: "card-focus-indicator" }, null, -1)),
          _("span", Y7, [e.avatarAvailable(R.avatar) ? (U(), q("img", {
            key: 0,
            src: R.avatar,
            loading: "lazy",
            decoding: "async",
            alt: "",
            onError: (D) => M.$emit("avatar-error", R.avatar)
          }, null, 40, X7)) : (U(), q("span", Q7, Q(R.name.slice(0, 1)), 1))]),
          _("span", Z7, [
            _("span", j7, [_("strong", null, Q(R.name), 1)]),
            _("span", eX, Q(e.shortText(R.description || R.personality || R.scenario || R.firstMessage || "暂无数据存档。", 72)), 1),
            _("span", tX, [R.id === e.liveCharacterId ? (U(), q("small", nX, "ACTIVE")) : ve("", !0), R.id === e.pendingCharacterSessionId ? (U(), q("small", rX, "LOADING...")) : R.id === e.pendingPreviewCharacterId ? (U(), q("small", sX, "READING...")) : ve("", !0)])
          ])
        ], 42, J7))), 128)), e.hiddenCount ? (U(), q("button", {
          key: 0,
          type: "button",
          class: "archive-load-more character-load-more",
          onClick: k[2] || (k[2] = (R) => M.$emit("load-more"))
        }, " 加载更多残卷 (" + Q(Math.min(e.hiddenCount, e.batchSize)) + ") ", 1)) : ve("", !0)], 544),
        e.visibleCharacters.length ? ve("", !0) : (U(), q("div", oX, " [SYS_WARN] 未检索到匹配档案。 "))
      ]), e.selectedCharacter ? (U(), q("main", iX, [_("div", aX, [_("div", lX, [
        _("p", uX, " ID: " + Q(e.selectedCharacter.id.substring(0, 8) || "UNKNOWN"), 1),
        _("div", cX, [_("h3", null, Q(e.selectedCharacter.name), 1), _("div", dX, [_("button", {
          type: "button",
          class: Fe(["os-system-act-btn character-worldbook-button", {
            "is-loading": e.characterWorldbookBusy,
            "is-bound": p.value
          }]),
          disabled: !!e.pendingCharacterSessionId || e.characterWorldbookBusy,
          title: h.value,
          "aria-label": "角色世界书",
          onClick: k[4] || (k[4] = (R) => M.$emit("open-character-worldbook"))
        }, [...k[19] || (k[19] = [_("svg", {
          class: "character-worldbook-icon",
          "aria-hidden": "true",
          fill: "none",
          stroke: "currentColor",
          viewBox: "0 0 24 24"
        }, [_("path", {
          "stroke-width": "1.6",
          d: "M12 3a9 9 0 1 1 0 18 9 9 0 0 1 0-18ZM3.8 9h16.4M3.8 15h16.4M12 3c2 2.2 3 5.2 3 9s-1 6.8-3 9M12 3c-2 2.2-3 5.2-3 9s1 6.8 3 9"
        })], -1)])], 10, fX), _("button", {
          type: "button",
          class: Fe(["os-system-act-btn enter-chat-button", { "is-loading": e.selectedCharacter.id === e.pendingCharacterSessionId }]),
          disabled: !!e.pendingCharacterSessionId,
          onClick: k[5] || (k[5] = (R) => M.$emit("enter-selected"))
        }, Q(e.selectedCharacter.id === e.pendingCharacterSessionId ? "进入中..." : "进入聊天"), 11, hX)])]),
        _("div", pX, Q(d.value ? "正在读取角色卡..." : e.selectedCharacter.description || e.selectedCharacter.personality || e.selectedCharacter.scenario || "暂无角色描述。"), 1)
      ])]), _("div", mX, [_("dl", gX, [_("div", vX, [_("dt", null, [k[20] || (k[20] = er("开场白 ", -1)), a.value.length > 1 ? (U(), q("span", yX, Q(e.selectedGreetingIndex + 1) + " / " + Q(a.value.length), 1)) : ve("", !0)]), _("dd", null, [a.value.length ? (U(), q("div", _X, [
        _("div", bX, [_("strong", null, Q(C(e.selectedGreetingIndex)), 1), a.value.length > 1 ? (U(), q("span", wX, Q(a.value.length) + " 个可选", 1)) : ve("", !0)]),
        _("div", SX, Q(l.value), 1),
        _("details", {
          open: S(i).isOpen(E("greetings")),
          class: Fe(["data-section greeting-picker", { "is-empty": !u.value }]),
          onToggle: k[6] || (k[6] = (R) => S(i).setOpenFromEvent(E("greetings"), R))
        }, [k[21] || (k[21] = _("summary", { class: "data-section-title" }, " 切换开场白 ", -1)), u.value && S(i).isOpen(E("greetings")) ? (U(), q("div", EX, [(U(!0), q(Ge, null, ut(a.value, (R, D) => (U(), q("button", {
          key: `${e.selectedCharacter.id}-greeting-${D}`,
          type: "button",
          class: Fe(["greeting-choice", { selected: D === e.selectedGreetingIndex }]),
          disabled: !!e.pendingCharacterSessionId,
          onClick: (m) => M.$emit("select-greeting", D)
        }, [_("span", AX, Q(C(D)), 1), _("span", IX, Q(e.shortText(R, 120)), 1)], 10, CX))), 128))])) : ve("", !0)], 42, TX)
      ])) : d.value ? (U(), q("div", xX, " 正在读取角色卡... ")) : (U(), q("div", kX, " 未填写 "))])])]), _("div", MX, [
        _("details", {
          class: "data-section character-data-section",
          open: S(i).isOpen(E("description")),
          onToggle: k[7] || (k[7] = (R) => S(i).setOpenFromEvent(E("description"), R))
        }, [k[22] || (k[22] = _("summary", { class: "data-section-title" }, [er(" 角色描述 "), _("span", null, "Description")], -1)), S(i).isOpen(E("description")) ? (U(), q("div", RX, Q(e.selectedCharacter.description || "未填写"), 1)) : ve("", !0)], 40, PX),
        _("details", {
          class: "data-section character-data-section",
          open: S(i).isOpen(E("personality")),
          onToggle: k[8] || (k[8] = (R) => S(i).setOpenFromEvent(E("personality"), R))
        }, [k[23] || (k[23] = _("summary", { class: "data-section-title" }, [er(" 性格摘要 "), _("span", null, "Personality")], -1)), S(i).isOpen(E("personality")) ? (U(), q("div", $X, Q(e.selectedCharacter.personality || "未填写"), 1)) : ve("", !0)], 40, NX),
        _("details", {
          class: "data-section character-data-section",
          open: S(i).isOpen(E("scenario")),
          onToggle: k[9] || (k[9] = (R) => S(i).setOpenFromEvent(E("scenario"), R))
        }, [k[24] || (k[24] = _("summary", { class: "data-section-title" }, [er(" 情景 "), _("span", null, "Scenario")], -1)), S(i).isOpen(E("scenario")) ? (U(), q("div", LX, Q(e.selectedCharacter.scenario || "未填写"), 1)) : ve("", !0)], 40, DX),
        _("details", {
          class: "data-section character-data-section",
          open: S(i).isOpen(E("depth")),
          onToggle: k[10] || (k[10] = (R) => S(i).setOpenFromEvent(E("depth"), R))
        }, [k[25] || (k[25] = _("summary", { class: "data-section-title" }, [er(" 角色备注 "), _("span", null, "Character's Note")], -1)), S(i).isOpen(E("depth")) ? (U(), q("div", FX, Q(e.selectedCharacter.characterDepthPrompt || "未填写"), 1)) : ve("", !0)], 40, OX),
        _("details", {
          class: "data-section character-data-section",
          open: S(i).isOpen(E("creator")),
          onToggle: k[11] || (k[11] = (R) => S(i).setOpenFromEvent(E("creator"), R))
        }, [k[26] || (k[26] = _("summary", { class: "data-section-title" }, [er(" 制作者备注 "), _("span", null, "Creator's Notes")], -1)), S(i).isOpen(E("creator")) ? (U(), q("div", UX, Q(e.selectedCharacter.creatorNotes || "未填写"), 1)) : ve("", !0)], 40, BX),
        _("details", {
          class: "data-section character-data-section",
          open: S(i).isOpen(E("examples")),
          onToggle: k[12] || (k[12] = (R) => S(i).setOpenFromEvent(E("examples"), R))
        }, [k[27] || (k[27] = _("summary", { class: "data-section-title" }, [er(" 示例对话 "), _("span", null, "Example dialogue")], -1)), S(i).isOpen(E("examples")) ? (U(), q("div", HX, Q(e.selectedCharacter.mesExample || "未填写"), 1)) : ve("", !0)], 40, qX)
      ])])])) : (U(), q("main", GX, [...k[28] || (k[28] = [_("div", { class: "cyber-placeholder" }, [_("div", { class: "ring" }), _("span", null, [
        er("AWAITING"),
        _("br"),
        er("SELECTION")
      ])], -1)])]))], 2)) : (U(), q("section", WX, [_("div", VX, [
        k[29] || (k[29] = _("h2", null, "数据库脱节", -1)),
        k[30] || (k[30] = _("p", null, "本地缓存在酒馆内未找到可用角色，请在主控端加载后重试。", -1)),
        _("button", {
          type: "button",
          class: "os-sys-button primary",
          onClick: k[13] || (k[13] = (R) => M.$emit("refresh"))
        }, " 重新唤醒数据 ")
      ])]))
    ])]));
  }
}), zX = KX, JX = { class: "tavern-home xb-page home-command-center" }, YX = { class: "home-hero home-command-hero" }, XX = { class: "home-brand-panel" }, QX = { class: "home-version-line" }, ZX = { class: "home-enter-desktop" }, jX = { class: "home-enter-mobile-label" }, eQ = { class: "home-enter-mobile-title" }, tQ = { class: "home-command-panel home-menu-panel" }, nQ = { class: "home-menu-mobile-footer" }, rQ = /* @__PURE__ */ jt({
  __name: "TavernHomePage",
  props: {
    dark: { type: Boolean },
    hasSession: { type: Boolean },
    subtitle: {},
    characterCount: {}
  },
  emits: [
    "toggle-theme",
    "exit",
    "enter",
    "open-characters",
    "open-settings",
    "open-about"
  ],
  setup(e) {
    return (t, n) => (U(), q("section", JX, [Ft(bd, {
      "include-exit": "",
      dark: e.dark,
      onToggleTheme: n[0] || (n[0] = (r) => t.$emit("toggle-theme")),
      onExit: n[1] || (n[1] = (r) => t.$emit("exit"))
    }, null, 8, ["dark"]), _("div", YX, [
      _("div", XX, [
        n[13] || (n[13] = _("p", { class: "eyebrow" }, " Private Narrative Archive ", -1)),
        n[14] || (n[14] = _("h2", null, [_("span", null, "Little White Tavern")], -1)),
        n[15] || (n[15] = _("p", null, " A quiet narrative desk where the scene, the archive, and the assistant stay in the same room. ", -1)),
        _("p", QX, Q(e.hasSession ? e.subtitle : `${e.characterCount} cards in archive`), 1)
      ]),
      _("button", {
        type: "button",
        class: "enter-btn home-enter-chat",
        onClick: n[2] || (n[2] = (r) => t.$emit("enter"))
      }, [
        _("span", ZX, Q(e.hasSession ? "ENTER LAST CHAT" : "OPEN CHARACTER ARCHIVE"), 1),
        _("span", jX, Q(e.hasSession ? "RESUME SESSION" : "OPEN ARCHIVE"), 1),
        _("span", eQ, Q(e.hasSession ? "继续聊天" : "选择角色"), 1),
        n[16] || (n[16] = _("span", {
          class: "home-enter-mobile-arrow",
          "aria-hidden": "true"
        }, "→", -1))
      ]),
      _("div", tQ, [
        _("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[3] || (n[3] = (r) => t.$emit("open-characters"))
        }, [...n[17] || (n[17] = [
          _("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [_("path", {
            "stroke-width": "1.6",
            d: "M16 7a4 4 0 1 1-8 0 4 4 0 0 1 8 0ZM5 21a7 7 0 0 1 14 0"
          })], -1),
          _("strong", null, "角色卡", -1),
          _("span", null, "Character Cards", -1)
        ])]),
        _("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[4] || (n[4] = (r) => t.$emit("open-settings", "worldbooks"))
        }, [...n[18] || (n[18] = [
          _("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [_("path", {
            "stroke-width": "1.6",
            d: "M12 3a9 9 0 1 1 0 18 9 9 0 0 1 0-18ZM3.8 9h16.4M3.8 15h16.4M12 3c2 2.2 3 5.2 3 9s-1 6.8-3 9M12 3c-2 2.2-3 5.2-3 9s1 6.8 3 9"
          })], -1),
          _("strong", null, "世界书", -1),
          _("span", null, "World Lore Books", -1)
        ])]),
        _("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[5] || (n[5] = (r) => t.$emit("open-settings", "chatPreset"))
        }, [...n[19] || (n[19] = [
          _("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [_("path", {
            "stroke-width": "1.6",
            d: "M5 5h14v10H9l-4 4V5ZM8 9h8M8 12h5"
          })], -1),
          _("strong", null, "聊天预设", -1),
          _("span", null, "Prompt Presets", -1)
        ])]),
        _("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[6] || (n[6] = (r) => t.$emit("open-settings", "assistantPreset"))
        }, [...n[20] || (n[20] = [
          _("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [_("path", {
            "stroke-width": "1.6",
            d: "M8 6.5A3.5 3.5 0 0 1 15 6a3.5 3.5 0 0 1 1 6.85V19a2 2 0 0 1-2 2h-4a2 2 0 0 1-2-2v-6.15A3.5 3.5 0 0 1 8 6.5ZM8 14h8M10 18h4"
          })], -1),
          _("strong", null, "助手预设", -1),
          _("span", null, "Memory Manager", -1)
        ])]),
        _("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[7] || (n[7] = (r) => t.$emit("open-settings", "regex"))
        }, [...n[21] || (n[21] = [
          _("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [_("path", {
            "stroke-width": "1.6",
            d: "M10 20 14 4M6 8l-4 4 4 4M18 8l4 4-4 4"
          })], -1),
          _("strong", null, "正则", -1),
          _("span", null, "Text Filters", -1)
        ])]),
        _("button", {
          type: "button",
          class: "home-menu-item",
          onClick: n[8] || (n[8] = (r) => t.$emit("open-settings", "api"))
        }, [...n[22] || (n[22] = [
          _("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [_("path", {
            "stroke-width": "1.6",
            d: "M9 7V3M15 7V3M7 11h10M8 7h8v5a4 4 0 0 1-8 0V7ZM12 16v5"
          })], -1),
          _("strong", null, "API 配置", -1),
          _("span", null, "Model Connection", -1)
        ])]),
        _("button", {
          type: "button",
          class: "home-menu-item desktop-only",
          onClick: n[9] || (n[9] = (r) => t.$emit("open-settings", "base"))
        }, [...n[23] || (n[23] = [
          _("svg", {
            class: "home-menu-icon",
            "aria-hidden": "true",
            fill: "none",
            stroke: "currentColor",
            viewBox: "0 0 24 24"
          }, [_("path", {
            "stroke-width": "1.6",
            d: "M12 12a4 4 0 1 0-4-4 4 4 0 0 0 4 4Zm-7 8a7 7 0 0 1 14 0"
          })], -1),
          _("strong", null, "基础设定", -1),
          _("span", null, "Base Setup", -1)
        ])]),
        _("button", {
          type: "button",
          class: "home-menu-item highlight desktop-only",
          onClick: n[10] || (n[10] = (r) => t.$emit("open-about"))
        }, [...n[24] || (n[24] = [_("strong", null, "关于界面", -1), _("span", null, "About", -1)])]),
        _("div", nQ, [_("button", {
          type: "button",
          class: "home-menu-item mobile-footer-item",
          onClick: n[11] || (n[11] = (r) => t.$emit("open-settings", "base"))
        }, [...n[25] || (n[25] = [_("strong", null, "基础设定", -1)])]), _("button", {
          type: "button",
          class: "home-menu-item mobile-footer-item mobile-footer-about",
          onClick: n[12] || (n[12] = (r) => t.$emit("open-about"))
        }, [...n[26] || (n[26] = [_("strong", null, "关于界面", -1)])])])
      ])
    ])]));
  }
}), sQ = rQ;
function JA(e, t) {
  if (!e) return null;
  const n = e.scrollHeight - e.scrollTop - e.clientHeight, r = typeof e.getBoundingClientRect == "function" ? e.getBoundingClientRect() : null, s = r && t ? Array.from(e.querySelectorAll(t.itemSelector)).map((o) => ({
    key: o?.dataset?.[t.datasetKey] || "",
    rect: typeof o?.getBoundingClientRect == "function" ? o.getBoundingClientRect() : null
  })).find((o) => o.key && o.rect && o.rect.bottom >= r.top + 1) : null;
  return {
    scrollTop: Number(e.scrollTop || 0),
    nearBottom: n < 80,
    anchorKey: s?.key || "",
    anchorTopOffset: s?.rect ? s.rect.top - r.top : 0
  };
}
function YA(e, t, n, r = {}) {
  if (e) {
    if (r.forceBottom) {
      e.scrollTop = e.scrollHeight;
      return;
    }
    if (!t) {
      r.defaultToBottom !== !1 && (e.scrollTop = e.scrollHeight);
      return;
    }
    if (r.preserveScrollTop) {
      if (e.scrollTop = Math.min(Math.max(0, t.scrollTop), e.scrollHeight), t.anchorKey && n) {
        const s = typeof e.getBoundingClientRect == "function" ? e.getBoundingClientRect() : null, o = Array.from(e.querySelectorAll(n.itemSelector)).find((a) => a?.dataset?.[n.datasetKey] === t.anchorKey), i = typeof o?.getBoundingClientRect == "function" ? o.getBoundingClientRect() : null;
        if (s && i) {
          const a = i.top - s.top;
          e.scrollTop = Math.min(Math.max(0, e.scrollTop + a - Number(t.anchorTopOffset || 0)), e.scrollHeight);
        }
      }
      return;
    }
    e.scrollTop = t.nearBottom ? e.scrollHeight : Math.min(t.scrollTop, e.scrollHeight);
  }
}
var oQ = {
  class: "tavern-contract-modal",
  tabindex: "-1"
}, iQ = { class: "tavern-contract-head" }, aQ = { class: "tavern-contract-body" }, lQ = { class: "tavern-contract-tier-bar" }, uQ = ["aria-pressed", "onClick"], cQ = { class: "tavern-contract-copy" }, dQ = { class: "tavern-contract-description" }, fQ = { class: "tavern-contract-foot" }, hQ = ["disabled"], pQ = { class: "tavern-contract-note" }, mQ = {
  key: 0,
  class: "tavern-contract-error"
}, gQ = /* @__PURE__ */ jt({
  __name: "TavernContractModal",
  props: {
    draftContract: {},
    dirty: { type: Boolean },
    saving: { type: Boolean },
    canSave: { type: Boolean },
    errorText: {}
  },
  emits: [
    "close",
    "toggle",
    "save"
  ],
  setup(e, { emit: t }) {
    const n = e, r = ie(() => {
      const o = /* @__PURE__ */ new Map();
      return _y.forEach((i) => {
        const a = o.get(i.tier) || [];
        a.push(i), o.set(i.tier, a);
      }), Array.from(o.entries()).map(([i, a]) => ({
        tier: i,
        mandates: a
      }));
    }), s = ie(() => Vq(n.draftContract));
    return (o, i) => (U(), q("div", {
      class: "tavern-contract-overlay",
      role: "dialog",
      "aria-modal": "true",
      "aria-labelledby": "tavern-contract-title",
      onClick: i[2] || (i[2] = Pn((a) => o.$emit("close"), ["self"])),
      onKeydown: i[3] || (i[3] = _l((a) => o.$emit("close"), ["esc"]))
    }, [_("section", oQ, [
      _("header", iQ, [i[4] || (i[4] = _("div", { class: "tavern-contract-heading" }, [_("h2", { id: "tavern-contract-title" }, " THE PACT "), _("p", { class: "eyebrow" }, " PLAYER — AGENT COVENANT ")], -1)), _("button", {
        type: "button",
        class: "tavern-contract-close",
        "aria-label": "关闭契约",
        onClick: i[0] || (i[0] = (a) => o.$emit("close"))
      }, " × ")]),
      _("div", aQ, [i[5] || (i[5] = _("p", { class: "tavern-contract-preamble" }, " Before the story begins, define what your Agent is allowed to do. Each sealed mandate grants a specific authority. Amend freely at any time. ", -1)), (U(!0), q(Ge, null, ut(r.value, (a) => (U(), q("section", {
        key: a.tier,
        class: "tavern-contract-tier"
      }, [_("div", lQ, [_("span", null, Q(S(Gq)[a.tier]), 1)]), (U(!0), q(Ge, null, ut(a.mandates, (l) => (U(), q("article", {
        key: l.key,
        class: Fe(["tavern-contract-mandate", { "is-off": !e.draftContract[l.key] }])
      }, [_("button", {
        type: "button",
        class: Fe(["tavern-contract-seal", { active: e.draftContract[l.key] }]),
        "aria-pressed": e.draftContract[l.key] ? "true" : "false",
        onClick: (u) => o.$emit("toggle", l.key)
      }, [_("span", null, Q(l.icon), 1)], 10, uQ), _("div", cQ, [_("h3", null, Q(l.title), 1), _("p", dQ, Q(l.description), 1)])], 2))), 128))]))), 128))]),
      _("footer", fQ, [
        _("button", {
          type: "button",
          class: "tavern-contract-save",
          disabled: e.saving || !e.canSave,
          onClick: i[1] || (i[1] = (a) => o.$emit("save"))
        }, Q(e.saving ? "SEALING..." : "SEAL THE COVENANT"), 9, hQ),
        _("p", pQ, Q(s.value) + " OF " + Q(S(_y).length) + " MANDATES ACTIVE ", 1),
        e.errorText ? (U(), q("p", mQ, Q(e.errorText), 1)) : ve("", !0)
      ])
    ])], 32));
  }
}), vQ = gQ, NN = /* @__PURE__ */ Symbol("TavernAppUiContext");
function Gl() {
  const e = ac(NN);
  if (!e) throw new Error("tavern_app_ui_context_missing");
  return e;
}
function wd() {
  return Gl().shell;
}
function _m() {
  return Gl().chat;
}
function $N() {
  return Gl().manager;
}
function DN() {
  return Gl().memory;
}
function LN() {
  return Gl().workspace;
}
function ca() {
  return Gl().settings;
}
function ON(e, t = 36) {
  const n = /* @__PURE__ */ se(!1);
  let r = null, s = 0;
  function o() {
    n.value = !1, r = null, s = 0;
  }
  function i(d) {
    d.button && d.button !== 0 || (r = d.clientY, s = 0, n.value = !0, d.currentTarget?.setPointerCapture?.(d.pointerId));
  }
  function a(d) {
    r !== null && (s = d.clientY - r, s > 0 && d.cancelable && d.preventDefault());
  }
  function l(d) {
    if (r === null) return;
    d.currentTarget?.releasePointerCapture?.(d.pointerId);
    const h = s >= t;
    o(), h && e();
  }
  function u(d) {
    d.currentTarget?.releasePointerCapture?.(d.pointerId), o();
  }
  return {
    dragging: n,
    handleSheetHandlePointerCancel: u,
    handleSheetHandlePointerDown: i,
    handleSheetHandlePointerMove: a,
    handleSheetHandlePointerUp: l
  };
}
var yQ = { class: "chat-side xb-sidebar" }, _Q = { class: "chat-profile xb-brand" }, bQ = { class: "avatar-orb" }, wQ = ["src"], SQ = { key: 1 }, TQ = {
  class: "xb-workspace-controller chat-layout-controller",
  "aria-label": "区域大小"
}, EQ = {
  class: "chat-side-switch",
  role: "tablist",
  "aria-label": "左侧目录"
}, CQ = ["aria-selected"], AQ = ["aria-selected"], IQ = {
  key: 0,
  class: "chat-side-block chat-directory-block"
}, xQ = { class: "side-block-head" }, kQ = { class: "session-list chat-directory-list xb-files" }, MQ = ["onClick"], PQ = ["onClick"], RQ = {
  key: 1,
  class: "muted compact"
}, NQ = {
  key: 1,
  class: "chat-side-block chat-directory-block"
}, $Q = { class: "side-block-head" }, DQ = {
  key: 0,
  class: "memory-search"
}, LQ = {
  key: 1,
  class: "memory-directory-list xb-files"
}, OQ = { class: "memory-file-group-title" }, FQ = { class: "memory-file-tree" }, BQ = ["onClick"], UQ = { class: "memory-file-main" }, qQ = ["onClick"], HQ = { class: "memory-file-main" }, GQ = {
  key: 0,
  class: "muted compact"
}, WQ = {
  key: 2,
  class: "muted compact"
}, VQ = /* @__PURE__ */ jt({
  __name: "TavernChatSidebar",
  emits: ["close"],
  setup(e, { emit: t }) {
    const n = t, r = wd(), s = _m(), o = DN(), { CHAT_SIDEBAR_BATCH_SIZE: i, chatLayout: a, chatSidebarSessionLimit: l, chatSidebarSessions: u, chatSidePanel: d, chatSubtitle: h, displayCharacterName: p, filteredChatSidebarSessionCount: y, hiddenChatSidebarSessionCount: w, removeSession: E, selectedSessionId: C, selectSession: M, sessionDisplayTitle: k, sessionFloorLabel: R, sessions: D, visibleCharacterAvatar: m } = s, { activeMemoryFiles: b, expandMemoryFileGroup: v, MEMORY_FILE_BATCH_SIZE: T, MEMORY_TURN_BATCH_SIZE: I, memoryDirectoryGroups: x, memoryFileDisplayName: P, memoryFiles: L, memoryFileSearchText: G, selectedMemoryFilePath: z, selectMemoryFile: V } = o, { rememberBrokenAvatar: Z } = r;
    function O() {
      n("close");
    }
    const { dragging: Y, handleSheetHandlePointerCancel: X, handleSheetHandlePointerDown: j, handleSheetHandlePointerMove: re, handleSheetHandlePointerUp: pe } = ON(O);
    return (B, N) => (U(), q("aside", yQ, [
      _("button", {
        type: "button",
        class: Fe(["chat-mobile-sheet-handle", { "is-dragging": S(Y) }]),
        title: "收起目录",
        "aria-label": "收起目录",
        onClick: O,
        onPointercancel: N[0] || (N[0] = (...H) => S(X) && S(X)(...H)),
        onPointerdown: N[1] || (N[1] = (...H) => S(j) && S(j)(...H)),
        onPointermove: N[2] || (N[2] = (...H) => S(re) && S(re)(...H)),
        onPointerup: N[3] || (N[3] = (...H) => S(pe) && S(pe)(...H))
      }, null, 34),
      _("section", _Q, [
        _("div", bQ, [S(m) ? (U(), q("img", {
          key: 0,
          src: S(m),
          alt: "",
          onError: N[4] || (N[4] = (H) => S(Z)(S(m)))
        }, null, 40, wQ)) : (U(), q("span", SQ, Q(S(p).slice(0, 1)), 1))]),
        _("div", null, [_("h2", null, Q(S(p)), 1), _("p", null, Q(S(h)), 1)]),
        _("div", TQ, [
          _("button", {
            type: "button",
            class: Fe(["xb-layout-button", { "is-active": S(a) === "chat" }]),
            onClick: N[5] || (N[5] = (H) => a.value = "chat")
          }, " 聊天 ", 2),
          _("button", {
            type: "button",
            class: Fe(["xb-layout-button", { "is-active": S(a) === "balanced" }]),
            onClick: N[6] || (N[6] = (H) => a.value = "balanced")
          }, " 平衡 ", 2),
          _("button", {
            type: "button",
            class: Fe(["xb-layout-button", { "is-active": S(a) === "editor" }]),
            onClick: N[7] || (N[7] = (H) => a.value = "editor")
          }, " 记忆 ", 2)
        ])
      ]),
      _("div", EQ, [_("button", {
        type: "button",
        role: "tab",
        "aria-selected": S(d) === "memory",
        class: Fe({ active: S(d) === "memory" }),
        onClick: N[8] || (N[8] = (H) => d.value = "memory")
      }, [N[12] || (N[12] = _("strong", null, "记忆", -1)), _("span", null, Q(S(b).length), 1)], 10, CQ), _("button", {
        type: "button",
        role: "tab",
        "aria-selected": S(d) === "sessions",
        class: Fe({ active: S(d) === "sessions" }),
        onClick: N[9] || (N[9] = (H) => d.value = "sessions")
      }, [N[13] || (N[13] = _("strong", null, "会话", -1)), _("span", null, Q(S(y)), 1)], 10, AQ)]),
      S(d) === "sessions" ? (U(), q("section", IQ, [_("div", xQ, [N[14] || (N[14] = _("strong", null, "会话目录", -1)), _("span", null, Q(S(y)), 1)]), _("div", kQ, [
        (U(!0), q(Ge, null, ut(S(u), (H) => (U(), q("div", {
          key: H.id,
          class: Fe(["session-card compact", { active: H.id === S(C) }])
        }, [_("button", {
          type: "button",
          class: "session-open",
          onClick: (le) => S(M)(H.id)
        }, [_("strong", null, Q(S(k)(H) || "未选择角色"), 1), _("small", null, Q(S(R)(H)), 1)], 8, MQ), _("button", {
          type: "button",
          class: "session-delete",
          title: "删除会话",
          "aria-label": "删除会话",
          onClick: (le) => S(E)(H.id, le)
        }, " 删除 ", 8, PQ)], 2))), 128)),
        S(w) ? (U(), q("button", {
          key: 0,
          type: "button",
          class: "session-more",
          onClick: N[10] || (N[10] = (H) => l.value += S(i))
        }, " 再显示 " + Q(Math.min(S(w), S(i))) + " 个会话 ", 1)) : ve("", !0),
        S(u).length ? ve("", !0) : (U(), q("p", RQ, " 当前角色还没有其他会话。 "))
      ])])) : ve("", !0),
      S(d) === "memory" ? (U(), q("section", NQ, [
        _("div", $Q, [N[15] || (N[15] = _("strong", null, "记忆目录", -1)), _("span", null, Q(S(b).length), 1)]),
        S(L).length ? (U(), q("label", DQ, [wn(_("input", {
          "onUpdate:modelValue": N[11] || (N[11] = (H) => /* @__PURE__ */ Xt(G) ? G.value = H : null),
          type: "search",
          placeholder: "检索档案"
        }, null, 512), [[ho, S(G)]])])) : ve("", !0),
        S(L).length ? (U(), q("div", LQ, [(U(!0), q(Ge, null, ut(S(x), (H) => (U(), q("div", {
          key: H.key,
          class: "memory-file-group"
        }, [_("div", OQ, [_("span", null, Q(H.title), 1), _("em", null, Q(H.totalCount), 1)]), _("div", FQ, [(U(!0), q(Ge, null, ut(H.files, (le) => (U(), q("button", {
          key: le.path,
          type: "button",
          class: Fe(["memory-file", {
            active: S(z) === le.path,
            stale: le.status === "stale"
          }]),
          onClick: (Ie) => S(V)(le.path)
        }, [_("span", UQ, Q(S(P)(le)), 1)], 10, BQ))), 128)), H.hiddenCount ? (U(), q("button", {
          key: 0,
          type: "button",
          class: "memory-file memory-file-more",
          onClick: (le) => S(v)(H.key)
        }, [_("span", HQ, "再显示 " + Q(Math.min(H.hiddenCount, H.key === "turns" ? S(I) : S(T))) + " 个", 1)], 8, qQ)) : ve("", !0)])]))), 128)), S(G) && !S(x).length ? (U(), q("p", GQ, " 没有匹配的记忆档案。 ")) : ve("", !0)])) : (U(), q("p", WQ, " 还没有记忆档案。 "))
      ])) : ve("", !0)
    ]));
  }
}), KQ = VQ, zQ = /* @__PURE__ */ jt({
  __name: "TavernScrollControls",
  props: {
    active: { type: Boolean },
    showTop: { type: Boolean },
    showBottom: { type: Boolean },
    extraClass: {}
  },
  emits: ["top", "bottom"],
  setup(e) {
    return (t, n) => (U(), q("div", { class: Fe(["chat-scroll-helpers", [e.extraClass, { active: e.active }]]) }, [_("button", {
      type: "button",
      class: Fe({ visible: e.showTop }),
      title: "回到顶部",
      "aria-label": "回到顶部",
      onClick: n[0] || (n[0] = (r) => t.$emit("top"))
    }, " ▲ ", 2), _("button", {
      type: "button",
      class: Fe({ visible: e.showBottom }),
      title: "回到底部",
      "aria-label": "回到底部",
      onClick: n[1] || (n[1] = (r) => t.$emit("bottom"))
    }, " ▼ ", 2)], 2));
  }
}), FN = zQ, JQ = { class: "message-edit-panel" }, YQ = ["data-message-editor"], XQ = { class: "message-edit-actions" }, QQ = ["disabled"], ZQ = ["disabled"], jQ = /* @__PURE__ */ jt({
  __name: "TavernMessageEditPanel",
  props: {
    message: {},
    messageKey: {}
  },
  emits: ["cancel", "save"],
  setup(e, { emit: t }) {
    const n = e, r = t, s = /* @__PURE__ */ se(String(n.message.content || "")), o = /* @__PURE__ */ se(null), i = /* @__PURE__ */ se(null);
    let a = 0;
    const l = ie(() => s.value.trim() !== String(n.message.content || "").trim());
    function u(w) {
      return w.closest(".chat-scroll");
    }
    function d() {
      const w = o.value, E = i.value;
      if (!w || !E) return;
      const C = u(w), M = C?.scrollTop ?? 0, k = w.offsetHeight, R = 144, D = 420, m = window.getComputedStyle(w);
      E.textContent = w.value || " ", E.style.width = `${w.clientWidth}px`, E.style.font = m.font, E.style.lineHeight = m.lineHeight, E.style.letterSpacing = m.letterSpacing, E.style.padding = m.padding, E.style.border = m.border, E.style.boxSizing = m.boxSizing;
      const b = Math.min(Math.max(E.scrollHeight, R), D);
      Math.abs(k - b) >= 1 && (w.style.height = `${b}px`, C && (C.scrollTop = M)), C && (a && cancelAnimationFrame(a), a = requestAnimationFrame(() => {
        a = 0, C.scrollTop = M;
      }));
    }
    function h() {
      Jt(() => {
        const w = o.value;
        w && (d(), w.focus(), w.setSelectionRange(w.value.length, w.value.length));
      });
    }
    function p(w = !1) {
      l.value && r("save", {
        content: s.value,
        rerun: w
      });
    }
    function y(w) {
      if (w.key === "Escape") {
        w.preventDefault(), r("cancel");
        return;
      }
      w.key === "Enter" && (w.ctrlKey || w.metaKey) && (w.preventDefault(), p(!1));
    }
    return yt(() => n.messageKey, () => {
      s.value = String(n.message.content || ""), h();
    }, { immediate: !0 }), vl(() => {
      a && (cancelAnimationFrame(a), a = 0);
    }), (w, E) => (U(), q("div", JQ, [
      wn(_("textarea", {
        ref_key: "textareaRef",
        ref: o,
        "onUpdate:modelValue": E[0] || (E[0] = (C) => s.value = C),
        class: "message-edit-box",
        rows: "6",
        "data-message-editor": e.messageKey,
        onInput: d,
        onKeydown: y
      }, null, 40, YQ), [[ho, s.value]]),
      _("div", {
        ref_key: "sizerRef",
        ref: i,
        class: "message-edit-sizer",
        "aria-hidden": "true"
      }, null, 512),
      _("div", XQ, [
        _("button", {
          type: "button",
          disabled: !l.value,
          onClick: E[1] || (E[1] = (C) => p(!1))
        }, Q(e.message.role === "user" ? "保存" : "保存修改"), 9, QQ),
        e.message.role === "user" ? (U(), q("button", {
          key: 0,
          type: "button",
          disabled: !l.value,
          onClick: E[2] || (E[2] = (C) => p(!0))
        }, " 保存并从这里重来 ", 8, ZQ)) : ve("", !0),
        _("button", {
          type: "button",
          onClick: E[3] || (E[3] = (C) => w.$emit("cancel"))
        }, " 取消 ")
      ])
    ]));
  }
}), eZ = jQ;
function tZ(e) {
  const t = String(e.text || ""), n = Array.isArray(e.thoughts) ? e.thoughts : [], r = Array.isArray(e.actionCheckEvents) ? e.actionCheckEvents : [];
  return !!t || n.length > 0 || r.length > 0;
}
function nZ(e) {
  const t = String(e.text || ""), n = Array.isArray(e.actionCheckEvents) ? e.actionCheckEvents : [];
  return !!t || n.length > 0;
}
var rZ = ["aria-hidden"], sZ = {
  key: 0,
  class: "chat-ambient-standee",
  "aria-hidden": "true"
}, oZ = ["src"], iZ = { class: "chat-head" }, aZ = { class: "chat-head-actions" }, lZ = { class: "chat-scroll-shell" }, uZ = ["data-chat-anchor-key"], cZ = ["data-chat-anchor-key"], dZ = { class: "bubble-meta" }, fZ = { class: "bubble-nameplate" }, hZ = { class: "bubble-avatar-stamp" }, pZ = ["src"], mZ = ["src"], gZ = { key: 2 }, vZ = { class: "bubble-role-name" }, yZ = ["open", "onToggle"], _Z = { class: "tavern-thought-label" }, bZ = [
  "data-markdown-signature",
  "data-action-check-groups",
  "innerHTML"
], wZ = ["title", "aria-label"], SZ = [
  "disabled",
  "title",
  "aria-label",
  "onClick"
], TZ = ["onClick"], EZ = ["disabled", "onClick"], CZ = [
  "disabled",
  "title",
  "aria-label",
  "onClick"
], AZ = ["disabled", "onClick"], IZ = { class: "scene-tag" }, xZ = {
  key: 1,
  "data-chat-anchor-key": "streaming:content",
  class: "chat-bubble from-assistant streaming"
}, kZ = { class: "bubble-meta" }, MZ = { class: "bubble-nameplate" }, PZ = { class: "bubble-avatar-stamp" }, RZ = ["src"], NZ = { key: 1 }, $Z = { class: "bubble-role-name" }, DZ = ["open"], LZ = { class: "tavern-thought-label" }, OZ = [
  "data-action-check-groups",
  "data-markdown-signature",
  "innerHTML"
], FZ = {
  key: 2,
  "data-chat-anchor-key": "streaming:empty",
  class: "chat-bubble from-assistant streaming thinking"
}, BZ = { class: "bubble-meta" }, UZ = { class: "bubble-nameplate" }, qZ = { class: "bubble-avatar-stamp" }, HZ = ["src"], GZ = { key: 1 }, WZ = { class: "bubble-role-name" }, VZ = {
  key: 3,
  class: "chat-empty"
}, KZ = {
  key: 0,
  class: "compose-error"
}, zZ = ["disabled"], JZ = ["disabled", "aria-label"], YZ = {
  class: "compose-send-icon",
  "aria-hidden": "true"
}, XZ = { class: "compose-send-label" }, Du = "chat:runtime-thoughts", QZ = /* @__PURE__ */ jt({
  __name: "TavernConversationPanel",
  emits: ["open-contract"],
  setup(e, { emit: t }) {
    const n = t, r = wd(), s = _m(), { activeView: o, homeThemeDark: i, openPromptInspector: a, rememberBrokenAvatar: l } = r, { actionFeedback: u, cancelEditMessage: d, canDrawMessage: h, canEditMessage: p, canRerunMessage: y, canSendMessage: w, chatComposeTextareaRef: E, chatFocus: C, chatMessages: M, chatMessageWindow: k, chatScrollControlsActive: R, chatScrollRef: D, copyMessage: m, currentUserMessage: b, deleteMessageTurn: v, displayMessageContent: T, displayMessageRenderProjection: I, displayMessageThoughtBlocks: x, displayRuntimeRenderProjection: P, displayRuntimeThoughtBlocks: L, drawMessage: G, drawMessageStatusClass: z, drawMessageStatusText: V, drawMessageTitle: Z, formatMessageTime: O, handleChatScroll: Y, handleChatSubmit: X, handleChatTouchMove: j, handleChatTouchStart: re, handleChatWheel: pe, handleComposeInput: B, handleComposeKeydown: N, isDrawingMessage: H, isEditingMessage: le, isCancellingRun: Ie, isRunning: De, latestErrorMessage: Be, markdownSignature: it, messageKey: Qe, renderChatMarkdown: je, rerunFromMessage: W, revealOlderChatMessages: ee, roleLabel: ue, runtimeActionCheckEvents: Se, runtimeText: be, runtimeThoughts: _e, saveEditMessage: Pe, scrollChatToBottom: xe, scrollChatToTop: ke, selectedSessionId: we, showChatScrollBottom: Ve, showChatScrollTop: Re, startEditMessage: $e, thoughtBlocks: Le, thoughtSummaryLabel: tt, visibleCharacterAvatar: nt, visibleChatMessages: at, visibleUserAvatar: mt } = s;
    function ft(Ce) {
      D.value = Ce instanceof HTMLElement ? Ce : null;
    }
    function Pt(Ce) {
      E.value = Ce instanceof HTMLTextAreaElement ? Ce : null;
    }
    function At() {
      n("open-contract");
    }
    function Ut() {
      return {
        roleplay: !0,
        userName: ue("user"),
        characterName: ue("assistant")
      };
    }
    function Mt(Ce = "", ye = "") {
      const ge = Ut();
      return it([
        Ce,
        ye,
        ge.userName,
        ge.characterName
      ].join("\0"));
    }
    function qt(Ce = "") {
      return je(Ce, Ut());
    }
    function Nt(Ce) {
      return `#${Math.max(1, Number(Ce.order) + 1)}`;
    }
    function Yn(Ce, ye = []) {
      const ge = h2(Ce, ye), Ze = ge.groups.length ? JSON.stringify(ge.groups) : "";
      return {
        text: ge.text,
        signature: Mt(ge.text, Ze),
        actionCheckGroups: Ze
      };
    }
    function Or(Ce) {
      if (Ce.role !== "assistant") {
        const ge = T(Ce);
        return {
          text: ge,
          signature: Mt(ge),
          actionCheckGroups: ""
        };
      }
      const ye = I(Ce);
      return Yn(ye.text, ye.actionCheckEvents);
    }
    const Qt = ie(() => {
      const Ce = P(be.value, Array.isArray(Se.value) ? Se.value : []);
      return Yn(Ce.text, Ce.actionCheckEvents);
    }), te = ie(() => tZ({
      text: be.value,
      thoughts: _e.value,
      actionCheckEvents: Se.value
    })), fe = ie(() => nZ({
      text: be.value,
      actionCheckEvents: Se.value
    })), ae = ym();
    function Je(Ce) {
      return `chat:thought:${Qe(Ce)}`;
    }
    return yt([
      o,
      C,
      we
    ], ([Ce, ye]) => {
      (Ce !== "chat" || ye !== "chat") && ae.reset();
    }), yt(() => `${we.value}:${k.value.startIndex}:${k.value.visibleCount}`, () => ae.reset()), (Ce, ye) => (U(), q("section", {
      class: "chat-face chat-face-front chat-main",
      "aria-hidden": S(C) === "manager"
    }, [
      S(nt) ? (U(), q("div", sZ, [_("img", {
        src: S(nt),
        alt: "",
        onError: ye[0] || (ye[0] = (ge) => S(l)(S(nt)))
      }, null, 40, oZ)])) : ve("", !0),
      _("header", iZ, [_("div", aZ, [
        _("button", {
          type: "button",
          class: "contract-trigger",
          title: "契约",
          "aria-label": "契约",
          onClick: At
        }, " 契约 "),
        _("button", {
          type: "button",
          class: "prompt-inspector-trigger",
          title: "请求日志",
          "aria-label": "请求日志",
          onClick: ye[1] || (ye[1] = (ge) => S(a)("history"))
        }, " 日志 "),
        _("button", {
          type: "button",
          class: "chat-flip-trigger",
          title: "助手聊天",
          "aria-label": "助手聊天",
          onClick: ye[2] || (ye[2] = (ge) => C.value = "manager")
        }, " 助手 ")
      ])]),
      _("div", lZ, [_("div", {
        ref: ft,
        class: "chat-scroll",
        onScroll: ye[11] || (ye[11] = (...ge) => S(Y) && S(Y)(...ge)),
        onWheelPassive: ye[12] || (ye[12] = (...ge) => S(pe) && S(pe)(...ge)),
        onTouchstartPassive: ye[13] || (ye[13] = (...ge) => S(re) && S(re)(...ge)),
        onTouchmovePassive: ye[14] || (ye[14] = (...ge) => S(j) && S(j)(...ge))
      }, [
        S(k).hiddenBefore ? (U(), q("div", {
          key: 0,
          class: "chat-history-gate",
          "data-chat-anchor-key": `gate:${S(k).hiddenBefore}`,
          role: "button",
          tabindex: "0",
          onClick: ye[3] || (ye[3] = (ge) => S(ee)(!0)),
          onKeydown: [ye[4] || (ye[4] = _l(Pn((ge) => S(ee)(!0), ["prevent"]), ["enter"])), ye[5] || (ye[5] = _l(Pn((ge) => S(ee)(!0), ["prevent"]), ["space"]))]
        }, " 展开较早记录 " + Q(S(k).hiddenBefore) + " 条 ", 41, uZ)) : ve("", !0),
        (U(!0), q(Ge, null, ut(S(at), (ge) => (U(), q(Ge, { key: `${ge.sessionId}-${ge.order}` }, [_("div", {
          "data-chat-anchor-key": `${ge.sessionId}:${ge.order}`,
          class: Fe(["chat-bubble", [ge.role === "user" ? "from-user" : "from-assistant", { "is-error": ge.error }]])
        }, [
          _("div", dZ, [_("span", fZ, [_("span", hZ, [ge.role === "user" && S(mt) ? (U(), q("img", {
            key: 0,
            src: S(mt),
            alt: "",
            onError: ye[6] || (ye[6] = (Ze) => S(l)(S(mt)))
          }, null, 40, pZ)) : ge.role !== "user" && S(nt) ? (U(), q("img", {
            key: 1,
            src: S(nt),
            alt: "",
            onError: ye[7] || (ye[7] = (Ze) => S(l)(S(nt)))
          }, null, 40, mZ)) : (U(), q("span", gZ, Q(String(S(ue)(ge.role)).slice(0, 1)), 1))]), _("span", vZ, Q(ge.error ? "错误" : S(ue)(ge.role)), 1)]), _("small", null, Q(S(O)(ge.createdAt)), 1)]),
          S(le)(ge) ? (U(), Zs(eZ, {
            key: 0,
            message: ge,
            "message-key": S(Qe)(ge),
            onCancel: S(d),
            onSave: (Ze) => S(Pe)(ge, Ze)
          }, null, 8, [
            "message",
            "message-key",
            "onCancel",
            "onSave"
          ])) : ve("", !0),
          (U(!0), q(Ge, null, ut([S(Le)(ge)], (Ze) => (U(), q(Ge, { key: `${S(Qe)(ge)}:raw-thoughts:${Ze.length}` }, [!S(le)(ge) && Ze.length ? (U(), q("details", {
            key: 0,
            class: "tavern-thought-details",
            open: S(ae).isOpen(Je(ge)),
            onToggle: (Ne) => S(ae).setOpenFromEvent(Je(ge), Ne)
          }, [_("summary", null, Q(S(tt)(Ze)), 1), S(ae).isOpen(Je(ge)) ? (U(!0), q(Ge, { key: 0 }, ut([S(x)(ge)], (Ne) => (U(), q(Ge, { key: `${S(Qe)(ge)}:display-thoughts:${Ne.length}` }, [(U(!0), q(Ge, null, ut(Ne, (oe, Ye) => (U(), q("div", {
            key: `${ge.sessionId}-${ge.order}-thought-${Ye}`,
            class: "tavern-thought-block"
          }, [_("div", _Z, Q(oe.label), 1), _("pre", null, Q(oe.text), 1)]))), 128))], 64))), 128)) : ve("", !0)], 40, yZ)) : ve("", !0)], 64))), 128)),
          S(le)(ge) ? ve("", !0) : (U(!0), q(Ge, { key: 1 }, ut([Or(ge)], (Ze) => (U(), q("div", {
            key: `${S(Qe)(ge)}:${Ze.signature}`,
            class: "xb-tavern-markdown",
            "data-markdown-signature": Ze.signature,
            "data-action-check-groups": Ze.actionCheckGroups || null,
            innerHTML: qt(Ze.text)
          }, null, 8, bZ))), 128)),
          S(le)(ge) ? ve("", !0) : (U(), q("div", {
            key: 2,
            class: Fe(["message-actions", { "has-status": !!S(V)(ge) }])
          }, [
            _("span", {
              class: "message-floor-label",
              title: `第 ${Nt(ge).slice(1)} 楼`,
              "aria-label": `第 ${Nt(ge).slice(1)} 楼`
            }, Q(Nt(ge)), 9, wZ),
            S(V)(ge) ? (U(), q("span", {
              key: 0,
              class: Fe(["message-draw-status", S(z)(ge)])
            }, Q(S(V)(ge)), 3)) : ve("", !0),
            _("button", {
              type: "button",
              disabled: !S(h)(ge),
              class: Fe([S(u)(ge, "draw"), { "is-running": S(H)(ge) }]),
              title: S(Z)(ge),
              "aria-label": S(Z)(ge),
              onClick: (Ze) => S(G)(ge)
            }, Q(S(H)(ge) ? "■" : "🎨"), 11, SZ),
            _("button", {
              type: "button",
              class: Fe(S(u)(ge, "copy")),
              title: "复制",
              "aria-label": "复制",
              onClick: (Ze) => S(m)(ge)
            }, " ⧉ ", 10, TZ),
            _("button", {
              type: "button",
              disabled: !S(p)(ge),
              class: Fe(S(u)(ge, "edit")),
              title: "编辑",
              "aria-label": "编辑",
              onClick: (Ze) => S($e)(ge)
            }, " ✎ ", 10, EZ),
            _("button", {
              type: "button",
              disabled: !S(y)(ge),
              class: Fe(S(u)(ge, "rerun")),
              title: ge.role === "user" ? "发送最后一楼" : "重新生成最后回复",
              "aria-label": ge.role === "user" ? "发送最后一楼" : "重新生成最后回复",
              onClick: (Ze) => S(W)(ge)
            }, " ↻ ", 10, CZ),
            _("button", {
              type: "button",
              disabled: S(De),
              class: Fe(S(u)(ge, "delete")),
              title: "从这里删除后续剧情",
              "aria-label": "从这里删除后续剧情",
              onClick: (Ze) => S(v)(ge)
            }, " ⌫ ", 10, AZ)
          ], 2))
        ], 10, cZ), (U(!0), q(Ge, null, ut(ge.role === "user" ? ge.runtimeEvents || [] : [], (Ze, Ne) => (U(), q("div", {
          key: `${ge.sessionId}-${ge.order}-runtime-event-${Ze.type}-${Ne}`,
          class: "chat-runtime-event scene-narration",
          "aria-hidden": "true"
        }, [_("div", IZ, Q(String(Ze.label || "")), 1)]))), 128))], 64))), 128)),
        S(De) && te.value ? (U(), q("div", xZ, [
          _("div", kZ, [_("span", MZ, [_("span", PZ, [S(nt) ? (U(), q("img", {
            key: 0,
            src: S(nt),
            alt: "",
            onError: ye[8] || (ye[8] = (ge) => S(l)(S(nt)))
          }, null, 40, RZ)) : (U(), q("span", NZ, Q(String(S(ue)("assistant")).slice(0, 1)), 1))]), _("span", $Z, Q(S(ue)("assistant")), 1)]), ye[20] || (ye[20] = _("small", null, "生成中", -1))]),
          (U(!0), q(Ge, null, ut([S(Le)(S(_e))], (ge) => (U(), q(Ge, { key: `${Du}:${ge.length}` }, [ge.length ? (U(), q("details", {
            key: 0,
            class: "tavern-thought-details",
            open: S(ae).isOpen(Du, !0),
            onToggle: ye[9] || (ye[9] = (Ze) => S(ae).setOpenFromEvent(Du, Ze))
          }, [_("summary", null, Q(S(tt)(ge, !0)), 1), S(ae).isOpen(Du, !0) ? (U(!0), q(Ge, { key: 0 }, ut([S(L)(ge)], (Ze) => (U(), q(Ge, { key: `${Du}:display:${Ze.length}` }, [(U(!0), q(Ge, null, ut(Ze, (Ne, oe) => (U(), q("div", {
            key: `runtime-thought-${oe}`,
            class: "tavern-thought-block"
          }, [_("div", LZ, Q(Ne.label), 1), _("pre", null, Q(Ne.text), 1)]))), 128))], 64))), 128)) : ve("", !0)], 40, DZ)) : ve("", !0)], 64))), 128)),
          fe.value ? (U(), q("div", {
            key: 0,
            class: "xb-tavern-markdown",
            "data-action-check-groups": Qt.value.actionCheckGroups || void 0,
            "data-markdown-signature": Qt.value.signature,
            innerHTML: qt(Qt.value.text)
          }, null, 8, OZ)) : ve("", !0)
        ])) : ve("", !0),
        S(De) && !te.value ? (U(), q("div", FZ, [_("div", BZ, [_("span", UZ, [_("span", qZ, [S(nt) ? (U(), q("img", {
          key: 0,
          src: S(nt),
          alt: "",
          onError: ye[10] || (ye[10] = (ge) => S(l)(S(nt)))
        }, null, 40, HZ)) : (U(), q("span", GZ, Q(String(S(ue)("assistant")).slice(0, 1)), 1))]), _("span", WZ, Q(S(ue)("assistant")), 1)]), ye[21] || (ye[21] = _("small", null, "生成中", -1))]), ye[22] || (ye[22] = _("p", null, "正在组织回复...", -1))])) : ve("", !0),
        !S(M).length && !S(De) ? (U(), q("p", VZ, " 写一句话，开始。 ")) : ve("", !0),
        ye[23] || (ye[23] = _("div", {
          class: "chat-compose-spacer",
          "aria-hidden": "true"
        }, null, -1))
      ], 32), Ft(FN, {
        active: S(R),
        "show-top": S(Re),
        "show-bottom": S(Ve),
        onTop: S(ke),
        onBottom: ye[15] || (ye[15] = (ge) => S(xe)(!0, {
          collapseWindow: !0,
          revealHelpers: !0
        }))
      }, null, 8, [
        "active",
        "show-top",
        "show-bottom",
        "onTop"
      ])]),
      _("form", {
        class: "chat-compose",
        onSubmit: ye[19] || (ye[19] = Pn((...ge) => S(X) && S(X)(...ge), ["prevent"]))
      }, [
        S(Be) ? (U(), q("div", KZ, Q(S(Be)), 1)) : ve("", !0),
        wn(_("textarea", {
          ref: Pt,
          "onUpdate:modelValue": ye[16] || (ye[16] = (ge) => /* @__PURE__ */ Xt(b) ? b.value = ge : null),
          rows: "2",
          placeholder: "对角色说一句话...",
          disabled: S(De),
          onInput: ye[17] || (ye[17] = (...ge) => S(B) && S(B)(...ge)),
          onKeydown: ye[18] || (ye[18] = (...ge) => S(N) && S(N)(...ge))
        }, null, 40, zZ), [[ho, S(b)]]),
        _("button", {
          type: "submit",
          class: "primary-action",
          disabled: !S(w),
          "aria-label": S(Ie) ? "正在停止" : S(De) ? "停止" : "发送"
        }, [_("span", YZ, Q(S(Ie) ? "..." : S(De) ? "■" : "➤"), 1), _("span", XZ, Q(S(Ie) ? "正在停止" : S(De) ? "停止" : "发送"), 1)], 8, JZ)
      ], 32)
    ], 8, rZ));
  }
}), ZZ = QZ, jZ = ["aria-hidden"], ej = { class: "manager-head" }, tj = { class: "manager-head-actions" }, nj = { class: "chat-scroll-shell manager-scroll-shell" }, rj = { key: 0 }, sj = ["data-manager-anchor-key"], oj = ["data-manager-anchor-key"], ij = { class: "manager-run-title" }, aj = {
  key: 0,
  class: "message-edit-panel manager-message-edit-panel"
}, lj = ["data-manager-message-editor", "onKeydown"], uj = { class: "message-edit-actions" }, cj = ["disabled", "onClick"], dj = ["disabled", "onClick"], fj = ["data-markdown-signature", "innerHTML"], hj = {
  key: 2,
  class: "message-actions manager-message-actions"
}, pj = ["onClick"], mj = ["disabled", "onClick"], gj = [
  "disabled",
  "title",
  "aria-label",
  "onClick"
], vj = ["disabled", "onClick"], yj = [
  "data-manager-anchor-key",
  "open",
  "onToggle"
], _j = {
  key: 0,
  class: "manager-tool-turn-body"
}, bj = { class: "manager-tool-round-title" }, wj = ["open", "onToggle"], Sj = ["data-markdown-signature", "innerHTML"], Tj = { class: "manager-tool-head" }, Ej = { key: 0 }, Cj = ["data-manager-anchor-key"], Aj = { class: "manager-run-title" }, Ij = ["data-markdown-signature", "innerHTML"], xj = [
  "data-manager-anchor-key",
  "open",
  "onToggle"
], kj = {
  key: 0,
  class: "manager-tool-turn-body"
}, Mj = { class: "manager-tool-round-title" }, Pj = ["open", "onToggle"], Rj = ["data-markdown-signature", "innerHTML"], Nj = { class: "manager-tool-head" }, $j = { key: 0 }, Dj = {
  key: 2,
  class: "manager-card manager-message manager-message-assistant manager-message-live",
  "data-manager-anchor-key": "live:manager-thinking"
}, Lj = ["open"], Oj = { key: 0 }, Fj = { key: 1 }, Bj = ["data-manager-anchor-key", "aria-busy"], Uj = { class: "manager-run-title" }, qj = { class: "manager-work-source" }, Hj = { class: "manager-run-activity" }, Gj = { class: "manager-work-status-grid" }, Wj = { key: 0 }, Vj = {
  key: 1,
  class: "manager-work-issue-line"
}, Kj = ["open"], zj = {
  key: 0,
  class: "manager-tool-list"
}, Jj = { class: "manager-tool-head" }, Yj = ["open", "onToggle"], Xj = ["data-markdown-signature", "innerHTML"], Qj = { key: 2 }, Zj = { key: 3 }, jj = { key: 4 }, eee = ["disabled"], tee = {
  key: 1,
  class: "manager-card manager-message manager-message-system",
  "data-manager-anchor-key": "meta:memory"
}, nee = { class: "manager-run-title" }, ree = { key: 0 }, see = ["open"], oee = { key: 0 }, iee = {
  key: 4,
  "data-manager-anchor-key": "empty",
  class: "chat-empty"
}, aee = {
  key: 0,
  class: "compose-error"
}, lee = ["disabled"], uee = ["disabled"], cee = /* @__PURE__ */ jt({
  __name: "TavernManagerPanel",
  emits: ["open-contract"],
  setup(e, { emit: t }) {
    const n = t, r = wd(), s = _m(), o = $N(), { activeView: i, shortText: a } = r, { chatFocus: l, cancelEditMessage: u, formatMessageTime: d, handleComposeInput: h, markdownSignature: p, renderChatMarkdown: y, roleLabel: w, thoughtBlocks: E, thoughtSummaryLabel: C } = s, { activeMemoryFiles: M, archivedManagerRuns: k, canEditManagerMessage: R, canRerunManagerMessage: D, canSendManagerMessage: m, copyManagerMessage: b, currentManagerWorkRun: v, deleteManagerMessageTurn: T, editingMessageDraft: I, formatRunActivityLine: x, formatRunIssueLine: P, formatRunInputLine: L, formatRunMapLine: G, formatRunMemoryLine: z, formatRunModelLine: V, handleEditInput: Z, handleManagerComposeKeydown: O, handleManagerEditKeydown: Y, handleManagerScroll: X, handleManagerSubmit: j, handleManagerTouchMove: re, handleManagerTouchStart: pe, handleManagerWheel: B, hiddenManagerRunCount: N, isEditingManagerMessage: H, isEditingManagerMessageDirty: le, isManagerAssistantCancelling: Ie, isManagerAssistantRunning: De, isManagerRunRetrying: Be, liveManagerChatDisplayItems: it, managerActionFeedback: Qe, managerBusy: je, managerCompactionOverlay: W, managerComposeTextareaRef: ee, managerInputDraft: ue, managerInputStatus: Se, managerMessageWindow: be, managerRuns: _e, managerRunTone: Pe, managerScrollControlsActive: xe, managerScrollRef: ke, managerStatusLabel: we, managerToolStatusLabel: Ve, managerToolTone: Re, managerToolTraceItems: $e, managerToolTurnPreview: Le, managerToolTurnSummary: tt, memoryFileDisplayName: nt, memoryFiles: at, memoryIndexStatusLine: mt, retryManagerRun: ft, revealOlderManagerMessages: Pt, rerunFromManagerMessage: At, saveEditManagerMessage: Ut, scrollManagerToBottom: Mt, scrollManagerToTop: qt, selectedMemoryFile: Nt, showManagerScrollBottom: Yn, showManagerScrollTop: Or, startEditManagerMessage: Qt, toolTraceSummary: te, visibleManagerChatItems: fe } = o;
    function ae(Ze) {
      ke.value = Ze instanceof HTMLElement ? Ze : null;
    }
    function Je(Ze) {
      ee.value = Ze instanceof HTMLTextAreaElement ? Ze : null;
    }
    function Ce() {
      n("open-contract");
    }
    const ye = ym();
    function ge(Ze, ...Ne) {
      return `manager:${Ze}:${Ne.map((oe) => String(oe ?? "")).join(":")}`;
    }
    return yt([i, l], ([Ze, Ne]) => {
      (Ze !== "chat" || Ne !== "manager") && ye.reset();
    }), yt(() => `${be.value.startIndex}:${be.value.visibleCount}`, () => ye.reset()), (Ze, Ne) => (U(), q("section", {
      class: "chat-face chat-face-back chat-manager",
      "aria-hidden": S(l) === "chat"
    }, [
      _("header", ej, [_("div", tj, [_("button", {
        type: "button",
        class: "contract-trigger",
        title: "契约",
        "aria-label": "契约",
        onClick: Ce
      }, " 契约 "), _("button", {
        type: "button",
        class: "chat-flip-trigger",
        title: "角色聊天",
        "aria-label": "角色聊天",
        onClick: Ne[0] || (Ne[0] = (oe) => l.value = "chat")
      }, " 角色 ")])]),
      _("div", nj, [_("div", {
        ref: ae,
        class: "manager-chat-scroll",
        onScroll: Ne[11] || (Ne[11] = (...oe) => S(X) && S(X)(...oe)),
        onWheelPassive: Ne[12] || (Ne[12] = (...oe) => S(B) && S(B)(...oe)),
        onTouchstartPassive: Ne[13] || (Ne[13] = (...oe) => S(pe) && S(pe)(...oe)),
        onTouchmovePassive: Ne[14] || (Ne[14] = (...oe) => S(re) && S(re)(...oe))
      }, [
        S(W)?.active ? (U(), q("div", {
          key: 0,
          class: Fe(["manager-compaction-overlay", { resolved: S(W).resolved }]),
          role: "status",
          "aria-live": "polite"
        }, [_("strong", null, Q(S(W).status), 1), _("small", null, [er(Q(S(W).currentTokens) + " / " + Q(S(W).triggerTokens || "...") + " ", 1), S(W).yieldTokens ? (U(), q("span", rj, " → " + Q(S(W).yieldTokens), 1)) : ve("", !0)])], 2)) : ve("", !0),
        S(be).hiddenBefore ? (U(), q("div", {
          key: 1,
          class: "chat-history-gate manager-history-gate",
          "data-manager-anchor-key": `gate:${S(be).hiddenBefore}`,
          role: "button",
          tabindex: "0",
          onClick: Ne[1] || (Ne[1] = (oe) => S(Pt)(!0)),
          onKeydown: [Ne[2] || (Ne[2] = _l(Pn((oe) => S(Pt)(!0), ["prevent"]), ["enter"])), Ne[3] || (Ne[3] = _l(Pn((oe) => S(Pt)(!0), ["prevent"]), ["space"]))]
        }, " 展开较早记录 " + Q(S(be).hiddenBefore) + " 条 ", 41, sj)) : ve("", !0),
        (U(!0), q(Ge, null, ut(S(fe), (oe) => (U(), q(Ge, { key: oe.key }, [oe.kind === "message" ? (U(), q("article", {
          key: 0,
          "data-manager-anchor-key": oe.anchorKey,
          class: Fe(["manager-card manager-message", oe.message.role === "user" ? "manager-message-user" : "manager-message-assistant"])
        }, [
          _("div", ij, [_("strong", null, Q(oe.message.role === "user" ? S(w)("user") : "助手"), 1), _("small", null, Q(S(d)(oe.message.createdAt)), 1)]),
          S(H)(oe.message) ? (U(), q("div", aj, [wn(_("textarea", {
            "onUpdate:modelValue": Ne[4] || (Ne[4] = (Ye) => /* @__PURE__ */ Xt(I) ? I.value = Ye : null),
            class: "message-edit-box",
            rows: "6",
            "data-manager-message-editor": `manager:${oe.message.sessionId}:${oe.message.order}`,
            onInput: Ne[5] || (Ne[5] = (...Ye) => S(Z) && S(Z)(...Ye)),
            onKeydown: (Ye) => S(Y)(Ye, oe.message)
          }, null, 40, lj), [[ho, S(I)]]), _("div", uj, [
            _("button", {
              type: "button",
              disabled: !S(le)(oe.message),
              onClick: (Ye) => S(Ut)(oe.message)
            }, Q(oe.message.role === "user" ? "保存" : "保存修改"), 9, cj),
            oe.message.role === "user" ? (U(), q("button", {
              key: 0,
              type: "button",
              disabled: !S(le)(oe.message),
              onClick: (Ye) => S(Ut)(oe.message, { rerun: !0 })
            }, " 保存并重发 ", 8, dj)) : ve("", !0),
            _("button", {
              type: "button",
              onClick: Ne[6] || (Ne[6] = (...Ye) => S(u) && S(u)(...Ye))
            }, " 取消 ")
          ])])) : ve("", !0),
          S(H)(oe.message) ? ve("", !0) : (U(), q("div", {
            key: 1,
            class: "xb-tavern-markdown",
            "data-markdown-signature": S(p)(oe.message.content),
            innerHTML: S(y)(oe.message.content)
          }, null, 8, fj)),
          S(H)(oe.message) ? ve("", !0) : (U(), q("div", hj, [
            _("button", {
              type: "button",
              class: Fe(S(Qe)(oe.message, "copy")),
              title: "复制",
              "aria-label": "复制",
              onClick: (Ye) => S(b)(oe.message)
            }, " ⧉ ", 10, pj),
            _("button", {
              type: "button",
              disabled: !S(R)(oe.message),
              class: Fe(S(Qe)(oe.message, "edit")),
              title: "编辑",
              "aria-label": "编辑",
              onClick: (Ye) => S(Qt)(oe.message)
            }, " ✎ ", 10, mj),
            _("button", {
              type: "button",
              disabled: !S(D)(oe.message),
              class: Fe(S(Qe)(oe.message, "rerun")),
              title: oe.message.role === "user" ? "从这里重问助手" : "重新生成这条助手回复",
              "aria-label": oe.message.role === "user" ? "从这里重问助手" : "重新生成这条助手回复",
              onClick: (Ye) => S(At)(oe.message)
            }, " ↻ ", 10, gj),
            _("button", {
              type: "button",
              disabled: S(De),
              class: Fe(S(Qe)(oe.message, "delete")),
              title: "删除",
              "aria-label": "删除",
              onClick: (Ye) => S(T)(oe.message)
            }, " ⌫ ", 10, vj)
          ]))
        ], 10, oj)) : (U(), q("details", {
          key: 1,
          class: "manager-tool-turn manager-tool-turn-history",
          "data-manager-anchor-key": oe.anchorKey,
          open: S(ye).isOpen(ge("tool-turn", oe.key)),
          onToggle: (Ye) => S(ye).setOpenFromEvent(ge("tool-turn", oe.key), Ye)
        }, [_("summary", null, [
          _("span", null, Q(oe.rounds.length > 1 ? `工具轮 ${oe.rounds.length} 轮` : "工具轮"), 1),
          _("small", null, Q(S(tt)(oe)), 1),
          _("em", null, Q(S(Le)(oe)), 1)
        ]), S(ye).isOpen(ge("tool-turn", oe.key)) ? (U(), q("div", _j, [(U(!0), q(Ge, null, ut(oe.rounds, (Ye, It) => (U(), q("div", {
          key: `${oe.key}-round-${Ye.assistantMessage.order}`,
          class: "manager-tool-round"
        }, [
          _("div", bj, [_("strong", null, "第 " + Q(Number(It) + 1) + " 轮 · " + Q(Ye.calls.length) + " 个工具", 1), _("span", null, Q(S(d)(Ye.assistantMessage.createdAt)), 1)]),
          S(E)(Ye.assistantMessage.thoughts).length ? (U(), q("details", {
            key: 0,
            class: "manager-tool-thoughts",
            open: S(ye).isOpen(ge("tool-thoughts", oe.key, It, Ye.assistantMessage.order)),
            onToggle: (ct) => S(ye).setOpenFromEvent(ge("tool-thoughts", oe.key, It, Ye.assistantMessage.order), ct)
          }, [_("summary", null, Q(S(C)(S(E)(Ye.assistantMessage.thoughts), !1)), 1), S(ye).isOpen(ge("tool-thoughts", oe.key, It, Ye.assistantMessage.order)) ? (U(!0), q(Ge, { key: 0 }, ut(S(E)(Ye.assistantMessage.thoughts), (ct, ar) => (U(), q("div", {
            key: `${oe.key}-round-${It}-thought-${ar}`,
            class: "chat-thought-block"
          }, [_("strong", null, Q(ct.label), 1), _("pre", null, Q(ct.text), 1)]))), 128)) : ve("", !0)], 40, wj)) : ve("", !0),
          Ye.assistantMessage.content ? (U(), q("div", {
            key: 1,
            class: "manager-tool-preface xb-tavern-markdown",
            "data-markdown-signature": S(p)(Ye.assistantMessage.content),
            innerHTML: S(y)(Ye.assistantMessage.content)
          }, null, 8, Sj)) : ve("", !0),
          (U(!0), q(Ge, null, ut(Ye.calls, (ct) => (U(), q("div", {
            key: ct.id,
            class: Fe(["manager-tool-item", ct.ok ? "is-resolved" : "is-error"])
          }, [
            _("div", Tj, [_("span", null, Q(ct.name), 1), _("em", null, Q(ct.toolMessage ? ct.ok ? "已返回" : "失败" : "等待返回"), 1)]),
            ct.argumentsText ? (U(), q("small", Ej, Q(ct.argumentsText), 1)) : ve("", !0),
            _("p", null, Q(ct.resultText), 1)
          ], 2))), 128))
        ]))), 128))])) : ve("", !0)], 40, yj))], 64))), 128)),
        (U(!0), q(Ge, null, ut(S(it), (oe) => (U(), q(Ge, { key: `live:${oe.key}` }, [oe.kind === "message" ? (U(), q("article", {
          key: 0,
          "data-manager-anchor-key": `live:${oe.anchorKey}`,
          class: Fe(["manager-card manager-message manager-message-live", oe.message.role === "user" ? "manager-message-user" : "manager-message-assistant"])
        }, [_("div", Aj, [_("strong", null, Q(oe.message.role === "user" ? S(w)("user") : "助手"), 1), Ne[20] || (Ne[20] = _("small", null, "正在处理", -1))]), _("div", {
          class: "xb-tavern-markdown",
          "data-markdown-signature": S(p)(oe.message.content),
          innerHTML: S(y)(oe.message.content)
        }, null, 8, Ij)], 10, Cj)) : (U(), q("details", {
          key: 1,
          class: "manager-tool-turn manager-tool-turn-live",
          "data-manager-anchor-key": `live:${oe.anchorKey}`,
          open: S(ye).isOpen(ge("live-tool-turn", oe.key), !0),
          onToggle: (Ye) => S(ye).setOpenFromEvent(ge("live-tool-turn", oe.key), Ye)
        }, [_("summary", null, [
          _("span", null, Q(oe.rounds.length > 1 ? `工具轮 ${oe.rounds.length} 轮` : "工具轮"), 1),
          _("small", null, Q(S(tt)(oe)), 1),
          _("em", null, Q(S(Le)(oe)), 1)
        ]), S(ye).isOpen(ge("live-tool-turn", oe.key), !0) ? (U(), q("div", kj, [(U(!0), q(Ge, null, ut(oe.rounds, (Ye, It) => (U(), q("div", {
          key: `live:${oe.key}-round-${Ye.assistantMessage.order}`,
          class: "manager-tool-round"
        }, [
          _("div", Mj, [_("strong", null, "第 " + Q(Number(It) + 1) + " 轮 · " + Q(Ye.calls.length) + " 个工具", 1), Ne[21] || (Ne[21] = _("span", null, "正在处理", -1))]),
          S(E)(Ye.assistantMessage.thoughts).length ? (U(), q("details", {
            key: 0,
            class: "manager-tool-thoughts",
            open: S(ye).isOpen(ge("live-tool-thoughts", oe.key, It, Ye.assistantMessage.order), !0),
            onToggle: (ct) => S(ye).setOpenFromEvent(ge("live-tool-thoughts", oe.key, It, Ye.assistantMessage.order), ct)
          }, [_("summary", null, Q(S(C)(S(E)(Ye.assistantMessage.thoughts), !0)), 1), S(ye).isOpen(ge("live-tool-thoughts", oe.key, It, Ye.assistantMessage.order), !0) ? (U(!0), q(Ge, { key: 0 }, ut(S(E)(Ye.assistantMessage.thoughts), (ct, ar) => (U(), q("div", {
            key: `live:${oe.key}-round-${It}-thought-${ar}`,
            class: "chat-thought-block"
          }, [_("strong", null, Q(ct.label), 1), _("pre", null, Q(ct.text), 1)]))), 128)) : ve("", !0)], 40, Pj)) : ve("", !0),
          Ye.assistantMessage.content ? (U(), q("div", {
            key: 1,
            class: "manager-tool-preface xb-tavern-markdown",
            "data-markdown-signature": S(p)(Ye.assistantMessage.content),
            innerHTML: S(y)(Ye.assistantMessage.content)
          }, null, 8, Rj)) : ve("", !0),
          (U(!0), q(Ge, null, ut(Ye.calls, (ct) => (U(), q("div", {
            key: `live:${ct.id}`,
            class: Fe(["manager-tool-item", ct.ok ? "is-resolved" : "is-error"])
          }, [
            _("div", Nj, [_("span", null, Q(ct.name), 1), _("em", null, Q(ct.toolMessage ? ct.ok ? "已返回" : "失败" : "等待返回"), 1)]),
            ct.argumentsText ? (U(), q("small", $j, Q(ct.argumentsText), 1)) : ve("", !0),
            _("p", null, Q(ct.resultText), 1)
          ], 2))), 128))
        ]))), 128))])) : ve("", !0)], 40, xj))], 64))), 128)),
        S(De) && !S(it).length ? (U(), q("article", Dj, [...Ne[22] || (Ne[22] = [_("div", { class: "manager-run-title" }, [_("strong", null, "助手"), _("small", null, "正在处理")], -1), _("p", null, "正在思考...", -1)])])) : ve("", !0),
        S(at).length || S(_e).length ? (U(), q("details", {
          key: 3,
          class: "manager-work-drawer",
          "data-manager-anchor-key": "meta:work",
          open: S(ye).isOpen(ge("work-drawer")),
          onToggle: Ne[10] || (Ne[10] = (oe) => S(ye).setOpenFromEvent(ge("work-drawer"), oe))
        }, [_("summary", null, [Ne[23] || (Ne[23] = _("strong", null, "工作记录", -1)), S(v) ? (U(), q("span", Oj, Q(S(Be)(S(v)) ? "重试中" : S(we)(S(v).status)) + " · " + Q(S(L)(S(v))), 1)) : (U(), q("span", Fj, Q(S(M).length) + "/" + Q(S(at).length) + " 份档案", 1))]), S(ye).isOpen(ge("work-drawer")) ? (U(), q(Ge, { key: 0 }, [S(v) ? (U(), q("article", {
          key: 0,
          "data-manager-anchor-key": `run:${S(v).id}`,
          class: Fe(["manager-card manager-message manager-message-run manager-work-current", [
            `is-${S(v).status}`,
            `tone-${S(Pe)(S(v))}`,
            { "is-retrying": S(Be)(S(v)) }
          ]]),
          "aria-busy": S(Be)(S(v)) ? "true" : "false"
        }, [
          _("div", Uj, [Ne[24] || (Ne[24] = _("strong", null, "本次后台工作", -1)), _("small", null, Q(S(Be)(S(v)) ? "重试中" : S(we)(S(v).status)), 1)]),
          _("p", qj, Q(S(L)(S(v))), 1),
          _("small", null, Q(S(V)(S(v))), 1),
          _("small", Hj, Q(S(Be)(S(v)) ? "重试已开始 · 正在等 API/工具返回" : S(x)(S(v))), 1),
          _("div", Gj, [_("p", null, Q(S(z)(S(v))), 1), _("p", null, Q(S(G)(S(v))), 1)]),
          S(v).outputText ? (U(), q("p", Wj, " 结果：" + Q(S(a)(S(v).outputText, 180)), 1)) : ve("", !0),
          S(P)(S(v)) ? (U(), q("p", Vj, Q(S(P)(S(v))), 1)) : ve("", !0),
          S($e)(S(v).toolTrace).length ? (U(), q("details", {
            key: 2,
            class: "manager-work-debug",
            open: S(ye).isOpen(ge("work-debug", S(v).id)),
            onToggle: Ne[7] || (Ne[7] = (oe) => S(ye).setOpenFromEvent(ge("work-debug", S(v).id), oe))
          }, [_("summary", null, Q(S(te)(S(v).toolTrace)), 1), S(ye).isOpen(ge("work-debug", S(v).id)) ? (U(), q("div", zj, [(U(!0), q(Ge, null, ut(S($e)(S(v).toolTrace), (oe) => (U(), q("div", {
            key: oe.id,
            class: Fe(["manager-tool-item", S(Re)(oe)])
          }, [
            _("div", Jj, [_("span", null, Q(oe.name), 1), _("em", null, [er(Q(S(Ve)(oe)), 1), oe.elapsedLabel ? (U(), q(Ge, { key: 0 }, [er(" · " + Q(oe.elapsedLabel), 1)], 64)) : ve("", !0)])]),
            oe.thoughts.length ? (U(), q("details", {
              key: 0,
              class: "manager-tool-thoughts",
              open: S(ye).isOpen(ge("work-tool-thoughts", S(v).id, oe.id)),
              onToggle: (Ye) => S(ye).setOpenFromEvent(ge("work-tool-thoughts", S(v).id, oe.id), Ye)
            }, [_("summary", null, Q(S(C)(oe.thoughts, !1)), 1), S(ye).isOpen(ge("work-tool-thoughts", S(v).id, oe.id)) ? (U(!0), q(Ge, { key: 0 }, ut(oe.thoughts, (Ye, It) => (U(), q("div", {
              key: `${oe.id}-stored-thought-${It}`,
              class: "chat-thought-block"
            }, [_("strong", null, Q(Ye.label), 1), _("pre", null, Q(Ye.text), 1)]))), 128)) : ve("", !0)], 40, Yj)) : ve("", !0),
            oe.preface ? (U(), q("div", {
              key: 1,
              class: "manager-tool-preface xb-tavern-markdown",
              "data-markdown-signature": S(p)(oe.preface),
              innerHTML: S(y)(oe.preface)
            }, null, 8, Xj)) : ve("", !0),
            oe.args ? (U(), q("small", Qj, Q(oe.args), 1)) : ve("", !0),
            oe.summary ? (U(), q("p", Zj, Q(oe.summary), 1)) : ve("", !0),
            oe.path ? (U(), q("p", jj, Q(oe.path), 1)) : ve("", !0)
          ], 2))), 128))])) : ve("", !0)], 40, Kj)) : ve("", !0),
          S(v).status === "failed" ? (U(), q("button", {
            key: 3,
            type: "button",
            class: "manager-run-retry-button",
            disabled: S(je) || S(Be)(S(v)),
            onClick: Ne[8] || (Ne[8] = (oe) => S(ft)(S(v)))
          }, Q(S(Be)(S(v)) ? "重试中" : "重试"), 9, eee)) : ve("", !0)
        ], 10, Bj)) : (U(), q("article", tee, [
          _("div", nee, [Ne[25] || (Ne[25] = _("strong", null, "记忆档案", -1)), _("small", null, Q(S(M).length) + "/" + Q(S(at).length), 1)]),
          _("p", null, Q(S(mt)), 1),
          S(Nt) ? (U(), q("p", ree, " 当前打开：" + Q(S(nt)(S(Nt))), 1)) : ve("", !0)
        ])), S(k).length || S(N) ? (U(), q("details", {
          key: 2,
          class: "manager-work-history",
          open: S(ye).isOpen(ge("work-history")),
          onToggle: Ne[9] || (Ne[9] = (oe) => S(ye).setOpenFromEvent(ge("work-history"), oe))
        }, [_("summary", null, [Ne[26] || (Ne[26] = _("strong", null, "历史记录", -1)), _("span", null, Q(S(k).length + S(N)) + " 条", 1)]), S(ye).isOpen(ge("work-history")) ? (U(), q(Ge, { key: 0 }, [(U(!0), q(Ge, null, ut(S(k), (oe) => (U(), q("div", {
          key: oe.id,
          class: Fe(["manager-history-row", [`tone-${S(Pe)(oe)}`]])
        }, [_("div", null, [_("strong", null, Q(S(we)(oe.status)), 1), _("small", null, Q(S(L)(oe)), 1)]), _("span", null, Q(S(te)(oe.toolTrace) || S(x)(oe)), 1)], 2))), 128)), S(N) ? (U(), q("p", oee, " 更早 " + Q(S(N)) + " 条已收起。 ", 1)) : ve("", !0)], 64)) : ve("", !0)], 40, see)) : ve("", !0)], 64)) : ve("", !0)], 40, Lj)) : ve("", !0),
        S(fe).length ? ve("", !0) : (U(), q("p", iee, " 还没有和助手对话。 "))
      ], 32), Ft(FN, {
        "extra-class": "manager-scroll-helpers",
        active: S(xe),
        "show-top": S(Or),
        "show-bottom": S(Yn),
        onTop: S(qt),
        onBottom: Ne[15] || (Ne[15] = (oe) => S(Mt)(!0, {
          collapseWindow: !0,
          revealHelpers: !0
        }))
      }, null, 8, [
        "active",
        "show-top",
        "show-bottom",
        "onTop"
      ])]),
      _("form", {
        class: "manager-compose chat-compose",
        onSubmit: Ne[19] || (Ne[19] = Pn((...oe) => S(j) && S(j)(...oe), ["prevent"]))
      }, [
        S(Se) ? (U(), q("div", aee, Q(S(Se)), 1)) : ve("", !0),
        wn(_("textarea", {
          ref: Je,
          "onUpdate:modelValue": Ne[16] || (Ne[16] = (oe) => /* @__PURE__ */ Xt(ue) ? ue.value = oe : null),
          rows: "3",
          placeholder: "和助手说一句话...",
          disabled: S(De),
          onInput: Ne[17] || (Ne[17] = (...oe) => S(h) && S(h)(...oe)),
          onKeydown: Ne[18] || (Ne[18] = (...oe) => S(O) && S(O)(...oe))
        }, null, 40, lee), [[ho, S(ue)]]),
        _("button", {
          type: "submit",
          class: "primary-action",
          disabled: !S(m)
        }, Q(S(Ie) ? "正在停止" : S(De) ? "停止" : "发送"), 9, uee)
      ], 32)
    ], 8, jZ));
  }
}), dee = cee, zc = [
  0,
  0,
  800,
  600
], fee = 48, Bf = 160;
function fs(e, t, n, r) {
  if (![
    e,
    t,
    n,
    r
  ].every((l) => Number.isFinite(l))) return null;
  const s = Math.min(e, n), o = Math.max(e, n), i = Math.min(t, r), a = Math.max(t, r);
  return {
    minX: s,
    minY: i,
    maxX: o,
    maxY: a,
    width: Math.max(0, o - s),
    height: Math.max(0, a - i)
  };
}
function BN(e, t) {
  return e ? t ? fs(Math.min(e.minX, t.minX), Math.min(e.minY, t.minY), Math.max(e.maxX, t.maxX), Math.max(e.maxY, t.maxY)) : e : t;
}
function UN(e, t) {
  return fs(e.minX - t, e.minY - t, e.maxX + t, e.maxY + t) || e;
}
function XA(e, t) {
  return t.map(([n, r]) => [e.at[0] + n, e.at[1] + r]);
}
function QA(e) {
  return !Array.isArray(e) || !e.length ? null : e.reduce((t, [n, r]) => BN(t, fs(n, r, n, r)), null);
}
function hee(e) {
  return e.rect ? fs(e.at[0], e.at[1], e.at[0] + e.rect[0], e.at[1] + e.rect[1]) : null;
}
function pee(e) {
  return typeof e.circle != "number" || e.circle <= 0 ? null : fs(e.at[0] - e.circle, e.at[1] - e.circle, e.at[0] + e.circle, e.at[1] + e.circle);
}
function mee(e) {
  return e.icon ? fs(e.at[0] - 10, e.at[1] - 10, e.at[0] + 10, e.at[1] + 10) : null;
}
function gee(e) {
  if (!e.text) return null;
  const t = Math.max(12, e.text.length * 8.4);
  return fs(e.at[0] - t / 2, e.at[1] - 14, e.at[0] + t / 2, e.at[1] + 6);
}
function vee(e) {
  const t = Number(e.style?.width || 2);
  return Math.max(4, Number.isFinite(t) && t > 0 ? t / 2 + 4 : 4);
}
function yee(e) {
  if (!Array.isArray(e) || e.length !== 4) return [...zc];
  const t = e.map((n) => Number(n));
  return t.every((n) => Number.isFinite(n)) ? [
    t[0],
    t[1],
    Math.max(1, t[2]),
    Math.max(1, t[3])
  ] : [...zc];
}
function B_(e) {
  let t = null;
  return e.rect ? t = hee(e) : typeof e.circle == "number" ? t = pee(e) : e.path ? t = QA(XA(e, e.path)) : e.curve ? t = QA(XA(e, e.curve)) : e.icon ? t = mee(e) : e.text && (t = gee(e)), t ? UN(t, vee(e)) : null;
}
function _ee(e) {
  return e?.elements?.length ? e.elements.reduce((t, n) => BN(t, B_(n)), null) : null;
}
function bee(e, t) {
  if (!Number.isFinite(t) || t <= 0) return e;
  const n = Math.max(e.width, 1), r = Math.max(e.height, 1), s = n / r;
  if (Math.abs(s - t) < 0.01) return e;
  if (s > t) {
    const i = (n / t - r) / 2;
    return fs(e.minX, e.minY - i, e.maxX, e.maxY + i) || e;
  }
  const o = (r * t - n) / 2;
  return fs(e.minX - o, e.minY, e.maxX + o, e.maxY) || e;
}
function wee(e, t) {
  const n = t >= 1 ? Bf : Bf * t, r = t >= 1 ? Bf / t : Bf;
  let s = e;
  if (s.width < n) {
    const o = (n - s.width) / 2;
    s = fs(s.minX - o, s.minY, s.maxX + o, s.maxY) || s;
  }
  if (s.height < r) {
    const o = (r - s.height) / 2;
    s = fs(s.minX, s.minY - o, s.maxX, s.maxY + o) || s;
  }
  return s;
}
function See(e, t) {
  const n = _ee(e);
  if (!n) return [...zc];
  const r = wee(bee(UN(n, fee), t), t);
  return [
    Number(r.minX.toFixed(2)),
    Number(r.minY.toFixed(2)),
    Number(r.width.toFixed(2)),
    Number(r.height.toFixed(2))
  ];
}
function Tee(e) {
  return e?.meta?.viewBox ? yee(e.meta.viewBox) : See(e, zc[2] / zc[3]);
}
var Eee = {
  key: 0,
  class: "tavern-editor-head xb-editor-head tavern-map-head"
}, Cee = { class: "tavern-map-title" }, Aee = { key: 0 }, Iee = { class: "tavern-editor-actions xb-editor-actions tavern-map-actions" }, xee = ["disabled"], kee = ["disabled"], Mee = ["disabled"], Pee = {
  key: 0,
  class: "tavern-map-canvas-title"
}, Ree = ["viewBox", "aria-label"], Nee = { class: "map-fill-layer" }, $ee = ["d", "fill"], Dee = {
  class: "map-line-layer",
  filter: "url(#tavern-map-sketch)"
}, Lee = [
  "d",
  "fill",
  "stroke",
  "stroke-width",
  "stroke-dasharray"
], Oee = { class: "map-label-layer" }, Fee = [
  "x",
  "y",
  "font-size",
  "fill",
  "text-anchor"
], Bee = { class: "map-removed-layer" }, Uee = ["d", "fill"], qee = [
  "d",
  "fill",
  "stroke",
  "stroke-width",
  "stroke-dasharray"
], Hee = [
  "x",
  "y",
  "font-size",
  "fill",
  "text-anchor"
], Gee = ["aria-expanded", "title"], Wee = {
  key: 2,
  class: "tavern-map-timeline-control"
}, Vee = {
  key: 2,
  class: "tavern-map-empty"
}, Kee = {
  key: 3,
  class: "tavern-editor-foot xb-editor-foot tavern-map-foot"
}, zee = { key: 0 }, Jee = { key: 1 }, Yee = /* @__PURE__ */ jt({
  __name: "TavernMapPanel",
  props: {
    document: {},
    patches: {},
    compact: {
      type: Boolean,
      default: !1
    }
  },
  setup(e) {
    const t = e, n = /* @__PURE__ */ se(0), r = /* @__PURE__ */ se("patch"), s = /* @__PURE__ */ se(0);
    let o;
    function i(te) {
      return JSON.parse(JSON.stringify(te));
    }
    function a() {
      return Uc();
    }
    function l(te) {
      if (!te || typeof te != "object" || Array.isArray(te)) return a();
      const fe = te, ae = a();
      return {
        meta: fe.meta && typeof fe.meta == "object" && !Array.isArray(fe.meta) ? {
          ...ae.meta,
          ...i(fe.meta)
        } : ae.meta,
        elements: Array.isArray(fe.elements) ? i(fe.elements).filter((Je) => !!Je && typeof Je == "object" && !Array.isArray(Je) && typeof Je.id == "string") : []
      };
    }
    const u = ie(() => {
      const te = t.document?.data;
      return !te || typeof te != "object" || Array.isArray(te) ? null : l(te);
    }), d = ie(() => t.patches.at(-1) || null), h = /* @__PURE__ */ se(!0), p = ie(() => {
      const te = [...t.patches].sort((ae, Je) => Number(ae.revision || 0) - Number(Je.revision || 0));
      let fe = a();
      return te.map((ae, Je) => {
        const Ce = Array.isArray(ae.ops) ? ae.ops : [];
        return fe = vH(l(fe), Ce), {
          revision: Number(ae.revision || Je + 1),
          document: i(fe),
          patch: ae,
          index: Je
        };
      });
    }), y = ie(() => {
      if (!p.value.length) return null;
      const te = Math.max(0, Math.min(s.value, p.value.length - 1));
      return p.value[te] || null;
    }), w = ie(() => p.value.length > 0 && Number(p.value[0]?.revision || 0) <= 1), E = ie(() => r.value === "timeline" && y.value?.patch || d.value), C = ie(() => r.value === "timeline" && y.value?.document || u.value), M = ie(() => Array.isArray(E.value?.ops) ? E.value?.ops : []), k = ie(() => new Set((E.value?.changedIds || []).map((te) => String(te || "").trim()).filter(Boolean))), R = ie(() => {
      const te = /* @__PURE__ */ new Map();
      return M.value.forEach((fe) => {
        const ae = String(fe.op || "").trim(), Je = fe.element && typeof fe.element == "object" && !Array.isArray(fe.element) ? fe.element : null, Ce = String(fe.id || Je?.id || "").trim();
        Ce && (ae === "add" || ae === "modify" || ae === "remove") && te.set(Ce, ae);
      }), te;
    }), D = ie(() => M.value.filter((te) => String(te.op || "").trim() === "remove").map((te, fe) => ({
      id: String(te.id || `removed-${fe}`).trim(),
      delayMs: fe * 120
    })).filter((te) => te.id)), m = ie(() => (Array.isArray(E.value?.removedElements) ? E.value?.removedElements : []).filter((te) => !!te && typeof te == "object" && !Array.isArray(te) && typeof te.id == "string")), b = ie(() => Tee(C.value)), v = ie(() => b.value.join(" ")), T = ie(() => String(C.value?.meta?.theme || "parchment")), I = ie(() => String(r.value === "timeline" ? C.value?.meta?.name || t.document?.title || "地图" : t.document?.title || C.value?.meta?.name || "地图")), x = ie(() => String(t.document?.digest || "")), P = ie(() => y.value && r.value === "timeline" ? `revision ${y.value.revision}` : t.document ? `revision ${t.document.revision}` : "no map"), L = ie(() => E.value?.summary || (E.value ? `revision ${E.value.revision}` : "等待空间变化")), G = ie(() => r.value === "full" ? "完整重绘" : r.value === "timeline" ? `回合 ${O.value}` : "地图更新"), z = ie(() => x.value.split(`
`).map((te) => te.trim()).filter(Boolean).slice(0, 4)), V = ie(() => C.value?.elements.length || 0), Z = ie(() => t.patches.length), O = ie(() => y.value ? `${y.value.index + 1} / ${p.value.length}` : "0 / 0"), Y = ie(() => pH(C.value)), X = ie(() => !!E.value);
    yt(() => t.document?.revision, () => {
      r.value = "patch", j(), s.value = Math.max(0, p.value.length - 1), n.value += 1, h.value = !0;
    }), yt(() => t.patches.length, () => {
      s.value >= p.value.length && (s.value = Math.max(0, p.value.length - 1)), r.value === "timeline" && !w.value && (r.value = "patch", j()), t.patches.length && (h.value = !0);
    }), vl(() => {
      j();
    });
    function j() {
      o !== void 0 && (window.clearTimeout(o), o = void 0);
    }
    function re(te, fe = [0, 0]) {
      if (!Array.isArray(te) || te.length < 2) return fe;
      const ae = Number(te[0]), Je = Number(te[1]);
      return Number.isFinite(ae) && Number.isFinite(Je) ? [ae, Je] : fe;
    }
    function pe(te, fe) {
      return fe.map(([ae, Je]) => [te.at[0] + ae, te.at[1] + Je]);
    }
    function B(te, fe = !1) {
      if (!Array.isArray(te) || te.length < 2) return "";
      const ae = [`M ${te[0][0]} ${te[0][1]}`, ...te.slice(1).map(([Je, Ce]) => `L ${Je} ${Ce}`)];
      return fe && ae.push("Z"), ae.join(" ");
    }
    function N(te, fe = !1) {
      if (!Array.isArray(te) || te.length < 2) return "";
      if (te.length === 2) return B(te, fe);
      const ae = 0.3, Je = [`M ${te[0][0]} ${te[0][1]}`];
      for (let Ce = 0; Ce < te.length - 1; Ce += 1) {
        const ye = te[Math.max(Ce - 1, 0)], ge = te[Ce], Ze = te[Ce + 1], Ne = te[Math.min(Ce + 2, te.length - 1)];
        Je.push(`C ${ge[0] + (Ze[0] - ye[0]) * ae} ${ge[1] + (Ze[1] - ye[1]) * ae}, ${Ze[0] - (Ne[0] - ge[0]) * ae} ${Ze[1] - (Ne[1] - ge[1]) * ae}, ${Ze[0]} ${Ze[1]}`);
      }
      return fe && Je.push("Z"), Je.join(" ");
    }
    function H(te) {
      const [fe, ae] = te.at, [Je, Ce] = re(te.rect, [10, 10]);
      return `M ${fe} ${ae} L ${fe + Je} ${ae} L ${fe + Je} ${ae + Ce} L ${fe} ${ae + Ce} Z`;
    }
    function le(te) {
      const [fe, ae] = te.at, Je = Number(te.circle || 8), Ce = Number.isFinite(Je) && Je > 0 ? Je : 8;
      return `M ${fe - Ce} ${ae} A ${Ce} ${Ce} 0 1 0 ${fe + Ce} ${ae} A ${Ce} ${Ce} 0 1 0 ${fe - Ce} ${ae}`;
    }
    function Ie(te) {
      const [fe, ae] = te.at, Je = 10, Ce = Je / 2, ye = String(te.icon || "+");
      return ye === "x" ? `M ${fe - Ce} ${ae - Ce} L ${fe + Ce} ${ae + Ce} M ${fe + Ce} ${ae - Ce} L ${fe - Ce} ${ae + Ce}` : ye === "o" ? `M ${fe - Ce} ${ae} A ${Ce} ${Ce} 0 1 0 ${fe + Ce} ${ae} A ${Ce} ${Ce} 0 1 0 ${fe - Ce} ${ae}` : ye === "tree" ? `M ${fe} ${ae - 8} L ${fe - 6} ${ae + 2} L ${fe + 6} ${ae + 2} Z M ${fe} ${ae + 2} L ${fe} ${ae + 8}` : ye === "star" ? `M ${fe} ${ae - 7} L ${fe + 2} ${ae - 2} L ${fe + 7} ${ae - 2} L ${fe + 3} ${ae + 1} L ${fe + 5} ${ae + 7} L ${fe} ${ae + 3} L ${fe - 5} ${ae + 7} L ${fe - 3} ${ae + 1} L ${fe - 7} ${ae - 2} L ${fe - 2} ${ae - 2} Z` : ye === "arrow-n" ? `M ${fe} ${ae + Je} L ${fe} ${ae - Je} M ${fe - Ce} ${ae - Ce} L ${fe} ${ae - Je} L ${fe + Ce} ${ae - Ce}` : ye === "arrow-s" ? `M ${fe} ${ae - Je} L ${fe} ${ae + Je} M ${fe - Ce} ${ae + Ce} L ${fe} ${ae + Je} L ${fe + Ce} ${ae + Ce}` : ye === "arrow-e" ? `M ${fe - Je} ${ae} L ${fe + Je} ${ae} M ${fe + Ce} ${ae - Ce} L ${fe + Je} ${ae} L ${fe + Ce} ${ae + Ce}` : ye === "arrow-w" ? `M ${fe + Je} ${ae} L ${fe - Je} ${ae} M ${fe - Ce} ${ae - Ce} L ${fe - Je} ${ae} L ${fe - Ce} ${ae + Ce}` : `M ${fe - Ce} ${ae} L ${fe + Ce} ${ae} M ${fe} ${ae - Ce} L ${fe} ${ae + Ce}`;
    }
    function De(te) {
      return te.rect ? H(te) : typeof te.circle == "number" ? le(te) : te.path ? B(pe(te, te.path), te.closed === !0) : te.curve ? N(pe(te, te.curve), te.closed === !0) : te.icon ? Ie(te) : "";
    }
    function Be(te) {
      if (te.style?.color) return String(te.style.color);
      const fe = {
        wall: "#2f2418",
        road: "#9a8253",
        water: "#2d7490",
        terrain: "#55734b",
        furniture: "#765d37",
        door: "#a5652b",
        danger: "#b94035",
        marker: "#b94035",
        label: "#2b2118",
        grid: "#9f987f",
        magic: "#76539a",
        secret: "#68625b"
      };
      return fe[String(te.cat || "wall")] || fe.wall;
    }
    function it(te) {
      return te.fill ? String(te.fill) : te.rect || typeof te.circle == "number" || (te.path || te.curve) && te.closed === !0 ? {
        water: "rgba(63, 136, 167, 0.24)",
        terrain: "rgba(87, 121, 70, 0.18)",
        danger: "rgba(180, 64, 54, 0.15)",
        magic: "rgba(126, 83, 154, 0.16)",
        secret: "rgba(63, 56, 48, 0.10)"
      }[String(te.cat || "")] || "rgba(92, 70, 36, 0.08)" : "none";
    }
    function Qe(te) {
      const fe = Number(te.style?.width || 2);
      return Number.isFinite(fe) && fe > 0 ? fe : 2;
    }
    function je(te) {
      return String(te.style?.dash || "");
    }
    function W(te, fe) {
      if (typeof te.circle == "number") return Math.max(32, Math.PI * 2 * te.circle);
      if (te.rect) return Math.max(40, Math.abs(te.rect[0]) * 2 + Math.abs(te.rect[1]) * 2);
      const ae = fe.match(/-?\d+(?:\.\d+)?/g)?.map(Number) || [];
      let Je = 0;
      for (let Ce = 0; Ce + 3 < ae.length; Ce += 2) {
        const ye = ae[Ce + 2] - ae[Ce], ge = ae[Ce + 3] - ae[Ce + 1];
        Je += Math.hypot(ye, ge);
      }
      return Math.max(28, Math.min(2400, Je || 180));
    }
    function ee(te) {
      return r.value === "full" ? "add" : R.value.get(te.id) || (k.value.has(te.id) ? "modify" : "stable");
    }
    function ue(te, fe) {
      if (!te) return fe;
      if (!fe) return te;
      const ae = Math.min(te.minX, fe.minX), Je = Math.min(te.minY, fe.minY), Ce = Math.max(te.maxX, fe.maxX), ye = Math.max(te.maxY, fe.maxY);
      return {
        minX: ae,
        minY: Je,
        maxX: Ce,
        maxY: ye,
        width: Math.max(0, Ce - ae),
        height: Math.max(0, ye - Je)
      };
    }
    function Se() {
      return (C.value?.elements || []).reduce((te, fe) => fe.text || String(fe.cat || "") === "label" ? te : ue(te, B_(fe)), null);
    }
    function be(te) {
      if (!te.text || String(te.cat || "") !== "label") return te.at;
      const fe = String(te.id || "").trim().toLowerCase();
      if (fe.startsWith("__label__")) return te.at;
      const [ae, Je, Ce, ye] = b.value, ge = Je + Math.max(72, ye * 0.26);
      if (![
        "label",
        "room-label",
        "scene-label",
        "place-label",
        "map-label"
      ].includes(fe) || te.at[1] >= ge) return te.at;
      const Ze = Se(), Ne = (Ze?.minX ?? ae) + 24, oe = (Ze?.maxX ?? ae + Ce) - 24, Ye = Ze ? Ze.minY + Math.min(90, Math.max(42, Ze.height * 0.22)) : ge, It = Math.max(ge, Ye);
      return [Math.min(oe, Math.max(Ne, te.at[0])), Math.min(Je + ye - 24, It)];
    }
    function _e(te) {
      if (!te.text || String(te.cat || "") !== "label") return 14;
      const fe = String(te.id || "").trim().toLowerCase();
      return fe.startsWith("__label__") ? 14 : [
        "label",
        "room-label",
        "scene-label",
        "place-label",
        "map-label"
      ].includes(fe) ? 13 : 14;
    }
    function Pe(te, fe, ae) {
      if (!B_(te)) return [];
      const Je = ae || ee(te), Ce = ae === "remove" ? fe * 110 : r.value === "full" ? fe * 115 : Je === "stable" ? 0 : fe * 30;
      if (te.text) {
        const [It, ct] = be(te);
        return [{
          element: te,
          id: `${te.id}-label`,
          layer: "label",
          path: "",
          color: ae === "remove" ? "#b94035" : Be(te),
          fill: "none",
          strokeWidth: 0,
          dash: "",
          delayMs: Ce,
          durationMs: 900,
          length: 60,
          opKind: Je,
          text: te.text,
          x: It,
          y: ct,
          fontSize: _e(te),
          anchor: "middle"
        }];
      }
      const ye = De(te);
      if (!ye) return [];
      const ge = W(te, ye), Ze = ae === "remove" ? 1050 : Math.max(680, Math.min(2200, ge * 4.2)), Ne = ae === "remove" ? "#b94035" : Be(te), oe = ae === "remove" ? "rgba(185, 64, 53, 0.16)" : it(te), Ye = [];
      return oe !== "none" && Ye.push({
        element: te,
        id: `${te.id}-fill`,
        layer: "fill",
        path: ye,
        color: Ne,
        fill: oe,
        strokeWidth: 0,
        dash: "",
        delayMs: Ce,
        durationMs: Ze,
        length: ge,
        opKind: Je,
        text: "",
        x: 0,
        y: 0,
        fontSize: 0,
        anchor: "middle"
      }), Ye.push({
        element: te,
        id: `${te.id}-line`,
        layer: "line",
        path: ye,
        color: Ne,
        fill: "none",
        strokeWidth: Qe(te),
        dash: je(te),
        delayMs: Ce,
        durationMs: Ze,
        length: ge,
        opKind: Je,
        text: "",
        x: 0,
        y: 0,
        fontSize: 0,
        anchor: "middle"
      }), Ye;
    }
    const xe = ie(() => (C.value?.elements || []).flatMap((te, fe) => Pe(te, fe))), ke = ie(() => xe.value.filter((te) => te.layer === "fill")), we = ie(() => xe.value.filter((te) => te.layer === "line")), Ve = ie(() => xe.value.filter((te) => te.layer === "label")), Re = ie(() => m.value.flatMap((te, fe) => Pe(te, fe, "remove"))), $e = ie(() => Re.value.filter((te) => te.layer === "fill")), Le = ie(() => Re.value.filter((te) => te.layer === "line")), tt = ie(() => Re.value.filter((te) => te.layer === "label")), nt = ie(() => xe.value.filter((te) => r.value === "full" || te.opKind !== "stable")), at = ie(() => {
      const te = nt.value;
      return te.length ? Math.max(...te.map((fe) => fe.delayMs + fe.durationMs)) + 360 : 1200;
    }), mt = ie(() => ({ "--map-progress-duration": `${at.value}ms` })), ft = ie(() => {
      const te = nt.value.find((fe) => fe.layer !== "label" && fe.path) || nt.value[0];
      return !te || !te.path ? { display: "none" } : {
        offsetPath: `path("${te.path}")`,
        animationDelay: `${te.delayMs}ms`,
        animationDuration: `${te.durationMs}ms`
      };
    });
    function Pt() {
      j(), !(r.value !== "timeline" || !w.value || p.value.length <= 1) && Jt(() => {
        o = window.setTimeout(() => {
          r.value === "timeline" && (s.value = s.value + 1 >= p.value.length ? 0 : s.value + 1, n.value += 1, Pt());
        }, at.value + 420);
      });
    }
    function At(te) {
      return {
        "--map-delay": `${te.delayMs}ms`,
        "--map-duration": `${te.durationMs}ms`,
        "--map-length": `${te.length}`
      };
    }
    function Ut(te) {
      return [
        "map-element",
        `map-${te.layer}`,
        `cat-${te.element.cat || "wall"}`,
        `op-${te.opKind}`,
        {
          "is-animated": r.value === "full" || te.opKind !== "stable",
          "is-latest": k.value.has(te.element.id)
        }
      ];
    }
    function Mt() {
      j(), r.value = "patch", n.value += 1;
    }
    function qt() {
      j(), r.value = "full", n.value += 1;
    }
    function Nt() {
      w.value && (r.value = "timeline", s.value = 0, n.value += 1, Pt());
    }
    function Yn(te) {
      if (j(), r.value = "timeline", !w.value) return;
      const fe = p.value.length;
      fe && (s.value = (s.value + te + fe) % fe, n.value += 1);
    }
    function Or() {
      h.value = !h.value;
    }
    function Qt() {
      h.value = !1;
    }
    return (te, fe) => (U(), q("aside", { class: Fe(["tavern-map-panel xb-editor", { "is-compact": e.compact }]) }, [
      e.compact ? ve("", !0) : (U(), q("header", Eee, [_("div", Cee, [
        fe[2] || (fe[2] = _("span", { class: "tavern-map-kicker" }, "LIVE CARTOGRAPHY", -1)),
        _("strong", null, Q(I.value), 1),
        e.document ? (U(), q("span", Aee, Q(P.value) + " · " + Q(V.value) + " elements", 1)) : ve("", !0)
      ]), _("div", Iee, [
        _("button", {
          type: "button",
          class: Fe(["tavern-editor-mode-button", { active: r.value === "patch" }]),
          disabled: !d.value,
          onClick: Mt
        }, " 最近回合 ", 10, xee),
        _("button", {
          type: "button",
          class: Fe(["tavern-editor-mode-button", { active: r.value === "full" }]),
          disabled: !Y.value,
          onClick: qt
        }, " 完整重绘 ", 10, kee),
        _("button", {
          type: "button",
          class: Fe(["tavern-editor-mode-button", { active: r.value === "timeline" }]),
          disabled: !w.value,
          onClick: Nt
        }, " 回合回放 ", 10, Mee)
      ])])),
      Y.value ? (U(), q("div", {
        key: 1,
        class: Fe(["tavern-map-canvas", [`theme-${T.value}`, `mode-${r.value}`]])
      }, [
        e.compact ? (U(), q("div", Pee, Q(I.value), 1)) : ve("", !0),
        (U(), q("svg", {
          key: n.value,
          viewBox: v.value,
          preserveAspectRatio: "xMidYMid meet",
          role: "img",
          "aria-label": I.value
        }, [
          fe[3] || (fe[3] = kl('<defs><filter id="tavern-map-sketch" x="-5%" y="-5%" width="110%" height="110%"><feTurbulence type="fractalNoise" baseFrequency="0.025" numOctaves="3" seed="7" result="noise"></feTurbulence><feDisplacementMap in="SourceGraphic" in2="noise" scale="0.65"></feDisplacementMap></filter><filter id="tavern-map-glow" x="-30%" y="-30%" width="160%" height="160%"><feGaussianBlur stdDeviation="3" result="blur"></feGaussianBlur><feMerge><feMergeNode in="blur"></feMergeNode><feMergeNode in="SourceGraphic"></feMergeNode></feMerge></filter></defs>', 1)),
          _("g", Nee, [(U(!0), q(Ge, null, ut(ke.value, (ae) => (U(), q("path", {
            key: ae.id,
            d: ae.path,
            fill: ae.fill,
            class: Fe(Ut(ae)),
            style: Ir(At(ae))
          }, null, 14, $ee))), 128))]),
          _("g", Dee, [(U(!0), q(Ge, null, ut(we.value, (ae) => (U(), q("path", {
            key: ae.id,
            d: ae.path,
            fill: ae.fill,
            stroke: ae.color,
            "stroke-width": ae.strokeWidth,
            "stroke-dasharray": ae.dash,
            "stroke-linecap": "round",
            "stroke-linejoin": "round",
            class: Fe(Ut(ae)),
            style: Ir(At(ae))
          }, null, 14, Lee))), 128))]),
          _("g", Oee, [(U(!0), q(Ge, null, ut(Ve.value, (ae) => (U(), q("text", {
            key: ae.id,
            x: ae.x,
            y: ae.y,
            "font-size": ae.fontSize,
            fill: ae.color,
            "text-anchor": ae.anchor,
            class: Fe(Ut(ae)),
            style: Ir(At(ae))
          }, Q(ae.text), 15, Fee))), 128))]),
          _("g", Bee, [
            (U(!0), q(Ge, null, ut($e.value, (ae) => (U(), q("path", {
              key: `removed-fill-${ae.id}`,
              d: ae.path,
              fill: ae.fill,
              class: Fe(Ut(ae)),
              style: Ir(At(ae))
            }, null, 14, Uee))), 128)),
            (U(!0), q(Ge, null, ut(Le.value, (ae) => (U(), q("path", {
              key: `removed-line-${ae.id}`,
              d: ae.path,
              fill: ae.fill,
              stroke: ae.color,
              "stroke-width": ae.strokeWidth,
              "stroke-dasharray": ae.dash,
              "stroke-linecap": "round",
              "stroke-linejoin": "round",
              class: Fe(Ut(ae)),
              style: Ir(At(ae))
            }, null, 14, qee))), 128)),
            (U(!0), q(Ge, null, ut(tt.value, (ae) => (U(), q("text", {
              key: `removed-label-${ae.id}`,
              x: ae.x,
              y: ae.y,
              "font-size": ae.fontSize,
              fill: ae.color,
              "text-anchor": ae.anchor,
              class: Fe(Ut(ae)),
              style: Ir(At(ae))
            }, Q(ae.text), 15, Hee))), 128))
          ])
        ], 8, Ree)),
        (U(), q("div", {
          key: `pen-${n.value}`,
          class: "tavern-map-pen",
          style: Ir(ft.value)
        }, null, 4)),
        (U(), q("div", {
          key: `progress-${n.value}`,
          class: "tavern-map-progress",
          style: Ir(mt.value)
        }, null, 4)),
        X.value ? (U(), q("div", {
          key: 1,
          class: Fe(["tavern-map-badge-shell", { "is-open": h.value }])
        }, [_("button", {
          type: "button",
          class: "tavern-map-badge-toggle",
          "aria-expanded": h.value,
          title: h.value ? "收起地图更新提示" : "显示地图更新提示",
          "aria-label": "切换地图更新提示",
          onClick: Or
        }, " ! ", 8, Gee), h.value ? (U(), q("button", {
          key: 0,
          type: "button",
          class: "tavern-map-badge",
          title: "收起地图更新提示",
          onClick: Qt
        }, [_("span", null, Q(G.value), 1), _("strong", null, Q(L.value), 1)])) : ve("", !0)], 2)) : ve("", !0),
        r.value === "timeline" && w.value && p.value.length > 1 ? (U(), q("div", Wee, [_("button", {
          type: "button",
          onClick: fe[0] || (fe[0] = (ae) => Yn(-1))
        }, " 上一回合 "), _("button", {
          type: "button",
          onClick: fe[1] || (fe[1] = (ae) => Yn(1))
        }, " 下一回合 ")])) : ve("", !0),
        D.value.length ? (U(), q("div", {
          key: `removed-${n.value}`,
          class: "tavern-map-removed-stack"
        }, [(U(!0), q(Ge, null, ut(D.value, (ae) => (U(), q("span", {
          key: ae.id,
          style: Ir({ animationDelay: `${ae.delayMs}ms` })
        }, " - " + Q(ae.id), 5))), 128))])) : ve("", !0)
      ], 2)) : (U(), q("div", Vee, [...fe[4] || (fe[4] = [_("strong", null, "地图待初始化", -1), _("span", null, "当前会话已备好种子地图；剧情出现明确空间变化后，后台会激活并维护它。", -1)])])),
      e.compact ? ve("", !0) : (U(), q("footer", Kee, [
        _("span", null, Q(Z.value) + " patches", 1),
        z.value.length ? (U(), q("span", zee, Q(z.value[0]), 1)) : ve("", !0),
        z.value[1] ? (U(), q("span", Jee, Q(z.value[1]), 1)) : ve("", !0)
      ]))
    ], 2));
  }
}), Xee = Yee, Qee = { class: "tavern-memory-editor xb-editor" }, Zee = { class: "tavern-editor-head xb-editor-head" }, jee = { class: "tavern-editor-actions xb-editor-actions" }, ete = ["disabled"], tte = ["disabled"], nte = ["disabled"], rte = ["disabled"], ste = { class: "tavern-editor-body xb-editor-body" }, ote = ["data-markdown-signature", "innerHTML"], ite = ["value"], ate = {
  key: 2,
  class: "tavern-editor-empty"
}, lte = { class: "tavern-editor-foot xb-editor-foot" }, ute = { key: 0 }, cte = { key: 1 }, dte = { key: 2 }, fte = { key: 3 }, hte = /* @__PURE__ */ jt({
  __name: "TavernMemoryEditor",
  props: {
    documentAvailable: { type: Boolean },
    readOnly: { type: Boolean },
    dirty: { type: Boolean },
    mode: {},
    draft: {},
    previewHtml: {},
    previewSignature: {},
    status: {},
    hasSelectedFile: { type: Boolean },
    loadedPath: {},
    fileMeta: {}
  },
  emits: [
    "update:draft",
    "enter-edit",
    "preview",
    "discard",
    "save"
  ],
  setup(e, { emit: t }) {
    const n = t;
    function r(s) {
      n("update:draft", s.target.value);
    }
    return (s, o) => (U(), q("aside", Qee, [
      _("header", Zee, [_("div", jee, [
        _("button", {
          type: "button",
          class: Fe(["tavern-editor-mode-button", { active: e.mode === "edit" }]),
          disabled: !e.documentAvailable || e.readOnly,
          onClick: o[0] || (o[0] = (i) => s.$emit("enter-edit"))
        }, " 编辑 ", 10, ete),
        _("button", {
          type: "button",
          class: Fe(["tavern-editor-mode-button", { active: e.mode === "preview" }]),
          disabled: !e.documentAvailable,
          onClick: o[1] || (o[1] = (i) => s.$emit("preview"))
        }, " 阅读 ", 10, tte),
        e.dirty ? (U(), q("button", {
          key: 0,
          type: "button",
          class: "tavern-editor-discard-button",
          disabled: !e.documentAvailable,
          onClick: o[2] || (o[2] = (i) => s.$emit("discard"))
        }, " 放弃 ", 8, nte)) : ve("", !0),
        e.dirty ? (U(), q("button", {
          key: 1,
          type: "button",
          class: "tavern-editor-save-button",
          disabled: !e.hasSelectedFile || !e.dirty || e.readOnly,
          onClick: o[3] || (o[3] = (i) => s.$emit("save"))
        }, " 保存 ", 8, rte)) : ve("", !0)
      ])]),
      _("div", ste, [e.documentAvailable && e.mode === "preview" ? (U(), q("div", {
        key: 0,
        class: "tavern-editor-preview xb-tavern-markdown",
        "data-markdown-signature": e.previewSignature,
        innerHTML: e.previewHtml
      }, null, 8, ote)) : e.documentAvailable ? (U(), q("textarea", {
        key: 1,
        value: e.draft,
        spellcheck: "false",
        "data-memory-editor-textarea": "true",
        onInput: r
      }, null, 40, ite)) : (U(), q("div", ate, " 选择一份记忆档案。 "))]),
      _("footer", lte, [
        e.readOnly || e.dirty ? (U(), q("span", ute, Q(e.readOnly ? "只读" : "有未保存修改"), 1)) : ve("", !0),
        e.status ? (U(), q("span", cte, Q(e.status), 1)) : ve("", !0),
        !e.hasSelectedFile && e.loadedPath ? (U(), q("span", dte, "原档案暂不可用")) : ve("", !0),
        e.hasSelectedFile ? (U(), q("span", fte, Q(e.fileMeta), 1)) : ve("", !0)
      ])
    ]));
  }
}), pte = hte, mte = { class: "tavern-workspace-panel" }, gte = { class: "tavern-workspace-tabs" }, vte = {
  key: 0,
  class: "tavern-state-panel"
}, yte = { class: "tavern-current-state" }, _te = ["data-markdown-signature", "innerHTML"], bte = {
  key: 1,
  class: "tavern-current-state-empty"
}, wte = { class: "tavern-memory-directory" }, Ste = { class: "tavern-memory-directory-head" }, Tte = {
  key: 0,
  class: "memory-search tavern-memory-search"
}, Ete = {
  key: 1,
  class: "memory-directory-list xb-files"
}, Cte = { class: "memory-file-group-title" }, Ate = { class: "memory-file-tree" }, Ite = ["onClick"], xte = { class: "memory-file-main" }, kte = ["onClick"], Mte = { class: "memory-file-main" }, Pte = {
  key: 0,
  class: "muted compact"
}, Rte = {
  key: 2,
  class: "muted compact"
}, Nte = ["disabled"], $te = /* @__PURE__ */ jt({
  __name: "TavernWorkspacePanel",
  props: { mobileMemoryDirectoryOpen: { type: Boolean } },
  emits: [
    "close",
    "close-memory-directory",
    "toggle-memory-directory"
  ],
  setup(e, { emit: t }) {
    const n = t, r = DN(), s = LN(), { chatWorkspacePanel: o } = s, { activeMemoryFiles: i, discardMemoryDraft: a, enterMemoryEditMode: l, expandMemoryFileGroup: u, formatMemoryFileMeta: d, MEMORY_FILE_BATCH_SIZE: h, MEMORY_TURN_BATCH_SIZE: p, memoryDirectoryGroups: y, memoryEditorDirty: w, memoryEditorDocumentAvailable: E, memoryEditorDraft: C, memoryEditorLoadedPath: M, memoryEditorMode: k, memoryEditorReadOnly: R, memoryEditorStatus: D, memoryFileDisplayName: m, memoryFiles: b, memoryFileSearchText: v, markdownSignature: T, previewMemoryDraft: I, renderChatMarkdown: x, saveSelectedMemoryFile: P, selectedMemoryFileEntry: L, selectedMemoryFilePath: G, selectMemoryFile: z, stateMemoryFile: V } = r, { mapStateDocument: Z, mapStatePatches: O } = s, Y = ie(() => V.value || null), X = ie(() => String(Y.value?.content || "").trim()), j = ie(() => x(X.value)), re = ie(() => T(X.value));
    function pe() {
      n("close");
    }
    const { dragging: B, handleSheetHandlePointerCancel: N, handleSheetHandlePointerDown: H, handleSheetHandlePointerMove: le, handleSheetHandlePointerUp: Ie } = ON(pe);
    function De() {
      n("toggle-memory-directory");
    }
    function Be(it) {
      z(it), n("close-memory-directory");
    }
    return (it, Qe) => (U(), q("aside", mte, [
      _("button", {
        type: "button",
        class: Fe(["chat-mobile-sheet-handle", { "is-dragging": S(B) }]),
        title: "收起记忆面板",
        "aria-label": "收起记忆面板",
        onClick: pe,
        onPointercancel: Qe[0] || (Qe[0] = (...je) => S(N) && S(N)(...je)),
        onPointerdown: Qe[1] || (Qe[1] = (...je) => S(H) && S(H)(...je)),
        onPointermove: Qe[2] || (Qe[2] = (...je) => S(le) && S(le)(...je)),
        onPointerup: Qe[3] || (Qe[3] = (...je) => S(Ie) && S(Ie)(...je))
      }, null, 34),
      _("div", gte, [_("button", {
        type: "button",
        class: Fe({ active: S(o) === "state" }),
        onClick: Qe[4] || (Qe[4] = (je) => o.value = "state")
      }, " 状态 ", 2), _("button", {
        type: "button",
        class: Fe({ active: S(o) === "memory" }),
        onClick: Qe[5] || (Qe[5] = (je) => o.value = "memory")
      }, " 记忆 ", 2)]),
      S(o) === "state" ? (U(), q("section", vte, [Ft(Xee, {
        compact: "",
        document: S(Z),
        patches: S(O)
      }, null, 8, ["document", "patches"]), _("article", yte, [X.value ? (U(), q("div", {
        key: 0,
        class: "tavern-current-state-body xb-tavern-markdown",
        "data-markdown-signature": re.value,
        innerHTML: j.value
      }, null, 8, _te)) : (U(), q("div", bte, " 暂无当前状态。 "))])])) : (U(), q("section", {
        key: 1,
        class: Fe(["tavern-memory-workspace", { "is-memory-directory-open": e.mobileMemoryDirectoryOpen }])
      }, [
        _("aside", wte, [
          _("header", Ste, [Qe[8] || (Qe[8] = _("strong", null, "记忆文档", -1)), _("span", null, Q(S(i).length), 1)]),
          S(b).length ? (U(), q("label", Tte, [wn(_("input", {
            "onUpdate:modelValue": Qe[6] || (Qe[6] = (je) => /* @__PURE__ */ Xt(v) ? v.value = je : null),
            type: "search",
            placeholder: "检索档案"
          }, null, 512), [[ho, S(v)]])])) : ve("", !0),
          S(b).length ? (U(), q("div", Ete, [(U(!0), q(Ge, null, ut(S(y), (je) => (U(), q("div", {
            key: je.key,
            class: "memory-file-group"
          }, [_("div", Cte, [_("span", null, Q(je.title), 1), _("em", null, Q(je.totalCount), 1)]), _("div", Ate, [(U(!0), q(Ge, null, ut(je.files, (W) => (U(), q("button", {
            key: W.path,
            type: "button",
            class: Fe(["memory-file", {
              active: S(G) === W.path,
              stale: W.status === "stale"
            }]),
            onClick: (ee) => Be(W.path)
          }, [_("span", xte, Q(S(m)(W)), 1)], 10, Ite))), 128)), je.hiddenCount ? (U(), q("button", {
            key: 0,
            type: "button",
            class: "memory-file memory-file-more",
            onClick: (W) => S(u)(je.key)
          }, [_("span", Mte, "再显示 " + Q(Math.min(je.hiddenCount, je.key === "turns" ? S(p) : S(h))) + " 个", 1)], 8, kte)) : ve("", !0)])]))), 128)), S(v) && !S(y).length ? (U(), q("p", Pte, " 没有匹配的记忆档案。 ")) : ve("", !0)])) : (U(), q("p", Rte, " 还没有记忆档案。 "))
        ]),
        _("button", {
          type: "button",
          class: Fe(["tavern-mobile-memory-picker", { "is-open": e.mobileMemoryDirectoryOpen }]),
          disabled: !S(b).length,
          title: "选择记忆文档",
          "aria-label": "选择记忆文档",
          onClick: De
        }, [_("span", null, Q(S(L) ? S(m)(S(L)) : "选择记忆文档"), 1), Qe[9] || (Qe[9] = _("em", { "aria-hidden": "true" }, "⌄", -1))], 10, Nte),
        Ft(pte, {
          draft: S(C),
          "onUpdate:draft": Qe[7] || (Qe[7] = (je) => /* @__PURE__ */ Xt(C) ? C.value = je : null),
          "document-available": S(E),
          "read-only": S(R),
          dirty: S(w),
          mode: S(k),
          "preview-html": S(x)(S(C)),
          "preview-signature": S(T)(S(C)),
          status: S(D),
          "has-selected-file": !!S(L),
          "loaded-path": S(M),
          "file-meta": S(L) ? S(d)(S(L)) : "",
          onEnterEdit: S(l),
          onPreview: S(I),
          onDiscard: S(a),
          onSave: S(P)
        }, null, 8, [
          "draft",
          "document-available",
          "read-only",
          "dirty",
          "mode",
          "preview-html",
          "preview-signature",
          "status",
          "has-selected-file",
          "loaded-path",
          "file-meta",
          "onEnterEdit",
          "onPreview",
          "onDiscard",
          "onSave"
        ])
      ], 2))
    ]));
  }
}), Dte = $te, Lte = { class: "chat-mobile-topbar" }, Ote = { class: "chat-mobile-primary-row" }, Fte = {
  class: "chat-mobile-segment",
  role: "tablist",
  "aria-label": "聊天视图"
}, Bte = ["aria-selected"], Ute = ["aria-selected"], qte = { class: "chat-mobile-action-group" }, Hte = ["title", "aria-label"], Gte = {
  key: 0,
  class: "chat-mobile-svg",
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  "stroke-width": "1.8",
  "stroke-linecap": "round",
  "stroke-linejoin": "round",
  "aria-hidden": "true"
}, Wte = {
  key: 1,
  class: "chat-mobile-svg",
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  "stroke-width": "1.8",
  "stroke-linecap": "round",
  "stroke-linejoin": "round",
  "aria-hidden": "true"
}, Vte = { class: "chat-mobile-context-row" }, Kte = { class: "chat-flip-card" }, zte = /* @__PURE__ */ jt({
  __name: "TavernChatPage",
  setup(e) {
    const t = wd(), n = _m(), r = $N(), s = LN(), { activeView: o, chatFocus: i, homeThemeDark: a, openPromptInspector: l } = t, { chatAutoScroll: u, chatLayout: d, chatScrollRef: h, chatSidePanel: p, updateChatScrollButtons: y } = n, { managerAutoScroll: w, managerScrollRef: E, updateManagerScrollButtons: C } = r, { chatWorkspacePanel: M, saveSessionContract: k, selectedSessionId: R, sessionContract: D } = s;
    let m = null, b = null;
    const v = /* @__PURE__ */ se(!1), T = /* @__PURE__ */ se(!1), I = /* @__PURE__ */ se(""), x = /* @__PURE__ */ se(Wr(D.value)), P = /* @__PURE__ */ se("none"), L = /* @__PURE__ */ se(!1), G = ie(() => JSON.stringify(x.value) !== JSON.stringify(D.value));
    function z() {
      P.value = "none", L.value = !1;
    }
    function V(N) {
      P.value = P.value === N ? "none" : N;
    }
    function Z() {
      p.value = "sessions", V("directory");
    }
    function O(N) {
      const H = P.value === "workspace" && M.value === N;
      M.value = N, P.value = H ? "none" : "workspace", L.value = !1;
    }
    function Y(N) {
      const H = N.target instanceof HTMLElement ? N.target : null;
      if (H?.closest?.(".chat-mobile-sheet-handle")) {
        z();
        return;
      }
      H?.closest?.(".session-open, .memory-file:not(.memory-file-more)") && z();
    }
    function X() {
      L.value = !L.value;
    }
    function j() {
      I.value = "", x.value = Wr(D.value), v.value = !0;
    }
    function re() {
      I.value = "", v.value = !1;
    }
    function pe(N) {
      x.value = {
        ...x.value,
        [N]: !x.value[N]
      };
    }
    async function B() {
      if (!R.value) {
        I.value = "", v.value = !1;
        return;
      }
      I.value = "", T.value = !0;
      try {
        if (!await k(x.value)) throw new Error("contract_save_failed");
        v.value = !1;
      } catch {
        I.value = "契约保存失败，请重试。";
      } finally {
        T.value = !1;
      }
    }
    return yt(() => D.value, (N) => {
      v.value || (x.value = Wr(N));
    }, { deep: !0 }), yt(() => R.value, () => {
      I.value = "", v.value = !1, x.value = Wr(D.value);
    }), JI(() => {
      m = JA(h.value, {
        itemSelector: ".chat-bubble[data-chat-anchor-key], .chat-history-gate[data-chat-anchor-key]",
        datasetKey: "chatAnchorKey"
      }), b = JA(E.value, {
        itemSelector: "[data-manager-anchor-key]",
        datasetKey: "managerAnchorKey"
      });
    }), YI(() => {
      const N = o.value === "chat" && i.value === "chat" && u.value !== !1, H = o.value === "chat" && i.value === "manager" && w.value !== !1;
      YA(h.value, m, {
        itemSelector: ".chat-bubble[data-chat-anchor-key], .chat-history-gate[data-chat-anchor-key]",
        datasetKey: "chatAnchorKey"
      }, {
        forceBottom: N,
        defaultToBottom: N,
        preserveScrollTop: !N
      }), YA(E.value, b, {
        itemSelector: "[data-manager-anchor-key]",
        datasetKey: "managerAnchorKey"
      }, {
        forceBottom: H,
        defaultToBottom: H,
        preserveScrollTop: !H
      }), m = null, b = null, y(), C();
    }), (N, H) => S(o) === "chat" ? (U(), q("section", {
      key: 0,
      class: Fe(["tavern-chat xb-page", [
        `chat-focus-${S(i)}`,
        `chat-layout-${S(d)}`,
        {
          "is-mobile-directory-open": P.value === "directory",
          "is-mobile-workspace-open": P.value === "workspace"
        }
      ]])
    }, [
      Ft(bd, {
        "include-home": "",
        dark: S(a),
        onHome: H[0] || (H[0] = (le) => o.value = "home"),
        onToggleTheme: H[1] || (H[1] = (le) => a.value = !S(a))
      }, null, 8, ["dark"]),
      _("header", Lte, [_("div", Ote, [_("div", Fte, [
        H[10] || (H[10] = _("span", { "aria-hidden": "true" }, null, -1)),
        _("button", {
          type: "button",
          role: "tab",
          "aria-selected": S(i) === "chat",
          class: Fe({ active: S(i) === "chat" }),
          onClick: H[2] || (H[2] = (le) => {
            i.value = "chat", z();
          })
        }, " 角色 ", 10, Bte),
        _("button", {
          type: "button",
          role: "tab",
          "aria-selected": S(i) === "manager",
          class: Fe({ active: S(i) === "manager" }),
          onClick: H[3] || (H[3] = (le) => {
            i.value = "manager", z();
          })
        }, " 助手 ", 10, Ute)
      ]), _("div", qte, [_("button", {
        type: "button",
        class: "chat-mobile-icon-button chat-mobile-utility-button",
        title: "首页",
        "aria-label": "首页",
        onClick: H[4] || (H[4] = (le) => {
          o.value = "home", z();
        })
      }, [...H[11] || (H[11] = [_("svg", {
        class: "chat-mobile-svg",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        "stroke-width": "1.8",
        "stroke-linecap": "round",
        "stroke-linejoin": "round",
        "aria-hidden": "true"
      }, [
        _("path", { d: "M3 11.5 12 4l9 7.5" }),
        _("path", { d: "M5.5 10.5V20h13v-9.5" }),
        _("path", { d: "M9.5 20v-5.5h5V20" })
      ], -1)])]), _("button", {
        type: "button",
        class: "chat-mobile-icon-button chat-mobile-utility-button",
        title: S(a) ? "切换到白天" : "切换到夜间",
        "aria-label": S(a) ? "切换到白天" : "切换到夜间",
        onClick: H[5] || (H[5] = (le) => a.value = !S(a))
      }, [S(a) ? (U(), q("svg", Gte, [...H[12] || (H[12] = [kl('<circle cx="12" cy="12" r="4"></circle><path d="M12 2v2"></path><path d="M12 20v2"></path><path d="m4.93 4.93 1.41 1.41"></path><path d="m17.66 17.66 1.41 1.41"></path><path d="M2 12h2"></path><path d="M20 12h2"></path><path d="m6.34 17.66-1.41 1.41"></path><path d="m19.07 4.93-1.41 1.41"></path>', 9)])])) : (U(), q("svg", Wte, [...H[13] || (H[13] = [_("path", { d: "M20.2 14.5A7.3 7.3 0 0 1 9.5 3.8 8.7 8.7 0 1 0 20.2 14.5Z" }, null, -1)])]))], 8, Hte)])]), _("div", Vte, [
        _("button", {
          type: "button",
          class: Fe(["chat-mobile-context-button", { "is-active": P.value === "directory" }]),
          title: "会话",
          "aria-label": "会话",
          onClick: Z
        }, " 会话 ", 2),
        _("button", {
          type: "button",
          class: Fe(["chat-mobile-context-button", { "is-active": P.value === "workspace" }]),
          title: "状态",
          "aria-label": "状态",
          onClick: H[6] || (H[6] = (le) => O("state"))
        }, " 状态 ", 2),
        _("button", {
          type: "button",
          class: "chat-mobile-context-button",
          title: "契约",
          "aria-label": "契约",
          onClick: H[7] || (H[7] = (le) => {
            z(), j();
          })
        }, " 契约 "),
        _("button", {
          type: "button",
          class: "chat-mobile-context-button",
          title: "请求日志",
          "aria-label": "请求日志",
          onClick: H[8] || (H[8] = (le) => {
            z(), S(l)("history");
          })
        }, " 日志 ")
      ])]),
      _("button", {
        type: "button",
        class: "chat-mobile-sheet-scrim",
        title: "收起面板",
        "aria-label": "收起面板",
        onClick: z
      }),
      Ft(KQ, {
        onClick: Y,
        onClose: z
      }),
      _("section", { class: Fe(["chat-workbench", { "is-manager": S(i) === "manager" }]) }, [_("div", Kte, [Ft(ZZ, { onOpenContract: j }), Ft(dee, { onOpenContract: j })])], 2),
      Ft(Dte, {
        "mobile-memory-directory-open": L.value,
        onClose: z,
        onCloseMemoryDirectory: H[9] || (H[9] = (le) => L.value = !1),
        onToggleMemoryDirectory: X
      }, null, 8, ["mobile-memory-directory-open"]),
      v.value ? (U(), Zs(vQ, {
        key: 0,
        "draft-contract": x.value,
        dirty: G.value,
        saving: T.value,
        "can-save": !!S(R),
        "error-text": I.value,
        onClose: re,
        onToggle: pe,
        onSave: B
      }, null, 8, [
        "draft-contract",
        "dirty",
        "saving",
        "can-save",
        "error-text"
      ])) : ve("", !0)
    ], 2)) : ve("", !0);
  }
}), Jte = zte;
function Yte(e) {
  return (Array.isArray(e) ? e : []).map((t, n) => {
    const r = t && typeof t == "object" ? t : {};
    return {
      label: String(r.label || `思考 ${n + 1}`).trim() || `思考 ${n + 1}`,
      text: String(r.text || "").trim()
    };
  }).filter((t) => t.text);
}
function Xte(e) {
  const t = /* @__PURE__ */ se(Date.now()), n = ie(() => e.managerRuns.value[0] || null), r = ie(() => e.managerRuns.value.find((v) => d(v.status)) || n.value), s = ie(() => {
    const v = String(r.value?.id || "");
    return e.managerRuns.value.filter((T) => String(T.id || "") !== v).slice(0, e.visibleRunLimit);
  }), o = ie(() => {
    const v = r.value ? 1 : 0;
    return Math.max(0, e.managerRuns.value.length - v - s.value.length);
  }), i = ie(() => e.managerRuns.value.some((v) => ["queued", "running"].includes(v.status)));
  function a(v = 0) {
    const T = Math.max(0, t.value - Number(v || 0));
    return !v || T < 3e3 ? "刚刚" : l(T);
  }
  function l(v = 0) {
    const T = Math.floor(v / 1e3);
    if (T < 60) return `${T} 秒前`;
    const I = Math.floor(T / 60);
    return I < 60 ? `${I} 分钟前` : `${Math.floor(I / 60)} 小时前`;
  }
  function u(v = 0) {
    const T = Math.max(0, t.value - Number(v || 0));
    return !v || T < 3e3 ? "刚开始" : l(T).replace(/前$/, "");
  }
  function d(v = "") {
    return ["queued", "running"].includes(String(v || ""));
  }
  function h(v = "") {
    return {
      queued: "排队",
      running: "运行中",
      completed: "完成",
      failed: "失败",
      cancelled: "已取消",
      superseded: "已作废",
      rolled_back: "未采用"
    }[v] || v || "未知";
  }
  function p(v = "") {
    const T = typeof v == "string" ? v : String(v?.status || "");
    if (typeof v != "string" && T === "running") {
      const I = Number(v.updatedAt) || Number(v.createdAt) || 0, x = Math.max(0, t.value - I);
      if (x > 3e4) return "danger";
      if (x > 9e3) return "warn";
    }
    return ["failed", "rolled_back"].includes(T) ? "danger" : ["cancelled", "superseded"].includes(T) ? "muted" : ["queued", "running"].includes(T) ? "active" : T === "completed" ? "done" : "neutral";
  }
  function y(v) {
    return v.status === "queued" ? "等待后台模型" : v.status === "running" ? "后台模型运行中" : [String(v.provider || "").trim(), String(v.model || "").trim()].filter(Boolean).join(" / ") || "未记录模型信息";
  }
  function w(v) {
    const T = String(v.status || ""), I = Number(v.updatedAt) || Number(v.createdAt) || 0;
    if (T === "queued") return `等待开始 · 建立于 ${a(v.createdAt)}`;
    if (T === "running") {
      const x = Math.max(0, t.value - I), P = u(v.createdAt);
      return x <= 9e3 ? `还活着 · 已运行 ${P} · 正在等 API/工具返回` : x <= 3e4 ? `等待中 · 已运行 ${P} · 上次心跳 ${a(I)}` : `可能卡住 · 已运行 ${P} · ${a(I)}没有心跳`;
    }
    return [
      "completed",
      "failed",
      "cancelled",
      "superseded",
      "rolled_back"
    ].includes(T) ? `已结束 · ${a(I)}` : I ? `最后更新 ${a(I)}` : "";
  }
  function E(v) {
    const T = String(v.error || "").trim(), I = {
      manager_memory_tool_failed: "记忆工具返回失败，系统没有采用这次结果。",
      manager_memory_tool_required: "本轮没有完成必要的记忆维护，系统没有采用这次结果。",
      manager_aborted: "本次后台工作已停止，系统没有采用这次结果。",
      manager_source_messages_changed: "原文消息已经变化，系统没有采用这次结果。"
    };
    return /工具轮次达到上限/.test(T) ? `原因：${T} 系统没有采用这次结果。` : T && I[T] ? `原因：${I[T]}` : v.status === "rolled_back" ? "原因：本次结果已撤回，当前记忆和地图保持上一版。" : T ? `原因：${T}` : "";
  }
  function C(v) {
    const T = Number(v.assistantOrder), I = Number(v.userOrder);
    return [
      Number.isInteger(T) && T >= 0 ? `第 ${T + 1} 楼` : `第 ${Math.max(0, Number(v.turn) || 0)} 次维护`,
      Number.isInteger(I) && I >= 0 && Number.isInteger(T) && T >= 0 ? `原文 ${I + 1}-${T + 1} 楼` : "",
      v.trigger === "after_turn" ? "自动记忆" : v.trigger === "manager_chat" ? "助手聊天" : String(v.trigger || "")
    ].filter(Boolean).join(" · ");
  }
  function M(v) {
    const T = Array.isArray(v.changedFiles) ? v.changedFiles : [];
    return v.status === "queued" ? "记忆：等待开始" : v.status === "running" ? "记忆：正在整理" : v.status === "failed" ? T.length ? `记忆：已写入 ${T.length} 份档案，但本轮失败` : "记忆：未完成" : ["cancelled", "superseded"].includes(v.status) ? "记忆：已停止，未采用本轮结果" : v.status === "rolled_back" ? "记忆：未采用，已撤回本轮写入" : T.length ? `记忆：已更新 ${T.length} 份档案` : "记忆：没有写入文件";
  }
  function k(v) {
    const T = Array.isArray(v.changedStates) ? v.changedStates : [];
    return v.status === "queued" ? "地图：等待开始" : v.status === "running" ? "地图：正在判断本轮有没有空间变化" : v.status === "failed" ? T.length ? `地图：已写入 ${T.length} 份状态，但本轮失败` : "地图：未完成" : ["cancelled", "superseded"].includes(v.status) ? "地图：已停止，未采用本轮结果" : v.status === "rolled_back" ? "地图：未采用，已撤回本轮更新" : T.length ? `地图：已更新 ${T.length} 份状态` : "地图：本轮没有明确空间变化，未更新";
  }
  function R(v) {
    if (!v) return "";
    if (Array.isArray(v)) {
      const T = v.filter((x) => x && typeof x == "object" && x.ok === !1).length, I = v.filter((x) => x && typeof x == "object" && String(x.status || "") === "running").length;
      return I ? `工具调用 ${v.length} 次 · ${I} 个运行中` : T ? `工具调用 ${v.length} 次 · ${T} 次失败` : `工具调用 ${v.length} 次 · 全部成功`;
    }
    if (typeof v == "object") {
      const T = v, I = [
        "calls",
        "toolCalls",
        "steps",
        "trace"
      ].map((P) => Array.isArray(T[P]) ? T[P].length : 0).filter((P) => P > 0);
      if (I.length) return `工具调用 ${Math.max(...I)} 次`;
      const x = Object.keys(T).length;
      return x ? `工具记录 ${x} 项` : "";
    }
    return "有工具记录";
  }
  function D(v) {
    if (!Array.isArray(v)) return [];
    const T = /* @__PURE__ */ new Set(), I = /* @__PURE__ */ new Set();
    return v.map((x, P) => {
      const L = x && typeof x == "object" ? x : {}, G = String(L.name || "工具").trim() || "工具", z = String(L.status || "").trim(), V = L.ok !== !1, Z = Math.max(0, Number(L.elapsedMs) || (Number(L.startedAt) && Number(L.finishedAt) ? Number(L.finishedAt) - Number(L.startedAt) : 0)), O = Math.max(1, Number(L.round) || 1), Y = String(L.preface || "").trim(), X = `${O}
${Y}`, j = Y && !T.has(X) ? Y : "";
      Y && T.add(X);
      const re = Yte(L.thoughts), pe = `${O}
${JSON.stringify(re)}`, B = re.length && !I.has(pe) ? re : [];
      return re.length && I.add(pe), {
        id: String(L.id || `${G}-${P}`),
        round: O,
        name: G,
        status: z,
        ok: V,
        args: String(L.args || "").trim(),
        path: String(L.path || "").trim(),
        summary: String(L.summary || L.error || "").trim(),
        error: String(L.error || "").trim(),
        preface: j,
        thoughts: B,
        elapsedLabel: Z ? `${(Z / 1e3).toFixed(1)}s` : ""
      };
    });
  }
  function m(v) {
    return v.status === "running" ? "运行中" : v.ok === !1 ? "失败" : "已返回";
  }
  function b(v) {
    return v.status === "running" ? "is-running" : v.ok === !1 ? "is-error" : "is-resolved";
  }
  return {
    archivedManagerRuns: s,
    currentManagerWorkRun: r,
    formatRunActivityLine: w,
    formatRunIssueLine: E,
    formatRunInputLine: C,
    formatRunMapLine: k,
    formatRunMemoryLine: M,
    formatRunModelLine: y,
    hiddenManagerRunCount: o,
    isManagerRunLive: d,
    managerBusy: i,
    managerRunTone: p,
    managerStatusClock: t,
    managerStatusLabel: h,
    managerToolStatusLabel: m,
    managerToolTone: b,
    managerToolTraceItems: D,
    toolTraceSummary: R
  };
}
function Qte(e) {
  let t = 0;
  function n(y = "") {
    return y === "stale" ? "过期" : "可用";
  }
  function r(y) {
    return y ? "contentLength" in y && Number.isFinite(Number(y.contentLength)) ? Math.max(0, Number(y.contentLength) || 0) : "content" in y ? Math.max(0, String(y.content || "").length) : 0 : 0;
  }
  function s(y) {
    return `${n(y.status)} · ${r(y)} 字`;
  }
  function o(y) {
    const w = String(y?.content || "");
    e.memoryEditorLoadedPath.value = String(y?.path || ""), e.memoryEditorBaseContent.value = w, e.memoryEditorDraft.value = w, e.memoryEditorMode.value = "preview", e.memoryEditorStatus.value = "";
  }
  function i(y = !0) {
    t += 1, y && (e.selectedMemoryFileRecord.value = null);
  }
  async function a(y = "") {
    const w = String(e.selectedSessionId.value || "").trim(), E = String(y || "").trim(), C = ++t;
    if (!w || !E)
      return i(), null;
    const M = await Qa(w, E);
    return C !== t ? null : (e.selectedMemoryFileRecord.value = M, M);
  }
  function l(y = "") {
    const w = String(y || "").trim();
    !w || w === e.selectedMemoryFilePath.value || e.memoryEditorDirty.value && !window.confirm("当前记忆档案有未保存修改，切换后会放弃这份草稿。继续切换？") || (e.selectedMemoryFilePath.value = w);
  }
  async function u() {
    const y = e.selectedMemoryFileEntry.value;
    if (!(!e.selectedSessionId.value || !y)) {
      e.memoryEditorStatus.value = "保存中";
      try {
        await Py(e.selectedSessionId.value, y.path, e.memoryEditorDraft.value, { source: "user" }), await e.refreshRecords(e.selectedSessionId.value), e.memoryEditorLoadedPath.value = y.path, e.memoryEditorBaseContent.value = e.memoryEditorDraft.value, e.memoryEditorMode.value = "preview", e.memoryEditorStatus.value = "";
      } catch (w) {
        e.memoryEditorStatus.value = w instanceof Error ? w.message : String(w || "保存失败");
      }
    }
  }
  function d() {
    e.memoryEditorDocumentAvailable.value && (e.memoryEditorMode.value = "edit", Jt(() => {
      document.querySelector('[data-memory-editor-textarea="true"]')?.focus();
    }));
  }
  function h() {
    e.memoryEditorDocumentAvailable.value && (e.memoryEditorMode.value = "preview");
  }
  function p() {
    e.memoryEditorDocumentAvailable.value && (e.memoryEditorDraft.value = e.memoryEditorBaseContent.value, e.memoryEditorMode.value = "preview", e.memoryEditorStatus.value = "");
  }
  return {
    discardMemoryDraft: p,
    enterMemoryEditMode: d,
    formatMemoryFileMeta: s,
    invalidateMemoryFileRecordLoad: i,
    loadMemoryFileIntoEditor: o,
    loadSelectedMemoryFileRecord: a,
    memoryFileStatusLabel: n,
    previewMemoryDraft: h,
    saveSelectedMemoryFile: u,
    selectMemoryFile: l
  };
}
var Zte = {
  class: "prompt-inspector-modal",
  tabindex: "-1"
}, jte = { class: "prompt-inspector-head" }, ene = {
  class: "prompt-inspector-tabs",
  "aria-label": "API 请求视图"
}, tne = { class: "prompt-inspector-body" }, nne = { class: "prompt-inspector-view" }, rne = { class: "prompt-inspector-summary" }, sne = {
  key: 0,
  class: "prompt-request-json"
}, one = {
  key: 1,
  class: "prompt-empty-state"
}, ine = { class: "prompt-inspector-view" }, ane = { class: "prompt-simulate-panel" }, lne = ["value"], une = {
  key: 0,
  class: "muted compact"
}, cne = {
  key: 1,
  class: "error"
}, dne = {
  key: 2,
  class: "prompt-request-json"
}, fne = /* @__PURE__ */ jt({
  __name: "TavernRequestLogModal",
  props: {
    tab: {},
    lastRequestRawJson: {},
    lastRequestSnapshot: {},
    simulateInput: {},
    simulateStatus: {},
    simulateError: {},
    simulateJson: {}
  },
  emits: [
    "close",
    "update:tab",
    "update:simulateInput",
    "simulate"
  ],
  setup(e, { emit: t }) {
    const n = t;
    function r(s) {
      n("update:simulateInput", s.target.value);
    }
    return (s, o) => (U(), q("div", {
      class: "prompt-inspector-overlay",
      role: "dialog",
      "aria-modal": "true",
      onClick: o[4] || (o[4] = Pn((i) => s.$emit("close"), ["self"])),
      onKeydown: o[5] || (o[5] = _l((i) => s.$emit("close"), ["esc"]))
    }, [_("section", Zte, [
      _("header", jte, [o[6] || (o[6] = _("div", { class: "prompt-inspector-heading" }, [_("h2", null, "请求日志")], -1)), _("button", {
        type: "button",
        class: "prompt-inspector-close",
        "aria-label": "关闭日志",
        onClick: o[0] || (o[0] = (i) => s.$emit("close"))
      }, " 关闭 ")]),
      _("div", ene, [_("button", {
        type: "button",
        class: Fe({ active: e.tab === "history" }),
        onClick: o[1] || (o[1] = (i) => s.$emit("update:tab", "history"))
      }, " 上次调用 ", 2), _("button", {
        type: "button",
        class: Fe({ active: e.tab === "simulate" }),
        onClick: o[2] || (o[2] = (i) => s.$emit("update:tab", "simulate"))
      }, " 模拟发送 ", 2)]),
      _("div", tne, [wn(_("section", nne, [_("div", rne, [
        _("span", null, Q(e.lastRequestRawJson ? "有记录" : "暂无记录"), 1),
        _("span", null, Q(e.lastRequestSnapshot?.requestKind || "history"), 1),
        _("span", null, Q(e.lastRequestSnapshot?.providerLabel || e.lastRequestSnapshot?.provider || "未调用"), 1),
        _("span", null, Q(e.lastRequestSnapshot?.model || "未选择模型"), 1)
      ]), e.lastRequestRawJson ? (U(), q("pre", sne, Q(e.lastRequestRawJson), 1)) : (U(), q("p", one, " 暂无请求历史。 "))], 512), [[Zo, e.tab === "history"]]), wn(_("section", ine, [
        _("div", ane, [_("div", null, [o[7] || (o[7] = _("label", { for: "request-simulate-input" }, "模拟本轮发言", -1)), _("textarea", {
          id: "request-simulate-input",
          value: e.simulateInput,
          rows: "5",
          placeholder: "写一句要模拟发送的话",
          onInput: r
        }, null, 40, lne)]), _("button", {
          type: "button",
          onClick: o[3] || (o[3] = (i) => s.$emit("simulate"))
        }, " 生成请求 ")]),
        e.simulateStatus ? (U(), q("p", une, Q(e.simulateStatus), 1)) : ve("", !0),
        e.simulateError ? (U(), q("p", cne, Q(e.simulateError), 1)) : ve("", !0),
        e.simulateJson ? (U(), q("pre", dne, Q(e.simulateJson), 1)) : ve("", !0)
      ], 512), [[Zo, e.tab === "simulate"]])])
    ])], 32));
  }
}), hne = fne, pne = { class: "xb-sidebar settings-sidebar" }, mne = { class: "panel guide-card" }, gne = { class: "guide-steps" }, vne = ["onClick"], yne = { class: "guide-label-full" }, _ne = { class: "guide-label-mobile" }, bne = { key: 0 }, wne = /* @__PURE__ */ jt({
  __name: "TavernSettingsSidebar",
  props: {
    active: {},
    items: {}
  },
  emits: ["select"],
  setup(e, { emit: t }) {
    const n = t;
    return (r, s) => (U(), q("aside", pne, [_("div", mne, [s[0] || (s[0] = _("h2", null, "设置", -1)), _("div", gne, [(U(!0), q(Ge, null, ut(e.items, (o) => (U(), q("button", {
      key: o.key,
      type: "button",
      class: Fe(["guide-step", { active: e.active === o.key }]),
      onClick: (i) => n("select", o.key)
    }, [_("strong", null, [_("span", yne, Q(o.label), 1), _("span", _ne, Q(o.mobileLabel || o.label), 1)]), o.badge ? (U(), q("em", bne, Q(o.badge), 1)) : ve("", !0)], 10, vne))), 128))])])]));
  }
}), Sne = wne, Tne = { class: "panel step-panel api-workspace" }, Ene = /* @__PURE__ */ jt({
  __name: "TavernApiSettingsPanel",
  setup(e) {
    const { activeSettingsWorkspace: t, apiSettingsRootRef: n } = ca();
    function r(s) {
      n.value = s instanceof HTMLElement ? s : null;
    }
    return (s, o) => wn((U(), q("div", Tne, [o[0] || (o[0] = _("div", { class: "panel-head" }, [_("div", null, [_("h2", null, "API 配置")])], -1)), _("div", { class: "api-scroll-body" }, [_("div", {
      ref: r,
      class: "tavern-api-settings"
    })])], 512)), [[Zo, S(t) === "api"]]);
  }
}), Cne = Ene, Ane = { class: "panel step-panel base-settings-workspace" }, Ine = {
  key: 0,
  class: "preset-status-line"
}, xne = { class: "base-settings-scroll" }, kne = { class: "base-settings-section" }, Mne = ["value", "disabled"], Pne = {
  key: 0,
  value: ""
}, Rne = ["value"], Nne = { class: "base-user-detail" }, $ne = { class: "base-user-avatar" }, Dne = ["src", "alt"], Lne = { key: 1 }, One = { class: "base-user-copy" }, Fne = { class: "base-settings-section" }, Bne = { class: "base-setting-row" }, Une = ["value", "disabled"], qne = { class: "base-setting-row" }, Hne = ["value", "disabled"], Gne = /* @__PURE__ */ jt({
  __name: "TavernBaseSettingsPanel",
  setup(e) {
    const { activeSettingsWorkspace: t, baseSettingsLoading: n, baseSettingsSaving: r, baseSettingsStatus: s, currentTavernUser: o, displaySettings: i, shortText: a, switchingTavernUserId: l, switchTavernUser: u, tavernUsers: d, updateDisplaySettingsPatch: h } = ca(), p = ie(() => o.value?.name || "未选择 USER"), y = ie(() => o.value?.description || "聊天上下文会沿用这位 USER 的身份与头像。"), w = ie(() => o.value ? C(o.value) : "U"), E = ie(() => o.value?.id || d.value.find((D) => D.active)?.id || "");
    function C(D) {
      return (String(D.name || D.id || "U").trim().slice(0, 1) || "U").toUpperCase();
    }
    function M(D) {
      const m = String(D || "").trim();
      !m || m === E.value || u(m);
    }
    function k(D) {
      const m = Number(D);
      Number.isFinite(m) && h({ hiddenOutsideCount: m });
    }
    function R(D) {
      const m = Number(D);
      Number.isFinite(m) && h({ loadBatchSize: m });
    }
    return (D, m) => wn((U(), q("div", Ane, [
      m[8] || (m[8] = _("div", { class: "panel-head preset-page-head" }, [_("div", null, [_("h2", null, "基础设定")])], -1)),
      S(s) ? (U(), q("div", Ine, [_("span", null, Q(S(s)), 1)])) : ve("", !0),
      _("div", xne, [
        _("section", kne, [
          m[3] || (m[3] = _("h3", null, "USER身份切换", -1)),
          _("select", {
            class: "base-user-select",
            value: E.value,
            disabled: S(n) || !!S(l) || !S(d).length,
            onChange: m[0] || (m[0] = (b) => M(b.target.value))
          }, [S(d).length ? ve("", !0) : (U(), q("option", Pne, Q(S(n) ? "正在读取 USER 列表" : "没有可切换的 USER"), 1)), (U(!0), q(Ge, null, ut(S(d), (b) => (U(), q("option", {
            key: b.id,
            value: b.id
          }, Q(b.name), 9, Rne))), 128))], 40, Mne),
          _("div", Nne, [_("div", $ne, [S(o)?.avatarUrl ? (U(), q("img", {
            key: 0,
            src: S(o).avatarUrl,
            alt: p.value
          }, null, 8, Dne)) : (U(), q("span", Lne, Q(w.value), 1))]), _("div", One, [_("strong", null, Q(p.value), 1), _("p", null, Q(S(a)(y.value, 120)), 1)])])
        ]),
        _("section", Fne, [
          m[6] || (m[6] = _("h3", null, "楼层隐藏", -1)),
          _("div", Bne, [m[4] || (m[4] = _("div", null, [_("strong", null, "显示楼层"), _("span", null, "数字外楼层将隐藏")], -1)), _("input", {
            value: S(i).hiddenOutsideCount,
            type: "number",
            min: "1",
            max: "20",
            step: "1",
            disabled: S(r),
            onChange: m[1] || (m[1] = (b) => k(b.target.value))
          }, null, 40, Une)]),
          _("div", qne, [m[5] || (m[5] = _("div", null, [_("strong", null, "翻旧楼"), _("span", null, "每次加载楼数")], -1)), _("input", {
            value: S(i).loadBatchSize,
            type: "number",
            min: "5",
            max: "50",
            step: "5",
            disabled: S(r),
            onChange: m[2] || (m[2] = (b) => R(b.target.value))
          }, null, 40, Hne)])
        ]),
        m[7] || (m[7] = _("section", { class: "base-settings-section" }, [_("h3", null, "上下文管理"), _("div", { class: "base-context-block" }, [_("strong", null, "当前暂不支持调节"), _("p", null, "超过 20 楼时触发压缩并保留最近 10 楼；删楼后窗口内不足 5 楼时，回退窗口补到 10 楼；窗口外记忆由契约中的记忆系统处理。")])], -1))
      ])
    ], 512)), [[Zo, S(t) === "base"]]);
  }
}), Wne = Gne;
function qN(e) {
  const t = /* @__PURE__ */ se(!1);
  if (typeof window > "u" || typeof window.matchMedia != "function") return t;
  const n = window.matchMedia(e), r = () => {
    t.value = n.matches;
  };
  return r(), typeof n.addEventListener == "function" ? (n.addEventListener("change", r), vl(() => n.removeEventListener("change", r))) : (n.addListener(r), vl(() => n.removeListener(r))), t;
}
var Vne = { class: "panel-head preset-page-head" }, Kne = { class: "panel-pills" }, zne = {
  key: 0,
  class: "pill warning"
}, Jne = { class: "pill" }, Yne = { class: "preset-command-bar" }, Xne = { class: "preset-source-select" }, Qne = ["value"], Zne = {
  key: 0,
  value: ""
}, jne = ["value"], ere = { class: "settings-toolstrip" }, tre = ["disabled"], nre = ["disabled"], rre = {
  key: 0,
  class: "preset-status-line"
}, sre = { class: "preset-studio" }, ore = { class: "preset-edit-main prompt-sequence-panel" }, ire = { class: "preset-form-grid prompt-sequence-summary" }, are = ["value"], lre = ["value"], ure = { class: "archive-toolbar preset-filterbar" }, cre = { class: "archive-search" }, dre = { class: "prompt-manager-list" }, fre = {
  key: 0,
  class: "inline-empty-note"
}, hre = ["onClick"], pre = ["onClick"], mre = { class: "prompt-row-main" }, gre = { class: "prompt-row-actions" }, vre = ["title", "aria-label"], yre = ["onClick"], _re = ["disabled", "onClick"], bre = ["disabled", "onClick"], wre = [
  "checked",
  "disabled",
  "onChange"
], Sre = {
  key: 0,
  class: "preset-preview-panel prompt-detail-panel prompt-editor-panel"
}, Tre = { class: "preset-preview-head" }, Ere = {
  key: 0,
  class: "prompt-detail-form prompt-editor-form"
}, Cre = ["value"], Are = ["value"], Ire = { class: "preset-text-field" }, xre = ["value", "disabled"], kre = {
  key: 0,
  class: "muted compact"
}, Mre = {
  key: 1,
  class: "empty-note"
}, Pre = /* @__PURE__ */ jt({
  __name: "TavernChatPresetSettingsPanel",
  setup(e) {
    const { activePromptOrderLabel: t, activeSettingsWorkspace: n, canEditPromptOrder: r, chatPresetOptions: s, discardPresetChanges: o, filteredPromptEditorRows: i, hiddenPromptCount: a, movePromptRow: l, preset: u, presetDirty: d, presetRows: h, presetStatus: p, presetTotalChars: y, PROMPT_EDITOR_BATCH_SIZE: w, promptEditorRows: E, promptRoleDisplay: C, promptRowIndex: M, promptSearchText: k, promptVisibleLimit: R, saveCurrentPreset: D, selectChatPresetFromHost: m, selectedPresetSourceId: b, selectedPromptIdentifier: v, selectedPromptRow: T, togglePromptRow: I, updatePromptByIdentifier: x, visiblePromptEditorRows: P } = ca(), L = ie(() => String(b.value || u.value.promptManager?.name || u.value.name || "").trim()), G = /* @__PURE__ */ se(!1), z = qN("(max-width: 560px)"), V = ie(() => n.value === "chatPreset" && (!z.value || G.value));
    function Z(X) {
      v.value = X, G.value = !0;
    }
    function O() {
      G.value = !1;
    }
    function Y(X) {
      return X === "assistant" ? "A" : X === "user" ? "U" : "S";
    }
    return yt(n, (X) => {
      X !== "chatPreset" && (G.value = !1);
    }), (X, j) => wn((U(), q("div", { class: Fe(["panel step-panel preset-workspace", { "is-mobile-editor-open": G.value }]) }, [
      _("div", Vne, [j[9] || (j[9] = _("div", null, [_("h2", null, "聊天预设")], -1)), _("div", Kne, [S(d) ? (U(), q("span", zne, "未保存")) : ve("", !0), _("span", Jne, Q(S(h).length) + " 条 · " + Q(S(y)) + " 字", 1)])]),
      _("div", Yne, [_("label", Xne, [_("select", {
        value: L.value,
        onChange: j[0] || (j[0] = (re) => S(m)(re.target.value))
      }, [S(s).length ? ve("", !0) : (U(), q("option", Zne, " 没有可用预设 ")), (U(!0), q(Ge, null, ut(S(s), (re) => (U(), q("option", {
        key: re.name,
        value: re.name
      }, Q(re.label), 9, jne))), 128))], 40, Qne)]), _("div", ere, [_("button", {
        type: "button",
        class: "settings-icon-tool",
        title: "放弃修改",
        "aria-label": "放弃修改",
        disabled: !S(d),
        onClick: j[1] || (j[1] = (...re) => S(o) && S(o)(...re))
      }, [...j[10] || (j[10] = [_("svg", {
        viewBox: "0 0 24 24",
        "aria-hidden": "true"
      }, [_("path", { d: "M3 7v6h6" }), _("path", { d: "M21 17a8 8 0 0 0-13.7-5.7L3 15" })], -1)])], 8, tre), _("button", {
        type: "button",
        class: "settings-icon-tool",
        title: "保存",
        "aria-label": "保存",
        disabled: !S(r) || !S(d),
        onClick: j[2] || (j[2] = (...re) => S(D) && S(D)(...re))
      }, [...j[11] || (j[11] = [_("svg", {
        viewBox: "0 0 24 24",
        "aria-hidden": "true"
      }, [
        _("path", { d: "M5 21h14a1 1 0 0 0 1-1V7.5L16.5 4H5a1 1 0 0 0-1 1v15a1 1 0 0 0 1 1Z" }),
        _("path", { d: "M8 21v-7h8v7" }),
        _("path", { d: "M8 4v5h7" })
      ], -1)])], 8, nre)])]),
      S(p) ? (U(), q("div", rre, [_("span", null, Q(S(p)), 1)])) : ve("", !0),
      _("div", sre, [_("section", ore, [
        _("div", ire, [_("label", null, [j[12] || (j[12] = _("span", null, "当前来源", -1)), _("input", {
          value: S(u).promptManager?.name || "",
          readonly: ""
        }, null, 8, are)]), _("label", null, [j[13] || (j[13] = _("span", null, "顺序", -1)), _("input", {
          value: S(t),
          readonly: ""
        }, null, 8, lre)])]),
        _("div", ure, [_("label", cre, [j[14] || (j[14] = _("span", null, "检索条目", -1)), wn(_("input", {
          "onUpdate:modelValue": j[3] || (j[3] = (re) => /* @__PURE__ */ Xt(k) ? k.value = re : null),
          type: "search",
          placeholder: "按名称、消息身份或内容搜索"
        }, null, 512), [[ho, S(k)]])]), _("p", null, " 显示 " + Q(Math.min(S(i).length, S(R))) + " / " + Q(S(i).length) + "，共 " + Q(S(E).length) + " 条 ", 1)]),
        _("div", dre, [S(P).length ? ve("", !0) : (U(), q("div", fre, " 没有匹配的提示词条目。 ")), (U(!0), q(Ge, null, ut(S(P), (re) => (U(), q("div", {
          key: re.identifier,
          class: Fe(["prompt-manager-row", {
            selected: S(v) === re.identifier,
            disabled: !re.enabled,
            marker: re.marker
          }]),
          onClick: (pe) => v.value = re.identifier
        }, [
          _("button", {
            type: "button",
            class: "prompt-row-index",
            title: "选择",
            onClick: Pn((pe) => v.value = re.identifier, ["stop"])
          }, Q(S(M)(re.identifier) + 1), 9, pre),
          _("div", mre, [_("strong", null, Q(re.name), 1), _("small", null, Q(S(C)(re.role)), 1)]),
          _("div", gre, [
            _("span", {
              class: "prompt-role-badge",
              title: S(C)(re.role),
              "aria-label": S(C)(re.role)
            }, Q(Y(re.role)), 9, vre),
            _("button", {
              type: "button",
              class: "prompt-edit-button",
              title: "编辑",
              "aria-label": "编辑",
              onClick: Pn((pe) => Z(re.identifier), ["stop"])
            }, [...j[15] || (j[15] = [_("svg", {
              viewBox: "0 0 24 24",
              "aria-hidden": "true"
            }, [_("path", { d: "M12 20h9" }), _("path", { d: "M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z" })], -1)])], 8, yre),
            _("button", {
              type: "button",
              title: "上移",
              class: "prompt-move-button",
              disabled: !S(r) || !!S(k) || S(M)(re.identifier) === 0,
              onClick: Pn((pe) => S(l)(re.identifier, -1), ["stop"])
            }, " ↑ ", 8, _re),
            _("button", {
              type: "button",
              title: "下移",
              class: "prompt-move-button",
              disabled: !S(r) || !!S(k) || S(M)(re.identifier) === S(E).length - 1,
              onClick: Pn((pe) => S(l)(re.identifier, 1), ["stop"])
            }, " ↓ ", 8, bre),
            _("label", {
              class: "prompt-toggle",
              title: "启用",
              onClick: j[4] || (j[4] = Pn(() => {
              }, ["stop"]))
            }, [_("input", {
              type: "checkbox",
              checked: re.enabled,
              disabled: !S(r),
              onChange: (pe) => S(I)(re.identifier, pe.target.checked)
            }, null, 40, wre)])
          ])
        ], 10, hre))), 128))]),
        S(a) ? (U(), q("button", {
          key: 0,
          type: "button",
          class: "archive-load-more",
          onClick: j[5] || (j[5] = (re) => R.value += S(w))
        }, " 再显示 " + Q(Math.min(S(a), S(w))) + " 条 ", 1)) : ve("", !0)
      ]), V.value ? (U(), q("aside", Sre, [_("div", Tre, [
        _("strong", null, Q(S(T)?.name || "提示词条目"), 1),
        _("span", null, Q(S(C)(String(S(T)?.role || "system"))), 1),
        _("button", {
          type: "button",
          class: "prompt-editor-close",
          title: "关闭",
          "aria-label": "关闭",
          onClick: O
        }, [...j[16] || (j[16] = [_("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [_("path", { d: "M6 6l12 12" }), _("path", { d: "M18 6 6 18" })], -1)])])
      ]), S(T) ? (U(), q("div", Ere, [
        _("label", null, [j[17] || (j[17] = _("span", null, "名称", -1)), _("input", {
          value: S(T).name,
          onInput: j[6] || (j[6] = (re) => S(x)(S(T).identifier, { name: re.target.value }))
        }, null, 40, Cre)]),
        _("label", null, [j[19] || (j[19] = _("span", null, "消息身份", -1)), _("select", {
          value: S(T).role,
          onChange: j[7] || (j[7] = (re) => S(x)(S(T).identifier, { role: re.target.value }))
        }, [...j[18] || (j[18] = [
          _("option", { value: "system" }, "系统", -1),
          _("option", { value: "user" }, "用户", -1),
          _("option", { value: "assistant" }, "助手", -1)
        ])], 40, Are)]),
        _("label", Ire, [j[20] || (j[20] = _("span", null, "内容", -1)), _("textarea", {
          value: S(T).content,
          rows: "16",
          spellcheck: "false",
          disabled: S(T).marker,
          onInput: j[8] || (j[8] = (re) => S(x)(S(T).identifier, { content: re.target.value }))
        }, null, 40, xre)]),
        S(T).marker ? (U(), q("p", kre, " 这是酒馆顺序占位，不编辑正文。 ")) : ve("", !0)
      ])) : (U(), q("div", Mre, " 当前预设没有可编辑条目。 "))])) : ve("", !0)])
    ], 2)), [[Zo, S(n) === "chatPreset"]]);
  }
}), Rre = Pre, Nre = { class: "panel step-panel native-workspace" }, $re = { class: "panel-head preset-page-head" }, Dre = { class: "panel-pills panel-head-actions" }, Lre = { class: "pill" }, Ore = {
  key: 0,
  class: "preset-status-line"
}, Fre = { class: "worldbook-section worldbook-global-enable" }, Bre = { class: "worldbook-global-bar" }, Ure = ["aria-expanded", "disabled"], qre = { class: "worldbook-global-current" }, Hre = { key: 0 }, Gre = { key: 1 }, Wre = {
  key: 0,
  class: "worldbook-global-list"
}, Vre = [
  "checked",
  "disabled",
  "onChange"
], Kre = {
  key: 0,
  class: "worldbook-global-empty"
}, zre = { class: "worldbook-section worldbook-main-section" }, Jre = { class: "preset-command-bar worldbook-command-bar" }, Yre = { class: "preset-source-select worldbook-source-select" }, Xre = ["value"], Qre = {
  key: 0,
  value: ""
}, Zre = ["value"], jre = { class: "archive-search native-search worldbook-search-field" }, ese = { class: "native-settings-studio worldbook-overview-grid" }, tse = { class: "native-detail-panel worldbook-overview-detail worldbook-gateway-panel" }, nse = {
  key: 0,
  class: "worldbook-preview-surface"
}, rse = {
  key: 0,
  class: "worldbook-preview-status"
}, sse = { class: "worldbook-metric-grid" }, ose = {
  key: 0,
  class: "worldbook-entry-preview-list"
}, ise = ["open", "onToggle"], ase = { class: "worldbook-entry-title" }, lse = { class: "worldbook-entry-state" }, use = {
  key: 0,
  class: "worldbook-entry-body"
}, cse = { class: "worldbook-entry-editor-head" }, dse = { key: 0 }, fse = { key: 1 }, hse = ["disabled"], pse = { class: "worldbook-entry-editor-grid" }, mse = ["value"], gse = ["value"], vse = ["value"], yse = ["value"], _se = { class: "worldbook-entry-editor-lines" }, bse = ["value"], wse = { class: "worldbook-entry-editor-lines" }, Sse = ["value"], Tse = { class: "worldbook-entry-editor-lines" }, Ese = ["value"], Cse = { class: "worldbook-entry-editor-toggles" }, Ase = ["checked"], Ise = ["checked"], xse = ["disabled"], kse = { class: "worldbook-entry-actions" }, Mse = ["onClick"], Pse = { key: 0 }, Rse = {
  key: 0,
  class: "worldbook-entry-keys"
}, Nse = {
  key: 1,
  class: "worldbook-entry-content"
}, $se = {
  key: 1,
  class: "empty-note"
}, Dse = {
  key: 1,
  class: "empty-note"
}, Lse = /* @__PURE__ */ jt({
  __name: "TavernWorldbooksSettingsPanel",
  setup(e) {
    const { activeSettingsWorkspace: t, cancelWorldbookEntryEdit: n, globalWorldbookOptions: r, globalWorldbookSelected: s, globalWorldbookSaving: o, globalWorldbookStatus: i, hiddenWorldbookPreviewEntryCount: a, isEditingWorldbookEntry: l, linesFromList: u, listFromLines: d, saveWorldbookEntryDraft: h, selectedWorldbook: p, selectedWorldbookName: y, showMoreWorldbookPreviewEntries: w, startWorldbookEntryEdit: E, toggleGlobalWorldbook: C, updateWorldbookEntryDraftPatch: M, WORLDBOOK_PREVIEW_BATCH_SIZE: k, worldbookEntryDirty: R, worldbookEntryDraft: D, worldbookEntrySaving: m, worldbookEntryStatus: b, worldbookOptions: v, worldbookPreview: T, worldbookPreviewStatus: I, worldbookSearchText: x, worldbookStatus: P } = ca(), L = ie(() => {
      const Y = String(x.value || "").trim().toLocaleLowerCase(), X = String(y.value || "").trim(), j = v.value.find((pe) => pe.name === X), re = Y ? v.value.filter((pe) => pe.name.toLocaleLowerCase().includes(Y)) : v.value;
      return j && !re.some((pe) => pe.name === j.name) ? [j, ...re] : re;
    }), G = ym(), z = /* @__PURE__ */ se(!1), V = ie(() => {
      const Y = s.value.length;
      return Y ? Y === 1 ? s.value[0] || "1 本" : `${Y} 本` : "未启用";
    }), Z = ie(() => s.value.length ? s.value.join(" / ") : "没有全局生效的世界书");
    function O(Y) {
      const X = Y.uid !== void 0 && Y.uid !== null && String(Y.uid).trim() ? String(Y.uid).trim() : Y.order !== void 0 && Y.order !== null && String(Y.order).trim() ? String(Y.order).trim() : Y.name;
      return `worldbook:${y.value}:${X}`;
    }
    return yt([
      t,
      y,
      () => T.value?.name
    ], () => G.reset()), (Y, X) => wn((U(), q("div", Nre, [
      _("div", $re, [X[15] || (X[15] = _("div", null, [_("h2", null, "世界书")], -1)), _("div", Dre, [_("span", Lre, Q(S(v).length) + " 本", 1)])]),
      S(P) ? (U(), q("div", Ore, [_("span", null, Q(S(P)), 1)])) : ve("", !0),
      _("section", Fre, [
        X[16] || (X[16] = _("div", { class: "worldbook-section-head worldbook-global-enable-head" }, [_("div", null, [_("span", { class: "worldbook-section-kicker" }, "全局区"), _("h3", null, "已启用的世界书")])], -1)),
        _("div", Bre, [_("button", {
          type: "button",
          class: "worldbook-global-toggle",
          "aria-expanded": z.value,
          disabled: S(o),
          onClick: X[0] || (X[0] = (j) => z.value = !z.value)
        }, Q(V.value), 9, Ure), _("div", qre, [S(i) ? (U(), q("span", Hre, Q(S(i)), 1)) : (U(), q("span", Gre, Q(Z.value), 1))])]),
        z.value ? (U(), q("div", Wre, [(U(!0), q(Ge, null, ut(S(r), (j) => (U(), q("label", {
          key: j,
          class: "worldbook-global-option"
        }, [_("input", {
          type: "checkbox",
          checked: S(s).includes(j),
          disabled: S(o),
          onChange: (re) => S(C)(j, re.target.checked)
        }, null, 40, Vre), _("span", null, Q(j), 1)]))), 128)), S(r).length ? ve("", !0) : (U(), q("span", Kre, "没有世界书"))])) : ve("", !0)
      ]),
      _("section", zre, [
        X[31] || (X[31] = _("div", { class: "worldbook-section-head worldbook-main-head" }, [_("div", null, [_("span", { class: "worldbook-section-kicker" }, "操作区"), _("h3", null, "世界书操作")])], -1)),
        _("div", Jre, [_("label", Yre, [_("select", {
          value: S(y),
          onChange: X[1] || (X[1] = (j) => y.value = j.target.value)
        }, [L.value.length ? ve("", !0) : (U(), q("option", Qre, " 没有世界书 ")), (U(!0), q(Ge, null, ut(L.value, (j) => (U(), q("option", {
          key: j.name,
          value: j.name
        }, Q(j.name), 9, Zre))), 128))], 40, Xre)]), _("label", jre, [wn(_("input", {
          "onUpdate:modelValue": X[2] || (X[2] = (j) => /* @__PURE__ */ Xt(x) ? x.value = j : null),
          type: "search",
          "aria-label": "筛选世界书",
          placeholder: "筛选世界书"
        }, null, 512), [[ho, S(x)]])])]),
        _("div", ese, [_("div", tse, [S(p) ? (U(), q("div", nse, [S(I) ? (U(), q("div", rse, Q(S(I)), 1)) : S(T) && S(T).name === S(p).name ? (U(), q(Ge, { key: 1 }, [_("div", sse, [
          _("span", null, [_("strong", null, Q(S(T).entryCount), 1), X[17] || (X[17] = _("small", null, "条目", -1))]),
          _("span", null, [_("strong", null, Q(S(T).enabledCount), 1), X[18] || (X[18] = _("small", null, "启用", -1))]),
          _("span", null, [_("strong", null, Q(S(T).constantCount), 1), X[19] || (X[19] = _("small", null, "常驻", -1))]),
          _("span", null, [_("strong", null, Q(S(T).keywordCount), 1), X[20] || (X[20] = _("small", null, "关键词", -1))]),
          _("span", null, [_("strong", null, Q(S(T).totalChars), 1), X[21] || (X[21] = _("small", null, "正文字符", -1))])
        ]), S(T).entries.length ? (U(), q("div", ose, [(U(!0), q(Ge, null, ut(S(T).entries, (j) => (U(), q("details", {
          key: O(j),
          class: Fe(["worldbook-entry-preview", { disabled: !j.enabled }]),
          open: S(G).isOpen(O(j)),
          onToggle: (re) => S(G).setOpenFromEvent(O(j), re)
        }, [_("summary", null, [_("span", ase, [_("strong", null, Q(j.name), 1)]), _("span", lse, Q(j.constant ? "常驻" : j.enabled ? "启用" : "关闭"), 1)]), S(G).isOpen(O(j)) ? (U(), q("div", use, [S(l)(j) && S(D) ? (U(), q("form", {
          key: 0,
          class: "worldbook-entry-editor",
          onSubmit: X[13] || (X[13] = Pn((...re) => S(h) && S(h)(...re), ["prevent"]))
        }, [
          _("div", cse, [S(b) ? (U(), q("span", dse, Q(S(b)), 1)) : (U(), q("span", fse, "编辑条目")), _("button", {
            type: "button",
            class: "worldbook-row-open",
            disabled: S(m),
            onClick: X[3] || (X[3] = (...re) => S(n) && S(n)(...re))
          }, " 取消 ", 8, hse)]),
          _("div", pse, [
            _("label", null, [X[22] || (X[22] = _("span", null, "备注", -1)), _("input", {
              value: S(D).comment,
              type: "text",
              onInput: X[4] || (X[4] = (re) => S(M)({ comment: re.target.value }))
            }, null, 40, mse)]),
            _("label", null, [X[23] || (X[23] = _("span", null, "名称", -1)), _("input", {
              value: S(D).name,
              type: "text",
              onInput: X[5] || (X[5] = (re) => S(M)({ name: re.target.value }))
            }, null, 40, gse)]),
            _("label", null, [X[24] || (X[24] = _("span", null, "标题", -1)), _("input", {
              value: S(D).title,
              type: "text",
              onInput: X[6] || (X[6] = (re) => S(M)({ title: re.target.value }))
            }, null, 40, vse)]),
            _("label", null, [X[25] || (X[25] = _("span", null, "排序", -1)), _("input", {
              value: S(D).order,
              type: "number",
              step: "1",
              onInput: X[7] || (X[7] = (re) => S(M)({ order: Number(re.target.value) }))
            }, null, 40, yse)])
          ]),
          _("label", _se, [X[26] || (X[26] = _("span", null, "关键词", -1)), _("textarea", {
            value: S(u)(S(D).key),
            rows: "3",
            onInput: X[8] || (X[8] = (re) => S(M)({ key: S(d)(re.target.value) }))
          }, null, 40, bse)]),
          _("label", wse, [X[27] || (X[27] = _("span", null, "次级关键词", -1)), _("textarea", {
            value: S(u)(S(D).keysecondary.length ? S(D).keysecondary : S(D).secondary_keys),
            rows: "3",
            onInput: X[9] || (X[9] = (re) => S(M)({ keysecondary: S(d)(re.target.value) }))
          }, null, 40, Sse)]),
          _("label", Tse, [X[28] || (X[28] = _("span", null, "内容", -1)), _("textarea", {
            value: S(D).content,
            rows: "9",
            onInput: X[10] || (X[10] = (re) => S(M)({ content: re.target.value }))
          }, null, 40, Ese)]),
          _("div", Cse, [
            _("label", null, [_("input", {
              type: "checkbox",
              checked: !S(D).disable,
              onChange: X[11] || (X[11] = (re) => S(M)({
                disable: !re.target.checked,
                enabled: re.target.checked
              }))
            }, null, 40, Ase), X[29] || (X[29] = _("span", null, "启用", -1))]),
            _("label", null, [_("input", {
              type: "checkbox",
              checked: S(D).constant,
              onChange: X[12] || (X[12] = (re) => S(M)({ constant: re.target.checked }))
            }, null, 40, Ise), X[30] || (X[30] = _("span", null, "常驻", -1))]),
            _("button", {
              type: "submit",
              class: "primary-action",
              disabled: !S(R) || S(m)
            }, " 保存 ", 8, xse)
          ])
        ], 32)) : (U(), q(Ge, { key: 1 }, [
          _("div", kse, [_("button", {
            type: "button",
            class: "worldbook-row-open",
            onClick: (re) => S(E)(j)
          }, " 编辑 ", 8, Mse), S(b) && S(l)(j) ? (U(), q("span", Pse, Q(S(b)), 1)) : ve("", !0)]),
          j.keys.length || j.secondaryKeys.length ? (U(), q("p", Rse, Q([...j.keys, ...j.secondaryKeys].join(" / ")), 1)) : ve("", !0),
          j.contentPreview ? (U(), q("p", Nse, Q(j.contentPreview), 1)) : ve("", !0)
        ], 64))])) : ve("", !0)], 42, ise))), 128)), S(a) ? (U(), q("button", {
          key: 0,
          type: "button",
          class: "native-add-row worldbook-preview-more",
          onClick: X[14] || (X[14] = (...j) => S(w) && S(w)(...j))
        }, " 再显示 " + Q(Math.min(S(a), S(k))) + " 条 ", 1)) : ve("", !0)])) : (U(), q("div", $se, " 这本世界书暂无条目。 "))], 64)) : ve("", !0)])) : (U(), q("div", Dse, " 选择一本世界书后查看预览。 "))])])
      ])
    ], 512)), [[Zo, S(t) === "worldbooks"]]);
  }
}), Ose = Lse, Fse = { class: "panel-head preset-page-head" }, Bse = { class: "panel-pills" }, Use = {
  key: 0,
  class: "pill warning"
}, qse = { class: "pill" }, Hse = { class: "preset-command-bar regex-command-bar" }, Gse = { class: "preset-source-select regex-group-select" }, Wse = ["value"], Vse = { class: "archive-search native-search regex-search-field" }, Kse = { class: "settings-toolstrip" }, zse = ["aria-expanded"], Jse = {
  key: 0,
  class: "regex-create-popover"
}, Yse = ["onClick"], Xse = {
  key: 0,
  class: "preset-status-line"
}, Qse = { class: "native-settings-studio regex-studio" }, Zse = { class: "native-list-panel regex-group-panel regex-library-panel" }, jse = { class: "assistant-preset-nav-head" }, eoe = ["onClick", "onKeydown"], toe = { class: "regex-row-state" }, noe = { class: "regex-row-copy" }, roe = { class: "regex-row-actions" }, soe = ["onClick"], ooe = ["onClick"], ioe = ["onClick"], aoe = {
  key: 0,
  class: "inline-empty-note"
}, loe = {
  key: 0,
  class: "native-detail-panel regex-detail-panel"
}, uoe = { class: "preset-preview-head" }, coe = { class: "regex-editor-actions" }, doe = ["disabled"], foe = { class: "regex-editor-grid" }, hoe = { class: "regex-editor-section regex-pattern-section" }, poe = { class: "native-form-grid regex-editor-meta" }, moe = ["value"], goe = ["value"], voe = { class: "preset-text-field regex-main-field" }, yoe = ["value"], _oe = { class: "preset-text-field regex-support-field" }, boe = ["value"], woe = { class: "regex-editor-section regex-placement-section" }, Soe = { class: "native-check-row placement-row regex-editor-switches" }, Toe = ["checked", "onChange"], Eoe = { class: "regex-editor-section regex-ephemeral-section" }, Coe = { class: "native-check-row regex-editor-switches" }, Aoe = { class: "inline-check" }, Ioe = ["checked"], xoe = { class: "inline-check" }, koe = ["checked"], Moe = { class: "regex-editor-section regex-options-section" }, Poe = { class: "native-check-row regex-editor-switches" }, Roe = { class: "inline-check" }, Noe = ["checked"], $oe = { class: "inline-check" }, Doe = ["checked"], Loe = { class: "native-form-grid regex-advanced-grid" }, Ooe = ["value"], Foe = ["value"], Boe = ["value"], Uoe = {
  key: 1,
  class: "empty-note"
}, qoe = /* @__PURE__ */ jt({
  __name: "TavernRegexSettingsPanel",
  setup(e) {
    const { activeSettingsWorkspace: t, applyActiveRegexScript: n, createRegexScript: r, deleteRegexScript: s, expandRegexGroup: o, linesFromList: i, listFromLines: a, REGEX_GROUP_BATCH_SIZE: l, regexDirty: u, regexDraft: d, regexDraftTypeLabel: h, regexGroups: p, regexGroupsForDisplay: y, regexPlacementLabel: w, regexScriptRows: E, regexSearchText: C, regexStatus: M, saveCurrentRegexScript: k, selectedRegexKey: R, selectedRegexRow: D, selectRegexScript: m, toggleRegexPlacement: b, updateRegexPatch: v } = ca(), T = /* @__PURE__ */ se(!1), I = qN("(max-width: 560px)"), x = /* @__PURE__ */ se(""), P = /* @__PURE__ */ se(!1), L = ie(() => {
      const B = x.value;
      return B ? y.value.filter((N) => N.key === B) : y.value;
    }), G = ie(() => p.value.filter((B) => Number.isFinite(B.scriptType))), z = ie(() => t.value === "regex" && (!I.value || T.value));
    function V(B) {
      m(B), T.value = !0;
    }
    function Z(B) {
      r(B), P.value = !1, T.value = !0;
    }
    function O() {
      T.value = !1;
    }
    function Y() {
      n(D.value), O();
    }
    async function X() {
      await k(), u.value || O();
    }
    function j() {
      P.value = !P.value;
    }
    function re(B) {
      return B.key === "scoped" ? "保存到酒馆本体当前角色" : B.key === "preset" ? "保存到当前酒馆预设" : "保存到全局正则";
    }
    function pe(B, N) {
      B.key !== "Enter" && B.key !== " " || (B.preventDefault(), V(N));
    }
    return yt(t, (B) => {
      B !== "regex" && (T.value = !1, P.value = !1);
    }), (B, N) => wn((U(), q("div", { class: Fe(["panel step-panel native-workspace", { "is-mobile-editor-open": T.value }]) }, [
      _("div", Fse, [N[13] || (N[13] = _("div", null, [_("h2", null, "正则")], -1)), _("div", Bse, [S(u) ? (U(), q("span", Use, "未保存")) : ve("", !0), _("span", qse, Q(S(E).length) + " 条", 1)])]),
      _("div", Hse, [
        _("label", Gse, [wn(_("select", { "onUpdate:modelValue": N[0] || (N[0] = (H) => x.value = H) }, [N[14] || (N[14] = _("option", { value: "" }, " 全部正则 ", -1)), (U(!0), q(Ge, null, ut(S(y), (H) => (U(), q("option", {
          key: H.key,
          value: H.key
        }, Q(H.label), 9, Wse))), 128))], 512), [[wF, x.value]])]),
        _("label", Vse, [wn(_("input", {
          "onUpdate:modelValue": N[1] || (N[1] = (H) => /* @__PURE__ */ Xt(C) ? C.value = H : null),
          type: "search",
          "aria-label": "筛选正则",
          placeholder: "名称、匹配式或替换文本"
        }, null, 512), [[ho, S(C)]])]),
        _("div", Kse, [_("div", { class: Fe(["regex-create-menu", { open: P.value }]) }, [_("button", {
          type: "button",
          class: "settings-icon-tool regex-create-trigger",
          title: "新增正则",
          "aria-label": "新增正则",
          "aria-expanded": P.value ? "true" : "false",
          onClick: j
        }, [...N[15] || (N[15] = [_("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [_("path", { d: "M12 5v14" }), _("path", { d: "M5 12h14" })], -1)])], 8, zse), P.value ? (U(), q("div", Jse, [(U(!0), q(Ge, null, ut(G.value, (H) => (U(), q("button", {
          key: H.key,
          type: "button",
          onClick: (le) => Z(H)
        }, [_("strong", null, Q(H.label), 1), _("span", null, Q(re(H)), 1)], 8, Yse))), 128))])) : ve("", !0)], 2)])
      ]),
      S(M) ? (U(), q("div", Xse, [_("span", null, Q(S(M)), 1)])) : ve("", !0),
      _("div", Qse, [_("aside", Zse, [(U(!0), q(Ge, null, ut(L.value, (H) => (U(), q("div", {
        key: H.key,
        class: "regex-group-block"
      }, [
        _("div", jse, [_("strong", null, Q(H.label), 1), _("span", null, Q(H.allowed === !1 ? "未允许" : `${H.visibleRows.length} / ${H.filteredCount} / ${H.totalCount} 条`), 1)]),
        (U(!0), q(Ge, null, ut(H.visibleRows, (le) => (U(), q("div", {
          key: le.key,
          class: Fe(["native-list-row regex-list-row", {
            selected: S(R) === le.key,
            disabled: le.script.disabled
          }]),
          role: "button",
          tabindex: "0",
          onClick: (Ie) => V(le),
          onKeydown: (Ie) => pe(Ie, le)
        }, [
          _("span", toe, Q(le.script.disabled ? "停" : "用"), 1),
          _("span", noe, [_("strong", null, Q(le.script.scriptName || "未命名正则"), 1), _("small", null, Q(le.script.findRegex || "空匹配式"), 1)]),
          _("span", roe, [_("button", {
            type: "button",
            class: "regex-row-icon regex-row-edit-icon",
            title: "编辑",
            "aria-label": "编辑",
            onClick: Pn((Ie) => V(le), ["stop"])
          }, [...N[16] || (N[16] = [_("svg", {
            viewBox: "0 0 24 24",
            "aria-hidden": "true"
          }, [_("path", { d: "M12 20h9" }), _("path", { d: "M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z" })], -1)])], 8, soe), _("button", {
            type: "button",
            class: "regex-row-icon regex-row-delete-icon",
            title: "删除",
            "aria-label": "删除",
            onClick: Pn((Ie) => S(s)(le), ["stop"])
          }, [...N[17] || (N[17] = [kl('<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 6h18"></path><path d="M8 6V4h8v2"></path><path d="M19 6l-1 14a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1L5 6"></path><path d="M10 11v6"></path><path d="M14 11v6"></path></svg>', 1)])], 8, ooe)])
        ], 42, eoe))), 128)),
        H.hiddenCount ? (U(), q("button", {
          key: 0,
          type: "button",
          class: "native-add-row",
          onClick: (le) => S(o)(H.key)
        }, " 再显示 " + Q(Math.min(H.hiddenCount, S(l))) + " 条 ", 9, ioe)) : ve("", !0)
      ]))), 128)), S(C) && !L.value.length ? (U(), q("div", aoe, " 没有匹配的正则。 ")) : ve("", !0)]), z.value ? (U(), q("section", loe, [S(R) || S(d).scriptName ? (U(), q(Ge, { key: 0 }, [_("div", uoe, [
        _("strong", null, Q(S(d).scriptName || "新正则"), 1),
        _("span", null, Q(S(h)()), 1),
        _("div", coe, [_("button", {
          type: "button",
          class: "regex-editor-secondary",
          onClick: Y
        }, " 取消 "), _("button", {
          type: "button",
          class: "regex-editor-primary",
          disabled: !S(d).scriptName || !S(u),
          onClick: X
        }, " 保存 ", 8, doe)])
      ]), _("div", foe, [
        _("section", hoe, [
          N[22] || (N[22] = _("div", { class: "regex-section-title" }, " 查找与替换 ", -1)),
          _("div", poe, [_("label", null, [N[18] || (N[18] = _("span", null, "名称", -1)), _("input", {
            value: S(d).scriptName || "",
            onInput: N[2] || (N[2] = (H) => S(v)({ scriptName: H.target.value }))
          }, null, 40, moe)]), _("label", null, [N[19] || (N[19] = _("span", null, "匹配", -1)), _("input", {
            value: S(d).findRegex || "",
            spellcheck: "false",
            onInput: N[3] || (N[3] = (H) => S(v)({ findRegex: H.target.value }))
          }, null, 40, goe)])]),
          _("label", voe, [N[20] || (N[20] = _("span", null, "替换为", -1)), _("textarea", {
            value: S(d).replaceString || "",
            rows: "5",
            spellcheck: "false",
            onInput: N[4] || (N[4] = (H) => S(v)({ replaceString: H.target.value }))
          }, null, 40, yoe)]),
          _("label", _oe, [N[21] || (N[21] = _("span", null, "裁剪字符串", -1)), _("textarea", {
            value: S(i)(S(d).trimStrings),
            rows: "3",
            spellcheck: "false",
            onInput: N[5] || (N[5] = (H) => S(v)({ trimStrings: S(a)(H.target.value) }))
          }, null, 40, boe)])
        ]),
        _("section", woe, [N[23] || (N[23] = _("div", { class: "regex-section-title" }, " 作用范围 ", -1)), _("div", Soe, [(U(), q(Ge, null, ut([
          1,
          2,
          3,
          5,
          6
        ], (H) => _("label", {
          key: H,
          class: "inline-check"
        }, [_("input", {
          type: "checkbox",
          checked: (S(d).placement || []).includes(H),
          onChange: (le) => S(b)(H, le.target.checked)
        }, null, 40, Toe), _("span", null, Q(S(w)(H)), 1)])), 64))])]),
        _("section", Eoe, [N[26] || (N[26] = _("div", { class: "regex-section-title" }, " 表层替换 ", -1)), _("div", Coe, [_("label", Aoe, [_("input", {
          type: "checkbox",
          checked: S(d).markdownOnly === !0,
          onChange: N[6] || (N[6] = (H) => S(v)({ markdownOnly: H.target.checked }))
        }, null, 40, Ioe), N[24] || (N[24] = _("span", null, "只改显示", -1))]), _("label", xoe, [_("input", {
          type: "checkbox",
          checked: S(d).promptOnly === !0,
          onChange: N[7] || (N[7] = (H) => S(v)({ promptOnly: H.target.checked }))
        }, null, 40, koe), N[25] || (N[25] = _("span", null, "只改提示词", -1))])])]),
        _("section", Moe, [
          N[33] || (N[33] = _("div", { class: "regex-section-title" }, " 其他选项 ", -1)),
          _("div", Poe, [_("label", Roe, [_("input", {
            type: "checkbox",
            checked: S(d).disabled === !0,
            onChange: N[8] || (N[8] = (H) => S(v)({ disabled: H.target.checked }))
          }, null, 40, Noe), N[27] || (N[27] = _("span", null, "停用", -1))]), _("label", $oe, [_("input", {
            type: "checkbox",
            checked: S(d).runOnEdit === !0,
            onChange: N[9] || (N[9] = (H) => S(v)({ runOnEdit: H.target.checked }))
          }, null, 40, Doe), N[28] || (N[28] = _("span", null, "编辑时执行", -1))])]),
          _("div", Loe, [
            _("label", null, [N[30] || (N[30] = _("span", null, "查找宏", -1)), _("select", {
              value: S(d).substituteRegex ?? 0,
              onChange: N[10] || (N[10] = (H) => S(v)({ substituteRegex: Number(H.target.value) }))
            }, [...N[29] || (N[29] = [
              _("option", { value: 0 }, "不替换", -1),
              _("option", { value: 1 }, "原样替换", -1),
              _("option", { value: 2 }, "转义替换", -1)
            ])], 40, Ooe)]),
            _("label", null, [N[31] || (N[31] = _("span", null, "最小深度", -1)), _("input", {
              type: "number",
              value: S(d).minDepth ?? "",
              onInput: N[11] || (N[11] = (H) => S(v)({ minDepth: H.target.value === "" ? null : Number(H.target.value) }))
            }, null, 40, Foe)]),
            _("label", null, [N[32] || (N[32] = _("span", null, "最大深度", -1)), _("input", {
              type: "number",
              value: S(d).maxDepth ?? "",
              onInput: N[12] || (N[12] = (H) => S(v)({ maxDepth: H.target.value === "" ? null : Number(H.target.value) }))
            }, null, 40, Boe)])
          ])
        ])
      ])], 64)) : (U(), q("div", Uoe, " 选择一条正则，或在左侧新建。 "))])) : ve("", !0)])
    ], 2)), [[Zo, S(t) === "regex"]]);
  }
}), Hoe = qoe, Goe = { class: "panel step-panel preset-workspace" }, Woe = { class: "panel-head preset-page-head" }, Voe = { class: "panel-pills" }, Koe = {
  key: 0,
  class: "pill warning"
}, zoe = { class: "preset-command-bar assistant-preset-command-bar" }, Joe = { class: "assistant-preset-picker" }, Yoe = ["value"], Xoe = {
  key: 0,
  value: ""
}, Qoe = ["value"], Zoe = { class: "assistant-preset-toolstrip" }, joe = ["disabled"], eie = ["disabled"], tie = ["disabled"], nie = {
  key: 0,
  class: "preset-status-line"
}, rie = { class: "assistant-preset-studio" }, sie = { class: "assistant-preset-item-list archive-item-list" }, oie = ["onClick"], iie = { class: "assistant-preset-detail-panel archive-editor-panel" }, aie = { class: "assistant-preset-line-head" }, lie = {
  key: 0,
  class: "assistant-preset-detail-fields"
}, uie = ["value"], cie = {
  key: 1,
  type: "button",
  class: "assistant-preset-empty-add",
  disabled: ""
}, die = /* @__PURE__ */ jt({
  __name: "TavernAssistantPresetSettingsPanel",
  setup(e) {
    const { activeAssistantPresetId: t, activeSettingsWorkspace: n, assistantPreset: r, assistantPresetDirty: s, assistantPresetItems: o, assistantPresets: i, assistantPresetStatus: a, createAssistantPreset: l, deleteCurrentAssistantPreset: u, importAssistantPreset: d, saveCurrentAssistantPreset: h, selectAssistantPreset: p, selectAssistantPresetItem: y, selectedAssistantPresetItem: w, shortText: E, updateAssistantPresetPatch: C, updateSelectedAssistantPresetItem: M } = ca(), k = ie(() => String(t.value || r.value.id || "").trim()), R = ie(() => i.value.find((I) => I.id === k.value) || null), D = /* @__PURE__ */ se(null);
    function m() {
      const I = String(r.value.name || "").trim() || "助手预设", x = window.prompt("输入预设名称：", I);
      if (x === null) return;
      const P = String(x || "").trim();
      !P || P === I || C({ name: P });
    }
    function b() {
      D.value?.click();
    }
    async function v(I) {
      const x = I.target, P = x?.files?.[0];
      if (P)
        try {
          const L = await P.text();
          await d(JSON.parse(L));
        } catch (L) {
          const G = L instanceof Error ? L.message : String(L || "导入失败");
          window.alert(G || "导入失败");
        } finally {
          x && (x.value = "");
        }
    }
    function T() {
      const I = String(r.value.name || "assistant-preset").trim() || "assistant-preset", x = {
        kind: "littlewhitebox-assistant-preset",
        version: 1,
        exportedAt: (/* @__PURE__ */ new Date()).toISOString(),
        preset: r.value
      }, P = new Blob([JSON.stringify(x, null, 2)], { type: "application/json;charset=utf-8" }), L = URL.createObjectURL(P), G = document.createElement("a");
      G.href = L, G.download = `${I.replace(/[\\/:*?"<>|]+/g, "-").replace(/\s+/g, " ").trim() || "assistant-preset"}.json`, document.body.appendChild(G), G.click(), G.remove(), URL.revokeObjectURL(L);
    }
    return (I, x) => wn((U(), q("div", Goe, [
      _("div", Woe, [x[5] || (x[5] = _("div", null, [_("h2", null, "助手预设")], -1)), _("div", Voe, [S(s) ? (U(), q("span", Koe, "未保存")) : ve("", !0)])]),
      _("div", zoe, [_("label", Joe, [_("select", {
        value: k.value,
        onChange: x[0] || (x[0] = (P) => S(p)(P.target.value))
      }, [S(i).length ? ve("", !0) : (U(), q("option", Xoe, " 没有可用助手预设 ")), (U(!0), q(Ge, null, ut(S(i), (P) => (U(), q("option", {
        key: P.id,
        value: P.id
      }, Q(P.name) + Q(P.isBuiltIn ? " · 内置" : ""), 9, Qoe))), 128))], 40, Yoe)]), _("div", Zoe, [
        _("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "改名",
          "aria-label": "改名",
          onClick: m
        }, [...x[6] || (x[6] = [_("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [_("path", { d: "M12 20h9" }), _("path", { d: "M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z" })], -1)])]),
        _("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "保存",
          "aria-label": "保存",
          disabled: !S(s),
          onClick: x[1] || (x[1] = (...P) => S(h) && S(h)(...P))
        }, [...x[7] || (x[7] = [_("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [
          _("path", { d: "M5 21h14a1 1 0 0 0 1-1V7.5L16.5 4H5a1 1 0 0 0-1 1v15a1 1 0 0 0 1 1Z" }),
          _("path", { d: "M8 21v-7h8v7" }),
          _("path", { d: "M8 4v5h7" })
        ], -1)])], 8, joe),
        _("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "删除",
          "aria-label": "删除",
          disabled: !R.value || R.value.id === S("littlewhitebox-assistant-default"),
          onClick: x[2] || (x[2] = (...P) => S(u) && S(u)(...P))
        }, [...x[8] || (x[8] = [kl('<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 6h18"></path><path d="M8 6V4h8v2"></path><path d="M19 6l-1 14a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1L5 6"></path><path d="M10 11v6"></path><path d="M14 11v6"></path></svg>', 1)])], 8, eie),
        _("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "新增",
          "aria-label": "新增",
          onClick: x[3] || (x[3] = (...P) => S(l) && S(l)(...P))
        }, [...x[9] || (x[9] = [_("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [_("path", { d: "M12 5v14" }), _("path", { d: "M5 12h14" })], -1)])]),
        _("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "导入",
          "aria-label": "导入",
          onClick: b
        }, [...x[10] || (x[10] = [_("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [
          _("path", { d: "M12 4v11" }),
          _("path", { d: "m7 11 5 5 5-5" }),
          _("path", { d: "M4 20h16" })
        ], -1)])]),
        _("button", {
          type: "button",
          class: "assistant-preset-tool icon-button",
          title: "导出",
          "aria-label": "导出",
          disabled: !k.value,
          onClick: T
        }, [...x[11] || (x[11] = [_("svg", {
          viewBox: "0 0 24 24",
          "aria-hidden": "true"
        }, [
          _("path", { d: "M12 20V9" }),
          _("path", { d: "m17 13-5-5-5 5" }),
          _("path", { d: "M4 20h16" })
        ], -1)])], 8, tie),
        _("input", {
          ref_key: "importInputRef",
          ref: D,
          class: "assistant-preset-import-input",
          type: "file",
          accept: "application/json,.json",
          onChange: v
        }, null, 544)
      ])]),
      S(a) ? (U(), q("div", nie, [_("span", null, Q(S(a)), 1)])) : ve("", !0),
      _("div", rie, [_("aside", sie, [x[12] || (x[12] = _("div", { class: "assistant-preset-nav-head" }, [_("strong", null, "记忆档案")], -1)), (U(!0), q(Ge, null, ut(S(o), (P) => (U(), q("button", {
        key: P.id,
        type: "button",
        class: Fe(["assistant-preset-nav-row", { selected: S(w)?.id === P.id }]),
        onClick: (L) => S(y)(P.id)
      }, [_("strong", null, Q(P.label), 1), _("small", null, Q(S(E)(P.content || "未填写职责。", 54)), 1)], 10, oie))), 128))]), _("section", iie, [_("div", aie, [_("div", null, [_("strong", null, Q(S(w)?.label || "记忆档案"), 1)])]), S(w) ? (U(), q("div", lie, [_("label", null, [x[13] || (x[13] = _("span", null, "职责说明", -1)), _("textarea", {
        value: S(w).content,
        rows: "12",
        placeholder: "写这个记忆档案应该维护什么、保留什么、避免什么。",
        onInput: x[4] || (x[4] = (P) => S(M)(P.target.value))
      }, null, 40, uie)])])) : (U(), q("button", cie, " 没有可编辑条目 "))])])
    ], 512)), [[Zo, S(n) === "assistantPreset"]]);
  }
}), fie = die, hie = { class: "xb-main" }, pie = /* @__PURE__ */ jt({
  __name: "TavernSettingsPage",
  setup(e) {
    const t = ca(), n = wd(), { activeSettingsWorkspace: r, selectSettingsWorkspace: s, settingsNavItems: o } = t, { activeView: i, homeThemeDark: a } = n;
    return (l, u) => S(i) === "settings" ? (U(), q("section", {
      key: 0,
      class: Fe(["xb-layout xb-page settings-layout", `is-${S(r)}-workspace`])
    }, [
      Ft(bd, {
        "include-home": "",
        dark: S(a),
        onHome: u[0] || (u[0] = (d) => i.value = "home"),
        onToggleTheme: u[1] || (u[1] = (d) => a.value = !S(a))
      }, null, 8, ["dark"]),
      Ft(Sne, {
        active: S(r),
        items: S(o),
        onSelect: S(s)
      }, null, 8, [
        "active",
        "items",
        "onSelect"
      ]),
      _("section", hie, [
        Ft(Cne),
        Ft(Rre),
        Ft(Ose),
        Ft(Hoe),
        Ft(fie),
        Ft(Wne)
      ])
    ], 2)) : ve("", !0);
  }
}), mie = pie, gie = { chat: { exclude: [
  "embedding",
  "embed",
  "rerank",
  "reranker",
  "tts",
  "speech",
  "audio",
  "whisper",
  "transcription",
  "stt",
  "image",
  "sdxl",
  "flux",
  "moderation"
] } }, vie = Object.freeze([
  "claude-opus-4-7",
  "claude-opus-4-6",
  "claude-opus-4-5",
  "claude-opus-4-5-20251101",
  "claude-sonnet-4-6",
  "claude-sonnet-4-5",
  "claude-sonnet-4-5-20250929",
  "claude-opus-4-1",
  "claude-opus-4-1-20250805",
  "claude-opus-4-0",
  "claude-opus-4-20250514",
  "claude-sonnet-4-0",
  "claude-sonnet-4-20250514"
]);
function ws(e, t, n = "") {
  if (e.replaceChildren(), n) {
    const r = document.createElement("option");
    r.value = "", r.textContent = n, e.appendChild(r);
  }
  t.forEach((r) => {
    const s = document.createElement("option");
    s.value = r.value, s.textContent = r.label, e.appendChild(s);
  });
}
function Jc(e = []) {
  const t = [...new Set(e.filter(Boolean).map((s) => String(s).trim()).filter(Boolean))], n = gie.chat, r = t.filter((s) => {
    const o = s.toLowerCase();
    return !n.exclude.some((i) => o.includes(i));
  });
  return r.length ? r : t;
}
function Uf(e = "") {
  return e === "delegate" ? "delegate" : "main";
}
function xl(e) {
  return String(e || "").trim().replace(/\/+$/, "");
}
function yie(e = "") {
  return e === "sillytavern-openai-compatible" || e === "sillytavern-claude" || e === "sillytavern-google";
}
function Oa(e = "") {
  return e === "openai-compatible" || e === "sillytavern-openai-compatible";
}
function U_(e = "") {
  return e === "anthropic" || e === "sillytavern-claude";
}
function _ie(e = "") {
  return e === "sillytavern-claude" ? lb : e === "sillytavern-google" ? ub : Ml;
}
function Yc(e = []) {
  return [...new Set(e.filter(Boolean).map((t) => String(t).trim()).filter(Boolean))];
}
function bie(e) {
  const t = xl(e);
  if (!t) return [];
  if (t.endsWith("/v1")) {
    const n = t.slice(0, -3);
    return Yc([
      `${t}/models`,
      `${n}/v1/models`,
      `${n}/models`
    ]);
  }
  return Yc([`${t}/v1/models`, `${t}/models`]);
}
function HN(e) {
  const t = xl(e);
  if (!t) return [];
  if (t.endsWith("/v1")) {
    const n = t.slice(0, -3);
    return Yc([
      `${t}/models`,
      `${n}/v1/models`,
      `${n}/models`
    ]);
  }
  return Yc([`${t}/v1/models`, `${t}/models`]);
}
function wie(e, t) {
  const n = xl(e);
  if (!n) return [];
  const r = n.endsWith("/v1beta") ? n.slice(0, -7) : n;
  return Yc([
    `${n}/models?key=${encodeURIComponent(t)}`,
    `${n}/models`,
    `${r}/v1beta/models?key=${encodeURIComponent(t)}`,
    `${r}/v1beta/models`,
    `${r}/models?key=${encodeURIComponent(t)}`,
    `${r}/models`
  ]);
}
function Sie(e, t) {
  const n = [
    e?.error?.message,
    e?.message,
    e?.detail,
    e?.details,
    e?.error
  ].find((r) => typeof r == "string" && r.trim());
  return n ? n.trim() : String(t || "").trim().slice(0, 160);
}
async function Tie(e, t = {}) {
  const n = await fetch(e, t), r = await n.text();
  let s = null, o = null;
  try {
    s = r ? JSON.parse(r) : {};
  } catch (i) {
    o = i;
  }
  return {
    ok: n.ok,
    status: n.status,
    url: e,
    data: s,
    rawText: r,
    parseError: o,
    errorSnippet: Sie(s, r)
  };
}
function Eie(e) {
  return Jc((e?.data || []).map((t) => String(t?.id || "").trim()).filter(Boolean));
}
function GN(e) {
  return Jc((e?.data || []).map((t) => String(t?.id || "").trim()).filter(Boolean));
}
function Cie(e) {
  return Jc((e?.models || e?.data || []).map((t) => String(t?.id || t?.name || "")).map((t) => t.split("/").pop() || "").filter(Boolean));
}
async function bh({ urls: e, requestOptionsList: t, extractModels: n, providerLabel: r }) {
  let s = null;
  for (const o of e) for (const i of t) {
    const a = await Tie(o, i);
    if (!a.ok) {
      s = a;
      continue;
    }
    if (a.parseError) {
      s = {
        ...a,
        errorSnippet: "返回的不是 JSON"
      };
      continue;
    }
    const l = n(a.data);
    if (l.length) return l;
    s = {
      ...a,
      errorSnippet: "返回成功，但模型列表为空"
    };
  }
  if (s) {
    const o = s.url ? ` (${s.url})` : "", i = s.errorSnippet ? `：${s.errorSnippet}` : "";
    throw new Error(`${r} 拉取模型失败：${s.status || "unknown"}${i}${o}`);
  }
  throw new Error(`${r} 拉取模型失败：未获取到模型列表。`);
}
async function Aie(e) {
  const t = String(e.apiKey || "").trim(), n = xl(e.baseUrl || ""), r = xl(n || Ex.claude);
  if (t && r) try {
    return await bh({
      urls: HN(r),
      requestOptionsList: [{ headers: {
        "x-api-key": t,
        "anthropic-version": "2023-06-01",
        Accept: "application/json"
      } }],
      extractModels: GN,
      providerLabel: "Anthropic"
    });
  } catch (s) {
    if (n) throw s;
  }
  return [...vie];
}
async function ZA(e) {
  const t = e.provider, n = xl(e.baseUrl || ""), r = String(e.apiKey || "").trim();
  if (t === "sillytavern-claude") return Jc(await Aie(e));
  if (yie(t)) return Jc(await $2(e, _ie(t)));
  if (!r) throw new Error("请先填写 API Key。");
  if (!n) throw new Error("请先填写 Base URL。");
  return t === "google" ? await bh({
    urls: wie(n, r),
    requestOptionsList: [
      { headers: {
        Accept: "application/json",
        "x-goog-api-key": r
      } },
      { headers: {
        Accept: "application/json",
        Authorization: `Bearer ${r}`
      } },
      { headers: { Accept: "application/json" } }
    ],
    extractModels: Cie,
    providerLabel: "Google AI"
  }) : U_(t) ? await bh({
    urls: HN(n),
    requestOptionsList: [{ headers: {
      "x-api-key": r,
      "anthropic-version": "2023-06-01",
      Accept: "application/json"
    } }],
    extractModels: GN,
    providerLabel: "Anthropic"
  }) : await bh({
    urls: bie(n),
    requestOptionsList: [{ headers: {
      Authorization: `Bearer ${r}`,
      Accept: "application/json"
    } }],
    extractModels: Eie,
    providerLabel: t === "openai-responses" ? "OpenAI Responses" : "OpenAI-Compatible"
  });
}
function Iie(e) {
  return e instanceof Error ? e.message : String(e || "unknown_error");
}
function xie(e = {}) {
  const { state: t, render: n, showToast: r, createRequestId: s = (B = "req") => `${B}-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`, saveConfig: o, describeError: i = Iie, getRuntimeSummaryText: a } = e;
  function l() {
    t.configFormSyncPending = !0;
  }
  function u(B, N = "main") {
    const H = String(B || "").trim() || "openai-compatible";
    return N === "delegate" ? `delegate:${H}` : H;
  }
  function d(B, N = "main") {
    return t.pullStateByProvider?.[u(B, N)] || {
      status: "idle",
      message: ""
    };
  }
  function h(B, N, H = "main") {
    t.pullStateByProvider = {
      ...t.pullStateByProvider || {},
      [u(B, H)]: N
    };
  }
  function p(B, N, H = "main") {
    t.modelOptionsByProvider = {
      ...t.modelOptionsByProvider || {},
      [u(B, H)]: Array.isArray(N) ? N : []
    };
  }
  function y(B, N = "main") {
    const H = u(B, N);
    return Array.isArray(t.modelOptionsByProvider?.[H]) ? t.modelOptionsByProvider[H] : [];
  }
  function w(B, N) {
    const H = t.config?.presets || {}, le = In(B || N || "默认");
    return H[le] ? le : N && H[N] ? N : Object.keys(H)[0] || "默认";
  }
  function E(B, N) {
    const H = w(B, Vw), le = N && typeof N == "object" ? N : kr(), Ie = le.provider || "openai-compatible", De = Ar(le.modelConfigs || {}), Be = De[Ie] || {};
    return {
      delegatePresetName: H,
      delegateProvider: Ie,
      delegateModelConfigs: De,
      delegateBaseUrl: String(Be.baseUrl || ""),
      delegateModel: String(Be.model || ""),
      delegateApiKey: String(Be.apiKey || ""),
      delegateTemperature: Cr(Be.temperature, 0.2),
      delegateSendTemperature: ml(Be),
      delegateReasoningEnabled: !!Be.reasoningEnabled,
      delegateReasoningEffort: Er(Be.reasoningEffort),
      delegateToolMode: Be.toolMode || "native"
    };
  }
  function C(B = "openai-compatible", N = {}) {
    const H = Ar(N || {})[B] || {};
    return {
      baseUrl: String(H.baseUrl || ""),
      model: String(H.model || ""),
      apiKey: String(H.apiKey || ""),
      temperature: Cr(H.temperature, 0.2),
      sendTemperature: ml(H),
      reasoningEnabled: !!H.reasoningEnabled,
      reasoningEffort: Er(H.reasoningEffort),
      toolMode: H.toolMode || "native"
    };
  }
  function M(B = "openai-compatible", N = {}) {
    const H = Ar(N || {})[B] || {};
    return {
      delegateBaseUrl: String(H.baseUrl || ""),
      delegateModel: String(H.model || ""),
      delegateApiKey: String(H.apiKey || ""),
      delegateTemperature: Cr(H.temperature, 0.2),
      delegateSendTemperature: ml(H),
      delegateReasoningEnabled: !!H.reasoningEnabled,
      delegateReasoningEffort: Er(H.reasoningEffort),
      delegateToolMode: H.toolMode || "native"
    };
  }
  function k(B, N, H = t.config) {
    const le = In(B || "默认"), Ie = N && typeof N == "object" ? N : kr(), De = Ie.provider || "openai-compatible", Be = Ar(Ie.modelConfigs || {}), it = C(De, Be), Qe = w(H?.delegatePresetName, le), je = E(Qe, H?.delegateConfig && typeof H.delegateConfig == "object" ? H.delegateConfig : (H?.presets || {})[Qe] || Ie);
    return {
      currentPresetName: le,
      presetDraftName: le,
      provider: De,
      modelConfigs: Be,
      ...it,
      tavilyApiKey: String(H?.tavilyApiKey || ""),
      tavilyBaseUrl: xr(H?.tavilyBaseUrl || "https://api.tavily.com"),
      permissionMode: tl(Ie.permissionMode),
      jsApiPermission: Js(H?.jsApiPermission),
      ...je
    };
  }
  function R() {
    if (t.configDraft) return t.configDraft;
    const B = In(t.config?.currentPresetName || "默认");
    return t.configDraft = k(B, (t.config?.presets || {})[B] || kr()), t.configDraft;
  }
  function D(B) {
    const N = R(), H = B.querySelector("#xb-assistant-provider")?.value || N.provider || "openai-compatible", le = B.querySelector("#xb-assistant-delegate-provider")?.value || N.delegateProvider || "openai-compatible", Ie = {
      baseUrl: B.querySelector("#xb-assistant-base-url")?.value.trim() || "",
      model: B.querySelector("#xb-assistant-model")?.value.trim() || "",
      apiKey: B.querySelector("#xb-assistant-api-key")?.value.trim() || "",
      temperature: Cr(B.querySelector("#xb-assistant-temperature")?.value, N.temperature ?? 0.2),
      sendTemperature: B.querySelector("#xb-assistant-send-temperature")?.checked ?? !!(N.sendTemperature ?? !0),
      reasoningEnabled: B.querySelector("#xb-assistant-reasoning-enabled")?.checked || !1,
      reasoningEffort: Er(B.querySelector("#xb-assistant-reasoning-effort")?.value),
      toolMode: Oa(H) ? B.querySelector("#xb-assistant-tool-mode")?.value || N.toolMode || "native" : void 0
    }, De = {
      baseUrl: B.querySelector("#xb-assistant-delegate-base-url")?.value.trim() ?? N.delegateBaseUrl ?? "",
      model: B.querySelector("#xb-assistant-delegate-model")?.value.trim() ?? N.delegateModel ?? "",
      apiKey: B.querySelector("#xb-assistant-delegate-api-key")?.value.trim() ?? N.delegateApiKey ?? "",
      temperature: Cr(B.querySelector("#xb-assistant-delegate-temperature")?.value, N.delegateTemperature ?? 0.2),
      sendTemperature: B.querySelector("#xb-assistant-delegate-send-temperature")?.checked ?? !!(N.delegateSendTemperature ?? !0),
      reasoningEnabled: B.querySelector("#xb-assistant-delegate-reasoning-enabled")?.checked ?? !!N.delegateReasoningEnabled,
      reasoningEffort: Er(B.querySelector("#xb-assistant-delegate-reasoning-effort")?.value || N.delegateReasoningEffort),
      toolMode: Oa(le) ? B.querySelector("#xb-assistant-delegate-tool-mode")?.value || N.delegateToolMode || "native" : void 0
    }, Be = {
      ...Ar(N.modelConfigs || {}),
      [H]: {
        ...Ar(N.modelConfigs || {})[H] || {},
        ...Ie
      }
    }, it = {
      ...Ar(N.delegateModelConfigs || {}),
      [le]: {
        ...Ar(N.delegateModelConfigs || {})[le] || {},
        ...De
      }
    };
    return {
      ...N,
      currentPresetName: N.currentPresetName,
      presetDraftName: In(B.querySelector("#xb-assistant-preset-name")?.value),
      provider: H,
      modelConfigs: Be,
      baseUrl: Ie.baseUrl,
      model: Ie.model,
      apiKey: Ie.apiKey,
      temperature: Ie.temperature,
      sendTemperature: Ie.sendTemperature,
      reasoningEnabled: Ie.reasoningEnabled,
      reasoningEffort: Ie.reasoningEffort,
      toolMode: Ie.toolMode || N.toolMode || "native",
      tavilyApiKey: B.querySelector("#xb-assistant-tavily-api-key")?.value.trim() || "",
      tavilyBaseUrl: xr(N.tavilyBaseUrl || "https://api.tavily.com"),
      permissionMode: tl(B.querySelector("#xb-assistant-permission-mode")?.value || N.permissionMode),
      jsApiPermission: Js(B.querySelector("#xb-assistant-jsapi-permission")?.value || N.jsApiPermission),
      delegatePresetName: w(B.querySelector("#xb-assistant-delegate-preset-select")?.value || N.delegatePresetName, N.currentPresetName),
      delegateProvider: le,
      delegateModelConfigs: it,
      delegateBaseUrl: De.baseUrl,
      delegateModel: De.model,
      delegateApiKey: De.apiKey,
      delegateTemperature: De.temperature,
      delegateSendTemperature: De.sendTemperature,
      delegateReasoningEnabled: De.reasoningEnabled,
      delegateReasoningEffort: De.reasoningEffort,
      delegateToolMode: De.toolMode || N.delegateToolMode || "native"
    };
  }
  function m(B) {
    return t.configDraft = D(B), t.configDraft;
  }
  function b(B = R()) {
    return U_(B.provider) ? 32e3 : null;
  }
  function v(B = R()) {
    return {
      baseUrl: String(B.baseUrl || ""),
      model: String(B.model || ""),
      apiKey: String(B.apiKey || ""),
      temperature: Cr(B.temperature, 0.2),
      sendTemperature: !!(B.sendTemperature ?? !0),
      reasoningEnabled: !!B.reasoningEnabled,
      reasoningEffort: Er(B.reasoningEffort),
      toolMode: Oa(B.provider) ? B.toolMode || "native" : void 0
    };
  }
  function T(B = R()) {
    return {
      baseUrl: String(B.delegateBaseUrl || ""),
      model: String(B.delegateModel || ""),
      apiKey: String(B.delegateApiKey || ""),
      temperature: Cr(B.delegateTemperature, 0.2),
      sendTemperature: !!(B.delegateSendTemperature ?? !0),
      reasoningEnabled: !!B.delegateReasoningEnabled,
      reasoningEffort: Er(B.delegateReasoningEffort),
      toolMode: Oa(B.delegateProvider) ? B.delegateToolMode || "native" : void 0
    };
  }
  function I(B = R()) {
    const N = B.delegateProvider || "openai-compatible", H = Ar(B.delegateModelConfigs || {});
    return {
      provider: N,
      modelConfigs: {
        ...H,
        [N]: {
          ...H[N] || {},
          ...T(B)
        }
      }
    };
  }
  function x(B = R()) {
    return {
      provider: B.provider || "openai-compatible",
      baseUrl: B.baseUrl || "",
      model: B.model || "",
      apiKey: B.apiKey || "",
      tavilyApiKey: B.tavilyApiKey || "",
      tavilyBaseUrl: xr(B.tavilyBaseUrl || "https://api.tavily.com"),
      temperature: B.sendTemperature === !1 ? void 0 : Cr(B.temperature, 0.2),
      sendTemperature: !!(B.sendTemperature ?? !0),
      maxTokens: b(B),
      timeoutMs: PA,
      toolMode: B.toolMode || "native",
      reasoningEnabled: !!B.reasoningEnabled,
      reasoningEffort: Er(B.reasoningEffort)
    };
  }
  function P(B = R()) {
    return {
      provider: B.delegateProvider || "openai-compatible",
      baseUrl: B.delegateBaseUrl || "",
      model: B.delegateModel || "",
      apiKey: B.delegateApiKey || "",
      tavilyApiKey: B.tavilyApiKey || "",
      tavilyBaseUrl: xr(B.tavilyBaseUrl || "https://api.tavily.com"),
      temperature: B.delegateSendTemperature === !1 ? void 0 : Cr(B.delegateTemperature, 0.2),
      sendTemperature: !!(B.delegateSendTemperature ?? !0),
      maxTokens: U_(B.delegateProvider) ? 32e3 : null,
      timeoutMs: PA,
      toolMode: B.delegateToolMode || "native",
      reasoningEnabled: !!B.delegateReasoningEnabled,
      reasoningEffort: Er(B.delegateReasoningEffort)
    };
  }
  function L(B = {}) {
    const N = (B.role === "delegate", R());
    return B.role === "delegate" ? P(N) : x(N);
  }
  function G(B) {
    R(), t.configDraft = {
      ...t.configDraft,
      presetDraftName: In(B.querySelector("#xb-assistant-preset-name")?.value)
    };
  }
  function z(B = R(), N = B.provider || "openai-compatible", H = "main") {
    const le = d(N, H);
    return typeof a == "function" ? a({
      state: t,
      draft: B,
      provider: N,
      pullState: le,
      providerLabel: DA(N)
    }) : `预设「${B.currentPresetName || "默认"}」 · ${DA(N)}`;
  }
  function V(B, N, H) {
    const le = B?.querySelector?.(N);
    if (!le) return;
    const Ie = String(H?.status || "idle"), De = String(H?.message || "").trim();
    le.textContent = De, le.hidden = !De, le.classList.toggle("is-loading", Ie === "loading"), le.classList.toggle("is-success", Ie === "success"), le.classList.toggle("is-error", Ie === "error");
  }
  function Z(B) {
    if (!B) return;
    const N = Uf(t.configPage);
    t.configPage = N, B.querySelectorAll("[data-config-page]").forEach((H) => {
      const le = Uf(H?.dataset?.configPage) === N;
      H.classList.toggle("is-active", le), H.setAttribute("aria-selected", le ? "true" : "false");
    }), B.querySelectorAll("[data-config-page-panel]").forEach((H) => {
      const le = Uf(H?.dataset?.configPagePanel) === N;
      H.toggleAttribute("hidden", !le);
    }), B.querySelector("#xb-assistant-delete-preset")?.toggleAttribute("hidden", N === "delegate");
  }
  function O(B) {
    if (!t.config) return;
    Z(B);
    const N = R(), H = N.provider || "openai-compatible", le = y(H), Ie = N.delegateProvider || "openai-compatible", De = y(Ie, "delegate"), Be = B.querySelector("#xb-assistant-tool-mode-wrap"), it = B.querySelector("#xb-assistant-tool-mode"), Qe = B.querySelector("#xb-assistant-reasoning-enabled"), je = B.querySelector("#xb-assistant-reasoning-effort-wrap"), W = B.querySelector("#xb-assistant-reasoning-effort"), ee = B.querySelector("#xb-assistant-permission-mode"), ue = B.querySelector("#xb-assistant-jsapi-permission"), Se = B.querySelector("#xb-assistant-model-pulled"), be = B.querySelector("#xb-assistant-preset-select"), _e = B.querySelector("#xb-assistant-preset-name"), Pe = B.querySelector("#xb-assistant-delegate-preset-select"), xe = B.querySelector("#xb-assistant-delegate-provider"), ke = B.querySelector("#xb-assistant-delegate-base-url"), we = B.querySelector("#xb-assistant-delegate-model"), Ve = B.querySelector("#xb-assistant-delegate-api-key"), Re = B.querySelector("#xb-assistant-tavily-api-key"), $e = B.querySelector("#xb-assistant-delegate-model-pulled"), Le = B.querySelector("#xb-assistant-delegate-tool-mode-wrap"), tt = B.querySelector("#xb-assistant-delegate-tool-mode"), nt = B.querySelector("#xb-assistant-delegate-reasoning-enabled"), at = B.querySelector("#xb-assistant-delegate-reasoning-effort-wrap"), mt = B.querySelector("#xb-assistant-delegate-reasoning-effort");
    if (!be || !_e) return;
    const ft = (t.config.presetNames || []).map((Mt) => ({
      value: Mt,
      label: Mt
    }));
    ws(be, ft), be.value = N.currentPresetName || t.config.currentPresetName || "默认", Pe && (ws(Pe, ft), Pe.value = w(N.delegatePresetName, N.currentPresetName)), _e.value = N.presetDraftName || N.currentPresetName || "默认", B.querySelector("#xb-assistant-provider").value = H, B.querySelector("#xb-assistant-base-url").value = N.baseUrl || "", B.querySelector("#xb-assistant-model").value = N.model || "", B.querySelector("#xb-assistant-api-key").value = N.apiKey || "", B.querySelector("#xb-assistant-temperature").value = String(Cr(N.temperature, 0.2)), B.querySelector("#xb-assistant-send-temperature").checked = !!(N.sendTemperature ?? !0), Re && (Re.value = N.tavilyApiKey || ""), Be.style.display = Oa(H) ? "" : "none", ws(it, RA), it.value = N.toolMode || "native", ee && (ws(ee, WY), ee.value = tl(N.permissionMode)), ue && (ws(ue, VY), ue.value = Js(N.jsApiPermission)), ws(W, M_), Qe.checked = !!N.reasoningEnabled, W.value = Er(N.reasoningEffort), je.style.display = Qe.checked ? "" : "none", ws(Se, le.map((Mt) => ({
      value: Mt,
      label: Mt
    })), "手动填写"), Se.value = le.includes(N.model) ? N.model : "", xe && (xe.value = Ie), ke && (ke.value = N.delegateBaseUrl || ""), we && (we.value = N.delegateModel || ""), Ve && (Ve.value = N.delegateApiKey || "");
    const Pt = B.querySelector("#xb-assistant-delegate-temperature"), At = B.querySelector("#xb-assistant-delegate-send-temperature");
    Pt && (Pt.value = String(Cr(N.delegateTemperature, 0.2))), At && (At.checked = !!(N.delegateSendTemperature ?? !0)), Le && (Le.style.display = Oa(Ie) ? "" : "none"), tt && (ws(tt, RA), tt.value = N.delegateToolMode || "native"), mt && (ws(mt, M_), mt.value = Er(N.delegateReasoningEffort)), nt && (nt.checked = !!N.delegateReasoningEnabled), at && (at.style.display = N.delegateReasoningEnabled ? "" : "none"), $e && (ws($e, De.map((Mt) => ({
      value: Mt,
      label: Mt
    })), "手动填写"), $e.value = De.includes(N.delegateModel) ? N.delegateModel : ""), V(B, "#xb-assistant-model-pull-status", d(H)), V(B, "#xb-assistant-delegate-model-pull-status", d(Ie, "delegate"));
    const Ut = B.querySelector("#xb-assistant-runtime");
    if (Ut) {
      const Mt = t.configPage === "delegate";
      Ut.textContent = z(Mt ? {
        ...N,
        currentPresetName: "分身",
        provider: Ie
      } : N, Mt ? Ie : H, Mt ? "delegate" : "main");
    }
  }
  function Y(B) {
    if (typeof o != "function") return;
    const N = o(B);
    N && typeof N.catch == "function" && N.catch((H) => {
      r?.(i(H));
    });
  }
  function X(B, N, H) {
    B.querySelector(N)?.addEventListener("click", () => {
      const le = B.querySelector(H);
      le && (le.type = le.type === "password" ? "text" : "password");
    });
  }
  function j(B) {
    const N = m(B), H = In(N.presetDraftName), le = In(N.currentPresetName || t.config?.currentPresetName || "默认"), Ie = (t.config?.presets || {})[le] || kr(), De = Ar(N.modelConfigs || Ie.modelConfigs || {}), Be = {
      ...Ie,
      provider: N.provider,
      permissionMode: tl(N.permissionMode),
      modelConfigs: {
        ...De,
        [N.provider]: {
          ...De[N.provider] || {},
          ...v(N)
        }
      }
    }, it = {
      ...t.config?.presets || {},
      [H]: Be
    };
    t.config = Sc({
      ...t.config,
      jsApiPermission: Js(N.jsApiPermission),
      tavilyApiKey: String(N.tavilyApiKey || ""),
      tavilyBaseUrl: xr(N.tavilyBaseUrl || "https://api.tavily.com"),
      currentPresetName: H,
      delegatePresetName: w(N.delegatePresetName, H),
      delegateConfig: I(N),
      presets: it
    }), t.configDraft = k(H, Be, t.config), l(), Y({
      requestId: s("save-config"),
      config: t.config,
      payload: {
        workspaceFileName: t.config?.workspaceFileName || "",
        jsApiPermission: Js(t.config?.jsApiPermission),
        tavilyApiKey: String(t.config?.tavilyApiKey || ""),
        tavilyBaseUrl: xr(t.config?.tavilyBaseUrl || "https://api.tavily.com"),
        currentPresetName: t.config?.currentPresetName || "默认",
        delegatePresetName: t.config?.delegatePresetName || t.config?.currentPresetName || "默认",
        delegateConfig: t.config?.delegateConfig || {},
        presets: t.config?.presets || {}
      }
    });
  }
  function re(B) {
    if (Object.keys(t.config?.presets || {}).length <= 1) {
      r?.("至少要保留一套预设");
      return;
    }
    const N = m(B), H = In(t.configDraft?.currentPresetName || t.config?.currentPresetName || "默认"), le = { ...t.config?.presets || {} };
    delete le[H];
    const Ie = Object.keys(le)[0] || "默认", De = le[Ie] || kr();
    t.config = Sc({
      ...t.config,
      jsApiPermission: Js(N.jsApiPermission),
      tavilyApiKey: String(N.tavilyApiKey || t.config?.tavilyApiKey || ""),
      tavilyBaseUrl: xr(N.tavilyBaseUrl || t.config?.tavilyBaseUrl || "https://api.tavily.com"),
      currentPresetName: Ie,
      delegatePresetName: w(N.delegatePresetName, Ie),
      delegateConfig: I(N),
      presets: le
    }), t.configDraft = k(Ie, De, t.config), l(), Y({
      requestId: s("delete-preset"),
      config: t.config,
      payload: {
        workspaceFileName: t.config?.workspaceFileName || "",
        jsApiPermission: Js(t.config?.jsApiPermission),
        tavilyApiKey: String(t.config?.tavilyApiKey || ""),
        tavilyBaseUrl: xr(t.config?.tavilyBaseUrl || "https://api.tavily.com"),
        currentPresetName: t.config?.currentPresetName || "默认",
        delegatePresetName: t.config?.delegatePresetName || t.config?.currentPresetName || "默认",
        delegateConfig: t.config?.delegateConfig || {},
        presets: t.config?.presets || {}
      }
    }), n?.();
  }
  function pe(B) {
    B?.querySelector?.("#xb-assistant-provider") && (B.querySelector("#xb-assistant-provider").addEventListener("change", (N) => {
      const H = N.currentTarget.value, le = m(B);
      t.configDraft = {
        ...le,
        provider: H,
        ...C(H, le.modelConfigs)
      }, l(), n?.();
    }), B.querySelector("#xb-assistant-preset-select").addEventListener("change", (N) => {
      const H = In(N.currentTarget.value), le = (t.config?.presets || {})[H] || kr(), Ie = m(B);
      t.config = Sc({
        ...t.config,
        jsApiPermission: Js(Ie.jsApiPermission),
        currentPresetName: H,
        delegatePresetName: w(Ie.delegatePresetName, H),
        delegateConfig: I(Ie)
      }), t.configDraft = k(H, le, t.config), l(), n?.();
    }), B.querySelector("#xb-assistant-preset-name").addEventListener("input", () => {
      G(B);
    }), B.querySelector("#xb-assistant-base-url").addEventListener("input", () => {
      m(B);
    }), B.querySelector("#xb-assistant-model").addEventListener("input", () => {
      m(B);
    }), B.querySelector("#xb-assistant-api-key").addEventListener("input", () => {
      m(B);
    }), B.querySelector("#xb-assistant-temperature")?.addEventListener("input", () => {
      m(B);
    }), B.querySelector("#xb-assistant-send-temperature")?.addEventListener("change", () => {
      m(B);
    }), B.querySelector("#xb-assistant-tavily-api-key")?.addEventListener("input", () => {
      m(B);
    }), B.querySelector("#xb-assistant-model-pulled").addEventListener("change", (N) => {
      const H = N.currentTarget.value;
      H && (B.querySelector("#xb-assistant-model").value = H, m(B));
    }), X(B, "#xb-assistant-toggle-key", "#xb-assistant-api-key"), X(B, "#xb-assistant-toggle-tavily-key", "#xb-assistant-tavily-api-key"), B.querySelector("#xb-assistant-delegate-provider")?.addEventListener("change", (N) => {
      const H = m(B), le = N.currentTarget.value;
      t.configDraft = {
        ...H,
        delegateProvider: le,
        ...M(le, H.delegateModelConfigs)
      }, l(), n?.();
    }), B.querySelector("#xb-assistant-delegate-base-url")?.addEventListener("input", () => {
      m(B);
    }), B.querySelector("#xb-assistant-delegate-model")?.addEventListener("input", () => {
      m(B);
    }), B.querySelector("#xb-assistant-delegate-api-key")?.addEventListener("input", () => {
      m(B);
    }), B.querySelector("#xb-assistant-delegate-temperature")?.addEventListener("input", () => {
      m(B);
    }), B.querySelector("#xb-assistant-delegate-send-temperature")?.addEventListener("change", () => {
      m(B);
    }), B.querySelector("#xb-assistant-delegate-model-pulled")?.addEventListener("change", (N) => {
      const H = N.currentTarget.value;
      if (!H) return;
      const le = B.querySelector("#xb-assistant-delegate-model");
      le && (le.value = H), m(B);
    }), X(B, "#xb-assistant-delegate-toggle-key", "#xb-assistant-delegate-api-key"), B.querySelector("#xb-assistant-reasoning-enabled").addEventListener("change", () => {
      m(B), l(), n?.();
    }), B.querySelector("#xb-assistant-reasoning-effort").addEventListener("change", () => {
      m(B);
    }), B.querySelector("#xb-assistant-tool-mode").addEventListener("change", () => {
      m(B);
    }), B.querySelector("#xb-assistant-delegate-reasoning-enabled")?.addEventListener("change", () => {
      m(B), l(), n?.();
    }), B.querySelector("#xb-assistant-delegate-reasoning-effort")?.addEventListener("change", () => {
      m(B);
    }), B.querySelector("#xb-assistant-delegate-tool-mode")?.addEventListener("change", () => {
      m(B);
    }), B.querySelector("#xb-assistant-permission-mode")?.addEventListener("change", () => {
      m(B);
    }), B.querySelector("#xb-assistant-jsapi-permission")?.addEventListener("change", () => {
      m(B);
    }), B.querySelector("#xb-assistant-delegate-preset-select")?.addEventListener("change", (N) => {
      const H = w(N.currentTarget?.value, t.configDraft?.currentPresetName || t.config?.currentPresetName || "默认"), le = (t.config?.presets || {})[H] || kr();
      t.configDraft = {
        ...m(B),
        ...E(H, le)
      }, l(), n?.();
    }), B.querySelectorAll("[data-config-page]").forEach((N) => {
      N.addEventListener("click", (H) => {
        m(B), t.configPage = Uf(H.currentTarget?.dataset?.configPage), Z(B), O(B);
      });
    }), B.querySelector("#xb-assistant-pull-models").addEventListener("click", async () => {
      m(B), l();
      const N = L();
      h(N.provider, {
        status: "loading",
        message: "正在拉取模型列表…"
      }), n?.();
      try {
        const H = await ZA(N);
        p(N.provider, H), h(N.provider, {
          status: "success",
          message: `已拉取 ${H.length} 个模型`
        });
      } catch (H) {
        p(N.provider, []), h(N.provider, {
          status: "error",
          message: i(H)
        });
      }
      l(), n?.();
    }), B.querySelector("#xb-assistant-delegate-pull-models")?.addEventListener("click", async () => {
      m(B), l();
      const N = L({ role: "delegate" });
      h(N.provider, {
        status: "loading",
        message: "正在拉取模型列表…"
      }, "delegate"), n?.();
      try {
        const H = await ZA(N);
        p(N.provider, H, "delegate"), h(N.provider, {
          status: "success",
          message: `已拉取 ${H.length} 个模型`
        }, "delegate");
      } catch (H) {
        p(N.provider, [], "delegate"), h(N.provider, {
          status: "error",
          message: i(H)
        }, "delegate");
      }
      l(), n?.();
    }), B.querySelector("#xb-assistant-save").addEventListener("click", () => {
      j(B);
    }), B.querySelector("#xb-assistant-delete-preset").addEventListener("click", () => {
      re(B);
    }));
  }
  return {
    getActiveProviderConfig: L,
    syncConfigToForm: O,
    bindSettingsPanelEvents: pe
  };
}
function wh(e = "") {
  return String(e || "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
}
function kie(e = {}) {
  const t = String(e?.status || "idle");
  return t === "saving" ? {
    className: "xb-assistant-save-button is-saving",
    title: "正在保存配置",
    html: '<span class="xb-assistant-save-spinner" aria-hidden="true"></span>保存中...'
  } : t === "success" ? {
    className: "xb-assistant-save-button is-success",
    title: "配置已保存",
    html: "已保存"
  } : t === "error" ? {
    className: "xb-assistant-save-button is-error",
    title: wh(e?.error || "保存失败"),
    html: "保存失败"
  } : {
    className: "xb-assistant-save-button",
    title: "保存配置",
    html: "保存配置"
  };
}
function Mie(e = {}) {
  const { configSave: t = {}, runtimeText: n = "", inlineToastText: r = "", showInlineToast: s = !0, showAssistantPermissions: o = !0, showDelegateSettings: i = !0, activePage: a = "main", delegatePresetHint: l = "DelegateRun 分身会使用这里的独立 API 配置；可以和主助手使用不同 Provider、Base URL、模型和 Tool 调用格式。", isBusy: u = !1, canDeletePreset: d = !0 } = e, h = kie(t), p = u || String(t?.status || "") === "saving" ? "disabled" : "", y = u || !d ? "disabled" : "", w = a === "delegate" ? "delegate" : "main", E = w === "main", C = w === "delegate", M = o ? `
            <label>
                <span>斜杠命令权限</span>
                <select id="xb-assistant-permission-mode"></select>
            </label>
            <label>
                <span>JavaScript API 权限</span>
                <select id="xb-assistant-jsapi-permission"></select>
            </label>` : "", k = i ? `
            <div class="xb-assistant-config-tabs" role="tablist" aria-label="API 配置分页">
                <button id="xb-assistant-config-tab-main" type="button" class="xb-assistant-config-tab ${E ? "is-active" : ""}" data-config-page="main" role="tab" aria-selected="${E ? "true" : "false"}">主助手 API</button>
                <button id="xb-assistant-config-tab-delegate" type="button" class="xb-assistant-config-tab ${C ? "is-active" : ""}" data-config-page="delegate" role="tab" aria-selected="${C ? "true" : "false"}">分身 API</button>
            </div>` : "", R = i ? `
            <div class="xb-assistant-config-page" data-config-page-panel="delegate" ${C ? "" : "hidden"}>
                <p class="xb-assistant-config-note">${wh(l)}</p>
                <label>
                    <span>已存预设</span>
                    <select id="xb-assistant-delegate-preset-select"></select>
                </label>
                <label>
                    <span>Provider</span>
                    <select id="xb-assistant-delegate-provider">
                        <option value="openai-responses">OpenAI Responses</option>
                        <option value="openai-compatible">OpenAI 兼容</option>
                        <option value="sillytavern-openai-compatible">酒馆 OpenAI 兼容</option>
                        <option value="sillytavern-claude">酒馆 Claude</option>
                        <option value="sillytavern-google">酒馆 Google AI</option>
                        <option value="anthropic">Anthropic</option>
                        <option value="google">Google AI</option>
                    </select>
                </label>
                <label>
                    <span>Base URL</span>
                    <input id="xb-assistant-delegate-base-url" type="text" />
                </label>
                <label>
                    <span>API Key</span>
                    <div class="xb-assistant-inline-input">
                        <input id="xb-assistant-delegate-api-key" type="password" />
                        <button id="xb-assistant-delegate-toggle-key" type="button" class="secondary ghost">显示</button>
                    </div>
                </label>
                <label>
                    <span>Model</span>
                    <input id="xb-assistant-delegate-model" type="text" />
                </label>
                <div class="xb-assistant-temperature-row">
                    <label>
                        <span>温度</span>
                        <input id="xb-assistant-delegate-temperature" type="number" min="0" max="2" step="0.05" />
                    </label>
                    <label class="xb-assistant-checkbox-row">
                        <span>允许传参</span>
                        <span class="xb-assistant-checkbox-control">
                            <input id="xb-assistant-delegate-send-temperature" type="checkbox" />
                        </span>
                    </label>
                </div>
                <div class="xb-assistant-inline-input xb-assistant-model-row">
                    <label class="xb-assistant-grow">
                        <span>已拉取模型</span>
                        <select id="xb-assistant-delegate-model-pulled">
                            <option value="">手动填写</option>
                        </select>
                    </label>
                    <button id="xb-assistant-delegate-pull-models" type="button" class="secondary" ${u ? "disabled" : ""}>拉取模型</button>
                </div>
                <div class="xb-assistant-inline-status" id="xb-assistant-delegate-model-pull-status" aria-live="polite" hidden></div>
                <label id="xb-assistant-delegate-tool-mode-wrap">
                    <span>Tool 调用格式</span>
                    <select id="xb-assistant-delegate-tool-mode"></select>
                </label>
                <label class="xb-assistant-checkbox-row">
                    <span>
                        Reasoning参数
                        <small>需 API 支持，否则报错</small>
                    </span>
                    <span class="xb-assistant-checkbox-control">
                        <input id="xb-assistant-delegate-reasoning-enabled" type="checkbox" />
                        <span>开启</span>
                    </span>
                </label>
                <label id="xb-assistant-delegate-reasoning-effort-wrap">
                    <span>思考强度</span>
                    <select id="xb-assistant-delegate-reasoning-effort"></select>
                </label>
            </div>` : "";
  return `
        <section class="xb-assistant-config">
            ${k}
            <div class="xb-assistant-config-page" data-config-page-panel="main" ${E ? "" : "hidden"}>
            <label>
                <span>已存预设</span>
                <select id="xb-assistant-preset-select"></select>
            </label>
            <label>
                <span>预设名称</span>
                <input id="xb-assistant-preset-name" type="text" placeholder="例如：OpenAI 测试号" />
            </label>
            <label>
                <span>Provider</span>
                <select id="xb-assistant-provider">
                    <option value="openai-responses">OpenAI Responses</option>
                    <option value="openai-compatible">OpenAI 兼容</option>
                    <option value="sillytavern-openai-compatible">酒馆 OpenAI 兼容</option>
                    <option value="sillytavern-claude">酒馆 Claude</option>
                    <option value="sillytavern-google">酒馆 Google AI</option>
                    <option value="anthropic">Anthropic</option>
                    <option value="google">Google AI</option>
                </select>
            </label>
            <label>
                <span>Base URL</span>
                <input id="xb-assistant-base-url" type="text" />
            </label>
            <label>
                <span>API Key</span>
                <div class="xb-assistant-inline-input">
                    <input id="xb-assistant-api-key" type="password" />
                    <button id="xb-assistant-toggle-key" type="button" class="secondary ghost">显示</button>
                </div>
            </label>
            <label>
                <span>Model</span>
                <input id="xb-assistant-model" type="text" />
            </label>
            <div class="xb-assistant-temperature-row">
                <label>
                    <span>温度</span>
                    <input id="xb-assistant-temperature" type="number" min="0" max="2" step="0.05" />
                </label>
                <label class="xb-assistant-checkbox-row">
                    <span>允许传参</span>
                    <span class="xb-assistant-checkbox-control">
                        <input id="xb-assistant-send-temperature" type="checkbox" />
                    </span>
                </label>
            </div>
            <div class="xb-assistant-inline-input xb-assistant-model-row">
                <label class="xb-assistant-grow">
                    <span>已拉取模型</span>
                    <select id="xb-assistant-model-pulled">
                        <option value="">手动填写</option>
                    </select>
                </label>
                <button id="xb-assistant-pull-models" type="button" class="secondary" ${u ? "disabled" : ""}>拉取模型</button>
            </div>
            <div class="xb-assistant-inline-status" id="xb-assistant-model-pull-status" aria-live="polite" hidden></div>
            <label>
                <span>Tavily API Key（全局）</span>
                <div class="xb-assistant-inline-input">
                    <input id="xb-assistant-tavily-api-key" type="password" />
                    <button id="xb-assistant-toggle-tavily-key" type="button" class="secondary ghost">显示</button>
                </div>
            </label>
            <label id="xb-assistant-tool-mode-wrap">
                <span>Tool 调用格式</span>
                <select id="xb-assistant-tool-mode"></select>
            </label>
            ${M}
            <label class="xb-assistant-checkbox-row">
                <span>
                    Reasoning参数
                    <small>需 API 支持，否则报错</small>
                </span>
                <span class="xb-assistant-checkbox-control">
                    <input id="xb-assistant-reasoning-enabled" type="checkbox" />
                    <span>开启</span>
                </span>
            </label>
            <label id="xb-assistant-reasoning-effort-wrap">
                <span>思考强度</span>
                <select id="xb-assistant-reasoning-effort"></select>
            </label>
            </div>
            ${R}
            <div class="xb-assistant-actions">
                <button id="xb-assistant-save" type="button" class="${h.className}" title="${h.title}" ${p}>${h.html}</button>
                <button id="xb-assistant-delete-preset" type="button" class="secondary" ${y} ${C ? "hidden" : ""}>删除配置</button>
            </div>
            <div class="xb-assistant-runtime" id="xb-assistant-runtime">${wh(n)}</div>
            ${s ? `<div class="xb-assistant-toast xb-assistant-toast-inline" id="xb-assistant-toast" aria-live="polite">${wh(r)}</div>` : ""}
        </section>
    `;
}
var Cv = 48, Av = 48, Iv = 80, xv = 80, qf = 24, kv = 60, Pie = [
  {
    key: "storyArcPrompt",
    label: "剧情脉络",
    summary: "长期脉络档案"
  },
  {
    key: "statePrompt",
    label: "状态栏",
    summary: "当前状态档案"
  },
  {
    key: "turnPrompt",
    label: "楼层小记",
    summary: "按楼层轻量记录"
  }
], Rie = {
  top: "最前面",
  beforeCharacter: "角色卡前",
  afterCharacter: "角色卡后",
  beforeHistory: "历史前",
  afterHistory: "历史后",
  assistantPrefill: "回复开头"
};
function yn(e) {
  return e && typeof e == "object" && !Array.isArray(e) ? e : {};
}
function Fa(e) {
  try {
    return JSON.parse(JSON.stringify(e));
  } catch {
    return e;
  }
}
function Ys(e = "") {
  return String(e || "").trim().toLocaleLowerCase();
}
function jA(e, t) {
  return t ? Ys(e).includes(t) : !0;
}
function Mv(e, t = 1600) {
  return e.map((n) => String(n ?? "").slice(0, t)).filter(Boolean).join(`
`);
}
function Ba(e) {
  return JSON.stringify(e || {});
}
function Nie(e) {
  const t = yn(e), n = Array.isArray(t.entries) ? t.entries.map((r) => {
    const s = yn(r);
    return {
      uid: String(s.uid || ""),
      name: String(s.name || "未命名条目"),
      keys: Array.isArray(s.keys) ? s.keys.map((o) => String(o || "")).filter(Boolean) : [],
      secondaryKeys: Array.isArray(s.secondaryKeys) ? s.secondaryKeys.map((o) => String(o || "")).filter(Boolean) : [],
      contentPreview: String(s.contentPreview || ""),
      enabled: s.enabled !== !1,
      constant: s.constant === !0,
      order: Number.isFinite(Number(s.order)) ? Number(s.order) : 100
    };
  }) : [];
  return {
    name: String(t.name || ""),
    entryCount: Number.isFinite(Number(t.entryCount)) ? Number(t.entryCount) : n.length,
    enabledCount: Number.isFinite(Number(t.enabledCount)) ? Number(t.enabledCount) : n.filter((r) => r.enabled).length,
    constantCount: Number.isFinite(Number(t.constantCount)) ? Number(t.constantCount) : n.filter((r) => r.constant).length,
    disabledCount: Number.isFinite(Number(t.disabledCount)) ? Number(t.disabledCount) : n.filter((r) => !r.enabled).length,
    keywordCount: Number.isFinite(Number(t.keywordCount)) ? Number(t.keywordCount) : n.reduce((r, s) => r + s.keys.length + s.secondaryKeys.length, 0),
    totalChars: Number.isFinite(Number(t.totalChars)) ? Number(t.totalChars) : n.reduce((r, s) => r + s.contentPreview.length, 0),
    entries: n
  };
}
function eI(e) {
  const t = yn(e), n = Array.isArray(t.keysecondary) ? t.keysecondary.map((o) => String(o || "").trim()).filter(Boolean) : [], r = Array.isArray(t.secondary_keys) ? t.secondary_keys.map((o) => String(o || "").trim()).filter(Boolean) : n, s = t.disable === !0 || t.enabled === !1;
  return {
    worldbookName: String(t.worldbookName || t.name || "").trim(),
    uid: t.uid === null || t.uid === void 0 ? "" : String(t.uid).trim(),
    comment: String(t.comment ?? ""),
    name: String(t.name ?? ""),
    title: String(t.title ?? ""),
    key: Array.isArray(t.key) ? t.key.map((o) => String(o || "").trim()).filter(Boolean) : [],
    keysecondary: n,
    secondary_keys: r,
    content: String(t.content ?? ""),
    disable: s,
    enabled: !s,
    constant: t.constant === !0,
    order: Number.isFinite(Number(t.order)) ? Number(t.order) : 100,
    entryHash: String(t.entryHash || ""),
    revision: String(t.revision || t.entryHash || "")
  };
}
function tI(e) {
  const t = yn(e), n = Array.isArray(t.options) ? t.options.map((r) => String(r || "").trim()).filter(Boolean) : [];
  return {
    options: n,
    selected: Array.isArray(t.selected) ? t.selected.map((r) => String(r || "").trim()).filter((r) => n.includes(r)) : []
  };
}
function Pv(e = {}) {
  const t = yn(e);
  return {
    ...t,
    id: String(t.id || ""),
    scriptName: String(t.scriptName || ""),
    findRegex: String(t.findRegex || ""),
    replaceString: String(t.replaceString || ""),
    trimStrings: Array.isArray(t.trimStrings) ? t.trimStrings.map((n) => String(n || "")).filter(Boolean) : [],
    placement: Array.isArray(t.placement) ? t.placement.map((n) => Number(n)).filter((n) => Number.isFinite(n)) : [],
    disabled: t.disabled === !0,
    markdownOnly: t.markdownOnly === !0,
    promptOnly: t.promptOnly === !0,
    runOnEdit: t.runOnEdit === !0,
    substituteRegex: Number.isFinite(Number(t.substituteRegex)) ? Number(t.substituteRegex) : 0,
    minDepth: t.minDepth === null || t.minDepth === "" || t.minDepth === void 0 ? null : Number(t.minDepth),
    maxDepth: t.maxDepth === null || t.maxDepth === "" || t.maxDepth === void 0 ? null : Number(t.maxDepth)
  };
}
function $ie(e) {
  const t = yn(e), n = String(t.id || "").trim();
  return n ? {
    id: n,
    name: String(t.name || n).trim() || n,
    avatarUrl: String(t.avatarUrl || "").trim(),
    description: String(t.description || "").trim(),
    active: t.active === !0
  } : null;
}
function Die(e) {
  const t = yn(e), n = String(t.currentUserId || "").trim() || null, r = Array.isArray(t.users) ? t.users.map((s) => $ie(s)).filter((s) => !!s) : [];
  return n && !r.some((s) => s.id === n) && r.unshift({
    id: n,
    name: n,
    avatarUrl: "",
    description: "",
    active: !0
  }), {
    users: r.map((s) => ({
      ...s,
      active: n ? s.id === n : s.active
    })),
    currentUserId: n
  };
}
function Lie() {
  const e = String(window.location.hash || "").replace(/^#\/?/, "").split("/")[1];
  return e === "api" || e === "chatPreset" || e === "worldbooks" || e === "regex" || e === "assistantPreset" || e === "base" ? e : "api";
}
function Oie(e) {
  const t = bk(), n = Bc(), r = /* @__PURE__ */ se(null), s = /* @__PURE__ */ se({
    status: "idle",
    requestId: "",
    error: ""
  }), o = /* @__PURE__ */ se(""), i = /* @__PURE__ */ se(t), a = /* @__PURE__ */ se(t), l = /* @__PURE__ */ se({}), u = /* @__PURE__ */ se(""), d = /* @__PURE__ */ se(JSON.stringify(t)), h = /* @__PURE__ */ se(""), p = /* @__PURE__ */ se(n), y = /* @__PURE__ */ se(n), w = /* @__PURE__ */ se([]), E = /* @__PURE__ */ se(""), C = /* @__PURE__ */ se(""), M = /* @__PURE__ */ se(JSON.stringify(n)), k = /* @__PURE__ */ se(""), R = /* @__PURE__ */ se("storyArcPrompt"), D = /* @__PURE__ */ se({}), m = /* @__PURE__ */ se(""), b = /* @__PURE__ */ se(""), v = /* @__PURE__ */ se([]), T = /* @__PURE__ */ se([]), I = /* @__PURE__ */ se(""), x = /* @__PURE__ */ se(!1), P = /* @__PURE__ */ se(null), L = /* @__PURE__ */ se(""), G = /* @__PURE__ */ se(null), z = /* @__PURE__ */ se(""), V = /* @__PURE__ */ se(""), Z = /* @__PURE__ */ se(""), O = /* @__PURE__ */ se(""), Y = /* @__PURE__ */ se(Cv), X = /* @__PURE__ */ se(""), j = /* @__PURE__ */ se(Av), re = /* @__PURE__ */ se(""), pe = /* @__PURE__ */ se(Iv), B = /* @__PURE__ */ se(""), N = /* @__PURE__ */ se(xv), H = /* @__PURE__ */ se(qf), le = /* @__PURE__ */ se(""), Ie = /* @__PURE__ */ se({}), De = /* @__PURE__ */ se({}), Be = /* @__PURE__ */ se(""), it = /* @__PURE__ */ se({}), Qe = /* @__PURE__ */ se(Ba(it.value)), je = /* @__PURE__ */ se(""), W = /* @__PURE__ */ se([]), ee = /* @__PURE__ */ se(null), ue = /* @__PURE__ */ se(""), Se = /* @__PURE__ */ se(!1), be = /* @__PURE__ */ se(!1), _e = /* @__PURE__ */ se(""), Pe = /* @__PURE__ */ se(Ya(e.tavernDisplaySettings.value)), xe = /* @__PURE__ */ se(Fa(Pe.value));
  let ke = 0, we = 0, Ve = "", Re = 0, $e = 0;
  const Le = {
    config: {},
    configDraft: null,
    configFormSyncPending: !0,
    configPage: "main",
    configSave: s.value,
    pullStateByProvider: {},
    modelOptionsByProvider: {}
  };
  let tt = null;
  const nt = (F = i.value) => JSON.stringify(F || {}), at = (F = p.value) => JSON.stringify(F || {}), mt = ie(() => nt(i.value) !== d.value), ft = ie(() => at(p.value) !== M.value), Pt = ie(() => Xw(e.agentConfig.value)), At = ie(() => Pt.value.readiness.ok), Ut = ie(() => Pt.value.readiness.message), Mt = ie(() => {
    const F = Pt.value;
    return `预设「${F.currentPresetName || "默认"}」 · ${F.providerLabel} / ${F.model || "未选择模型"}`;
  }), qt = ie(() => {
    const F = yn(l.value.components), J = Array.isArray(F.promptManager) ? F.promptManager : [], me = String(i.value.promptManager?.name || i.value.name || "").trim(), Te = /* @__PURE__ */ new Set();
    return [me, ...J].map((ot) => {
      if (typeof ot == "string") return ot.trim();
      const vt = yn(ot);
      return String(vt.name || vt.label || vt.id || "").trim();
    }).filter((ot) => !ot || Te.has(ot) ? !1 : (Te.add(ot), !0)).map((ot) => ({
      name: ot,
      label: ot
    }));
  }), Nt = ie(() => {
    const F = Ys(O.value);
    return F ? qt.value.filter((J) => jA(J.label || J.name, F)) : qt.value;
  }), Yn = ie(() => {
    const F = Nt.value.slice(0, Y.value), J = String(k.value || i.value.promptManager?.name || i.value.name || "").trim();
    if (!J || F.some((Te) => Te.name === J)) return F;
    const me = qt.value.find((Te) => Te.name === J);
    return me ? [me, ...F] : F;
  }), Or = ie(() => Math.max(0, Nt.value.length - Math.min(Nt.value.length, Y.value))), Qt = ie(() => (Array.isArray(D.value.books) ? D.value.books : []).map((F) => {
    const J = yn(F);
    return {
      name: String(J.name || "").trim(),
      globalActive: J.globalActive === !0
    };
  }).filter((F) => F.name)), te = ie(() => {
    const F = Ys(B.value);
    return F ? Qt.value.filter((J) => jA(J.name, F)) : Qt.value;
  }), fe = ie(() => {
    const F = te.value.slice(0, N.value), J = String(m.value || "").trim();
    if (!J || F.some((Te) => Te.name === J)) return F;
    const me = Qt.value.find((Te) => Te.name === J);
    return me ? [me, ...F] : F;
  }), ae = ie(() => Math.max(0, te.value.length - Math.min(te.value.length, N.value))), Je = ie(() => Qt.value.find((F) => F.name === m.value) || null), Ce = ie(() => {
    const F = P.value;
    return !F || F.name !== m.value ? 0 : Math.max(0, F.entryCount - F.entries.length);
  }), ye = ie(() => G.value ? Ba(G.value) !== z.value : !1), ge = ie(() => Z.value === "正在保存"), Ze = ie(() => (Array.isArray(De.value.groups) ? De.value.groups : []).map((F) => {
    const J = yn(F), me = Array.isArray(J.scripts) ? J.scripts.map((Te) => yn(Te)) : [];
    return {
      key: String(J.key || ""),
      label: String(J.label || J.key || ""),
      scriptType: Number(J.scriptType),
      scripts: me,
      allowed: J.allowed === !0
    };
  }).filter((F) => F.key && Number.isFinite(F.scriptType))), Ne = ie(() => Ze.value.flatMap((F) => F.scripts.map((J, me) => {
    const Te = {
      key: `${F.scriptType}:${String(J.id || me)}`,
      groupKey: F.key,
      groupLabel: F.label,
      scriptType: F.scriptType,
      script: J
    };
    return Te.searchCorpus = Ys(Mv([
      Te.groupLabel,
      Te.script.scriptName,
      Te.script.findRegex,
      Te.script.replaceString,
      Te.script.trimStrings,
      Te.script.placement,
      Te.key
    ], 1200)), Te;
  })));
  function oe(F = "") {
    const J = Number(Ie.value[F]);
    return Number.isFinite(J) && J > 0 ? Math.floor(J) : kv;
  }
  function Ye(F = "") {
    const J = oe(F);
    Ie.value = {
      ...Ie.value,
      [F]: J + kv
    };
  }
  const It = ie(() => {
    const F = Ys(le.value), J = String(Be.value || "").trim();
    return Ze.value.map((me) => {
      const Te = Ne.value.filter(($t) => $t.groupKey === me.key), ot = F ? Te.filter(($t) => String($t.searchCorpus || "").includes(F)) : Te, vt = oe(me.key), Dt = ot.slice(0, vt);
      if (J && !Dt.some(($t) => $t.key === J)) {
        const $t = Te.find((qm) => qm.key === J);
        $t && Dt.unshift($t);
      }
      return {
        ...me,
        visibleRows: Dt,
        totalCount: Te.length,
        filteredCount: ot.length,
        hiddenCount: Math.max(0, ot.length - Math.min(ot.length, vt))
      };
    }).filter((me) => me.filteredCount || !F);
  }), ct = ie(() => Ne.value.find((F) => F.key === Be.value) || null), ar = ie(() => Ba(it.value) !== Qe.value), Wl = ie(() => [
    {
      key: "worldbooks",
      label: "世界书",
      mobileLabel: "世界"
    },
    {
      key: "chatPreset",
      label: "聊天预设",
      mobileLabel: "聊天预设",
      badge: mt.value ? "未保存" : ""
    },
    {
      key: "assistantPreset",
      label: "助手预设",
      mobileLabel: "助手",
      badge: ft.value ? "未保存" : ""
    },
    {
      key: "regex",
      label: "正则",
      badge: ar.value ? "未保存" : ""
    },
    {
      key: "api",
      label: "API 配置",
      mobileLabel: "API"
    },
    {
      key: "base",
      label: "基础设定",
      mobileLabel: "基础"
    }
  ]), ai = ie(() => W.value.find((F) => F.id === ee.value) || null), li = ie(() => Pie.map((F) => ({
    id: F.key,
    key: F.key,
    label: F.label,
    summary: F.summary,
    content: String(p.value[F.key] || "")
  }))), da = ie(() => {
    const F = Ys(X.value);
    return F ? w.value.filter((J) => Ys(Mv([
      J.name,
      J.description,
      J.id,
      J.preset?.name,
      J.preset?.description
    ], 1200)).includes(F)) : w.value;
  }), an = ie(() => {
    const F = da.value.slice(0, j.value), J = String(E.value || p.value.id || "").trim();
    if (!J || F.some((Te) => Te.id === J)) return F;
    const me = w.value.find((Te) => Te.id === J);
    return me ? [me, ...F] : F;
  }), ln = ie(() => Math.max(0, da.value.length - Math.min(da.value.length, j.value))), Sd = ie(() => w.value.find((F) => F.id === E.value) || null), fa = ie(() => li.value.find((F) => F.id === R.value) || li.value[0] || null), bo = ie(() => !!lr()), Fn = ie(() => {
    const F = ci(), J = new Map(F.map((vt, Dt) => [String(vt.identifier || vt.id || `prompt-${Dt + 1}`).trim(), vt])), me = di(), Te = /* @__PURE__ */ new Set(), ot = [];
    return me.forEach((vt) => {
      const Dt = String(vt.identifier || "").trim();
      if (!Dt || Te.has(Dt)) return;
      const $t = J.get(Dt) || {
        identifier: Dt,
        name: Dt
      };
      Te.add(Dt), ot.push({
        identifier: Dt,
        name: String($t.name || $t.label || Dt),
        role: String($t.role || "system"),
        content: String($t.content || ""),
        enabled: vt.enabled !== !1,
        marker: $t.marker === !0,
        systemPrompt: $t.system_prompt === !0,
        injectionPosition: Number($t.injection_position ?? 0),
        injectionDepth: Number.isFinite(Number($t.injection_depth)) ? Number($t.injection_depth) : "",
        source: $t.extension ? "扩展" : $t.system_prompt ? "系统" : "预设",
        orderEntry: vt,
        prompt: $t,
        listed: !0,
        searchCorpus: Ys(Mv([
          $t.name || $t.label || Dt,
          $t.role || "system",
          $t.extension ? "扩展" : $t.system_prompt ? "系统" : "预设",
          Dt,
          $t.content || ""
        ]))
      });
    }), ot;
  }), ha = ie(() => {
    const F = Ys(re.value);
    return F ? Fn.value.filter((J) => String(J.searchCorpus || "").includes(F)) : Fn.value;
  }), bm = ie(() => {
    const F = ha.value.slice(0, pe.value), J = String(h.value || "").trim();
    if (!J || F.some((Te) => Te.identifier === J)) return F;
    const me = Fn.value.find((Te) => Te.identifier === J);
    return me ? [me, ...F] : F;
  }), wm = ie(() => Math.max(0, ha.value.length - Math.min(ha.value.length, pe.value))), Sm = ie(() => new Map(Fn.value.map((F, J) => [F.identifier, J]))), Tm = ie(() => Fn.value.find((F) => F.identifier === h.value) || Fn.value[0] || null), Em = ie(() => lr() ? "当前角色顺序" : "未读取到当前角色顺序"), Td = ie(() => (i.value.sections || []).map((F, J) => ({
    ...F,
    previewId: F.id || `chat-preset-section-${J}`,
    previewLabel: F.label || F.source || `提示词 ${J + 1}`,
    previewPlacement: F.source === "promptManager" ? F.marker ? "酒馆标记" : "酒馆顺序" : Rie[F.placement || "beforeHistory"] || F.placement || "历史前",
    sectionIndex: J,
    chars: String(F.content || "").length
  })).filter((F) => (F.content || F.marker) && F.enabled !== !1)), Cm = ie(() => Td.value.reduce((F, J) => F + J.chars, 0));
  function Qr(F) {
    if (!F) return "";
    const J = F.uid !== void 0 && F.uid !== null && String(F.uid).trim() ? String(F.uid).trim() : F.order !== void 0 && F.order !== null && String(F.order).trim() ? String(F.order).trim() : String(F.name || "").trim();
    return `${String(m.value || "").trim()}:${J}`;
  }
  function ui(F) {
    return !!V.value && V.value === Qr(F);
  }
  function pa(F) {
    return Sm.value.get(F) ?? -1;
  }
  function Zr() {
    return yn(i.value.promptManager);
  }
  function Fs() {
    return yn(Zr().rawPreset);
  }
  function ci() {
    const F = Fs(), J = Zr();
    return (Array.isArray(F.prompts) ? F.prompts : Array.isArray(J.prompts) ? J.prompts : []).map((me) => yn(me)).filter((me) => String(me.identifier || me.id || "").trim());
  }
  function Bs() {
    const F = Fs(), J = Zr();
    return (Array.isArray(F.prompt_order) ? F.prompt_order : Array.isArray(J.promptOrder) ? J.promptOrder : []).map((me) => yn(me));
  }
  function di() {
    const F = Zr(), J = Array.isArray(F.activeOrder) ? F.activeOrder.map((ot) => yn(ot)) : [];
    if (J.length) return J;
    const me = String(F.activeCharacterId ?? "").trim();
    if (!me) return [];
    const Te = Bs().find((ot) => String(ot.character_id ?? "") === me);
    return Array.isArray(Te?.order) ? Te.order.map((ot) => yn(ot)) : [];
  }
  function lr() {
    return String(Zr().activeCharacterId ?? "").trim();
  }
  function ur(F = {}, J = {}) {
    const me = Kg(F);
    a.value = me, d.value = nt(me), k.value = String(me.promptManager?.name || me.name || "").trim(), J.replaceDraft !== !1 && (i.value = me);
  }
  function cr(F) {
    if (!F) {
      it.value = {}, Qe.value = Ba({}), Be.value = "";
      return;
    }
    const J = Pv(F.script);
    it.value = J, Qe.value = Ba(J), Be.value = F.key;
  }
  function dr(F = {}, J = {}) {
    const me = Ub(F);
    y.value = me, M.value = at(me), J.replaceDraft !== !1 && (p.value = me);
  }
  function mn(F, J = "继续？") {
    return window.confirm(`当前${F}有未保存修改，${J}会放弃这些草稿。继续？`);
  }
  function Ed(F) {
    const J = eI(F);
    G.value = J, z.value = Ba(J), V.value = Qr(J);
  }
  async function Am() {
    if (ft.value && !mn("助手预设", "刷新")) {
      C.value = "";
      return;
    }
    const [F, J, me] = await Promise.all([
      Ma(),
      Zh(),
      zg()
    ]);
    w.value = F, E.value = J || me.id, dr(me, { replaceDraft: !ft.value });
  }
  async function fi() {
    if (mt.value) {
      u.value = "";
      return;
    }
    u.value = "正在同步";
    try {
      const F = await e.requestHost("xb-tavern:list-chat-presets"), J = F.result || F;
      l.value = J, ur(J.active), u.value = "";
    } catch (F) {
      u.value = F instanceof Error ? F.message : String(F || "读取失败");
    }
  }
  async function Cd(F = k.value) {
    const J = String(F || "").trim(), me = String(i.value.promptManager?.name || i.value.name || "").trim();
    if (!J) {
      k.value = me;
      return;
    }
    if (J === me) {
      k.value = me;
      return;
    }
    if (mt.value && !mn("聊天预设", "切换")) {
      k.value = me;
      return;
    }
    u.value = "正在切换";
    try {
      const Te = await e.requestHost("xb-tavern:select-chat-preset", { payload: { promptManagerName: J } });
      ur(Te.result || Te), u.value = "", e.postToHost("xb-tavern:refresh-context", {});
    } catch (Te) {
      k.value = me, u.value = Te instanceof Error ? Te.message : String(Te || "切换失败");
    }
  }
  async function Tn() {
    if (!bo.value) {
      u.value = "未读取到当前角色顺序，请刷新后再保存";
      return;
    }
    if (!mt.value) return;
    u.value = "正在保存";
    const F = await e.requestHost("xb-tavern:save-chat-preset", { payload: i.value });
    if (F.ok === !1) {
      u.value = String(F.error || "保存失败");
      return;
    }
    ur(F.result), u.value = "", e.postToHost("xb-tavern:refresh-context", {});
  }
  async function hi(F = {}) {
    b.value = "正在同步";
    try {
      const J = await e.requestHost("xb-tavern:list-worldbook-sources", { payload: { context: e.effectiveContext.value } });
      D.value = J.result || J;
      const me = String(m.value || "").trim(), Te = !!me && Qt.value.some((vt) => vt.name === me), ot = Qt.value.find((vt) => vt.globalActive)?.name || Qt.value[0]?.name || "";
      m.value = F.keepSelection && Te ? m.value : ot, b.value = "", jr(m.value);
    } catch (J) {
      b.value = J instanceof Error ? J.message : String(J || "读取失败");
    }
  }
  async function ma() {
    const F = ++Re;
    I.value = "正在同步";
    try {
      const J = await e.requestHost("xb-tavern:get-global-worldbooks");
      if (F !== Re) return;
      const me = tI(J.result || J);
      $e && $e <= F && ($e = 0, x.value = !1), v.value = me.options, T.value = me.selected, I.value = "";
    } catch (J) {
      if (F !== Re) return;
      $e && $e <= F && ($e = 0, x.value = !1), I.value = J instanceof Error ? J.message : String(J || "读取失败");
    }
  }
  async function Vl(F = T.value) {
    const J = Array.from(new Set(F.map((Te) => String(Te || "").trim()).filter(Boolean))), me = ++Re;
    $e = me, T.value = J, x.value = !0, I.value = "正在保存";
    try {
      const Te = await e.requestHost("xb-tavern:set-global-worldbooks", { payload: { selected: J } });
      if (me !== Re) return;
      const ot = tI(Te.result || Te);
      $e === me && ($e = 0, x.value = !1), v.value = ot.options, T.value = ot.selected, I.value = "", e.postToHost("xb-tavern:refresh-context", {}), hi({ keepSelection: !0 });
    } catch (Te) {
      if (me !== Re) return;
      $e === me && ($e = 0, x.value = !1), I.value = Te instanceof Error ? Te.message : String(Te || "保存失败"), ma();
    }
  }
  function Ad(F, J) {
    const me = String(F || "").trim();
    if (!me || x.value) return;
    const Te = new Set(T.value);
    J ? Te.add(me) : Te.delete(me), Vl([...Te]);
  }
  async function jr(F = m.value) {
    const J = String(F || "").trim();
    if (!J) {
      P.value = null, L.value = "";
      return;
    }
    const me = J;
    L.value = "正在读取预览";
    try {
      const Te = await e.requestHost("xb-tavern:get-worldbook-preview", { payload: {
        name: me,
        limit: H.value
      } });
      if (String(m.value || "").trim() !== me) return;
      P.value = Nie(Te.result || Te), L.value = "";
    } catch (Te) {
      if (String(m.value || "").trim() !== me) return;
      P.value = null, L.value = Te instanceof Error ? Te.message : String(Te || "预览读取失败");
    }
  }
  function ga() {
    m.value && (H.value += qf, jr(m.value));
  }
  async function Kl(F) {
    const J = String(m.value || "").trim(), me = F?.uid === void 0 || F.uid === null ? "" : String(F.uid).trim();
    if (!J || !me) return;
    const Te = Qr(F);
    if (ye.value && V.value !== Te && !mn("世界书条目", "切换")) return;
    V.value = Te, we += 1;
    const ot = `${Te}:${we}`;
    Ve = ot, Z.value = "正在读取条目";
    try {
      const vt = await e.requestHost("xb-tavern:get-worldbook-entry", { payload: {
        name: J,
        uid: me
      } });
      if (String(m.value || "").trim() !== J || V.value !== Te || Ve !== ot) return;
      Ed(vt.result || vt), Z.value = "";
    } catch (vt) {
      if (V.value !== Te || Ve !== ot) return;
      Z.value = vt instanceof Error ? vt.message : String(vt || "条目读取失败");
    }
  }
  function wo() {
    G.value = null, z.value = "", V.value = "", Ve = "", Z.value = "";
  }
  function pi(F) {
    G.value && (G.value = eI({
      ...G.value,
      ...F
    }), Z.value && Z.value !== "正在保存" && (Z.value = ""));
  }
  async function fr() {
    const F = G.value, J = String(m.value || F?.worldbookName || "").trim();
    if (!(!F || !J || !F.uid) && ye.value) {
      Z.value = "正在保存";
      try {
        const me = await e.requestHost("xb-tavern:save-worldbook-entry", { payload: {
          name: J,
          uid: F.uid,
          entryHash: F.entryHash,
          draft: F
        } });
        Ed(me.result || me), Z.value = "已保存", await jr(J), e.postToHost("xb-tavern:refresh-context", {});
      } catch (me) {
        Z.value = me instanceof Error ? me.message : String(me || "保存失败");
      }
    }
  }
  async function So() {
    if (ar.value && !mn("正则", "刷新")) {
      je.value = "";
      return;
    }
    je.value = "正在读取";
    try {
      const F = await e.requestHost("xb-tavern:list-regex-scripts");
      De.value = F.result || F, cr(Ne.value.find((J) => J.key === Be.value) || Ne.value[0] || null), je.value = "";
    } catch (F) {
      je.value = F instanceof Error ? F.message : String(F || "读取失败");
    }
  }
  function zl(F) {
    ar.value && !mn("正则", "切换") || cr(F);
  }
  function Id(F) {
    ar.value && !mn("正则", "新建") || (it.value = Pv({
      scriptName: "新正则",
      findRegex: "",
      replaceString: "",
      trimStrings: [],
      placement: [],
      disabled: !1,
      markdownOnly: !1,
      promptOnly: !1,
      runOnEdit: !1,
      substituteRegex: 0,
      minDepth: null,
      maxDepth: null
    }), Qe.value = "", Be.value = `${F.scriptType}:new`);
  }
  function ps(F) {
    it.value = Pv({
      ...it.value,
      ...F
    });
  }
  function To(F, J) {
    const me = new Set((it.value.placement || []).map((Te) => Number(Te)));
    J ? me.add(F) : me.delete(F), ps({ placement: [...me] });
  }
  async function va() {
    const F = ct.value?.scriptType || Number(Be.value.split(":")[0]);
    if (Number.isFinite(F) && ar.value) {
      je.value = "正在保存";
      try {
        const J = await e.requestHost("xb-tavern:save-regex-script", { payload: {
          scriptType: F,
          script: it.value
        } }), me = J.result || J;
        De.value = me;
        const Te = String(me.savedScriptId || it.value.id || ""), ot = Number(me.savedScriptType ?? F);
        cr(Ne.value.find((vt) => vt.scriptType === ot && Te && vt.script.id === Te) || Ne.value.find((vt) => vt.script.scriptName === it.value.scriptName) || Ne.value[0] || null), je.value = "";
      } catch (J) {
        je.value = J instanceof Error ? J.message : String(J || "保存失败");
      }
    }
  }
  async function Eo() {
    const F = ct.value;
    if (F) {
      await ms(F);
      return;
    }
    const J = String(it.value.id || ""), me = Number(Be.value.split(":")[0]);
    !J || !Number.isFinite(me) || await ms({
      key: `${me}:${J}`,
      groupKey: "",
      groupLabel: "",
      scriptType: me,
      script: it.value
    });
  }
  async function ms(F) {
    if (ar.value && Be.value !== F.key && !mn("正则", "删除")) return;
    const J = String(F?.script.id || ""), me = F?.scriptType;
    if (!(!J || !Number.isFinite(me)) && window.confirm("删除这个正则脚本？")) {
      je.value = "正在删除";
      try {
        const Te = await e.requestHost("xb-tavern:delete-regex-script", { payload: {
          scriptType: me,
          id: J
        } });
        De.value = Te.result || Te, cr(Ne.value[0] || null), je.value = "";
      } catch (Te) {
        je.value = Te instanceof Error ? Te.message : String(Te || "删除失败");
      }
    }
  }
  function mi() {
    return Ya(e.tavernDisplaySettings.value);
  }
  function ya(F = {}) {
    const J = mi();
    Pe.value = J, F.updateCommitted !== !1 && (xe.value = Fa(J));
  }
  function Bn(F) {
    Pe.value = F, e.tavernDisplaySettings.value = Ya(F);
  }
  async function Im(F) {
    const J = ++ke;
    Se.value = !0, ue.value = "正在保存显示设置", Bn(F);
    try {
      if (await e.requestHost("xb-tavern:save-display-settings", { payload: F }), J !== ke) return;
      xe.value = Fa(mi()), ue.value = "显示设置已保存";
    } catch (me) {
      if (J !== ke) return;
      Bn(Fa(xe.value)), ue.value = me instanceof Error ? me.message : String(me || "保存失败");
    } finally {
      J === ke && (Se.value = !1);
    }
  }
  function Jl(F) {
    Im(Ya({
      ...Pe.value,
      ...F
    }));
  }
  function xm(F) {
    const J = Number(Pe.value.hiddenOutsideCount) || 5, me = Math.min(20, Math.max(1, J + F));
    me !== J && Jl({ hiddenOutsideCount: me });
  }
  function km(F) {
    const J = Number(Pe.value.loadBatchSize) || 20, me = Math.min(50, Math.max(5, J + F * 5));
    me !== J && Jl({ loadBatchSize: me });
  }
  function Yl(F) {
    const J = Die(F);
    W.value = J.users, ee.value = J.currentUserId;
  }
  async function xd() {
    be.value = !0, _e.value || (ue.value = "正在读取 USER 列表");
    try {
      const F = await e.requestHost("xb-tavern:list-users");
      Yl(F.result || F), _e.value || (ue.value = "");
    } catch (F) {
      ue.value = F instanceof Error ? F.message : String(F || "USER 列表读取失败");
    } finally {
      be.value = !1;
    }
  }
  async function Mm(F) {
    const J = String(F || "").trim();
    if (!(!J || J === ee.value || _e.value)) {
      _e.value = J, ue.value = "正在切换 USER";
      try {
        const me = await e.requestHost("xb-tavern:switch-user", { payload: { userId: J } });
        Yl(me.result || me), ue.value = ai.value ? `已切换到 ${ai.value.name}` : "USER 已切换", e.postToHost("xb-tavern:refresh-context", {});
      } catch (me) {
        ue.value = me instanceof Error ? me.message : String(me || "切换失败");
      } finally {
        _e.value = "";
      }
    }
  }
  async function Xl() {
    mt.value && (i.value = Kg(a.value), d.value = nt(a.value), u.value = "");
  }
  function oS(F, J) {
    i.value = Kg({
      ...i.value,
      [F]: {
        ...i.value[F] || {},
        ...J
      }
    });
  }
  function kd(F, J = {}) {
    oS("promptManager", {
      rawPreset: F,
      prompts: Array.isArray(F.prompts) ? F.prompts : [],
      promptOrder: Array.isArray(F.prompt_order) ? F.prompt_order : [],
      ...J
    });
  }
  function Md(F, J) {
    const me = String(F || "").trim();
    if (!me) return;
    const Te = Fa(Fs()), ot = ci().map((Dt) => ({ ...Dt })), vt = ot.findIndex((Dt) => String(Dt.identifier || Dt.id || "").trim() === me);
    vt >= 0 ? ot[vt] = {
      ...ot[vt],
      ...J,
      identifier: me
    } : ot.push({
      identifier: me,
      name: me,
      role: "system",
      content: "",
      ...J
    }), Te.prompts = ot, kd(Te);
  }
  function Pd(F) {
    const J = Fa(Fs()), me = Zr(), Te = Bs().map((Dt) => ({ ...Dt })), ot = String(me.activeCharacterId ?? "").trim();
    if (!ot) {
      u.value = "未读取到当前角色顺序，请刷新后再保存";
      return;
    }
    let vt = Te.findIndex((Dt) => String(Dt.character_id ?? "") === ot);
    vt < 0 && (vt = Te.length, Te.push({
      character_id: ot,
      order: []
    })), Te[vt] = {
      ...Te[vt],
      order: F
    }, J.prompt_order = Te, kd(J, { activeOrder: F });
  }
  function Rd(F = Fn.value) {
    return F.filter((J) => J.listed).map((J) => ({
      ...J.orderEntry,
      identifier: J.identifier,
      enabled: J.enabled
    }));
  }
  function Us(F, J) {
    const me = String(F || "").trim();
    if (!bo.value) {
      u.value = "未读取到当前角色顺序，请刷新后再保存";
      return;
    }
    if (!me) return;
    const Te = Fn.value;
    Pd(Rd(Te).map((ot) => String(ot.identifier || "").trim() === me ? {
      ...ot,
      ...J,
      identifier: me
    } : ot));
  }
  function Pm(F, J) {
    if (!bo.value) {
      u.value = "未读取到当前角色顺序，请刷新后再保存";
      return;
    }
    const me = Fn.value, Te = me.findIndex(($t) => $t.identifier === F), ot = Te + J;
    if (Te < 0 || ot < 0 || ot >= me.length) return;
    const vt = me.slice(), [Dt] = vt.splice(Te, 1);
    Dt && (vt.splice(ot, 0, Dt), Pd(Rd(vt)), h.value = F);
  }
  function Rm(F, J) {
    Us(F, { enabled: J });
  }
  function Nm(F = "") {
    const J = {
      system: "系统",
      user: "用户",
      assistant: "助手"
    }, me = String(F || "system").trim();
    return J[me] || me || "系统";
  }
  function Nd(F) {
    return Array.isArray(F) ? F.map((J) => String(J || "").trim()).filter(Boolean).join(`
`) : String(F || "");
  }
  function $m(F = "") {
    return String(F || "").split(/\r?\n/).map((J) => J.trim()).filter(Boolean);
  }
  function Dm(F) {
    return {
      1: "用户输入",
      2: "AI 回复",
      3: "斜杠命令",
      5: "世界书",
      6: "推理内容"
    }[F] || String(F);
  }
  function Lm(F) {
    return Ze.value.find((J) => J.scriptType === F);
  }
  function _a() {
    return Lm(ct.value?.scriptType || Number(Be.value.split(":")[0]))?.label || "正则";
  }
  function Ql(F) {
    p.value = {
      ...p.value,
      ...F
    };
  }
  function Om(F) {
    R.value = F;
  }
  function $d(F = "") {
    const J = fa.value;
    J && Ql({ [J.key]: String(F || "") });
  }
  async function Dd(F) {
    const J = String(F || "").trim();
    !J || J === E.value || ft.value && !mn("助手预设", "切换") || (await Ga(J), E.value = J, dr(await zg()), C.value = "");
  }
  async function qs() {
    if (!ft.value) return;
    const F = await qu(String(Sd.value?.id || "") === "littlewhitebox-assistant-default" ? {
      ...p.value,
      id: `assistant-preset-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
      name: `${p.value.name || "助手预设"} 自定义`
    } : p.value);
    await Ga(F.id), E.value = F.id, dr(F.preset), w.value = await Ma(), C.value = "";
  }
  async function gi() {
    const F = await qu({
      ...p.value,
      id: `assistant-preset-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
      name: `${p.value.name || "助手预设"} 副本`
    });
    await Ga(F.id), E.value = F.id, dr(F.preset), w.value = await Ma(), C.value = "";
  }
  async function ba() {
    if (ft.value && !mn("助手预设", "新建")) return;
    const F = await qu({
      ...Bc(),
      id: `assistant-preset-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
      name: "新助手预设",
      description: ""
    });
    await Ga(F.id), E.value = F.id, dr(F.preset), w.value = await Ma(), C.value = "";
  }
  async function wa(F) {
    if (ft.value && !mn("助手预设", "导入")) return !1;
    const J = F && typeof F == "object" ? F : {}, me = J.preset && typeof J.preset == "object" ? J.preset : J, Te = await qu({
      ...me,
      id: `assistant-preset-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
      name: String(me.name || J.name || "导入助手预设").trim() || "导入助手预设",
      description: String(me.description || J.description || "").trim()
    });
    return await Ga(Te.id), E.value = Te.id, dr(Te.preset), w.value = await Ma(), C.value = "", !0;
  }
  async function Ld() {
    const F = String(E.value || p.value.id || "").trim(), J = w.value.find((me) => me.id === F) || null;
    !J || J.id === "littlewhitebox-assistant-default" || ft.value && !mn("助手预设", "删除") || window.confirm(`删除「${J.name || "当前助手预设"}」？`) && (await hH(J.id), w.value = await Ma(), E.value = await Zh(), dr(await zg()), C.value = "");
  }
  async function Fm() {
    ft.value && (p.value = { ...y.value }, M.value = at(y.value), C.value = "");
  }
  function Zl() {
    Le.config = Sc(e.agentConfig.value || {}), Le.configDraft = null, Le.configFormSyncPending = !0;
  }
  function Bm(F = "") {
    s.value = {
      status: "saving",
      requestId: F,
      error: ""
    }, Le.configSave = s.value, o.value = "正在保存共享 API 配置...", Jt(Co);
  }
  function hr(F = "", J = {}) {
    F && s.value.requestId && F !== s.value.requestId || (s.value = {
      status: J.ok ? "success" : "error",
      requestId: F,
      error: J.error || ""
    }, Le.configSave = s.value, o.value = J.ok ? "" : `保存失败：${J.error || "unknown_error"}`, window.setTimeout(() => {
      s.value.requestId !== F || s.value.status !== "success" || (s.value = {
        status: "idle",
        requestId: "",
        error: ""
      }, Le.configSave = s.value, o.value = "", Jt(Co));
    }, 1400), Jt(Co));
  }
  function jl(F) {
    const J = String(F.requestId || `save-config-${Date.now()}`);
    Bm(J), e.postToHost("xb-tavern:save-config", {
      requestId: J,
      payload: F.payload || {}
    });
  }
  function Co() {
    const F = r.value;
    F && (tt || (tt = xie({
      state: Le,
      render: Co,
      describeError: e.describeError,
      showToast: (J) => {
        o.value = String(J || "");
      },
      saveConfig: jl,
      getRuntimeSummaryText: () => Mt.value
    })), Le.configSave = s.value, F.innerHTML = Mie({
      configSave: s.value,
      runtimeText: Mt.value,
      inlineToastText: o.value,
      showAssistantPermissions: !1,
      showDelegateSettings: !0,
      activePage: String(Le.configPage || "main"),
      delegatePresetHint: "记忆整理会复用这里的分身 API；当前聊天仍使用主 API。",
      isBusy: e.isRunning.value,
      canDeletePreset: Object.keys(Le.config?.presets || {}).length > 1
    }), tt.syncConfigToForm(F), tt.bindSettingsPanelEvents(F));
  }
  function Um(F) {
    if (F.ok === !0) {
      e.agentConfig.value = F.config || e.agentConfig.value, Zl(), hr(String(F.requestId || ""), { ok: !0 });
      return;
    }
    Zl(), hr(String(F.requestId || ""), {
      ok: !1,
      error: String(F.error || "保存失败")
    });
  }
  function eu(F) {
    "chatPreset" in F && ur(F.chatPreset, { replaceDraft: !mt.value }), "chatPresetList" in F && (l.value = F.chatPresetList || {});
  }
  function tu(F) {
    e.activeSettingsWorkspace.value = F, e.activeView.value = "settings";
  }
  function vi(F = "") {
    const J = String(F || "").trim();
    J && (m.value = J), tu("worldbooks");
  }
  function Sa(F) {
    const J = F;
    (J === "api" || J === "chatPreset" || J === "worldbooks" || J === "regex" || J === "assistantPreset" || J === "base") && tu(J);
  }
  return yt(Fn, (F) => {
    if (!F.length) {
      h.value = "";
      return;
    }
    F.some((J) => J.identifier === h.value) || (h.value = F[0]?.identifier || "");
  }, { immediate: !0 }), yt(O, () => {
    Y.value = Cv;
  }), yt(X, () => {
    j.value = Av;
  }), yt(re, () => {
    pe.value = Iv;
  }), yt(B, () => {
    N.value = xv;
  }), yt(le, () => {
    Ie.value = {};
  }), yt(() => e.tavernDisplaySettings.value, () => {
    ya({ updateCommitted: !Se.value });
  }, { immediate: !0 }), yt(li, (F) => {
    if (!F.length) {
      R.value = "";
      return;
    }
    F.some((J) => J.id === R.value) || (R.value = F[0]?.id || "");
  }, { immediate: !0 }), yt([
    () => e.activeSettingsWorkspace.value,
    () => e.activeView.value,
    () => s.value.status,
    () => e.agentConfig.value
  ], () => {
    e.activeView.value === "settings" && e.activeSettingsWorkspace.value === "api" && Jt(Co), e.activeView.value === "settings" && e.activeSettingsWorkspace.value === "regex" && !Ze.value.length && So();
  }), yt([e.activeView, e.activeSettingsWorkspace], ([F, J], [me, Te]) => {
    F === "settings" && J === "chatPreset" && (me !== F || Te !== J) && fi(), F === "settings" && J === "worldbooks" && (me !== F || Te !== J) && (hi({ keepSelection: !0 }), ma()), F === "settings" && J === "base" && (me !== F || Te !== J) && xd();
  }), yt(m, (F) => {
    e.activeView.value !== "settings" || e.activeSettingsWorkspace.value !== "worldbooks" || (wo(), H.value = qf, jr(F));
  }), {
    activeAssistantPreset: y,
    activeChatPreset: a,
    apiReady: At,
    apiReadyDetail: Ut,
    apiRuntimeLine: Mt,
    apiSettingsRootRef: r,
    applyHostChatPreset: eu,
    handleApiConfigSaved: Um,
    openSettingsWorkspace: tu,
    openWorldbookWorkspace: vi,
    loadTavernUsers: xd,
    refreshPresets: Am,
    refreshRegexFromHost: So,
    renderApiSettingsPanel: Co,
    selectSettingsWorkspace: Sa,
    settingsContext: {
      activeAssistantPresetId: E,
      activePromptOrderLabel: Em,
      activeSettingsWorkspace: e.activeSettingsWorkspace,
      activeView: e.activeView,
      apiReady: At,
      apiReadyDetail: Ut,
      apiRuntimeLine: Mt,
      apiSettingsRootRef: r,
      applyActiveRegexScript: cr,
      ASSISTANT_PRESET_BATCH_SIZE: Av,
      assistantPreset: p,
      assistantPresetDirty: ft,
      assistantPresetItems: li,
      assistantPresets: w,
      assistantPresetSearchText: X,
      assistantPresetStatus: C,
      assistantPresetVisibleLimit: j,
      canEditPromptOrder: bo,
      cancelWorldbookEntryEdit: wo,
      chatPresetOptions: qt,
      chatPresetSourceSearchText: O,
      chatPresetSourceVisibleLimit: Y,
      CHAT_PRESET_SOURCE_BATCH_SIZE: Cv,
      createAssistantPreset: ba,
      createRegexScript: Id,
      deleteCurrentAssistantPreset: Ld,
      deleteCurrentRegexScript: Eo,
      deleteRegexScript: ms,
      deriveAssistantPreset: gi,
      discardAssistantPresetChanges: Fm,
      discardPresetChanges: Xl,
      expandRegexGroup: Ye,
      filteredPromptEditorRows: ha,
      globalWorldbookOptions: v,
      globalWorldbookSelected: T,
      globalWorldbookSaving: x,
      globalWorldbookStatus: I,
      hiddenAssistantPresetCount: ln,
      hiddenChatPresetOptionCount: Or,
      hiddenPromptCount: wm,
      hiddenWorldbookCount: ae,
      hiddenWorldbookPreviewEntryCount: Ce,
      homeThemeDark: e.homeThemeDark,
      importAssistantPreset: wa,
      isEditingWorldbookEntry: ui,
      linesFromList: Nd,
      listFromLines: $m,
      movePromptRow: Pm,
      postToHost: e.postToHost,
      preset: i,
      presetDirty: mt,
      presetRows: Td,
      presetStatus: u,
      presetTotalChars: Cm,
      PROMPT_EDITOR_BATCH_SIZE: Iv,
      promptEditorRows: Fn,
      promptRoleDisplay: Nm,
      promptRowIndex: pa,
      promptSearchText: re,
      promptVisibleLimit: pe,
      refreshRegexFromHost: So,
      currentTavernUser: ai,
      currentTavernUserId: ee,
      displaySettings: Pe,
      REGEX_GROUP_BATCH_SIZE: kv,
      regexDirty: ar,
      regexDraft: it,
      regexDraftTypeLabel: _a,
      regexGroups: Ze,
      regexGroupsForDisplay: It,
      regexPlacementLabel: Dm,
      regexScriptRows: Ne,
      regexSearchText: le,
      regexStatus: je,
      saveCurrentAssistantPreset: qs,
      saveCurrentPreset: Tn,
      saveCurrentRegexScript: va,
      saveGlobalWorldbooksToHost: Vl,
      saveWorldbookEntryDraft: fr,
      selectAssistantPreset: Dd,
      selectAssistantPresetItem: Om,
      selectChatPresetFromHost: Cd,
      selectedAssistantPresetItem: fa,
      selectedPresetSourceId: k,
      selectedPromptIdentifier: h,
      selectedPromptRow: Tm,
      selectedRegexKey: Be,
      selectedRegexRow: ct,
      selectedWorldbook: Je,
      selectedWorldbookName: m,
      selectRegexScript: zl,
      selectSettingsWorkspace: Sa,
      settingsNavItems: Wl,
      shortText: e.shortText,
      showMoreWorldbookPreviewEntries: ga,
      stepHiddenOutsideCount: xm,
      stepLoadBatchSize: km,
      startWorldbookEntryEdit: Kl,
      switchingTavernUserId: _e,
      syncGlobalWorldbooksFromHost: ma,
      syncWorldbooksFromHost: hi,
      switchTavernUser: Mm,
      tavernUsers: W,
      togglePromptRow: Rm,
      toggleGlobalWorldbook: Ad,
      toggleRegexPlacement: To,
      updateAssistantPresetPatch: Ql,
      updateDisplaySettingsPatch: Jl,
      updatePromptByIdentifier: Md,
      updateRegexPatch: ps,
      updateSelectedAssistantPresetItem: $d,
      updateWorldbookEntryDraftPatch: pi,
      baseSettingsLoading: be,
      baseSettingsSaving: Se,
      baseSettingsStatus: ue,
      visibleAssistantPresetRecords: an,
      visibleChatPresetOptions: Yn,
      visiblePromptEditorRows: bm,
      visibleWorldbookOptions: fe,
      WORLDBOOK_BATCH_SIZE: xv,
      WORLDBOOK_PREVIEW_BATCH_SIZE: qf,
      worldbookEntryDirty: ye,
      worldbookEntryDraft: G,
      worldbookEntryEditingKey: V,
      worldbookEntrySaving: ge,
      worldbookEntryStatus: Z,
      worldbookOptions: Qt,
      worldbookPreview: P,
      worldbookPreviewStatus: L,
      worldbookPreviewVisibleLimit: H,
      worldbookSearchText: B,
      worldbookStatus: b,
      worldbookVisibleLimit: N
    },
    syncApiSettingsConfigFromAgentConfig: Zl,
    syncChatPresetFromHost: fi,
    syncWorldbooksFromHost: hi
  };
}
var Fie = { class: "xb-os-stage" }, Bie = { class: "character-worldbook-picker" }, Uie = {
  key: 0,
  class: "worldbook-picker-status"
}, qie = { class: "worldbook-picker-list" }, Hie = ["disabled", "onClick"], Gie = "xb-tavern-app", Wie = "xb-tavern-host", nI = 5e3, rI = 15e3, Hf = 48, sI = 6, Vie = 12, Kie = 12, zie = 36, oI = 48, Rv = 24, iI = 480, Jie = 200, aI = 1e9, lI = "LittleWhiteBox_Tavern_theme", Yie = 1e3 * 60 * 20, Xie = "配图已生成", Qie = /* @__PURE__ */ jt({
  __name: "App",
  setup(e) {
    const t = /* @__PURE__ */ se({}), n = /* @__PURE__ */ se({}), r = /* @__PURE__ */ se({}), s = /* @__PURE__ */ se(Ya({})), o = ie(() => Do(s.value.hiddenOutsideCount)), i = ie(() => sb(s.value.loadBatchSize)), a = /* @__PURE__ */ se({}), l = /* @__PURE__ */ se("15px"), u = /* @__PURE__ */ se("23px"), d = /* @__PURE__ */ se([]), h = /* @__PURE__ */ se(""), p = /* @__PURE__ */ se(""), y = /* @__PURE__ */ se(0), w = /* @__PURE__ */ se(""), E = /* @__PURE__ */ se(""), C = /* @__PURE__ */ se(null), M = /* @__PURE__ */ se(!1), k = /* @__PURE__ */ se(""), R = /* @__PURE__ */ se(!1), D = /* @__PURE__ */ se([]), m = /* @__PURE__ */ se(0), b = /* @__PURE__ */ se(""), v = /* @__PURE__ */ se("等待读取角色与会话"), T = /* @__PURE__ */ se(""), I = /* @__PURE__ */ se("raw"), x = /* @__PURE__ */ se(""), P = /* @__PURE__ */ se([]), L = /* @__PURE__ */ se(""), G = /* @__PURE__ */ se(""), z = /* @__PURE__ */ se(""), V = /* @__PURE__ */ se(""), Z = /* @__PURE__ */ se(!1), O = /* @__PURE__ */ se(!1), Y = /* @__PURE__ */ se({
      provider: "disabled",
      enabled: !1,
      ready: !1
    }), X = /* @__PURE__ */ se(""), j = /* @__PURE__ */ se(""), re = /* @__PURE__ */ se("idle"), pe = /* @__PURE__ */ se(""), B = /* @__PURE__ */ se([]), N = /* @__PURE__ */ se(""), H = /* @__PURE__ */ se([]), le = /* @__PURE__ */ se({}), Ie = /* @__PURE__ */ se([]), De = /* @__PURE__ */ se([]), Be = /* @__PURE__ */ se(null), it = /* @__PURE__ */ se(Qg()), Qe = /* @__PURE__ */ se(""), je = /* @__PURE__ */ se(null), W = /* @__PURE__ */ se(null), ee = /* @__PURE__ */ se(""), ue = /* @__PURE__ */ se(""), Se = /* @__PURE__ */ se(""), be = /* @__PURE__ */ se("preview"), _e = /* @__PURE__ */ se(""), Pe = /* @__PURE__ */ se("state"), xe = /* @__PURE__ */ se(null), ke = /* @__PURE__ */ se([]), we = /* @__PURE__ */ se(""), Ve = /* @__PURE__ */ se(""), Re = /* @__PURE__ */ se(""), $e = /* @__PURE__ */ se(""), Le = /* @__PURE__ */ se([]), tt = /* @__PURE__ */ se(!1), nt = /* @__PURE__ */ se(!1), at = /* @__PURE__ */ se(null), mt = /* @__PURE__ */ se([]), ft = /* @__PURE__ */ se(null), Pt = /* @__PURE__ */ se(""), At = /* @__PURE__ */ se(Hf), Ut = /* @__PURE__ */ se(""), Mt = /* @__PURE__ */ se({}), qt = /* @__PURE__ */ se(sI), Nt = /* @__PURE__ */ se({});
    function Yn() {
      const [g] = String(window.location.hash || "").replace(/^#\/?/, "").split("/");
      return g === "chat" || g === "characters" || g === "settings" || g === "about" ? g : "home";
    }
    function Or() {
      try {
        const g = globalThis.localStorage?.getItem(lI);
        if (g === "dark") return !0;
        if (g === "light") return !1;
      } catch {
      }
      return !1;
    }
    const Qt = /* @__PURE__ */ new Map();
    function te(g = "") {
      return String(g || "").trim().toLocaleLowerCase();
    }
    function fe(g, A) {
      const $ = Number.parseFloat(String(g || ""));
      return Number.isFinite($) && $ > 0 ? `${Math.round($ * 100) / 100}px` : A;
    }
    function ae(g = l.value) {
      const A = Number.parseFloat(String(g || ""));
      return Number.isFinite(A) && A > 0 ? `${Math.round((A + 8) * 100) / 100}px` : "23px";
    }
    function Je(g, A = 1600) {
      return g.map(($) => String($ ?? "").slice(0, A)).filter(Boolean).join(`
`);
    }
    function Ce(g) {
      return Array.isArray(g) ? g.map((A) => String(A || "").trim()).filter(Boolean) : [];
    }
    const ye = ie(() => B.value.find((g) => g.id === N.value) || null), ge = ie(() => nr(ye.value?.state || {})), Ze = ie(() => Wr(ge.value.contract)), Ne = ie(() => !!(ye.value && String(ye.value.characterId || "").trim())), oe = /* @__PURE__ */ se(Yn()), Ye = /* @__PURE__ */ se(Lie()), It = /* @__PURE__ */ se(Or()), ct = /* @__PURE__ */ se("chat"), ar = /* @__PURE__ */ se("balanced"), Wl = /* @__PURE__ */ se("memory"), ai = /* @__PURE__ */ se(null), li = /* @__PURE__ */ se(null), da = /* @__PURE__ */ se(null), an = H0({
      totalItems: () => vi.value.length,
      defaultLimit: o,
      loadBatchSize: i
    }), ln = H0({
      totalItems: () => Wm.value.length,
      defaultLimit: o,
      loadBatchSize: i
    }), Sd = an.scrollRef, fa = ln.scrollRef, bo = an.autoScroll, Fn = ln.autoScroll, ha = an.showScrollTop, bm = an.showScrollBottom, wm = an.scrollControlsActive, Sm = ln.showScrollTop, Tm = ln.showScrollBottom, Em = ln.scrollControlsActive, Td = an.messageWindowLimit, Cm = ln.messageWindowLimit, Qr = /* @__PURE__ */ se(""), ui = /* @__PURE__ */ se(""), pa = /* @__PURE__ */ se(!1), Zr = /* @__PURE__ */ se("history"), Fs = /* @__PURE__ */ se(""), ci = /* @__PURE__ */ se(""), Bs = /* @__PURE__ */ se(""), di = /* @__PURE__ */ se(""), lr = /* @__PURE__ */ se({}), ur = /* @__PURE__ */ se({}), cr = /* @__PURE__ */ se(null), dr = /* @__PURE__ */ se(null), mn = /* @__PURE__ */ se(null), Ed = ie(() => ({
      "--xb-host-main-font-size": l.value,
      "--xb-host-prose-line-height": u.value
    })), { clearMarkdownCache: Am, enhanceChatMarkdown: fi, enhanceManagerMarkdown: Cd, markdownSignature: Tn, renderChatMarkdown: hi, stripTavernImageMarkers: ma } = _2({
      chatScrollRef: Sd,
      managerScrollRef: fa,
      requestHost: Xn,
      imageRequestTimeoutMs: nI * 2
    }), Vl = /* @__PURE__ */ new Map(), Ad = /* @__PURE__ */ new WeakMap();
    let jr = null, ga = 0, Kl = 0, wo = 0, pi = null, fr = null, So = null, zl = !1, Id = 0;
    const ps = /* @__PURE__ */ new Set(), To = /* @__PURE__ */ new Map(), va = /* @__PURE__ */ new Map(), Eo = /* @__PURE__ */ new Map();
    let ms = 0;
    const mi = ie(() => ({
      ...t.value,
      history: N.value ? _d(H.value) : t.value.history
    })), { activeAssistantPreset: ya, activeChatPreset: Bn, applyHostChatPreset: Im, handleApiConfigSaved: Jl, loadTavernUsers: xm, openSettingsWorkspace: km, openWorldbookWorkspace: Yl, refreshPresets: xd, refreshRegexFromHost: Mm, renderApiSettingsPanel: Xl, selectSettingsWorkspace: oS, settingsContext: kd, syncApiSettingsConfigFromAgentConfig: Md, syncChatPresetFromHost: Pd, syncWorldbooksFromHost: Rd } = Oie({
      activeView: oe,
      activeSettingsWorkspace: Ye,
      agentConfig: r,
      tavernDisplaySettings: s,
      effectiveContext: mi,
      homeThemeDark: It,
      isRunning: Z,
      describeError: Ta,
      postToHost: gs,
      requestHost: Xn,
      shortText: ig
    }), Us = ie(() => mi.value.character || {}), Pm = ie(() => hr(Us.value.name || "", "未选择角色")), Rm = ie(() => {
      const g = String(Us.value.avatar || "").trim(), A = String(Us.value.id || h.value || "").trim(), $ = String(Us.value.name || "").trim(), K = d.value.find((he) => String(he.id || "").trim() === A) || d.value.find((he) => String(he.name || "").trim() === $), ne = [g, String(K?.avatar || "").trim()].filter((he, Ee, ze) => he && ze.indexOf(he) === Ee);
      return ne.find((he) => !Nt.value[he]) || ne[0] || "";
    }), Nm = ie(() => {
      const g = Rm.value;
      return g && !Nt.value[g] ? g : "";
    }), Nd = ie(() => mi.value.user || {}), $m = ie(() => hr(Nd.value.name || "", "User")), Dm = ie(() => String(Nd.value.avatar || "").trim()), Lm = ie(() => {
      const g = Dm.value;
      return g && !Nt.value[g] ? g : "";
    }), _a = ie(() => t.value.character || {}), Ql = ie(() => String(_a.value.id || h.value || "").trim());
    function Om(g = "") {
      const A = String(g || "").trim();
      return !!A && !Nt.value[A];
    }
    function $d(g = "") {
      const A = String(g || "").trim();
      !A || Nt.value[A] || (Nt.value = {
        ...Nt.value,
        [A]: !0
      });
    }
    function Dd(g, A = "") {
      const $ = String(g.id || A || "").trim();
      if (!$) return null;
      const K = String(g.name || "").trim() || `角色 ${$}`, ne = String(g.avatar || "").trim(), he = g.shallow === !0, Ee = String(g.description || "").trim(), ze = String(g.personality || "").trim(), Me = String(g.scenario || "").trim(), He = String(g.firstMessage || g.first_mes || "").trim(), gn = Ce(g.alternateGreetings || g.alternate_greetings), es = String(g.mesExample || g.mes_example || "").trim(), Ea = String(g.creatorNotes || g.creator_notes || "").trim(), Et = String(g.characterDepthPrompt || g.character_depth_prompt || "").trim(), Si = JSON.stringify([
        $,
        K,
        ne,
        he,
        Ee,
        ze,
        Me,
        He,
        gn,
        es,
        Ea,
        Et
      ]), ZS = Vl.get($);
      if (ZS?.signature === Si) return ZS.option;
      const gr = {
        id: $,
        name: K,
        avatar: ne,
        shallow: he,
        description: Ee,
        personality: ze,
        scenario: Me,
        firstMessage: He,
        alternateGreetings: gn,
        mesExample: es,
        creatorNotes: Ea,
        characterDepthPrompt: Et
      };
      return gr.searchCorpus = te(Je([
        gr.id,
        gr.name,
        gr.description,
        gr.personality,
        gr.scenario,
        gr.firstMessage,
        ...gr.alternateGreetings || [],
        gr.mesExample,
        gr.creatorNotes,
        gr.characterDepthPrompt
      ], 900)), Vl.set($, {
        signature: Si,
        option: gr
      }), gr;
    }
    const qs = ie(() => {
      const g = /* @__PURE__ */ new Map();
      d.value.forEach(($) => {
        const K = Dd($);
        K && g.set(K.id, K);
      });
      const A = String(_a.value.id || "").trim();
      if (A && _a.value.name && !g.has(A)) {
        const $ = Dd(_a.value, A);
        $ && g.set(A, $);
      }
      return [...g.values()].sort(($, K) => $.name.localeCompare(K.name, "zh-Hans-CN"));
    }), gi = ie(() => {
      const g = te(Pt.value);
      return g ? qs.value.filter((A) => String(A.searchCorpus || "").includes(g)) : qs.value;
    }), ba = ie(() => {
      const g = gi.value.slice(0, At.value), A = String(p.value || "").trim();
      if (!A || g.some((K) => K.id === A)) return g;
      const $ = qs.value.find((K) => K.id === A);
      return $ ? [$, ...g] : g;
    }), wa = ie(() => {
      const g = String(p.value || "").trim();
      if (g) {
        const A = qs.value.find(($) => $.id === g);
        if (A) return A;
      }
      return null;
    }), Ld = ie(() => {
      const g = wa.value;
      return g ? [String(g.firstMessage || "").trim(), ...g.alternateGreetings || []].filter(Boolean) : [];
    }), Fm = ie(() => Math.max(0, gi.value.length - Math.min(gi.value.length, At.value)));
    function Zl(g = "") {
      return String(g || "").replace(/\s*[·-]\s*小白酒馆\s*$/g, "").replace(/\s*[·-]\s*会话\s*$/g, "").replace(/^小白酒馆会话$/g, "").replace(/\s*·\s*第\s*\d+\s*轮\s*·\s*\d+\s*条可用消息\s*$/g, "").trim();
    }
    function Bm(g = "") {
      return /^(sillytavern\s+system|system)\b/i.test(String(g || "").trim());
    }
    function hr(g = "", A = "") {
      const $ = Zl(g);
      return $ && !Bm($) ? $ : A;
    }
    function jl(g) {
      if (!g) return "";
      const A = hr(g.characterName || "");
      if (A) return A;
      const $ = hr(g.title || "");
      return $ || "";
    }
    const Co = ie(() => jl(ye.value) || hr(Us.value.name || "") || "未选择角色"), Um = ie(() => N.value ? Co.value : hr(Pm.value) || "未选择角色"), eu = ie(() => ye.value?.state?.lastRequestSnapshot), tu = ie(() => String(eu.value?.rawRequestJson || eu.value?.rawMessagesJson || "")), vi = ie(() => H.value), Sa = ie(() => Wv({ uiMessageWindowLimit: Td.value }, vi.value.length, { defaultLimit: o.value })), F = ie(() => vi.value.slice(Sa.value.startIndex)), J = ie(() => G.value), me = ie(() => {
      const g = [...H.value].sort((A, $) => A.order - $.order).at(-1);
      return g?.error ? `${g.sessionId}:${g.order}:${g.content || ""}` : "";
    }), { archivedManagerRuns: Te, currentManagerWorkRun: ot, formatRunActivityLine: vt, formatRunIssueLine: Dt, formatRunInputLine: $t, formatRunMapLine: qm, formatRunMemoryLine: WN, formatRunModelLine: VN, hiddenManagerRunCount: KN, isManagerRunLive: zN, managerBusy: iS, managerRunTone: JN, managerStatusClock: YN, managerStatusLabel: XN, managerToolStatusLabel: QN, managerToolTone: ZN, managerToolTraceItems: jN, toolTraceSummary: e$ } = Xte({
      managerRuns: Ie,
      visibleRunLimit: Kie
    }), aS = ie(() => {
      const g = String(ye.value?.characterId || mi.value.character?.id || "").trim();
      return g ? B.value.filter((A) => String(A.characterId || "").trim() === g) : B.value;
    }), Od = ie(() => {
      const g = aS.value, A = g.slice(0, qt.value);
      if (N.value && !A.some(($) => $.id === N.value)) {
        const $ = g.find((K) => K.id === N.value);
        $ && A.unshift($);
      }
      return A;
    }), Hm = ie(() => aS.value.length), t$ = ie(() => Math.max(0, Hm.value - Od.value.length));
    function n$(g = "", A = 0) {
      const $ = String(g || "").trim();
      if (!$) return;
      const K = Math.max(0, Math.floor(Number(A) || 0));
      le.value[$] !== K && (le.value = {
        ...le.value,
        [$]: K
      });
    }
    function r$(g = "") {
      const A = String(g || "").trim();
      if (!A || !(A in le.value)) return;
      const $ = { ...le.value };
      delete $[A], le.value = $;
    }
    function s$(g) {
      const A = String(g?.id || "").trim();
      return A ? A === N.value ? vi.value.length : Math.max(0, Number(le.value[A]) || 0) : 0;
    }
    function lS(g) {
      return `第 ${s$(g)} 楼`;
    }
    async function o$() {
      const g = Od.value.map((ne) => String(ne.id || "").trim()).filter(Boolean), A = [...new Set(g)].filter((ne) => ne !== N.value && !(ne in le.value));
      if (!A.length) return;
      const $ = await Promise.all(A.map(async (ne) => [ne, await rH(ne)])), K = { ...le.value };
      $.forEach(([ne, he]) => {
        ne === N.value || ne in le.value || (K[ne] = Math.max(0, Math.floor(Number(he) || 0)));
      }), le.value = K;
    }
    const Fd = ie(() => De.value.filter((g) => g.status !== "stale")), nu = ie(() => De.value.find((g) => g.path === Qe.value) || De.value[0] || null), uS = ie(() => je.value?.sessionId === N.value && je.value?.path === nu.value?.path ? je.value : null);
    function Gm(g) {
      const A = typeof g == "string" ? g : String(g?.path || "");
      if (A === "memory/session.md") return "剧情脉络";
      if (A === "memory/state.md") return "状态栏";
      const $ = A.match(/^memory\/turns\/(\d{8})-(\d+)\.md$/);
      if ($) {
        const K = Number($[2]) || 0;
        return K > 0 ? `第 ${K} 楼小记` : "楼层小记";
      }
      return A.replace(/^memory\//, "").replace(/\.md$/i, "") || "记忆档案";
    }
    function cS(g) {
      const A = typeof g == "string" ? g : String(g?.path || "");
      return A === "memory/session.md" ? "剧情为什么走到现在" : A === "memory/state.md" ? "当前事实与状态" : A.startsWith("memory/turns/") ? "楼层小记" : "记忆档案";
    }
    function dS(g = "") {
      return g === "memory/session.md" ? 0 : g === "memory/state.md" ? 1 : g.startsWith("memory/turns/") ? 20 : 30;
    }
    function i$(g) {
      const A = Ad.get(g);
      if (A) return A;
      const $ = te([
        g.path,
        Gm(g),
        cS(g),
        g.status,
        g.searchText || g.preview || ""
      ].filter(Boolean).join(`
`));
      return Ad.set(g, $), $;
    }
    function fS(g = "") {
      const A = g === "turns" ? zie : Rv, $ = Number(Mt.value[g]);
      return Number.isFinite($) && $ > 0 ? Math.floor($) : A;
    }
    function a$(g = "") {
      const A = fS(g);
      Mt.value = {
        ...Mt.value,
        [g]: A + (g === "turns" ? oI : Rv)
      };
    }
    const l$ = ie(() => {
      const g = [{
        key: "core",
        title: "核心档案",
        files: []
      }, {
        key: "turns",
        title: "楼层小记",
        files: []
      }], A = [];
      [...De.value].sort((he, Ee) => dS(he.path) - dS(Ee.path) || String(he.path || "").localeCompare(String(Ee.path || ""))).forEach((he) => {
        ["memory/session.md", "memory/state.md"].includes(he.path) ? g[0].files.push(he) : he.path.startsWith("memory/turns/") ? g[1].files.push(he) : A.push(he);
      });
      const $ = g.filter((he) => he.files.length);
      A.length && $.push({
        key: "other",
        title: "其他档案",
        files: A
      });
      const K = te(Ut.value), ne = String(Qe.value || "").trim();
      return $.map((he) => {
        const Ee = K ? he.files.filter((He) => i$(He).includes(K)) : he.files, ze = fS(he.key), Me = Ee.slice(0, ze);
        if (ne && !Me.some((He) => He.path === ne)) {
          const He = Ee.find((gn) => gn.path === ne);
          He && Me.unshift(He);
        }
        return {
          ...he,
          files: Me,
          totalCount: he.files.length,
          filteredCount: Ee.length,
          hiddenCount: Math.max(0, Ee.length - Me.length)
        };
      }).filter((he) => he.filteredCount || !K);
    }), hS = ie(() => !!nu.value || !!ue.value), u$ = ie(() => !1), ru = ie(() => !!ue.value && ee.value !== Se.value), c$ = ie(() => {
      const g = it.value;
      if (g.status !== "ready")
        return g.status === "failed" ? `记忆检索准备失败：${g.error || "memory_tokenizer_failed"}` : g.status === "loading" ? "记忆检索准备中" : "记忆检索尚未准备好";
      const A = Be.value;
      return A ? A.status === "ready" ? "记忆可检索" : A.status === "failed" ? `记忆整理失败：${A.error || "memory_index_failed"}` : "记忆正在整理" : "还没有可检索记忆";
    }), d$ = ie(() => F.value.map((g) => `${g.sessionId}:${g.order}:${g.error ? 1 : 0}:${Tn(g.content)}`).join("|")), f$ = ie(() => {
      const g = [...H.value].filter((A) => Ym(A)).sort((A, $) => A.order - $.order);
      return g.reduce((A, $, K) => (A[Un($)] = Math.max(0, g.length - K - 1), A), {});
    }), h$ = ie(() => P.value.map((g, A) => `${A}:${Tn(String(g.label || ""))}:${Tn(String(g.text || ""))}`).join("|")), p$ = ie(() => mt.value.map((g, A) => [
      A,
      g.toolCallId || "",
      g.stat,
      g.action,
      g.roll,
      g.difficulty,
      g.insertAfterChars,
      g.success ? 1 : 0
    ].join(":")).join("|")), Wm = ie(() => zA(Le.value)), m$ = ie(() => {
      const g = at.value;
      return !g || g.sessionId !== N.value ? [] : g.draft ? [...g.messages, g.draft] : g.messages;
    }), pS = ie(() => zA(m$.value)), Vm = ie(() => Wv({ uiMessageWindowLimit: Cm.value }, Wm.value.length, { defaultLimit: o.value })), Km = ie(() => Wm.value.slice(Vm.value.startIndex)), g$ = ie(() => Km.value.filter((g) => g.kind === "message").map((g) => g.message)), v$ = ie(() => Km.value.map((g) => g.kind === "message" ? `${g.message.sessionId}:${g.message.order}:${Tn(g.message.content)}` : `${g.key}:${Tn(g.assistantMessage.content)}:${g.calls.map((A) => `${A.id}:${Tn(A.resultText)}`).join(",")}`).join("|")), y$ = ie(() => pS.value.map((g) => g.kind === "message" ? `${g.message.sessionId}:${g.message.order}:${Tn(g.message.content)}` : `${g.key}:${Tn(g.assistantMessage.content)}:${g.calls.map((A) => `${A.id}:${Tn(A.resultText)}`).join(",")}`).join("|")), mS = ie(() => N.value ? lS(ye.value) : "写一句话后会自动创建独立会话。"), _$ = ie(() => !O.value && (Z.value || !!T.value.trim())), b$ = ie(() => !nt.value && (tt.value || !!N.value && !!Re.value.trim()));
    function w$(g) {
      const A = /* @__PURE__ */ new WeakSet();
      try {
        return JSON.parse(JSON.stringify(g, ($, K) => {
          if (typeof K == "bigint") return String(K);
          if (!(typeof K == "function" || typeof K == "symbol")) {
            if (!K || typeof K != "object") return K;
            if (!A.has(K))
              return A.add(K), K;
          }
        }));
      } catch {
        return {};
      }
    }
    yt(Pt, () => {
      At.value = Hf;
    }), yt(gi, (g) => {
      if (!g.length) {
        p.value = "";
        return;
      }
      const A = String(p.value || "").trim();
      A && g.some(($) => $.id === A) || (p.value = "");
    }, { immediate: !0 }), yt(p, () => {
      y.value = 0, C.value = null, k.value = "", R.value = !1, D.value = [], oe.value === "characters" && iu();
    }), yt([
      oe,
      p,
      Ql
    ], ([g, A]) => {
      if (g !== "characters") return;
      const $ = String(A || "").trim();
      $ && rg($);
    }), yt(Ld, (g) => {
      if (!g.length) {
        y.value = 0;
        return;
      }
      const A = g.length - 1;
      y.value > A && (y.value = A);
    }), yt(Ut, () => {
      Mt.value = {};
    }), yt([() => N.value, () => H.value.length], ([g, A]) => {
      n$(String(g || ""), Number(A) || 0);
    }), yt(() => Od.value.map((g) => g.id).join("|"), () => {
      o$();
    }, { immediate: !0 });
    function Ta(g) {
      return g instanceof Error ? g.message : String(g || "unknown_error");
    }
    function zm() {
      fr && (window.clearTimeout(fr), fr = null), G.value = "";
    }
    function su(g = "", A = 4200) {
      const $ = String(g || "").trim();
      if (!$) {
        zm();
        return;
      }
      fr && (window.clearTimeout(fr), fr = null), G.value = $, fr = window.setTimeout(() => {
        fr = null, G.value === $ && (G.value = "");
      }, A);
    }
    function gs(g, A = {}) {
      const $ = w$(A);
      window.parent?.postMessage({
        source: Gie,
        type: g,
        payload: $
      }, window.location.origin);
    }
    function S$(g = "host") {
      return `${g}-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
    }
    function gS() {
      try {
        return new DOMException("request_aborted", "AbortError");
      } catch {
        return /* @__PURE__ */ new Error("request_aborted");
      }
    }
    function Xn(g, A = {}, $ = {}) {
      const K = S$();
      return $.signal?.aborted ? Promise.reject(gS()) : (gs(g, {
        ...A,
        requestId: K
      }), new Promise((ne, he) => {
        const Ee = () => {
          const He = Qt.get(K);
          He?.abort && $.signal && $.signal.removeEventListener("abort", He.abort), Qt.delete(K);
        }, ze = window.setTimeout(() => {
          Ee(), he(/* @__PURE__ */ new Error("host_request_timeout"));
        }, Number($.timeoutMs) || nI), Me = () => {
          window.clearTimeout(ze), Ee(), gs("xb-tavern:cancel-request", { requestId: K }), he(gS());
        };
        $.signal && $.signal.addEventListener("abort", Me, { once: !0 }), Qt.set(K, {
          resolve: ne,
          reject: he,
          timer: ze,
          type: g,
          abort: $.signal ? Me : void 0,
          signal: $.signal
        });
      }));
    }
    function T$(g = "", A = "") {
      const $ = String(g || ""), K = String(A || "").trim();
      if (!$ || !K) return -1;
      const ne = $.indexOf(K);
      if (ne >= 0) return ne + K.length;
      const he = $.replace(/\s+/g, ""), Ee = K.replace(/\s+/g, "");
      if (!Ee) return -1;
      const ze = he.indexOf(Ee);
      if (ze < 0) return -1;
      let Me = 0;
      for (let He = 0; He < $.length; He += 1)
        if (!/\s/.test($[He]) && (Me += 1, Me >= ze + Ee.length))
          return He + 1;
      return -1;
    }
    function E$(g = "", A = -1) {
      const $ = String(g || "");
      if (A < 0 || !$) return A;
      if (A >= $.length) return $.length;
      const K = 80, ne = Math.min($.length, A + K), he = /* @__PURE__ */ new Set([
        "。",
        "！",
        "？",
        "!",
        "?",
        "…"
      ]), Ee = /* @__PURE__ */ new Set([
        "”",
        "“",
        "’",
        "‘",
        "」",
        "』",
        "】",
        "）",
        ")",
        '"',
        "'",
        "*",
        "~",
        "～",
        "]"
      ]), ze = (Me) => {
        let He = Me;
        for (; He < $.length && Ee.has($[He]); ) He += 1;
        return He;
      };
      if (A > 0 && he.has($[A - 1])) return ze(A);
      for (let Me = 0; Me < K && A + Me < ne; Me += 1) {
        const He = A + Me, gn = $[He];
        if (gn === `
`) return He + 1;
        if (he.has(gn)) return ze(He + 1);
        if (gn === "." && $.slice(He, He + 3) === "...") return ze(He + 3);
      }
      return A;
    }
    function C$(g = "", A = {}) {
      const $ = String(A.slotId || "").trim();
      if (!$) return {
        content: g,
        inserted: !1,
        appended: !1
      };
      const K = `[tavern-image:${$}]`, ne = String(g || "");
      if (ne.includes(K)) return {
        content: ne,
        inserted: !1,
        appended: !1
      };
      let he = T$(ne, String(A.anchor || ""));
      if (he >= 0 && (he = E$(ne, he)), he >= 0) {
        const Ee = ne.slice(0, he), ze = ne.slice(he);
        let Me = K;
        return Ee.length > 0 && !Ee.endsWith(`
`) && (Me = `
${Me}`), ze.length > 0 && !ze.startsWith(`
`) && (Me = `${Me}
`), {
          content: `${Ee}${Me}${ze}`,
          inserted: !0,
          appended: !1
        };
      }
      return {
        content: `${ne}${ne.length > 0 && !ne.endsWith(`
`) ? `
` : ""}${K}`,
        inserted: !0,
        appended: !0
      };
    }
    function A$(g = "", A = []) {
      let $ = String(g || ""), K = 0, ne = 0;
      return (Array.isArray(A) ? A : []).forEach((he) => {
        const Ee = he && typeof he == "object" ? he : {};
        if (!Ee.slotId || Ee.success === !1) return;
        const ze = C$($, Ee);
        $ = ze.content, ze.inserted && (K += 1), ze.appended && (ne += 1);
      }), {
        content: $,
        inserted: K,
        appended: ne
      };
    }
    function I$(g = "", A = {}) {
      const $ = Number(A.current) || 0, K = Number(A.total) || 0, ne = K ? ` ${$}/${K}` : "";
      switch (g) {
        case "llm":
          return "正在分析画面";
        case "gen":
          return K ? `准备生成 ${K} 张图` : "准备生成图片";
        case "queued":
          return Number(A.ahead) > 0 ? `画图排队中，前方 ${Number(A.ahead)} 个任务` : `画图排队中${ne}`;
        case "progress":
          return `正在生成图片${ne}`;
        case "cooldown": {
          const he = Number.isFinite(Number(A.remainingMs)) ? Number(A.remainingMs) : Number(A.duration);
          return !Number.isFinite(he) || he <= 0 ? "等待画图冷却" : `等待下一张图片${K ? ` ${A.nextIndex || $}/${K}` : ""}，剩余 ${(he / 1e3).toFixed(1)}s`;
        }
        case "success": {
          const he = Number(A.success) || 0, Ee = K || he;
          return Ee > 0 && he === 0 ? `画图结束，${Ee} 张都失败` : `画图完成 ${he}/${Ee}`;
        }
        default:
          return "正在处理画图";
      }
    }
    async function x$() {
      try {
        const g = await Xn("xb-tavern:draw-status", {});
        Y.value = {
          provider: String(g.provider || "disabled"),
          enabled: g.enabled === !0,
          ready: g.ready === !0
        };
      } catch {
        Y.value = {
          provider: "disabled",
          enabled: !1,
          ready: !1
        };
      }
      return Y.value;
    }
    async function ou(g) {
      if (!g.length) return {
        items: [],
        changedCount: 0
      };
      const A = await Xn("xb-tavern:apply-regex", { payload: { items: g } }), $ = A.result || A;
      return {
        items: Array.isArray($.items) ? $.items : [],
        changedCount: Number($.changedCount) || 0
      };
    }
    function vS() {
      To.forEach((g) => window.clearTimeout(g.timer)), To.clear(), va.clear(), Eo.clear();
    }
    function yS() {
      ms += 1, ps.clear(), vS(), ur.value = {};
    }
    function _S(g, A) {
      const $ = {
        ...ur.value,
        [g]: A
      }, K = Object.keys($);
      K.length > iI && K.slice(0, K.length - iI).forEach((ne) => {
        delete $[ne];
      }), ur.value = $, oe.value === "chat" && ct.value === "chat" && Jt(() => {
        fi(), mg();
      });
    }
    function Bd(g, A) {
      const $ = Tx(g, A.actionCheckEvents || [], A.actionCheckBoundaries || []);
      return {
        text: $.text,
        actionCheckEvents: $.events
      };
    }
    function bS(g, A, $, K) {
      _S(A, $), Eo.set(g, Bd($, K));
    }
    function Jm(g) {
      return g.role === "user" ? "userInput" : g.role === "assistant" ? "aiOutput" : null;
    }
    function Ym(g) {
      return ["user", "assistant"].includes(g.role) && !g.error && !!String(g.content || "").trim();
    }
    function Xm(g) {
      const A = Number(f$.value[Un(g)]);
      return Number.isFinite(A) && A >= 0 ? A : 0;
    }
    function Qm(g) {
      return String(g.name || Gd(g.role) || "").trim();
    }
    function Zm(g, A, $) {
      return [
        g,
        A.sessionId,
        String(A.order),
        A.role,
        String(A.name || ""),
        String($.index ?? ""),
        $.placement,
        String($.depth),
        $.characterOverride || "",
        $.actionCheckSignature || "",
        Tn(String($.label || "")),
        Tn($.text)
      ].join("");
    }
    function wS(g, A) {
      const $ = [...H.value].sort((K, ne) => K.order - ne.order).at(-1)?.order ?? -1;
      return [
        "runtime",
        g,
        N.value,
        String($),
        "assistant",
        String(A.index ?? ""),
        A.placement,
        String(A.depth),
        A.characterOverride || "",
        A.actionCheckSignature || "",
        Tn(String(A.label || "")),
        Tn(A.text)
      ].join("");
    }
    function SS(g = []) {
      return Wo(g).map((A, $) => [
        $,
        A.toolCallId || "",
        A.createdAt || "",
        A.stat,
        A.action,
        A.roll,
        A.difficulty,
        A.insertAfterChars,
        A.success ? 1 : 0,
        A.summary || "",
        A.stakes || ""
      ].join(":")).join("|");
    }
    async function jm(g) {
      if (ps.has(g.key)) return;
      const A = ms;
      ps.add(g.key);
      try {
        const $ = await ou([{
          id: g.key,
          text: g.text,
          placement: g.placement,
          options: g.options
        }]);
        if (A !== ms) return;
        const K = $.items.find((ne) => ne.id === g.key) || $.items[0];
        _S(g.key, K?.text ?? g.text);
      } catch ($) {
        console.warn("[小白酒馆] 显示正则应用失败", $);
      } finally {
        ps.delete(g.key);
      }
    }
    async function k$(g, A) {
      if (ps.has(A.key)) return;
      const $ = ms;
      ps.add(A.key);
      try {
        const K = await ou([{
          id: A.key,
          text: A.text,
          placement: A.placement,
          options: A.options
        }]);
        if ($ !== ms || va.get(g) !== A.key) return;
        const ne = K.items.find((he) => he.id === A.key) || K.items[0];
        bS(g, A.key, ne?.text ?? A.text, A);
      } catch (K) {
        console.warn("[小白酒馆] 生成中显示正则应用失败", K), $ === ms && va.get(g) === A.key && bS(g, A.key, A.text, A);
      } finally {
        ps.delete(A.key);
      }
    }
    function TS(g, A) {
      va.set(g, A.key);
      const $ = To.get(g);
      if ($) {
        $.key = A.key, $.input = A;
        return;
      }
      const K = window.setTimeout(() => {
        const ne = To.get(g);
        ne && (To.delete(g), k$(g, ne.input));
      }, Jie);
      To.set(g, {
        timer: K,
        key: A.key,
        input: A
      });
    }
    function ES(g) {
      const A = String(g.content || "");
      if (!A) return "";
      if (!Ym(g)) return A;
      const $ = Jm(g);
      if (!$) return A;
      const K = Xm(g), ne = Qm(g), he = Zm("message", g, {
        placement: $,
        text: A,
        depth: K,
        characterOverride: ne
      }), Ee = ur.value[he];
      return Ee !== void 0 ? Ee : (jm({
        key: he,
        text: A,
        placement: $,
        options: {
          isMarkdown: !0,
          depth: K,
          characterOverride: ne
        }
      }), A);
    }
    function M$(g) {
      const A = String(g.content || "");
      if (!A) return {
        text: "",
        actionCheckEvents: []
      };
      const $ = g.role === "assistant" ? Wo(g.runtimeEvents) : [];
      if (!$.length) return {
        text: ES(g),
        actionCheckEvents: []
      };
      if (!Ym(g)) return {
        text: A,
        actionCheckEvents: $
      };
      const K = Jm(g);
      if (!K) return {
        text: A,
        actionCheckEvents: $
      };
      const ne = Kv(A, $), he = Xm(g), Ee = Qm(g), ze = Zm("message", g, {
        placement: K,
        text: ne.text,
        depth: he,
        characterOverride: Ee,
        actionCheckSignature: SS($)
      }), Me = {
        key: ze,
        text: ne.text,
        placement: K,
        options: {
          isMarkdown: !0,
          depth: he,
          characterOverride: Ee
        },
        actionCheckEvents: $,
        actionCheckBoundaries: ne.boundaries
      }, He = ur.value[ze];
      return He !== void 0 ? Bd(He, Me) : (jm(Me), Bd(ne.text, Me));
    }
    function P$(g) {
      const A = Xm(g);
      return _i(g).map(($, K) => {
        const ne = String($.text || "");
        if (!ne) return $;
        const he = Zm("reasoning", g, {
          placement: "reasoning",
          text: ne,
          depth: A,
          index: K,
          label: $.label
        }), Ee = ur.value[he];
        return Ee !== void 0 ? {
          ...$,
          text: Ee
        } : (jm({
          key: he,
          text: ne,
          placement: "reasoning",
          options: {
            isMarkdown: !0,
            depth: A
          }
        }), $);
      }).filter(($) => String($.text || "").trim());
    }
    function CS(g = "", A = []) {
      const $ = String(g || ""), K = Wo(A);
      if (!$ && !K.length) return {
        text: "",
        actionCheckEvents: []
      };
      if (!$) return {
        text: "",
        actionCheckEvents: K
      };
      const ne = Kv($, K), he = 0, Ee = String(Gd("assistant") || "").trim(), ze = wS("message", {
        placement: "aiOutput",
        text: ne.text,
        depth: he,
        characterOverride: Ee,
        actionCheckSignature: SS(K)
      }), Me = {
        key: ze,
        text: ne.text,
        placement: "aiOutput",
        options: {
          isMarkdown: !0,
          depth: he,
          characterOverride: Ee
        },
        actionCheckEvents: K,
        actionCheckBoundaries: ne.boundaries
      }, He = ur.value[ze];
      if (He !== void 0) {
        const gn = Bd(He, Me);
        return Eo.set("runtime:message", gn), gn;
      }
      return TS("runtime:message", Me), Eo.get("runtime:message") ?? {
        text: "",
        actionCheckEvents: []
      };
    }
    function R$(g = "") {
      return CS(g).text;
    }
    function N$(g = []) {
      return _i(g).map(($, K) => {
        const ne = String($.text || "");
        if (!ne) return $;
        const he = wS("reasoning", {
          placement: "reasoning",
          text: ne,
          depth: 0,
          index: K,
          label: $.label
        }), Ee = ur.value[he];
        if (Ee !== void 0)
          return Eo.set(`runtime:reasoning:${K}`, {
            text: Ee,
            actionCheckEvents: []
          }), {
            ...$,
            text: Ee
          };
        const ze = {
          key: he,
          text: ne,
          placement: "reasoning",
          options: {
            isMarkdown: !0,
            depth: 0
          }
        }, Me = `runtime:reasoning:${K}`;
        return TS(Me, ze), {
          ...$,
          text: Eo.get(Me)?.text ?? ""
        };
      }).filter(($) => String($.text || "").trim());
    }
    async function Ud(g) {
      if (!g.length) return {
        items: [],
        changedCount: 0
      };
      const A = await Xn("xb-tavern:substitute-params", { payload: { items: g } }), $ = A.result || A;
      return {
        items: Array.isArray($.items) ? $.items : [],
        changedCount: Number($.changedCount) || 0
      };
    }
    async function eg(g) {
      const A = await Xn("xb-tavern:get-worldbook-runtime", { payload: {
        context: g.context,
        currentUserMessage: g.currentUserMessage,
        trigger: g.trigger,
        timedState: g.timedState
      } });
      return A.result || A;
    }
    async function AS(g = {}, A = {}) {
      const $ = await Xn("xb-tavern:get-context", { payload: {
        characterId: g.characterId,
        includeHistory: g.includeHistory,
        includeWorldbooks: g.includeWorldbooks
      } }, A);
      return $.result || $;
    }
    function IS() {
      return String(t.value.character?.id || h.value || "").trim();
    }
    function $$() {
      return !!hr(t.value.character?.name || "") && !!IS();
    }
    async function qd(g = {}) {
      const A = String(g.sessionId || N.value || "").trim(), $ = B.value.find((Me) => Me.id === A) || (A === N.value ? ye.value : null);
      if (!$) return t.value;
      const K = String($.characterId || "").trim();
      if (!K || !g.force && $$() && IS() === K) return t.value;
      const ne = ++Id, he = await AS({
        characterId: K,
        includeHistory: !1
      });
      if (ne !== Id || A !== String(N.value || "").trim()) return t.value;
      ng(he);
      const Ee = he.context || t.value, ze = await xk(A, {
        contextSnapshot: Ee,
        characterId: String(Ee.character?.id || $.characterId || ""),
        characterName: String(Ee.character?.name || $.characterName || "")
      });
      return ze && (B.value = B.value.map((Me) => Me.id === ze.id ? ze : Me)), Ee;
    }
    async function tg(g = N.value) {
      const A = String(g || "").trim();
      return A ? await qd({
        sessionId: A,
        force: !0
      }) : t.value;
    }
    function D$(g = {}) {
      const A = String(g.requestId || ""), $ = Qt.get(A);
      if ($) {
        if (window.clearTimeout($.timer), $.abort && $.signal && $.signal.removeEventListener("abort", $.abort), Qt.delete(A), g.ok === !1) {
          const K = String(g.error || "host_request_failed"), ne = /* @__PURE__ */ new Error(`${$.type}: ${K}`), he = String(g.errorStack || "");
          he && (ne.stack = `${ne.message}
${he}`), console.error("[小白酒馆] host request failed", {
            type: $.type,
            error: K,
            errorName: String(g.errorName || ""),
            errorStack: he
          }), $.reject(ne);
          return;
        }
        $.resolve(g), [
          "xb-tavern:list-regex-scripts",
          "xb-tavern:save-regex-script",
          "xb-tavern:delete-regex-script"
        ].includes($.type) && yS();
      }
    }
    function L$(g = {}) {
      a.value = (g.hostRequestHeaders && typeof g.hostRequestHeaders == "object" ? g.hostRequestHeaders : a.value) || {}, G0(async () => {
        try {
          const A = await Xn("xb-tavern:get-host-request-headers");
          return A.hostRequestHeaders && typeof A.hostRequestHeaders == "object" ? A.hostRequestHeaders : a.value;
        } catch {
          return a.value;
        }
      });
    }
    function ng(g) {
      L$(g), t.value = g.context || {}, n.value = g.diagnostics || {};
      const A = "hostMainFontSizePx" in g ? fe(g.hostMainFontSizePx, l.value) : l.value;
      l.value = A, u.value = "hostProseLineHeightPx" in g ? fe(g.hostProseLineHeightPx, ae(A)) : ae(A), "agentConfig" in g && (r.value = g.agentConfig || r.value, Md()), "tavernDisplaySettings" in g && (s.value = Ya(g.tavernDisplaySettings)), Im(g), d.value = g.availableCharacters || d.value, h.value = String(g.selectedCharacterId || t.value.character?.id || h.value || ""), v.value = n.value.message || "", Z$().catch(($) => {
        b.value = $ instanceof Error ? $.message : String($ || "create_session_failed"), au();
      }), Jt(Xl);
    }
    function O$(g) {
      const A = g.availableCharacters;
      Array.isArray(A) && (d.value = A);
    }
    function F$(g) {
      return !!g && !!(g.description || g.personality || g.scenario || g.firstMessage || g.mesExample || g.creatorNotes || g.characterDepthPrompt || g.alternateGreetings?.length);
    }
    function xS(g) {
      if (g.origin !== window.location.origin) return;
      const A = g.data || {};
      if (A.source === Wie) {
        if (A.type === "xb-tavern:host-result") {
          D$(A.payload || {});
          return;
        }
        if (A.type === "xb-tavern:draw-progress") {
          const $ = A.payload || {};
          X.value && (j.value = X.value, re.value = $.state === "success" ? "success" : "running", pe.value = I$(String($.state || ""), $.data || {}));
          return;
        }
        if (A.type === "xb-tavern:config") {
          ng(A.payload || {});
          return;
        }
        if (A.type === "xb-tavern:context") {
          ng(A.payload || {});
          return;
        }
        A.type === "xb-tavern:config-saved" && Jl(A.payload || {});
      }
    }
    function kS() {
      oe.value = "characters", b.value = "", p.value = "", MS();
    }
    function MS() {
      v.value = "正在读取角色列表", b.value = "", gs("xb-tavern:refresh-context", {});
    }
    async function B$(g) {
      const A = String(g || "").trim();
      if (!A || E.value) return;
      p.value = A, rg(A);
      const $ = qs.value.find((ne) => ne.id === A);
      if ($ && $.shallow !== !0 && F$($)) return;
      const K = ++ga;
      w.value = A, b.value = "";
      try {
        const ne = await AS({
          characterId: A,
          includeHistory: !1,
          includeWorldbooks: !1
        }, { timeoutMs: rI });
        if (K !== ga || p.value !== A) return;
        O$(ne);
      } catch (ne) {
        if (K !== ga || p.value !== A) return;
        b.value = ne instanceof Error ? ne.message : String(ne || "character_preview_failed");
      } finally {
        K === ga && w.value === A && (w.value = "");
      }
    }
    async function rg(g = p.value) {
      const A = String(g || "").trim();
      if (!A) {
        C.value = null, R.value = !1, D.value = [];
        return;
      }
      const $ = ++Kl;
      try {
        const K = await Xn("xb-tavern:get-character-worldbook-state", { payload: { characterId: A } });
        if ($ !== Kl || String(p.value || "").trim() !== A) return;
        C.value = K.result || K, k.value = "";
      } catch (K) {
        if ($ !== Kl || String(p.value || "").trim() !== A) return;
        k.value = Ta(K);
      }
    }
    async function U$() {
      const g = String(p.value || "").trim();
      if (!(!g || M.value)) {
        M.value = !0, k.value = "";
        try {
          let A = await Xn("xb-tavern:activate-character-worldbook", { payload: { characterId: g } }), $ = A.result || A;
          if ($.action === "needs_import_confirmation") {
            const ne = String($.name || "").trim(), he = $.state;
            if (he && (C.value = he), !ne || !window.confirm(`世界书「${ne}」已存在，导入角色内嵌世界书会覆盖它。继续？`)) return;
            A = await Xn("xb-tavern:activate-character-worldbook", { payload: {
              characterId: g,
              confirmed: !0
            } }), $ = A.result || A;
          }
          const K = String($.action || "");
          if (K === "selected" || K === "imported") {
            const ne = $.state;
            ne && (C.value = ne), K === "imported" && gs("xb-tavern:refresh-context", {}), Yl(String($.name || ""));
            return;
          }
          if (K === "needs_selection") {
            D.value = Ce($.worldbookOptions), R.value = !0;
            const ne = $.state;
            ne && (C.value = ne);
            return;
          }
          await rg(g);
        } catch (A) {
          k.value = Ta(A);
        } finally {
          M.value = !1;
        }
      }
    }
    function PS() {
      R.value = !1;
    }
    async function q$(g) {
      const A = String(p.value || "").trim(), $ = String(g || "").trim();
      if (!(!A || !$ || M.value)) {
        M.value = !0, k.value = "";
        try {
          const K = await Xn("xb-tavern:bind-character-worldbook", { payload: {
            characterId: A,
            name: $
          } });
          C.value = K.result || K, R.value = !1, gs("xb-tavern:refresh-context", {}), Yl($);
        } catch (K) {
          k.value = Ta(K);
        } finally {
          M.value = !1;
        }
      }
    }
    function H$(g) {
      const A = Ld.value;
      if (!A.length) {
        y.value = 0;
        return;
      }
      y.value = Math.max(0, Math.min(A.length - 1, Number(g) || 0));
    }
    function iu() {
      Jt(() => {
        da.value?.scrollSelectedIntoView();
      });
    }
    function RS(g) {
      const A = ba.value;
      if (!A.length || E.value) return;
      const $ = String(wa.value?.id || p.value || "").trim();
      if (!$) {
        p.value = A[g < 0 ? A.length - 1 : 0]?.id || "", iu();
        return;
      }
      const K = Math.max(0, A.findIndex((ne) => ne.id === $));
      p.value = A[Math.min(A.length - 1, Math.max(0, K + g))]?.id || "", iu();
    }
    function G$(g) {
      g.key === "ArrowDown" ? (g.preventDefault(), RS(1)) : g.key === "ArrowUp" ? (g.preventDefault(), RS(-1)) : g.key === "Home" ? (g.preventDefault(), p.value = ba.value[0]?.id || "", iu()) : g.key === "End" ? (g.preventDefault(), p.value = ba.value.at(-1)?.id || "", iu()) : g.key === "Enter" && (g.preventDefault(), NS());
    }
    async function NS() {
      const g = String(wa.value?.id || p.value || "").trim();
      !g || E.value || await LS(g);
    }
    async function yi() {
      B.value = await eH();
      const g = String(await tH() || "").trim();
      N.value = B.value.some((A) => A.id === g) ? g : "", g && !N.value && await uf(""), H.value = N.value ? await Hr(N.value) : [], await Qn(N.value), N.value && qd({ sessionId: N.value });
    }
    async function W$(g = {}) {
      const A = String(N.value || "").trim();
      if (!A) return null;
      const $ = await Ak(A, { contract: Wr({
        ...Ze.value,
        ...g
      }) });
      return $ ? (B.value = B.value.map((K) => K.id === $.id ? $ : K), $) : null;
    }
    async function Qn(g = N.value) {
      const A = String(g || "").trim();
      if (!A) {
        Le.value = [], Ie.value = [], De.value = [], Be.value = null, ag(), W.value = null, xe.value = null, ke.value = [], Qe.value = "";
        return;
      }
      const [$, K, ne, he, Ee] = await Promise.all([
        qr(A),
        iH(A, { limit: 18 }),
        jk(A),
        OH(A),
        Qa(A, "memory/state.md")
      ]), ze = ne && ne.status === "ready" && Array.isArray(ne.files) ? ne : await Xo(A);
      if (A === N.value && (Le.value = $, Ie.value = K, De.value = Array.isArray(ze.files) ? ze.files.map((Me) => ({
        path: String(Me.path || ""),
        status: Me.status === "stale" ? "stale" : "active",
        createdAt: Number(Me.createdAt) || Number(Me.updatedAt) || 0,
        updatedAt: Number(Me.updatedAt) || 0,
        source: String(Me.source || ""),
        staleFromOrder: Number.isFinite(Number(Me.staleFromOrder)) ? Number(Me.staleFromOrder) : void 0,
        contentLength: Math.max(0, Number(Me.contentLength) || 0),
        title: String(Me.title || ""),
        preview: String(Me.preview || ""),
        searchText: String(Me.searchText || "")
      })) : [], Be.value = ze, W.value = Ee, xe.value = he.document, ke.value = he.patches, !De.value.some((Me) => Me.path === Qe.value))) {
        if (ru.value && Qe.value) {
          _e.value = "当前档案已变化，草稿仍保留";
          return;
        }
        Qe.value = De.value[0]?.path || "";
      }
    }
    async function V$() {
      if (YN.value = Date.now(), !zl && N.value && !(!Ie.value.some((g) => zN(g.status)) && !Z.value && !tt.value)) {
        zl = !0;
        try {
          await Qn(N.value);
        } finally {
          zl = !1;
        }
      }
    }
    async function $S(g = H.value) {
      if (!N.value) return;
      const A = nr(ye.value?.state || {}), $ = await PN({
        messages: g,
        contextSnapshot: await tg(N.value),
        chatPreset: Bn.value,
        historyMode: I.value,
        diagnostics: n.value,
        applySubstituteParams: Ud,
        getNativeWorldInfoRuntime: eg
      });
      await Ik(N.value, {
        ...$,
        contract: A.contract,
        contextWindowStartOrder: jw({
          messages: g,
          contextWindowStartOrder: A.contextWindowStartOrder
        }).contextWindowStartOrder
      }), await yi();
    }
    function Hd() {
      wo += 1, T.value = "", x.value = "", L.value = "", Fs.value = "", ci.value = "", Bs.value = "", di.value = "", pa.value = !1, Zr.value = "history", ft.value = null, sg(), og(), Am();
    }
    const sg = an.resetWindowState, og = ln.resetWindowState, K$ = an.revealOlderMessages, z$ = ln.revealOlderMessages;
    function J$(g = {}) {
      return [String(g.firstMessage || g.first_mes || "").trim(), ...Ce(g.alternateGreetings || g.alternate_greetings)].filter(Boolean);
    }
    async function Y$(g, A, $ = 0) {
      const K = J$(A.character || {}), ne = Math.max(0, Math.min(K.length - 1, Number($) || 0)), he = String(K[ne] || "").trim();
      he && await Uu(g, {
        role: "assistant",
        name: String(A.character?.name || ""),
        content: he,
        contextSnapshot: A,
        chatPresetId: String(Bn.value.id || ""),
        chatPresetName: String(Bn.value.name || ""),
        presetId: String(Bn.value.id || ""),
        presetName: String(Bn.value.name || "")
      });
    }
    async function DS(g = {}) {
      const A = g.contextSnapshot || t.value, $ = qx({
        context: A,
        chatPreset: Bn.value,
        currentUserMessage: "",
        historyMode: I.value,
        turn: 0,
        entryStates: {},
        diagnostics: n.value
      }), K = await Ck({
        title: String(A.character?.name || "未选择角色"),
        characterId: String(A.character?.id || ""),
        characterName: String(A.character?.name || "未选择角色"),
        contextSnapshot: A,
        buildSnapshot: $.buildSnapshot,
        chatPresetId: String(Bn.value.id || ""),
        chatPresetName: String(Bn.value.name || ""),
        presetId: String(Bn.value.id || ""),
        presetName: String(Bn.value.name || ""),
        state: {
          turn: 0,
          worldEntryStates: {},
          nativeWorldInfoTimedState: {
            sticky: {},
            cooldown: {}
          }
        }
      });
      return g.includeFirstMessage !== !1 && await Y$(K.id, A, g.greetingIndex), N.value = K.id, await yi(), K;
    }
    async function X$(g = {}) {
      await DS(g), oe.value = "chat", ct.value = "chat", mr(!0);
    }
    async function Q$() {
      if (Ne.value) {
        tD();
        return;
      }
      kS();
    }
    function au() {
      jr && (window.clearTimeout(jr), jr = null), E.value = "", m.value = 0;
    }
    async function Z$() {
      const g = E.value;
      if (!g || String(t.value.character?.id || h.value || "").trim() !== g) return;
      if (!hr(t.value.character?.name || "")) {
        au(), b.value = "没有读到这张角色卡。";
        return;
      }
      const A = m.value;
      au(), b.value = "", Hd(), await X$({ greetingIndex: A });
    }
    async function LS(g) {
      const A = String(g || "").trim();
      if (!A || E.value) return;
      const $ = A === String(p.value || "").trim() ? y.value : 0;
      p.value = A, y.value = $, N.value = "", H.value = [], await uf(""), await Qn(""), Hd(), h.value = A, b.value = "", v.value = "正在读取角色卡", E.value = A, m.value = $, jr && window.clearTimeout(jr), jr = window.setTimeout(() => {
        E.value === A && (au(), b.value = "读取角色卡超时。");
      }, rI), gs("xb-tavern:refresh-context", {
        characterId: A,
        includeHistory: !1
      });
    }
    async function j$(g) {
      Hd(), ag(), N.value = g, await uf(g), H.value = await Hr(g), await Qn(g), qd({
        sessionId: g,
        force: !0
      }), oe.value = "chat", ct.value = "chat", mr(!0);
    }
    async function eD(g, A) {
      A?.stopPropagation();
      const $ = String(g || "").trim();
      if (!$) return;
      const K = jl(B.value.find((ne) => ne.id === $)) || "这个会话";
      if (window.confirm(`删除「${K}」？`) && ($ === N.value && Z.value && cr.value?.abort(), !!await nH($))) {
        if (r$($), $ === N.value && Hd(), await yi(), !N.value) {
          H.value = [], await Qn(""), oe.value = "home";
          return;
        }
        oe.value = "chat", ct.value = "chat", mr(!0);
      }
    }
    function tD() {
      oe.value = "chat", ct.value = "chat", mr(!0);
    }
    function nD(g = "history") {
      Zr.value = g, pa.value = !0;
    }
    function rD() {
      pa.value = !1;
    }
    async function sD() {
      const g = Fs.value.trim();
      if (di.value = "", Bs.value = "", ci.value = "", !g) {
        di.value = "先写一句话。";
        return;
      }
      const A = wo + 1;
      wo = A;
      const $ = N.value;
      Bs.value = "模拟中";
      try {
        const K = await tg($), ne = await m7({
          sessionId: $,
          agentConfig: r.value,
          contextSnapshot: K,
          chatPreset: Bn.value,
          currentUserMessage: g,
          runtimeState: nr(ye.value?.state || {}),
          diagnostics: n.value,
          historyMode: I.value,
          applyRegex: ou,
          applySubstituteParams: Ud,
          getNativeWorldInfoRuntime: eg
        });
        if (A !== wo || $ !== N.value) return;
        ci.value = ne.requestSnapshot.rawRequestJson || ne.requestSnapshot.rawMessagesJson || "", Bs.value = `模拟完成 · ${ne.requestSnapshot.providerLabel || ne.provider} / ${ne.model || "未选择模型"}`;
      } catch (K) {
        if (A !== wo || $ !== N.value) return;
        console.error("[小白酒馆] simulate request failed", K), Bs.value = "", di.value = K instanceof Error ? K.message : String(K || "simulate_failed");
      }
    }
    function ig(g = "", A = 180) {
      const $ = String(g || "").trim();
      return $.length > A ? `${$.slice(0, A)}...` : $;
    }
    const { discardMemoryDraft: oD, enterMemoryEditMode: iD, formatMemoryFileMeta: aD, invalidateMemoryFileRecordLoad: ag, loadMemoryFileIntoEditor: OS, loadSelectedMemoryFileRecord: lD, memoryFileStatusLabel: uD, previewMemoryDraft: cD, saveSelectedMemoryFile: dD, selectMemoryFile: fD } = Qte({
      memoryEditorBaseContent: Se,
      memoryEditorDocumentAvailable: hS,
      memoryEditorDraft: ee,
      memoryEditorDirty: ru,
      memoryEditorLoadedPath: ue,
      memoryEditorMode: be,
      memoryEditorStatus: _e,
      selectedMemoryFileEntry: nu,
      selectedMemoryFilePath: Qe,
      selectedMemoryFileRecord: je,
      selectedSessionId: N,
      refreshRecords: Qn
    });
    async function hD(g) {
      const A = String(g.id || "");
      if (!(!A || !N.value || iS.value || Ve.value)) {
        Ve.value = A, we.value = "记忆正在重试。";
        try {
          const $ = await Hr(N.value), K = $.find((Ee) => Ee.order === g.userOrder && Ee.role === "user"), ne = $.find((Ee) => Ee.order === g.assistantOrder && Ee.role === "assistant" && !Ee.error);
          if (!K || !ne) {
            we.value = "原文楼层不存在，无法重试。", await Qn();
            return;
          }
          const he = await wN({
            sessionId: N.value,
            agentConfig: r.value,
            userMessage: K,
            assistantMessage: ne,
            turn: g.turn,
            trigger: "manual_retry",
            assistantPreset: ya.value,
            sessionContract: Ze.value
          });
          we.value = he.ok ? "" : `失败：${he.error || "manager_retry_failed"}`;
        } catch ($) {
          we.value = $ instanceof Error ? $.message : String($ || "manager_retry_failed");
        } finally {
          try {
            await Qn();
          } finally {
            Ve.value = "";
          }
        }
      }
    }
    function pD(g) {
      return !!g && !!Ve.value && String(g.id || "") === Ve.value;
    }
    function Gd(g = "") {
      return g === "assistant" ? hr(ye.value?.characterName || Us.value.name || "", "角色") : g === "user" ? hr($m.value || "", "User") : {
        system: "规则",
        tool: "工具结果"
      }[g] || g || "未知";
    }
    function _i(g) {
      return (Array.isArray(g) ? g : Array.isArray(g?.thoughts) ? g.thoughts : []).map((A, $) => ({
        label: String(A?.label || `思考 ${$ + 1}`).trim() || `思考 ${$ + 1}`,
        text: String(A?.text || "").trim()
      })).filter((A) => A.text);
    }
    function mD(g, A = !1) {
      const $ = _i(g);
      if (!$.length) return "";
      const K = A ? "正在思考" : "展开思考块";
      return $.length > 1 ? `${K}（${$.length} 段）` : K;
    }
    function gD(g) {
      const A = Number(g) || 0;
      if (!A) return "";
      try {
        return new Intl.DateTimeFormat("zh-CN", {
          hour: "2-digit",
          minute: "2-digit"
        }).format(new Date(A));
      } catch {
        return "";
      }
    }
    function Un(g) {
      return `${g.sessionId}:${g.order}`;
    }
    function lu(g) {
      return `manager:${g.sessionId}:${g.order}`;
    }
    function lg(g) {
      return !Z.value && !g.error && ["user", "assistant"].includes(g.role);
    }
    function ug(g) {
      return !tt.value && !g.error && ["user", "assistant"].includes(g.role);
    }
    function FS(g) {
      if (Z.value) return !1;
      const A = [...H.value].sort((K, ne) => K.order - ne.order), $ = A.at(-1);
      return !$ || $.sessionId !== g.sessionId || $.order !== g.order ? !1 : g.role === "user" ? !0 : g.role !== "assistant" ? !1 : A.slice(0, -1).some((K) => K.role === "user");
    }
    function BS(g) {
      if (tt.value) return !1;
      if (g.role === "user") return !0;
      const A = [...Le.value].sort((K, ne) => K.order - ne.order), $ = A.findIndex((K) => K.order === g.order);
      return $ < 0 ? !1 : A.slice(0, $ + 1).some((K) => K.role === "user");
    }
    function US(g) {
      return Qr.value === Un(g);
    }
    function qS(g) {
      return Qr.value === lu(g);
    }
    function HS(g) {
      return qS(g) && ui.value.trim() !== String(g.content || "").trim();
    }
    function pr(g, A, $) {
      const K = `${Un(g)}:${A}`;
      lr.value = {
        ...lr.value,
        [K]: $ ? "success" : "error"
      }, window.setTimeout(() => {
        const ne = { ...lr.value };
        delete ne[K], lr.value = ne;
      }, 1100);
    }
    function bi(g, A, $) {
      const K = `${lu(g)}:${A}`;
      lr.value = {
        ...lr.value,
        [K]: $ ? "success" : "error"
      }, window.setTimeout(() => {
        const ne = { ...lr.value };
        delete ne[K], lr.value = ne;
      }, 1100);
    }
    function vD(g, A) {
      return lr.value[`${Un(g)}:${A}`] || "";
    }
    function yD(g, A) {
      return lr.value[`${lu(g)}:${A}`] || "";
    }
    function Wd(g) {
      return X.value === Un(g);
    }
    function wi(g, A = "", $ = "running", K = 0) {
      if (j.value = Un(g), re.value = $, pe.value = A, K > 0) {
        const ne = Un(g);
        window.setTimeout(() => {
          j.value !== ne || X.value === ne || (j.value = "", re.value = "idle", pe.value = "");
        }, K);
      }
    }
    function GS(g) {
      return j.value === Un(g) ? pe.value : "";
    }
    function _D(g) {
      return j.value === Un(g) ? `is-${re.value}` : "";
    }
    function cg(g) {
      return Z.value || US(g) || g.error || !["user", "assistant"].includes(g.role) || X.value && !Wd(g) ? !1 : !!ma(g.content || "");
    }
    function bD(g) {
      return Wd(g) ? GS(g) || "停止画图" : cg(g) ? (Y.value.enabled && Y.value.ready, "为这条消息画图") : X.value ? "已有画图任务正在运行" : "这条消息暂不能画图";
    }
    async function WS(g = "") {
      const A = String(g || "");
      if (!A) return !1;
      try {
        if (navigator.clipboard?.writeText)
          return await navigator.clipboard.writeText(A), !0;
      } catch {
      }
      try {
        const $ = document.createElement("textarea");
        $.value = A, $.setAttribute("readonly", "readonly"), $.style.position = "fixed", $.style.opacity = "0", $.style.pointerEvents = "none", document.body.appendChild($), $.select(), $.setSelectionRange(0, $.value.length);
        const K = document.execCommand("copy");
        return $.remove(), K;
      } catch {
        return !1;
      }
    }
    async function wD(g) {
      pr(g, "copy", await WS(g.content || ""));
    }
    async function SD(g) {
      bi(g, "copy", await WS(g.content || ""));
    }
    async function TD(g) {
      if (Wd(g)) {
        mn.value?.abort(), wi(g, "正在停止画图", "running");
        return;
      }
      if (!cg(g)) {
        pr(g, "draw", !1);
        return;
      }
      const A = await x$();
      if (!A.enabled || !A.ready) {
        wi(g, "请开启小白X画图模块", "error", 3200), pr(g, "draw", !1);
        return;
      }
      const $ = new AbortController();
      mn.value = $, X.value = Un(g), wi(g, "正在准备画图", "running");
      try {
        const K = await Xn("xb-tavern:draw-generate", { payload: {
          source: "tavern",
          text: ma(g.content || ""),
          title: Gd(g.role),
          sessionId: g.sessionId,
          messageOrder: g.order,
          role: g.role,
          characterName: ye.value?.characterName || Us.value.name || ""
        } }, {
          timeoutMs: Yie,
          signal: $.signal
        });
        if ($.signal.aborted) {
          pr(g, "draw", !1);
          return;
        }
        const ne = K.result || K, he = A$(g.content || "", Array.isArray(ne.images) ? ne.images : []);
        if (!he.inserted) {
          const ze = Number(ne.success) || 0, Me = Number(ne.total) || (Array.isArray(ne.images) ? ne.images.length : 0);
          wi(g, Me > 0 && ze === 0 ? `画图任务结束：${Me} 张都失败了，未插入图片。` : ze > 0 ? `画图完成 ${ze}/${Me || ze}，但没有返回可用图片占位。` : "画图任务结束，但没有返回可用图片。", "error", 4200), pr(g, "draw", !1);
          return;
        }
        const Ee = await wy(g.sessionId, g.order, { content: he.content });
        Ee && N.value === g.sessionId && (H.value = await Hr(g.sessionId)), wi(g, `${Xie}${he.appended ? `，${he.appended} 张追加到末尾` : ""}`, "success", 2600), pr(Ee || g, "draw", !!Ee), Jt(fi);
      } catch (K) {
        const ne = Ta(K);
        /abort|已取消|request_aborted/i.test(ne) ? wi(g, "配图已取消", "error", 1800) : wi(g, `配图失败：${ne}`, "error", 4200), pr(g, "draw", !1);
      } finally {
        mn.value === $ && (mn.value = null), X.value === Un(g) && window.setTimeout(() => {
          !mn.value && X.value === Un(g) && (X.value = "");
        }, 1200);
      }
    }
    function ED(g) {
      lg(g) && (Qr.value = Un(g));
    }
    function CD(g) {
      ug(g) && (Qr.value = lu(g), ui.value = g.content || "", Jt(() => {
        const A = fa.value?.querySelector(`[data-manager-message-editor="${lu(g)}"]`);
        A && (dg(A), A.focus(), A.setSelectionRange(A.value.length, A.value.length));
      }));
    }
    function vs() {
      Qr.value = "", ui.value = "", Jt(() => {
        fi(), Cd();
      });
    }
    function dg(g, A = {}) {
      if (!g) return;
      const $ = Number(A.minHeight ?? 144), K = Number(A.maxHeight ?? 420);
      g.style.height = "auto", g.style.height = `${Math.min(Math.max(g.scrollHeight, $), K)}px`;
    }
    function fg(g) {
      g && (g.style.height = "");
    }
    function AD(g, A) {
      if (g.key === "Escape") {
        g.preventDefault(), vs();
        return;
      }
      g.key === "Enter" && (g.ctrlKey || g.metaKey) && (g.preventDefault(), VS(A));
    }
    function ID(g) {
      dg(g.target);
    }
    function xD(g) {
      const A = g.target;
      dg(A, A.closest(".manager-compose") ? {
        minHeight: 48,
        maxHeight: 180
      } : {
        minHeight: 36,
        maxHeight: 99
      });
    }
    async function kD(g, A = {}) {
      if (!lg(g)) return;
      const $ = ("content" in A ? String(A.content || "") : String(g.content || "")).trim();
      if (!$) {
        pr(g, "edit", !1);
        return;
      }
      if (!A.rerun && $ === String(g.content || "").trim()) {
        vs();
        return;
      }
      if (A.rerun && g.role !== "user") {
        vs(), await hg(g);
        return;
      }
      const K = await QD(g, await XD(g, $)), ne = await wy(g.sessionId, g.order, {
        content: K,
        ...g.role === "user" ? { runtimeEvents: [] } : {}
      });
      if (ne) {
        const he = await L_(g.sessionId, g.order);
        await Ey(g.sessionId, g.order), he.conflicts.length || await Xo(g.sessionId);
      }
      ne && N.value && (H.value = await Hr(N.value), await Qn(N.value)), vs(), pr(ne || g, "edit", !!ne), ne && A.rerun ? ne.role === "user" ? await Jd({
        messageText: ne.content,
        reuseUserMessageOrder: ne.order,
        rerollRuntimeEvents: !0
      }) : await hg(ne) : ne && await $S();
    }
    async function VS(g, A = {}) {
      if (!ug(g)) return;
      const $ = ui.value.trim();
      if (!$) {
        bi(g, "edit", !1);
        return;
      }
      if (!A.rerun && !HS(g)) {
        vs();
        return;
      }
      if (A.rerun && g.role !== "user") {
        vs(), await pg(g);
        return;
      }
      const K = await sH(g.sessionId, g.order, {
        content: $,
        clearProtocolPayload: g.role === "assistant"
      });
      K && N.value === g.sessionId && (Le.value = await qr(g.sessionId)), vs(), bi(K || g, "edit", !!K), K && A.rerun && await pg(K);
    }
    function MD(g) {
      const A = [...H.value].sort((K, ne) => K.order - ne.order), $ = A.findIndex((K) => K.order === g.order);
      return $ < 0 ? [g.order] : A.slice($).map((K) => K.order);
    }
    async function PD(g) {
      if (Z.value) return;
      const A = MD(g), $ = [...H.value].sort((Me, He) => Me.order - He.order).findIndex((Me) => Me.order === g.order), K = $ >= 0 ? $ + 1 : Math.max(1, Number(g.order) + 1), ne = A.length > 1 ? `从第 ${K} 楼开始删除后续剧情？将移除 ${A.length} 楼。` : `删除第 ${K} 楼？`;
      if (!window.confirm(ne)) return;
      const he = Math.min(...A), Ee = await L_(g.sessionId, he), ze = await kk(g.sessionId, A);
      ze > 0 && (await Ey(g.sessionId, g.order), Ee.conflicts.length || await Xo(g.sessionId)), N.value && (H.value = await Hr(N.value), await Qn(N.value)), ze > 0 && await $S(), Qr.value.startsWith(`${g.sessionId}:`) && vs(), pr(g, "delete", ze > 0);
    }
    async function hg(g) {
      if (!FS(g)) {
        pr(g, "rerun", !1);
        return;
      }
      const A = [...H.value].sort((ne, he) => ne.order - he.order), $ = A.findIndex((ne) => ne.order === g.order), K = g.role === "user" ? g : [...A.slice(0, Math.max(0, $ + 1))].reverse().find((ne) => ne.role === "user");
      if (!K) {
        pr(g, "rerun", !1);
        return;
      }
      pr(g, "rerun", !0), await Jd({
        messageText: K.content,
        reuseUserMessageOrder: K.order
      });
    }
    function RD(g) {
      const A = [...Le.value].sort((ne, he) => ne.order - he.order), $ = A.findIndex((ne) => ne.order === g.order);
      if ($ < 0) return [g.order];
      if (g.role !== "user") {
        let ne = $;
        for (let Ee = $; Ee >= 0; Ee -= 1) if (A[Ee]?.role === "user") {
          ne = Ee;
          break;
        }
        const he = [];
        for (let Ee = ne + 1; Ee < A.length; Ee += 1) {
          const ze = A[Ee];
          if (ze.role === "user") break;
          he.push(ze.order);
        }
        return he.length ? he : [g.order];
      }
      const K = [];
      for (let ne = $; ne < A.length; ne += 1) {
        const he = A[ne];
        if (ne > $ && he.role === "user") break;
        K.push(he.order);
      }
      return K;
    }
    async function ND(g) {
      if (tt.value) return;
      const A = RD(g), $ = g.role === "user" ? `删除这轮助手对话？将移除 ${A.length} 条记录。` : "删除这条助手回复？";
      if (!window.confirm($)) return;
      const K = await Sy(g.sessionId, A);
      N.value === g.sessionId && (Le.value = await qr(g.sessionId)), Qr.value.startsWith(`manager:${g.sessionId}:`) && vs(), bi(g, "delete", K > 0);
    }
    async function pg(g) {
      if (!BS(g)) {
        bi(g, "rerun", !1);
        return;
      }
      const A = [...Le.value].sort((Ee, ze) => Ee.order - ze.order), $ = A.findIndex((Ee) => Ee.order === g.order), K = g.role === "user" ? g : [...A.slice(0, Math.max(0, $ + 1))].reverse().find((Ee) => Ee.role === "user");
      if (!K || !N.value) {
        bi(g, "rerun", !1);
        return;
      }
      const ne = A.filter((Ee) => Ee.order >= K.order).map((Ee) => Ee.order);
      if (ne.length > 2 && !window.confirm(`从这里重新询问助手会移除后面的 ${ne.length} 条记录。继续？`))
        return;
      const he = A.filter((Ee) => Ee.order < K.order);
      bi(g, "rerun", !0), await XS(K.sessionId, K.content, {
        historyBeforeTurn: he,
        replaceOrdersAfterAppend: ne
      });
    }
    const mg = an.updateScrollButtons, KS = ln.updateScrollButtons, mr = an.scrollToBottom, $D = an.scrollToTop, gg = ln.scrollToBottom, DD = ln.scrollToTop, LD = an.handleScroll, OD = ln.handleScroll, FD = an.handleWheel, BD = ln.handleWheel, UD = an.handleTouchStart, qD = ln.handleTouchStart, HD = an.handleTouchMove, GD = ln.handleTouchMove;
    function WD(g) {
      g.key === "Enter" && (g.isComposing || g.shiftKey || g.altKey || (g.ctrlKey || g.metaKey || window.innerWidth >= 760) && (g.preventDefault(), Jd()));
    }
    function VD(g) {
      g.key === "Enter" && (g.isComposing || g.shiftKey || g.altKey || (g.ctrlKey || g.metaKey || window.innerWidth >= 760) && (g.preventDefault(), QS()));
    }
    function zS() {
      pi && (window.clearTimeout(pi), pi = null);
    }
    function Vd(g) {
      const A = ft.value || null, $ = String(g.id || A?.id || `manager-compaction-${Date.now()}`), K = Number(g.visibleSince) || (A?.id === $ ? Number(A.visibleSince) : 0) || Date.now();
      ft.value = {
        id: $,
        active: !0,
        resolved: !1,
        currentTokens: 0,
        yieldTokens: 0,
        triggerTokens: 0,
        status: "正在释放较早管理员对话...",
        ...A,
        ...g,
        visibleSince: K
      };
    }
    function JS(g = 3e3) {
      const A = ft.value?.id || "", $ = Number(ft.value?.visibleSince) || Date.now(), K = Math.max(0, Date.now() - $), ne = Math.max(0, g - K);
      zS(), pi = window.setTimeout(() => {
        pi = null, !(!A || ft.value?.id !== A) && (ft.value = null);
      }, ne);
    }
    function vg(g, A) {
      const $ = at.value?.sessionId === g ? at.value : {
        sessionId: g,
        messages: [],
        draft: null,
        nextOrder: aI
      }, K = $.nextOrder;
      return $.nextOrder += 1, at.value = $, {
        sessionId: g,
        order: K,
        role: A.role,
        content: A.content,
        name: A.name,
        error: A.error === !0,
        createdAt: Date.now(),
        updatedAt: Date.now(),
        provider: A.provider,
        model: A.model,
        finishReason: A.finishReason,
        thoughts: A.thoughts,
        providerPayload: A.providerPayload,
        toolCalls: A.toolCalls,
        toolCallId: A.toolCallId,
        toolName: A.toolName,
        toolDisplay: A.toolDisplay
      };
    }
    function KD(g, A = {}) {
      return !Number.isFinite(Number(A.reuseUserMessageOrder)) && g.trim().startsWith("/");
    }
    function zD(g) {
      if (g == null) return "";
      if (typeof g == "string") return g.trim();
      if (typeof g == "number" || typeof g == "boolean" || typeof g == "bigint") return String(g);
      try {
        return JSON.stringify(g, null, 2);
      } catch {
        return String(g);
      }
    }
    async function JD(g, A = {}) {
      if (!KD(g, A)) return g;
      const $ = await Xn("xb-tavern:run-slash-command", { payload: { command: g } }), K = $.result || $, ne = K.execution && typeof K.execution == "object" ? K.execution : {};
      if (K.ok === !1 || ne.isError === !0 || ne.isAborted === !0) throw new Error(String(K.error || ne.errorMessage || ne.abortReason || "slash_command_failed"));
      const he = zD(K.pipe);
      return he || (T.value = "", Jt(() => fg(ai.value)), su("斜杠命令已执行，没有输出。", 2600), "");
    }
    function YD(g = {}) {
      const A = {}, $ = String(g.user?.name || "").trim(), K = String(g.character?.name || "").trim();
      return $ && (A.name1Override = $), K && (A.name2Override = K), A;
    }
    async function XD(g, A) {
      const $ = (B.value.find((K) => K.id === g.sessionId) || (N.value === g.sessionId ? ye.value : null))?.contextSnapshot || t.value || {};
      return (await Ud([{
        id: `edit:${g.sessionId}:${g.order}`,
        text: A,
        options: YD($)
      }])).items[0]?.text ?? A;
    }
    async function QD(g, A) {
      const $ = Jm(g);
      return $ ? (await ou([{
        id: `edit:${g.sessionId}:${g.order}`,
        text: A,
        placement: $,
        options: {
          isEdit: !0,
          characterOverride: Qm(g)
        }
      }])).items[0]?.text ?? A : A;
    }
    function Kd(g) {
      const A = String(g || "").trim();
      return A ? ((!at.value || at.value.sessionId !== A) && (at.value = {
        sessionId: A,
        messages: [],
        draft: vg(A, {
          role: "assistant",
          content: "正在思考..."
        }),
        nextOrder: aI + 1
      }), at.value) : null;
    }
    function YS(g = "") {
      const A = String(g || "").trim();
      (!A || at.value?.sessionId === A) && (at.value = null);
    }
    function ZD(g, A) {
      const $ = Kd(g);
      $ && ($.draft = {
        ...$.draft || vg(g, {
          role: "assistant",
          content: A.content
        }),
        ...A,
        role: "assistant",
        content: A.content,
        updatedAt: Date.now()
      });
    }
    function jD(g) {
      return Array.isArray(g.toolCalls) && g.toolCalls.length ? g.toolCalls.map((A) => ({
        id: typeof A?.id == "string" ? A.id : "",
        name: typeof A?.name == "string" ? A.name : "",
        arguments: typeof A?.arguments == "string" ? A.arguments : "{}"
      })) : Array.isArray(g.tool_calls) ? g.tool_calls.map((A) => ({
        id: typeof A?.id == "string" ? A.id : "",
        name: typeof A?.function?.name == "string" ? A.function.name : "",
        arguments: typeof A?.function?.arguments == "string" ? A.function.arguments : "{}"
      })) : [];
    }
    function eL(g, A) {
      const $ = Kd(g);
      $ && ($.messages = [...$.messages, vg(g, {
        role: A.role,
        content: String(A.content || ""),
        name: A.name,
        thoughts: Array.isArray(A.thoughts) ? _i(A.thoughts) : void 0,
        providerPayload: A.providerPayload,
        toolCalls: A.role === "assistant" ? jD(A) : void 0,
        toolCallId: A.role === "tool" ? String(A.toolCallId || A.tool_call_id || "") : void 0,
        toolName: A.role === "tool" ? String(A.toolName || "") : void 0,
        toolDisplay: A.role === "tool" ? A.toolDisplay : void 0
      })]);
    }
    function tL(g, A) {
      const $ = String(g || "").trim();
      if ($) {
        if (A.type === "clear_stream_draft") {
          const K = Kd($);
          K && (K.draft = null);
          return;
        }
        eL($, A.message);
      }
    }
    function zd() {
      vS(), x.value = "", P.value = [], mt.value = [];
    }
    async function nL(g, A, $, K) {
      const ne = (Array.isArray(A) ? A : []).filter((Me) => Me && ["assistant", "tool"].includes(Me.role)), he = ne.length ? ne : [{
        role: "assistant",
        content: $
      }], Ee = [...he].map((Me, He) => ({
        message: Me,
        index: He
      })).reverse().find(({ message: Me }) => {
        const He = Array.isArray(Me.toolCalls) && Me.toolCalls.length || Array.isArray(Me.tool_calls) && Me.tool_calls.length;
        return Me.role === "assistant" && !He;
      })?.index ?? -1;
      Ee < 0 ? he.push({
        role: "assistant",
        content: $
      }) : String(he[Ee]?.content || "").trim() || (he[Ee] = {
        ...he[Ee],
        content: $
      });
      const ze = [];
      for (let Me = 0; Me < he.length; Me += 1) {
        const He = he[Me], gn = Array.isArray(He.toolCalls) && He.toolCalls.length || Array.isArray(He.tool_calls) && He.tool_calls.length, es = He.role === "assistant" && !gn && Me === he.length - 1, Ea = await cf(g, {
          role: He.role,
          content: es ? String(He.content || "").trim() || $ : String(He.content || ""),
          name: He.name,
          thoughts: He.thoughts,
          providerPayload: He.providerPayload,
          toolCalls: He.toolCalls,
          tool_calls: He.tool_calls,
          toolCallId: He.toolCallId || He.tool_call_id,
          toolName: He.toolName,
          toolDisplay: He.toolDisplay,
          provider: He.role === "assistant" ? K.provider : void 0,
          model: He.role === "assistant" ? K.model : void 0,
          finishReason: He.role === "assistant" && !gn ? K.finishReason : void 0,
          error: He.role === "assistant" && !gn ? K.error : !1
        });
        ze.push(Ea);
      }
      return ze;
    }
    async function XS(g, A, $ = {}) {
      const K = String(A || "").trim();
      if (!g || !K) return;
      const ne = Number(ge.value.turn || 0), he = new AbortController();
      dr.value = he, tt.value = !0, nt.value = !1, $e.value = "运行中", Fn.value = !0, og();
      const Ee = M7();
      try {
        const ze = await J9({
          sessionId: g,
          agentConfig: r.value,
          assistantPreset: ya.value,
          question: K,
          history: $.historyBeforeTurn,
          signal: he.signal,
          onCompactionStart: (Et) => {
            Vd({
              id: `manager-compaction-${Date.now()}`,
              active: !0,
              resolved: !1,
              currentTokens: Et.currentTokens,
              yieldTokens: Et.yieldTokens || 0,
              triggerTokens: Et.triggerTokens,
              status: Et.status
            });
          },
          onCompactionProgress: (Et) => {
            Vd({
              currentTokens: Et.currentTokens,
              yieldTokens: Et.yieldTokens || Et.currentTokens,
              triggerTokens: Et.triggerTokens,
              status: Et.status
            });
          },
          onCompactionComplete: (Et) => {
            Vd({
              resolved: !0,
              currentTokens: Et.currentTokens,
              yieldTokens: Et.yieldTokens || Et.currentTokens,
              triggerTokens: Et.triggerTokens,
              status: Et.status
            }), JS(), Qn(g);
          },
          onCompactionUnable: (Et) => {
            Vd({
              resolved: !0,
              currentTokens: Et.currentTokens,
              yieldTokens: Et.yieldTokens || Et.currentTokens,
              triggerTokens: Et.triggerTokens,
              status: Et.status
            }), JS();
          }
        }), Me = Array.isArray($.historyBeforeTurn) ? ze.history : await qr(g), He = await cf(g, {
          role: "user",
          content: K
        });
        N.value === g && (Le.value = [...Le.value, He].sort((Et, Si) => Et.order - Si.order)), Kd(g);
        const gn = [...new Set(($.replaceOrdersAfterAppend || []).map((Et) => Number(Et)).filter((Et) => Number.isInteger(Et) && Et >= 0))];
        gn.length && (await Sy(g, gn), N.value === g && (Le.value = await qr(g)));
        const es = await Y9({
          sessionId: g,
          agentConfig: r.value,
          assistantPreset: ya.value,
          question: K,
          history: Me,
          turn: ne,
          signal: he.signal,
          onProtocolEvent: (Et) => {
            Ee.reset(), tL(g, Et);
          },
          onStreamProgress: (Et) => {
            const Si = Ee.update(Et);
            ZD(g, {
              content: Si.content,
              thoughts: Array.isArray(Et.thoughts) ? _i(Et.thoughts) : void 0,
              toolCalls: Si.toolCalls
            });
          }
        }), Ea = String(es.text || "").trim() || "没有返回内容。";
        await nL(g, es.protocolMessages, Ea, {
          provider: es.provider,
          model: es.model,
          finishReason: es.ok ? "stop" : "error",
          error: !es.ok
        }), YS(g), N.value === g && (Le.value = await qr(g)), await Qn(g), $e.value = "";
      } catch (ze) {
        YS(g), he.signal.aborted ? (await cf(g, {
          role: "assistant",
          content: "已停止。",
          finishReason: "aborted"
        }), N.value === g && (Le.value = await qr(g)), $e.value = "") : (await cf(g, {
          role: "assistant",
          content: ze instanceof Error ? ze.message : String(ze || "assistant_failed"),
          error: !0,
          finishReason: "error"
        }), N.value === g && (Le.value = await qr(g)), $e.value = "失败");
      } finally {
        Ee.reset(), dr.value === he && (dr.value = null), nt.value = !1, tt.value = !1;
      }
    }
    async function QS() {
      if (tt.value) {
        nt.value || (nt.value = !0, $e.value = "正在停止..."), dr.value?.abort();
        return;
      }
      const g = Re.value.trim();
      if (!g || !N.value) return;
      const A = N.value;
      Re.value = "", Jt(() => fg(li.value)), await XS(A, g);
    }
    function rL() {
      !Z.value || !cr.value || (O.value || (O.value = !0, x.value = x.value || "正在停止..."), cr.value.abort());
    }
    function sL() {
      Jd();
    }
    async function oL() {
      it.value = Qg();
      const g = await oM();
      it.value = Qg(), g || (we.value = `记忆检索准备失败：${it.value.error || "memory_tokenizer_failed"}`);
    }
    async function Jd(g = {}) {
      if (Z.value) {
        rL();
        return;
      }
      zm();
      let A = String(g.messageText ?? T.value ?? "").trim();
      if (!A) {
        L.value = "先写一句话。", su("先写一句话。");
        return;
      }
      try {
        A = await JD(A, g);
      } catch (K) {
        const ne = Ta(K);
        L.value = ne, su(ne);
        return;
      }
      if (!A) return;
      const $ = new AbortController();
      cr.value = $, Z.value = !0, O.value = !1, L.value = "", zm(), x.value = "", P.value = [], mt.value = [], z.value = "", V.value = "", bo.value = !0, sg();
      try {
        N.value || await DS();
        const K = await tg(N.value);
        if ($.signal.aborted) {
          zd();
          return;
        }
        const ne = await b7({
          sessionId: N.value,
          agentConfig: r.value,
          contextSnapshot: K,
          chatPreset: Bn.value,
          assistantPreset: ya.value,
          currentUserMessage: A,
          runtimeState: nr(ye.value?.state || {}),
          diagnostics: n.value,
          historyMode: I.value,
          signal: $.signal,
          reuseUserMessageOrder: g.reuseUserMessageOrder,
          rerollRuntimeEvents: g.rerollRuntimeEvents,
          runManager: !0,
          applyRegex: ou,
          applySubstituteParams: Ud,
          getNativeWorldInfoRuntime: eg,
          onStreamProgress: (he) => {
            typeof he.text == "string" && (x.value = he.text), Array.isArray(he.thoughts) && (P.value = _i(he.thoughts)), Array.isArray(he.liveActionCheckEvents) && (mt.value = he.liveActionCheckEvents.map((Ee) => ({ ...Ee })));
          },
          onUserMessageSaved: async (he, Ee) => {
            N.value = he, await uf(he);
            const ze = H.value.findIndex((Me) => Me.sessionId === Ee.sessionId && Me.order === Ee.order);
            H.value = ze >= 0 ? H.value.map((Me, He) => He === ze ? Ee : Me) : [...H.value, Ee].sort((Me, He) => Me.order - He.order), T.value = "", Jt(() => fg(ai.value)), await yi(), mr(!0);
          },
          onAssistantMessageSaved: async (he, Ee) => {
            N.value = he;
            const ze = H.value.findIndex((Me) => Me.sessionId === Ee.sessionId && Me.order === Ee.order);
            H.value = ze >= 0 ? H.value.map((Me, He) => He === ze ? Ee : Me) : [...H.value, Ee].sort((Me, He) => Me.order - He.order), zd(), await yi(), mr();
          },
          onManagerRunSaved: async (he) => {
            await Qn(he);
          }
        });
        N.value = ne.sessionId, zd(), L.value = ne.error || "", z.value = ne.provider || "", V.value = ne.model || "", await yi(), await Qn(ne.sessionId), mr();
      } catch (K) {
        console.error("[小白酒馆] turn failed", K), zd(), L.value = K instanceof Error ? K.message : String(K || "run_failed");
      } finally {
        cr.value === $ && (cr.value = null), O.value = !1, Z.value = !1, mr();
      }
    }
    return yt([
      () => F.value.length,
      () => Sa.value.startIndex,
      () => d$.value,
      () => x.value,
      () => h$.value,
      () => p$.value,
      () => oe.value,
      () => ct.value
    ], () => {
      oe.value === "chat" && ct.value === "chat" && (mr(), Jt(() => {
        fi(), mg();
      }));
    }), yt([
      () => g$.value.length,
      () => Vm.value.startIndex,
      () => v$.value,
      () => y$.value,
      () => tt.value,
      () => oe.value,
      () => ct.value
    ], () => {
      oe.value === "chat" && ct.value === "manager" && (gg(), Jt(() => {
        Cd(), KS();
      }));
    }), yt([() => oe.value, () => ct.value], () => {
      oe.value === "chat" && ct.value === "chat" && mr(!0);
    }), yt(() => N.value, () => {
      sg(), og(), bo.value = !0, Fn.value = !0, an.resetPositionState(), ln.resetPositionState(), Ut.value = "", Mt.value = {}, qt.value = sI, Jt(() => {
        mr(!0), gg(!0);
      });
    }), yt([() => Fd.value.length, () => Hm.value], ([g, A]) => {
      Wl.value === "sessions" && g && !A && (Wl.value = "memory");
    }), yt(L, (g) => {
      g && su(g);
    }), yt(me, (g) => {
      if (!g) return;
      const A = g.split(":").slice(2).join(":").trim();
      A && su(A);
    }), yt(nu, async (g) => {
      const A = String(g?.path || "");
      if (!A) {
        if (ru.value) {
          _e.value = "当前档案已变化，草稿仍保留";
          return;
        }
        ag(), OS(null);
        return;
      }
      if (ue.value === A && ru.value) {
        je.value?.sessionId !== N.value && (je.value = null), _e.value = "档案已刷新，当前草稿仍保留";
        return;
      }
      const $ = await lD(A);
      String(Qe.value || "") === A && OS($);
    }, { immediate: !0 }), yt(It, (g) => {
      try {
        globalThis.localStorage?.setItem(lI, g ? "dark" : "light");
      } catch {
      }
    }), qI(NN, {
      shell: {
        activeView: oe,
        chatFocus: ct,
        homeThemeDark: It,
        openPromptInspector: nD,
        postToHost: gs,
        rememberBrokenAvatar: $d,
        shortText: ig
      },
      chat: {
        actionFeedback: vD,
        cancelEditMessage: vs,
        canDrawMessage: cg,
        canEditMessage: lg,
        canRerunMessage: FS,
        canSendMessage: _$,
        CHAT_SIDEBAR_BATCH_SIZE: Vie,
        chatAutoScroll: bo,
        chatFocus: ct,
        chatLayout: ar,
        chatMessages: vi,
        chatMessageWindow: Sa,
        chatComposeTextareaRef: ai,
        chatScrollControlsActive: wm,
        chatScrollRef: Sd,
        chatSidebarSessionLimit: qt,
        chatSidebarSessions: Od,
        chatSidePanel: Wl,
        chatSubtitle: mS,
        copyMessage: wD,
        currentUserMessage: T,
        deleteMessageTurn: PD,
        displayMessageContent: ES,
        displayMessageRenderProjection: M$,
        displayMessageThoughtBlocks: P$,
        displayRuntimeContent: R$,
        displayRuntimeRenderProjection: CS,
        displayRuntimeThoughtBlocks: N$,
        displayCharacterName: Um,
        drawMessage: TD,
        drawMessageStatusClass: _D,
        drawMessageStatusText: GS,
        drawMessageTitle: bD,
        drawProgressText: pe,
        filteredChatSidebarSessionCount: Hm,
        formatMessageTime: gD,
        handleChatScroll: LD,
        handleChatSubmit: sL,
        handleChatTouchMove: HD,
        handleChatTouchStart: UD,
        handleChatWheel: FD,
        handleComposeInput: xD,
        handleComposeKeydown: WD,
        hiddenChatSidebarSessionCount: t$,
        isDrawingMessage: Wd,
        isEditingMessage: US,
        isCancellingRun: O,
        isRunning: Z,
        latestErrorMessage: J,
        markdownSignature: Tn,
        messageKey: Un,
        normalizeTavernSessionState: nr,
        removeSession: eD,
        renderChatMarkdown: hi,
        rerunFromMessage: hg,
        revealOlderChatMessages: K$,
        roleLabel: Gd,
        runtimeText: x,
        runtimeThoughts: P,
        runtimeActionCheckEvents: mt,
        saveEditMessage: kD,
        scrollChatToBottom: mr,
        scrollChatToTop: $D,
        selectedSessionId: N,
        selectSession: j$,
        sessionDisplayTitle: jl,
        sessionFloorLabel: lS,
        sessions: B,
        showChatScrollBottom: bm,
        showChatScrollTop: ha,
        startEditMessage: ED,
        thoughtBlocks: _i,
        thoughtSummaryLabel: mD,
        updateChatScrollButtons: mg,
        visibleCharacterAvatar: Nm,
        visibleChatMessages: F,
        visibleUserAvatar: Lm
      },
      manager: {
        activeMemoryFiles: Fd,
        archivedManagerRuns: Te,
        canEditManagerMessage: ug,
        canRerunManagerMessage: BS,
        canSendManagerMessage: b$,
        copyManagerMessage: SD,
        currentManagerWorkRun: ot,
        deleteManagerMessageTurn: ND,
        editingMessageDraft: ui,
        formatRunActivityLine: vt,
        formatRunIssueLine: Dt,
        formatRunInputLine: $t,
        formatRunMapLine: qm,
        formatRunMemoryLine: WN,
        formatRunModelLine: VN,
        handleEditInput: ID,
        handleManagerComposeKeydown: VD,
        handleManagerEditKeydown: AD,
        handleManagerScroll: OD,
        handleManagerSubmit: QS,
        handleManagerTouchMove: GD,
        handleManagerTouchStart: qD,
        handleManagerWheel: BD,
        hiddenManagerRunCount: KN,
        isEditingManagerMessage: qS,
        isEditingManagerMessageDirty: HS,
        isManagerRunRetrying: pD,
        isManagerAssistantCancelling: nt,
        isManagerAssistantRunning: tt,
        liveManagerChatDisplayItems: pS,
        managerActionFeedback: yD,
        managerAutoScroll: Fn,
        managerBusy: iS,
        managerCompactionOverlay: ft,
        managerComposeTextareaRef: li,
        managerInputDraft: Re,
        managerInputStatus: $e,
        managerMessageWindow: Vm,
        managerRuns: Ie,
        managerRunTone: JN,
        managerScrollControlsActive: Em,
        managerScrollRef: fa,
        managerStatusLabel: XN,
        managerToolStatusLabel: QN,
        managerToolTone: ZN,
        managerToolTraceItems: jN,
        managerToolTurnPreview: k7,
        managerToolTurnSummary: x7,
        memoryFileDisplayName: Gm,
        memoryFiles: De,
        memoryIndexStatusLine: c$,
        retryManagerRun: hD,
        revealOlderManagerMessages: z$,
        rerunFromManagerMessage: pg,
        saveEditManagerMessage: VS,
        scrollManagerToBottom: gg,
        scrollManagerToTop: DD,
        selectedMemoryFile: uS,
        showManagerScrollBottom: Tm,
        showManagerScrollTop: Sm,
        startEditManagerMessage: CD,
        toolTraceSummary: e$,
        updateManagerScrollButtons: KS,
        visibleManagerChatItems: Km
      },
      memory: {
        activeMemoryFiles: Fd,
        discardMemoryDraft: oD,
        enterMemoryEditMode: iD,
        expandMemoryFileGroup: a$,
        formatMemoryFileMeta: aD,
        markdownSignature: Tn,
        MEMORY_FILE_BATCH_SIZE: Rv,
        MEMORY_TURN_BATCH_SIZE: oI,
        memoryDirectoryGroups: l$,
        memoryEditorDirty: ru,
        memoryEditorDocumentAvailable: hS,
        memoryEditorDraft: ee,
        memoryEditorLoadedPath: ue,
        memoryEditorMode: be,
        memoryEditorReadOnly: u$,
        memoryEditorStatus: _e,
        memoryFileDisplayName: Gm,
        memoryFileKindLabel: cS,
        memoryFiles: De,
        memoryFileSearchText: Ut,
        memoryFileStatusLabel: uD,
        previewMemoryDraft: cD,
        renderChatMarkdown: hi,
        saveSelectedMemoryFile: dD,
        selectedMemoryFileEntry: nu,
        selectedMemoryFile: uS,
        selectedMemoryFilePath: Qe,
        selectMemoryFile: fD,
        stateMemoryFile: W
      },
      workspace: {
        activeMemoryFiles: Fd,
        chatWorkspacePanel: Pe,
        mapStateDocument: xe,
        mapStatePatches: ke,
        saveSessionContract: W$,
        selectedSessionId: N,
        sessionContract: Ze,
        stateMemoryFile: W
      },
      settings: kd
    }), zI(async () => {
      window.addEventListener("message", xS), So = window.setInterval(() => {
        V$();
      }, 2e3), oe.value === "settings" && Ye.value === "api" && Jt(Xl), oL(), await xd(), await yi(), Md(), oe.value === "settings" && Ye.value === "api" && await Jt(Xl), oe.value === "settings" && Ye.value === "chatPreset" && Pd(), oe.value === "settings" && Ye.value === "worldbooks" && Rd(), oe.value === "settings" && Ye.value === "regex" && Mm(), oe.value === "settings" && Ye.value === "base" && xm(), gs("xb-tavern:frame-ready"), N.value && qd({
        sessionId: N.value,
        force: !0
      }), oe.value === "chat" && ct.value === "chat" && mr(!0);
    }), j_(() => {
      window.removeEventListener("message", xS), G0(null), Qt.forEach((g) => {
        window.clearTimeout(g.timer), g.abort && g.signal && g.signal.removeEventListener("abort", g.abort), g.reject(/* @__PURE__ */ new Error("tavern_unmounted"));
      }), Qt.clear(), cr.value?.abort(), dr.value?.abort(), mn.value?.abort(), an.cleanup(), ln.cleanup(), fr && (window.clearTimeout(fr), fr = null), So && (window.clearInterval(So), So = null), yS(), zS(), au();
    }), (g, A) => (U(), q("main", {
      class: Fe(["xb-tavern xb-os-shell", {
        "is-home-view": oe.value === "home" || oe.value === "about",
        "theme-dark": It.value,
        "theme-light": !It.value
      }]),
      style: Ir(Ed.value)
    }, [
      _("section", Fie, [
        oe.value === "home" ? (U(), Zs(sQ, {
          key: 0,
          dark: It.value,
          "has-session": Ne.value,
          subtitle: mS.value,
          "character-count": qs.value.length,
          onToggleTheme: A[0] || (A[0] = ($) => It.value = !It.value),
          onExit: A[1] || (A[1] = ($) => gs("xb-tavern:close")),
          onEnter: Q$,
          onOpenCharacters: kS,
          onOpenSettings: S(km),
          onOpenAbout: A[2] || (A[2] = ($) => oe.value = "about")
        }, null, 8, [
          "dark",
          "has-session",
          "subtitle",
          "character-count",
          "onOpenSettings"
        ])) : ve("", !0),
        oe.value === "about" ? (U(), Zs(O7, {
          key: 1,
          dark: It.value,
          onToggleTheme: A[3] || (A[3] = ($) => It.value = !It.value),
          onBack: A[4] || (A[4] = ($) => oe.value = "home")
        }, null, 8, ["dark"])) : ve("", !0),
        oe.value === "characters" ? (U(), Zs(zX, {
          key: 2,
          ref_key: "characterArchivePageRef",
          ref: da,
          "search-text": Pt.value,
          "onUpdate:searchText": A[5] || (A[5] = ($) => Pt.value = $),
          dark: It.value,
          "pending-error": b.value,
          characters: qs.value,
          "visible-characters": ba.value,
          "filtered-count": gi.value.length,
          "live-character-id": Ql.value,
          "selected-character": wa.value,
          "selected-greeting-index": y.value,
          "pending-preview-character-id": w.value,
          "pending-character-session-id": E.value,
          "character-worldbook-state": C.value,
          "character-worldbook-busy": M.value,
          "hidden-count": Fm.value,
          "batch-size": Hf,
          "avatar-available": Om,
          "short-text": ig,
          onToggleTheme: A[6] || (A[6] = ($) => It.value = !It.value),
          onBack: A[7] || (A[7] = ($) => oe.value = "home"),
          onRefresh: MS,
          onSelect: B$,
          onSelectGreeting: H$,
          onEnterSelected: NS,
          onOpenCharacterWorldbook: U$,
          onEnterCharacter: LS,
          onLoadMore: A[8] || (A[8] = ($) => At.value += Hf),
          onKeydown: G$,
          onAvatarError: $d
        }, null, 8, [
          "search-text",
          "dark",
          "pending-error",
          "characters",
          "visible-characters",
          "filtered-count",
          "live-character-id",
          "selected-character",
          "selected-greeting-index",
          "pending-preview-character-id",
          "pending-character-session-id",
          "character-worldbook-state",
          "character-worldbook-busy",
          "hidden-count"
        ])) : ve("", !0),
        oe.value === "chat" ? (U(), Zs(Jte, { key: 3 })) : ve("", !0),
        oe.value === "settings" ? (U(), Zs(mie, { key: 4 })) : ve("", !0)
      ]),
      pa.value ? (U(), Zs(hne, {
        key: 0,
        tab: Zr.value,
        "onUpdate:tab": A[9] || (A[9] = ($) => Zr.value = $),
        "simulate-input": Fs.value,
        "onUpdate:simulateInput": A[10] || (A[10] = ($) => Fs.value = $),
        "last-request-raw-json": tu.value,
        "last-request-snapshot": eu.value,
        "simulate-status": Bs.value,
        "simulate-error": di.value,
        "simulate-json": ci.value,
        onClose: rD,
        onSimulate: sD
      }, null, 8, [
        "tab",
        "simulate-input",
        "last-request-raw-json",
        "last-request-snapshot",
        "simulate-status",
        "simulate-error",
        "simulate-json"
      ])) : ve("", !0),
      R.value ? (U(), q("div", {
        key: 1,
        class: "character-worldbook-picker-overlay",
        onClick: Pn(PS, ["self"])
      }, [_("section", Bie, [
        _("header", null, [A[11] || (A[11] = _("strong", null, "选择角色世界书", -1)), _("button", {
          type: "button",
          class: "worldbook-picker-close",
          "aria-label": "关闭",
          onClick: PS
        }, " × ")]),
        k.value ? (U(), q("div", Uie, Q(k.value), 1)) : ve("", !0),
        _("div", qie, [(U(!0), q(Ge, null, ut(D.value, ($) => (U(), q("button", {
          key: $,
          type: "button",
          disabled: M.value,
          onClick: (K) => q$($)
        }, Q($), 9, Hie))), 128))])
      ])])) : ve("", !0)
    ], 6));
  }
}), Zie = Qie;
IF(Zie).mount("#app");
